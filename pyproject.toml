[project]
name = "fv-copilot"
version = "0.1.0"
description = "FV-Copilot vault sync — recursive directory-matching with per-file symlinks"
requires-python = ">=3.12"

[project.optional-dependencies]
dev = ["pytest>=8.0"]

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-v"
