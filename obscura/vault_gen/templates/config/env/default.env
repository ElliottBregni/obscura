# Default environment configuration for ${name}
# Source this file or reference it from your Obscura launcher.
# Do NOT commit secrets here — use a secrets manager.

VAULT_NAME=${name}
VAULT_TYPE=${type}

# Obscura runtime
# OBSCURA_LOG_LEVEL=info
# OBSCURA_MAX_WORKERS=4

# Model overrides (leave unset to use Obscura defaults)
# OBSCURA_DEFAULT_MODEL=claude-opus-4-6
