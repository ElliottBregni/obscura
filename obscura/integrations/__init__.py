"""
obscura.integrations — Protocol adapters and integration boundaries.

Sub-packages:
- mcp: Model Context Protocol (server, client, tools)
- a2a: Agent-to-Agent protocol (server, client, transports)
- messaging: Platform-agnostic messaging abstractions
- imessage: macOS iMessage integration
- whatsapp: WhatsApp via Twilio API
- signal: Signal via signal-cli REST bridge
- slack: Slack via Web API
- webhook: Generic outbound webhook channel
- push: Push notifications (APNs / FCM / Expo)
"""
