# MCP Configuration

This is a **copy** of your MCP config for reference/editing in Obsidian.

**Live config location**: `copilot-cli/mcp-config.json` (symlinked to `~/.copilot/mcp-config.json`)

**To apply changes made here:**
```bash
cp ~/FV-Copilot/mcp-config.json ~/.copilot/mcp-config.json
```

Or edit directly via:
- File: `~/.copilot/mcp-config.json`
- CLI: `/mcp` commands in Copilot CLI

**Current servers**: See `mcp-config.json` in this vault
