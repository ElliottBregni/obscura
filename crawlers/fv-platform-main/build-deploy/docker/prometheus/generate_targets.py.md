# Diagram: build-deploy/docker/prometheus/generate_targets.py


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
flowchart TD
    Start[Start] --> CheckEnv{AWS_REGION and MSK_CLUSTER_ARN set?}
    CheckEnv -- No --> EnvError[Raise EnvironmentError]
    EnvError --> Exit[Exit]
    CheckEnv -- Yes --> TryBlock[Try]
    TryBlock --> CreateClient[Create boto3 kafka client (region)]
    CreateClient --> ListNodes[client.list_nodes(ClusterArn)]
    ListNodes --> ForEach{for node in NodeInfoList}
    ForEach --> GetEndpoint[endpoint = node.BrokerNodeInfo.Endpoints[0]]
    GetEndpoint --> AddJMX[jmx_targets.append(endpoint + ":11001")]
    GetEndpoint --> AddNode[node_targets.append(endpoint + ":11002")]
    AddJMX --> ForEach
    AddNode --> ForEach
    ForEach --> PrintTargets[print(jmx_targets)]
    PrintTargets --> ComposeJobs[Create jmx_job and node_job dictionaries]
    ComposeJobs --> WriteFile[Write /etc/prometheus/targets.json]
    WriteFile --> Exit
    TryBlock -- exception --> Except[except Exception -> log.error(str(ex))]
    Except --> Exit
```

> SVG rendering failed for this diagram.
