# Diagram: database/freightverify/releases/release.NP-1016/NP-1016_delete_infra.sh


> Auto-generated by Obscura crawlers

## Diagram 1

```mermaid
flowchart TD
  Start((Start))
  CheckArgs{Are there at least 2 args?}
  Usage[/Print usage and exit/]
  SetProfile[Set AWS_PROFILE = $1; shift]
  DetectRegion{Next arg matches region regex?}
  SetRegion[Set AWS_REGION = $1; shift]
  DefaultRegion[Set AWS_REGION = us-east-1]
  SetPrefixes[Set PREFIXES = remaining args]
  Config[Configure RULE_SUFFIXES, LAMBDA_SUFFIX, AWS command]
  Info[Echo AWS Profile, Region, Prefixes]
  PrefixLoop[For each PREFIX in PREFIXES]
  SuffixLoop[For each SUFFIX in RULE_SUFFIXES]
  ComposeRule[RULE = PREFIX + "-" + SUFFIX]
  ListTargets[aws events list-targets-by-rule --rule $RULE → IDS]
  HasIds{IDS non-empty?}
  RemoveTargets[aws events remove-targets --rule $RULE --ids $IDS]
  NoTargets[Echo: No targets found (or rule missing)]
  DeleteRule[aws events delete-rule --name $RULE (or echo skip)]
  ComposeLambda[LAMBDA = PREFIX + "-" + LAMBDA_SUFFIX]
  DeleteLambda[aws lambda delete-function --function-name $LAMBDA (or echo skip)]
  End((Cleanup complete))

  Start --> CheckArgs
  CheckArgs -- No --> Usage
  Usage --> End
  CheckArgs -- Yes --> SetProfile
  SetProfile --> DetectRegion
  DetectRegion -- Yes --> SetRegion
  DetectRegion -- No --> DefaultRegion
  SetRegion --> SetPrefixes
  DefaultRegion --> SetPrefixes
  SetPrefixes --> Config
  Config --> Info
  Info --> PrefixLoop
  PrefixLoop --> SuffixLoop
  SuffixLoop --> ComposeRule
  ComposeRule --> ListTargets
  ListTargets --> HasIds
  HasIds -- Yes --> RemoveTargets
  HasIds -- No --> NoTargets
  RemoveTargets --> DeleteRule
  NoTargets --> DeleteRule
  DeleteRule --> SuffixLoop
  SuffixLoop --> ComposeLambda
  ComposeLambda --> DeleteLambda
  DeleteLambda --> PrefixLoop
  PrefixLoop --> End
```

> SVG rendering failed for this diagram.

## Diagram 2

```mermaid
sequenceDiagram
  participant User
  participant Script
  participant AWS
  User->>Script: invoke script with args
  Script->>Script: validate arg count (exit if < 2)
  Script->>Script: set AWS_PROFILE = $1; shift
  alt region arg matches pattern
    Script->>Script: set AWS_REGION = $1; shift
  else
    Script->>Script: set AWS_REGION = us-east-1
  end
  Script->>Script: set PREFIXES = remaining args
  loop For each PREFIX in PREFIXES
    loop For each SUFFIX in RULE_SUFFIXES
      Script->>AWS: events list-targets-by-rule(rule=PREFIX-SUFFIX)
      AWS-->>Script: return IDS (may be empty)
      alt IDS non-empty
        Script->>AWS: events remove-targets(rule=PREFIX-SUFFIX, ids=IDS)
        AWS-->>Script: targets removed
      else
        Script-->>Script: echo "No targets found (or rule missing)"
      end
      Script->>AWS: events delete-rule(name=PREFIX-SUFFIX)
      AWS-->>Script: rule deleted or not found
    end
    Script->>AWS: lambda delete-function(function-name=PREFIX-LAMBDA_SUFFIX)
    AWS-->>Script: lambda deleted or not found
  end
  Script-->>User: prints "Cleanup complete"
```

> SVG rendering failed for this diagram.
