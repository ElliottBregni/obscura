# Diagram: platform-java-lambdas/location/serverless.location.yml


> Auto-generated by Obscura crawlers

## Diagram 1

```mermaid
flowchart LR
  SVC[service: location-java]
  FRV[frameworkVersion: "3"]
  PRV[provider: aws<br/>runtime: java11<br/>timeout: 30<br/>memorySize: ${self:custom.globalConfig.memorySize}]
  SVC --> FRV
  SVC --> PRV
  STK[stackTags<br/>service: ${opt:stage}-${self:service}]
  PRV --> STK
  ENV[environment<br/>AWS_STAGE: ${opt:stage}<br/>LOG_LEVEL: ${self:custom.globalConfig.config.LOG_LEVEL, "INFO"}<br/>SERVICE: ${self:service}]
  PRV --> ENV
  VPC[vpc: ${self:custom.globalConfig.vpc}]
  PRV --> VPC
  IAM[iam.role.statements: []]
  PRV --> IAM
  PLG[plugins<br/>- serverless-prune-plugin]
  SVC --> PLG
  FNCS[functions]
  SVC --> FNCS
  LGET[locations_get_v2<br/>name: ${opt:stage}-get-locations-v2<br/>handler: com.freightverify.location.lambda.LocationsGetLambda<br/>snapStart: true]
  FNCS --> LGET
  PKG[package<br/>artifact: target/location-1.0-SNAPSHOT.jar<br/>patterns:<br/>!.serverless*/**<br/>!.cache/**<br/>!.serverless/**<br/>!node_modules/**]
  SVC --> PKG
  CST[custom<br/>prune: ${self:custom.globalConfig.prune}<br/>globalConfig: ${file(${env:FV_PATH}/config.global.yml)}]
  SVC --> CST
```

> SVG rendering failed for this diagram.

## Diagram 2

```mermaid
classDiagram
  class ServerlessConfig {
    +service : location-java
    +frameworkVersion : "3"
    +plugins : serverless-prune-plugin
    +artifact : target/location-1.0-SNAPSHOT.jar
    +custom.globalConfig : ${file(${env:FV_PATH}/config.global.yml)}
  }
  class Provider {
    +name : aws
    +runtime : java11
    +timeout : 30
    +memorySize : ${self:custom.globalConfig.memorySize}
    +vpc : ${self:custom.globalConfig.vpc}
    +iam.role.statements : []
    +stackTags.service : ${opt:stage}-${self:service}
    +environment.AWS_STAGE : ${opt:stage}
    +environment.LOG_LEVEL : ${self:custom.globalConfig.config.LOG_LEVEL, "INFO"}
    +environment.SERVICE : ${self:service}
  }
  class Function_locations_get_v2 {
    +snapStart : true
    +name : ${opt:stage}-get-locations-v2
    +handler : com.freightverify.location.lambda.LocationsGetLambda
  }
  ServerlessConfig --> Provider
  ServerlessConfig --> Function_locations_get_v2
  Provider --> Function_locations_get_v2
```

> SVG rendering failed for this diagram.
