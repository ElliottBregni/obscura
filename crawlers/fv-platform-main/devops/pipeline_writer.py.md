# Diagram: devops/pipeline_writer.py


> Auto-generated by Obscura crawlers

## Diagram 1

```mermaid
classDiagram
    class PipelineWriter {
        - type: str
        - event_type: str
        - project_type: str
        - environments: list(tuple)
        + __init__(type, event_type, project_type="standard")
        + _get_environments(): list(tuple)
        + _get_account_id(environment): str
        + _format_environment(environment): str
        + _generate_child_pipeline(manual=false) <<abstract>>
        + _generate_empty_pipeline(): str
        + write_downstream(file, manual=false)
    }
    class ACCOUNT_MAP <<constant>> {
        + dev: {environments: [dev, dev2, qa, qa2], id: DEV_FV_ACCOUNT_ID}
        + staging: {environments: [staging1], id: STAGING_FV_ACCOUNT_ID}
        + prod: {environments: [test, prod-a, prod-b], id: PROD_FV_ACCOUNT_ID}
    }
    PipelineWriter ..> ACCOUNT_MAP : reads
```

> SVG rendering failed for this diagram.

## Diagram 2

```mermaid
flowchart TD
    Start([_get_environments()])
    GetEvent[Retrieve event_object from environments_map using type/project_type/event_type]
    CheckEvent{event_object present?}
    RaiseEmpty[/Raise ValueError: Empty event object returned/]
    GetEnvs[envs = event_object.get("envs", [dev, dev2])]
    GetRegions[regions = event_object.get("regions", [us-east-1])]
    BuildOut[Build out = [(env, region) for env in envs for region in regions]]
    CheckOut{out non-empty?}
    RaiseNoEnvs[/Raise ValueError: No environments were retrieved/]
    ReturnOut([return out])

    Start --> GetEvent
    GetEvent --> CheckEvent
    CheckEvent -- No --> RaiseEmpty
    CheckEvent -- Yes --> GetEnvs
    GetEnvs --> GetRegions
    GetRegions --> BuildOut
    BuildOut --> CheckOut
    CheckOut -- No --> RaiseNoEnvs
    CheckOut -- Yes --> ReturnOut
```

> SVG rendering failed for this diagram.
