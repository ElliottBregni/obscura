# Diagram: research/serverless.ml_eta.yml


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
flowchart TD
  Service[service: ml-eta]
  Framework[frameworkVersion: "3"]
  Service --> Framework
  Provider[provider: aws]
  Service --> Provider
  Runtime[runtime: python3.13<br/>architecture: arm64<br/>timeout: 300]
  Provider --> Runtime
  Layers[layers]
  Provider --> Layers
  L1[FvLambdaLayerQualifiedArn]
  L2[PythonRequirementsLambdaLayerQualifiedArn]
  L3[AWSOpenTelemetryDistroPython:12]
  Layers --> L1
  Layers --> L2
  Layers --> L3
  IAM[iam.role.tags]
  Provider --> IAM
  StackTags[stackTags / tags]
  Provider --> StackTags
  Environment[provider.environment]
  Provider --> Environment
  EV_AWS_STAGE[AWS_STAGE]
  EV_ACCOUNT_ID[ACCOUNT_ID]
  EV_EXEC_WRAPPER[AWS_LAMBDA_EXEC_WRAPPER]
  EV_SERVICE[SERVICE: ml-eta]
  EV_SPEC_ENV[SPECIFIED_ENV]
  EV_REDIS[REDIS_HOST]
  EV_METRICS[POWERTOOLS_METRICS_NAMESPACE]
  Environment --> EV_AWS_STAGE
  Environment --> EV_ACCOUNT_ID
  Environment --> EV_EXEC_WRAPPER
  Environment --> EV_SERVICE
  Environment --> EV_SPEC_ENV
  Environment --> EV_REDIS
  Environment --> EV_METRICS
  VPC[vpc]
  Provider --> VPC
  Plugins[plugins]
  Service --> Plugins
  P1[serverless-python-requirements]
  P2[serverless-prune-plugin]
  P3[serverless-logging-config]
  Plugins --> P1
  Plugins --> P2
  Plugins --> P3
  Functions[functions]
  Service --> Functions
  GetML[get_ml_eta<br/>handler: get_ml_eta.lambda_handler<br/>name: ${opt:stage}-get_ml_eta<br/>description: Get ML ETA]
  Functions --> GetML
  Package[package.patterns]
  Service --> Package
  Custom[custom]
  Service --> Custom
  C_CORS[cors: origin, headers, allowCredentials]
  C_LOG[serverless-logging-config: enableJson, logGroupName, applicationLogLevel]
  C_PRUNE[prune, base, authorizer, pythonRequirements, customDomain, globalConfig]
  Custom --> C_CORS
  Custom --> C_LOG
  Custom --> C_PRUNE
  Package --> Excludes["many !patterns (exclusions)"]
```

> SVG rendering failed for this diagram.
