# Diagram: research/orchestrator/scripts/check_redis_model_updated.py


> Auto-generated by Obscura crawlers

## Diagram 1

```mermaid
flowchart TD
    Start((Start))
    ReadEnv[Read REDIS_HOST or default]
    Connect[Instantiate redis.Redis(host=h, db=1)]
    Scan[Scan keys: r.scan_iter("*")]
    GetVal[Get key value: r.get(key)]
    Parse{json.loads(redis_value.decode()) successful?}
    ParseErr[Print parse error and continue]
    Extract[Extract model_date -> model_date_str and model_name]
    CounterExists{model_name in model_to_observations?}
    CreateCounter[Create Counter and increment date count]
    Increment[Increment Counter[model_name][model_date_str]]
    CountCheck{count > sample_size?}
    Summary[Print SUMMARY and header]
    Sort[Sort model_to_observations by model_name]
    PrintDetails[For each model: print most_common_date and samples]
    End((End))

    Start --> ReadEnv --> Connect --> Scan --> GetVal
    GetVal --> Parse
    Parse --|no|--> ParseErr --> Scan
    Parse --|yes|--> Extract --> CounterExists
    CounterExists --|no|--> CreateCounter --> Increment
    CounterExists --|yes|--> Increment
    Increment --> CountCheck
    CountCheck --|no|--> Scan
    CountCheck --|yes|--> Summary --> Sort --> PrintDetails --> End
```

> SVG rendering failed for this diagram.

## Diagram 2

```mermaid
sequenceDiagram
    participant Script
    participant OS
    participant Redis
    participant JSON
    participant Counter
    participant Stdout

    Script->>OS: getenv('REDIS_HOST', default)
    OS-->>Script: host string
    Script->>Redis: redis.Redis(host=h, db=1)
    Redis-->>Script: connected
    loop Iterate keys from r.scan_iter("*")
        Script->>Redis: get(key)
        Redis-->>Script: redis_value (bytes)
        Script->>JSON: json.loads(redis_value.decode())
        alt parse success
            JSON-->>Script: dict
            Script->>Script: extract model_date and model_name
            Script->>Counter: increment Counter[model_name][model_date_str]
            Script->>Script: count += 1; check sample_size
        else parse failure
            JSON-->>Script: error (Exception)
            Script->>Stdout: print parse error and continue
        end
    end
    Script->>Stdout: print SUMMARY and header
    Script->>Script: sort model_to_observations
    Script->>Stdout: print most_common_date and samples per model
```

> SVG rendering failed for this diagram.
