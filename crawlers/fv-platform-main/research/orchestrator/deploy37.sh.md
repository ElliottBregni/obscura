# Diagram: research/orchestrator/deploy37.sh


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
flowchart TD
    Start([Start]) --> CheckFV{FV_HOME set?}
    CheckFV -- No --> FVFail[/"Echo 'FV_HOME is not set' and exit 1"/]
    CheckFV -- Yes --> CD_HOME[/cd $FV_HOME/]
    CD_HOME --> GetBranch[Get current git branch (BRANCH)]
    GetBranch --> GitPull[git pull origin BRANCH]
    GitPull --> CD_CONFIG[/cd research/orchestrator/configuration/]
    CD_CONFIG --> UpdateCron[Update crontab from orchestrator_cron]
    UpdateCron --> CheckEnv{/home/ec2-user/orchestrator_env exists?}
    CheckEnv -- No --> EnvFail[/"Print missing orchestrator_env and exit 1"/]
    CheckEnv -- Yes --> UpdateVenv[Start virtual environment update]
    UpdateVenv --> CheckProjectRoot{PROJECT_ROOT set?}
    CheckProjectRoot -- Yes --> CheckEnv37PR{env37 exists in PROJECT_ROOT/research/orchestrator?}
    CheckEnv37PR -- Yes --> PipInstallPR[Run PROJECT_ROOT/.../env37/bin/pip install -r requirements-37.txt]
    CheckEnv37PR -- No --> SkipPipPR([Skip pip install for PROJECT_ROOT])
    CheckProjectRoot -- No --> CheckEnv37Home{env37 exists in /home/ec2-user/FV-Platform-Main/research/orchestrator?}
    CheckEnv37Home -- Yes --> PipInstallHome[Run /home/.../env37/bin/pip install -r requirements-37.txt]
    CheckEnv37Home -- No --> SkipPipHome([Skip pip install for home env37])
    PipInstallPR --> ZipStart
    SkipPipPR --> ZipStart
    PipInstallHome --> ZipStart
    SkipPipHome --> ZipStart
    ZipStart --> CheckZipProjRoot{PROJECT_ROOT set?}
    CheckZipProjRoot -- No --> SkipZips([Skip zipping graphreduce and shapely])
    CheckZipProjRoot -- Yes --> CheckGraph{graphreduce dir exists in PROJECT_ROOT env37 site-packages?}
    CheckGraph -- Yes --> ZipGraph[zip graphreduce -> $PROJECT_ROOT/graphreduce.zip]
    CheckGraph -- No --> SkipGraph([Skip graphreduce zip])
    ZipGraph --> CheckShapely{shapely dir exists in PROJECT_ROOT env37 lib64 site-packages?}
    SkipGraph --> CheckShapely
    CheckShapely -- Yes --> ZipShapely[zip shapely -> $PROJECT_ROOT/shapely.zip]
    CheckShapely -- No --> SkipShapely([Skip shapely zip])
    CheckShapely --> UpdateServices
    SkipShapely --> UpdateServices
    UpdateServices --> CopyService[cp luigi.service -> /etc/systemd/system/]
    CopyService --> DaemonReload[sudo systemctl daemon-reload]
    DaemonReload --> StopService[sudo systemctl stop luigi.service]
    StopService --> StartService[sudo systemctl start luigi.service]
    StartService --> StatusService[sudo systemctl status luigi.service]
    StatusService --> Chown[sudo chown -R ec2-user:ec2-user /home/ec2-user/FV-Platform-Main]
    Chown --> End([End])
```

> SVG rendering failed for this diagram.
