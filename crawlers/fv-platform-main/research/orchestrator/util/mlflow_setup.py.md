# Diagram: research/orchestrator/util/mlflow_setup.py


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
flowchart TD
    ST[set mlflow tracking URI = databricks]
    ST --> INIT[initialize_mlflow(experiment_name)]
    INIT --> CHECK[mlflow.get_experiment_by_name(/Shared/mlflow-landing/NAME)]
    CHECK -->|not found| CREATE[mlflow.create_experiment(name) returns experiment_id]
    CHECK -->|found| SETEXP[mlflow.set_experiment(name)]
    CREATE --> SETEXP
    SETEXP --> AUTO[mlflow.sklearn.autolog()]

    CREATE --> SET_EXP_PERMS[set_permissions_on_experiment(experiment_id)]
    SET_EXP_PERMS --> EXP_PUT[PUT /api/2.0/permissions/experiments/(experiment_id)]
    EXP_PUT -->|uses| CFG[databricks_config: host, token, group_name]
    EXP_PUT -->|200| LOG_EXP_OK[logger.info: permissions set]
    EXP_PUT -->|error| LOG_EXP_ERR[logger.error: failed to set permissions]

    SET_MODEL_PERMS[set_permissions_on_registered_model(model_name)] --> GET_MODEL_ID[get_model_id_by_name(model_name)]
    GET_MODEL_ID --> GET_REQ[GET /api/2.0/mlflow/databricks/registered-models/get?name=...]
    GET_REQ -->|uses| CFG
    GET_REQ -->|200| GET_RESP[response.json() -> registered_model_databricks.id]
    GET_RESP --> SET_MODEL_PERMS
    SET_MODEL_PERMS --> MODEL_PUT[PUT /api/2.0/permissions/registered-models/(registered_model_id)]
    MODEL_PUT -->|uses| CFG
    MODEL_PUT -->|200| LOG_MODEL_OK[logger.info: permissions set]
    MODEL_PUT -->|error| LOG_MODEL_ERR[logger.error: failed to set permissions]
```

> SVG rendering failed for this diagram.
