# Diagram: research/orchestrator/util/helpers.py


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
flowchart TD
  A([Input: versioned_path])
  A --> B[get_latest_path(versioned_path)]
  B --> C[split path by slash]
  C --> D[remove element at index -2]
  D --> E[join remaining parts with slash]
  E --> F([Output: cleaned_path])
```

> SVG rendering failed for this diagram.
