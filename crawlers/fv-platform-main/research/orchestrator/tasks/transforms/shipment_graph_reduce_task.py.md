# Diagram: research/orchestrator/tasks/transforms/shipment_graph_reduce_task.py


> Auto-generated by Obscura crawlers

## Diagram 1

```mermaid
classDiagram
    class ShipmentGraphReduceTask {
        +week : luigi.DateIntervalParameter
        +date : luigi.DateParameter
        +name = shipment_feature_transform_task
        +app = tasks/transforms/shipment_graph_reduce_spark.py
        +master = yarn
        +deploy_mode = client
        +driver_memory = 14g
        +executor_memory = 20g
        +executor_cores = 1
        +num_executors = 98
        +py_files : list
        +app_options()
        +requires()
        +output()
    }
    class EMRSparkTask
    class ETLShipmentsTask
    class datanectar
    class os
    ShipmentGraphReduceTask --|> EMRSparkTask
    ShipmentGraphReduceTask ..> ETLShipmentsTask : requires
    ShipmentGraphReduceTask ..> datanectar : output()
    ShipmentGraphReduceTask ..> os : getenv()
    datanectar ..> "luigi.OutputTarget" : returns
```

> SVG rendering failed for this diagram.

## Diagram 2

```mermaid
flowchart LR
    SGRT[ShipmentGraphReduceTask]
    EMR[EMRSparkTask (base)]
    ETL[ETLShipmentsTask]
    DN[get_luigi_output_target()]
    OS[os.getenv('PROJECT_ROOT')]
    APP[tasks/transforms/shipment_graph_reduce_spark.py]
    ZIP[graphreduce.zip]
    FEATURES[research/orchestrator/feature_repo/shipments/shipment_features.py]
    PARQ[ROOT/shipment_feature_transform_task.parquet]
    SGRT -->|inherits| EMR
    SGRT -->|requires(week)| ETL
    SGRT -->|calls output()| DN
    SGRT -->|uses app| APP
    SGRT -->|reads py_files from| OS
    OS -->|provides path| FEATURES
    OS -->|provides path| ZIP
    DN -->|writes| PARQ
```

> SVG rendering failed for this diagram.
