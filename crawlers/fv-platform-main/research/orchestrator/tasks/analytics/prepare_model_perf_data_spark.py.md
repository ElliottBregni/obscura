# Diagram: research/orchestrator/tasks/analytics/prepare_model_perf_data_spark.py


> Auto-generated by Obscura crawlers

## Diagram 1

```mermaid
flowchart TD
    Args[Parse args: input_s3_path, output_s3_path, env] --> Main[main() → process()]
    Main --> Init[Initialize SparkContext & SQLContext]
    Init --> ReadDF[Read events parquet (s3_input_path) → df]
    ReadDF --> Select[Select columns from df]
    Select --> DayOfWeek[Add evt_dayofweek column]
    DayOfWeek --> ReadEnt[Read ENTITY parquet - filter trip_plan_complete_ts - rename ent_*]
    DayOfWeek --> ReadShip[Read SHIPMENT parquet - filter delivered_at - rename ship_*]
    DayOfWeek --> ReadPart[Read PARTVIEW parquet - filter destination_arrived_ts - rename part_*]
    ReadDF --> JoinEnt[Join df + ent on key1 == ent_external_id (left)]
    ReadEnt --> JoinEnt
    JoinEnt --> JoinShip[Join joined + ship on key1 == ship_creator_shipment_id (left)]
    ReadShip --> JoinShip
    JoinShip --> JoinPart[Join joined + partview on package_container_id == part_id (left)]
    ReadPart --> JoinPart
    JoinPart --> ComputeArr[Compute arrived_date using eta_source (ENTITY/SHIPMENT/PARTVIEW)]
    ComputeArr --> InterWrite[Write intermediate parquet → inter_s3_path]
    InterWrite --> InterRead[Read intermediate parquet → joined]
    InterRead --> ParseDates[udf_to_date: parse etaDate, departure_date, pts_interest_date, event_ts]
    ParseDates --> ComputeDiffs[Compute eta_diff_seconds and eta_diff_minutes]
    ComputeDiffs --> ComputePick[Compute departure_dt, pts_interest_dt, event_dt]
    ComputePick --> PickEvent[fpick → eta_event_date (pick_event_date)]
    PickEvent --> EventToArrival[Compute event_to_arrival_minutes]
    EventToArrival --> ComputeWeight[Compute eta_diff_weight]
    ComputeWeight --> ArrivedDt[Add arrived_dt and arrived_week]
    ArrivedDt --> ComputeWindow[Compute eta_compute_window bucket]
    ComputeWindow --> Filter[Filter rows: event_dt < arrived_date OR arrived_date IS NULL]
    Filter --> WriteOut[Write final parquet to s3_output_path]
    WriteOut --> End[Done]
```

> SVG rendering failed for this diagram.

## Diagram 2

```mermaid
erDiagram
    EVENTS {
      string id
      string source
      string key1
      string package_container_id
      datetime event_ts
      string etaDate
      string eta_source
    }
    ENT {
      string ent_id
      string ent_external_id
      datetime ent_trip_plan_complete_ts
    }
    SHIP {
      string ship_id
      string ship_creator_shipment_id
      datetime ship_delivered_at
    }
    PART {
      string part_id
      datetime part_destination_arrived_ts
    }
    EVENTS ||--o{ ENT : key1 == ent_external_id
    EVENTS ||--o{ SHIP : key1 == ship_creator_shipment_id
    EVENTS ||--o{ PART : package_container_id == part_id
```

> SVG rendering failed for this diagram.
