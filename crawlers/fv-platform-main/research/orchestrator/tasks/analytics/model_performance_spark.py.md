# Diagram: research/orchestrator/tasks/analytics/model_performance_spark.py


> Auto-generated by Obscura crawlers

## Diagram 1

```mermaid
flowchart TD
  Start([Start])
  ParseArgs[/Parse CLI args: s3_input_path, s3_output_path, env, models/]
  InitSpark[/Init SparkContext & SQLContext/]
  ReadParquet["sqlCtx.read.parquet(s3_input_path)"]
  AddArrivedWeek["Add arrived_week via UDF"]
  FilterNow{Filter arrived_date < now}
  ComputeBase["Compute base_path from s3_output_path"]
  SourcesLoop{Loop over sources (from models)}
  SourceFilter["source_df = df.filter(df['source'] == sourcename.upper())"]
  GenByWeek["generate_moments_by_week(source_df)"]
  WriteSource["write.parquet(source_path)"]
  ModelTypeLoop{Loop over model_type per source}
  ModelTypeFilter["model_type_df = source_df.filter(model_name.startswith(model_type))"]
  GenByWeekModelType["generate_moments_by_week(model_type_df)"]
  WriteModelType["write.parquet(model_type_path)"]
  ModelLoop{Loop over models for model_type}
  ModelFilter["model_df = model_type_df.filter(model_name == model)"]
  GenByWeekModel["generate_moments_by_week(model_df)"]
  WriteModel["write.parquet(model_path)"]
  FinalWrite["joined.write.parquet(s3_output_path)"]
  End([End])

  Start --> ParseArgs --> InitSpark --> ReadParquet --> AddArrivedWeek --> FilterNow --> ComputeBase --> SourcesLoop
  SourcesLoop --> SourceFilter --> GenByWeek --> WriteSource --> ModelTypeLoop
  ModelTypeLoop --> ModelTypeFilter --> GenByWeekModelType --> WriteModelType --> ModelLoop
  ModelLoop --> ModelFilter --> GenByWeekModel --> WriteModel --> FinalWrite --> End

  subgraph generate_moments_by_week [generate_moments_by_week]
    G1[Select distinct 'arrived_week']
    G2[For each week: data = df.filter(arrived_week == week)]
    G3[Call generate_performance_moment(data)]
    G4[Append moment['group']=week; collect week_moments; return list]
  end

  subgraph generate_performance_moment [generate_performance_moment]
    P1[Compute earliest_event and latest_event via agg(min/max)]
    P2[Filter df: arrivedcol <= latest_event and diffcol not null]
    P3[Compute latest_delivery and n (count)]
    P4[Compute median/mean/std for diffcol and wdiffcol]
    P5[Compute success_rate and conservative_rate]
    P6[Compute mu, attempt rmse via squared errors]
    P7[Compute fv_eta_val and return performance dict]
  end

  GenByWeek --> G1
  G3 --> P1
  P7 --> G4
  G4 --> WriteSource
```

> SVG rendering failed for this diagram.

## Diagram 2

```mermaid
sequenceDiagram
  participant Script
  participant Spark as SparkContext
  participant SQL as SQLContext
  participant Weeks as generate_moments_by_week
  participant Moment as generate_performance_moment
  participant S3 as S3

  Script->>Spark: SparkContext(master="yarn", appName="model_perf_task")
  Script->>SQL: SQLContext(sc) and sc.setLogLevel("ERROR")
  Script->>SQL: sqlCtx.read.parquet(s3_input_path)
  SQL-->>Script: DataFrame df
  Script->>Script: add arrived_week via UDF; filter arrived_date < now
  Script->>Weeks: generate_moments_by_week(df)
  Weeks->>Weeks: select distinct arrived_week and iterate weeks
  Weeks->>Moment: call generate_performance_moment(filtered_df)
  Moment->>SQL: perform aggregates (min,max,percentile,mean,std,count) and filters
  SQL-->>Moment: aggregated metrics and filtered data
  Moment-->>Weeks: return performance moment dict
  Weeks-->>Script: return week_moments list
  Script->>SQL: createDataFrame(week_moments).write.parquet(path, mode='overwrite')
  SQL->>S3: write parquet files (source/model_type/model paths)
  Script->>S3: final joined.write.parquet(s3_output_path)
  Script-->>Script: End
```

> SVG rendering failed for this diagram.
