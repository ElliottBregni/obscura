# Diagram: research/orchestrator/tasks/models/shipment_model_spark.py


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
flowchart TD
  Start((Start))
  Args[/Parse CLI args: s3_input_path, model_path_map, env, ship_mode, model_type/]
  InitSpark[Initialize SparkContext (master="yarn") and SQLContext]
  ReadBase[Read parquet -> graph_reduce_base = sqlCtx.read.parquet(s3_input_path)]
  FilterBase[Filter dataset by ship_mode_id == ship_mode]
  ComputePaths[Compute base_output_path and raw_data_output_path]
  GetLatest[get_latest_path(versioned_path) -> remove version segment]
  WriteRaw[Write joined -> get_latest_path(raw_data_output_path) (overwrite)]
  ReadJoined[Read joined = sqlCtx.read.parquet(get_latest_path(raw_data_output_path))]
  ImportMap[Import model_feature_set_map from model_mapper]
  Loop{For each model in model_path_map}
  RollupKeys[rollup_keys = list(model_feature_set_map[model])]
  WSpec[wspec = Window.partitionBy(rollup_keys)]
  AddCols[Add columns: std_shipment_status_to_arrival_seconds, avg, median, mean, stddev_distance]
  OutlierFilter[Filter rows where stddev_distance between -3 and 3]
  ComputeDeltas[Compute delt30, delt60, delt90, delt120, delt180, delt365 = now - days]
  Aggregate[GroupBy(rollup_keys) and compute 30/60/90/120/180/365 day stats and percentiles]
  InferPath[this_model_path = model_path_map[model.value]]
  WriteModel[Write model_rollup -> get_latest_path(this_model_path) and -> this_model_path (overwrite)]
  EndLoop((End loop))
  End((End))

  Start --> Args
  Args --> InitSpark
  InitSpark --> ReadBase
  ReadBase --> FilterBase
  FilterBase --> ComputePaths
  ComputePaths --> GetLatest
  GetLatest --> WriteRaw
  WriteRaw --> ReadJoined
  ReadJoined --> ImportMap
  ImportMap --> Loop
  Loop --> RollupKeys
  RollupKeys --> WSpec
  WSpec --> AddCols
  AddCols --> OutlierFilter
  OutlierFilter --> ComputeDeltas
  ComputeDeltas --> Aggregate
  Aggregate --> InferPath
  InferPath --> WriteModel
  WriteModel --> EndLoop
  EndLoop --> End
```

> SVG rendering failed for this diagram.
