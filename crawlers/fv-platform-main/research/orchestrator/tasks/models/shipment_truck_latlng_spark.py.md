# Diagram: research/orchestrator/tasks/models/shipment_truck_latlng_spark.py


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
flowchart TD
  CLI_ARGS[CLI args: s3_input_path<br/>s3_shipment_statuses<br/>s3_orgs_path<br/>model_path_map<br/>env] --> MAIN[main()]
  GET_LATEST[get_latest_path(versioned_path)]
  GET_S3_FOLDER[get_s3_output_folder(s3_output_path)]
  MAIN --> INIT[SparkContext & SQLContext]
  INIT --> READ_BASE[Read parquet(s3_input_path) -> base]
  READ_BASE --> IS_DELIVERED{Delivered?}
  IS_DELIVERED -- yes --> FILTER_DELIVERED[Filter: final_stop_location_id == ship_destination_newloc_id<br/>AND sstop_final_stop_arrived_at IS NOT NULL]
  IS_DELIVERED -- no --> DISCARD1((discard))
  FILTER_DELIVERED --> READ_ORGS[Read parquet(s3_orgs_path) -> orgs (select id,fv_id; rename -> org_*)]
  READ_ORGS --> JOIN_ORGS[Join on ship_created_by_org_id == org_id]
  JOIN_ORGS --> FILTER_TRUCK[Filter: ship_mode_id == 1]
  FILTER_TRUCK --> READ_STATUSES[Read parquet(s3_shipment_statuses)]
  READ_STATUSES --> JOIN_STATUSES[Join base with shipment_statuses on ship_id == sstat2_shipment_id]
  JOIN_STATUSES --> CALC_ARRIVAL[Compute shipment_status_to_arrival_seconds = unix_timestamp(arrived_at) - unix_timestamp(actual_created_at)]
  CALC_ARRIVAL --> FILTER_DEPARTED{sstop_min_departed_at is null<br/>OR sstat2_actual_created_at > sstop_min_departed_at}
  FILTER_DEPARTED --> FILTER_POS[Filter shipment_status_to_arrival_seconds > 0]
  FILTER_POS --> ADD_DAY[Add day_of_week column]
  ADD_DAY --> READ_LOC[Read location parquet for env -> loc]
  READ_LOC --> PREPARE_LOC[Select id,city,state,country,code;<br/>create dest_shipper_location_id_city;<br/>rename id->dupe_dest_loc_id;state->dest_shipper_location_id_state;code->dest_code;drop city,country]
  PREPARE_LOC --> JOIN_LOC[Join on true_destination_location_id == dupe_dest_loc_id]
  JOIN_LOC --> EXCLUDE_AP[Exclude dest_code in {AP05A,AP06A} between 2024-01-25 and 2024-02-01]
  EXCLUDE_AP --> WINDOW_FILTER[Filter sstop_final_stop_arrived_at >= window_start_date (now - 365 days)]
  WINDOW_FILTER --> SUN_THRU_THURS[Add sun_thru_thurs column]
  SUN_THRU_THURS --> COMPUTE_RAW_PATH[Compute raw_data_output_path from model_path_map]
  COMPUTE_RAW_PATH --> SAVE_RAW[Write joined -> model_raw_data.parquet]
  SAVE_RAW --> READ_RAW[Read raw_data_output_path back -> joined]
  READ_RAW --> HAD_UPDATE[GroupBy ship_id -> shipment_update_date (max when sstat2_status_code == SU)]
  HAD_UPDATE --> JOIN_HAD_UPDATE[Left join; drop had_update.ship_id]
  JOIN_HAD_UPDATE --> FILTER_UPDATE{(sstat2_actual_created_at > shipment_update_date) OR (shipment_update_date is null)}
  FILTER_UPDATE --> FILTER_LO[Filter sstat2_status_code == LO]
  FILTER_LO --> DEDUP_G[GroupBy keys -> joinedG (max sstat2_actual_created_at, avg shipment_status_to_arrival_seconds)]
  DEDUP_G --> ADD_HOUR[Add hour & hour_group via UDF]
  ADD_HOUR --> READ_SEX[Read public.shipment_exceptions.parquet; filter type==36; groupBy shipment_id -> sex_shipment_id, sex_resolved_at]
  READ_SEX --> JOIN_SEX[Left join joinedG with sex]
  JOIN_SEX --> FILTER_SEX[Filter sex_shipment_id IS NULL]
  FILTER_SEX --> FILTER_TRANSIT[Filter shipment_status_to_arrival_seconds between 5 minutes and 30 days]
  FILTER_TRANSIT --> RAW_DEDUPED_PATH[Write joinedG -> raw_data_output_path_deduped (versioned)]
  RAW_DEDUPED_PATH --> GET_LATEST
  GET_LATEST --> READ_DEDUPED[Read get_latest_path(raw_data_output_path_deduped) -> joined]
  READ_DEDUPED --> MODEL_SETUP[Prepare model_type & model_hierarchy (model_mapper)]
  MODEL_SETUP --> MODEL_LOOP{For each model in model_hierarchy}
  MODEL_LOOP --> ROLLUP_KEYS[rollup_keys = model_feature_set_map[model]]
  ROLLUP_KEYS --> PERCENTILE[percentile = get_model_config(model).percentile (default 60)]
  PERCENTILE --> WSPEC[Window.partitionBy(rollup_keys)]
  WSPEC --> STATS_WINDOW[Compute stddev, avg, median, mean;<br/>stddev_distance = (value - mean)/stddev;<br/>Filter |stddev_distance| <= 3.0]
  STATS_WINDOW --> DELTAS[Compute deltas: 30,60,90,120,180,365 days]
  DELTAS --> MODEL_ROLLUP[GroupBy rollup_keys -> aggregate 30/60/90/120/180/365 mean,std,countDistinct,pctiles]
  MODEL_ROLLUP --> THIS_MODEL_PATH[Determine this_model_path; model_version = parts[-2]; s3_output_folder = get_s3_output_folder(this_model_path)]
  THIS_MODEL_PATH --> GET_S3_FOLDER
  GET_S3_FOLDER --> WRITE_VERSIONED[Write model_rollup -> this_model_path (versioned)]
  WRITE_VERSIONED --> READ_MODEL[Read this_model_path -> model_rollup]
  READ_MODEL --> REDIS[Call redis_bulk_load.rollup_to_redis_protocol(model_rollup, model.value, s3_output_folder, this_model_path, model_version)]
  REDIS --> WRITE_LATEST[Write model_rollup -> get_latest_path(this_model_path)]
  WRITE_LATEST --> NEXT_MODEL((next model or end))
  NEXT_MODEL --> MODEL_LOOP
  MODEL_LOOP --> END([End])
```

> SVG rendering failed for this diagram.
