# Diagram: research/orchestrator/tasks/models/shipment_stats_truck_model.py


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
flowchart LR
  SGR[ShipmentGraphReduceTask] --> Stats[ShipmentStatsTruckModelTask]
  Stats --> DS
  subgraph SparkPipeline["Spark Pipeline"]
    direction LR
    DS[DatabricksSession.builder.profile(DATABRICKS_PROFILE).getOrCreate()] --> ReadInput[Read: fv_prod.eta.shipment_graph_reduce]
    ReadInput --> FilterDelivered[Filter: final stop == destination & sstop_final_stop_arrived_at NOT NULL]
    FilterDelivered --> JoinOrgs[Join ORGS_TABLE -> rename org_* columns]
    JoinOrgs --> FilterTruck[Filter: ship_mode_id == 1 (Truck)]
    FilterTruck --> ReadLoc[Read: fv_prod.bronze.location_location]
    ReadLoc --> TransformLoc[Transform LOC: select id,city,state,country,code; create dest_shipper_location_id_city; rename id->dupe_dest_loc_id; drop city,country]
    TransformLoc --> JoinLoc[Join on true_destination_location_id == dupe_dest_loc_id]
    JoinLoc --> ExcludeAP[Exclude rows where dest_code in {AP05A,AP06A} between 2024-01-25 and 2024-02-01]
    ExcludeAP --> WindowFilter[Filter: sstop_final_stop_arrived_at >= window_start_date (1 year)]
    WindowFilter --> AddSun[Add column sun_thru_thurs]
    AddSun --> SaveRaw[Save -> fv_prod.eta.shipment_model_raw_data]
    SaveRaw --> ReadRaw[Read -> fv_prod.eta.shipment_model_raw_data]
    ReadRaw --> HadUpdate[Aggregate had_update per ship_id (max SU sstat_actual_created_at)]
    HadUpdate --> JoinUpdate[Join had_update & keep rows after shipment update or with no update]
    JoinUpdate --> FilterLO[Filter: sstat_status_code == 'LO']
    FilterLO --> Dedup[GroupBy dedupe on keys including sstat_lnglat_tenth; agg max(actual_created_at), avg(arrival_seconds)]
    Dedup --> AddHour[Add hour & hour_group columns]
    AddHour --> ReadSex[Read SHIPMENT_EXCEPTIONS and aggregate type_id==36 -> sex_shipment_id & sex_resolved_at]
    ReadSex --> JoinSex[Left join and filter out shipments with exceptions (sex_shipment_id IS NULL)]
    JoinSex --> FilterExtreme[Filter ship_status_to_arrival_seconds between 5min and 30 days]
    FilterExtreme --> SaveDedup[Save -> fv_prod.eta.shipment_truck_model_raw_data]
    SaveDedup --> ReadDedup[Read -> fv_prod.eta.shipment_truck_model_raw_data]
    ReadDedup --> AddModelCols[Add columns: day_of_week, sstat2_lnglat_tenth]
    AddModelCols --> RollupLoop[For each model in MODEL_HIERARCHY compute rollup stats & percentiles (30/60/90/120/180/365 days) using Window functions]
    RollupLoop --> AddTs[Add modelTs = current_timestamp()]
    AddTs --> WriteTables[Write model rollup tables -> fv_prod.eta.<model>]
    WriteTables --> WriteDone[Write 'Done' to output_path]
  end
  WriteDone --> Artifact[artifact: shipment_stats_truck_rollup_<week>.txt]
  Artifact --> Truck[ShipmentTruckTask]
  Truck --> ModelLoop{for model in MODEL_HIERARCHY}
  ModelLoop --> Copy[Copy fv_prod.eta.<model> -> databricks_to_redis.copy_databricks_to_redis(databricks_table, model_name, REDIS_DB_NUM)]
  Copy --> Redis[(Redis DB 1)]
  Redis --> ModelLoop
  Truck --> TruckDone[Write artifact: shipment_truck_model_<week>.txt]
```

> SVG rendering failed for this diagram.
