# Diagram: research/admin_app/.gitlab-ci.yml


> Auto-generated by Obscura crawlers

## Diagram 1

```mermaid
classDiagram
class ml_eta_admin_app_venv {
  <<job>>
  cache_key: pip-eta-admin-app
  cache_paths: .cache/pip-eta-admin-app
  variables: PIP_CACHE_DIR, PYTHONPATH
  rules_changes: research/admin_app/.gitlab-ci.yml; research/admin_app/**/*.py
  before_script: python -m venv venv; source venv/bin/activate; pip install ${FV_REPO_DIR}/platform/common/fv/python; pip install -r research/admin_app/requirements.txt --use-deprecated=legacy-resolver; pushd research/admin_app
}
class eta_admin_app_ruff {
  <<job>>
  extends: .ml-eta-admin-app-venv, .ruff-strict
}
class eta_admin_app_pyright {
  <<job>>
  extends: .ml-eta-admin-app-venv, .pyright
  variables: LINT_DIRECTORIES=.
}
ml_eta_admin_app_venv <|-- eta_admin_app_ruff
ml_eta_admin_app_venv <|-- eta_admin_app_pyright
```

> SVG rendering failed for this diagram.

## Diagram 2

```mermaid
flowchart TD
A[python -m venv venv] --> B[source venv/bin/activate]
B --> C[pip install ${FV_REPO_DIR}/platform/common/fv/python]
C --> D[pip install -r research/admin_app/requirements.txt --use-deprecated=legacy-resolver]
D --> E[pushd research/admin_app]
```

> SVG rendering failed for this diagram.
