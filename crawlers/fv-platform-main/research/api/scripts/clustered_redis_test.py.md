# Diagram: research/api/scripts/clustered_redis_test.py


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
flowchart TD
    Start([Running test])
    Start --> Loop{for i in range(1000)}
    Loop --> |spawn thread| Thread_i[/Thread i started/]
    subgraph Threads [Thread pool / concurrent threads]
        Thread_i --> doit_call[doit()]
        doit_call --> t_start[(t = time.time())]
        t_start --> create_client[[Create redis.Redis(host='prod-b-eta-redis...:6379')]]
        create_client --> rc_get[[rc.get(2)]]
        rc_get --> rc_close[[rc.close()]]
        rc_close --> print_i[[print(i)]]
        print_i --> elapsed_calc[(elapsed = time.time() - t)]
        elapsed_calc --> slow_check{elapsed > 1?}
        slow_check -- Yes --> print_slow[[print("redis time = {elapsed} seconds")]]
        slow_check -- No --> thread_end([thread end])
        print_slow --> thread_end
    end
    thread_end --> Loop
    Loop --> End([All threads spawned])
```

> SVG rendering failed for this diagram.
