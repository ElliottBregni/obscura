# Diagram: research/common/redis_ops.py


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
flowchart TD
    A[copy_s3_parquet_to_redis(model_name, s3_parquet_path, redis_db_num)] --> B[copy_from_parquet_to_redis_protocol(model_name, model_s3_path, redis_s3_output_path)]
    B --> C[DeltaTable(model_s3_path) : dt.files() loop]
    C --> D[Read parquet delta file with dask (dd.read_parquet) -> iterate rows]
    D --> RK[row_to_redis_key(row, model_type, model) -> uses model_mapper.model_feature_set_map]
    D --> RV[row_to_redis_value(row, model) -> suffix-based value extraction]
    D --> KM[model_mapper.get_redis_key_from_row(model, row_dict)]
    D --> VM[model_mapper.get_redis_value_from_row(row_dict)]
    KM --> G[gen_redis_proto(["SET", redis_key, redis_value])]
    VM --> G
    RK --> G
    RV --> G
    G --> W[Write redis protocol string to s3_output_path via smart_open]
    W --> FileLoop[Loop: count++, log every 10000]
    FileLoop --> FileComplete[writer.close(); log completed file]
    FileComplete --> NextDelta[continue to next delta file]
    B -->|on exception| Err[print error and continue]
    A --> H[copy_redis_proto_to_redis(redis_proto_s3_path, redis_db_num)]
    H --> StageDir[/tmp/redis_data_stage_{uuid} created]
    StageDir --> S3Copy[aws s3 cp redis_proto_s3_path StageDir --recursive]
    S3Copy --> LocalCopy[copy_redis_proto_to_redis_local(StageDir, redis_db_num)]
    LocalCopy --> LList[os.listdir(redis_proto_local_dir) -> iterate files]
    LList --> ForEachFile[for each file: build redis_bulk_load_cmd]
    ForEachFile --> ExecRedis[cat file | redis-cli -h REDIS_HOST -n redis_db_num --pipe]
    ExecRedis --> LogOutput[print redis_cmd_output]
    LocalCopy --> TimeLog[print total time taken]
    H --> Cleanup[shutil.rmtree(StageDir)]
    Cleanup --> Done[print completed copying redis files]
    style RED fill:#f9f,stroke:#333,stroke-width:1px
    style Err fill:#fee,stroke:#a33,stroke-width:1px
    style Done fill:#efe,stroke:#393,stroke-width:1px
```

> SVG rendering failed for this diagram.
