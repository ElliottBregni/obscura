# Diagram: research/deploy/dev/setup_minikube.sh


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
flowchart TD
    A{Is minikube host \"Running\"?} -->|No| B[cpus = nproc - 2]
    A -->|Yes| I[Echo: \"minikube already started!\"]
    B --> C[available_memory = free --giga | awk ... (GB)]
    C --> D{available_memory > 4?}
    D -->|Yes| E[available_memory = available_memory - 4]
    D -->|No| F[Keep available_memory as-is]
    E --> G[Append \"GB\" to available_memory]
    F --> G
    G --> H[Run: minikube start --cpus=$cpus --memory=$available_memory]
```

> SVG rendering failed for this diagram.
