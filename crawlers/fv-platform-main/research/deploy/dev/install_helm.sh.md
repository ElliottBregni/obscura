# Diagram: research/deploy/dev/install_helm.sh


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
flowchart TD
    Start((Start)) --> Check{Is "helm" available (command -v helm)?}
    Check -- No --> Install[Install Helm<br>sudo snap install helm --classic]
    Check -- Yes --> Skip[Helm already installed]
    Install --> Echo[echo "HELM INSTALLED"]
    Skip --> Echo
    Echo --> Version[Run: helm version]
    Version --> End((End))
```

> SVG rendering failed for this diagram.
