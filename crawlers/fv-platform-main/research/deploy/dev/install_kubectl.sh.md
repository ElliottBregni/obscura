# Diagram: research/deploy/dev/install_kubectl.sh


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
flowchart TD
  Start((Start)) --> Check{Is kubectl executable?}
  Check -->|No| EchoInstall[echo kubectl not installed - installing now]
  EchoInstall --> CurlBin[curl -LO kubectl binary (stable release)]
  CurlBin --> CurlSha[curl -LO kubectl.sha256 (stable release)]
  CurlSha --> Verify{sha256sum --check succeeds?}
  Verify -->|Yes| Install[sudo install -o root -g root -m 0755 kubectl /usr/local/bin/kubectl]
  Install --> EchoInstalled[echo kubectl installed]
  EchoInstalled --> KubectlVersion[kubectl version --client]
  KubectlVersion --> End((End))
  Verify -->|No| VerifyFail[checksum verification failed]
  VerifyFail --> Abort[abort / exit with error]
  Abort --> End
  Check -->|Yes| EchoAlready[echo kubectl installed]
  EchoAlready --> KubectlVersion
```

> SVG rendering failed for this diagram.
