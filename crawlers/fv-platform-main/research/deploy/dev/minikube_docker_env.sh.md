# Diagram: research/deploy/dev/minikube_docker_env.sh


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
flowchart TD
    Start([Start]) --> ReadCmd[Set command = $1]
    ReadCmd --> IsActivate{command == "activate?"}
    IsActivate -- yes --> Activate[eval $(minikube docker-env)<br/>docker images]
    IsActivate -- no --> IsDeactivate{command == "deactivate?"}
    IsDeactivate -- yes --> Deactivate[unset DOCKER_TLS_VERIFY<br/>unset DOCKER_HOST<br/>unset DOCKER_CERT_PATH<br/>unset DOCKER_API_VERSION]
    IsDeactivate -- no --> Invalid[echo "$command not a valid command"]
    Activate --> End((End))
    Deactivate --> End
    Invalid --> End
```

> SVG rendering failed for this diagram.
