# Diagram: research/deploy/dev/green_button_dev.sh


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
flowchart TD
  Start([Start]) --> CheckEnv{FV_RESEARCH_HOME set?}
  CheckEnv -- No --> Exit([Print "FV_RESEARCH_HOME not set" & exit])
  CheckEnv -- Yes --> SetEnv[export ENV=dev]
  SetEnv --> Cd[cd $FV_RESEARCH_HOME/deploy]
  Cd --> EchoYellow[echo "YELLOW FOR PROVISIONING"]
  EchoYellow --> EchoGreen[echo "GREEN FOR DEPLOYMENT"]
  EchoGreen --> EchoGrab[echo "this might take a bit, so go grab:"]
  EchoGrab --> CatCoffee[cat $FV_RESEARCH_HOME/deploy/dev/coffee.txt]
  CatCoffee --> Sleep5[sleep 5]
  Sleep5 --> AptUpdate[sudo apt-get update]
  AptUpdate --> InstallPip[sudo apt-get install python3-pip]
  InstallPip --> PipVirtualenv[python3 -m pip install virtualenv]
  PipVirtualenv --> CreateVenv[python3 -m virtualenv $FV_RESEARCH_HOME/api/venv]
  CreateVenv --> VenvPip[$FV_RESEARCH_HOME/api/venv/bin/pip install -r $FV_RESEARCH_HOME/api/requirements.txt]
  VenvPip --> WhichYq[which yq]
  WhichYq --> YqPresent{yq installed?}
  YqPresent -- No --> SnapInstall[sudo snap install yq]
  YqPresent -- Yes --> SkipYq[skip yq install]
  SnapInstall --> AfterYq
  SkipYq --> AfterYq
  AfterYq --> InstallMinikube[$FV_RESEARCH_HOME/deploy/dev/install_minikube.sh]
  InstallMinikube --> InstallKubectl[$FV_RESEARCH_HOME/deploy/dev/install_kubectl.sh]
  InstallKubectl --> InstallDocker[$FV_RESEARCH_HOME/deploy/dev/install_docker_linux.sh]
  InstallDocker --> InstallHelm[$FV_RESEARCH_HOME/deploy/dev/install_helm.sh]
  InstallHelm --> SetupMinikube[$FV_RESEARCH_HOME/deploy/dev/setup_minikube.sh]
  SetupMinikube --> EvalDockerEnv[eval $(minikube docker-env)]
  EvalDockerEnv --> DockerBuild[docker build -f $FV_RESEARCH_HOME/Dockerfile -t aieta:latest $FV_RESEARCH_HOME]
  DockerBuild --> PrepareK8[$FV_RESEARCH_HOME/deploy/dev/prepare_k8_yaml.sh]
  PrepareK8 --> KubectlApply[kubectl apply -f $FV_RESEARCH_HOME/deploy/dev/ai_controller.yaml]
  KubectlApply --> End([End])
```

> SVG rendering failed for this diagram.
