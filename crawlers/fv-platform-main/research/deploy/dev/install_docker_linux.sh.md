# Diagram: research/deploy/dev/install_docker_linux.sh


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
flowchart TD
    Start([Start]) --> CheckDocker{Is docker executable present?}
    CheckDocker -- No --> AptUpdate[Run: sudo apt update]
    AptUpdate --> InstallPrereqs[Install: ca-certificates<br>curl<br>gnupg<br>lsb-release]
    InstallPrereqs --> MakeKeyrings[Create: /etc/apt/keyrings]
    MakeKeyrings --> AddGPG[Add Docker GPG and write /etc/apt/keyrings/docker.gpg]
    AddGPG --> AddRepo[Add Docker apt repository to /etc/apt/sources.list.d/docker.list]
    AddRepo --> AptUpdate2[Run: sudo apt-get update]
    AptUpdate2 --> InstallDocker[Install: docker-ce docker-ce-cli containerd.io docker-compose-plugin]
    InstallDocker --> HelloInside[Run: sudo docker run hello-world]
    HelloInside --> MergeAfterIf[After if / continue]
    CheckDocker -- Yes --> MergeAfterIf
    MergeAfterIf --> HelloOutside[Run: sudo docker run hello-world]
    HelloOutside --> CaptureUser[who = $(whoami)]
    CaptureUser --> ChownSock[Run: sudo chown $who:$who /var/run/docker.sock]
    ChownSock --> End([End])
```

> SVG rendering failed for this diagram.
