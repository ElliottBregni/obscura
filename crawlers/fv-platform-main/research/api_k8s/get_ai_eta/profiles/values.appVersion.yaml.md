# Diagram: research/api_k8s/get_ai_eta/profiles/values.appVersion.yaml


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
flowchart TD
    AS[ApplicationSet] --> AE[All Environments]
    AE -->|default imageTag| IT[imageTag: 0.5.25]
    AE --> PROD[prod (pinned)]
    PROD -.->|updated only when pinned revision moves| AS
```

> SVG rendering failed for this diagram.
