# Diagram: research/api_k8s/get_ai_eta/src/ai_models/partview_eta_ai_model.py


> Auto-generated by Obscura crawlers

## Diagram 1

```mermaid
classDiagram
class PartviewEtaModel {
  +CatBoostRegressor partview_model
  +_get_partview_eta_override(from_location_code, from_milestone, to_location_code) int|null
  +feature_extract_and_predict(input_dict, location_redis, timezone_finder) PredictionResult
}
class PartviewAIFeatures <<external>> {
  +async_redis_features(location_redis, timezone_finder)
  +for_catboost(model)
  +for_response()
}
class PredictionResult <<external>> {
  +eta_seconds
  +feature_dict
}
class CatBoostRegressor <<lib>>
class Redis <<lib>>
class TimezoneFinder <<lib>>
class numpy <<lib>>
class handle_null_features {
  +handle_null_features(vector, dict_fe_map, cat_features) vector
}
PartviewEtaModel --> PartviewAIFeatures : uses
PartviewEtaModel --> PredictionResult : returns
PartviewEtaModel ..> CatBoostRegressor : holds
feature_null_dep ..> numpy : uses
handle_null_features ..> numpy : uses
PartviewEtaModel ..> Redis : accepts
PartviewEtaModel ..> TimezoneFinder : accepts
```

> SVG rendering failed for this diagram.

## Diagram 2

```mermaid
flowchart TD
Start([feature_extract_and_predict(input_dict, location_redis, timezone_finder)]) --> CreateFeatures[Create PartviewAIFeatures(input_dict)]
CreateFeatures --> AwaitRedis[Await features_instance.async_redis_features(location_redis, timezone_finder)]
AwaitRedis --> ToVector[feature_vector = features_instance.for_catboost(self.partview_model)]
ToVector --> Predict[eta = self.partview_model.predict(feature_vector)[0]]
Predict --> CreatePred[pred = PredictionResult(eta_seconds=eta)]
CreatePred --> SetFeat[pred.feature_dict = features_instance.for_response()]
SetFeat --> Return[return pred]
Return --> End([Done])
```

> SVG rendering failed for this diagram.
