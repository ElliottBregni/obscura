# Diagram: research/api_k8s/get_ai_eta/src/features/features.py


> Auto-generated by Obscura crawlers

## Diagram 1

```mermaid
classDiagram
class LocationFeatures <<TypedDict>> {
  city_group: str
  city: str
  state: str
  country: str
  zip: str
  lad_id: str
}
class EntityAIFeatures {
  +ent_solution_id: str
  +sup_status_update_month: str
  +sup_status_update_partofmonth: str
  +suploc_country: str
  +destloc_country: str
  +dwell_proportion: float|None
  +non_redis_features(input_params, raw_params)
  +async_redis_features(location_conn, model)
  +for_catboost(mdl)
  +for_response()
}
class PartviewAIFeatures {
  +pc_weight: float
  +evt_event_code: str
  +non_redis_features(input_params)
  +async_redis_features(redis_stats_conn, timezone_finder)
  +for_catboost(mdl)
  +for_response()
}
class TruckAIFeatures {
  +ship_line_of_business_id: int
  +dloc_country: str
  +non_redis_features(params, timezone_finder)
  +for_catboost(mdl)
  +for_response()
}
class ContainerAIFeatures {
  +num_future_legs: float|None
  +ports_in_future_legs: int|None
  +distance: float|None
  +non_redis_features(input_params)
  +async_redis_features(redis_stats_conn)
  +for_response()
}
class Redis <<External>>
class CatBoostRegressor <<External>>
class TimezoneFinder <<External>>
class EtaProxyParameters <<External>>
class EntityEtaParameters <<External>>
class haversine <<function>> {
  +(lat1, lon1, lat2, lon2)
}
class localize_dt <<function>> {
  +(dt, lat, lng, tf)
}
class get_direction <<function>> {
  +(suploc_lat, suploc_lng, destloc_lat, destloc_lng)
}
class get_location_features_from_id <<async function>> {
  +(location_id, location_conn)
}
class dest_city_group <<async function>> {
  +(params, location_redis)
}
EntityAIFeatures --> Redis : uses
EntityAIFeatures --> CatBoostRegressor : uses
EntityAIFeatures --> localize_dt : calls
EntityAIFeatures --> haversine : calls
EntityAIFeatures --> get_direction : calls
EntityAIFeatures --> get_location_features_from_id : calls
PartviewAIFeatures --> Redis : uses
PartviewAIFeatures --> TimezoneFinder : uses
PartviewAIFeatures --> haversine : calls
TruckAIFeatures --> TimezoneFinder : uses
TruckAIFeatures --> localize_dt : calls
TruckAIFeatures --> country_from_latlon : uses
ContainerAIFeatures --> Redis : uses
ContainerAIFeatures --> haversine : calls
dest_city_group --> get_location_features_from_id : calls
```

> SVG rendering failed for this diagram.

## Diagram 2

```mermaid
flowchart TD
  InputParams[EntityEtaParameters + raw_params] --> Offline[non_redis_features()]
  Offline -->|origin/dest ids| RedisLookup[get_location_features_from_id(location_id, Redis)]
  Offline -->|model artifact| ModelMeta[model.get_metadata()]
  RedisLookup --> AsyncRedis[async_redis_features(location_conn, model)]
  ModelMeta --> AsyncRedis
  AsyncRedis --> BuildFull[assemble features -> _full]
  BuildFull --> CatboostCall[for_catboost(mdl) -> numpy array]
  BuildFull --> APIResponse[for_response() -> sanitized dict]
```

> SVG rendering failed for this diagram.
