# Diagram: research/api_k8s/get_ai_eta/src/conftest.py


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
flowchart LR
  Env[Environment variables: LOG_LEVEL, ENVIRONMENT, REDIS_HOST] --> LoadDev[Load helm/devModels.yaml -> os.environ.update]
  LoadDev --> Imports[Imports: EntityAIFeatures, location_redis_conn, override_redis_conn, app]
  Imports --> App[FastAPI app (app)]

  subgraph Clients
    ClientFixture[TestClient fixture: client]
    ItestFixture[itest_client fixture (async)]
    App --> ClientFixture
    App --> ItestFixture
    ItestFixture --> Lifespan[LifespanManager]
    Lifespan --> AsyncClientNode[AsyncClient via ASGITransport]
  end

  subgraph Redis
    OverrideConn[override_redis_conn]
    LocationConn[location_redis_conn]
    OverrideConn --> OverrideRedisFixture[override_redis fixture]
    LocationConn --> LocationRedisFixture[location_redis fixture]
    _json[_json_dumps_nan_safe] --> SeedFunc[_seed_redis(r)]
    SeedFunc --> Seeded[_seeded_redis (session-seeded FakeRedis)]
    Seeded --> AsyncMock[async_mock_redis (per-test clone)]
    SyncMock[sync_mock_redis (fakeredis FakeRedis)]
    DataSetKeys[(many redis keys set: locations, vin=..., entity_stats=..., container_id=...)]
    SeedFunc --> DataSetKeys
    SyncMock --> DataSetKeys
  end

  subgraph DataFixtures
    intermediate1[intermediate_eta_qsp]
    intermediate2[intermediate_eta_qsp2]
    entity1[entity_qsp]
    entity2[entity_qsp_two]
    partview1[partview_qsp]
    partview2[partview_qsp_two]
    container1[container_qsp]
    container2[container_qsp2]
    truck[truck_qsp]
    truck_no_dist[truck_qsp_no_dist]
  end

  DataFixtures -->|seed values| SyncMock
  DataFixtures -->|seed values| SeedFunc

  EntityFeatures[EntityAIFeatures (feature spec)] --> EntityModel[entity_status_model fixture]
  EntityModel --> CatBoost[CatBoostRegressor trained on synthetic DataFrame]
  CatBoost --> ModelMeta[model.metadata (dwell_proportions)]
  Test[test_train_mock_entity_status] -->|assert| EntityModel

  App -->|uses| OverrideConn
  App -->|uses| LocationConn
  SeedFunc -->|uses| _json
  Seeded -->|copied keys| AsyncMock
  SyncMock -->|returned by| sync_mock_redis
  AsyncMock -->|yield| async_mock_redis
```

> SVG rendering failed for this diagram.
