# Diagram: research/api_k8s/get_ai_eta/helm/templates/hpa.yaml


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
flowchart TD
  HPA[HorizontalPodAutoscaler\nmetadata:\n name: {{ .Values.appName }}\n namespace: {{ .Values.namespace }}]
  Metrics[metrics:\n- resource:\n    name: cpu\n    target:\n      averageUtilization: {{ .Values.hpa.averageCpuUtilization }}\n      type: Utilization]
  Replicas[minReplicas: {{ .Values.hpa.minReplicas }}\nmaxReplicas: {{ .Values.hpa.maxReplicas }}]
  Target[scaleTargetRef:\napiVersion: apps/v1\nkind: Deployment\nname: {{ .Values.appName }}]
  HPA --> Metrics
  HPA --> Replicas
  HPA --> Target
  Metrics --> CPU[cpu]
```

> SVG rendering failed for this diagram.
