# Diagram: platform/tools/ide_local_testing/localTest/test/byUrl/entityGetStatusUpdate.py


> Auto-generated by Obscura crawlers

## Diagram 1

```mermaid
flowchart TD
    Start([Start]) --> AuthorizerNode[Create Authorizer and syncFromAuth0()]
    AuthorizerNode --> OrgCheck{activeOrgId?}
    OrgCheck -- yes --> OrgSetup[setOrganizationId / setOrganizationProfiles / setSolutions]
    OrgCheck -- no --> OrgSkip[skip org setup]
    OrgSetup --> EventBuild[Construct CanonicalEvent]
    OrgSkip --> EventBuild
    EventBuild --> EventChain[Call methods: setAuthorizer, setHttpMethod, setAcceptHeader, parseUri, addPathParameter x2, prepare, toAwsEvent]
    EventChain --> CallLambda[Invoke lambdaHandler(event, DictionaryToObject(context))]
    CallLambda --> RetvalCheck{retval and retval.body?}
    RetvalCheck -- yes --> Parse[parse body -> prettyRetval = json.dumps(body, indent=2, sort_keys=True)]
    RetvalCheck -- no --> Empty[prettyRetval = ""]
    Parse --> Print[print(prettyRetval)]
    Empty --> Print
    Print --> PrintTime[print(Lambda execution time)]
    PrintTime --> End([End])
```

> SVG rendering failed for this diagram.

## Diagram 2

```mermaid
classDiagram
    class Authorizer {
        +setUsername(username)
        +syncFromAuth0()
        +setOrganizationId(id)
        +setOrganizationProfiles(profiles)
        +setSolutions(solutions)
    }
    class CanonicalEvent {
        +setAuthorizer(authorizer)
        +setHttpMethod(method)
        +setAcceptHeader(header)
        +parseUri(uri)
        +addPathParameter(key, value)
        +prepare()
        +toAwsEvent()
    }
    class DictionaryToObject {
        +__init__(data)
    }
    class LambdaHandler {
        +lambda_handler(event, context)
    }
    Authorizer --> CanonicalEvent : provided to
    CanonicalEvent --> LambdaHandler : produces event for
    DictionaryToObject --> LambdaHandler : provides context to
    LambdaHandler ..> "HTTP JSON response" : returns
```

> SVG rendering failed for this diagram.
