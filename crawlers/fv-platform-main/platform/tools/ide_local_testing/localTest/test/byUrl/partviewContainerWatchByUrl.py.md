# Diagram: platform/tools/ide_local_testing/localTest/test/byUrl/partviewContainerWatchByUrl.py


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
flowchart TD
  Start([Start]) --> Imports[Imports and constants]
  Imports --> Authz[Authorizer() instantiation]
  Authz --> SetUser[setUsername("dave.damon...") & syncFromAuth0()]
  SetUser --> CheckOrg{activeOrgId?}
  CheckOrg -- yes --> ConfigureOrg[setOrganizationId / setOrganizationProfiles / setFeatures]
  CheckOrg -- no --> SkipOrg[skip org config]
  ConfigureOrg --> EventCreate[CanonicalEvent() creation]
  SkipOrg --> EventCreate
  EventCreate --> Chain[Chain calls: setAuthorizer → setHttpMethod POST → setAcceptHeader → setBody watch=True → parseUri(uri) → addPathParameter(type,containerId) → prepare → toAwsEvent]
  Chain --> TimerStart[start timer]
  TimerStart --> CallLambda[call lambdaHandler(event, DictionaryToObject(...))]
  CallLambda --> TimerEnd[end timer]
  CallLambda --> CheckRetval{retval and retval.body?}
  CheckRetval -- yes --> ParseBody[json.loads(retval.body) → prettyRetval = json.dumps(body, indent=2, sort_keys=True)]
  CheckRetval -- no --> EmptyPretty[prettyRetval = ""]
  ParseBody --> PrintPretty[print(prettyRetval)]
  EmptyPretty --> PrintPretty
  TimerEnd --> PrintTime[print execution time]
  PrintPretty --> PrintTime
  PrintTime --> End([End])
```

> SVG rendering failed for this diagram.
