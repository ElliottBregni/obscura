# Diagram: platform/tools/ide_local_testing/localTest/test/user/getFeaturesViaLambda.py


> Auto-generated by Obscura crawlers

## Diagram 1

```mermaid
flowchart LR
    Start([Script start]) --> SetVars[Set organization_id & privileges & lambdaLevel]
    SetVars --> CallHas[Call has_feature(Finished Vehicle, organization_id, privileges, 0)]
    CallHas --> has_featureFunc[has_feature(feature, organization_id, privileges, lambda_level)]
    has_featureFunc --> invoke_get[invoke_get_features(organization_id, privileges, lambda_level)]
    invoke_get --> build_event[Build event dict with requestContext.authorizer and lambda_level]
    build_event --> call_lambda[invoke_lambda get_features with full payload]
    call_lambda --> response[response]
    response --> parse[Parse response body (json.loads if str)]
    parse --> status{status_code >= 400?}
    status -- yes --> raise[Raise fv.error.HandledException]
    status -- no --> return_features[Return features]
    return_features --> iterate[Iterate features list]
    iterate --> found{Match found? feature_name.lower() == lower_feature}
    found -- yes --> true[Return True]
    found -- no --> false[Return False]
    true --> print[print result]
    false --> print
    print --> End((End))
```

> SVG rendering failed for this diagram.

## Diagram 2

```mermaid
sequenceDiagram
    participant Script
    participant has_feature
    participant invoke_get_features
    participant invoke_lambda
    participant HandledException

    Script->>has_feature: has_feature(Finished Vehicle, organization_id, privileges, 0)
    has_feature->>invoke_get_features: invoke_get_features(organization_id, privileges, lambda_level)
    invoke_get_features->>invoke_lambda: invoke_lambda(get_features, full_payload=event)
    invoke_lambda-->>invoke_get_features: response
    invoke_get_features->>invoke_get_features: parse body -> features
    alt status_code >= 400
        invoke_get_features->>HandledException: raise HandledException(message, message, status_code)
        HandledException-->>Script: propagate error
    else
        invoke_get_features-->>has_feature: return features
        has_feature->>has_feature: iterate features; compare feature_name.lower()
        alt match found
            has_feature-->>Script: return True
        else
            has_feature-->>Script: return False
        end
    end
    Script->>Script: print result
```

> SVG rendering failed for this diagram.
