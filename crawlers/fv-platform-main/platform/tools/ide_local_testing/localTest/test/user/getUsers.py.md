# Diagram: platform/tools/ide_local_testing/localTest/test/user/getUsers.py


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
flowchart TD
    Start((Start)) --> Imports[Import modules: iam_service.vendor.auth0.management_api, iam_service.v1.lambdas.users.get_users.format_users_list]
    Imports --> GetInitial[Call management_api.get_all_users(page=0)]
    GetInitial --> FormatInitial[format_users_list(response) --> userList]
    FormatInitial --> Loop[for loop in range(0, 10)]
    Loop --> CallGetAll[Call management_api.get_all_users(page=loop)]
    CallGetAll --> CheckResponse{len(response) > 0?}
    CheckResponse -- Yes --> Format[format_users_list(response) --> userList]
    Format --> Iterate[for user in userList]
    Iterate --> CheckEmail{email startswith alligator?}
    CheckEmail -- Yes --> PrintAlligator[print user_id -> email]
    CheckEmail -- No --> CheckFV{email startswith fv_?}
    CheckFV -- Yes --> PrintFV[print user_id -> email]
    CheckFV -- No --> Skip[no match / continue]
    PrintAlligator --> Iterate
    PrintFV --> Iterate
    Skip --> Iterate
    Iterate --> LoopContinue[next loop iteration]
    LoopContinue --> Loop
    CheckResponse -- No --> Break[break out of loop]
    Break --> End((End))
```

> SVG rendering failed for this diagram.
