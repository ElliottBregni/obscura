# Diagram: platform/tools/ide_local_testing/localTest/test/user/createUsers.py


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
graph TD
  Start((Start))
  InitDB[connector = ShipmentDatabase(createUsers.test)]
  UsersList[users = list of user dicts]
  UserOrg[user_org = freightVerify_organization_id]
  Actor[actor_id = get_actor_id()]
  UserId[user_id = actor_id]
  OrgDetails[org_details = get_organization_by_id(cursor, user_org)]
  SolutionsFromDetails[solutions = org_details.solutions]
  SolutionsOverride[solutions = get_org_solutions(cursor, user_org)]
  OrgType[org_type = org_details.org_type_code]
  RolesAll[roles = get_all_roles(cursor, user_org)]
  RoleDict[role_dict = reverse_roles(roles)]
  AllowedRoles[allowed_roles = get_roles(cursor, actor_id, user_org)]
  AllowedIds[allowed_ids = keys(allowed_roles)]
  AllowedNames[allowed_names = lowercased role names]
  Headers[headers = HTTP request headers dict]
  LoopStart((for user in users))
  GetAuth0[userDict_byEmail = get_auth0_user_by_email(user.email)]
  Found{userDict_byEmail ?}
  RemoveUser[user = remove_user(connector, user_org, email)]
  NotFound[NotFoundError caught -> pass]
  Cursor[cursor = connector.get_cursor()]
  CreateDry[create_users(... dry_run=True ...)]
  CreateProd[create_users(... dry_run=False ...)]
  End((End))

  Start --> InitDB
  InitDB --> UsersList
  UsersList --> UserOrg
  UserOrg --> Actor
  Actor --> UserId
  UserId --> OrgDetails
  OrgDetails --> SolutionsFromDetails
  SolutionsFromDetails --> SolutionsOverride
  SolutionsOverride --> RolesAll
  OrgDetails --> OrgType
  RolesAll --> RoleDict
  Actor --> AllowedRoles
  UserOrg --> AllowedRoles
  AllowedRoles --> AllowedIds
  AllowedRoles --> AllowedNames
  UsersList --> LoopStart
  LoopStart --> GetAuth0
  GetAuth0 --> Found
  Found -->|yes| RemoveUser
  Found -->|no| LoopStart
  GetAuth0 -->|raises NotFoundError| NotFound
  NotFound --> LoopStart
  LoopStart --> Cursor
  Cursor --> CreateDry
  RoleDict --> CreateDry
  AllowedIds --> CreateDry
  AllowedNames --> CreateDry
  RolesAll --> CreateDry
  Headers --> CreateDry
  UserOrg --> CreateDry
  Actor --> CreateDry
  SolutionsOverride --> CreateDry
  OrgType --> CreateDry
  CreateDry --> CreateProd
  CreateProd --> End
```

> SVG rendering failed for this diagram.
