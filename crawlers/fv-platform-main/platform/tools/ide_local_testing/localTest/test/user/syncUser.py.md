# Diagram: platform/tools/ide_local_testing/localTest/test/user/syncUser.py


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
flowchart TD
    Start((Script start))
    Start --> Env[Read env vars: AUTH0_CONNECTION and AUTH0_INITIAL_USER_PASSWORD]
    Env --> Imports[Import: sync_user, ShipmentDatabase, get_user_id_by_email]
    Imports --> LargeList[Define large emailList tuple (many emails)]
    LargeList --> Override1[Override emailList => shipper-org-admin@yopmail.com]
    Override1 --> Override2[Override emailList => test@freightverify-api.com]
    Override2 --> Override3[Override emailList => vinview1@ford.com, vinview@ford.com, vdimitri@ford.com]
    Override3 --> LoopStart[Start loop: for email in emailList]
    LoopStart --> Inc[Increment count and print count]
    Inc --> PrintEmail[Print email]
    PrintEmail --> GetUser[get_user_id_by_email(email)]
    GetUser --> Decision{user_id is not None?}
    Decision -- Yes --> SyncCall[Create ShipmentDatabase(syncUser.test) and call sync_user(db, user_id) then print result]
    Decision -- No --> NotFound[Print "Couldn't find user"]
    SyncCall --> LoopStart
    NotFound --> LoopStart
    LoopStart --> End((Script end))
```

> SVG rendering failed for this diagram.
