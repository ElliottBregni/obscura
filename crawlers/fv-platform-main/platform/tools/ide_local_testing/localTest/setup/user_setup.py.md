# Diagram: platform/tools/ide_local_testing/localTest/setup/user_setup.py


> Auto-generated by Obscura crawlers

## Diagram 1

```mermaid
flowchart TD
    Start([Script start]) --> CallCreate{{create_user_if_missing(email, organization_id, roles)}}
    CallCreate --> QueryAuth0[get_auth0_user_by_email(email)]
    QueryAuth0 -->|no user| CreateUser[create_user(connector, email, organization_id, roles, fv_admin_auth0_id)]
    CreateUser --> SetPwdFromCreate[set_user_password(user_id)]
    QueryAuth0 -->|user found| ExistingUser[user exists]
    ExistingUser --> SetPwdExisting[set_user_password(user_id)]
    SetPwdFromCreate --> Update[management_api.update_user(user_id, DEFAULT_PASSWORD)]
    SetPwdExisting --> Update
    Update --> Return[return user_id, user]
    CreateUser --> Return
    subgraph Setup
        Connector[connector = ShipmentDatabase("user_setup.test")]
        DefaultPwd[DEFAULT_PASSWORD = "Freight!123"]
    end
    CallCreate -.-> Connector
    CallCreate -.-> DefaultPwd
```

> SVG rendering failed for this diagram.

## Diagram 2

```mermaid
sequenceDiagram
    participant Script
    participant App as create_user_if_missing
    participant Auth0 as get_auth0_user_by_email
    participant Create as create_user
    participant Mgmt as management_api.update_user
    participant DB as ShipmentDatabase
    participant Role as Role
    Script->>Script: connector = ShipmentDatabase("user_setup.test")
    Script->>Script: DEFAULT_PASSWORD = "Freight!123"
    Script->>App: create_user_if_missing(email, organization_id, roles)
    App->>Auth0: get_auth0_user_by_email(email)
    alt user not found
        Auth0-->>App: null
        App->>Create: create_user(connector, email, organization_id, roles, fv_admin_auth0_id)
        Create-->>App: user_id, user
        App->>Mgmt: update_user(user_id, DEFAULT_PASSWORD)
    else user exists
        Auth0-->>App: user
        App->>App: user_id = user.get("user_id")
        App->>Mgmt: update_user(user_id, DEFAULT_PASSWORD)
    end
    App-->>Script: user_id, user
```

> SVG rendering failed for this diagram.
