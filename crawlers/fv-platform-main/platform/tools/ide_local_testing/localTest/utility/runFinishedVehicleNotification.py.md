# Diagram: platform/tools/ide_local_testing/localTest/utility/runFinishedVehicleNotification.py


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
flowchart TD
    Start[Start] --> Imports[Import modules: os, uuid, time, DictionaryToObject, lambda_handler]
    Imports --> GenUUID[Generate UUID: identifier = uuid.uuid4()]
    GenUUID --> PrintID[Print identifier]
    PrintID --> StartTimer[Record start time: start = time.time()]
    StartTimer --> BuildContext[Create context: DictionaryToObject({ requestId: identifier, function_name: "manualFinishedVehicleSync" })]
    BuildContext --> CallLambda[Call lambdaHandler({}, context)]
    CallLambda --> Result[Receive result]
    Result --> ComputeDuration[Compute duration: duration = time.time() - start]
    ComputeDuration --> PrintDuration[Print Duration: print(f"Duration: {duration}")]
    PrintDuration --> End[End]
```

> SVG rendering failed for this diagram.
