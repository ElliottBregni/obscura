# Diagram: platform/partview_core/partview_service/partview_service/core/business/open_search/OpenSearchCountsQueryParamBuilder.py


> Auto-generated by Obscura crawlers

## Diagram 1

```mermaid
classDiagram
class OpenSearchCountsQueryParamBuilder {
  -__version_qsp
  -__destination_not_null_field : string
  -__exception_reason_code_not_null : string
  -__solution_id_qsp
  -__status_qsp
  -__user_id
  -__is_dealer_qsp
  -__index_name
  -__deliveryDateFrom_qsp
  -__deliveryDateTo_qsp
  -query_builders : dict
  +OpenSearchCountsQueryParamBuilder(count_params)
  +build_base_query() : dict
  +add_must_clause(query, must_conditions) : dict
  +add_aggregation(query, aggregation) : dict
  +get_exception_counts_query() : dict
  +get_lifecycle_state_counts_query() : dict
  +get_delivered_counts_queries() : list
  +get_total_counts_queries() : list
  +build_counts_query() : dict
}
class Datetime {
  +Datetime(value)
  +to_ISO_string() : string
}
class CountTypes <<enumeration>> {
  +EXCEPTION_COUNT
  +LIFECYCLE_STATE_COUNT
  +DELIVERED_COUNT
  +TOTAL_COUNT
}
class OpensearchIndexNames {
  +COUNT
}
class DefaultSolution {
  +DEFAULT_DEALER_SOLUTION
}
class ExceptionHelper {
  +get_exception_codes(solution_id, is_dealer)
}
OpenSearchCountsQueryParamBuilder ..> Datetime : uses
OpenSearchCountsQueryParamBuilder ..> CountTypes : references
OpenSearchCountsQueryParamBuilder ..> OpensearchIndexNames : references
OpenSearchCountsQueryParamBuilder ..> DefaultSolution : uses
OpenSearchCountsQueryParamBuilder ..> ExceptionHelper : uses
```

> SVG rendering failed for this diagram.

## Diagram 2

```mermaid
flowchart TD
  A[build_counts_query(version)] --> B{version in query_builders?}
  B -- No --> C[Raise ValueError: "Unknown query type"]
  B -- Yes --> D[query_builder = query_builders[version]]
  D --> QB[QueryBuilders mapping]
  subgraph QueryBuilders
    EXC[CountTypes.EXCEPTION_COUNT] --> GE[get_exception_counts_query()]
    LIF[CountTypes.LIFECYCLE_STATE_COUNT] --> GL[get_lifecycle_state_counts_query()]
    DEL[CountTypes.DELIVERED_COUNT] --> GD[get_delivered_counts_queries()]
    TOT[CountTypes.TOTAL_COUNT] --> GT[get_total_counts_queries()]
  end
  QB --> GE
  QB --> GL
  QB --> GD
  QB --> GT
  GE --> R1[Adds filters, may add terms filter, must_not Delivered, adds exceptionCodes aggregation]
  GL --> R2[Adds lifecycle_state_counts aggregation and returns query]
  GD --> R3[Builds arrival_ts_query and destination_eta_query (with date range must clauses) and returns list]
  GT --> R4[Returns [exceptionCountsQuery, lifecycleStateQuery, arrivalTsQuery, destinationEtaQuery]]
  R1 --> End[Return query or queries]
  R2 --> End
  R3 --> End
  R4 --> End
```

> SVG rendering failed for this diagram.
