# Diagram: platform/partview_core/partview_service/partview_service/core/business/ocean_tracking/OceanConstants.py


> Auto-generated by Obscura crawlers

## Diagram 1

```mermaid
classDiagram
    class StringEnum
    class TransshipReferenceKey <<enumeration>>
    TransshipReferenceKey : +KEY = "code"
    TransshipReferenceKey : +VALUE = "T"
    StringEnum <|-- TransshipReferenceKey
    class Module <<module>>
    Module : +TRANSSHIP_MIN_PORT_TO_PORT_TRIPS:int = 2
    Module : +PORT_LAD_CODE:str = "P"
    Module : +is_ocean_feature(feature, org_id): bool
    class Auth <<utility>>
    Auth : +Feature.OCEAN_TRACKING
    class InvokeGetSolution <<utility>>
    InvokeGetSolution : +invoke_get_solution_by_org_and_feature(org_id, feature)
    Module ..> Auth : uses
    Module ..> InvokeGetSolution : calls
```

> SVG rendering failed for this diagram.

## Diagram 2

```mermaid
flowchart TD
    Start([is_ocean_feature(feature, org_id)])
    Start --> HasFeature{feature ?}
    HasFeature -- yes --> CompareFeature{str(feature).lower() == Auth.Feature.OCEAN_TRACKING.lower()}
    CompareFeature -- true --> ReturnTrue([return True])
    CompareFeature -- false --> ReturnFalse([return False])
    HasFeature -- no --> OrgIdNone{org_id is None?}
    OrgIdNone -- true --> RaiseError([raise ValueError: org_id is required when feature is not provided])
    OrgIdNone -- false --> InvokeSolution([InvokeGetSolution.invoke_get_solution_by_org_and_feature(org_id, Auth.Feature.OCEAN_TRACKING)])
    InvokeSolution --> CheckSolution{isinstance(solution, dict) and bool(solution.get('solution_id'))}
    CheckSolution -- true --> ReturnTrue2([return True])
    CheckSolution -- false --> ReturnFalse2([return False])
```

> SVG rendering failed for this diagram.
