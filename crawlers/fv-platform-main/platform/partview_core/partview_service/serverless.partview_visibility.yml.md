# Diagram: platform/partview_core/partview_service/serverless.partview_visibility.yml


> Auto-generated by Obscura crawlers

## Diagram 1

```mermaid
flowchart LR
  Client[Client]
  APIGW[API Gateway (HTTP)]
  subgraph Lambdas
    direction TB
    L1["trigger-post-visibility-grant-handler<br/>POST /api/visibility-grants/data"]
    L2["visibility-grant-handler<br/>GET/POST/PUT/PATCH/DELETE /api/visibility-grant"]
    L3["partview-dealer-onboarding-handler<br/>POST /api/partview-dealer-onboarding-upload"]
    L4["dealer_onboard_batch_upload_receiver<br/>SQS event (reservedConcurrency:10, timeout:900)"]
  end
  SQS["SQS: ${opt:stage}-PV-dealer-onboard.fifo"]
  S3["S3: ${opt:stage}-fv-csv-export"]
  SNS["SNS (Create/Publish)"]
  Secrets["Secrets Manager (GetSecretValue)"]
  Layers["Lambda Layers (fv_core_layers)"]
  IAM["IAM Role (managed policies + statements)"]
  VPC["VPC"]
  Datadog["Datadog env vars"]
  CloudWatch["CloudWatch Logs (Create/Put denied)"]

  Client --> APIGW
  APIGW --> L1
  APIGW --> L2
  APIGW --> L3
  SQS --> L4
  L4 --> S3
  L1 --> Secrets
  L2 --> Secrets
  L3 --> Secrets
  L1 --> SNS
  IAM --> L1
  IAM --> L2
  IAM --> L3
  IAM --> L4
  Layers --> L1
  Layers --> L2
  Layers --> L3
  Layers --> L4
  Datadog --> L1
  Datadog --> L2
  Datadog --> L3
  Datadog --> L4
  IAM -.->|Deny: logs:CreateLogGroup/CreateLogStream/PutLogEvents| CloudWatch
  VPC -.-> L1
  VPC -.-> L2
  VPC -.-> L3
  VPC -.-> L4
```

> SVG rendering failed for this diagram.

## Diagram 2

```mermaid
classDiagram
  class TriggerPostVisibilityGrantHandler {
    +handler: partview_service/api/visibility_grant/trigger_post_visibility_grant_handler.lambda_handler
    +event: POST /api/visibility-grants/data
    +env: POWERTOOLS_SERVICE_NAME
  }
  class VisibilityGrantHandler {
    +handler: partview_service/api/visibility_grant/visibility_grant_handler.lambda_handler
    +events: GET, POST, PUT, PATCH, DELETE /api/visibility-grant
    +env: POWERTOOLS_SERVICE_NAME
  }
  class PartviewDealerOnboardingHandler {
    +handler: partview_service/api/partview_dealer_onboard_upload/partview_dealer_onboard_upload_handler.lambda_handler
    +event: POST /api/partview-dealer-onboarding-upload
    +env: POWERTOOLS_SERVICE_NAME
  }
  class DealerOnboardBatchUploadReceiver {
    +handler: partview_service/api/partview_dealer_onboard_upload/dealer_onboard_batch_upload_receiver.lambda_handler
    +event: SQS ${opt:stage}-PV-dealer-onboard.fifo
    +reservedConcurrency: 10
    +timeout: 900
  }
  class S3Bucket {
    +arn: arn:aws:s3:::${opt:stage}-fv-csv-export/*
  }
  class SQSQueue {
    +arn: arn:aws:sqs:${aws:region}:${aws:accountId}:${opt:stage}-PV-dealer-onboard.fifo
  }
  class SNS {}
  class SecretsManager {}
  class IAMRole { +managedPolicies; +statements }
  class VPC {}
  class LambdaLayers {}
  class Datadog { +env_vars }
  class CloudWatch { +logs_denied }

  TriggerPostVisibilityGrantHandler --> SecretsManager
  VisibilityGrantHandler --> SecretsManager
  PartviewDealerOnboardingHandler --> SecretsManager
  DealerOnboardBatchUploadReceiver --> SQSQueue
  DealerOnboardBatchUploadReceiver --> S3Bucket
  TriggerPostVisibilityGrantHandler --> SNS
  TriggerPostVisibilityGrantHandler --> IAMRole
  VisibilityGrantHandler --> IAMRole
  PartviewDealerOnboardingHandler --> IAMRole
  DealerOnboardBatchUploadReceiver --> IAMRole
  TriggerPostVisibilityGrantHandler --> LambdaLayers
  VisibilityGrantHandler --> LambdaLayers
  PartviewDealerOnboardingHandler --> LambdaLayers
  DealerOnboardBatchUploadReceiver --> LambdaLayers
  TriggerPostVisibilityGrantHandler --> Datadog
  VisibilityGrantHandler --> Datadog
  PartviewDealerOnboardingHandler --> Datadog
  DealerOnboardBatchUploadReceiver --> Datadog
  IAMRole ..> CloudWatch : Deny logs
```

> SVG rendering failed for this diagram.
