# Diagram: devops/util/check_eof.sh


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
flowchart TD
  Start((Start)) --> ParseArgs[Parse arguments (while $# > 0)]
  ParseArgs --> OptCheck{Is $1 an option? (-* or --*)}
  OptCheck -->| -p / --prefix | SetPrefix[Set PREFIX = $2; shift 2]
  OptCheck -->| unknown option | Unknown[echo Uknown option $1; exit 1]
  OptCheck -->| positional arg | AddPos[Append to POSITIONAL_ARGS; shift]
  SetPrefix --> ParseArgs
  AddPos --> ParseArgs
  Unknown --> EndFail([Exit 1])
  ParseArgs --> ParseDone[Set positional args from POSITIONAL_ARGS]
  ParseDone --> LoopStart{Any positional args left?}
  LoopStart -->| yes | ForEach[Iterate over each var in "$@"]
  ForEach --> PrefixCheck{Is PREFIX set?}
  PrefixCheck -->| yes | Prepend[Set var = PREFIX/var]
  PrefixCheck -->| no | NoPrefix[Keep var unchanged]
  Prepend --> FileCheck
  NoPrefix --> FileCheck
  FileCheck{File non-empty (-s)?} -->| yes | TailCheck{Does tail -c 1 var yield empty after substitution?\n(i.e., newline at EOF)}
  TailCheck -->| yes | EchoYes[Print: Newline at end of file!]
  EchoYes --> LoopNext[Next var]
  TailCheck -->| no | EchoNo[Print: No newline at end of file $var; exit 1]
  EchoNo --> EndFail
  FileCheck -->| no | EchoNo
  LoopNext --> LoopStart
  LoopStart -->| no | EndSuccess([End - all files checked])
```

> SVG rendering failed for this diagram.
