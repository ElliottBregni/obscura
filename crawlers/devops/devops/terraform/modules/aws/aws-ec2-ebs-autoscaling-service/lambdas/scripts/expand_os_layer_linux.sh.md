# Diagram: devops/terraform/modules/aws/aws-ec2-ebs-autoscaling-service/lambdas/scripts/expand_os_layer_linux.sh


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
flowchart TD
  Start((Start)) --> ParseArgs{Parse CLI arguments}
  ParseArgs -->|--volume-size| SetVSize[VOLUME_SIZE = arg]
  ParseArgs -->|--volume-mount| SetVMount[VOLUME_MOUNT = arg]
  ParseArgs -->|unknown option| UnknownOpt[help() <br/> exit 1]
  ParseArgs --> PosArgs[Collect positional args]
  PosArgs --> Execute[execute()]
  Execute --> ExtendPartition[extend_partition()]
  ExtendPartition --> GetDeviceName[get_device_name()]
  ExtendPartition --> GetPartition[get_partition()]
  GetDeviceName --> GrowPart[sudo growpart /dev/deviceName partition]
  GetPartition --> GrowPart
  GrowPart -->|output starts with NOCHANGE| GP_NOCHANGE[echo NOCHANGE <br/> exit 0]
  GrowPart -->|growpart failed| GP_ERROR[echo growpart failed <br/> exit non-zero]
  GrowPart -->|success| ExtendFileSystem[extend_file_system()]
  ExtendFileSystem --> GetFS[get_file_system_type()]
  GetFS --> IsXFS{fs == xfs?}
  IsXFS -->|yes| XFS_GROWFS[sudo xfs_growfs -d VOLUME_MOUNT]
  XFS_GROWFS -->|output contains "Nothing to do"| XFS_NOCHANGE[echo NOCHANGE <br/> exit 0]
  XFS_GROWFS -->|command failed| XFS_ERROR[echo xfs_growfs failed <br/> exit non-zero]
  XFS_GROWFS -->|success| ExitOK[exit 0]
  IsXFS -->|no| IsEXT4{fs == ext4?}
  IsEXT4 -->|yes| GetDrive[get_drive()]
  GetDrive --> RESIZE2FS[sudo resize2fs /dev/device]
  RESIZE2FS -->|output contains "Nothing to do"| RESIZE_NOCHANGE[echo NOCHANGE <br/> exit 0]
  RESIZE2FS -->|command failed| RESIZE_ERROR[echo resize2fs failed <br/> exit non-zero]
  RESIZE2FS -->|success| ExitOK
  IsEXT4 -->|no| ExitOK
  GP_NOCHANGE --> ExitOK
  GP_ERROR --> ExitErr[exit non-zero]
  XFS_ERROR --> ExitErr
  RESIZE_ERROR --> ExitErr
```

> SVG rendering failed for this diagram.
