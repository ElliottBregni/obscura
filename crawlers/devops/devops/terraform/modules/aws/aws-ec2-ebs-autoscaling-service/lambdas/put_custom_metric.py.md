# Diagram: devops/terraform/modules/aws/aws-ec2-ebs-autoscaling-service/lambdas/put_custom_metric.py


> Auto-generated by Obscura crawlers

## Diagram 1

```mermaid
flowchart TD
    Event[Event] --> LambdaHandler[lambda_handler]
    LambdaHandler --> LogReceived[log: Received event]
    LambdaHandler --> ExtractFields[extract fields: metric_name, namespace, value, dimensions]
    ExtractFields --> PutMetric[put_metric(name, namespace, value, dimensions)]
    PutMetric --> CloudWatchClient[CloudWatch Client - boto3]
    CloudWatchClient --> PutMetricData[put_metric_data(Namespace, MetricData)]
    PutMetricData --> PutMetricReturn[returns metric_data]
    PutMetricReturn --> LambdaHandler
    LambdaHandler --> BuildResponse[build_response(event, data)]
    BuildResponse --> DateHandler[date_handler handles datetime -> isoformat]
    DateHandler --> BuildResponse
    BuildResponse --> Output[JSON response (statusCode, body)]
    LambdaHandler --> LogFinished[log: Processing finished]
    LambdaHandler --> Output
```

> SVG rendering failed for this diagram.

## Diagram 2

```mermaid
sequenceDiagram
    participant Event
    participant Lambda as lambda_handler
    participant Log as log
    participant PutMetric as put_metric
    participant CloudWatch as CloudWatchClient
    participant BuildResponse as build_response
    participant DateHandler as date_handler
    Event->>Lambda: invoke(event, context)
    Lambda->>Log: log("Received event: ...")
    Lambda->>PutMetric: put_metric(metric_name, namespace, value, dimensions)
    PutMetric->>CloudWatch: boto3.client('cloudwatch') ; put_metric_data(Namespace, MetricData)
    CloudWatch-->>PutMetric: put_metric_data response (success)
    PutMetric-->>Lambda: return { 'metric_data': [...] }
    Lambda->>BuildResponse: build_response(event, data)
    BuildResponse->>DateHandler: date_handler(datetime objects)
    DateHandler-->>BuildResponse: isoformat string
    BuildResponse-->>Lambda: JSON string (statusCode + body)
    Lambda->>Log: log("Processing finished: ...")
    Lambda-->>Event: return output
```

> SVG rendering failed for this diagram.
