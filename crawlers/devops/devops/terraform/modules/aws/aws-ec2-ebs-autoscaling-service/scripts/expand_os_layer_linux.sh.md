# Diagram: devops/terraform/modules/aws/aws-ec2-ebs-autoscaling-service/scripts/expand_os_layer_linux.sh


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
flowchart TD
  Start((Start))
  ParseArgs[Parse CLI args: --volume-size, --volume-mount, positional args]
  Execute[execute()]
  Start --> ParseArgs --> Execute

  Execute --> ExtendPartition[extend_partition()]
  ExtendPartition --> GetDeviceName[get_device_name()]
  ExtendPartition --> GetPartition[get_partition()]
  GetDeviceName --> RunGrowpart[run sudo growpart /dev/$device_name $partition]
  GetPartition --> RunGrowpart
  RunGrowpart --> GP_NOCHANGE{Output starts with NOCHANGE?}
  GP_NOCHANGE -- yes --> GP_NOCHANGE_ACTION[Echo NOCHANGE when extending partition and exit 0] --> EndOK((Exit 0))
  GP_NOCHANGE -- no --> GP_FAILED{growpart exit code != 0?}
  GP_FAILED -- yes --> GP_FAIL[Echo growpart failed and exit non-zero] --> EndErr((Exit non-zero))
  GP_FAILED -- no --> ExtendFileSystem[extend_file_system()]

  ExtendFileSystem --> GetFsType[get_file_system_type()]
  GetFsType --> FS_XFS{fs equals xfs?}
  FS_XFS -- yes --> RunXfs[run sudo xfs_growfs -d $VOLUME_MOUNT]
  RunXfs --> XFS_NOCHANGE{Output contains Nothing to do?}
  XFS_NOCHANGE -- yes --> XFS_NOCHANGE_ACTION[Echo NOCHANGE when extending xfs file system and exit 0] --> EndOK
  XFS_NOCHANGE -- no --> XFS_FAILED{exit code != 0?}
  XFS_FAILED -- yes --> XFS_FAIL[Echo xfs_growfs failed and exit non-zero] --> EndErr
  XFS_FAILED -- no --> EndOK

  FS_XFS -- no --> FS_EXT4{fs equals ext4?}
  FS_EXT4 -- yes --> GetDrive[get_drive()]
  GetDrive --> RunResize[run sudo resize2fs /dev/$drive]
  RunResize --> RESIZE_NOCHANGE{Output contains Nothing to do?}
  RESIZE_NOCHANGE -- yes --> RESIZE_NOCHANGE_ACTION[Echo NOCHANGE when extending ext4 file system and exit 0] --> EndOK
  RESIZE_NOCHANGE -- no --> RESIZE_FAILED{exit code != 0?}
  RESIZE_FAILED -- yes --> RESIZE_FAIL[Echo resize2fs failed and exit non-zero] --> EndErr
  RESIZE_FAILED -- no --> EndOK

  FS_EXT4 -- no --> EndOK
```

> SVG rendering failed for this diagram.
