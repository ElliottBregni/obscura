# Diagram: devops/terraform/modules/controller/controller-fv-vault/aws-lambdas/archive-file/templates/fetch_secret.sh


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
flowchart TD
  Start([Start]) --> Inputs[Inputs: $1 (secret-id), $2 (region), $3 (output_dir)]
  Inputs --> AWSCLI[AWS CLI: get-secret-value --secret-id $1 --region $2 --output text --query SecretString]
  AWSCLI --> SecretResult[secret_result (SecretString)]
  SecretResult --> JQ[jq -r '.vault_ca']
  JQ --> Base64[base64 -di]
  Base64 --> Write[Write file: $3/vault-ca.pem]
  Write --> End([End])
```

> SVG rendering failed for this diagram.
