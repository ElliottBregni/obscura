# Diagram: devops/terraform/modules/controller/controller-fv-kafka/ecs-akhq-task-definition/templates/akhq_task_definition_1.yaml


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
graph TD
  akhq["akhq"] --> connections["connections"]
  connections --> msk["${msk_name}"]
  msk --> properties["properties"]
  properties --> bs['bootstrap.servers: "${bootstrap_servers}"']
  properties --> sp['security.protocol: SASL_SSL']
  properties --> sm['sasl.mechanism: AWS_MSK_IAM']
  properties --> jaas['sasl.jaas.config: software.amazon.msk.auth.iam.IAMLoginModule required awsDebugCreds=true;']
  properties --> cb['sasl.client.callback.handler.class: software.amazon.msk.auth.iam.IAMClientCallbackHandler']
  msk --> sr['schema-registry']
  sr --> sru['url: "https://${schema_name}:${schema_port}/"']
```

> SVG rendering failed for this diagram.
