# Diagram: devops/terraform/modules/controller/controller-fv-kafka/update_mqtt_secrets.sh


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
flowchart TD
Start([Start]) --> ParseArgs[Parse CLI args: --environment, --project, --dry-run]
ParseArgs --> MainCall[main()]
MainCall --> LoadManifest[Load update_mqtt_secret_manifest.json]
LoadManifest --> ForEachSecret[For each secret in manifest (.secrets[])]
ForEachSecret --> CallProcess[Call process_secret(name,key)]

subgraph process_secret["process_secret(name, key)"]
  PS_Start[Start process_secret] --> GetActive[get_active_cluster(PROJECT_PATH)]
  GetActive --> CheckEks{eks_weight == green-100?}
  CheckEks -- yes --> ActiveGreen[active_cluster = green]
  CheckEks -- no --> ActiveBlue[active_cluster = blue]
  ActiveGreen --> DeployLoop
  ActiveBlue --> DeployLoop
  DeployLoop[Loop deployments: blue, green] --> BuildSecret[secret = ENVIRONMENT + "/" + secret_name + "-" + deployment]
  BuildSecret --> Retrieve[aws secretsmanager get-secret-value(secret)]
  Retrieve --> GotSecret{retrieval success?}
  GotSecret -- yes --> DetermineActive[is_active = (active_cluster == deployment) ? true : false]
  GotSecret -- no --> EnvLegacy{ENVIRONMENT ends with 1?}
  EnvLegacy -- yes --> LegacyTry[legacy_environment = ENVIRONMENT%?; secret = legacy_environment + "/" + secret_name + "-" + deployment]
  LegacyTry --> RetrieveLegacy[aws secretsmanager get-secret-value(secret)]
  RetrieveLegacy --> GotLegacy{retrieval success?}
  GotLegacy -- yes --> DetermineActive
  GotLegacy -- no --> ExitFail[exit 1]
  EnvLegacy -- no --> ExitFail
  DetermineActive --> UpdateJSON[updated_secret = jq set key=is_active]
  UpdateJSON --> DryRunCheck{DRY_RUN == false?}
  DryRunCheck -- yes --> AwsUpdate[aws secretsmanager update-secret --secret-id secret --secret-string updated_secret]
  AwsUpdate --> UpdateOk{update success?}
  UpdateOk -- yes --> SuccessMsg[echo Successfully updated secret]
  UpdateOk -- no --> ExitFail
  DryRunCheck -- no --> DryRunMsg[echo dry-run enabled; skipping update]
  DryRunMsg --> SuccessMsg
  SuccessMsg --> DeployNext{more deployments?}
  DeployNext -- yes --> DeployLoop
  DeployNext -- no --> PS_End([End process_secret])
end

CallProcess --> PS_Start
PS_End --> SecretNext{more secrets?}
SecretNext -- yes --> ForEachSecret
SecretNext -- no --> End([End / return 0])
```

> SVG rendering failed for this diagram.
