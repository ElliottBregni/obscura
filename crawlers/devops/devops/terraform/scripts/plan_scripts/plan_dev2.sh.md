# Diagram: devops/terraform/scripts/plan_scripts/plan_dev2.sh


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
flowchart TD
    Start([Start]) --> Init1[Set SCRIPT_DIR & SCRIPT_PARENT_DIR]
    Init1 --> Init2[Define color constants RED, GREEN, NC]
    Init2 --> Init3[SILENT = false]
    Init3 --> Init4[POSITIONAL_ARGS = []]
    Init4 --> ArgLoop{Arguments remaining?}
    ArgLoop -- Yes --> CaseSwitch[Switch on first argument]
    CaseSwitch -- -c/--cred --> SetCRED[CRED = next arg; shift 2]
    CaseSwitch -- -t/--target --> SetTARGET[TARGET_DIR = next arg; shift 2]
    CaseSwitch -- -s/--silent --> SetSILENT[SILENT = true; shift]
    CaseSwitch -- unknown option --> UnknownOpt[Echo unknown option and exit 1]
    CaseSwitch -- positional --> AddPos[Add arg to POSITIONAL_ARGS; shift]
    SetCRED --> ArgLoop
    SetTARGET --> ArgLoop
    SetSILENT --> ArgLoop
    AddPos --> ArgLoop
    UnknownOpt --> ExitErr([Exit 1])
    ArgLoop -- No --> SetPositional[Set positional parameters from POSITIONAL_ARGS]
    SetPositional --> CheckCred{CRED == dev?}
    CheckCred -- Yes --> DevFetch[Run python3 tf_db_secret_fetch.py dev all TARGET_DIR]
    DevFetch --> CheckDevRet{ret != 0?}
    CheckDevRet -- Yes --> DevFail[Echo Secret fetch Failed and exit 1]
    CheckDevRet -- No --> DevSilent{SILENT == false?}
    DevSilent -- Yes --> EchoDev[Echo Dev2-dev requirements Complete]
    DevSilent -- No --> EndOK([End])
    CheckCred -- No --> CheckProd{CRED == prod?}
    CheckProd -- Yes --> ProdRm[rm -f dev-platform-state.tfvars]
    ProdRm --> ProdRun[Run bash tf_state_outputs.sh -e dev2 -s platform -p aws -h import --silent]
    ProdRun --> CheckProdRet{ret != 0?}
    CheckProdRet -- Yes --> ProdFail([Exit 1])
    CheckProdRet -- No --> AppendState[Append dev1_platform_state from aws-dev-platform-state.json to ./dev-platform-state.tfvars]
    AppendState --> ProdSilent{SILENT == false?}
    ProdSilent -- Yes --> EchoProd[Echo Dev2-prod requirements Complete]
    ProdSilent -- No --> EndOK
    CheckProd -- No --> EndOK
```

> SVG rendering failed for this diagram.
