# Diagram: devops/terraform/scripts/plan_scripts/plan.vault.configuration.sh


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
flowchart TD
  Start([Start]) --> InitVars[Initialize variables:<br/>SCRIPT_DIR<br/>SCRIPT_PARENT_DIR<br/>Color constants (RED,GREEN,NC)<br/>SILENT=false]
  InitVars --> ArgLoop{Arguments remaining?}
  ArgLoop -->|Yes| Case{Argument type?}
  Case -->| -t / --target | SetTarget[Set TARGET_DIR and shift 2]
  Case -->| -s / --silent | SetSilent[Set SILENT=true and shift 1]
  Case -->| -* / --* | Unknown[Print unknown option and exit 1]
  Case -->| positional | AddPos[Append argument to POSITIONAL_ARGS and shift 1]
  SetTarget --> ArgLoop
  SetSilent --> ArgLoop
  AddPos --> ArgLoop
  Unknown --> Exit([Exit 1])
  ArgLoop -->|No| PostArgs[Rebuild positional parameters from POSITIONAL_ARGS]
  PostArgs --> TFVar[Define tfvar_file = vault-server-ami-ids.tfvars]
  TFVar --> Remove[Remove existing tfvar_file (rm -f)]
  Remove --> Describe[AWS: describe-instances filter tag Name=fv-vault-server]
  Describe --> Extract[Extract ImageId(s) using jq and tr]
  Extract --> ToJSON[Convert IDS lines to JSON array]
  ToJSON --> Write[Append "vault_server_ami_ids = JSON_IDS" to tfvar file]
  Write --> SilentCheck{SILENT == false?}
  SilentCheck -->|Yes| Echo[Print colored completion message: Vault Server AMI fetch: Complete]
  SilentCheck -->|No| End([End])
  Echo --> End
```

> SVG rendering failed for this diagram.
