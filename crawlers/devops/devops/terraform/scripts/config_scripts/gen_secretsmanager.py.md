# Diagram: devops/terraform/scripts/config_scripts/gen_secretsmanager.py


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
graph TD
Start[Start] --> Parse[Parse CLI args: --prefixs --env --full --no-write]
Parse --> EnvCheck{Is env provided?}
EnvCheck -- No --> PrintExit[Print Did not specify environment --env and exit]
EnvCheck -- Yes --> ListSecrets[Call AWS SecretsManager list_secrets with Filters=prefixs]
ListSecrets --> Loop[For each secret in response.SecretList]
Loop --> BuildSOut[Build s_out_dic: name, description, rotation_lambda_arn, kms_key_id, rotation_rules]
Loop --> BuildTemp[Build temp: keys: secret.Name, value: secret.ARN]
BuildSOut --> AppendOut[Append s_out_dic to secret_out_lis]
BuildTemp --> AppendImport[Append temp to secret_import_lis]
Loop --> RotationCheck{RotationLambdaARN present?}
RotationCheck -- Yes --> AppendRotation[Append temp to secret_rotation_import_lis]
AppendRotation --> AfterLoop
AppendImport --> AfterLoop
AppendOut --> AfterLoop
AfterLoop --> DecideFull{is_full?}
DecideFull -- True --> BuildOutFull[Create out with defaults, required_keys and resources using secret_out_lis]
DecideFull -- False --> BuildOutPartial[Create out with aws_secretsmanager_secret = secret_out_lis]
BuildOutFull --> PrintFound[Print Found N secrets]
BuildOutPartial --> PrintFound
PrintFound --> NoWriteCheck{no_write?}
NoWriteCheck -- False --> WriteFiles[Write tf_{env}_secretsmanager_RAW.json and imports JSONs to config_path]
NoWriteCheck -- True --> End[End]
WriteFiles --> End
```

> SVG rendering failed for this diagram.
