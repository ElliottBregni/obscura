# Diagram: devops/terraform/scripts/tf_process_plan.py


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
flowchart TD
    Start([Start]) --> ParseArgs["Parse command-line args<br/>(--path, --out)"]
    ParseArgs --> PathCheck{Path provided?}
    PathCheck --|No| PrintNoPath["Print 'No path specified'<br/>Exit"]
    PathCheck --|Yes| OpenFile["Open file at path<br/>(f = open(path, 'r'))"]
    OpenFile --> LoadJSON["Load JSON: data = json.load(f)"]
    LoadJSON --> ForEach["For each change in data['resource_changes']"]
    ForEach --> CheckCreate{Does change['change']['actions'] contain 'create'?}
    CheckCreate --|No| ForEach
    CheckCreate --|Yes| CheckType{Is change['type'] in<br/>['null_resource','time_sleep']?}
    CheckType --|Yes| ForEach
    CheckType --|No| Accumulate["out_dic[change['type']].append(change['name'])<br/>count_dic[change['type']] += 1"]
    Accumulate --> ForEach
    ForEach -->|end of loop| FinallyClose["finally: if f is not None: f.close()"]
    FinallyClose --> OutCheck{Is --out provided?}
    OutCheck --|Yes| WriteOut["Write count_dic to out as JSON<br/>with open(out, 'w+') as f: json.dump(count_dic, f, indent=4)"]
    OutCheck --|No| PrintCounts["pprint.pprint(count_dic)"]
    WriteOut --> End([End])
    PrintCounts --> End
    PrintNoPath --> End
```

> SVG rendering failed for this diagram.
