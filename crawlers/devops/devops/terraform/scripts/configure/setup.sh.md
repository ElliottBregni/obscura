# Diagram: devops/terraform/scripts/configure/setup.sh


> Auto-generated by Obscura crawlers

## Diagram 1

```mermaid
flowchart TD
    Start([Start]) --> Parse{Parse CLI args}
    Parse -->|--az-pfx-path| SetAz([Set AZURE_CERT_PATH])
    Parse -->|--region| SetRegion([Set REGION])
    Parse -->|positional| PosArgs([Collect positional args])
    Parse --> Execute([execute()])
    Execute --> ConfigureVars([configure_azure_variables()])
    ConfigureVars --> GetSecretConfig[AWS SecretsManager: get-secret-value terraform/azure/configuration]
    GetSecretConfig -->|success| InsertEnv1([Insert ARM_SUBSCRIPTION_ID, ARM_TENANT_ID, ARM_CLIENT_ID into /etc/profile])
    GetSecretConfig -->|failure| ErrorConfig([error() -> exit])
    InsertEnv1 --> SourceProfile1([source /etc/profile])
    SourceProfile1 --> ConfigureCert([configure_azure_certificate()])
    ConfigureCert --> GetSecretCert[AWS SecretsManager: get-secret-value terraform/azure/certificate]
    GetSecretCert -->|success| InsertEnv2([Insert ARM_CLIENT_CERTIFICATE_PATH and ARM_CLIENT_CERTIFICATE_PASSWORD into /etc/profile])
    GetSecretCert -->|failure| ErrorCert([error() -> exit])
    InsertEnv2 --> SourceProfile2([source /etc/profile])
    SourceProfile2 --> WritePfx([Create cert dir and write pfx to ARM_CLIENT_CERTIFICATE_PATH])
    WritePfx --> ConfigureSettings([configure_azure_settings()])
    ConfigureSettings --> UpdateResolv([Write nameservers to /etc/resolv.conf])
    UpdateResolv --> Done([Complete])
    ErrorConfig --> Exit([Exit])
    ErrorCert --> Exit
```

> SVG rendering failed for this diagram.

## Diagram 2

```mermaid
sequenceDiagram
    participant Script
    participant AWS as "AWS SecretsManager"
    participant JQ as "jq"
    participant SUDO as "sudo/tee"
    participant Shell as "shell (/etc/profile)"
    Script->>AWS: get-secret-value terraform/azure/configuration
    alt secret found
        AWS-->>Script: SecretString (JSON)
        Script->>JQ: jq -r '.subscription_id' '.tenant_id' '.client_id'
        JQ-->>Script: values
        Script->>SUDO: append export VARs to /etc/profile (sudo tee -a)
        SUDO-->>Script: OK
        Script->>Shell: source /etc/profile
        Shell-->>Script: OK
    else secret missing
        AWS-->>Script: error
        Script-->>Script: error() -> exit
    end
    Script->>AWS: get-secret-value terraform/azure/certificate
    alt secret found
        AWS-->>Script: SecretString (pfx, password)
        Script->>JQ: jq -r '.password' and jq -r '.pfx'
        JQ-->>Script: password / base64 pfx
        Script->>SUDO: mkdir -p cert_dir; write pfx file; insert exports into /etc/profile
        SUDO-->>Script: OK
        Script->>Shell: source /etc/profile
        Shell-->>Script: OK
    else secret missing
        AWS-->>Script: error
        Script-->>Script: error() -> exit
    end
    Script->>SUDO: write nameserver entries to /etc/resolv.conf
    SUDO-->>Script: OK
    Script-->>Script: Done
```

> SVG rendering failed for this diagram.
