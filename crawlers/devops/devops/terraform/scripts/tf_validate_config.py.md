# Diagram: devops/terraform/scripts/tf_validate_config.py


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
flowchart LR
  Start((Start)) --> ResolvePath[/"Resolve script location and config_path"/]
  ResolvePath --> Walk[/"Walk config directory (os.walk)"/]
  Walk --> CheckBlockedDir{Is root in blocked_dir?}
  CheckBlockedDir -- yes --> SkipDir[/Skip directory/]
  CheckBlockedDir -- no --> ForFiles[/For each file in directory/]
  SkipDir --> ContinueWalk[/"Continue walking"/]
  ForFiles --> CheckBlockedName{Filename contains blocked_name?}
  CheckBlockedName -- yes --> SkipFile[/Skip file/]
  CheckBlockedName -- no --> OpenFile[/"Open file and read JSON"/]
  SkipFile --> ContinueWalk
  OpenFile --> ParseJSON{JSON parse OK?}
  ParseJSON -- no --> ExitInvalid1[/Exit: "Invalid config. Missing required keys."/]
  ParseJSON -- yes --> HasRequiredKeys{Has 'required_keys'?}
  HasRequiredKeys -- no --> ExitInvalid2[/Exit: "Invalid config. Missing required keys."/]
  HasRequiredKeys -- yes --> HasResources{Has 'resources'?}
  HasResources -- no --> ExitInvalid3[/Exit: "Invalid config. Missing resources config."/]
  HasResources -- yes --> ValidateResources[/Try validating resources (dict case)/]
  ValidateResources --> ResourcesDictOK{Dict-of-dicts format?}
  ResourcesDictOK -- yes --> CheckSubset1[/Check required_keys subset of resource keys/]
  ResourcesDictOK -- no --> FallbackList[/Fallback: assume list-of-dicts format/]
  FallbackList --> CheckSubset2[/Check required_keys subset of resource keys in list/]
  CheckSubset1 --> ValidOK{All resources valid?}
  CheckSubset2 --> ValidOK
  ValidOK -- no --> ExitInvalid4[/Exit: "Invalid config: <filename>"/]
  ValidOK -- yes --> CloseFile[/"Close file and continue"/]
  CloseFile --> ContinueWalk
  ContinueWalk --> Walk
  Walk --> End((End - all configs validated))
```

> SVG rendering failed for this diagram.
