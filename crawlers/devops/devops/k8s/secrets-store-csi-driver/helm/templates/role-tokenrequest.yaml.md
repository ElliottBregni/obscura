# Diagram: devops/k8s/secrets-store-csi-driver/helm/templates/role-tokenrequest.yaml


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
flowchart TD
A[Condition: .Values.tokenRequests?] -->|true| B[Create ClusterRole: secretprovidertokenrequest-role]
A -->|false| I[No resource rendered]
B --> C[metadata.name: secretprovidertokenrequest-role]
B --> D[labels: include "sscd.labels"]
B --> E[rules]
E --> F[apiGroups: ""]
E --> G[resources: serviceaccounts/token]
E --> H[verbs: create]
```

> SVG rendering failed for this diagram.
