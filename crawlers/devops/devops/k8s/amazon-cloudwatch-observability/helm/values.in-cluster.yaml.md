# Diagram: devops/k8s/amazon-cloudwatch-observability/helm/values.in-cluster.yaml


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
flowchart LR
  subgraph Inputs
    A1[Tail: /var/log/containers/*.log\nDB:/var/fluent-bit/state/flb_container.db]
    A2[Tail: /var/log/containers/fluent-bit*\nDB:/var/fluent-bit/state/flb_log.db]
    A3[Tail: /var/log/containers/cloudwatch-agent*\nDB:/var/fluent-bit/state/flb_cwagent.db]
  end

  subgraph MainPipeline["Main application pipeline (Tag: application.*)"]
    direction TB
    F1[AWS filter\n(Name: aws)]
    F2[Kubernetes filter\n(Name: kubernetes)]
    F3[Rewrite Tag\n(Name: rewrite_tag)\nRule: "Circular dependency detected" → circular.$TAG]
    F4[Grep filter\n(Exclude: Circular dependency detected)]
  end

  subgraph CircularPipeline["Circular logs (Tag: circular.*)"]
    direction TB
    T1[Emitter: circular_emitter\nStorage: memory]
    T2[Throttle filter\n(Rate 5 / Window 60s)]
  end

  subgraph Outputs
    O1[CloudWatch Logs Output\nMatch: application.*\nLogGroup: /aws/containerinsights/${CLUSTER_NAME}/application]
    O2[CloudWatch Logs Output\nMatch: circular.*\nLogGroup: /aws/containerinsights/${CLUSTER_NAME}/application]
  end

  A1 -->|application.*| F1
  A2 -->|application.*| F1
  A3 -->|application.*| F1

  F1 --> F2
  F2 --> F3
  F3 -->|emit circular.*| T1
  F3 -->|continue application.*| F4
  T1 --> T2
  T2 --> O2
  F4 --> O1

  style Inputs fill:#f8f9fa,stroke:#333,stroke-width:1px
  style MainPipeline fill:#eef7ff,stroke:#1f77b4,stroke-width:1px
  style CircularPipeline fill:#fff4e6,stroke:#ff7f0e,stroke-width:1px
  style Outputs fill:#f4fff0,stroke:#2ca02c,stroke-width:1px
```

> SVG rendering failed for this diagram.
