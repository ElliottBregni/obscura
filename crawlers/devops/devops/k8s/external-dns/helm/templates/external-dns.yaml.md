# Diagram: devops/k8s/external-dns/helm/templates/external-dns.yaml


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
flowchart TD
  SA[ServiceAccount: {{ .Release.Name }}-external-dns]
  CR[ClusterRole: {{ .Release.Name }}-external-dns]
  CRB[ClusterRoleBinding: {{ .Release.Name }}-external-dns-viewer]
  DEP[Deployment: {{ .Release.Name }}-external-dns]
  CNT[Container: external-dns]
  ENV[AWS_DEFAULT_REGION={{ .Values.region }}]
  ARGS[Args: provider=aws, policy=upsert-only, zone=public, registry=txt, txt-owner-id={{ .Values.hostedzone.id }}]
  SOURCES[Sources: service, ingress]
  FILTERS[Filters: domain={{ .Values.domain }}, annotation={{ .Values.annotationFilter }}]
  SECURITY[securityContext: fsGroup=65534]
  REVISION[revisionHistoryLimit={{ .Values.extraArgs.revisionHistoryLimit }}]

  SA -->|serviceAccountName| DEP
  DEP --> CNT
  CNT --> ENV
  CNT --> ARGS
  CNT --> SOURCES
  CNT --> FILTERS
  DEP --> SECURITY
  DEP --> REVISION

  CRB -->|roleRef -> ClusterRole| CR
  CRB -->|subjects -> ServiceAccount| SA
  CR -->|verbs: get,watch,list on resources| DEP
  CR -->|resources: services,endpoints,pods,nodes,ingresses| DEP
```

> SVG rendering failed for this diagram.
