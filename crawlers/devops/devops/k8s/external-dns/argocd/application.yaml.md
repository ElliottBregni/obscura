# Diagram: devops/k8s/external-dns/argocd/application.yaml


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
flowchart TD
  A[Application: ${name_prefix}-external-dns] --> B[metadata]
  A --> C[spec]

  B --> B1[name: ${name_prefix}-external-dns]
  B --> B2[labels]
  B2 --> B2a[environment: ${env}]

  C --> D[destination]
  C --> E[source]
  C --> F[project: ${env}-services]
  C --> G[syncPolicy]

  D --> D1[name: '']
  D --> D2[namespace: kube-system]
  D --> D3[server: ${cluster_endpoint}]

  E --> E1[path: devops/k8s/external-dns/helm]
  E --> E2[repoURL: https://gitlab.com/freightverify-nextgen/devops.git]
  E --> E3[targetRevision: ${revision}]
  E --> E4[helm]
  E4 --> E4a[valueFiles]
  E4a --> E4a1[values.yaml]

  G --> G1[automated: {}]
  G --> G2[syncOptions]
  G2 --> G2a[CreateNamespace=true]
```

> SVG rendering failed for this diagram.
