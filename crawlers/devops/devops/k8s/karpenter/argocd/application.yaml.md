# Diagram: devops/k8s/karpenter/argocd/application.yaml


> Auto-generated by Obscura crawlers

## Diagram 1

```mermaid
graph TD
    A[ArgoCD Application: ${name_prefix}-karpenter] --> B[Destination]
    A --> C[Source]
    A --> D[Project: ${env}-services]
    A --> E[Sync Policy]
    
    B --> B1[Namespace: karpenter]
    B --> B2[Server: ${cluster_endpoint}]
    
    C --> C1[Repo: GitLab]
    C --> C2[Path: devops/k8s/karpenter/helm]
    C --> C3[Revision: ${revision}]
    C --> C4[Helm Configuration]
    
    C4 --> C4a[Values File: values.yaml]
    C4 --> C4b[Parameter: cluster.name]
    C4 --> C4c[Parameter: settings.clusterName]
    
    E --> E1[Automated Sync]
    E --> E2[Create Namespace]
```

> SVG rendering failed for this diagram.

## Diagram 2

```mermaid
flowchart LR
    subgraph Metadata
        name["name: ${name_prefix}-karpenter"]
        env["environment: ${env}"]
    end
    
    subgraph Deployment
        ns[Namespace: karpenter]
        cluster[Cluster: ${cluster_endpoint}]
    end
    
    subgraph GitSource
        repo[GitLab Repository]
        path[devops/k8s/karpenter/helm]
        rev[Revision: ${revision}]
    end
    
    subgraph HelmConfig
        values[values.yaml]
        param1[cluster.name: ${cluster_name}]
        param2[settings.clusterName: ${cluster_name}]
    end
    
    Metadata --> Deployment
    GitSource --> HelmConfig
    HelmConfig --> Deployment
```

> SVG rendering failed for this diagram.
