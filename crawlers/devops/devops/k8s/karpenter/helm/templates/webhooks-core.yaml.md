# Diagram: devops/k8s/karpenter/helm/templates/webhooks-core.yaml


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
flowchart LR
  A[Conditional: .Values.webhook.enabled] --> B[ValidatingWebhookConfiguration: validation.webhook.karpenter.sh]
  A --> C[ValidatingWebhookConfiguration: validation.webhook.config.karpenter.sh]

  subgraph Webhook1 [validation.webhook.karpenter.sh]
    W1_name["name: validation.webhook.karpenter.sh"]
    W1_review["admissionReviewVersions: [v1]"]
    W1_client["clientConfig.service:\n - name: {{ include \"karpenter.fullname\" . }}\n - namespace: {{ .Release.Namespace }}\n - port: {{ .Values.webhook.port }}"]
    W1_policy["failurePolicy: Fail"]
    W1_side["sideEffects: None"]
    W1_rules["rules:"]
    R1["- apiGroup: karpenter.sh\n  apiVersion: v1beta1\n  operations: [CREATE, UPDATE]\n  resources: [nodeclaims, nodeclaims/status]\n  scope: '*'\n- apiGroup: karpenter.sh\n  apiVersion: v1beta1\n  operations: [CREATE, UPDATE]\n  resources: [nodepools, nodepools/status]\n  scope: '*'"]
  end

  subgraph Webhook2 [validation.webhook.config.karpenter.sh]
    W2_name["name: validation.webhook.config.karpenter.sh"]
    W2_review["admissionReviewVersions: [v1]"]
    W2_client["clientConfig.service:\n - name: {{ include \"karpenter.fullname\" . }}\n - namespace: {{ .Release.Namespace }}\n - port: {{ .Values.webhook.port }}"]
    W2_policy["failurePolicy: Fail"]
    W2_side["sideEffects: None"]
    W2_objectSelector["objectSelector:\n matchLabels:\n  app.kubernetes.io/part-of: {{ template \"karpenter.name\" . }}"]
  end

  B --> Webhook1
  C --> Webhook2

  W1_name --> W1_review
  W1_review --> W1_client
  W1_client --> W1_policy
  W1_policy --> W1_side
  W1_side --> W1_rules
  W1_rules --> R1

  W2_name --> W2_review
  W2_review --> W2_client
  W2_client --> W2_policy
  W2_policy --> W2_side
  W2_side --> W2_objectSelector
```

> SVG rendering failed for this diagram.
