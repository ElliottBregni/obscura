# Diagram: devops/k8s/keda/helm/values.yaml


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
classDiagram
    class Global {
        +image.registry
    }
    class Image {
        +keda.repository
        +metricsApiServer.repository
        +webhooks.repository
        +pullPolicy
    }
    class Operator {
        +name
        +replicaCount
        +revisionHistoryLimit
        +disableCompression
    }
    class MetricsServer {
        +replicaCount
        +dnsPolicy
        +useHostNetwork
    }
    class Webhooks {
        +enabled
        +port
        +healthProbePort
        +name
    }
    class Resources {
        +operator.limits
        +metricServer.limits
        +webhooks.limits
    }
    class Prometheus {
        +operator.enabled
        +metricServer.enabled
        +webhooks.enabled
    }
    class Certificates {
        +autoGenerated
        +secretName
        +mountPath
    }
    class Security {
        +securityContext.operator
        +podSecurityContext.operator
    }
    class Service {
        +type
        +portHttps
    }
    Global --> Image : contains
    Image --> Operator : provides images to
    Image --> MetricsServer : provides images to
    Image --> Webhooks : provides images to
    Operator --> Resources : uses
    MetricsServer --> Resources : uses
    Webhooks --> Resources : uses
    Operator --> Prometheus : exposes metrics
    MetricsServer --> Prometheus : exposes metrics
    Webhooks --> Prometheus : exposes metrics
    Certificates --> Operator : mounted to
    Security --> Operator : applied to
    Service --> MetricsServer : serves port
    classClusterConfig {
        +clusterName
        +clusterDomain
        +watchNamespace
        +imagePullSecrets
        +rbac.create
        +serviceAccount.name
    }
    classClusterConfig --> Operator : configures
    classClusterConfig --> MetricsServer : configures
    classClusterConfig --> Webhooks : configures
```

> SVG rendering failed for this diagram.
