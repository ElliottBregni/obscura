# Diagram: devops/k8s/keda/helm/templates/crds/crd-clustertriggerauthentications.yaml


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
classDiagram
    ClusterTriggerAuthentication "1" --o "1" Spec
    ClusterTriggerAuthentication "1" --o "1" Status

    class ClusterTriggerAuthentication {
      +apiVersion: string
      +kind: string
      +metadata: object
    }

    class Spec {
      +configMapTargetRef: ConfigMapTargetRef[]
      +env: Env[]
      +secretTargetRef: SecretTargetRef[]
      +podIdentity: PodIdentity
      +awsSecretManager: AwsSecretManager
      +azureKeyVault: AzureKeyVault
      +gcpSecretManager: GcpSecretManager
      +hashiCorpVault: HashiCorpVault
      +region: string
    }

    class Status {
      +scaledobjects: string
      +scaledjobs: string
    }

    Spec o-- AwsSecretManager
    Spec o-- AzureKeyVault
    Spec o-- GcpSecretManager
    Spec o-- HashiCorpVault
    Spec o-- PodIdentity
    Spec o-- SecretTargetRef
    Spec o-- Env
    Spec o-- ConfigMapTargetRef

    class AwsSecretManager {
      +secrets: Secret[]
      +credentials: AwsCredentials
      +podIdentity: PodIdentity
      +region: string
    }

    class AwsCredentials {
      +accessKey: SecretValue
      +accessSecretKey: SecretValue
      +accessToken: SecretValue
    }

    class SecretValue {
      +valueFrom: SecretKeyRefHolder
    }

    class SecretKeyRefHolder {
      +secretKeyRef: SecretKeyRef
    }

    class SecretKeyRef {
      +name: string
      +key: string
    }

    class AzureKeyVault {
      +vaultUri: string
      +secrets: Secret[]
      +credentials: AzureCredentials
      +podIdentity: PodIdentity
      +cloud: CloudInfo
    }

    class AzureCredentials {
      +clientId: string
      +clientSecret: SecretValue
      +tenantId: string
    }

    class CloudInfo {
      +type: string
      +activeDirectory
```

> SVG rendering failed for this diagram.
