# Diagram: devops/k8s/keda/argocd/application.yaml


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
classDiagram
class Application {
  +apiVersion: "argoproj.io/v1alpha1"
  +kind: "Application"
  +project: string
  +sources: []
}
class Metadata {
  +name: string
  +labels.environment: string
}
class Spec {
  +destination: Destination
  +source: Source
  +project: string
  +syncPolicy: SyncPolicy
}
class Destination {
  +name: string
  +namespace: string
  +server: string
}
class Source {
  +path: string
  +repoURL: string
  +targetRevision: string
  +helm: Helm
}
class Helm {
  +valueFiles: list
}
class SyncPolicy {
  +automated: object
  +syncOptions: list
}
Application --> Metadata : metadata
Application --> Spec : spec
Spec --> Destination : destination
Spec --> Source : source
Spec --> SyncPolicy : syncPolicy
Source --> Helm : helm
Helm : valueFiles = ["values.yaml"]
Metadata : name = "${name_prefix}-keda"
Metadata : labels.environment = "${env}"
Destination : namespace = "keda"
Destination : server = "${cluster_endpoint}"
Source : path = "devops/k8s/keda/helm"
Source : repoURL = "https://gitlab.com/freightverify-nextgen/devops.git"
Source : targetRevision = "${revision}"
SyncPolicy : syncOptions = ["CreateNamespace=true","ServerSideApply=true"]
```

> SVG rendering failed for this diagram.
