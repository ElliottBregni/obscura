# Diagram: devops/k8s/aws-load-balancer-controller/helm/templates/service.yaml


> Auto-generated by Obscura crawlers

## Diagram 1

```mermaid
flowchart LR
  ns[Namespace: {{ .Release.Namespace }}]
  metricsSvc[Service: {{ template "aws-load-balancer-controller.fullname" . }}<br/>Port: 8080 (metrics-server)<br/>targetPort: metrics-server]
  webhookSvc[Service: {{ template "aws-load-balancer-controller.webhookService" . }}<br/>Port: 443 (webhook-server)<br/>targetPort: webhook-server<br/>labels: app.kubernetes.io/component=webhook<br/>prometheus.io/service-monitor="false"]
  selector[Selector: {{ include "aws-load-balancer-controller.selectorLabels" . }}]
  pods[Pods matched by selectorLabels]
  ns -.-> metricsSvc
  ns --> webhookSvc
  metricsSvc --> selector
  webhookSvc --> selector
  selector --> pods
```

> SVG rendering failed for this diagram.

## Diagram 2

```mermaid
classDiagram
  class Service {
    +apiVersion: v1
    +kind: Service
    +metadata.name
    +metadata.namespace
    +metadata.annotations
    +metadata.labels
    +spec.ports
    +spec.selector
  }
  class MetricsService {
    +condition: .Values.serviceMonitor.enabled
    +name: {{ template "aws-load-balancer-controller.fullname" . }}
    +port: 8080
    +portName: metrics-server
    +targetPort: metrics-server
    +annotations: (optional) .Values.serviceAnnotations
  }
  class WebhookService {
    +name: {{ template "aws-load-balancer-controller.webhookService" . }}
    +port: 443
    +portName: webhook-server
    +targetPort: webhook-server
    +labels: app.kubernetes.io/component=webhook; prometheus.io/service-monitor="false"
    +annotations: .Values.serviceAnnotations
  }
  Service <|-- MetricsService
  Service <|-- WebhookService
```

> SVG rendering failed for this diagram.
