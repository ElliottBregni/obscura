# Diagram: devops/k8s/aws-load-balancer-controller/helm/templates/hpa.yaml


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
flowchart TD
  Start(Helm Template) --> Enabled{.Values.autoscaling.enabled?}
  Enabled -- "no" --> NoHPA[No HorizontalPodAutoscaler generated]
  Enabled -- "yes" --> KubeVer{semverCompare ">=1.23-0" .Capabilities.KubeVersion.GitVersion?}
  KubeVer -- "yes" --> ApiV2[apiVersion: autoscaling/v2]
  KubeVer -- "no" --> ApiV2beta2[apiVersion: autoscaling/v2beta2]
  ApiV2 --> HPA[HorizontalPodAutoscaler]
  ApiV2beta2 --> HPA
  HPA --> Metadata[metadata]
  Metadata --> Name[name: include "aws-load-balancer-controller.fullname"]
  Metadata --> Namespace[namespace: .Release.Namespace]
  Metadata --> Labels[labels: include "aws-load-balancer-controller.labels"]
  Metadata --> Annotations[annotations: .Values.annotations]
  HPA --> Spec[spec]
  Spec --> ScaleTargetRef[scaleTargetRef: apiVersion: apps/v1 / kind: Deployment / name: include "aws-load-balancer-controller.fullname"]
  Spec --> MinReplicas[minReplicas: .Values.autoscaling.minReplicas]
  Spec --> MaxReplicas[maxReplicas: .Values.autoscaling.maxReplicas (required)]
  Spec --> CPUCond{.Values.autoscaling.targetCPUUtilizationPercentage?}
  CPUCond -- "yes" --> Metrics[metrics: - type: Resource → resource.name: cpu → target.averageUtilization: value, type: Utilization]
  CPUCond -- "no" --> NoMetrics[metrics: none]
  Spec --> BehaviorCond{.Values.autoscaling.autoscaleBehavior?}
  BehaviorCond -- "yes" --> Behavior[behavior: .Values.autoscaling.autoscaleBehavior]
  BehaviorCond -- "no" --> NoBehavior[behavior: none]
```

> SVG rendering failed for this diagram.
