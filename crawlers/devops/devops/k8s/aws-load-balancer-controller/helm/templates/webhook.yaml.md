# Diagram: devops/k8s/aws-load-balancer-controller/helm/templates/webhook.yaml


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
flowchart LR
  subgraph MutatingWebhookConfiguration
    MWC[MutatingWebhookConfiguration: namePrefix-webhook]
    MPOD[mpod.elbv2.k8s.aws]
    MWC --> MPOD
    MPOD --> MPOD_client[clientConfig]
    MPOD_client --> MPOD_service[service: webhookService\nnamespace: Release.Namespace\npath: /mutate-v1-pod]
    MPOD --> MPOD_ca[caBundle: $tls.caCert (when no cert-manager)]
    MPOD --> MPOD_failure[failurePolicy: Fail]
    MPOD --> MPOD_adm[admissionReviewVersions: v1beta1]
    MPOD --> MPOD_namespace[namespaceSelector: matchExpressions (default or provided)]
    MPOD --> MPOD_object[objectSelector: matchExpressions NotIn app.kubernetes.io/name]
    MPOD --> MPOD_rules[Rules:\napiGroups:(core)\napiVersions:v1\nops:CREATE\nresources:pods]
    MPOD --> MPOD_side[sideEffects: None]

    MSVC[mservice.elbv2.k8s.aws]
    MWC --> MSVC
    MSVC --> MSVC_client[clientConfig]
    MSVC_client --> MSVC_service[service: webhookService\npath: /mutate-v1-service]
    MSVC --> MSVC_rules[Rules:\napiGroups:(core)\napiVersions:v1\nops:CREATE\nresources:services]
    MSVC --> MSVC_failure[failurePolicy: Fail]
    MSVC --> MSVC_adm[admissionReviewVersions: v1beta1]

    MTG[mtargetgroupbinding.elbv2.k8s.aws]
    MWC --> MTG
    MTG --> MTG_client[clientConfig]
    MTG_client --> MTG_service[service: webhookService\npath: /mutate-elbv2-k8s-aws-v1beta1-targetgroupbinding]
    MTG --> MTG_rules[Rules:\napiGroups:elbv2.k8s.aws\napiVersions:v1beta1\nops:CREATE, UPDATE\nresources:targetgroupbindings]
    MTG --> MTG_failure[failurePolicy: Fail]
    MTG --> MTG_adm[admissionReviewVersions: v1beta1]
  end

  subgraph ValidatingWebhookConfiguration
    VWC[ValidatingWebhookConfiguration: namePrefix-webhook]
    VWC --> V1[vingressclassparams.elbv2.k8s.aws]
    V1 --> V1_client[clientConfig]
    V1_client --> V1_service[service: webhookService\npath: /validate-elbv2-k8s-aws-v1beta1-ingressclassparams]
    V1 --> V1_rules[Rules:\napiGroups:elbv2.k8s.aws\napiVersions:v1beta1\nops:CREATE, UPDATE\nresources:ingressclassparams]
    V1 --> V1_failure[failurePolicy: Fail]
    V1 --> V1_adm[admissionReviewVersions: v1beta1]

    VWC --> V2[vtargetgroupbinding.elbv2.k8s.aws]
    V2 --> V2_client[clientConfig]
    V2_client --> V2_service[service: webhookService\npath: /validate-elbv2-k8s-aws-v1beta1-targetgroupbinding]
    V2 --> V2_rules[Rules:\napiGroups:elbv2.k8s.aws\napiVersions:v1beta1\nops:CREATE, UPDATE\nresources:targetgroupbindings]
    V2 --> V2_failure[failurePolicy: Fail]
    V2 --> V2_adm[admissionReviewVersions: v1beta1]

    VWC --> V3[vingress.elbv2.k8s.aws]
    V3 --> V3_client[clientConfig]
    V3_client --> V3_service[service: webhookService\npath: /validate-networking-v1-ingress]
    V3 --> V3_rules[Rules:\napiGroups:networking.k8s.io\napiVersions:v1\nops:CREATE, UPDATE\nresources:ingresses]
    V3 --> V3_failure[failurePolicy: Fail]
    V3 --> V3_match[matchPolicy: Equivalent]
    V3 --> V3_adm[admissionReviewVersions: v1beta1]
  end

  subgraph Certificates_and_Secrets
    S[Secret: webhookCertSecret (kubernetes.io/tls)]
    S --> CA[ca.crt: $tls.caCert]
    S --> TLSCRT[tls.crt: $tls.clientCert]
    S --> TLSKEY[tls.key: $tls.clientKey]

    C[Certificate: namePrefix-serving-cert (cert-manager)]
    C --> DNS1[DNS: webhookService.Release.Namespace.svc]
    C --> DNS2[DNS: webhookService.Release.Namespace.svc.cluster.dnsDomain]
    C --> ISS[Issuer: namePrefix-selfsigned-issuer (selfSigned)]
  end

  MPOD_client -.-> S
  MSVC_client -.-> S
  MTG_client -.-> S
  V1_client -.-> S
  V2_client -.-> S
  V3_client -.-> S
  C --> S
```

> SVG rendering failed for this diagram.
