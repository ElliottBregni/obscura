# Diagram: devops/k8s/aws-load-balancer-controller/helm/templates/serviceaccount.yaml


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
flowchart TD
  A[Check .Values.serviceAccount.create] -->|true| B[Render ServiceAccount YAML]
  A -->|false| Z[No ServiceAccount created]
  B --> C[apiVersion: v1]
  B --> D[kind: ServiceAccount]
  B --> E[metadata]
  E --> F[name: include aws-load-balancer-controller.serviceAccountName]
  E --> G[namespace: .Release.Namespace]
  E --> H[labels: include aws-load-balancer-controller.labels]
  E --> I{annotations present?}
  I -->|yes| J[annotations: range $.Values.serviceAccount.annotations]
  J --> K[key: value (quoted)]
  B --> L[automountServiceAccountToken: .Values.serviceAccount.automountServiceAccountToken]
  B --> M{imagePullSecrets present?}
  M -->|yes| N[imagePullSecrets: toYaml .]
```

> SVG rendering failed for this diagram.
