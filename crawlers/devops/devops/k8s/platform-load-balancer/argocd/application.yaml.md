# Diagram: devops/k8s/platform-load-balancer/argocd/application.yaml


> Auto-generated by Obscura crawlers

## Diagram 1

```mermaid
graph TD
    A[ArgoCD Application] -->|defines| B[Metadata]
    A -->|defines| C[Spec]
    B -->|contains| D[Name: ${name_prefix}-platform-load-balancer]
    B -->|contains| E[Labels: environment=${env}]
    C -->|defines| F[Destination]
    C -->|defines| G[Source]
    C -->|defines| H[Project: ${env}-platform]
    C -->|defines| I[Sync Policy]
    F -->|targets| J[Cluster: ${cluster_endpoint}]
    F -->|namespace| K[default]
    G -->|repository| L[GitLab DevOps Repo]
    G -->|path| M[devops/k8s/platform-load-balancer/helm]
    G -->|revision| N[${revision}]
    G -->|contains| O[Helm Configuration]
    O -->|uses| P[values.yaml]
    O -->|parameters| Q[Load Balancer Name]
    O -->|parameters| R[Ingress Hosts]
    O -->|parameters| S[Deployment Name]
    R -->|host 1| T[k8s.${legacy_env}.freightverify.com]
    R -->|host 2| U[k8s-${deploy_name}.${legacy_env}.freightverify.com]
    R -->|host 3| V[api.${legacy_env}.freightverify.com]
    I -->|enables| W[Automated Sync]
    I -->|option| X[CreateNamespace=true]
```

> SVG rendering failed for this diagram.

## Diagram 2

```mermaid
flowchart LR
    subgraph ArgoCD
        APP[Application Resource]
    end
    
    subgraph GitLab
        REPO[DevOps Repository]
        HELM[Helm Chart]
    end
    
    subgraph Kubernetes
        NS[default Namespace]
        LB[Load Balancer]
        ING[Ingress]
    end
    
    APP -->|pulls from| REPO
    REPO -->|contains| HELM
    APP -->|deploys to| NS
    HELM -->|creates| LB
    HELM -->|creates| ING
    ING -->|routes| HOST1[k8s.${legacy_env}.freightverify.com]
    ING -->|routes| HOST2[k8s-${deploy_name}.${legacy_env}.freightverify.com]
    ING -->|routes| HOST3[api.${legacy_env}.freightverify.com]
```

> SVG rendering failed for this diagram.
