# Diagram: devops/k8s/rbac/argocd/application.yaml


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
flowchart LR
  App[Application: ${name_prefix}-eks-rbac]
  App --> Metadata[metadata]
  Metadata --> Name[name: ${name_prefix}-eks-rbac]
  Metadata --> Labels[labels\nenvironment: ${env}]
  App --> Spec[spec]
  Spec --> Destination[destination]
  Destination --> DestName[name: '']
  Destination --> Namespace[namespace: kube-system]
  Destination --> Server[server: ${cluster_endpoint}]
  Spec --> Source[source]
  Source --> Path[path: devops/k8s/rbac/helm]
  Source --> Repo[repoURL: https://gitlab.com/freightverify-nextgen/devops.git]
  Source --> TargetRev[targetRevision: ${revision}]
  Source --> Helm[helm]
  Helm --> Values[valueFiles:\n- values.yaml]
  Spec --> Sources[sources: []]
  Spec --> Project[project: ${env}-services]
  Spec --> SyncPolicy[syncPolicy]
  SyncPolicy --> Automated[automated: {}]
  SyncPolicy --> SyncOptions[syncOptions:\n- CreateNamespace=true]
  SyncPolicy --> Preserve[preserveResourcesOnDeletion: true]
```

> SVG rendering failed for this diagram.
