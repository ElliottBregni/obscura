# Diagram: devops/k8s/argocd/ecr-repositories/helm/templates/secrets.yaml


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
flowchart TB
  Values[Values (chart values)]
  Repos[Repositories (range .Values.repositories)]
  Values --> Repos

  subgraph LoopPerRepo["Per-repository resources"]
    direction TB
    Secret["Secret: argocd-repo-<repo>"]:::k8s
    CronJob["CronJob: argocd-ecr-credentials-<repo>"]:::k8s
    Job["Job: argocd-ecr-credentials-<repo>"]:::k8s

    Secret -->|patched by| CronJob
    Secret -->|patched by| Job
    CronJob --> UpdateContainer
    Job --> UpdateContainer
    UpdateContainer["Container: update-secret\nimage: {{ $.Values.image.name }}\nrequests/limits cpu/mem"]:::container
    UpdateContainer --> Command
    Command["Command:\nPASSWORD=$(aws ecr get-login-password --region \"{{ $.Values.region }}\" | base64 -w 0)\nkubectl patch secret \"argocd-repo-<repo>\" -n \"{{ $.Values.namespace }}\" --type merge -p \"{\\\"data\\\": {\\\"password\\\": \\\"$PASSWORD\\\"}}\""]:::cmd

    CronJob -. schedule every 6h .-> CronJob
    CronJob -. serviceAccount: argocd-server .-> CronJob
    Job -. annotations (post-install, post-upgrade) .-> Job

    NodeAffinity["optional: nodeAffinity\n.matchExpressions: kubernetes.io/arch In <architectures>"]:::opt
    CronJob -->|may include| NodeAffinity
    Job -->|may include| NodeAffinity
  end

  classDef k8s fill:#f9f,stroke:#333,stroke-width:1px;
  classDef container fill:#ffeb99,stroke:#333,stroke-width:1px;
  classDef cmd fill:#e6f7ff,stroke:#333,stroke-width:1px;
  classDef opt fill:#f0f0f0,stroke:#666,stroke-dasharray: 5 5;
```

> SVG rendering failed for this diagram.
