# Diagram: devops/k8s/argocd/app-manager/helm/templates/descheduler.yaml


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
flowchart LR
  A[Check: .Values.descheduler.enable] -->|true| B[Range over .Values.cluster]
  A -->|false| Z[No output]
  B --> C[Set $config = .Values.descheduler]
  C --> D[Set $source = $config.valueFiles.source]
  C --> E[Set $chart = "descheduler"]
  E --> F[Create Application manifest]
  F --> G[Include app.metadata with environment, deployment, name]
  F --> H[spec.project = "{{ $.Values.environment }}-services"]
  H --> I[spec.destination.namespace = $config.namespace]
  I --> J[spec.destination.server = .server]
  D -->|helm| K[Add sources.repoURL = $.Values.repositories.helm]
  K --> K1[chart = descheduler]
  K --> K2[targetRevision = $config.revision]
  K --> K3[helm.valueFiles -> "values.yaml"]
  D -->|git| L[Set source.path = "devops/k8s/descheduler/helm"]
  L --> L1[repoURL = $.Values.repositories.devops]
  L --> L2[targetRevision = $config.valueFiles.revision]
  L --> L3[helm.valueFiles -> 'values.yaml']
  F --> M[Include app.syncPolicy with $config.syncPolicy]
  B --> N[--- separator between apps]
  N --> B
  Z --> END[End]
```

> SVG rendering failed for this diagram.
