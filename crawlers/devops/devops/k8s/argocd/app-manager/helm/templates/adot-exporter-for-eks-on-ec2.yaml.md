# Diagram: devops/k8s/argocd/app-manager/helm/templates/adot-exporter-for-eks-on-ec2.yaml


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
flowchart TD
  Enable{"Values.adotExporter.enable"} -->|true| ForEachCluster["Range over Values.cluster"]
  ForEachCluster --> CreateApp["Create Application (adot-exporter-for-eks-on-ec2)"]
  CreateApp --> Metadata["Include app.metadata (environment, deployment, name, app)"]
  CreateApp --> SpecProject["spec.project = \"<environment>-services\""]
  CreateApp --> Destination["destination.namespace = Values.adotExporter.namespace<br/>destination.server = .server"]
  ForEachCluster --> SourceChoice{"valueFiles.source == 'helm' ? 'helm' : 'git'"}
  SourceChoice -->|helm| Helm["sources:\n repoURL = Values.repositories.helm\n chart = adot-exporter-for-eks-on-ec2\n targetRevision = Values.adotExporter.revision\n helm.valueFiles: [values.yaml]\n include adotExporter.parameters (name, region, global)"]
  SourceChoice -->|git| Git["source:\n path = devops/k8s/adot-exporter-for-eks-on-ec2/helm\n repoURL = Values.repositories.devops\n targetRevision = Values.adotExporter.valueFiles.revision\n helm.valueFiles: [values.yaml]\n include adotExporter.parameters (name, region, global)"]
  CreateApp --> Sync["Include app.syncPolicy (adotExporter.syncPolicy)"]
  ForEachCluster --> Separator[--- (multiple Applications per cluster)]
```

> SVG rendering failed for this diagram.
