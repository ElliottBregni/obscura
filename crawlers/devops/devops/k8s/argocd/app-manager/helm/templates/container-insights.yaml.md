# Diagram: devops/k8s/argocd/app-manager/helm/templates/container-insights.yaml


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
flowchart LR
  A[Start] --> B{Values.containerInsights.enable?}
  B -- false --> Z[No diagrams generated]
  B -- true --> C[For each .Values.cluster]
  C --> D[Set config := $.Values.containerInsights]
  D --> E[Set source := config.valueFiles.source]
  D --> F[Set chart := "container-insights"]
  D --> G[Set parametersDict (name, fluentbitEnable)]
  C --> H[Create Argo Application manifest]
  H --> I[include "app.metadata" with metadataDict]
  H --> J[spec.project = "{{ $.Values.environment }}-services"]
  H --> K[spec.destination.namespace = config.namespace]
  H --> L[spec.destination.server = .server]
  H --> M{source == "helm" or "git"?}
  M -- "helm" --> N[Add sources:\n- repoURL: $.Values.repositories.helm\n- chart: container-insights\n- targetRevision: config.revision\n- helm.valueFiles: values.yaml]
  N --> O[include "containerInsights.parameters" with parametersDict]
  M -- "git" --> P[Add source:\npath: devops/k8s/container-insights/helm\nrepoURL: $.Values.repositories.devops\ntargetRevision: config.valueFiles.revision\nhelm.valueFiles: values.yaml]
  P --> Q[include "containerInsights.parameters" with parametersDict]
  O --> R[include "app.syncPolicy" with syncPolicyDict]
  Q --> R
  R --> S[Append '---' separator]
  S --> T[Next cluster or finish]
  T --> U[End]
```

> SVG rendering failed for this diagram.
