# Diagram: devops/k8s/argocd/app-manager/helm/templates/secrets-store-csi-driver.yaml


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
flowchart LR
  A[Start] --> B{.Values.secretStoreCsiDriver.enable?}
  B -- No --> Z[Done]
  B -- Yes --> C[For each .Values.cluster]
  C --> D[Set chart = "secrets-store-csi-driver"]
  D --> E[Create Application (apiVersion: argoproj.io/v1alpha1)]
  E --> F[Include metadata via app.metadata(environment, deployment, name)]
  F --> G[spec.project = "{{ $.Values.environment }}-services"]
  G --> H[spec.destination.namespace = kube-system]
  H --> I[spec.destination.server = "{{ .server }}"]
  I --> J{valueFiles.source == "helm" or "git"?}
  J -- helm --> K[spec.sources: repoURL={{ $.Values.repositories.helm }}, chart={{ chart }}, targetRevision={{ $config.revision }}, helm.valueFiles: ["values.yaml"]]
  J -- git --> L[spec.source: path=devops/k8s/{{ chart }}/helm, repoURL={{ $.Values.repositories.devops }}, targetRevision={{ $config.valueFiles.revision }}, helm.valueFiles: ['values.yaml']]
  K --> M[Include app.syncPolicy(syncPolicy)]
  L --> M
  M --> N[Emit Application YAML block]
  N --> O[Repeat for next cluster]
  O --> P[End of range]
  P --> Z
```

> SVG rendering failed for this diagram.
