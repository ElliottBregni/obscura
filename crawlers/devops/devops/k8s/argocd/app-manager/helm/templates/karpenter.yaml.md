# Diagram: devops/k8s/argocd/app-manager/helm/templates/karpenter.yaml


> Auto-generated by Obscura crawlers

## Diagram 1

```mermaid
flowchart TD
    A[Values.karpenter.enable == true] --> B[Iterate over Values.cluster]
    B --> C[Create Application: karpenter]
    C --> D[spec.project = "{{ $.Values.environment }}-services"]
    C --> E[Destination: namespace = "{{ $.Values.karpenter.namespace }}", server = .server]
    C --> F{valueFiles.source == "helm"}
    F -->|true| G[Sources: Helm repo="{{ $.Values.repositories.helm }}" chart=karpenter targetRevision={{ $.Values.karpenter.revision }}]
    F -->|true| H[Helm.valueFiles: values.yaml, $values/devops/k8s/karpenter/helm/values.{{ $.Values.environment }}.yaml]
    F -->|true| I[Include karpenter.parameters (name)]
    F -->|false| J{valueFiles.source == "git"}
    J -->|true| K[Source: path=devops/k8s/karpenter/helm repoURL={{ $.Values.repositories.devops }} targetRevision={{ $.Values.karpenter.valueFiles.revision }}]
    J -->|true| L[Helm.valueFiles: values.yaml]
    J -->|true| M[Include karpenter.parameters (name)]
    C --> N[Include app.syncPolicy: {{ $.Values.karpenter.syncPolicy }}]
    style A fill:#f9f,stroke:#333,stroke-width:1px
    style C fill:#bbf,stroke:#333,stroke-width:1px
    style F fill:#ffd,stroke:#333,stroke-width:1px
    style J fill:#ffd,stroke:#333,stroke-width:1px
```

> SVG rendering failed for this diagram.

## Diagram 2

```mermaid
classDiagram
    class Application {
        +string apiVersion = argoproj.io/v1alpha1
        +string kind = Application
        +string metadata.name
        +string spec.project
        +Destination destination
        +Source[] sources
        +Source source
        +string spec.syncPolicy
    }
    class Destination {
        +string name
        +string namespace
        +string server
    }
    class Source {
        +string repoURL
        +string chart
        +string targetRevision
        +Helm helm
        +string path
        +string ref
        +string refType
    }
    class Helm {
        +string[] valueFiles
        +object helm.parameters
    }
    Application --> Destination : has
    Application --> Source : may_have
    Source --> Helm : may_include
    note left of Application : templates include app.metadata, karpenter.parameters, app.syncPolicy
```

> SVG rendering failed for this diagram.
