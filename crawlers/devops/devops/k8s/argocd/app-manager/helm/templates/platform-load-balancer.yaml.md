# Diagram: devops/k8s/argocd/app-manager/helm/templates/platform-load-balancer.yaml


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
flowchart LR
  A[Application (argoproj.io/v1alpha1)] --> B[metadata: name, environment, deployment]
  A --> C[spec]
  C --> D[project: environment-platform]
  C --> E[destination]
  E --> E1[name]
  E --> E2[namespace: platformLoadBalancer.namespace]
  E --> E3[server: .server]
  C --> F[source/sources]
  F --> G{source type}
  G -->|helm| H[helm sources]
  G -->|git| I[git source]
  H --> H1[repoURL: repositories.helm]
  H --> H2[chart: platform-load-balancer]
  H --> H3[targetRevision: platformLoadBalancer.revision]
  H --> H4[valueFiles: values.yaml, env-specific values]
  H --> H5[additional repo: repositories.devops, ref: values, targetRevision: valueFiles.revision]
  I --> I1[repoURL: repositories.devops]
  I --> I2[targetRevision: platformLoadBalancer.valueFiles.revision]
  I --> I3[path: devops/k8s/platform-load-balancer/helm]
  I --> I4[helm.valueFiles: values.yaml, values.env.yaml]
  F --> J[platformLoadBalancer.parameters]
  J --> J1[deployment, environment, url(hostedZone.url)]
  J --> J2[optional proxyUrl]
  C --> K[syncPolicy]
  K --> K1[defined by platformLoadBalancer.syncPolicy]
  style A fill:#f9f,stroke:#333,stroke-width:2px
  style B fill:#bbf
  style C fill:#bfb
  style F fill:#ffd
  style J fill:#dfd
```

> SVG rendering failed for this diagram.
