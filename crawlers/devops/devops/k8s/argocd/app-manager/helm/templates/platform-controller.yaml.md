# Diagram: devops/k8s/argocd/app-manager/helm/templates/platform-controller.yaml


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
flowchart TD
  A[platformController.enabled?] -->|true| B[iterate over .Values.cluster]
  B --> C[Render Application manifest\nkind: Application\nname: platform-controller]
  C --> M[Include metadata via app.metadata]
  C --> S[spec.project = "{{ $.Values.environment }}-platform"]
  S --> D{source == "helm" ?}
  D -->|yes| E[spec.sources:\n- repoURL: {{ $.Values.repositories.helm }}\n  chart: platform-controller\n  targetRevision: {{ $.Values.platformController.revision }}\n  helm.valueFiles:\n  - $values/devops/k8s/platform-controller/helm/values.{{ $.Values.environment }}.yaml\n- repoURL: {{ $.Values.repositories.devops }}\n  targetRevision: {{ $.Values.platformController.valueFiles.revision }}\n  ref: values]
  D -->|no| G{source == "git" ?}
  G -->|yes| H[spec.source:\npath: devops/k8s/platform-controller/helm\nrepoURL: {{ $.Values.repositories.devops }}\ntargetRevision: {{ $.Values.platformController.valueFiles.revision }}\nhelm.valueFiles:\n- values.yaml]
  H --> F[spec.destination:\nnamespace: {{ $.Values.platformController.namespace }}\nserver: cluster.server]
  E --> F
  F --> P[Include syncPolicy via app.syncPolicy]
  P --> Z[Final Application resources created per cluster]
  A -->|false| X[No Application resources created]
```

> SVG rendering failed for this diagram.
