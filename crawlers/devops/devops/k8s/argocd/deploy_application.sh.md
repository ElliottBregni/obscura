# Diagram: devops/k8s/argocd/deploy_application.sh


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
flowchart LR
    main[main] --> parse[Parse args (while/case)]
    parse --> validate[Validate required args]
    main --> fetch_cluster[Fetch cluster (if CLUSTER_NAME empty)]
    fetch_cluster --> get_eks_cluster[get_eks_cluster(environment,is_active)]
    get_eks_cluster --> aws_list[aws eks list-clusters]
    get_eks_cluster --> aws_describe_tags[aws eks describe-cluster -> tags]
    get_eks_cluster --> return_cluster[return target_cluster or error]
    main --> get_eks_endpoint[get_eks_endpoint(cluster_name)]
    get_eks_endpoint --> aws_describe_endpoint[aws eks describe-cluster -> endpoint]
    get_eks_endpoint --> return_endpoint[return endpoint or error]
    main --> get_eks_deploy_name[get_eks_deploy_name(cluster_name)]
    get_eks_deploy_name --> aws_describe_tags2[aws eks describe-cluster (jq .cluster.tags) || string-match blue/green]
    get_eks_deploy_name --> return_deploy[return deploy name or empty]
    main --> compute_name_prefix[Compute name_prefix from ENVIRONMENT and deploy_name]
    main --> process_type[TYPE == app/application?]
    process_type --> manifest_flow{MANIFEST specified?}
    manifest_flow -->|yes| read_manifest[read manifest file (jq .applications[])]
    read_manifest --> loop_apps[for each app -> application_handler]
    manifest_flow -->|no| single_app[application_handler for NAME]
    loop_apps --> application_handler[application_handler(cluster_name, cluster_endpoint, name_prefix, name)]
    single_app --> application_handler
    application_handler --> locate_files[determine app_dir, helm_dir, app_yaml, env_yaml]
    application_handler --> cp_temp[cp app_yaml to temp_application.yaml]
    application_handler --> legacy_env[derive legacy_env]
    application_handler --> sed_replace[sed replace variables in temp_yaml]
    application_handler --> case_command[case COMMAND]
    case_command -->|create| argocd_create[argocd_create(temp_yaml, env_yaml)]
    case_command -->|delete| argocd_delete[argocd, env_yaml]
    argocd_create --> check_dry_run_create{DRY_RUN?}
    check_dry_run_create -->|true| log_create[log "argocd app create -f ... --upsert"]
    check_dry_run_create -->|false| run_create[argocd app create -f ... (--values env_file) --upsert]
    run_create --> create_result[return status]
    argocd_delete --> yq_app_name[yq -r .metadata.name temp_yaml -> app_name]
    argocd_delete --> check_dry_run_delete{DRY_RUN?}
    check_dry_run_delete -->|true| log_delete[log "argocd app delete ... --cascade"]
    check_dry_run_delete -->|false| run_delete[argocd app delete app_name --cascade]
    run_delete -->|if FORCE true| kubectl_patch[kubectl patch kind/name remove /metadata/finalizers -n argocd]
    application_handler --> rm_temp[rm temp_application.yaml]
    main --> post_processing[Post-processing (delete-force finalizer cleanup with kubectl patch applications)]
    validate --> error_node[error function -> exit]
    help_node[help] --> exit_help[prints help and exits]
```

> SVG rendering failed for this diagram.
