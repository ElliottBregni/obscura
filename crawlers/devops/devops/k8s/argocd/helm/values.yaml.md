# Diagram: devops/k8s/argocd/helm/values.yaml


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
flowchart TD
    Values["values.yaml\nargocdVersion: 6.2.3\nfullnameOverride: argocd\nregion: us-east-1"] --> Argocd[Argocd]
    subgraph Argocd[Argocd Components]
        direction TB
        Server[server]
        RepoServer[repoServer]
        Configs[configs.cm & rbac]
        Ingress[ingress (HTTP) - ALB]
        IngressGRPC[ingressGrpc (gRPC) - ALB]
    end
    Argocd --> ExternalDNS[externaldns\nversion: 0.14.0\nnamespace: kube-system]

    Server --> Ingress
    Server --> IngressGRPC
    Server --> Configs
    RepoServer --> RepoEnv[env: ARGOCD_EXEC_TIMEOUT="300s"]
    Configs --> CM[cm: admin.enabled=true\nhelm.valuesFileSchemes=secrets+...]
    Configs --> RBAC["rbac.policy.default: role: readonly"]

    subgraph ALB_HTTP[ALB (HTTP Ingress) Details]
        direction LR
        A1[ingressClassName: alb]
        A2[controller: aws]
        A3[ingressClassName: alb]
        A4[annotations: scheme=internal, healthcheck=/healthz, backend-protocol=HTTP, target-type=ip, listen-ports=443, ssl-policy=ELBSecurityPolicy-TLS-1-1-2017-01, success-codes=200]
        A5[aws: backendProtocolVersion=HTTP2\nserviceType=NodePort]
    end

    Ingress --> A1
    Ingress --> A2
    Ingress --> A4
    Ingress --> A5

    subgraph ALB_GRPC[ALB (gRPC Ingress) Details]
        direction LR
        G1[ingressClassName: alb]
        G2[isAWSALB: true]
        G3[awsALB: serviceType=NodePort\nbackendProtocolVersion=GRPC]
    end

    IngressGRPC --> G1
    IngressGRPC --> G2
    IngressGRPC --> G3

    classDef cluster fill:#f9f,stroke:#333,stroke-width:1px;
    class Argocd,ExternalDNS cluster
```

> SVG rendering failed for this diagram.
