# Diagram: devops/k8s/nginx-ingress-controller/argocd/application.yaml


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
flowchart TD
  App[Application: ${name_prefix}-nginx-ingress-controller]
  App --> Metadata[metadata]
  Metadata --> MetaName[name: ${name_prefix}-nginx-ingress-controller]
  Metadata --> Labels[labels]
  Labels --> LabelEnv[environment: ${env}]
  App --> Spec[spec]
  Spec --> Destination[destination]
  Destination --> DestName[name: '']
  Destination --> DestNamespace[namespace: default]
  Destination --> DestServer[server: '${cluster_endpoint}']
  Spec --> Source[source]
  Source --> Path[path: devops/k8s/nginx-ingress-controller/helm]
  Source --> Repo[repoURL: https://gitlab.com/freightverify-nextgen/devops.git]
  Source --> Target[targetRevision: ${revision}]
  Source --> Helm[helm]
  Helm --> ValueFiles[valueFiles: []]
  Spec --> Sources[sources: []]
  Spec --> Project[project: ${env}-services]
  Spec --> Sync[syncPolicy]
  Sync --> Automated[automated: {}]
  Sync --> SyncOptions[syncOptions]
  SyncOptions --> Opt1[CreateNamespace=true]
```

> SVG rendering failed for this diagram.
