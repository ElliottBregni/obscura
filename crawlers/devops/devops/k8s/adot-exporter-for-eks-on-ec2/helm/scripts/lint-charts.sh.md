# Diagram: devops/k8s/adot-exporter-for-eks-on-ec2/helm/scripts/lint-charts.sh


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
flowchart TD
    Start([Start]) --> SetOpts[set -euo pipefail]
    SetOpts --> Root[ROOT_PROJECT = git rev-parse --show-toplevel]
    Root --> Build[BUILD_DIR = ${ROOT_PROJECT}/build]
    Build --> Tools[TOOLS_DIR = ${BUILD_DIR}/tools]
    Tools --> Chart[CHART_DIR = ${ROOT_PROJECT}/charts/adot-exporter-for-eks-on-ec2]
    Chart --> Export[export PATH = ${TOOLS_DIR}:${PATH}]
    Export --> Init[FAILED_V3 = ()]
    Init --> Echo[Print linting message for CHART_DIR]
    Echo --> Lint[Run helmv3 lint ${CHART_DIR}/]
    Lint --> Check{Did helmv3 lint fail?}
    Check -- No --> Success[Print "All charts passed linting" and exit 0]
    Check -- Yes --> Append[Append CHART_DIR to FAILED_V3]
    Append --> Header[Print "Helm v3:" header]
    Header --> Loop[Print each failed chart with a cross mark]
    Success --> End([Exit])
    Loop --> End
```

> SVG rendering failed for this diagram.
