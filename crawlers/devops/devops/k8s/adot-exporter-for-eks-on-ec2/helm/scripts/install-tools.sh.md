# Diagram: devops/k8s/adot-exporter-for-eks-on-ec2/helm/scripts/install-tools.sh


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
flowchart TD
    Start((Start))
    StrictMode[set -euo pipefail]
    Platform[Determine PLATFORM (uname -s -> lowercase)]
    ArchDecision{uname -m == "x86_64"?}
    ArchAMD[Set ARCH = amd64]
    ArchARM[Set ARCH = arm64]
    Root[Get ROOT_PROJECT via git rev-parse --show-toplevel]
    BuildDir[Set BUILD_DIR = ROOT_PROJECT/build]
    TmpDir[Set TMP_DIR = BUILD_DIR/tmp]
    ToolsDir[Set TOOLS_DIR = BUILD_DIR/tools]
    MkTools[mkdir -p TOOLS_DIR]
    ExportPath[export PATH = TOOLS_DIR:PATH]
    HelmTag[Fetch HELM_VERSION_TAG from GitHub releases page (curl + parse)]
    HelmVer[Set HELM_VERSION = "v" + HELM_VERSION_TAG]
    MkKubeval[mkdir -p TMP_DIR/kubeval]
    DownloadKubeval[Download kubeval tar.gz for PLATFORM/ARCH]
    ExtractKubeval[Extract archive into TMP_DIR/kubeval]
    MoveKubeval[Move kubeval binary to TOOLS_DIR/kubeval]
    MkHelm[mkdir -p TMP_DIR/helmv3]
    DownloadHelm[Download helm tar.gz for HELM_VERSION/PLATFORM/ARCH]
    ExtractHelm[Extract archive into TMP_DIR/helmv3]
    MoveHelm[Move helm binary to TOOLS_DIR/helmv3]
    RmPlatformArch[rm -rf PLATFORM-ARCH leftover directory]
    RmTmp[rm -rf TMP_DIR (cleanup)]
    Done((Done))

    Start --> StrictMode --> Platform
    Platform --> ArchDecision
    ArchDecision -- yes --> ArchAMD
    ArchDecision -- no --> ArchARM
    ArchAMD --> Root
    ArchARM --> Root
    Root --> BuildDir --> TmpDir --> ToolsDir --> MkTools --> ExportPath --> HelmTag --> HelmVer --> MkKubeval --> DownloadKubeval --> ExtractKubeval --> MoveKubeval --> MkHelm --> DownloadHelm --> ExtractHelm --> MoveHelm --> RmPlatformArch --> RmTmp --> Done
```

> SVG rendering failed for this diagram.
