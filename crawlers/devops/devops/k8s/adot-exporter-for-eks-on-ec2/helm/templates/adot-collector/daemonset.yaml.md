# Diagram: devops/k8s/adot-exporter-for-eks-on-ec2/helm/templates/adot-collector/daemonset.yaml


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
flowchart TD
  Enabled{.Values.adotCollector.daemonSet.enabled?}
  Enabled -->|true| DaemonSet[DaemonSet\napiVersion: apps/v1\nkind: DaemonSet]
  Enabled -->|false| Disabled[DaemonSet disabled]
  DaemonSet --> Metadata[metadata\nname\nnamespace\nlabels]
  DaemonSet --> Spec[spec]
  Spec --> Selector[selector\nmatchLabels]
  Spec --> Template[template]
  Template --> TMeta[metadata\nlabels\nannotations\nchecksum/config]
  Template --> PodSpec[spec\ncontainers\nvolumes\nserviceAccountName\ntolerations]
  PodSpec --> Container[container\nname\nimage\nimagePullPolicy\nenv\ncommand\nvolumeMounts\nresources]
  PodSpec --> Volumes[volumes (toYaml)]
  Container --> Image[image: repository:tag]
  Container --> Env[env (toYaml)]
  Container --> Command[command (toYaml)]
  Container --> VolumeMounts[volumeMounts (toYaml)]
  Container --> Resources[resources (toYaml)]
  Template --> ServiceAccount[serviceAccountName]
  Template --> Tolerations[tolerations (toYaml)]
```

> SVG rendering failed for this diagram.
