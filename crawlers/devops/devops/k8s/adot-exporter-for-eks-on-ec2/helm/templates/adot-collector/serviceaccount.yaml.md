# Diagram: devops/k8s/adot-exporter-for-eks-on-ec2/helm/templates/adot-collector/serviceaccount.yaml


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
graph LR
Cond[Condition: Values.adotCollector.daemonSet.enabled AND Values.adotCollector.daemonSet.serviceAccount.create]
Cond --> SA[Create ServiceAccount]
SA --> API[apiVersion: v1]
SA --> Kind[kind: ServiceAccount]
SA --> Metadata[metadata]
Metadata --> Name[name: serviceAccount.name (from Values)]
Metadata --> Namespace[namespace: include adotCollector.daemonSet.namespace]
Metadata --> AnnCheck{serviceAccount.annotations present?}
AnnCheck -- yes --> Annotations[annotations: toYaml(annotations) | nindent 4]
AnnCheck -- no --> NoAnnotations[(no annotations)]
Cond -- no --> Skip[(ServiceAccount not created)]
```

> SVG rendering failed for this diagram.
