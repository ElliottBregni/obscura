# Diagram: devops/k8s/adot-exporter-for-eks-on-ec2/helm/templates/adot-collector/configmap.yaml


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
flowchart TD
  COND[Condition: .Values.adotCollector.daemonSet.enabled] --> CM[ConfigMap: adotCollector.daemonSet.configMap]
  CM --> ADOT[adot-config]
  ADOT --> EXT[extensions]
  ADOT --> RCV[receivers]
  ADOT --> PRC[processors]
  ADOT --> EXP[exporters]
  ADOT --> SVC[service]
  EXT --> HC[health_check: {{ .Values.adotCollector.daemonSet.extensions.healthCheck }}]
  EXT --> SIG[sigv4auth\nregion: {{ .Values.awsRegion }}]
  RCV --> AWSC[awscontainerinsightreceiver\ncollection_interval\ncontainer_orchestrator\nadd_service_as_attribute\nprefer_full_pod_name\nadd_full_pod_name_metric_label]
  RCV --> PROM[prometheus\nconfig]
  PROM --> PROM_G[global\nscrape_interval: {{ .Values.adotCollector.daemonSet.ampreceivers.scrapeInterval }}\nscrape_timeout: {{ .Values.adotCollector.daemonSet.ampreceivers.scrapeTimeout }}]
  PROM --> PROM_S[scrape_configs\n{{ .Values.adotCollector.daemonSet.ampreceivers.scrapeConfigs }}]
  PRC --> BATCH[batch/metrics\ntimeout: {{ .Values.adotCollector.daemonSet.processors.timeout }}]
  EXP --> AWSEMF[awsemf\nnamespace: {{ .Values.adotCollector.daemonSet.cwexporters.namespace }}\nlog_group_name: /aws/containerinsights/{{ .Values.clusterName }}/performance\nlog_stream_name: {{ .Values.adotCollector.daemonSet.cwexporters.logStreamName }}\nregion: {{ .Values.awsRegion }}\nresource_to_telemetry_conversion.enabled: {{ .Values.adotCollector.daemonSet.cwexporters.enabled }}\ndimension_rollup_option: {{ .Values.adotCollector.daemonSet.cwexporters.dimensionRollupOption }}\nparse_json_encoded_attr_values: ...\nmetric_declarations: ...]
  EXP --> PRW[prometheusremotewrite\nnamespace: {{ .Values.adotCollector.daemonSet.ampexporters.namespace }}\nendpoint: {{ .Values.adotCollector.daemonSet.ampexporters.endpoint }}\nresource_to_telemetry_conversion.enabled: {{ .Values.adotCollector.daemonSet.ampexporters.resourcetootel }}\nauth.authenticator: {{ .Values.adotCollector.daemonSet.ampexporters.authenticator }}]
  SVC --> PIPE[pipelines]
  PIPE --> MET[metrics]
  MET --> MET_RX[receivers]
  MET --> MET_PS[processors]
  MET --> MET_EX[exporters]
  MET_RX --> RCV
  MET_PS --> PRC
  MET_EX --> EXP
  SVC --> SEXT[extensions]
  subgraph VALUES["Referenced values"]
    V_AWS[.Values.awsRegion]
    V_CLUSTER[.Values.clusterName]
    V_ADOT[.Values.adotCollector.daemonSet.*]
  end
  V_AWS --> SIG
  V_AWS --> AWSEMF
  V_CLUSTER --> AWSEMF
  V_ADOT --> CM
```

> SVG rendering failed for this diagram.
