# Diagram: devops/k8s/adot-exporter-for-eks-on-ec2/helm/templates/adot-collector/namespace.yaml


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
flowchart TD
  Start[Start] --> Check{adotCollector.daemonSet.enabled<br/>AND createNamespace?}
  Check -- "true" --> CreateNS[Create Namespace<br/>apiVersion: v1<br/>kind: Namespace]
  CreateNS --> Name[metadata.name = include adotCollector.daemonSet.namespace .]
  CreateNS --> LabelName[labels.name = include adotCollector.daemonSet.namespace .]
  CreateNS --> Labels[labels += include adotCollector.daemonSet.labels . (indented)]
  Check -- "false" --> Skip[No Namespace created]
```

> SVG rendering failed for this diagram.
