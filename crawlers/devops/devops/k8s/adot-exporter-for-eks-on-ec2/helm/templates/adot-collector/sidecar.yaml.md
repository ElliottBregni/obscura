# Diagram: devops/k8s/adot-exporter-for-eks-on-ec2/helm/templates/adot-collector/sidecar.yaml


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
flowchart TD
  En{Values.adotCollector.sidecar.enabled?}
  En -->|true| Dep[Deployment (apps/v1)]
  En -->|false| Noop[No output rendered]

  Dep --> MD[metadata]
  MD --> MD_name[name = Values.adotCollector.sidecar.name]
  MD --> MD_ns[namespace = Values.adotCollector.sidecar.namespace]
  MD --> MD_labels[labels = name + include(adotCollector.daemonSet.labels)]

  Dep --> Spec[spec]
  Spec --> Replicas[replicas = Values.adotCollector.sidecar.replicas]
  Spec --> Selector[selector.matchLabels.name = Values.adotCollector.sidecar.name]
  Spec --> Template[template]

  Template --> TMD_labels[template.metadata.labels = name + include(adotCollector.daemonSet.labels)]
  Template --> TSpec[template.spec.containers]

  TSpec --> C_sidecar[container: sidecar]
  C_sidecar --> C1_name[name = Values.adotCollector.sidecar.image.name]
  C_sidecar --> C1_image[image = Values.adotCollector.sidecar.image.repository:Values.adotCollector.sidecar.image.tag]
  C_sidecar --> C1_pull[imagePullPolicy = Values.adotCollector.sidecar.image.pullPolicy]

  TSpec --> C_main[container: adotCollector]
  C_main --> C2_name[name = Values.adotCollector.image.name]
  C_main --> C2_image[image = Values.adotCollector.image.repository:Values.adotCollector.image.tag]
  C_main --> C2_env[env: AWS_REGION = Values.awsRegion]
  C_main --> C2_pull[imagePullPolicy = Values.adotCollector.image.sidecarPullPolicy]
  C_main --> C2_resources[resources = toYaml(Values.adotCollector.sidecar.resources)]
```

> SVG rendering failed for this diagram.
