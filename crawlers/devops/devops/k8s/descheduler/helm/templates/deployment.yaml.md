# Diagram: devops/k8s/descheduler/helm/templates/deployment.yaml


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
● skill(crawl-diagrams)

✗ Create /tmp/helm-template.yaml
  Permission denied and could not request permission from user

✗ Generate Mermaid diagrams using copilot subprocess
  $ cat > /tmp/helm-template-$$.yaml << 'EOF'
  {{- if eq .Values.kind "Deployment" }}
  apiVersion: apps/v1
  kind: Deployment
  metadata:...
  Permission denied and could not request permission from user

✗ Generate Mermaid diagrams
  $ cd /Users/bregnie/obscura && python3 << 'PYEOF'
  import subprocess, re

  code = '''{{- if eq .Values.kind "Deployment" }}
  apiVersion: apps/v1...
  Permission denied and could not request permission from user

✗ Generate Mermaid diagrams with copilot
  $ copilot -p 'Generate Mermaid diagrams for the following source code.
  Rules:
  - Output ONLY valid Mermaid diagram code, nothing else.
  - Do NOT wrap output in markdown fences (no ```).
  - Do NOT include any explanatory text, comments, or prose....
  Permission denied and could not request permission from user

I cannot complete this task because the system is denying permission to execute commands. The crawl-diagrams skill requires running a subprocess to call `copilot -p`, but all attempts to execute commands (bash, python, copilot) are being blocked with "Permission denied."
```

> SVG rendering failed for this diagram.
