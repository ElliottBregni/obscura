# Diagram: mobile/FreightVerifyMobileTracking/src/components/atoms/shipment-status.tsx


> Auto-generated by Obscura crawlers

## Diagram 1

```mermaid
classDiagram
    class ShipmentStatus {
        +props: ShipmentStatusProps
        +render()
    }
    class ShipmentStatusProps {
        +status: any
        +google_url
        +creator_shipment_id
    }
    ShipmentStatus --> ShipmentStatusProps : uses
    class I18n {
        +t(key)
    }
    class style {
        +row
    }
    class createMapLink {
        +createMapLink(url)
    }
    ShipmentStatus --> I18n : localize
    ShipmentStatus --> style : styling
    ShipmentStatus --> createMapLink : calls
```

> SVG rendering failed for this diagram.

## Diagram 2

```mermaid
flowchart TD
    A[Render ShipmentStatus] --> B[Display Shipment ID Text]
    B --> C{TouchableWithoutFeedback}
    C -->|onPress| D[call createMapLink(google_url)]
    D --> E[Open map / deep link]
    A --> F[Read props.status.creator_shipment_id]
    A --> G[Read props.status.google_url]
```

> SVG rendering failed for this diagram.
