# Diagram: mobile/FreightVerifyMobileTracking/src/components/atoms/vins-collapsible.tsx


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
classDiagram
    class VinsCollapsible {
        +VinsCollapsibleProps props
        -string vins
        -int vin_list
        +renderViewLess(handlePress)
        +renderViewMore(handlePress)
        +render()
    }
    class VinsCollapsibleProps {
        +any[] vins
    }
    class ViewMoreText
    class Text
    class I18n {
        +t(key): string
    }
    class style {
        +bold_text
    }

    VinsCollapsible "1" o-- "1" VinsCollapsibleProps : accepts
    VinsCollapsible --> ViewMoreText : uses
    VinsCollapsible --> Text : renders
    VinsCollapsible --> I18n : translates
    VinsCollapsible --> style : applies
    ViewMoreText ..> VinsCollapsible : renderViewMore / renderViewLess callbacks
    note right of VinsCollapsible::render() : logic: vins = props.vins ? join : ""\nvin_list = props.vins ? length : 0\nconditionally render heading and content based on vin_list >= 1
```

> SVG rendering failed for this diagram.
