# Diagram: mobile/FreightVerifyMobileTracking/src/components/atoms/single-pickup-delivery.tsx


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
classDiagram
class SinglePickupDelivery {
  +status: any
  +title: any
  +formatTime(time, format): string
  -key: string
  -zoneAbr: string
  +render(): JSX.Element
}
class Moment {
  +utc(time): Moment
  +local(): Moment
  +format(format): string
  +tz(name): any
  +zoneAbbr(): string
}
class I18n
class style
class View
class Text

SinglePickupDelivery --|> React
SinglePickupDelivery ..> Moment : uses
SinglePickupDelivery ..> I18n : uses
SinglePickupDelivery ..> style : uses
SinglePickupDelivery ..> View : renders
SinglePickupDelivery ..> Text : renders

class React {
}

note for SinglePickupDelivery "key = title === \"Pickup\" ? \"origin\" : \"destination\"\nformatTime calls moment.utc(...).local().format(format)\nzoneAbr = moment(...).tz(moment.tz.guess()).zoneAbbr()"
```

> SVG rendering failed for this diagram.
