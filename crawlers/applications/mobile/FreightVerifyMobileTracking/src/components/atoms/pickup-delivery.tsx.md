# Diagram: mobile/FreightVerifyMobileTracking/src/components/atoms/pickup-delivery.tsx


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
classDiagram
class PickupDeliveryProps {
  status: any
  key: number
  title: string
}
class SectionItem {
  id
  name
  address
  city
  state
  postal_code
  early
  late
  vins
  google_url
}
class PickupDelivery {
  +formatTime(time, format)
  +zoneAbr
  +SECTION: SectionItem[]
  +sections (state)
  +_renderHeader(section, i, isActive)
  +_renderSectionTitle()
  +_renderContent(_section)
  +_updateSections(activeSections)
}
class Accordion
class VinsCollapsible
class FontAwesomeIcon
class createMapLink
class moment
class lodash

PickupDelivery o-- PickupDeliveryProps
PickupDelivery o-- SectionItem : SECTION
PickupDelivery ..> Accordion : renders
PickupDelivery ..> VinsCollapsible : renders
PickupDelivery ..> FontAwesomeIcon : uses
PickupDelivery ..> createMapLink : calls
PickupDelivery ..> moment : uses (formatting, tz)
PickupDelivery ..> lodash : uses (uniqBy, findIndex, uniq)

SectionItem <|-- "constructed from" : child items
```

> SVG rendering failed for this diagram.
