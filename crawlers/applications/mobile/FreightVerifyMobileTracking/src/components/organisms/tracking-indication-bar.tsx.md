# Diagram: mobile/FreightVerifyMobileTracking/src/components/organisms/tracking-indication-bar.tsx


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
classDiagram
    class TrackingIndicationBar {
        +isLocationDisabled?: boolean
        +stopNavigation?: boolean
        +render()
        +formatTime(time, format)
        +navigateToTrackingViaTrackingMenu()
    }
    class TrackingIndicationBarProps {
        +isLocationDisabled?: boolean
        +stopNavigation?: boolean
    }
    class AppContext {
        +selectedShipmentId: number
        +isTracking: boolean
        +shipments: { data: Shipment[] }
        +saveAndUpdateItem(key, value)
    }
    class Navigation {
        +navigate(route, params)
        +push(route, params)
    }
    class Shipment {
        +id: number
        +creator_shipment_id: string
        +destination_name: string
        +destination_earliest_arrival: string
        +destination_latest_arrival: string
        +tracking_interval: string[] | string
    }
    class Styles {
        +trackingBarContainer
        +leftIconContainer
        +rightIconContainer
        +shipmentInfoContainer
        +regularInfo
        +shipmentDetailsContainer
        +details
        +rightArrowContainer
        +expectedDetailsContainer
        +expectedLeftContainer
        +expectedRightContainer
    }
    TrackingIndicationBar "1" --|> TrackingIndicationBarProps : props
    TrackingIndicationBar ..> AppContext : uses
    TrackingIndicationBar ..> Navigation : uses
    TrackingIndicationBar ..> Shipment : reads
    TrackingIndicationBar ..> Styles : uses
    AppContext "1" o-- "*" Shipment : contains
    TrackingIndicationBar : renders different UI branches
    note for TrackingIndicationBar "Branches:\n- if isLocationDisabled -> red bar with error icon\n- if no selectedShipmentId or shipment -> returns null\n- else -> pressable tracking bar (green if isTracking, red if not)"
```

> SVG rendering failed for this diagram.
