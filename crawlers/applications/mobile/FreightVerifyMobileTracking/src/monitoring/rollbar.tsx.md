# Diagram: mobile/FreightVerifyMobileTracking/src/monitoring/rollbar.tsx


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
flowchart TD
  A[Config imports<br/>(react-native-config)] --> B[config: ConfigurationOptions]
  B -->|enabled = !__DEV__ && Boolean(ROLLBAR_CLIENT_ACCESS_TOKEN && BUILD_TYPE)| C{config.enabled?}
  C -->|true| D[rollbarClient = new Client(config)]
  D --> E[rollbarClient.rollbar]
  E --> F[Provider instance]
  F --> G[RollbarProviderConfigured]
  C -->|false| H[rollbar not initialized]
  H --> G
  G --> I[props.children rendered]
  B --> J[accessToken = Config.ROLLBAR_CLIENT_ACCESS_TOKEN]
  B --> K[environment = Config.BUILD_TYPE]
  B --> L[captureUncaught = true]
  B --> M[captureUnhandledRejections = true]
  B --> N[captureDeviceInfo = true]
```

> SVG rendering failed for this diagram.
