# Diagram: mobile/FreightVerifyMobileTracking/src/index.tsx


> Auto-generated by Obscura crawlers

## Diagram 1

```mermaid
classDiagram
    class App {
        +useEffect()
        +processStopTracking()
        +onRemoteNotification()
        +dd: navigationTracking
    }
    class Providers {
        <<wrapper>>
        +DatadogProviderConfigured
        +RollbarProviderConfigured
        +SafeAreaProvider
        +AppContextProvider
    }
    class Navigation {
        +NavigationContainer
        +Stack.Navigator
    }
    class Screens {
        +PhoneLookup
        +TrackingMenu
        +Tracking
        +ErrorSelection
    }
    class Notifications {
        +notifee.onForegroundEvent()
        +notifee.onBackgroundEvent()
        +PushNotification.configure()
        +messaging().onMessage()
        +messaging().setBackgroundMessageHandler()
        +PushNotificationIOS.addEventListener()
    }
    class Utils {
        +stopBackgroundPackage()
        +I18n.t()
    }
    Providers --> App
    App --> Navigation
    Navigation --> Screens
    App --> Notifications
    App --> Utils
    Screens --> Header
    App o-- "spinner:boolean" : uses
    App ..> FontAwesomeIcon : renders
    Notifications ..> PushNotificationIOS : integrates
    Notifications ..> notifee : integrates
    Notifications ..> messaging : integrates
```

> SVG rendering failed for this diagram.

## Diagram 2

```mermaid
flowchart TD
    A[App start / mount] --> B[Register notification handlers]
    B --> C[notifee.onForegroundEvent]
    B --> D[notifee.onBackgroundEvent]
    B --> E[PushNotification.configure]
    B --> F[messaging handlers]
    C --> C1{notification from settings category?}
    C1 -- yes --> C2[Open app-settings: via Linking]
    C2 --> C3[notifee.cancelNotification(id)]
    C1 -- no --> C4[Ignore / no action]
    D --> D1{EventType.ACTION_PRESS && pressAction=="open-settings"?}
    D1 -- yes --> D2[Linking.openURL("app-settings:")]
    D2 --> D3[notifee.cancelNotification(id)]
    D1 -- no --> D4[Cancel notification if present]
    E --> E1[onRegister -> saveAndUpdateItem(device_token)]
    E --> E2[onNotification -> processStopTracking()]
    F --> F1[onMessage -> processStopTracking()]
    F --> F2[setBackgroundMessageHandler -> processStopTracking()]
    subgraph StopTracking
        S1[changeMobileTrackingState(false)]
        S2[stopBackgroundPackage()]
        S1 --> S2
    end
    E2 --> StopTracking
    F1 --> StopTracking
    F2 --> StopTracking
```

> SVG rendering failed for this diagram.
