# Diagram: mobile/FreightVerifyMobileTracking/src/utils/i18n.ts


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
flowchart LR
  subgraph Modules
    I18n["I18n\n(react-native-i18n)"]
    Lang["Languages\n(react-native-languages)"]
    Assets["Locales JSON\n(en, es, pt, de, fr, fr-CA, it, nl, pl, ru, zh)"]
  end

  I18n -->|set fallbacks = true| Fallbacks["Fallbacks = true"]
  Assets -->|mapped into| Translations["I18n.translations\n{ en, es, pt, de, fr, \"fr-CA\", it, nl, pl, ru, zh }"]
  Translations -->|assigned to| I18n
  Lang -->|addEventListener(\"change\", handler)| Listener["change handler\n({ language })"]
  Listener -->|sets I18n.locale = language| I18n
  I18n -->|export default| Export["export default I18n"]
```

> SVG rendering failed for this diagram.
