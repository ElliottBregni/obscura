# Diagram: mobile/FreightVerifyMobileTracking/src/utils/map-linking.ts


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
flowchart LR
  A[createMapLink(google_url)] --> B{Platform.OS === "ios"}
  B -- yes --> C[type = "maps."]
  B -- no --> D[type = "www."]
  C --> E{google_url truthy?}
  D --> E
  E -- yes --> F[url = "https://{type}google.com/maps/{google_url}"]
  E -- no --> G[exit (no URL)]
  F --> H[Linking.canOpenURL(url)]
  H --> I{supported?}
  I -- yes --> J[Linking.openURL(url)]
  I -- no --> K[console.log("Don't know how to open URI: {url}")]
  H --> L[(promise rejection)]
  L --> M[console.log(err)]
  G --> Z[return undefined]
```

> SVG rendering failed for this diagram.
