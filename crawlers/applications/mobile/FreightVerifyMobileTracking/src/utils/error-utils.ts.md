# Diagram: mobile/FreightVerifyMobileTracking/src/utils/error-utils.ts


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
classDiagram
    class Error {
        <<builtin>>
        +name: string
        +message: string
    }

    class LocationError {
        <<enum>>
        0 LocationUnknown
        1 PermissionDenied
        2 NetworkError
        3 WhenInUseBackgroundAttempt
        408 Timeout
        499 Cancelled
    }

    class TrackingErrorCodes {
        <<enum>>
        RC
        SC
        ST
        SO
        XX
        D1
        LO
    }

    class BGLocationError {
        +name: "BGLocationError"
        +message: string
        +constructor(errorCode: LocationError, ...otherParams: any)
        +mapCodeToMessage()
    }

    class BGConfigError {
        +name: "BGConfigError"
        +message: string
        +constructor(errorCode: TrackingErrorCodes, ...otherParams: any)
        +mapCodeToMessage()
    }

    class ShipmentServerError {
        +name: "ShipmentServerError"
        +message: string
        +constructor(statusCode: number, ...otherParams: any)
    }

    class NetworkError {
        +name: "NetworkError"
        +constructor(...otherParams: any)
    }

    BGLocationError --|> Error
    BGConfigError --|> Error
    ShipmentServerError --|> Error
    NetworkError --|> Error

    BGLocationError --> LocationError : uses
    BGConfigError --> TrackingErrorCodes : uses
    ShipmentServerError --> "number" : statusCode
    BGLocationError : maps 0 -> "Location unknown"
    BGLocationError : maps 1 -> "Location permission denied"
    BGLocationError : maps 2 -> "Network error"
    BGLocationError : maps 3 -> "Attempt to initiate location-services in background with WhenInUse authorization"
    BGLocationError : maps 408 -> "Location timeout Error"
    BGLocationError : maps 499 -> "Location request cancelled Error"

    BGConfigError : maps RC -> "Ready Config Failed"
    BGConfigError : maps SC -> "Start Config Failed"
    BGConfigError : maps ST -> "Start Event Failed"
    BGConfigError : maps SO -> "Stop Event Failed"
    BGConfigError : maps XX -> "Transfer Event Failed"
    BGConfigError : maps D1 -> "Complete Event Failed"
    BGConfigError : maps LO -> "Tracking Event Failed"
```

> SVG rendering failed for this diagram.
