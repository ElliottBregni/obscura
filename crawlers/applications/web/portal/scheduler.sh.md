# Diagram: web/portal/scheduler.sh


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
flowchart TD
  Start([Start]) --> Define[Define post_to_slack()]
  Define --> Call[Invoke post_to_slack with script args ($@)]
  Call --> SetVar[Assign SLACK_MESSAGE from script args ($@)]
  SetVar --> Curl[Send POST payload (text: SLACK_MESSAGE) to SLACK_BNS]
  Env[SLACK_BNS (env var)] --> Curl
  Curl --> Exit([exit 0])
```

> SVG rendering failed for this diagram.
