# Diagram: web/portal/src/shared/hooks/columns/useUpdatesTable.columns.js


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
● skill(crawl-diagrams)

✗ Create temp file with source code
  $ cat > /tmp/source_code.jsx << 'EOF'
  /** @jsxImportSource @emotion/react */
  import React, { useMemo } from "react";
  import { useTranslation } from "react-i18next";
  import PropTypes from "prop-types";...
  Permission denied and could not request permission from user

✗ Generate Mermaid diagrams via copilot subprocess
  $ python3 << 'PYEOF'
  import subprocess, re

  code = """/** @jsxImportSource @emotion/react */
  import React, { useMemo } from "react";...
  Permission denied and could not request permission from user

✗ Generate Mermaid diagrams and strip fences
  $ copilot -p "Generate Mermaid diagrams for the following source code.
  Rules:
  - Output ONLY valid Mermaid diagram code, nothing else.
  - Do NOT wrap output in markdown fences (no \`\`\`).
  - Do NOT include any explanatory text, comments, or prose....
  Permission denied and could not request permission from user

I apologize, but I'm unable to execute the copilot command in this environment due to permission restrictions. The crawl-diagrams skill requires calling `copilot -p` as a subprocess, which I don't have permission to do in the current session.

To generate Mermaid diagrams for this code, you would need to:
1. Run the command directly: `copilot -p "<prompt with the code>"`
2. Or use a Python script with subprocess.run() to call copilot programmatically

The skill is designed to work autonomously but requires execution permissions I currently don't have access to.
```

> SVG rendering failed for this diagram.
