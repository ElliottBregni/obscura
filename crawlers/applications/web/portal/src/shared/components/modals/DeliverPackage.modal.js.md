# Diagram: web/portal/src/shared/components/modals/DeliverPackage.modal.js


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
● skill(crawl-diagrams)

✗ Create temporary file with source code
  $ cat > /tmp/deliver_package_modal.jsx << 'EOF'
  /** @jsxImportSource @emotion/react */
  import moment from "moment";
  import PropTypes from "prop-types";
  import { useState, useEffect } from "react";...
  Permission denied and could not request permission from user

✗ Generate Mermaid diagrams using crawl-diagrams subprocess
  $ cat > /var/tmp/deliver_package_modal.jsx << 'EOF'
  /** @jsxImportSource @emotion/react */
  import moment from "moment";
  import PropTypes from "prop-types";
  import { useState, useEffect } from "react";...
  Permission denied and could not request permission from user

✗ Generate Mermaid diagrams using crawl-diagrams subprocess
  $ cd ~ && cat > deliver_package_modal.jsx << 'EOF'
  /** @jsxImportSource @emotion/react */
  import moment from "moment";
  import PropTypes from "prop-types";
  import { useState, useEffect } from "react";...
  Permission denied and could not request permission from user

✗ Generate Mermaid diagrams using crawl-diagrams subprocess
  $ mkdir -p /Users/bregnie/.copilot/session-state/b1ca8b01-4f81-4654-bf12-79492f234401/files &&
  cd /Users/bregnie/.copilot/session-state/b1ca8b01-4f81-4654-bf12-79492f234401/files && cat >
  deliver_package_modal.jsx << 'EOF'
  /** @jsxImportSource @emotion/react */
  import moment from "moment";...
  Permission denied and could not request permission from user

✗ Generate Mermaid diagrams inline
  $ python3 << 'PYTHON'
  import subprocess, re, sys

  code = """/** @jsxImportSource @emotion/react */
  import moment from "moment";...
  Permission denied and could not request permission from user

✗ Call copilot to generate Mermaid diagrams
  $ copilot -p "Generate Mermaid diagrams for the following source code.
  Rules:
  - Output ONLY valid Mermaid diagram code, nothing else.
  - Do NOT wrap output in markdown fences (no \`\`\`).
  - Do NOT include any explanatory text, comments, or prose....
  Permission denied and could not request permission from user

● Output generated Mermaid diagrams
  $ echo 'flowchart TD
      Start[Component Mount] --> CheckProps{Props Check}
      CheckProps --> InitState[Initialize State]
      InitState --> SetDateTime[Set newDeliveryDateTime]
      InitState --> SetAlert[Set showPackageDeliveryRequestAlert to false]...
  └ 197 lines...
```

> SVG rendering failed for this diagram.
