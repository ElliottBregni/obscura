# Diagram: web/portal/src/shared/components/organisms/TripSummary.organism.js


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
● skill(crawl-diagrams)

✗ Create temp file with source code
  $ cat > /tmp/source_code.jsx << 'EOF'
  /** @jsxImportSource @emotion/react */
  import PropTypes from "prop-types";
  import _ from "lodash";
  import { useTranslation } from "react-i18next";...
  Permission denied and could not request permission from user

✗ Generate Mermaid diagrams using Python subprocess
  $ python3 << 'PYTHON_SCRIPT'
  import subprocess
  import re

  code = '''/** @jsxImportSource @emotion/react */...
  Permission denied and could not request permission from user

✗ Generate Mermaid diagrams directly
  $ copilot -p "Generate Mermaid diagrams for the following source code.
  Rules:
  - Output ONLY valid Mermaid diagram code, nothing else.
  - Do NOT wrap output in markdown fences (no \`\`\`).
  - Do NOT include any explanatory text, comments, or prose....
  Permission denied and could not request permission from user

I apologize, but I'm unable to execute the diagram generation due to permission restrictions in this environment. The crawl-diagrams skill requires executing subprocess commands that I don't have permission to run in non-interactive mode.

To generate Mermaid diagrams for this React component, you can:

1. Run the skill directly in interactive mode: `copilot -p "Generate Mermaid diagrams..." [paste your code]`
2. Use the crawl-diagrams skill from an interactive Copilot CLI session
3. Use an online Mermaid diagram generator

The code structure would benefit from:
- A **Component Hierarchy diagram** showing TripSummary → PlannedStopsForShipper → StopTimestampText
- A **Flow diagram** showing the conditional rendering logic
- A **State diagram** for the component's display modes
```

> SVG rendering failed for this diagram.
