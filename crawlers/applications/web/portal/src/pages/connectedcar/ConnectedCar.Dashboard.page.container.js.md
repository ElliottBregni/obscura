# Diagram: web/portal/src/pages/connectedcar/ConnectedCar.Dashboard.page.container.js


> Auto-generated by Obscura crawlers

## Diagram 1

```mermaid
flowchart LR
  ConnectedCarView["ConnectedCar.Dashboard.page\n(ConnectedCarView)"]
  Connect["connect(mapStateToProps, mapDispatchToProps)"]
  ConnectedCarView -->|wrapped by| Connect

  subgraph StateSelectors [ConnectedCarSearchBarState.selectors]
    S_getSearchResults[getSearchResults(state)]
    S_getTotalEntities[getTotalEntities(state)]
    S_getIsLoadingError[getIsLoadingError(state)]
    S_getIsLoading[getIsLoading(state)]
    S_getIsExporting[getIsExporting(state)]
    S_getClickedSearchOrEnter[getClickedSearchOrEnter(state)]
    S_getAreThereFiltersSelected[getAreThereFiltersSelected(state)]
    S_getExportIdentifier[getExportIdentifier(state)]
    S_getExportName[getExportName(state)]
    S_getExportFailed[getExportFailed(state)]
  end

  subgraph ActionCreators [ConnectedCarSearchBarState.actionCreators]
    A_setPagination[setPagination(solutionId, page, limit, flag)]
    A_clickedSearchOrEnter[clickedSearchOrEnter(flag)]
    A_resetSearchBar[resetSearchBar()]
    A_clearSearchFilters[clearSearchFilters()]
    A_clearEntities[clearEntities()]
    A_resetExport[resetExport()]
    A_exportSearch[exportSearch(solutionId)]
  end

  MapState["modules/map/MapState"]
  MapState --> MapAction["setViewCentered(isCentered)"]

  OrganizationsState["../../modules/organizations/OrganizationsState\n(getSolutionId)"]

  Connect -->|uses| mapStateToProps["mapStateToProps(state)"]
  Connect -->|uses| mapDispatchToProps["mapDispatchToProps(dispatch)"]

  mapStateToProps --> S_getSearchResults
  mapStateToProps --> S_getTotalEntities
  mapStateToProps --> S_getIsLoadingError
  mapStateToProps --> S_getIsLoading
  mapStateToProps --> S_getIsExporting
  mapStateToProps --> S_getClickedSearchOrEnter
  mapStateToProps --> S_getAreThereFiltersSelected
  mapStateToProps --> S_getExportIdentifier
  mapStateToProps --> S_getExportName
  mapStateToProps --> S_getExportFailed
  mapStateToProps --> OrganizationsState
  mapStateToProps -->|spreads| EndToEnd["state.endToEnd"]

  mapDispatchToProps --> A_setPagination
  mapDispatchToProps --> A_clickedSearchOrEnter
  mapDispatchToProps --> A_resetSearchBar
  mapDispatchToProps --> A_clearSearchFilters
  mapDispatchToProps --> A_clearEntities
  mapDispatchToProps --> A_resetExport
  mapDispatchToProps --> A_exportSearch
  mapDispatchToProps --> MapAction

  A_setPagination -->|dispatch(...) with RECORD_LIMIT| RECORD_LIMIT[RECORD_LIMIT]
  RECORD_LIMIT -.->|constant import| ConnectedCarSearchBarState["ConnectedCarSearchBarState"]

  A_exportSearch -->|dispatch exportSearch(solutionId)| OrganizationsState
  A_setPagination --> OrganizationsState

  Connect --> ConnectedCarView
```

> SVG rendering failed for this diagram.

## Diagram 2

```mermaid
classDiagram
  class ConnectedCarView {
    <<React Component>>
  }
  class Connect {
    +function connect(mapStateToProps, mapDispatchToProps)
  }
  class ConnectedCarSearchBarState {
    +selectors
    +actionCreators
    +RECORD_LIMIT
  }
  class MapState {
    +actionCreators
  }
  class OrganizationsState {
    +getSolutionId(state)
  }
  ConnectedCarView --|> Connect : wrapped_by
  Connect ..> ConnectedCarSearchBarState : uses
  Connect ..> MapState : uses
  Connect ..> OrganizationsState : uses
  ConnectedCarSearchBarState o-- "selectors" : provides
  ConnectedCarSearchBarState o-- "actionCreators" : provides
  MapState o-- "actionCreators" : provides
  OrganizationsState : getSolutionId()
```

> SVG rendering failed for this diagram.
