# Diagram: web/portal/src/pages/connectedcar/search/ConnectedCarFilterSectionContainer.js


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
flowchart LR
  FilterSection["FilterSection\n(React Component)"]
  Connect["connect(mapStateToProps,\nmapDispatchToProps,\nmergeProps)"]
  mapStateToProps["mapStateToProps(state)"]
  mapDispatchToProps["mapDispatchToProps(dispatch)"]
  mergeProps["mergeProps(stateProps,\ndispatchProps, ownProps)"]
  ConnectedCarSearchBarState["ConnectedCarSearchBarState\n(selectors, actionCreators)"]
  ConnectedCarDomainData["ConnectedCarDomainDataState.selectors\n(getLifeCycleOptions,\ngetNgavsStatusCodeOptions,\ngetStatesOptions)"]
  AuthSelector["getAuthorization(state)"]
  OrgSelector["getSolutionId(state)"]
  FILTERS["FILTERS\n(ConnectedCarFilterSectionCategoryDefs)"]

  Connect --> FilterSection
  mapStateToProps --> Connect
  mapDispatchToProps --> Connect
  mergeProps --> Connect

  mapStateToProps --> ConnectedCarDomainData
  mapStateToProps --> AuthSelector
  mapStateToProps --> OrgSelector
  mapStateToProps --> ConnectedCarSearchBarState
  ConnectedCarSearchBarState --> mapStateToProps

  mapDispatchToProps --> ConnectedCarSearchBarState
  ConnectedCarSearchBarState --> mapDispatchToProps

  mergeProps --> FILTERS
  mergeProps --> mapStateToProps
  mergeProps --> mapDispatchToProps
  mergeProps --> "ownProps (spread into props)"
```

> SVG rendering failed for this diagram.
