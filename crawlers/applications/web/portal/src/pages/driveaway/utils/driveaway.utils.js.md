# Diagram: web/portal/src/pages/driveaway/utils/driveaway.utils.js


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
flowchart TD
  subgraph TranslateDenyReason
    TR_In["Input: t, denyReason"] --> TR_Check{denyReason?}
    TR_Check -- no --> TR_None[return undefined]
    TR_Check -- yes --> TR_Lower[denyReason.toLowerCase()]
    TR_Lower --> TR_Cases{Match case}
    TR_Cases -->|inoperable (meaning - battery, ...)| TR_T1["return t('driveaway-search:Inoperable (meaning - battery, flat tire, not in condition to handoff)')"]
    TR_Cases -->|damaged (meaning damage found - ...)| TR_T2["return t('driveaway-search:Damaged (meaning damage found - dings, dents, scratches - during the POD inspection, i.e., the dealer / dealer agent refuses during the POD inspection process)')"]
    TR_Cases -->|inoperable| TR_T3["return t('driveaway-search:Inoperable')"]
    TR_Cases -->|damaged| TR_T4["return t('driveaway-search:Damaged')"]
    TR_Cases -->|not onsite/not found| TR_T5["return t('driveaway-search:Not onsite/Not found')"]
    TR_Cases -->|dealer/dealer agent missed window, no show| TR_T6["return t('driveaway-search:Dealer/dealer agent missed window, no show')"]
    TR_Cases -->|built into preferred carrier load within next 48 hours| TR_T7["return t('driveaway-search:Built into preferred carrier load within next 48 hours')"]
    TR_Cases -->|other| TR_T8["return t('driveaway-search:Other')"]
    TR_Cases -->|default| TR_Default[return denyReason]
  end

  subgraph TranslateReasonExplanation
    RE_In["Input: t, reasonExplanation"] --> RE_Check{reasonExplanation?}
    RE_Check -- no --> RE_None[return undefined]
    RE_Check -- yes --> RE_Lower[reasonExplanation.toLowerCase()]
    RE_Lower --> RE_Cases{Match case}
    RE_Cases -->|meaning - battery, ...| RE_T1["return t('driveaway-search:meaning - battery, flat tire, not in condition to handoff')"]
    RE_Cases -->|meaning damage found - ...| RE_T2["return t('driveaway-search:meaning damage found - dings, dents, scratches - during the POD inspection, i.e., the dealer / dealer agent refuses during the POD inspection process')"]
    RE_Cases -->|default| RE_Default[return reasonExplanation]
  end

  subgraph DenyCodeToReadableFormat
    DC_In["Input: t, denyCode"] --> DC_Check{denyCode?}
    DC_Check -- no --> DC_None[return undefined]
    DC_Check -- yes --> DC_Switch{denyCode value}
    DC_Switch -->|NO_SHOW| DC_T1["return t('driveaway-search:No Show')"]
    DC_Switch -->|DAMAGED| DC_T2["return t('driveaway-search:Damaged')"]
    DC_Switch -->|CARRIER_ASSIGNED| DC_T3["return t('driveaway-search:Carrier Assigned')"]
    DC_Switch -->|INOPERABLE| DC_T4["return t('driveaway-search:Inoperable')"]
    DC_Switch -->|NOT_FOUND| DC_T5["return t('driveaway-search:Not Found')"]
    DC_Switch -->|OTHER| DC_T6["return t('driveaway-search:Other')"]
    DC_Switch -->|VIN_SHIPPED| DC_T7["return t('driveaway-search:VIN Shipped')"]
    DC_Switch -->|default| DC_Default[return denyCode]
  end

  subgraph TranslateStatusLabel
    TS_In["Input: t, label"] --> TS_Switch{label value}
    TS_Switch -->|DPUStatus.APPROVED| TS_T1["return t('driveaway-search:Approved')"]
    TS_Switch -->|DPUStatus.AVAILABLE| TS_T2["return t('driveaway-search:Available')"]
    TS_Switch -->|DPUStatus.DELIVERED| TS_T3["return t('driveaway-search:Delivered')"]
    TS_Switch -->|DPUStatus.DENIED| TS_T4["return t('driveaway-search:Denied')"]
    TS_Switch -->|DPUStatus.SUBMITTED| TS_T5["return t('driveaway-search:Submitted')"]
    TS_Switch -->|default| TS_Default[return label]
  end

  subgraph IsNumberOnly
    IN_In["Input: input"] --> IN_Re["re = /^[0-9]*$/g"]
    IN_Re --> IN_Test{input.length === 0 || re.test(input)}
    IN_Test -- true --> IN_True[return true]
    IN_Test -- false --> IN_False[return false]
  end

  subgraph FormatIntlPhoneNum
    FP_In["Input: phoneNumberString"] --> FP_Clean["cleaned = phoneNumberString.replace(/\\D/g, '')"]
    FP_Clean --> FP_Match["match = cleaned.match(/^(1|)?(\\d{3})(\\d{3})(\\d{4})$/)"]
    FP_Match --> FP_If{match?}
    FP_If -- yes --> FP_Format["intlCode = match[1] ? '+1 ' : ''\nreturn intlCode + '('+match[2]+') '+match[3]+'-'+match[4]"]
    FP_If -- no --> FP_Empty[return \"\"]
  end

  %% High-level relationships
  TR_Default -.-> DC_In
  DC_Default -.-> TS_In
  RE_Default -.-> FP_In
  IN_True -.-> FP_In
```

> SVG rendering failed for this diagram.
