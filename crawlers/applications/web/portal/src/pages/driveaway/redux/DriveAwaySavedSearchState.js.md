# Diagram: web/portal/src/pages/driveaway/redux/DriveAwaySavedSearchState.js


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
classDiagram
    class buildSavedSearchState <<factory>> {
        +Function(config)
    }
    class DriveAwaySavedSearch <<module>> {
        +topic: "driveAwaySavedSearch"
        +savedSearchUrl: "/preferences-ng/search-history"
        +searchType: "DRIVEAWAY"
        +searchCategories
        +actions
    }
    class apiUrl <<module>> {
        +Function(path)
    }
    class SEARCH_CATEGORIES <<const>> {
        +Object
    }
    class SearchState <<module>> {
        class actionCreators {
            +clearSearchFilters()
            +setSearchCategory()
            +setSearchText()
            +setSearchFilter()
        }
    }

    buildSavedSearchState --> DriveAwaySavedSearch : returns
    DriveAwaySavedSearch ..> apiUrl : uses
    DriveAwaySavedSearch ..> SEARCH_CATEGORIES : uses
    DriveAwaySavedSearch ..> SearchState : references.actionCreators
    apiUrl --> "/preferences-ng/search-history" : invokedWith
    SEARCH_CATEGORIES <-- "../components/search/DriveAway.searchOptions"
    SearchState <-- "./DriveAwaySearchBarState"
```

> SVG rendering failed for this diagram.
