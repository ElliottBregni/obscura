# Diagram: web/portal/src/pages/driveaway/components/table-cells/status/PickupWindowField.js


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
flowchart LR
  A[PickupWindowField(props: pickupWindows, pickUpWindowTz)] --> B{pickupWindows?.length > 0}
  B -- no --> Z[null / render nothing]
  B -- yes --> C[Render header Text + Tooltip + Icon]
  C --> D{pickUpWindowTz && pickupWindows[0].startTs && pickupWindows[0].endTs (not nil)}
  D -- false --> E[Render Text: "Pickup Window unavailable, please contact FreightVerify"]
  D -- true --> F{pickupWindows.length === 1}
  F -- true --> G[Render formatted single window using moment.utc(...).tz(pickUpWindowTz).format(dateFormat timeFormat z)]
  F -- false --> H[Render Button "Show All Pickup Windows"]
  H --> I[onClick -> setShowTimeWindowsDialog(true)]
  I --> J[PickUpWindowsModal(show=showTimeWindowsDialog, onHide -> setShowTimeWindowsDialog(false), pickupWindow=pickupWindows, pickupWindowTz)]
  G --> J
  E --> J
  style A fill:#f9f,stroke:#333,stroke-width:1px
  style J fill:#eef,stroke:#333,stroke-width:1px
```

> SVG rendering failed for this diagram.
