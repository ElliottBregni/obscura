# Diagram: web/portal/src/pages/driveaway/dashboard/components/vin-history-search/DriveAway.VinHistory.SearchBar.container.js


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
flowchart LR
  subgraph Redux_State["Redux State"]
    State((state))
  end

  subgraph Selectors["SearchBarState.selectors"]
    S_getSearchText[getSearchText(state)]
    S_getSearchCategory[getSearchCategory(state)]
    S_getTypeahead[getTypeaheadOptionsMetadata(state)]
    S_getHasChanged[getHasSearchCriteriaChanged(state)]
    S_getVins[getVins(state)]
    S_getVinsLoading[getVinsLoading(state)]
  end

  subgraph ActionCreators["SearchBarState.actionCreators"]
    A_setCategory[setSearchCategoryForKey(key)]
    A_setText[setSearchText(text, ignoreValueChange)]
    A_clearText[clearSearchText()]
    A_reset[resetSearchBar()]
    A_searchEntities[searchEntities(solutionId, resetPagination, preventRedirect)]
    A_clearSaved[clearSavedSearch()]
    A_searchVins[searchVins(query)]
  end

  subgraph OtherSelectors["External Selectors"]
    O_getSolutionId[getSolutionId(state)]
    O_getAuth[getAuthorization(state)]
  end

  State --> S_getSearchText
  State --> S_getSearchCategory
  State --> S_getTypeahead
  State --> S_getHasChanged
  State --> S_getVins
  State --> S_getVinsLoading
  State --> O_getSolutionId
  State --> O_getAuth

  S_getSearchText --> mapState[mapStateToProps]
  S_getSearchCategory --> mapState
  S_getTypeahead --> mapState
  S_getHasChanged --> mapState
  S_getVins --> mapState
  S_getVinsLoading --> mapState
  O_getSolutionId --> mapState
  O_getAuth --> mapState

  A_setCategory --> mapDispatch[mapDispatchToProps]
  A_setText --> mapDispatch
  A_clearText --> mapDispatch
  A_reset --> mapDispatch
  A_searchEntities --> mapDispatch
  A_clearSaved --> mapDispatch
  A_searchVins --> mapDispatch

  mapState --> merge[mergeProps]
  mapDispatch --> merge
  ownProps[ownProps] --> merge

  merge --> connectFn[connect(mapStateToProps, mapDispatchToProps, mergeProps)]
  connectFn --> ConnectedSearchBar[Connected SearchBar]
  ConnectedSearchBar --> SearchBarComponent[SearchBar Component]

  style State fill:#f9f,stroke:#333,stroke-width:1px
  style mapState fill:#fffbcc,stroke:#666
  style mapDispatch fill:#fffbcc,stroke:#666
  style merge fill:#cfeffc,stroke:#666
  style connectFn fill:#dcdcdc,stroke:#333
  style ConnectedSearchBar fill:#e6ffe6,stroke:#2d862d
  style SearchBarComponent fill:#ffffff,stroke:#333
```

> SVG rendering failed for this diagram.
