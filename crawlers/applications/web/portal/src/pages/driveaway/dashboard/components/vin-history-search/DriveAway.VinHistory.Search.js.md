# Diagram: web/portal/src/pages/driveaway/dashboard/components/vin-history-search/DriveAway.VinHistory.Search.js


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
classDiagram
    class VinHistorySearch {
        +bool isLoading
        +bool showError
        +string showErrorMessage
        +array searchResults
        +BaseTablePropTypes tableProps
        +elementType SearchBarContainer
        +render()
    }

    class BaseTable {
        +theme
        +data
        +...tableProps
    }

    class SearchBarContainer {
        +isShowingAdvancedSearchButton
        +showSearchOptions
        +isShowingFilters
        +toggleShowFilters
    }

    class Alert {
        +variant
        +show
        +style
    }

    class Themes {
        <<enumeration>>
        LIGHT
        DARK
    }

    class AlertVariant {
        <<enumeration>>
        Danger
        Info
        Warning
        Success
    }

    class BaseTablePropTypes {
        +columns
        +pagination
        +sort
        +...props
    }

    VinHistorySearch --> BaseTable : renders
    VinHistorySearch --> SearchBarContainer : instantiates
    VinHistorySearch --> Alert : conditionally renders
    BaseTable ..> Themes : uses
    Alert ..> AlertVariant : uses
    VinHistorySearch ..> BaseTablePropTypes : expects
    VinHistorySearch ..> searchResults : reads
    note right of VinHistorySearch : Hides data when isLoading or showError is true
```

> SVG rendering failed for this diagram.
