# Diagram: web/portal/src/pages/shipments/redux/ShipmentsSavedSearchState.js


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
graph TD
apiUrl["apiUrl(\"/preferences-ng/search-history\")"] --> build["buildSavedSearchState(...)"]
topic["topic: \"shipmentSavedSearch\""] --> build
savedUrl["savedSearchUrl: apiUrl(...)"] --> build
stype["searchType: \"SHIPMENT\""] --> build
scats["searchCategories: SEARCH_CATEGORIES"] --> build
actions["actions: { clearSearchFilters, setSearchCategory, setSearchText, setSearchFilter }"] --> build
SEARCH_CATEGORIES["SEARCH_CATEGORIES (import)"] --> scats
Shipments["ShipmentsSearchBarState.actionCreators (import)"] --> actions
build --> Saved["SavedSearchState (const)"]
Saved --> Export["export default SavedSearchState"]
```

> SVG rendering failed for this diagram.
