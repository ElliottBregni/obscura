# Diagram: web/portal/src/pages/shipments/redux/InboundArrivalStatusWidgetState.js


> Auto-generated by Obscura crawlers

## Diagram 1

```mermaid
flowchart TD
    subgraph Imports
        lodash((lodash))
        apiUrl["apiUrl"]
        buildFetchDuck["buildFetchDuck"]
        buildSearchBarState["buildSearchBarState"]
        getBasicQueryString["getBasicQueryString"]
    end

    INBOUND_ARRIVAL_STATUS_URL["INBOUND_ARRIVAL_STATUS_URL\n(apiUrl(`/shipping-ng/destination-arrival-status`))"]
    getInboundArrivalStatusUrl["getInboundArrivalStatusUrl(solutionId, queryString)"]
    INBOUND_ARRIVAL_STATUS_URL --> getInboundArrivalStatusUrl

    transformArrivalStatusDetailsData["transformArrivalStatusDetailsData(data)"]
    duckInboundArrivalStatusCount["duckInboundArrivalStatusCount\n(buildFetchDuck(STORE_MOUNT_POINT,'count'))"]
    fetchCount["fetchCount(status, locationCode)"]
    fetchShipments["fetchShipments(queryString, _solutionId_ignored, duck, dispatch)"]
    InboundArrivalStatusWidgetState["InboundArrivalStatusWidgetState\n(buildSearchBarState(...))"]

    fetchCount -->|calls| duckInboundArrivalStatusCount
    fetchCount -->|uses| INBOUND_ARRIVAL_STATUS_URL
    duckInboundArrivalStatusCount -->|fetch -> transform| transformArrivalStatusDetailsData
    transformArrivalStatusDetailsData -->|returns sorted list of {id,name,code,count}| InboundArrivalStatusWidgetState

    fetchShipments -->|calls| getInboundArrivalStatusUrl
    fetchShipments -->|dispatches duck.fetch| duckInboundArrivalStatusCount
    InboundArrivalStatusWidgetState -->|uses| fetchShipments
    InboundArrivalStatusWidgetState -->|includes reducer| duckInboundArrivalStatusCount

    subgraph ActionsSelectors
        A1["actionCreators.fetchCount -> fetchCount"]
        A2["actionCreators.exportSearch -> exportEntities partial"]
        S1["selectors.getCountData -> duckInboundArrivalStatusCount.selectors.getData"]
    end

    InboundArrivalStatusWidgetState --> ActionsSelectors
    duckInboundArrivalStatusCount --> S1
```

> SVG rendering failed for this diagram.

## Diagram 2

```mermaid
classDiagram
    class buildFetchDuck {
        <<factory>>
        +(mountPoint, name)
    }
    class buildSearchBarState {
        <<factory>>
        +(mountPoint, categories, queryBuilders, fetchFn, reducers, options)
    }
    class duckInboundArrivalStatusCount {
        +fetch(url, config, transform?)
        +selectors.getData()
        +reducer()
    }
    class InboundArrivalStatusWidgetState {
        +actionCreators
        +selectors
        +defaultSort
    }
    class transformArrivalStatusDetailsData {
        +(data) : Array<{id,name,code,count}>
    }
    class fetchCount {
        +(status, locationCode) : Function(dispatch)
    }
    class fetchShipments {
        +(queryString, _solutionId_ignored, duck, dispatch)
    }
    class getInboundArrivalStatusUrl {
        +(solutionId, queryString) : string
    }

    buildFetchDuck <|-- duckInboundArrivalStatusCount
    buildSearchBarState <|-- InboundArrivalStatusWidgetState
    InboundArrivalStatusWidgetState --> fetchCount : actionCreators.fetchCount
    InboundArrivalStatusWidgetState --> fetchShipments : fetch function
    fetchCount --> duckInboundArrivalStatusCount : uses.fetch
    fetchCount --> transformArrivalStatusDetailsData : passes as transform
    fetchShipments --> getInboundArrivalStatusUrl : builds URL
    duckInboundArrivalStatusCount --> transformArrivalStatusDetailsData : transform function used in fetch
```

> SVG rendering failed for this diagram.
