# Diagram: web/portal/src/pages/shipments/redux/InboundLiveDwellWidgetState.js


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
classDiagram
    class Module {
        +STORE_MOUNT_POINT: string
        +INBOUND_LIVE_DWELL_URL: string
        +getInboundLiveDwellUrl(solutionId, queryString)
        +transformLiveDwellDetailsData(data)
        +fetchCount(locationCode)
        +fetchShipments(queryString, _solutionId_ignored, duck, dispatch)
        +InboundLiveDwellWidgetState
        +exportSearch(...)
    }
    class External {
        +_: lodash
        +apiUrl()
        +buildFetchDuck()
        +buildSearchBarState()
        +getBasicQueryString()
    }
    class duckLiveDwellCount {
        +fetch(url, config, transform?)
        +reducer
        +selectors.getData()
    }
    class InboundLiveDwellWidgetState {
        +actionCreators
        +selectors
    }
    Module "1" --> External : imports
    Module "1" o-- duckLiveDwellCount : creates
    Module "1" --> InboundLiveDwellWidgetState : builds
    duckLiveDwellCount --> InboundLiveDwellWidgetState : provides reducer/selectors
    InboundLiveDwellWidgetState : defaultSort = "destination_earliest_arrival"
    InboundLiveDwellWidgetState : actionCreators.fetchCount -> Module.fetchCount
    InboundLiveDwellWidgetState : selectors.getCountData -> duckLiveDwellCount.selectors.getData
    class transformLiveDwellDetailsData {
        +input: data[]
        +output: sorted array of {id,name,code,count}
    }
    Module --> transformLiveDwellDetailsData : defines
    class getInboundLiveDwellUrl {
        +returns URL string with queryString
    }
    Module --> getInboundLiveDwellUrl : defines
    note right of Module::fetchCount
      fetchCount dispatches duckLiveDwellCount.fetch with COUNT headers
    endnote
    note right of Module::fetchShipments
      fetchShipments dispatches duck.fetch with DETAIL headers using getInboundLiveDwellUrl
    endnote
```

> SVG rendering failed for this diagram.
