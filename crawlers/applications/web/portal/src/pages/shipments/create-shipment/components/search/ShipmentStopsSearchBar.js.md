# Diagram: web/portal/src/pages/shipments/create-shipment/components/search/ShipmentStopsSearchBar.js


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
flowchart LR
  Props[Props: shipperLocations, label, ind, shipperLocationsLoading, eventHandler, stop, setSearchText, searchLocations, selectedShipper]
  SearchBarComponent[SearchBar Component]
  useRefLoc[useRef: location.current -> shipperLocations]
  useEffect[useEffect: sync location.current on shipperLocations change]
  getLocations[getLocations(ind) → locations[ind] or []]
  getLocationTZ[getLocationTimeZone(ind, locationId) → location.time_zone]
  customStyles[customStyles]
  AsyncPaginate[AsyncPaginate (react-select-async-paginate)]
  TranslationComp[Translation(ns="create-shipment")]
  onChangeHandler[onChange: eventHandler(ind, "location"/"location_name"/"timezone")]
  loadOptions[loadOptions (debounced 500ms)]
  setSearchTextNode[setSearchText(input, ind)]
  searchLocationsNode[searchLocations(org_id, ind) -> returns locations]
  mapOptions[map locations -> {value: id, label: "name (code)"}]
  stopValue[stop.location, stop.location_name]

  Props --> SearchBarComponent
  SearchBarComponent --> useRefLoc
  SearchBarComponent --> useEffect
  SearchBarComponent --> customStyles
  SearchBarComponent --> TranslationComp
  TranslationComp --> AsyncPaginate
  AsyncPaginate --> stopValue
  AsyncPaginate --> onChangeHandler
  AsyncPaginate --> loadOptions
  onChangeHandler --> eventHandler
  loadOptions --> |input length < 2| emptyResult[return {options: [], hasMore: false}]
  loadOptions --> |input length >= 2| setSearchTextNode
  setSearchTextNode --> searchLocationsNode
  searchLocationsNode --> getLocations
  getLocations --> mapOptions
  mapOptions --> loadOptions
  getLocationTZ --> getLocations
  onChangeHandler --> getLocationTZ
  Props --> AsyncPaginate
  customStyles --> AsyncPaginate
```

> SVG rendering failed for this diagram.
