# Diagram: web/portal/src/pages/shipments/create-shipment/CreateShipment.page.js


> Auto-generated by Obscura crawlers

## Diagram 1

```mermaid
classDiagram
    class CreateShipment {
        - data: object {customer,assetID,shipmentID,trailerID,routeID,origin,destination,stops,selectedShipper}
        - tabIndex: int
        - validation: object
        + updateField(field, val)
        + updateStop(n, field, val)
        + addStop()
        + removeStop(n)
        + reorderStop(from, to)
        + changeTab(n)
        + checkStopValidation(stops)
        + resetForm()
        + isValid(data) : object
    }
    class STOP_INIT {
        + location: null
        + stopType: null
        + stopRole: null
        + earlyArrival: null
        + lateArrival: null
        + timezone: USER_TIMEZONE
    }
    class USER_TIMEZONE {
        <<constant>>
    }
    class ShipmentInfoForm
    class ShipmentStopsForm
    class ShipmentFormControls
    class hooks {
        + useSetTitleOnMount()
        + useSetDescriptionOnMount()
        + useTrackWithMixpanelOnce()
        + useOrganizations()
    }
    class utils {
        + moment, moment-timezone, lodash, immer:produce
        + checkValidation()
    }

    CreateShipment --> ShipmentInfoForm : renders
    CreateShipment --> ShipmentStopsForm : renders
    CreateShipment --> ShipmentFormControls : renders
    CreateShipment ..> STOP_INIT : uses
    CreateShipment ..> USER_TIMEZONE : reads
    CreateShipment ..> hooks : calls
    CreateShipment ..> utils : uses
    ShipmentStopsForm --> locationOptions : receives
    ShipmentFormControls --> validation : receives
    STOP_INIT <|-- USER_TIMEZONE
```

> SVG rendering failed for this diagram.

## Diagram 2

```mermaid
graph LR
    Start([Start: Mount CreateShipment]) --> InitState[Initialize state: data = {origin,destination,stops,...}, tabIndex=0]
    InitState --> SetTitle["useSetTitleOnMount / useSetDescriptionOnMount"]
    InitState --> Track["useTrackWithMixpanelOnce: View event"]
    User[User interactions] -->|select customer| UpdateField[updateField(customer)]
    User -->|edit stop fields| UpdateStop[updateStop(n, field, val)]
    User -->|add stop| AddStop[addStop()]
    User -->|remove stop| RemoveStop[removeStop(n)]
    User -->|reorder stops| Reorder[reorderStop(from,to)]
    UpdateField --> DataUpdated[setData via immer]
    UpdateStop --> DataUpdated
    AddStop --> DataUpdated
    RemoveStop --> DataUpdated
    Reorder --> DataUpdated
    DataUpdated --> Validation[isValid(data)]
    Validation --> UIUpdate[render validation in ShipmentStopsForm & ShipmentFormControls]
    UIUpdate --> Submit[createShipment invoked]
    Submit -->|success| Created([CreateStatus = CREATED])
    Submit -->|duplicate| Duplicate([CreateStatus = DUPLICATE])
    Submit -->|error| Error([CreateStatus = ERROR])
    Created --> ResetForm["resetForm() called (after timeout)"]
    Error --> ResetTimeout["resetCreateForm called after timeout"]
    Duplicate --> ResetTimeout
```

> SVG rendering failed for this diagram.
