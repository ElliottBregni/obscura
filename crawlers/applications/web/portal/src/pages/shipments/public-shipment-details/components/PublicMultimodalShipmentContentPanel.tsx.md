# Diagram: web/portal/src/pages/shipments/public-shipment-details/components/PublicMultimodalShipmentContentPanel.tsx


> Auto-generated by Obscura crawlers

## Diagram 1

```mermaid
classDiagram
    class PublicMultimodalShipmentContentPanel {
        +parentShipment: ShipmentTrip
        +childShipments: {number: Shipment}
        +organization: Organization
        +setSelectedLegId(id)
        +render()
    }
    class PublicMultimodalShipmentEndToEndTabPanel {
        +parentShipment: ShipmentTrip
        +childShipments: {number: Shipment}
        +render()
    }
    class Tabs {
        +TabList()
        +TabPanel()
    }
    class ShipmentDetailsCollapsible
    class ArrivalAndDeparturePanelGroup
    class PublicShipmentContentPanel
    class Utils {
        +getLocationDataFromShipments()
        +getTrailerAndEquipmentNumberFromChildShipments()
        +endToEndTab()
        +legTabs()
    }
    PublicMultimodalShipmentContentPanel "1" --> "1" Tabs : uses
    PublicMultimodalShipmentContentPanel "1" --> "1" PublicMultimodalShipmentEndToEndTabPanel : renders
    PublicMultimodalShipmentContentPanel "0..*" --> "0..*" PublicShipmentContentPanel : renders legs
    PublicMultimodalShipmentEndToEndTabPanel "1" --> "1" ShipmentDetailsCollapsible : renders
    PublicMultimodalShipmentEndToEndTabPanel "1" --> "1" ArrivalAndDeparturePanelGroup : renders
    PublicMultimodalShipmentEndToEndTabPanel "1" --> Utils : uses
    PublicMultimodalShipmentContentPanel "1" --> Utils : uses
```

> SVG rendering failed for this diagram.

## Diagram 2

```mermaid
graph TD
    A[Parent Shipment (parentShipment)] --> B[child_shipments list]
    B --> C[childShipments map]
    C --> D[getLocationDataFromShipments()]
    C --> E[getTrailerAndEquipmentNumberFromChildShipments()]
    D --> F[origin, destination, frozenEta, isFvEta]
    E --> G[trailerAndEquipmentNumbers (uniq)]
    F --> H[calculate calculatedEta (parent.destination_eta or frozenEta)]
    H --> I[PublicMultimodalShipmentEndToEndTabPanel props]
    G --> I
    I --> J[ShipmentDetailsCollapsible: render header/status/trailer numbers]
    I --> K[ArrivalAndDeparturePanelGroup: render origin/destination/ETA/messages]
    L[isExtraLarge from useIsExtraLarge()] --> M[Tabs.TabList maxTabs]
    M --> N[endToEndTab + legTabs]
    N --> O[Tabs.TabPanel list]
    O --> I
    O --> P[PublicShipmentContentPanel for each leg]
```

> SVG rendering failed for this diagram.
