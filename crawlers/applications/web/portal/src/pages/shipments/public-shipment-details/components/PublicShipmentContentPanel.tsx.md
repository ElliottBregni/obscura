# Diagram: web/portal/src/pages/shipments/public-shipment-details/components/PublicShipmentContentPanel.tsx


> Auto-generated by Obscura crawlers

## Diagram 1

```mermaid
classDiagram
    class PublicShipmentContentPanel {
        +shipment: Shipment
        +organization: Organization
        +showEta?: boolean
        +render()
    }
    class ShipmentDetailsCollapsible {
        +shipmentId
        +assetId
        +mode
        +status
        +shipmentType
        +shipperName
        +carrierName
        +carrierScac
        +currentRoad
        +trailerAndEquipmentNumbers
        +serviceCode
        +showException
        +showMadChiclet
        +railTrainId
        +loadedStatus
    }
    class ArrivalAndDeparturePanelGroup {
        +origin
        +destination
        +eta
        +etaMessage
        +onlyShowEtaDate
        +showEtaAtTimezone
        +hideScheduled
        +etaWarningMessage
        +isFvEta
        +visibleFields
        +availableForUnloadTs
        +SubHeaders
        +LastUpdateSubHeader
    }
    class Hooks {
        +useTranslation(namespace)
        +useShipmentTranslation()
    }
    class Utils {
        +getLocationDataFromShipments(shipments)
        +extractLastReportedAt(...)
        +getAssetId(org, shipment)
        +getReferenceValue(shipment, key)
    }
    class Lodash {
        +uniq(array)
        +isNil(value)
    }
    PublicShipmentContentPanel --> ShipmentDetailsCollapsible : renders
    PublicShipmentContentPanel --> ArrivalAndDeparturePanelGroup : renders
    PublicShipmentContentPanel ..> Hooks : uses
    PublicShipmentContentPanel ..> Utils : calls
    PublicShipmentContentPanel ..> Lodash : uses
    ArrivalAndDeparturePanelGroup o-- "SubHeaders" : contains
    ArrivalAndDeparturePanelGroup.SubHeaders ..> ArrivalAndDeparturePanelGroup.LastUpdateSubHeader : includes
    Utils --> ArrivalAndDeparturePanelGroup : provides location data
    Utils --> PublicShipmentContentPanel : provides reportedAt, assetId, serviceCode
    Lodash --> PublicShipmentContentPanel : uniq/isNil used for data processing
```

> SVG rendering failed for this diagram.

## Diagram 2

```mermaid
flowchart TD
    Props[Props: shipment, organization, showEta?] --> Extract[Extract basic values]
    Extract -->|mode,status| ModeStatus[mode, status]
    Extract -->|getLocationDataFromShipments| LocationData[origin,destination,calculatedEta,frozenEta,isFvEta]
    Extract -->|getAssetId| AssetId[assetId]
    Extract -->|uniq(trailer_equipment_number)| TrailerNums[trailerAndEquipmentNumbers]
    Extract -->|getReferenceValue(service_code)| ServiceCode[serviceCode]
    Extract -->|extractLastReportedAt(...)| ReportedAt[reportedAt{lastCity,lastTime}]
    LocationData --> DetermineEta[determine displayedEta]
    ModeStatus --> OnlyShowDate[determine onlyShowEtaDate]
    DetermineEta --> ArrivalGroup[ArrivalAndDeparturePanelGroup]
    OnlyShowDate --> ArrivalGroup
    AssetId --> ShipmentDetails[ShipmentDetailsCollapsible]
    TrailerNums --> ShipmentDetails
    ServiceCode --> ShipmentDetails
    ModeStatus --> ShipmentDetails
    ReportedAt --> ArrivalGroup.SubHeaders[LastUpdateSubHeader\n(location, ts)]
    ArrivalGroup --> ArrivalGroup.SubHeaders
    ShipmentDetails --> RenderOut[Rendered UI]
    ArrivalGroup --> RenderOut
    RenderOut --> End[User-visible Shipment Panel]
```

> SVG rendering failed for this diagram.
