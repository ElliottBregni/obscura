# Diagram: web/portal/src/pages/shipments/public-shipment-details/PublicShipmentDetail.page.container.js


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
flowchart LR
  subgraph ReduxState["Redux State"]
    State[(state)]
  end

  subgraph Selectors["PublicShipmentDetailState.selectors"]
    getShipmentDetails["getShipmentDetails(state)"]
    getChildShipmentDetails["getChildShipmentDetails(state)"]
    getRouteHeatmap["getRouteHeatmap(state)"]
    getIsLoaded["getIsLoaded(state)"]
    getActiveOrganization["getActiveOrganization(state)"]
  end

  subgraph ActionCreators["PublicShipmentDetailState.actionCreators"]
    fetchShipmentDetails["fetchShipmentDetails(shipmentID)"]
  end

  subgraph Connect["react-redux connect(...)"]
    mapStateToProps["mapStateToProps(state) -> props"]
    mapDispatchToProps["mapDispatchToProps(dispatch) -> props"]
    ConnectedComponent["Connected(PublicShipmentDetailPage)"]
  end

  State -->|location.query.id| mapStateToProps
  State --> getShipmentDetails
  State --> getChildShipmentDetails
  State --> getRouteHeatmap
  State --> getIsLoaded
  State --> getActiveOrganization

  getShipmentDetails -->|value| mapStateToProps
  getChildShipmentDetails -->|value| mapStateToProps
  getRouteHeatmap -->|value| mapStateToProps
  getIsLoaded -->|value| mapStateToProps
  getActiveOrganization -->|value| mapStateToProps

  mapStateToProps -->|returns shipmentId, shipment, childShipments, routeHeatmap, activeOrganization, isLoaded| ConnectedComponent

  fetchShipmentDetails -->|action creator| mapDispatchToProps
  mapDispatchToProps -->|provides fetchShipmentDetails(shipmentID) -> dispatch(...)| ConnectedComponent

  ConnectedComponent --> PublicShipmentDetailPage[/"PublicShipmentDetailPage"/]
```

> SVG rendering failed for this diagram.
