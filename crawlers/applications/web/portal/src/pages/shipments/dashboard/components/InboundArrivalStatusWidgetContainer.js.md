# Diagram: web/portal/src/pages/shipments/dashboard/components/InboundArrivalStatusWidgetContainer.js


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
flowchart LR
  subgraph StateModule["InboundArrivalStatusWidgetState"]
    direction TB
    Selectors[("selectors")]
    ActionCreators[("actionCreators")]
    Selectors --> getCountData["getCountData(state)"]
    Selectors --> getSearchResults["getSearchResults(state)"]
    Selectors --> getIsLoading["getIsLoading(state)"]
    Selectors --> getPage["getPage(state)"]
    Selectors --> getPageSize["getPageSize(state)"]
    Selectors --> getTotalPages["getTotalPages(state)"]
    Selectors --> getSortColumn["getSortColumn(state)"]
    Selectors --> getReverseSort["getReverseSort(state)"]
    Selectors --> getExportName["getExportName(state)"]
    Selectors --> getIsExporting["getIsExporting(state)"]
    Selectors --> getExportFailed["getExportFailed(state)"]
    Selectors --> getExportIdentifier["getExportIdentifier(state)"]
    ActionCreators --> fetchCount["fetchCount"]
    ActionCreators --> searchEntities["searchEntities (fetchShipments)"]
    ActionCreators --> setSearchFilter["setSearchFilter"]
    ActionCreators --> setPagination["setPagination"]
    ActionCreators --> setSort["setSort"]
    ActionCreators --> exportSearch["exportSearch"]
    ActionCreators --> resetExport["resetExport"]
  end

  subgraph MapState["mapStateToProps(state)"]
    direction LR
    MS_count[countData]
    MS_results[shipmentResults]
    MS_loading[isShipmentsLoading]
    MS_page[page]
    MS_pageSize[pageSize]
    MS_totalPages[totalPages]
    MS_sortColumn[sortColumn]
    MS_reverseSort[reverseSort]
    MS_exportName[exportName]
    MS_isExporting[isExporting]
    MS_exportFailed[exportFailed]
    MS_exportIdentifier[exportIdentifier]
  end

  subgraph Actions["actionCreators (object)"]
    direction TB
    A_fetchCount[fetchCount]
    A_fetchShipments[fetchShipments]
    A_setSearchFilter[setSearchFilter]
    A_setPagination[setPagination]
    A_setSort[setSort]
    A_pushShipmentDetailView[pushShipmentDetailView(id)]
    A_exportSearch[exportSearch]
    A_resetExport[resetExport]
  end

  Selectors -->|provides| MS_count
  Selectors -->|provides| MS_results
  Selectors -->|provides| MS_loading
  Selectors -->|provides| MS_page
  Selectors -->|provides| MS_pageSize
  Selectors -->|provides| MS_totalPages
  Selectors -->|provides| MS_sortColumn
  Selectors -->|provides| MS_reverseSort
  Selectors -->|provides| MS_exportName
  Selectors -->|provides| MS_isExporting
  Selectors -->|provides| MS_exportFailed
  Selectors -->|provides| MS_exportIdentifier

  ActionCreators -->|maps to| A_fetchCount
  ActionCreators -->|maps to| A_fetchShipments
  ActionCreators -->|maps to| A_setSearchFilter
  ActionCreators -->|maps to| A_setPagination
  ActionCreators -->|maps to| A_setSort
  ActionCreators -->|maps to| A_exportSearch
  ActionCreators -->|maps to| A_resetExport

  A_pushShipmentDetailView -->|custom action| PushAction["{ type: 'SHIPMENT_DETAIL', payload: { shipment_id: id } }"]

  subgraph Connect["connect(mapStateToProps, actionCreators)"]
    direction TB
    ConnectedComponent[InboundArrivalStatusWidgetConnected]
  end

  MapState --> Connect
  Actions --> Connect
  Connect --> InboundArrivalStatusWidget[InboundArrivalStatusWidget (presentational)]
  PushAction --> Actions
```

> SVG rendering failed for this diagram.
