# Diagram: web/portal/src/pages/shipments/components/search/Shipments.EditSavedSearchModal.container.js


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
classDiagram
    class EditSavedSearchModal {
        <<Component>>
        +props
    }
    class Connect {
        +mapStateToProps(state)
        +mapDispatchToProps(dispatch, ownProps)
    }
    class SearchBarContainer {
        <<Container>>
    }
    class FilterSectionContainer {
        <<Container>>
    }
    class ShipmentsSearchBarState {
        <<ReduxState>>
        +selectors.getSearchFilters(state)
        +selectors.getSearchCategory(state)
        +selectors.getSearchText(state)
    }
    class ShipmentsSavedSearchState {
        <<ReduxState>>
        +actionCreators.saveSearch(name, search)
        +actionCreators.editSearch(id, name, search)
    }
    class ShipmentSavedSearchCardDataState {
        <<ReduxState>>
        +actionCreators.fetchSavedSearchCardData(...params)
    }
    class ShipmentsSavedSearchSubscriptionState {
        <<ReduxState>>
        +selectors.getIsSubscriptionLoading(state)
        +selectors.getSubscription(state)
        +selectors.getSubscriptionRequestError(state)
        +selectors.getIsSubscriptionUpdating(state)
        +selectors.getSubscriptionUpdateSuccess(state)
        +selectors.getSubscriptionUpdateInProgress(state)
        +selectors.getSubscriptionUpdateError(state)
        +actionCreators.fetchSubscription(...params)
        +actionCreators.subscribe(...params)
        +actionCreators.updateSubscription(...params)
        +actionCreators.refreshSubscription(...params)
        +actionCreators.unsubscribe(...params)
        +actionCreators.deleteSubscription(...params)
    }

    Connect --> EditSavedSearchModal : wraps
    Connect --> ShipmentsSearchBarState : uses selectors
    Connect --> ShipmentsSavedSearchSubscriptionState : uses selectors & actionCreators
    Connect --> ShipmentsSavedSearchState : uses actionCreators
    Connect --> ShipmentSavedSearchCardDataState : uses actionCreators
    Connect --> SearchBarContainer : injects as prop
    Connect --> FilterSectionContainer : injects as prop

    EditSavedSearchModal o-- "props.searchFilters" : receives
    EditSavedSearchModal o-- "props.searchCategory" : receives
    EditSavedSearchModal o-- "props.searchText" : receives
    EditSavedSearchModal o-- "props.saveSearch(name,search)" : dispatches
    EditSavedSearchModal o-- "props.editSearch(id,name,search)" : dispatches
    EditSavedSearchModal o-- "props.fetchSavedSearchCardData(...)" : dispatches
    EditSavedSearchModal o-- "props.isSavedSearchSubscriptionLoading" : receives
    EditSavedSearchModal o-- "props.savedSearchSubscription" : receives
    EditSavedSearchModal o-- "props.subscriptionRequestError" : receives
    EditSavedSearchModal o-- "props.isSubscriptionUpdating" : receives
    EditSavedSearchModal o-- "props.subscriptionUpdateSuccess" : receives
    EditSavedSearchModal o-- "props.subscriptionUpdateInProgress" : receives
    EditSavedSearchModal o-- "props.subscriptionUpdateError" : receives
    EditSavedSearchModal o-- "props.fetchSavedSearchSubscription(...)" : dispatches
    EditSavedSearchModal o-- "props.subscribe(...)" : dispatches
    EditSavedSearchModal o-- "props.updateSubscription(...)" : dispatches
    EditSavedSearchModal o-- "props.refreshSubscription(...)" : dispatches
    EditSavedSearchModal o-- "props.unsubscribe(...)" : dispatches
    EditSavedSearchModal o-- "props.deleteSubscription(...)" : dispatches

    ShipmentsSearchBarState ..> ShipmentsSearchBarState : selectors
    ShipmentsSavedSearchSubscriptionState ..> ShipmentsSavedSearchSubscriptionState : actionCreators & selectors
    ShipmentsSavedSearchState ..> ShipmentsSavedSearchState : actionCreators
    ShipmentSavedSearchCardDataState ..> ShipmentSavedSearchCardDataState : actionCreators
```

> SVG rendering failed for this diagram.
