# Diagram: web/portal/src/pages/shipments/components/search/Shipments.SearchBar.container.js


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
graph TD
  subgraph StateSelectors
    SSBS[ShipmentsSearchBarState.selectors]
    SDDS[ShipmentsDomainDataState.selectors]
    DDS[DomainDataState.selectors]
    StccLoader[stccCodeLoaderState.selectors]
    HouseLoader[houseReferenceNumberLoaderState.selectors]
    BillLoader[billOfLadingNumberCategoryLoaderState.selectors]
    TrailerLoader[trailerAndEquipmentNumberLoaderState.selectors]
    AssetSelector[selectAssetIdOptions]
    TrainSelector[selectTrainIdOptions]
    ServiceSelector[selectServiceCodeFilterOptions]
  end

  subgraph ActionCreators
    SDDA[ShipmentsDomainDataState.actionCreators]
    SSB_A[ShipmentsSearchBarState.actionCreators]
    StccActions[stccCodeLoaderState.actionCreators]
    HouseActions[houseReferenceNumberLoaderState.actionCreators]
    BillActions[billOfLadingNumberCategoryLoaderState.actionCreators]
    TrailerActions[trailerAndEquipmentNumberLoaderState.actionCreators]
  end

  subgraph Redux
    MapState[mapStateToProps]
    MapDispatch[mapDispatchToProps]
    Connect[connect(mapStateToProps, mapDispatchToProps)]
    SearchBarComp[SearchBar Component]
  end

  %% selectors -> mapState
  SSBS -->|getSearchText, getSearchCategory, getIgnoreSearchCategory, getTypeaheadOptionsMetadata, getHasSearchCriteriaChanged, getCanUserSearch, getLocations, getLocationsLoading, getVins, getVinsLoading| MapState
  SDDS -->|getShipmentIDs, getRouteIDs, getOrderNumbers, getTripPlanNumbers| MapState
  AssetSelector -->|assetIDs| MapState
  DDS -->|selectCarriersAlphabetically -> carriers| MapState
  TrainSelector -->|trainIDs| MapState
  ServiceSelector -->|serviceCode| MapState
  StccLoader -->|getResults, getIsLoading -> stccCodeOptions, isStccCodeOptionsLoading| MapState
  HouseLoader -->|getResults, getIsLoading -> houseReferenceNumberOptions, isHouseReferenceNumberOptionsLoading| MapState
  BillLoader -->|getResults, getIsLoading -> billOfLadingOptions, isBillOfLadingOptionsLoading| MapState
  TrailerLoader -->|getResults, getIsLoading -> trailerAndEquipmentNumberOptions, isTrailerAndEquipmentNumberOptionsLoading| MapState

  %% action creators -> mapDispatch
  SDDA -->|fetchDomainData| MapDispatch
  SSB_A -->|setSearchCategoryForKey, setSearchText, setSearchValue, clearSearchText, resetSearchBar, clearSavedSearch, searchEntities, searchLocations, searchVins| MapDispatch
  StccActions -->|fetch| MapDispatch
  HouseActions -->|fetch| MapDispatch
  BillActions -->|fetch| MapDispatch
  TrailerActions -->|fetch| MapDispatch

  %% mapDispatch behavior
  MapDispatch -->|dispatch(fetchDomainData())| ReduxDispatch[dispatch]
  MapDispatch -->|setSearchCategory(text) -> dispatch(setSearchCategoryForKey)| ReduxDispatch
  MapDispatch -->|setSearchText(text, ignore) -> dispatch(setSearchText(...))| ReduxDispatch
  MapDispatch -->|searchLocations -> debouncePromise -> dispatch(searchLocations(query))| ReduxDispatch
  MapDispatch -->|fetchStccCodeOptions -> dispatch(stcc.fetch(query,max))| ReduxDispatch
  MapDispatch -->|fetchHouseReferenceNumberOptions -> dispatch(house.fetch(...))| ReduxDispatch
  MapDispatch -->|fetchBillOfLadingOptions -> dispatch(bill.fetch(...))| ReduxDispatch
  MapDispatch -->|fetchTrailerAndEquipmentNumberOptions -> dispatch(trailer.fetch(...))| ReduxDispatch

  %% connect to component
  MapState --> Connect
  MapDispatch --> Connect
  Connect --> SearchBarComp

  style SSBS fill:#f9f,stroke:#333,stroke-width:1px
  style SDDS fill:#ff9,stroke:#333,stroke-width:1px
  style DDS fill:#9ff,stroke:#333,stroke-width:1px
  style MapState fill:#efe,stroke:#333,stroke-width:1px
  style MapDispatch fill:#efe,stroke:#333,stroke-width:1px
  style Connect fill:#ccf,stroke:#333,stroke-width:2px
  style SearchBarComp fill:#cfc,stroke:#333,stroke-width:2px
```

> SVG rendering failed for this diagram.
