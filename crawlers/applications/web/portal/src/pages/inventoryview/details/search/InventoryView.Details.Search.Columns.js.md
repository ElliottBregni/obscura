# Diagram: web/portal/src/pages/inventoryview/details/search/InventoryView.Details.Search.Columns.js


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
classDiagram
    class VinCell <<React Component>> {
        +props.value
        +handleCopy(e)
        -clipboardIcon
        -clipboardIconColor
    }
    class CurrentLocationCell <<React Component>> {
        +props.value
    }
    class ForecastedArrivalCell <<React Component>> {
        +props.value
    }
    class InitialEtaCell <<React Component>> {
        +props.value
    }
    class TimeOnSiteCell <<React Component>> {
        +props.value
        -calculatedDuration
        -setColor
        -displayDuration
        -formattedDurationText
        -getTooltipText()
    }
    class DestinationCell <<React Component>> {
        +props.value
    }
    class useColumns <<hook>> {
        +returns columns[]
    }
    class calculateColorBasedOnDuration <<fn>> {
        +calculatedDuration -> color
    }
    class getDaysHrsMinsBetweenTimestamps <<util>> {
        +(start, end) -> {duration, formattedDuration}
    }
    class BrowserStorage <<util>> {
        +locationTimezone
    }
    class translateCurrentPositionType <<util>> {
        +(t, type) -> string
    }
    class getInitialEtaCellColorCode <<util>> {
        +(value) -> color
    }
    class DepartedSource <<const>> {
        <<enum>>
    }
    class UIAtoms {
        <<module>>
        +Text
        +DateTime
        +Icon
        +Tooltip
    }
    class Molecules {
        <<module>>
        +LocationAddressComplex
        +ShippabilityStatusText
        +ExternalStateText
    }
    %% relationships
    VinCell --> UIAtoms : uses
    VinCell --> Molecules : uses
    VinCell --> BrowserStorage : reads
    VinCell --> calculateColorBasedOnDuration : calls
    VinCell --> getDaysHrsMinsBetweenTimestamps : calls
    CurrentLocationCell --> UIAtoms : uses
    ForecastedArrivalCell --> UIAtoms : uses
    InitialEtaCell --> UIAtoms : uses
    InitialEtaCell --> getInitialEtaCellColorCode : calls
    TimeOnSiteCell --> UIAtoms : uses
    TimeOnSiteCell --> getDaysHrsMinsBetweenTimestamps : calls
    TimeOnSiteCell --> calculateColorBasedOnDuration : calls
    TimeOnSiteCell --> DepartedSource : checks
    DestinationCell --> UIAtoms : uses
    DestinationCell --> Molecules : uses
    useColumns --> VinCell : references as Cell
    useColumns --> CurrentLocationCell : references as Cell
    useColumns --> ForecastedArrivalCell : references as Cell
    useColumns --> InitialEtaCell : references as Cell
    useColumns --> DestinationCell : references as Cell
    useColumns --> TimeOnSiteCell : references as Cell
    useColumns --> translateCurrentPositionType : calls
    calculateColorBasedOnDuration --> Colors : reads
    InitialEtaCell --> BrowserStorage : reads
    ForecastedArrivalCell --> BrowserStorage : reads
    TimeOnSiteCell --> BrowserStorage : reads
    UIAtoms ..> Colors : uses
    Molecules ..> Colors : uses
    class Colors {
        <<styles>>
        +text
        +inventoryView
        +highlight
        +tables
    }
```

> SVG rendering failed for this diagram.
