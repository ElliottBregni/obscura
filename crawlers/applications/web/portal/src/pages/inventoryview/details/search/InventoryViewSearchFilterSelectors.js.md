# Diagram: web/portal/src/pages/inventoryview/details/search/InventoryViewSearchFilterSelectors.js


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
flowchart TD
  A[InventoryViewDomainDataState.selectors.getCurrentLocation] --> B[createSelector]
  B --> C[createOptionsMapper({ labelKey, valueKey })]
  C --> D{Options Mapper}
  D -->|labelKey(item) = `${item.label}`| E[Label: string]
  D -->|valueKey(item) = `${item.value}`| F[Value: string]
  B --> G[selectCurrentLocation]
```

> SVG rendering failed for this diagram.
