# Diagram: web/portal/src/pages/inventoryview/pinned/InventoryView.Pinned.page.container.js


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
classDiagram
    class InventoryViewPinned {
        <<React Component>>
    }
    class connect {
        +(mapStateToProps, actionCreators)(Component)
    }
    class mapStateToProps {
        +solutionId
        +watchedLocations
        +allWatchedLocations
        +isWatchedLocationsLoading
        +currentOrganization
        +watchedLocationsPageCount
        +isInventoryViewFeatureEnabled
    }
    class actionCreators {
        +searchEntities(solutionId)
        +redirectToDashboardView(locationId)
        +fetchWatchedLocations()
        +fetchAllWatchedLocations()
        +saveWatchedLocations(locations)
        +resetSearchAndFilters()
        +redirectToInsightsView(locationId)
        +redirectToDetailsView(solutionId, locationId)
        +setSearchFilter(filter)
    }
    class InventoryViewDashboardState {
        class selectors {
            +getWatchedLocations(state)
            +getAllWatchedLocations(state)
            +getWatchedLocationsLoading(state)
            +getWatchedLocationsPageCount(state)
        }
        class actionCreators {
            +fetchWatchedLocations()
            +fetchAllWatchedLocations()
            +saveWatchedLocations()
        }
    }
    class InventoryViewDetailsState {
        class actionCreators {
            +redirectToDashboardView(locationId)
            +redirectToInsightsView(locationId)
            +redirectToDetailsView(locationId)
        }
    }
    class InventorySearchBarState {
        class actionCreators {
            +searchEntities(solutionId)
            +resetSearchAndFilters()
            +setSearchFilter(filter)
            +setCurrentLocation(locationId)
        }
    }
    class OrganizationsState {
        +getSolutionId(state)
        +getActiveOrganization(state)
    }
    class AuthorizationSelectors {
        +getAuthorization(state)
    }
    class Features {
        +INVENTORY_VIEW
    }

    InventoryViewPinned --> connect : wrapped by
    connect --> mapStateToProps : uses
    connect --> actionCreators : binds
    mapStateToProps --> AuthorizationSelectors : calls getAuthorization(state)
    mapStateToProps --> OrganizationsState : calls getSolutionId(state), getActiveOrganization(state)
    mapStateToProps --> InventoryViewDashboardState.selectors : reads selectors
    actionCreators --> InventorySearchBarState.actionCreators : uses searchEntities, reset, setSearchFilter
    actionCreators --> InventoryViewDetailsState.actionCreators : uses redirects
    actionCreators --> InventoryViewDashboardState.actionCreators : uses fetch/save actions
    actionCreators --> InventorySearchBarState.actionCreators : calls setCurrentLocation via dispatch
    InventorySearchBarState.actionCreators --> InventoryViewDetailsState.actionCreators : dispatched in redirect helpers
    AuthorizationSelectors --> Features : checks hasFeatures([INVENTORY_VIEW])
```

> SVG rendering failed for this diagram.
