# Diagram: web/portal/src/pages/inventoryview/shared/InventoryInsightOnClickHandler.js


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
flowchart TD
    A[InventoryInsightOnClickHandlers(solutionId, location, setSearchFilter, resetSearchAndFilters, redirectToDetailsView)] --> B{Init}
    B --> B1[locationId = location.locationId]
    B --> B2[locationTimezone = location.timeZone ?? moment.tz.guess()]
    B --> B3[locationDate = moment.tz(moment.utc(), locationTimezone).format("YYYY-MM-DD")]
    B --> B4[dateToday() -> moment(locationDate)]
    B --> C[initializeFilter()]
    C --> C1[resetSearchAndFilters()]
    C --> C2[BrowserStorage.locationTimezone = locationTimezone]

    A --> H1[currentInventory()]
    H1 --> C
    H1 --> S1[setSearchFilter(TIME_ON_SITE, ALL)]
    S1 --> R1[redirectToDetailsView(solutionId, locationId)]

    A --> H2[daysOnSite()]
    H2 --> C
    H2 --> S2[setSearchFilter(TIME_ON_SITE, DAYS_10)]
    S2 --> R2[redirectToDetailsView(solutionId, locationId)]

    A --> H3[todaysForecastedArrival()]
    H3 --> C
    H3 --> T1[compute today: from = dateToday().startOf("day").toISOString(), to = dateToday().add(1, "days").startOf("day").toISOString()]
    T1 --> S3[setSearchFilter(FORECASTED_ARRIVAL, today)]
    S3 --> R3[redirectToDetailsView(solutionId, locationId)]

    A --> H4[yesterdaysDeparture()]
    H4 --> C
    H4 --> T2[compute yesterday: from = dateToday().subtract(1, "days").startOf("day").toISOString(), to = dateToday().startOf("day").toISOString()]
    T2 --> S4[setSearchFilter(DEPARTURE_DATE, yesterday)]
    S4 --> R4[redirectToDetailsView(solutionId, locationId)]
```

> SVG rendering failed for this diagram.
