# Diagram: web/portal/src/pages/inventoryview/react-queries/TopTenDaysOnSite.ts


> Auto-generated by Obscura crawlers

## Diagram 1

```mermaid
classDiagram
    class locationSchema {
        <<zod>>
        +id: number
        +name: string
        +code: string
        +ladName: string|null
    }
    class locationAggregationItemSchema {
        <<zod>>
        +location: locationSchema
        +count: number
        +timeZone: string
    }
    class locationAggregationSchema {
        <<zod array>>
        +items: locationAggregationItemSchema[]
    }
    class ResponseSchema {
        <<zod>>
        +locationAggregation: locationAggregationSchema
    }
    class createQueryRequestHook {
        <<factory>>
        +<T, E>(opts): HookResult<T,E>
    }
    class transformTopTenPlusDaysOnSite {
        <<utility>>
        +(data: ResponseSchema): any
    }
    class useTopTenDaysOnSiteQuery {
        <<hook>>
        +(params: string): { data, isFetching, isError }
    }
    class useTopTenDaysOnSiteHook {
        <<hook>>
        +(params: string): { listOfLocations, isFetching, isError }
    }
    class axios {
        <<http>>
        +get(url): Promise<Response>
    }
    class customerApiUrl {
        <<util>>
        +(path: string): string
    }
    class BaseErrorResponse {
        <<type>>
    }

    ResponseSchema --> locationAggregationSchema
    locationAggregationSchema --> locationAggregationItemSchema
    locationAggregationItemSchema --> locationSchema
    useTopTenDaysOnSiteQuery --> createQueryRequestHook
    useTopTenDaysOnSiteQuery --> axios
    useTopTenDaysOnSiteQuery --> customerApiUrl
    useTopTenDaysOnSiteHook --> useTopTenDaysOnSiteQuery
    useTopTenDaysOnSiteHook --> transformTopTenPlusDaysOnSite
```

> SVG rendering failed for this diagram.

## Diagram 2

```mermaid
flowchart TD
    P[params:string] --> BuildUrl[Build URL using customerApiUrl]
    BuildUrl --> MaybeQuery[Append ?params if present]
    MaybeQuery --> AxiosGet[axios.get(url)]
    AxiosGet --> Response[response.data]
    Response --> InitialData{initialData exists?}
    Response --> Transform[transformTopTenPlusDaysOnSite(data)]
    Transform --> HookReturn[Return { listOfLocations, isFetching, isError }]
    CreateHook[createQueryRequestHook factory] --> UseQuery[useTopTenDaysOnSiteQuery calls factory]
    UseQuery --> AxiosGet
    UseHook[useTopTenDaysOnSiteHook] --> UseQuery
    UseHook --> HookReturn
```

> SVG rendering failed for this diagram.
