# Diagram: web/portal/src/pages/inventoryview/react-queries/TopExceededCapacity.ts


> Auto-generated by Obscura crawlers

## Diagram 1

```mermaid
classDiagram
    class Location {
        +number id
        +string name
        +string code
        +string|null ladName
    }
    class LocationAggregationItem {
        +Location location
        +number vinCount
        +number capacity
        +number overage
    }
    class Response {
        +LocationAggregationItem[] locationAggregation
    }
    class ChartResponse
    class createQueryRequestHook
    class axios
    class transformTopExceededCapacity
    class useTopExceededCapacityQuery {
        +params: string
        +returns: ChartResponse
    }
    class useTopExceededCapacityHook {
        +params: string
        +returns: { listOfLocations, isFetching, isError }
    }
    LocationAggregationItem --> Location
    Response --> LocationAggregationItem
    useTopExceededCapacityQuery --> createQueryRequestHook
    useTopExceededCapacityQuery --> axios
    useTopExceededCapacityHook --> useTopExceededCapacityQuery
    useTopExceededCapacityHook --> transformTopExceededCapacity
```

> SVG rendering failed for this diagram.

## Diagram 2

```mermaid
flowchart TD
    A[useTopExceededCapacityHook(params)] --> B[call useTopExceededCapacityQuery(params)]
    B --> C[createQueryRequestHook configured with queryKey & queryFn]
    C --> D[queryFn executes: build URL via customerApiUrl]
    D --> E[axios.get(url)]
    E --> F[response.data]
    F --> G[transformTopExceededCapacity(data)]
    G --> H[{return {listOfLocations, isFetching, isError}}]
    B --> I[initialData: { locationAggregation: [] }]
    C --> J[refetchOnWindowFocus: false<br/>retry: false]
```

> SVG rendering failed for this diagram.
