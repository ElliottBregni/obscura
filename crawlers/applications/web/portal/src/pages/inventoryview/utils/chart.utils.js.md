# Diagram: web/portal/src/pages/inventoryview/utils/chart.utils.js


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
classDiagram
    class Colors {
        <<imported>>
        +graphs
        +inventoryView
    }
    class BrowserStorage {
        <<imported>>
        +locationTimezone
    }
    class DateTimeUtils {
        <<imported>>
        +getDifferenceBetweenTimestamps()
        +stringToMoment()
    }
    class Configs {
        <<imported>>
        +pieChartDataByTimeOnSite
        +pieChartDataByType
        +pieChartDataByEta
        +pieChartDataByInitialEta
        +pieChartDataByGateThreshold
    }
    class DataTypes {
        <<imported>>
        +SHIPPABLE
        +REPORTED
        +NON_SHIPPABLE
        +FORECASTED
    }

    class Styles {
        +barStyle
        +getStyle(bar)
        +getLabels(bar)
        +formatXAxisName(datum)
        +calculateLabelPosition(bar)
        +generateTickFormat({difference,maxLimit})
    }

    class ColorThemes {
        -inventoryColorCodes
        -daysOnSiteColorCodes
        -forecastedArrivalsColorCodes
        -departureColorCodes
        -getColorTheme(insightBlock)
        +getColors(insightBlock, value, capacity=0)
    }

    class DateFormatting {
        -formatDateAndCheckTodaysDate(date)
    }

    class Transforms {
        +transformInventoryChartData(data, locationId)
        +transformPipelineChartData(data, locationId)
        +transformArrivalGraphData(data, locationId)
        +transformDepartureGraphData(data, locationId)
        +populateCounts(data)
        +transformInitialETAPieChartData(data)
        +transformGateThresholdPieChartData(data)
        +transformTopTenPlusDaysOnSite(data)
        +transformTopExceededCapacity(data)
    }

    class Params {
        +getParams(locationId, locationTimezone, orgFvId, isShipperOrg, inventoryDateWindow=7, arrivalDateWindow=7, departureDataWindow=7)
    }

    Colors <-- Styles : uses
    Colors <-- ColorThemes : uses
    BrowserStorage <-- DateFormatting : reads
    DateTimeUtils <-- DateFormatting : uses
    DateFormatting <-- Transforms : used by
    Configs <-- Transforms : uses
    DataTypes <-- Styles : uses
    ColorThemes --> Styles : provides
    Transforms --> Params : consumes
    Styles --> Transforms : provides styling
    Transforms ..> Configs : reads pie configs
    Params ..> DateTimeUtils : uses moment/timezone functions
```

> SVG rendering failed for this diagram.
