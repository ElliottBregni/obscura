# Diagram: web/portal/src/pages/inventoryview/utils/count.utils.js


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
graph TD
A[InventoryChartData.data] --> B{Item.isTodaysDate?}
B -->|yes| C[Sum item.shippable + item.nonShippable (reduce)]
B -->|no| Z1[ignored]
C --> D[TodaysInventory]

E[isArrivalGraphDataFetchingError] --> F{is false?}
arr[arrivalGraphData] --> G{Not _.isEmpty(arrivalGraphData) and not error?}
F --> G
G -->|yes| H{Item.isTodaysDate?}
H -->|yes| I[TodaysArrivals = first.forecasted ?? 0]
H -->|no| Z2[no todays arrivals]
I --> J[TodaysArrivalsCount]
F -->|no| J0[error -> TodaysArrivalsCount = 0]
J0 --> J

dep[departuresChartData] --> K{_.has(departuresChartData,"capacity") and capacity?}
K -->|yes| L[fivePercentOfTotalCapacity = trunc((capacity * 5) / 100)]
K -->|no| M[fivePercentOfTotalCapacity = 0]

D --> R[(Return {todaysInventory, todaysArrivalsCount, fivePercentOfTotalCapacity})]
J --> R
L --> R
M --> R
```

> SVG rendering failed for this diagram.
