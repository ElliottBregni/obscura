# Diagram: web/portal/src/pages/inventoryview/dashboard/components/InventoryView.FilterSectionContainer.js


> Auto-generated by Obscura crawlers

## Diagram 1

```mermaid
flowchart LR
  State["Redux State"] -->|getAuthorization(state)| Auth[getAuthorization]
  State -->|getSolutionId(state)| Sol[getSolutionId]
  State -->|selectors.getSearchFilters(state)| Selectors["InventoryViewDashboardAdvancedFiltersState.selectors.getSearchFilters"]
  State -->|selectors.getHasSearchCriteriaChanged(state)| HasChanged["selectors.getHasSearchCriteriaChanged"]
  State -->|selectors.getCanUserSearch(state)| CanSearch["selectors.getCanUserSearch"]
  Auth -->|returns| mapStateToProps[mapStateToProps]
  Sol -->|returns| mapStateToProps
  Selectors -->|returns| mapStateToProps
  HasChanged -->|returns| mapStateToProps
  CanSearch -->|returns| mapStateToProps
  InventoryState["InventoryViewDashboardAdvancedFiltersState"] -.->|actionCreators| setSearchFilter
  InventoryState -.-> clearSearchFilter
  InventoryState -.-> searchEntities
  InventoryState -.-> resetSavedSearch
  InventoryState -.-> clearSavedSearch
  setSearchFilter -->|included in| ActionCreators[actionCreators]
  clearSearchFilter --> ActionCreators
  searchEntities --> ActionCreators
  resetSavedSearch --> ActionCreators
  clearSavedSearch --> ActionCreators
  mapStateToProps -->|props| connect[connect(...)(FilterSection)]
  ActionCreators -->|dispatch props| connect
  mergeProps["mergeProps(stateProps, dispatchProps, ownProps)"] -->|combines| connect
  INVENTORY_VIEW_FILTERS -->|attached as filtersMetadata| mergeProps
  FilterSection[FilterSection Component] <--|connected component| connect
```

> SVG rendering failed for this diagram.

## Diagram 2

```mermaid
classDiagram
  class FilterSection {
    <<React Component>>
    +props: auth, searchFilters, solutionId
    +props: hasSearchCriteriaChanged, canUserSearch
    +props: setSearchFilter(), clearSearchFilter()
    +props: searchEntities(), resetSavedSearch(), clearSavedSearch()
    +props: filtersMetadata
  }
  class mapStateToProps {
    +state -> { auth, searchFilters, solutionId, hasSearchCriteriaChanged, canUserSearch }
  }
  class InventoryViewDashboardAdvancedFiltersState {
    <<Module>>
    +selectors.getSearchFilters(state)
    +selectors.getHasSearchCriteriaChanged(state)
    +selectors.getCanUserSearch(state)
    +actionCreators: setSearchFilter(), clearSearchFilter(), searchEntities(), resetSavedSearch(), clearSavedSearch()
  }
  class getAuthorization {
    +(state) : auth
  }
  class getSolutionId {
    +(state) : solutionId
  }
  class connect {
    +connect(mapStateToProps, actionCreators, mergeProps)(Component)
  }
  class INVENTORY_VIEW_FILTERS {
    <<Constant>>
  }
  mapStateToProps --> InventoryViewDashboardAdvancedFiltersState : uses selectors
  mapStateToProps --> getAuthorization : uses
  mapStateToProps --> getSolutionId : uses
  connect --> FilterSection : wraps
  connect --> mapStateToProps : wired with
  connect --> InventoryViewDashboardAdvancedFiltersState : wired with actionCreators
  mergeProps ..> INVENTORY_VIEW_FILTERS : attaches filtersMetadata
```

> SVG rendering failed for this diagram.
