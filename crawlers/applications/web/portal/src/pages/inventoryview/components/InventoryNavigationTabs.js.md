# Diagram: web/portal/src/pages/inventoryview/components/InventoryNavigationTabs.js


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
classDiagram
    class BaseTab {
        +border: 0
        +borderRadius: "4px 4px 0 0"
        +fontSize: 20
        +marginRight: "5px"
        +padding: "15px 20px"
        +borderBottom: "4px"
        +borderStyle: "solid"
        +opacity: 1
        +cursor: "pointer"
        +minHeight: "65px"
    }

    class SelectedTab {
        +color: Colors.inventoryView.BLUE
        +borderColor: Colors.inventoryView.BLUE
        +backgroundColor: Colors.inventoryView.WHITE
        +cursor: "auto"
    }

    class DefaultTab {
        +color: Colors.inventoryView.DARK_GRAY
        +borderColor: Colors.inventoryView.LIGHT_GRAY
        +opacity: 0.6
    }

    class TabsListStyle {
        +display: "flex"
        +flexDirection: "row"
        +marginBottom: "1em"
        +borderBottom: "1px solid"
        +borderColor: Colors.tabs.BACKGROUND
        +alignItems: "center"
        +minHeight: "65px"
        +listStyle: "none"
        +paddingLeft: 0
    }

    class DisabledState {
        +opacity: 1
        +color: Colors.inventoryView.BLUE
        +backgroundColor: Colors.inventoryView.WHITE
        +borderColor: Colors.inventoryView.BLUE
        +cursor: "auto"
    }

    class HoverState {
        <<uses>>
    }

    class Colors {
        <<module>>
        +inventoryView
        +tabs
    }

    BaseTab <|-- SelectedTab
    BaseTab <|-- DefaultTab
    SelectedTab o-- DisabledState : ":disabled"
    DefaultTab o-- HoverState : ":hover"
    HoverState --> SelectedTab : inherits styles
    Colors ..> SelectedTab
    Colors ..> DefaultTab
    Colors ..> TabsListStyle
```

> SVG rendering failed for this diagram.
