# Diagram: web/portal/src/pages/createmilestone/utils/CreateMilestone.utils.js


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
classDiagram
  class CreateMilestoneConstants {
    <<enumeration>>
    +DependencyType
    +ItemAttribute
  }

  class defaultDependencies {
    +partnerType: string = ""
    +milestoneEvent: string = ""
  }

  class isRequired {
    +isRequired(input, dependencies)
  }

  class isRequiredHelper {
    +isRequiredHelper(valueDef, dependencies)
  }

  class isDisplayed {
    +isDisplayed({item, partnerType, milestoneEvent})
  }

  class lookupValue {
    +lookupValue({item, attribute, partnerType, milestoneEvent})
  }

  class hasDisableUntilValue {
    +hasDisableUntilValue({item, partnerType, milestoneEvent})
  }

  class getMilestoneEventValue {
    +getMilestoneEventValue(inputVal)
  }

  class hasKey {
    +hasKey(object, key)
  }

  class hasMilestoneDependency {
    +hasMilestoneDependency({item, attribute, partnerType})
  }

  CreateMilestoneConstants <|-- isRequiredHelper : uses
  CreateMilestoneConstants <|-- lookupValue : uses
  CreateMilestoneConstants <|-- hasMilestoneDependency : uses

  isRequired --> isRequiredHelper : calls
  isDisplayed --> lookupValue : calls
  hasDisableUntilValue --> lookupValue : calls
  lookupValue --> hasKey : calls
  lookupValue --> hasMilestoneDependency : calls
  hasMilestoneDependency --> CreateMilestoneConstants : checks DependencyType
  isRequiredHelper --> isRequiredHelper : recursive call
  getMilestoneEventValue --> getMilestoneEventValue : returns {code, vmacsCode}
  isRequired ..> defaultDependencies : default param
  isRequired --> CreateMilestoneConstants : references ItemAttribute
```

> SVG rendering failed for this diagram.
