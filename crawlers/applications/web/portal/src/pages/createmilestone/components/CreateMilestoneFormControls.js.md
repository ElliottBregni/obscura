# Diagram: web/portal/src/pages/createmilestone/components/CreateMilestoneFormControls.js


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
classDiagram
    class CreateMilestoneFormControls {
        +isValid
        +resetHandler()
        +submitHandler()
        +isSubmitting
        +t()
        +render()
    }
    class withTranslation {
        +namespaces
        +wrap(component)
    }
    class FlexColDiv {
        +style
    }
    class FormRow {
        +css
    }
    class Button {
        +variant
        +onClick()
        +disabled
        +label
    }
    CreateMilestoneFormControls --> FlexColDiv : uses
    CreateMilestoneFormControls --> FormRow : contains
    FormRow --> Button : contains "Reset" (variant: danger)
    FormRow --> Button : contains "Submit" (variant: success)
    CreateMilestoneFormControls --> withTranslation : exported-via
    CreateMilestoneFormControls ..> PropTypes : propTypes
    PropTypes <|-- CreateMilestoneFormControls
    note for CreateMilestoneFormControls "Styles: backgroundColor: LIGHT_GRAY\ncolor: DARK_BLUE\npadding: 1em"
```

> SVG rendering failed for this diagram.
