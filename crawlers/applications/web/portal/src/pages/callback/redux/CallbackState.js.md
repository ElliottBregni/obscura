# Diagram: web/portal/src/pages/callback/redux/CallbackState.js


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
flowchart LR
  Start(("Action dispatched")) --> TypeCheck{action.type}
  TypeCheck -->|NOT_FOUND| ReturnNull[/"return null"/]
  TypeCheck -->|CALLBACK or SAML_CALLBACK| BuildObj["Build new state object"]
  TypeCheck -->|default| ReturnState["return state"]
  BuildObj --> AuthCodeCheck{action.meta.query has \"code\"?}
  AuthCodeCheck -->|yes| SetAuthCode["authCode = action.meta.query.code"]
  AuthCodeCheck -->|no| SetAuthCodeNull["authCode = null"]
  BuildObj --> ErrorCheck{action.meta.query has \"error\"?}
  ErrorCheck -->|yes| SetError["error = action.meta.query.error"]
  ErrorCheck -->|no| SetErrorNull["error = null"]
  SetAuthCode --> Merge["...state + fields"]
  SetAuthCodeNull --> Merge
  SetError --> Merge
  SetErrorNull --> Merge
  Merge --> SetAuthType["authType = action.type"]
  SetAuthType --> ReturnNewState[/"return new state object"/]
```

> SVG rendering failed for this diagram.
