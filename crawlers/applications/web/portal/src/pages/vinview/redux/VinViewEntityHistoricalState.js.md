# Diagram: web/portal/src/pages/vinview/redux/VinViewEntityHistoricalState.js


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
flowchart LR
A[import apiUrl from "api-url"] --> D[getHistoryUrl(solutionId, entityId, qualifier, dealerOrgId)]
B[import { buildEntityHistoricalSlice } from "shared/redux/EntityHistoricalSlice"] --> E[VinViewEntityHistoricalState]
C[STORE_MOUNT_POINT = "vinViewEntityHistory"] --> E
D --> K[let url = "/entity/internal/{entityId}/reference-history?qualifier={qualifier}&"]
D --> F{dealerOrgId ?}
F -- yes --> G[append "dealerOrgId={dealerOrgId}&" to url]
F -- no --> H[do not append dealerOrgId]
G --> L[call apiUrl(url) and return]
H --> L
K --> F
L --> E
E --> J[export default VinViewEntityHistoricalState]
```

> SVG rendering failed for this diagram.
