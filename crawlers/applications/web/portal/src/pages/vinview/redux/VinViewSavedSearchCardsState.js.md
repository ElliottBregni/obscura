# Diagram: web/portal/src/pages/vinview/redux/VinViewSavedSearchCardsState.js


> Auto-generated by Obscura crawlers

## Diagram 1

```mermaid
classDiagram
class VinViewSavedSearchCardsState {
  +String mountPoint
  +Object actionCreators
  +Object selectors
  +Function reducer
}
class reducer {
  +state = {}
  +action -> state
}
class fetchSavedSearchCardData {
  +savedSearch
  +isDealerOrg
  +dealerOrgId
  +returns function(dispatch)
}
class formatResponse {
  +response -> {exceptions, deliveredCount, activeCount, completeCount}
}
class getSavedSearchQueryString {
  +savedSearch -> queryString
}
class tokens {
  -Map id -> { cancelRequest }
}
class constants {
  +MOUNT_POINT
  +FETCH_SAVED_SEARCH_CARD_DATA
  +RECEIVE_SAVED_SEARCH_CARD_DATA
}
VinViewSavedSearchCardsState --> reducer
VinViewSavedSearchCardsState --> fetchSavedSearchCardData
VinViewSavedSearchCardsState --> getSavedSearchQueryString
fetchSavedSearchCardData --> tokens : uses
fetchSavedSearchCardData --> formatResponse : formats response
getSavedSearchQueryString --> constants : reads SEARCH_CATEGORIES,FILTERS
reducer ..> constants : handles action types
```

> SVG rendering failed for this diagram.

## Diagram 2

```mermaid
flowchart TD
  Start([Start fetchSavedSearchCardData])
  CheckTokens{tokens[savedSearch.id] exists?}
  CancelPrev[Cancel previous request via cancelRequest(cancelMessage)]
  ResetTokens[Reset tokens[savedSearch.id] = {}]
  DispatchFetch[dispatch(FETCH_SAVED_SEARCH_CARD_DATA, id)]
  Clone[Clone savedSearch and set clonedSavedSearch]
  DetermineBatch{isBatch = clonedSavedSearch.search.batch != null}
  BuildParams[params = new URLSearchParams(getSavedSearchQueryString(clonedSavedSearch))]
  AppendBatch[if isBatch append batchType & set request method POST and body]
  AppendDealerOrg[if !isDealerOrg append dealerOrgId]
  AxiosRequest[axios(request with url, method, headers, cancelToken)]
  ThenSuccess[.then -> dispatch(RECEIVE_SAVED_SEARCH_CARD_DATA, data=formatResponse(response))]
  CatchError[.catch -> if not canceled log error]
  End([End])
  Start --> CheckTokens
  CheckTokens -- Yes --> CancelPrev --> ResetTokens
  CheckTokens -- No --> ResetTokens
  ResetTokens --> DispatchFetch --> Clone --> DetermineBatch
  DetermineBatch --> BuildParams --> AppendBatch --> AppendDealerOrg --> AxiosRequest
  AxiosRequest --> ThenSuccess --> End
  AxiosRequest --> CatchError --> End
```

> SVG rendering failed for this diagram.
