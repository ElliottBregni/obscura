# Diagram: web/portal/src/pages/vinview/search/VinView.Search.page.container.js


> Auto-generated by Obscura crawlers

## Diagram 1

```mermaid
classDiagram
    class VinViewSearch {
        +props
    }
    class mapStateToProps {
        +selectedDealerOrgFvId
        +savedSearch
        +savedSearchOS
        +vinSearchResults
        +vinSearchResultsOS
        +showFilters
        +showFiltersOS
        +isLoading
        +isLoadingOS
        +loadingError
        +isLoadingError
        +isLoadingErrorOS
        +sortColumn
        +sortColumnOS
        +reverseSort
        +reverseSortOS
        +page
        +pageOS
        +pageSize
        +pageSizeOS
        +totalPages
        +totalPagesOS
        +totalEntities
        +totalEntitiesOS
        +totalEntitiesIsLoading
        +exportIdentifier
        +exportIdentifierOS
        +exportName
        +exportNameOS
        +isExporting
        +isExportingOS
        +exportFailed
        +exportFailedOS
        +showItssData
        +selectedDealerOrgId
        +selectedDealerOrgIdOS
        +displayShippabilityStatus
        +displayExternalState
        +isDealerOrg
        +isFvAdmin
        +isOpenSearchEnabled
        +canDisplayConnectedCarSource
        +useVinViewOpenSearchEnabled
        +vinviewFeatureFlagConfig
        +vinviewFeatureFlagConfigIsLoading
    }
    class mapDispatchToProps {
        +loadSavedSearch(savedSearch)
        +loadSavedSearchOS(savedSearch)
        +pushVinViewDetailsPage(internalId, selectedDealerOrgId)
        +toggleShowFilters(showFilters)
        +setPagination(page, pageSize)
        +setSort(sortColumn, reverse)
        +exportEntities()
        +clearExportErrors()
        +resetSearch()
        +resetExport()
        +toggleShowFiltersOS(showFilters)
        +setPaginationOS(page, pageSize)
        +setSortOS(sortColumn, reverse)
        +exportEntitiesOS(dealerOrgFvId)
        +clearExportErrorsOS()
        +resetSearchOS()
        +resetExportOS()
        +setToggleSwitchState(flag)
        +fetchVinViewSystemConfigForOpenSearch(solutionId)
    }
    class Selectors {
        <<enumeration>>
        VinViewSearchBarState
        VinViewOpenSearchSearchBarState
        VinViewSavedSearchState
        VinViewOpenSearchSavedSearchState
        VinViewDomainDataState
        TitleState
    }
    class Auth {
        +getAuthorization(state)
        +hasFeatures()
        +hasOrganizationTypes()
        +isAuthorized()
        +validateEntitySystemConfigValue()
        +isFvAdmin()
    }
    VinViewSearch --> mapStateToProps : receives props from
    VinViewSearch --> mapDispatchToProps : receives actions from
    mapStateToProps --> Selectors : reads
    mapStateToProps --> Auth : queries
    mapDispatchToProps --> Selectors : dispatches actionCreators
    mapDispatchToProps --> "Redux dispatch" : uses
```

> SVG rendering failed for this diagram.

## Diagram 2

```mermaid
flowchart LR
    subgraph StateSelectors
        A[VinViewSearchBarState.selectors]
        B[VinViewOpenSearchSearchBarState.selectors]
        C[VinViewDomainDataState.selectors]
        D[TitleState.selectors]
    end
    subgraph AuthSelectors
        E[getAuthorization(state)]
        F[getActiveOrganization(state)]
        G[getSelectedDealerOrgFvId(state, currentOrganization)]
    end
    subgraph Component
        V[VinViewSearch Component]
    end
    subgraph Actions
        S1[VinViewSavedSearchState.actionCreators]
        S2[VinViewOpenSearchSavedSearchState.actionCreators]
        S3[VinViewSearchBarState.actionCreators]
        S4[VinViewOpenSearchSearchBarState.actionCreators]
        S5[VinViewDomainDataState.actionCreators]
        T[TitleState.actionCreators]
    end
    B -->|getSelectedSavedSearch / getSearchResults / ...| StateSelectors
    A -->|getSelectedSavedSearch / getSearchResults / ...| StateSelectors
    C -->|getIsVinViewOpenSearchConfigured / isLoading| StateSelectors
    D -->|getOpenSearchToggleState| StateSelectors
    E -->|hasFeatures / isAuthorized / validateEntitySystemConfigValue / hasOrganizationTypes / isFvAdmin| AuthSelectors
    F --> G
    G -->|selectedDealerOrgFvId| mapStateToProps[mapStateToProps]
    A --> mapStateToProps
    B --> mapStateToProps
    C --> mapStateToProps
    D --> mapStateToProps
    E --> mapStateToProps
    mapStateToProps --provides props--> V
    S1 -->|loadSavedSearch| mapDispatchToProps[mapDispatchToProps]
    S2 -->|loadSavedSearch (OS)| mapDispatchToProps
    S3 -->|setPagination / setSort / exportSearch / reset / toggleShowFilters / ...| mapDispatchToProps
    S4 -->|setPagination / setSort / exportSearch / reset / toggleShowFilters / ...| mapDispatchToProps
    S5 -->|fetchVinViewSystemConfigForOpenSearch| mapDispatchToProps
    T -->|setToggleSwitchState| mapDispatchToProps
    mapDispatchToProps --provides action props--> V
    V -->|dispatches actions via props| S1 & S2 & S3 & S4 & S5 & T
```

> SVG rendering failed for this diagram.
