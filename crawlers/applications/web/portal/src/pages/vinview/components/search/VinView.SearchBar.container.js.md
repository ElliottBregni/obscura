# Diagram: web/portal/src/pages/vinview/components/search/VinView.SearchBar.container.js


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
classDiagram
    class SearchBar {
        <<React Component>>
    }
    class Connect {
        +mapStateToProps
        +actionCreators
    }
    class mapStateToProps {
        +auth
        +typeaheadOptionsMetadata
        +searchText
        +searchCategory
        +ignoreSearchCategory
        +hasSearchCriteriaChanged
        +vins
        +productTypes
        +orderNumbers
        +exceptionTypes
        +domainDataArgs
        +areAllPrerequisiteFiltersSelected
        +canUserSearch
    }
    class actionCreators {
        +setSearchCategory()
        +setSearchText()
        +clearSearchText()
        +resetSearchBar()
        +searchEntities()
        +clearSavedSearch()
        +fetchDomainData()
    }
    class SearchBarState_selectors {
        +getSearchText()
        +getSearchCategory()
        +getIgnoreSearchCategory()
        +getTypeaheadOptionsMetadata()
        +getHasSearchCriteriaChanged()
        +getAreAllPrerequisiteFiltersSelected()
        +getCanUserSearch()
        +getSelectedDealerOrgId()
    }
    class SearchBarState_actionCreators {
        +setSearchText()
        +setSearchCategoryForKey()
        +clearSearchText()
        +resetSearchBar()
        +searchEntities()
        +clearSavedSearch()
    }
    class VinViewDomainDataState_selectors {
        +getVINs()
        +getProductTypes()
        +getOrderNumbers()
        +getExceptionTypes()
    }
    class VinViewDomainDataState_actionCreators {
        +fetchDomainData()
    }
    class AuthorizationSelectors {
        +getAuthorization()
    }
    class OrganizationsState {
        +getActiveOrganization()
    }

    Connect --> SearchBar : connects
    Connect --> mapStateToProps : uses
    Connect --> actionCreators : provides

    mapStateToProps --> SearchBarState_selectors : reads
    mapStateToProps --> VinViewDomainDataState_selectors : reads
    mapStateToProps --> AuthorizationSelectors : calls getAuthorization()
    mapStateToProps --> OrganizationsState : calls getActiveOrganization()

    actionCreators --> SearchBarState_actionCreators : delegates setSearchCategory, clear, reset, search, clearSavedSearch
    actionCreators --> VinViewDomainDataState_actionCreators : delegates fetchDomainData
    actionCreators .down.> SearchBarState_actionCreators : setSearchText wraps and dispatches

    SearchBarState_selectors <|-- SearchBarState_actionCreators : part of same module
    VinViewDomainDataState_selectors <|-- VinViewDomainDataState_actionCreators : part of same module
```

> SVG rendering failed for this diagram.
