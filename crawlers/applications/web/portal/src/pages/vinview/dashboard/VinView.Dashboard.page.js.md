# Diagram: web/portal/src/pages/vinview/dashboard/VinView.Dashboard.page.js


> Auto-generated by Obscura crawlers

## Diagram 1

```mermaid
classDiagram
    class VinViewDashboard {
        +props
        +useState(showFilters)
        +useState(didMount)
        +useEffect(mount)
        +useEffect(featureFlag)
        +useEffect(fetchOnOrgSelect)
        +applyFilters(filters)
        +handleClickException(e)
    }
    class Dashboard <<component>> 
    class ExceptionsPanel <<organism>>
    class WatchedVins <<organism>>
    class VinViewSavedSearchesPanel <<organism>>
    class VinViewOpenSearchSavedSearchesPanel <<organism>>
    class NewsFeedWidget <<organism>>
    class SearchBarContainer <<container>>
    class SearchBarOpenSearchContainer <<container>>
    class FiltersContainer <<container>>
    class FiltersOpenSearchContainer <<container>>
    class Hooks <<utilities>>
    class Utils <<utils>>
    class Trackers <<analytics>>

    VinViewDashboard --> Dashboard : renders
    Dashboard --> SearchBarContainer : uses (conditional)
    Dashboard --> SearchBarOpenSearchContainer : uses (conditional)
    Dashboard --> FiltersContainer : uses (conditional)
    Dashboard --> FiltersOpenSearchContainer : uses (conditional)
    VinViewDashboard --> ExceptionsPanel : includes
    VinViewDashboard --> WatchedVins : includes
    VinViewDashboard --> VinViewSavedSearchesPanel : includes (postgres)
    VinViewDashboard --> VinViewOpenSearchSavedSearchesPanel : includes (opensearch)
    VinViewDashboard --> NewsFeedWidget : includes (conditional)
    VinViewDashboard --> Hooks : calls (useSetTitleOnMount, useSetDescriptionOnMount, useSetOpenSearchToggleSwitch)
    VinViewDashboard --> Trackers : calls (useTrackWithMixpanelOnce)
    VinViewDashboard --> Utils : uses (getIconData, lodash)
```

> SVG rendering failed for this diagram.

## Diagram 2

```mermaid
graph TD
    A[Component Mount] --> B[initialize translations & hooks]
    B --> C[setToggleSwitchVisibility(isFvAdmin)]
    C --> D[setDidMount(true)]
    D --> E[fetchVinViewSystemConfigForOpenSearch]
    D --> F{canFetchData && !isOpenSearchEnabled}
    F -->|true| G[fetchEntityCount, fetchNewEntityCount, fetchEntityDeliveredCount]
    D --> H{canFetchDataOS && isOpenSearchEnabled}
    H -->|true| I[fetchCountsOpenSearch(isDealerOrg, fvId)]
    subgraph Watched VINs fetches
        J[canFetchData && !isOpenSearchEnabled ?] -->|true| K[fetchWatchedVins & fetchWatchedEntitiesTotalPages]
        L[canFetchDataOS && isOpenSearchEnabled ?] -->|true| M[fetchWatchedVinsOpenSearch]
    end
    B --> N[Later effects: feature flag update]
    N --> O[setToggleSwitchState(vinviewFeatureFlagConfig)]
    P[User interacts with chart/exception] --> Q[applyFilters -> searchEntities or searchEntitiesOS]
    Q --> R[Dashboard updates displayed widgets]
```

> SVG rendering failed for this diagram.
