# Diagram: web/portal/src/pages/vinview/utils/filter.utils.js


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
flowchart LR
  F[translateCodeLabelLookup(t, value)] --> L{lookup[value] ?}
  L -- yes --> R1[return lookup[value]]
  L -- no --> R2[return value]
  subgraph lookupEntries [lookup object]
    style lookupEntries fill:#f9f,stroke:#333,stroke-width:1
    K_manually_completed["manually_completed → t(\"filters:Shipment manually completed/closed\")"]
    K_1300["1300 → t(\"filters:No asset assigned - shipment should be in transit\")"]
    K_missed_pickup_exceptions["missed_pickup_exceptions → t(\"filters:Missed Pickup\")"]
    K_missed_delivery_exceptions["missed_delivery_exceptions → t(\"filters:Missed Drop-Off\")"]
    K_behind_schedule["behind_schedule → t(\"filters:Behind Schedule\")"]
    K_1380["1380 → t(\"filters:Tracking asset past destination delivery window.\")"]
    K_bad_order_exceptions["bad_order_exceptions → t(\"filters:Bad Order\")"]
    K_in_hold_exceptions["in_hold_exceptions → t(\"filters:In Hold\")"]
    K_conflicting_etas["conflicting_etas → t(\"filters:ETA conflict between shipment and last stop records\")"]
    K_idle_train_exceptions["idle_train_exceptions → t(\"filters:Idle Train\")"]
    K_missed_delivery["missed_delivery → t(\"filters:Shipment Missed Delivery\")"]
    K_2070["2070 → t(\"filters:Shipment arrived at ultimate destination, but no delivery event\")"]
    K_1070["1070 → t(\"filters:Dweller Shipment\")"]
    K_under_review["under_review → t(\"filters:Under Review\")"]
    K_1000["1000 → t(\"filters:Shipment has conflicting asset IDs\")"]
    K_1100["1100 → t(\"filters:Invalid missed pickup exception\")"]
    K_1200["1200 → t(\"filters:Completed shipment not showing properly\")"]
    K_1350["1350 → t(\"filters:Missed Asset assignment for in-transit shipment\")"]
    K_Excessive_Dwell["Excessive Dwell → t(\"filters:Aging Vin\")"]
  end
  F --> lookupEntries
  lookupEntries --> L
  R1 --> OUT[output: translated label]
  R2 --> OUT[output: original value]
```

> SVG rendering failed for this diagram.
