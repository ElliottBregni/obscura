# Diagram: web/portal/src/pages/vinview/utils/vinViewDetailsTabDefinitions.js


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
flowchart LR
  A[getVinTabs(params)] --> B[filteredCombinedExceptions(combinedExceptions, shouldFilterExceptions)]
  A --> C[getOrderDetails(t, details)]
  A --> D[getVinEquipment(details)]
  A --> E[getDiagnosticsData(t, details, showDiagnosticDataTab, getDiagnosticLabel, getColorByState)]
  B --> F[filterException]
  C --> G[orderDetails]
  D --> H[vinEquipment]
  E --> I[diagnosticsData]
  F --> J[data object]
  G --> J
  H --> J
  I --> J
  J[data = { combinedExceptions, currentLocation, orderDetails, vinEquipment, diagnosticsData }] --> K[vinTabsArray(t, data, connectedCarCoords, showDiagnosticDataTab, showConnectedCarTab, hasTripPlan)]
  K --> L[addTab(hasTripPlan) => "Trip Plan" (accessor: updates)]
  K --> M[addTab(alwaysShow) => "Updates" (accessor: statusUpdates)]
  K --> N[addTab(alwaysShow) => "Active Exceptions" (accessor: combinedExceptions)]
  K --> O[addTab(alwaysShow) => "Coordinates" (accessor: current_location)]
  K --> P[addTab(vinTabsData.orderDetails?.length > 0) => "Order Details"]
  K --> Q[addTab(alwaysShow) => "VIN Details" (accessor: references)]
  K --> R[addTab(showConnectedCarTab && connectedCarCoords.length > 0) => "Connected Car"]
  K --> S[addTab(showDiagnosticDataTab && vinTabsData.diagnosticsData?.length > 0) => "Diagnostics Data"]
  L --> T[result.array]
  M --> T
  N --> T
  O --> T
  P --> T
  Q --> T
  R --> T
  S --> T
  J --> U[return { array: result.array, data }]
  T --> U
```

> SVG rendering failed for this diagram.
