# Diagram: web/portal/src/pages/vinview/utils/utils.ts


> Auto-generated by Obscura crawlers

## Diagram 1

```mermaid
classDiagram
    class LabelValue {
        +string label
        +string value
    }
    class BatchInfo {
        +string batch_type
        +string batch_list
    }
    class Search {
        <<map>>
        +string|boolean|array|LabelValue[]|BatchInfo values
    }
    class SavedSearch {
        +number id
        +string name
        +Search search
    }
    class KeyMapEntry {
        +string|string[] baseQuery
        +string|string[] resolvedQuery
    }
    class KeyMapType {
        <<map>>
        +KeyMapEntry[]
    }
    class BatchFilterType {
        <<map>>
        +string
    }
    class FilterDef {
        +string|string[] queryKey
        +string|BatchFilterType queryKeyAlias
        +label(t)
        +Component(props)
        +optionsState
        +queryBuilder(query)
    }
    class CategoryDef {
        +string|string[] queryKey
        +string queryKeyAlias
        +label(t)
    }
    class ContextType {
        <<enum>>
        +postGresQueryKey
        +openSearchQueryKey
    }
    class flattenSearchArray {
        +searchArray: SearchEntry[] -> Record<string, any>
    }
    class useSavedSearchTransform {
        +getTransformedBatchQuery(context, searchTerm) -> {batch:BatchInfo}
        +getResolvedFilterKey(savedSearches, context) -> SavedSearch[]
    }

    SavedSearch "1" o-- "1" Search : has
    KeyMapType "1" o-- "*" KeyMapEntry : contains
    FilterDef --> Component : uses
    useSavedSearchTransform --> flattenSearchArray : uses
    useSavedSearchTransform --> KeyMapType : builds
    useSavedSearchTransform --> FilterDef : reads
    useSavedSearchTransform --> CategoryDef : reads
    getTransformedBatchQuery ..> BatchInfo : transforms
    getResolvedFilterKey ..> SavedSearch : maps
```

> SVG rendering failed for this diagram.

## Diagram 2

```mermaid
flowchart TD
    A[Start: useSavedSearchTransform.getResolvedFilterKey(savedSearches, context)] --> B[getDefinitions(context)]
    B --> C{context == "postGresQueryKey"?}
    C -->|yes| D[filters = OPEN_SEARCH_FILTERS\ncategories = OPEN_SEARCH_SEARCH_CATEGORIES]
    C -->|no| E[filters = FILTERS\ncategories = SEARCH_CATEGORIES]
    D --> F[mapQueryKeys over filters (skipBatch=false)]
    E --> F
    F --> G[mapQueryKeys over categories (skipBatch=true)]
    G --> H[Build keyMapper entries\n(handle arrays, aliases, special cases)]
    H --> I[newSavedSearch = savedSearches.map(searchItem => { ... })]
    I --> J[For each searchItem.search: for each key -> resolvedQueryKey = keyMapper[key]?.resolvedQuery || key]
    J --> K{key === "batch"?}
    K -->|yes| L[getTransformedBatchQuery(context, searchTerm)]
    K -->|no| M[{resolvedQueryKey: value}]
    L --> N[apply batch transformation switch by context and batch_type\n-> transformed batch object]
    M --> O[collect mapped entries]
    N --> O
    O --> P[flattenSearchArray(mappedEntries) -> new search object]
    P --> Q[return newSavedSearch array]
    Q --> R[End]
```

> SVG rendering failed for this diagram.
