# Diagram: web/portal/src/pages/locations/redux/EndToEndState.js


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
graph LR
subgraph Action_Creators
  A1["selectLob(lob)\n-> { type: SELECT_LOB, lob }"]
  A2["selectLocation(loc)\n-> { type: SELECT_LOCATION, location }"]
  A3["selectLad(lad)\n-> { type: SELECT_LAD, lad }"]
  A4["toggleLadsPanel(toggle)\n-> { type: TOGGLE_LADS_PANEL, toggle }"]
  A5["toggleLocationsPanel(toggle)\n-> { type: TOGGLE_LOCATIONS_PANEL, toggle }"]
  A6["setSearchStr(str)\n-> { type: SET_SEARCH_STR, searchStr }"]
end

Reducer["EndToEndReducer(state, action)"] 
State["State\nsearchStr: \"\"\nselectedLad: null\nselectedLob: 0\nselectedLocation: null\nshowLadsPanel: true\nshowLocationsPanel: false"]

A1 -->|dispatch SELECT_LOB| Reducer
A2 -->|dispatch SELECT_LOCATION| Reducer
A3 -->|dispatch SELECT_LAD| Reducer
A4 -->|dispatch TOGGLE_LADS_PANEL| Reducer
A5 -->|dispatch TOGGLE_LOCATIONS_PANEL| Reducer
A6 -->|dispatch SET_SEARCH_STR| Reducer

Reducer -->|on SELECT_LOB -> selectedLob = action.lob| State
Reducer -->|on SELECT_LOCATION -> selectedLocation = action.location| State
Reducer -->|on SELECT_LAD -> selectedLad = action.lad| State
Reducer -->|on TOGGLE_LADS_PANEL -> showLadsPanel = action.toggle| State
Reducer -->|on TOGGLE_LOCATIONS_PANEL -> showLocationsPanel = action.toggle| State
Reducer -->|on SET_SEARCH_STR -> searchStr = action.searchStr| State
```

> SVG rendering failed for this diagram.
