# Diagram: web/portal/src/pages/finishedvehicle/redux/FinVehicleSavedSearchCardsState.js


> Auto-generated by Obscura crawlers

## Diagram 1

```mermaid
classDiagram
    class Module {
        <<module>>
        +MOUNT_POINT: string
        +FETCH_SAVED_SEARCH_CARD_DATA: string
        +RECEIVE_SAVED_SEARCH_CARD_DATA: string
        +tokens: object
        +cancelMessage: string
        +formatResponse(response): object
        +getSavedSearchQueryString(savedSearch): string
        +fetchSavedSearchCardData(solutionId, savedSearch): function
        +getSavedSearchCardData(id): selector
        +reducer(state, action): state
        +FinVehicleSavedSearchCardsState: object
    }
    class formatResponse {
        +response
        +returns {exceptions, deliveredCount, activeCount, completeCount}
    }
    class getSavedSearchQueryString {
        +savedSearch
        -uses SEARCH_CATEGORIES
        -uses FILTERS
        -uses formatGroupCategoryParamInQueryString
        +returns queryString
    }
    class fetchSavedSearchCardData {
        +solutionId
        +savedSearch
        -cancels previous token if exists
        -creates axios request (GET/POST)
        -dispatches FETCH_SAVED_SEARCH_CARD_DATA
        -on success dispatches RECEIVE_SAVED_SEARCH_CARD_DATA
        -on error logs unless canceled
    }
    class reducer {
        +state
        +action
        -handles FETCH_SAVED_SEARCH_CARD_DATA
        -handles RECEIVE_SAVED_SEARCH_CARD_DATA
    }
    Module --> formatResponse : contains
    Module --> getSavedSearchQueryString : contains
    Module --> fetchSavedSearchCardData : contains
    Module --> getSavedSearchCardData : contains
    Module --> reducer : contains
    fetchSavedSearchCardData ..> tokens : uses
    getSavedSearchQueryString ..> SEARCH_CATEGORIES : reads
    getSavedSearchQueryString ..> FILTERS : reads
```

> SVG rendering failed for this diagram.

## Diagram 2

```mermaid
flowchart LR
    A[Start: fetchSavedSearchCardData(solutionId, savedSearch)] --> B{tokens[savedSearch.id] exists?}
    B -- yes --> C[Call tokens[savedSearch.id].cancelRequest("CANCELED")]
    B -- no --> D[no cancel]
    C --> D
    D --> E[Clone savedSearch and determine isBatch]
    E --> F[getSavedSearchQueryString(clonedSavedSearch)]
    F --> G[qs.parse(...) -> params]
    G --> H{isBatch?}
    H -- yes --> I[params.batchType = batch.batch_type]
    H -- no --> J[skip batch param]
    I --> K[queryString = qs.stringify(params)]
    J --> K
    K --> L[queryString = formatGroupCategoryParamInQueryString(queryString)]
    L --> M[Create axios request with method POST if batch else GET]
    M --> N[Create cancelToken and save tokens[savedSearch.id].cancelRequest]
    N --> O[dispatch({ type: FETCH_SAVED_SEARCH_CARD_DATA, id })]
    O --> P[request.then -> formatResponse(response) -> dispatch RECEIVE_SAVED_SEARCH_CARD_DATA]
    O --> Q[request.catch -> if error.message != "CANCELED" log error]
    P --> R[End: state updated with data and isLoading:false]
    Q --> R
```

> SVG rendering failed for this diagram.
