# Diagram: web/portal/src/pages/finishedvehicle/redux/FinVehicleEntitiesState.js


> Auto-generated by Obscura crawlers

## Diagram 1

```mermaid
classDiagram
    class FinVehicleEntitiesState {
        +mountPoint: "fvEntities"
        +actionCreators()
        +selectors()
        +reducer()
    }
    class ActionCreators {
        +fetchEntityCount(solutionId)
        +fetchEntityDeliveredCount(solutionId)
        +fetchEntityCompleteCount(solutionId)
        +fetchWatchedVins(solutionId)
        +fetchWatchedEntitiesTotalPages(solutionId)
        +setWatchedVinsPageIndex(pageIndex)
        +searchVINs(query, solutionId)
        +fetchFinViewCounts(solutionId)
        +updateVinLabel(solutionId, externalId, data, isDelete)
        +resetLabeling()
    }
    class Selectors {
        +getEntityCount(state)
        +getEntityDeliveredCount(state)
        +getEntityCompleteCount(state)
        +getWatchedVins(state)
        +getFinViewCounts(state)
        +getIsLabelEditing(state)
        +getIsEditSuccess(state)
        +getIsEditError(state)
    }
    class Reducer {
        +initialState
        +handle(action)
    }
    class initialState {
        +entityCount: 0
        +entityCountLoading: false
        +watchedEntities: []
        +watchedEntitiesLoading: false
        +watchedEntitiesPageIndex: 0
        +watchedEntitiesPageSize: 10
        +watchedEntitiesPageCount: 0
        +entityDeliveredCount: 0
        +entityCompleteCount: 0
        +finviewCounts: {}
        +isUpdatingLabel: false
        +isSuccess: false
        +isError: false
    }
    FinVehicleEntitiesState --> ActionCreators
    FinVehicleEntitiesState --> Selectors
    FinVehicleEntitiesState --> Reducer
    Reducer --> initialState
```

> SVG rendering failed for this diagram.

## Diagram 2

```mermaid
flowchart LR
    A[dispatch REQUEST_WATCHED_ENTITIES] --> B[axios.get(entity-watch) with params]
    B -->|success| C[extract entityIds from response.data.data]
    C --> D[build responseBody = entities with externalId, solutionId]
    D --> E[axios.post(batchUrl, {entities: responseBody})]
    E -->|labels success| F[transformWatchedVinsResponse(response.data.data, labelsResponse)]
    F --> G[dispatch RECEIVE_WATCHED_ENTITIES with watchedData]
    E -->|labels error| H[console.error(labelError)]
    H --> I[dispatch RECEIVE_WATCHED_ENTITIES with original response.data.data]
    B -->|error| J[console.error(error)]
    J --> K[dispatch RECEIVE_WATCHED_ENTITIES_ERROR]
```

> SVG rendering failed for this diagram.
