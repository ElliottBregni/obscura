# Diagram: web/portal/src/pages/finishedvehicle/redux/partnerview/PartnerViewSubscriptionState.js


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
flowchart TD
  S[subscribee] -->|getSubscribeeId(subscribee) => subscribee.internal_id| ID[id]
  ID -->|getUrl(id) => apiUrl(`/entity/internal/${id}`)| URL[/apiUrl(`/entity/internal/${id}`)/]
  URL -->|passed as getUrl| BS([buildSubscriptionState({topic: "PartnerView", systemType: "entity", ...})])
  BS --> PVS[PartnerViewSubscriptionState (export default)]
```

> SVG rendering failed for this diagram.
