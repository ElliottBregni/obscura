# Diagram: web/portal/src/pages/finishedvehicle/redux/FinVehicleEntityDetailsState.js


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
classDiagram
class FinVehicleEntityDetailsState {
  +mountPoint: string
  +actionCreators()
  +selectors()
  +reducer()
}
class Actions {
  +RECEIVE_ENTITY_DETAILS
  +RECEIVE_ENTITY_DETAILS_ERROR
  +RECEIVE_ENTITY_EXCEPTIONS
  +RECEIVE_ENTITY_POSITION_UPDATES
  +RECEIVE_ENTITY_CONNECTED_CAR_POSITION_UPDATES
  +CLEAR_MEDIA
  +RECEIVE_ENTITY_MEDIA
  +FETCH_COMMENTS / RECEIVE_COMMENTS
  +SUBMIT_NEW_COMMENT / RECEIVE_NEW_COMMENT
  +SUBMIT_BATCH_COMMENTS / SUBMIT_BATCH_COMMENTS_SUCCESS / FAILED
  +SHARE_VIN_BEGIN / SUCCESS / FAILED / CLEAR
  +RECEIVE_ENTITY_STATE_HISTORY / ERROR
  +CLEAR_ENTITY_DETAILS
}
class Selectors {
  +getEntityDetails(state)
  +getEntityDetailsError(state)
  +getEntityPositionUpdates(state)
  +getEntityConnectedCarPositionUpdates(state)
  +getEntityExceptions(state)
  +getEntityHolds(state)
  +getEntityEvents(state)
  +getEntityMedia(state)
  +getCombinedEntityExceptions()
  +getEntityCurrentLocation()
  +getIsFetchingComments()
  +getComments()
  +getIsSharingVin()
  +getIsBatchCommentInProgress()
  +getEntityStateChangeTs()
  +getEntityDocuments()
}
class ActionCreators {
  +fetchEntityDetails(solutionId, entityId)
  +fetchEntityStateHistory(solutionId, entityId, stateQuery)
  +fetchEntityPositionUpdates(solutionId, entityId,...)
  +clearEntityMedia()
  +fetchEntityMedia(url)
  +fetchEntityExceptions(solutionId, entityId,...)
  +fetchEntityHolds(solutionId, entityId,...)
  +fetchEntityEvents(solutionId, entityId,...)
  +fetchComments(solutionId, entityId,...)
  +addComment(solutionId, entityId, data)
  +addBatchComments(solutionId, data, isCsvFormat)
  +clearBatchComments()
  +cancelAddComment(fakeCommentId)
  +updateComment(solutionId, entityId, commentId, updatedData)
  +cancelUpdateComment(commentId)
  +markCommentsRead(solutionId, entityId, datetime)
  +shareVin(solutionId, entityId, emails, capturedScreenshot, timezone)
  +clearShareVin(clearIsSuccessful)
  +clearVinDetailsData()
  +setWatchEntity(solutionId, entityId, watch)
  +setNewWatchEntity(solutionId, entityId, watch)
  +fetchEntityDocuments(entityId)
}
class Reducer {
  +initialState
  +FinVehicleEntityDetailsReducer(state, action)
  --handles actions-->
  +updates entityDetails, entityMedia, entityPositionUpdates, entityConnectedCarPositionUpdates
  +updates entityExceptions, entityHolds, entityEvents
  +manages comments lifecycle and batch/share flags
  +manages entityStateHistory and documents
}
class API {
  +apiUrl(path)
  +customerApiUrl(path)
  +entityUrl(solutionId, entityId)
  +ESEntityUrl(solutionId, entityId)
}
FinVehicleEntityDetailsState --> Actions : "defines"
FinVehicleEntityDetailsState --> ActionCreators : "exposes"
FinVehicleEntityDetailsState --> Selectors : "exposes"
FinVehicleEntityDetailsState --> Reducer : "provides"
ActionCreators --> API : "calls"
ActionCreators --> Actions : "dispatches"
Reducer --> Actions : "handles"
Selectors --> Reducer : "reads state from"
API ..> "axios" : "uses for HTTP"
```

> SVG rendering failed for this diagram.
