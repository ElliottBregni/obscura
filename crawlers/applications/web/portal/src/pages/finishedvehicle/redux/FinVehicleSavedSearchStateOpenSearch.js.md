# Diagram: web/portal/src/pages/finishedvehicle/redux/FinVehicleSavedSearchStateOpenSearch.js


> Auto-generated by Obscura crawlers

## Diagram 1

```mermaid
classDiagram
    class buildSavedSearchState {
        <<function>>
        +({topic, savedSearchUrl, searchType, searchCategories, actions}) return SavedSearchState
    }
    class apiUrl {
        <<module-function>>
        +(path) string
    }
    class SEARCH_CATEGORIES_OPEN_SEARCH {
        <<const>>
    }
    class SearchState {
        <<module>>
        actionCreators
    }
    class actionCreators {
        +clearSearchFilters()
        +setSearchCategory()
        +setSearchText()
        +setSearchFilter()
    }
    class fvSavedSearchOpenSearch {
        <<exported instance>>
        topic: "fvSavedSearchOpenSearch"
        searchType: "ENTITY"
        savedSearchUrl: "/preferences-ng/search-history"
    }
    buildSavedSearchState --> fvSavedSearchOpenSearch : creates
    apiUrl --> fvSavedSearchOpenSearch : savedSearchUrl
    SEARCH_CATEGORIES_OPEN_SEARCH --> fvSavedSearchOpenSearch : searchCategories
    SearchState --> actionCreators : exposes
    actionCreators --> fvSavedSearchOpenSearch : actions mapped to
```

> SVG rendering failed for this diagram.

## Diagram 2

```mermaid
flowchart TD
    A[imports] -->|buildSavedSearchState| B[SavedSearchStateBuilder]
    A -->|SEARCH_CATEGORIES_OPEN_SEARCH| C[FinVehicleSearchCategoryDefsOpenSearch]
    A -->|apiUrl| D[api-url]
    A -->|SearchState| E[FinVehicleSearchBarStateOpenSearch]
    subgraph Build
        B --> F{config object}
        F --> G[topic: "fvSavedSearchOpenSearch"]
        F --> H[savedSearchUrl: apiUrl("/preferences-ng/search-history")]
        F --> I[searchType: "ENTITY"]
        F --> J[searchCategories: SEARCH_CATEGORIES_OPEN_SEARCH]
        F --> K[actions]
        K --> L[clearSearchFilters -> SearchState.actionCreators.clearSearchFilters]
        K --> M[setSearchCategory -> SearchState.actionCreators.setSearchCategory]
        K --> N[setSearchText -> SearchState.actionCreators.setSearchText]
        K --> O[setSearchFilter -> SearchState.actionCreators.setSearchFilter]
    end
    B --> P[export default fvSavedSearchOpenSearch]
```

> SVG rendering failed for this diagram.
