# Diagram: web/portal/src/pages/finishedvehicle/redux/partnerview/PartnerViewEntityDetailsState.js


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
classDiagram
class ActionConstants {
  +STORE_MOUNT_POINT
  +RECEIVE_ENTITY_DETAILS()
  +RECEIVE_ENTITY_EXCEPTIONS()
  +RECEIVE_ENTITY_HOLDS()
  +RECEIVE_ENTITY_POSITION_UPDATES()
  +RECEIVE_ENTITY_CONNECTED_CAR_POSITION_UPDATES()
  +CLEAR_MEDIA()
  +RECEIVE_ENTITY_MEDIA()
  +RECEIVE_ENTITY_EVENTS()
  +ENTITY_UPDATE_SUCCESS()
  +FETCH_COMMENTS()
  +FETCH_COMMENTS_FAILED()
  +RECEIVE_COMMENTS()
  +SUBMIT_NEW_COMMENT()
  +RECEIVE_NEW_COMMENT()
  +SUBMIT_NEW_COMMENT_FAILED()
  +CANCEL_NEW_COMMENT()
  +SET_IS_UPDATING_COMMENT()
  +SET_IS_UPDATING_COMMENT_FAILED()
  +CANCEL_UPDATE_COMMENT()
  +SUBMIT_BATCH_COMMENTS()
  +SUBMIT_BATCH_COMMENTS_SUCCESS()
  +SUBMIT_BATCH_COMMENTS_FAILED()
  +SUBMIT_BATCH_COMMENTS_RESET()
  +SHARE_VIN_CLEAR()
  +SHARE_VIN_BEGIN()
  +SHARE_VIN_SUCCESS()
  +SHARE_VIN_FAILED()
  +FETCH_SHIPPERS()
  +FETCH_SHIPPERS_SUCCESS()
  +FETCH_SHIPPERS_FAILED()
  +RECEIVE_ENTITY_DOCUMENTS()
  +RECEIVE_ENTITY_DOCUMENTS_ERROR()
}

class ActionCreators {
  +fetchEntityDetails(solutionId, entityId)
  +fetchEntityPositionUpdates(solutionId, entityId, type, maxResults)
  +clearEntityMedia()
  +fetchEntityMedia(url)
  +fetchEntityExceptions(entityId, exceptionStatus)
  +fetchEntityHolds(entityId, holdStatus)
  +fetchEntityDocuments(entityId)
  +fetchEntityEvents(solutionId, entityId)
  +fetchComments(solutionId, entityId, pageNumber, pageSize)
  +addComment(solutionId, entityId, data)
  +addBatchComments(data, isCsvFormat)
  +clearBatchComments()
  +fetchEntityShippers()
  +cancelAddComment(fakeCommentId)
  +updateComment(solutionId, entityId, commentId, updatedData)
  +cancelUpdateComment(commentId)
  +markCommentsRead(solutionId, entityId, datetime)
  +shareVin(solutionId, entityId, emails, capturedScreenshot, timezone)
  +clearShareVin(clearIsSuccessful)
  +setWatchEntity(solutionId, entityId, watch)
}

class Selectors {
  +getEntityDetails(state)
  +getEntityPositionUpdates(state)
  +getEntityConnectedCarPositionUpdates(state)
  +getEntityExceptions(state)
  +getEntityHolds(state)
  +getCombinedExceptions(state)
  +getEntityEvents(state)
  +getEntityMedia(state)
  +getEntityCurrentLocation(state)
  +getIsFetchingComments(state)
  +getComments(state)
  +getIsSharingVin(state)
  +getIsShareVinSuccessful(state)
  +getIsShareVinFailed(state)
  +getIsBatchCommentInProgress(state)
  +getIsBatchCommentSuccessful(state)
  +getIsBatchCommentFailed(state)
  +getEntityShippers(state)
  +getEntityDocuments(state)
}

class Reducer {
  +initialState
  +PartnerViewEntityDetailsReducer(state, action)
  --handles actions-->
  +RECEIVE_ENTITY_DETAILS
  +RECEIVE_ENTITY_MEDIA
  +CLEAR_MEDIA
  +RECEIVE_ENTITY_POSITION_UPDATES
  +RECEIVE_ENTITY_CONNECTED_CAR_POSITION_UPDATES
  +RECEIVE_ENTITY_EXCEPTIONS
  +RECEIVE_ENTITY_HOLDS
  +RECEIVE_ENTITY_DOCUMENTS
  +RECEIVE_ENTITY_DOCUMENTS_ERROR
  +RECEIVE_ENTITY_EVENTS
  +FETCH_COMMENTS
  +FETCH_COMMENTS_FAILED
  +RECEIVE_COMMENTS
  +SUBMIT_NEW_COMMENT
  +RECEIVE_NEW_COMMENT
  +SUBMIT_NEW_COMMENT_FAILED
  +CANCEL_NEW_COMMENT
  +SET_IS_UPDATING_COMMENT
  +SET_IS_UPDATING_COMMENT_FAILED
  +CANCEL_UPDATE_COMMENT
  +SUBMIT_BATCH_COMMENTS
  +SUBMIT_BATCH_COMMENTS_SUCCESS
  +SUBMIT_BATCH_COMMENTS_FAILED
  +SUBMIT_BATCH_COMMENTS_RESET
  +SHARE_VIN_CLEAR
  +SHARE_VIN_BEGIN
  +SHARE_VIN_SUCCESS
  +SHARE_VIN_FAILED
  +FETCH_SHIPPERS_SUCCESS
}

class State {
  +entityExceptions: []
  +entityHolds: []
  +entityPositionUpdates: []
  +entityConnectedCarPositionUpdates: []
  +entityDocuments: []
  +entityDocumentsError: null
  +entityShippers: []
  +isFetchingComments: false
  +comments: { totalPages,totalCount,totalCountUnread,data }
  +isSharingVin: false
  +isShareVinSuccessful: false
  +isShareVinFailed: false
  +isBatchCommentInProgress: false
  +isBatchCommentSuccessful: false
  +isBatchCommentFailed: false
  +entityDetails
  +entityMedia
  +entityEvents
}

ActionCreators ..> ActionConstants : uses
Reducer o-- State : contains
Selectors --> State : reads
ActionCreators --> Reducer : dispatches actions to
ActionCreators ..> "axios/moment/uuid/lodash" : depends on
ActionCreators ..> "apiUrl/customerApiUrl/CarrierPartnerAndDealerTripSummaryEventCodes" : uses external APIs/constants
```

> SVG rendering failed for this diagram.
