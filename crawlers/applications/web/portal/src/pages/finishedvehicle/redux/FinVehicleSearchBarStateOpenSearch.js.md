# Diagram: web/portal/src/pages/finishedvehicle/redux/FinVehicleSearchBarStateOpenSearch.js


> Auto-generated by Obscura crawlers

## Diagram 1

```mermaid
flowchart LR
  A[addLifeCycleParam(qs)] -->|returns modified qs| B[entitiesUrl(solutionId, queryString)]
  A -->|if contains lifeCycleState| A_end[returns qs unchanged]
  B --> C[entitiesExportUrl(solutionId, queryString)]
  D[axiosConfig()] -->|returns headers.x-time-zone| E((moment.tz.guess()))
  F[formatGroupCategoryParamInQueryString(queryString)] -->|parses qs| G[qs.parse/queryParams]
  G --> H{groupCategory:ITSS / SB present?}
  H -->|both| I[groupCategory = ITSS,SB combined]
  H -->|one only| J[groupCategory = single value]
  I --> K[delete temp params; qs.stringify]
  J --> K
  K --> L[returned queryString]

  subgraph FetchSearchFlow
    M[fetchSearch(queryString, solutionId, duck, dispatch, state, preventRedirect, requestBody)]
    M --> N{batchFilter && batchFilter.batch_type?}
    N -->|yes| O[build requestBody.filter with batch values]
    O --> P[axiosConfiguration {method:POST, url: entitiesUrl, data: requestBody, headers}]
    P --> Q[dispatch(duck.fetch(url, axiosConfiguration))]
    P --> R{!preventRedirect}
    R -->|true| S[dispatch({ type: "VIN_SEARCH" })]
    N -->|no| T[setup featuredReferencesKeys from requestBody.filter]
    T --> U{featuredReferencesKeys.length > 0?}
    U -->|true| V[normalize featuredReferences into requestBody.filter.featuredReferences]
    V --> W[delete original featuredReferences keys]
    U -->|either| X[axiosConfiguration {method:POST, url: entitiesUrl, data: requestBody, headers}]
    X --> Q
    X --> R
  end
```

> SVG rendering failed for this diagram.

## Diagram 2

```mermaid
classDiagram
  class SearchBarStateOpenSearch {
    <<factory result>>
    +actionCreators
    +selectors
  }
  class Build {
    +buildSearchBarState(mountPoint, categories, filters, fetchSearch, [], {}, pageSize, paginationType, sortType)
  }
  SearchBarStateOpenSearch --|> Build : created by
  SearchBarStateOpenSearch : actionCreators.exportSearch = _.partial(exportEntities, entitiesExportUrl, entitiesExportUrl, headers, "fin-vehicle-search-results")
  SearchBarStateOpenSearch : selectors = (inherited from builder)
```

> SVG rendering failed for this diagram.
