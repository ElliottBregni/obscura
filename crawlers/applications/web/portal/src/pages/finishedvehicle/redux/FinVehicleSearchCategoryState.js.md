# Diagram: web/portal/src/pages/finishedvehicle/redux/FinVehicleSearchCategoryState.js


> Auto-generated by Obscura crawlers

## Diagram 1

```mermaid
classDiagram
    class AsyncCategoryOptionsState {
        +topic: string
        +getUrl(solutionId): string
        +queryParam: string
        +getResponseData(data): any[]
        +transformResult(item): {label, value}
        +debounceWaitTimeInMs: number
        +method: RequestMethod
    }

    class ProductTypeLoaderState {
        +topic = "PRODUCT_TYPE"
        +queryParam = "q"
        +debounceWaitTimeInMs = 500
        +method = RequestMethod.POST
    }

    class LocationLoaderState {
        +topic = "LOCATION"
        +queryParam = "q"
        +debounceWaitTimeInMs = 500
        +method = RequestMethod.POST
    }

    AsyncCategoryOptionsState <|-- ProductTypeLoaderState
    AsyncCategoryOptionsState <|-- LocationLoaderState
```

> SVG rendering failed for this diagram.

## Diagram 2

```mermaid
flowchart TD
    subgraph URL_Sources
        A[getFilterUrlOpenSearch(solutionId, "productTypeFilter")] -->|used by| PT[productTypeLoaderState.getUrl]
        B[getFilterUrlOpenSearch(solutionId, "location")] -->|used by| LOC[locationLoaderState.getUrl]
    end

    subgraph Transformations
        IT[item] --> TR1{{transformResult (productType)}}
        IT --> TR2{{transformResult (location)}}
        TR1 --> L1[label = item.value]
        TR1 --> V1[value = item.value]
        TR2 --> L2[label = item.value + " (" + item.key + ")"]
        TR2 --> V2[value = item.key]
    end

    PT -->|fetches| ES1[(OpenSearch POST q)]
    LOC -->|fetches| ES2[(OpenSearch POST q)]
    ES1 --> R1[data]
    ES2 --> R2[data]
    R1 -->|extract| RD1[getResponseData => data?.data ?? []]
    R2 -->|extract| RD2[getResponseData => data?.data ?? []]
    RD1 --> IT
    RD2 --> IT
```

> SVG rendering failed for this diagram.
