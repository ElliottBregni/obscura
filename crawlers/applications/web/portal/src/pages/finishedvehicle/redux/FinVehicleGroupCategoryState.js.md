# Diagram: web/portal/src/pages/finishedvehicle/redux/FinVehicleGroupCategoryState.js


> Auto-generated by Obscura crawlers

## Diagram 1

```mermaid
classDiagram
    class fvGroupCategories {
        +mountPoint: "fvGroupCategories"
        +actionCreators
        +selectors
        +reducer
    }
    class itssCountDuck {
        +fetch(url)
        +selectors.getData()
        +reducer()
    }
    class spotBuyCountDuck {
        +fetch(url)
        +selectors.getData()
        +reducer()
    }
    class chainReducers()
    class buildFetchDuck()
    fvGroupCategories --> itssCountDuck : uses
    fvGroupCategories --> spotBuyCountDuck : uses
    fvGroupCategories --> chainReducers : composes reducer
    itssCountDuck ..> buildFetchDuck : created by
    spotBuyCountDuck ..> buildFetchDuck : created by
```

> SVG rendering failed for this diagram.

## Diagram 2

```mermaid
flowchart TD
    A[fetchItssCount(solutionId)] --> B[build URL: /entity-group/solution/{solutionId}/group-type/ITSS/group-category/count]
    B --> C[apiUrl(url)]
    C --> D[itssCountDuck.fetch(url)]
    D --> E[dispatch(result)]
    F[fetchSpotBuyCount(solutionId)] --> G[build URL: /entity-group/solution/{solutionId}/group-type/SB/group-category/count]
    G --> H[apiUrl(url)]
    H --> I[spotBuyCountDuck.fetch(url)]
    I --> J[dispatch(result)]
    subgraph exports
        K[mountPoint: fvGroupCategories]
        L[actionCreators: fetchItssCount, fetchSpotBuyCount]
        M[selectors: getItssCountData, getSpotBuyCountData]
        N[reducer: chained reducers]
    end
    E --> N
    J --> N
    K --> L
    K --> M
    K --> N
```

> SVG rendering failed for this diagram.
