# Diagram: web/portal/src/pages/finishedvehicle/search/FinVehicleSearchFiltersContainerOpenSearch.js


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
flowchart LR
  FilterSection[FilterSection Component]
  Connect[connect()]
  MapState(mapStateToProps)
  Actions[actionCreators]
  Merge(mergeProps)
  FinState[FinVehicleSearchBarState]
  OPEN[OPEN_SEARCH_FILTERS]
  Domain[DomainDataState.selectors]
  Auth[getAuthorization / auth]
  Org[getSolutionId]
  Selectors[FinVehicleSearchFilterSelectors]
  State[state]

  Connect -->|wraps| FilterSection
  Connect --> MapState
  Connect --> Actions
  Connect --> Merge

  MapState --> State
  MapState --> Auth
  MapState --> Org
  MapState --> FinState
  MapState --> Domain
  MapState --> Selectors

  Auth -->|validateEntitySystemConfigValue| MapState
  Org -->|getSolutionId(state)| MapState
  Domain -->|selectRegionFilterOptions(state)| MapState
  FinState -->|selectors.getSearchFilters / getHasSearchCriteriaChanged / getCanUserSearch| MapState

  Selectors -->|carrierFilterOptions etc.| MapState

  Actions --> FinState
  FinState -->|actionCreators: setSearchFilter, clearSearchFilter, searchEntities, resetSavedSearch, clearSavedSearch| Actions

  Merge --> OPEN
  Merge -->|combines| MapState
  Merge -->|combines| Actions
  Merge -->|combines| FilterSection

  FilterSection -->|receives props from| Merge
```

> SVG rendering failed for this diagram.
