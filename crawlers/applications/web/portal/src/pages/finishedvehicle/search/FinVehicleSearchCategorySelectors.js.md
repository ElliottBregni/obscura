# Diagram: web/portal/src/pages/finishedvehicle/search/FinVehicleSearchCategorySelectors.js


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
flowchart TD
  A[modules/domain-data/FinVehicleDomainData] --> B[getLocations()]
  B --> C[createSelector(...)]
  C --> D[locations.map(l => `${l.name} (${l.code})`)]
  D --> E[selectLocationOptions (exported selector)]
```

> SVG rendering failed for this diagram.
