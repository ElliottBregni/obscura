# Diagram: web/portal/src/pages/finishedvehicle/search/FinVehicleSearchBarContainer.js


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
flowchart LR
  subgraph Imports
    A[react-redux.connect]
    B[components/search-bar/SearchBar]
    C[pages/finishedvehicle/redux/FinVehicleSearchBarState]
    D[modules/organizations/OrganizationsState.getSolutionId]
    E[modules/domain-data/FinVehicleDomainData]
    F[pages/finishedvehicle/search/FinVehicleSearchCategorySelectors.selectLocationOptions]
    G[modules/auth/AuthorizationSelectors.getAuthorization]
  end

  subgraph MapStateToProps
    MS(mapStateToProps)
    MS -->|uses selectors| SBarSelectors[SearchBarState.selectors]
    SBarSelectors --> getSearchText[getSearchText()]
    SBarSelectors --> getSearchCategory[getSearchCategory()]
    SBarSelectors --> getIgnoreSearchCategory[getIgnoreSearchCategory()]
    SBarSelectors --> getTypeaheadOptionsMetadata[getTypeaheadOptionsMetadata()]
    SBarSelectors --> getHasSearchCriteriaChanged[getHasSearchCriteriaChanged()]
    SBarSelectors --> getCanUserSearch[getCanUserSearch()]
    MS --> auth[getAuthorization(state)]
    MS --> typeahead[typeaheadOptionsMetadata]
    MS --> searchText[searchText]
    MS --> searchCategory[searchCategory]
    MS --> ignoreSearchCategory[ignoreSearchCategory]
    MS --> hasSearchCriteriaChanged[hasSearchCriteriaChanged]
    MS --> canUserSearch[canUserSearch]
    MS --> solutionId[getSolutionId(state)]
    MS --> vins[finVehicleDomainData.selectors.getVINs(state)]
    MS --> shipments[finVehicleDomainData.selectors.getShipments(state)]
    MS --> locations[selectLocationOptions(state)]
    MS --> productTypes[finVehicleDomainData.selectors.getProductTypes(state)]
    MS --> orderNumbers[finVehicleDomainData.selectors.getOrderNumbers(state)]
    MS --> exceptionTypes[finVehicleDomainData.selectors.getExceptionTypes(state)]
    MS --> routeIds[finVehicleDomainData.selectors.getRouteIds(state)]
  end

  subgraph ActionCreators
    AC(actionCreators)
    AC --> setSearchCategoryForKey[setSearchCategoryForKey]
    AC --> setSearchTextWrapper[setSearchText(text, ignoreValueChange) -> dispatch]
    AC --> clearSearchText[clearSearchText]
    AC --> resetSearchBar[resetSearchBar]
    AC --> searchEntities[searchEntities]
    AC --> clearSavedSearch[clearSavedSearch]
    AC --> fetchDomainData[fetchDomainData]
    setSearchTextWrapper -->|dispatches| setSearchTextInner[setSearchText(text, ignoreValueChange, true)]
  end

  A -->|connects| Connect[connect(mapStateToProps, actionCreators)]
  Connect --> B
  C --> SBarSelectors
  C --> setSearchCategoryForKey
  C --> setSearchTextInner
  C --> clearSearchText
  C --> resetSearchBar
  C --> searchEntities
  C --> clearSavedSearch
  E --> vins
  E --> shipments
  E --> productTypes
  E --> orderNumbers
  E --> exceptionTypes
  E --> routeIds
  E --> fetchDomainData
  D --> solutionId
  F --> locations
  G --> auth

  style Imports fill:#f9f,stroke:#333,stroke-width:1px
  style MapStateToProps fill:#efe,stroke:#333,stroke-width:1px
  style ActionCreators fill:#eef,stroke:#333,stroke-width:1px
```

> SVG rendering failed for this diagram.
