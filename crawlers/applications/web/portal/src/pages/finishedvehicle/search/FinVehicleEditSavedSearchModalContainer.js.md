# Diagram: web/portal/src/pages/finishedvehicle/search/FinVehicleEditSavedSearchModalContainer.js


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
classDiagram
    class EditSavedSearchModal {
        +props.searchFilters
        +props.searchCategory
        +props.searchText
        +props.saveSearch(name, search)
        +props.editSearch(id, name, search)
        +props.SearchBarContainer
        +props.FilterSectionContainer
    }

    class Connect {
        +connect(mapStateToProps, mapDispatchToProps)(Component)
    }

    class mapStateToProps {
        +searchFilters
        +searchCategory
        +searchText
        +SearchBarContainer
        +FilterSectionContainer
    }

    class mapDispatchToProps {
        +saveSearch(name, search)
        +editSearch(id, name, search)
    }

    class FinVehicleSearchBarState {
        class selectors {
            +getSearchFilters(state)
            +getSearchCategory(state)
            +getSearchText(state)
        }
    }

    class FinVehicleSavedSearchState {
        class actionCreators {
            +saveSearch(name, search)
            +editSearch(id, name, search)
        }
    }

    class SearchBarContainer
    class FilterSectionContainer

    Connect --> EditSavedSearchModal : wraps
    mapStateToProps --> FinVehicleSearchBarState.selectors : uses
    mapDispatchToProps --> FinVehicleSavedSearchState.actionCreators : uses
    mapStateToProps --> SearchBarContainer : provides
    mapStateToProps --> FilterSectionContainer : provides
    Connect --> mapStateToProps : accepts
    Connect --> mapDispatchToProps : accepts
    EditSavedSearchModal --> SearchBarContainer : renders
    EditSavedSearchModal --> FilterSectionContainer : renders
    mapDispatchToProps --> EditSavedSearchModal : provides actions
    mapStateToProps --> EditSavedSearchModal : provides props
```

> SVG rendering failed for this diagram.
