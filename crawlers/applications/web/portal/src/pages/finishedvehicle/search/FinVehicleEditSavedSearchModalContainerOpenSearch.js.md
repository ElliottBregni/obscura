# Diagram: web/portal/src/pages/finishedvehicle/search/FinVehicleEditSavedSearchModalContainerOpenSearch.js


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
classDiagram
    class EditSavedSearchModal {
        <<React Component>>
    }
    class connect {
        <<HOC>>
        +(mapStateToProps, mapDispatchToProps)(Component)
    }
    class mapStateToProps {
        +searchFilters
        +searchCategory
        +searchText
        +SearchBarContainer
        +FilterSectionContainer
    }
    class mapDispatchToProps {
        +saveSearch(name, search)
        +editSearch(id, name, search)
    }
    class FinVehicleSearchBarState {
        class selectors {
            +getSearchFilters(state)
            +getSearchCategory(state)
            +getSearchText(state)
        }
    }
    class FinVehicleSavedSearchState {
        class actionCreators {
            +saveSearch(name, search)
            +editSearch(id, name, search)
        }
    }
    class SearchBarContainer {
        <<Component Reference>>
    }
    class FilterSectionContainer {
        <<Component Reference>>
    }

    FinVehicleSearchBarState --|> mapStateToProps : "uses selectors"
    FinVehicleSavedSearchState --|> mapDispatchToProps : "uses actionCreators"
    SearchBarContainer --|> mapStateToProps : "included as prop"
    FilterSectionContainer --|> mapStateToProps : "included as prop"
    mapStateToProps --> connect : "provided to"
    mapDispatchToProps --> connect : "provided to"
    connect --> EditSavedSearchModal : "wraps and exports"
```

> SVG rendering failed for this diagram.
