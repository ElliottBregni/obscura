# Diagram: web/portal/src/pages/finishedvehicle/search/FinishedVehicle.Search.columns.js


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
classDiagram
    class findReference {
        +references
        +key
        +return value|null
    }
    class VinCell {
        +props.value
        +handleCopy(e)
        +render()
    }
    class LastUpdateCell {
        +props.value
        +render()
    }
    class LastUpdateCellOpenSearch {
        +props.value
        +render()
    }
    class ExceptionCell {
        +props.value
        +render()
    }
    class ExceptionCellOpenSearch {
        +props.value
        +computeLatestClearDate()
        +render()
    }
    class LocationCell {
        +props.value
        +render()
    }
    class columns {
        +t,solutionId,showItssData,showSpotBuyData,showScheduleWindow,isOpenSearchFeatureEnabled,displayShippabilityStatus,displayExternalState
        +returns results[]
    }
    %% Relationships
    columns --> VinCell : uses
    columns --> LastUpdateCell : uses (fallback)
    columns --> LastUpdateCellOpenSearch : uses (openSearch)
    columns --> ExceptionCell : uses (fallback)
    columns --> ExceptionCellOpenSearch : uses (openSearch)
    columns --> LocationCell : uses
    VinCell --> findReference : calls
    LastUpdateCell --> translateRailStatusCode : calls
    LastUpdateCell --> localizedTimeFormatter : calls
    LastUpdateCell --> localizedDateFormatter : calls
    LastUpdateCell --> getCurrentPositionDetails : calls
    LastUpdateCellOpenSearch --> getCurrentPositionDetails : calls
    LastUpdateCellOpenSearch --> localizedTimeFormatter : calls
    LastUpdateCellOpenSearch --> localizedDateFormatter : calls
    ExceptionCell --> translateExceptionName : calls
    ExceptionCellOpenSearch --> translateExceptionName : calls
    ExceptionCellOpenSearch --> DateTimeCell : uses
    LocationCell --> DateTime : uses
    LocationCell --> DateTimeRange : uses
    LocationCell --> useEtaTranslations : uses
    VinCell ..> "Icon,Tooltip,Text,FeaturedReference,ShippabilityStatusText,ExternalStateText" : composes
    LastUpdateCell ..> MadChiclet : composes
    LastUpdateCellOpenSearch ..> MadChiclet : composes
    ExceptionCell ..> ShowMoreList : composes
    ExceptionCellOpenSearch ..> ShowMoreList : composes
    LocationCell ..> DateTimeCell : composes (when completed)
    columns ..> WatchToggle : composes in Watch column
    class translateRailStatusCode,localizedTimeFormatter,localizedDateFormatter,getCurrentPositionDetails,translateExceptionName,DateTime,DateTimeRange,DateTimeCell,ShowMoreList,WatchToggle,MadChiclet,Icon,Tooltip,Text,FeaturedReference,ShippabilityStatusText,ExternalStateText {
        <<utility/component>>
    }
```

> SVG rendering failed for this diagram.
