# Diagram: web/portal/src/pages/finishedvehicle/details/utils/utils.js


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
graph TD
  U[updateHasLocation (external)]:::external
  GC[getCoordinates] -->|returns coords| GL[getLatLngFromTripPlanLocation]
  GL -->|uses coords from location.coordinates.center/firstCenter| GC
  ETL[extractTripLegLocationDetails] -->|calls|getCoordinates
  ETL -->|calls|U
  ETL -->|maps tripLegs.dest -> getCoordinates| GD[getCoordinates (dest)]
  ETL -->|returns| Locations[Array of locations]
  ETE[extractTripLegEndpoints] -->|validates tripLegs array| Validate[Array.isArray check]
  ETE -->|defines|getPosLocationLad[getPosLocationLad(loc, tripLegType)]
  getPosLocationLad -->|calls| GL
  getPosLocationLad -->|calls| U
  ETE -->|computes firstOrigin,lastDest -> returns| Endpoints[[firstOrigin, lastDest]]
  EML[extractMilestoneLocationDetails] -->|parses milestoneCodeToBeDisplayedInMap| Parse[JSON.parse or use as-is]
  EML -->|filters milestoneCoordinates by codes| Filter
  EML -->|maps items to pos/location/lad| MapTransform
  EML -->|returns| Milestones[[configured milestone locations]]
  GL -->|uses|null coalesce lat/lng| NullCoalesce[(latitude/longitude ?? null)]
  classDef external fill:#f9f,stroke:#333,stroke-width:1px;
  class GC,GD,GL getPosLocationLad,ETL,ETE,EML Parse,Filter,MapTransform,Validate external;
```

> SVG rendering failed for this diagram.
