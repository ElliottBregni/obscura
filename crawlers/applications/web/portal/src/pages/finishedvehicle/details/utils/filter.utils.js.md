# Diagram: web/portal/src/pages/finishedvehicle/details/utils/filter.utils.js


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
flowchart LR
  A[translateCodeLabelLookup(t, value)] --> B{lookup object}
  B --> C1["manually_completed → t(\"filters:Shipment manually completed/closed\")"]
  B --> C2["1300 → t(\"filters:No asset assigned - shipment should be in transit\")"]
  B --> C3["missed_pickup_exceptions → t(\"filters:Missed Pickup\")"]
  B --> C4["missed_delivery_exceptions → t(\"filters:Missed Drop-Off\")"]
  B --> C5["behind_schedule → t(\"filters:Behind Schedule\")"]
  B --> C6["1380 → t(\"filters:Tracking asset past destination delivery window.\")"]
  B --> C7["bad_order_exceptions → t(\"filters:Bad Order\")"]
  B --> C8["in_hold_exceptions → t(\"filters:In Hold\")"]
  B --> C9["conflicting_etas → t(\"filters:ETA conflict between shipment and last stop records\")"]
  B --> C10["idle_train_exceptions → t(\"filters:Idle Train\")"]
  B --> C11["missed_delivery → t(\"filters:Shipment Missed Delivery\")"]
  B --> C12["2070 → t(\"filters:Shipment arrived at ultimate destination, but no delivery event\")"]
  B --> C13["1070 → t(\"filters:Dweller Shipment\")"]
  B --> C14["under_review → t(\"filters:Under Review\")"]
  B --> C15["1000 → t(\"filters:Shipment has conflicting asset IDs\")"]
  B --> C16["1100 → t(\"filters:Invalid missed pickup exception\")"]
  B --> C17["1200 → t(\"filters:Completed shipment not showing properly\")"]
  B --> C18["1350 → t(\"filters:Missed Asset assignment for in-transit shipment\")"]
  B --> C19["\"Excessive Dwell\" → t(\"filters:Aging Vin\")"]
  B --> D{value in lookup?}
  D -- yes --> E[return lookup[value]]
  D -- no --> F[return value] 
  A --> D
```

> SVG rendering failed for this diagram.
