# Diagram: web/portal/src/pages/finishedvehicle/details/utils/PartnerViewDetailsTabDefinitions.js


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
flowchart TD
  A[getVinTabs(params)] --> B[filteredCombinedExceptions(combinedExceptions, shouldFilterExceptions)]
  A --> C[getOrderDetails(t, details)]
  A --> D[getVinEquipment(details)]
  A --> E[getDiagnosticsData(t, details, showDiagnosticDataTab, getDiagnosticLabel, getColorByState)]
  B --> F[data.combinedExceptions = filterException]
  C --> G[data.orderDetails = orderDetails]
  D --> H[data.vinEquipment = vinEquipment]
  E --> I[data.diagnosticsData = diagnosticsData]
  A --> J[data.currentLocation = currentLocation]
  F & G & H & I & J --> K[data object assembled]
  K --> L[vinTabsArray(t, data, connectedCarCoords, showDiagnosticDataTab, showConnectedCarTab, hasTripPlan)]
  L --> M{vinTabsArray: build tabs}
  M --> N[define alwaysShow = true]
  M --> O[define tabs = []]
  M --> P[define addTab(condition, tab) -> if condition push tab]
  P --> Q[addTab(hasTripPlan, Trip Plan)]
  P --> R[addTab(alwaysShow, Updates)]
  P --> S[addTab(alwaysShow, Active Exceptions)]
  P --> T[addTab(alwaysShow, Coordinates)]
  P --> U[addTab(vinTabsData.orderDetails?.length > 0, Order Details)]
  P --> V[addTab(alwaysShow, VIN Details)]
  P --> W[addTab(showConnectedCarTab && connectedCarCoords.length > 0, Connected Car)]
  P --> X[addTab(showDiagnosticDataTab && vinTabsData.diagnosticsData?.length > 0, Diagnostics Data)]
  Q --> Y[tabs array populated]
  R --> Y
  S --> Y
  T --> Y
  U --> Y
  V --> Y
  W --> Y
  X --> Y
  Y --> Z[return { array: tabs, data }]
  Z --> END[Result returned]
```

> SVG rendering failed for this diagram.
