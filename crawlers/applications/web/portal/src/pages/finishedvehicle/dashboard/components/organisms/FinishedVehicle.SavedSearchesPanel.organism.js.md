# Diagram: web/portal/src/pages/finishedvehicle/dashboard/components/organisms/FinishedVehicle.SavedSearchesPanel.organism.js


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
flowchart TD
  FVSSP[FinishedVehicleSavedSearchesPanel]
  SSP[SavedSearchesPanel]
  FVS[FinishedVehicleSavedSearch]
  Modal[FinVehicleEditSavedSearchModalContainer]
  ReduxFS[FinVehicleSavedSearchState\n(selectors & actionCreators)]
  ReduxFB[FinVehicleSearchBarState\n(actionCreators)]
  useShow[(state: showModal)]
  useCurrent[(state: currentSavedSearch)]
  useDeleting[(state: deletingSavedSearchId)]

  FVSSP -->|renders| SSP
  FVSSP -->|renders| Modal
  FVSSP -->|passes CardComponent| FVS
  FVSSP -->|uses selector getSavedSearches| ReduxFS
  FVSSP -->|uses selector getIsLoading| ReduxFS
  FVSSP -->|dispatches fetchSavedSearches on mount| ReduxFS
  FVSSP -->|dispatch| ReduxFS
  FVSSP -->|dispatch| ReduxFB

  SSP -->|onAddSavedSearch -> set showModal true| useShow
  SSP -->|getCardProps(savedSearch) supplies props| FVS
  FVS -->|onEditClick -> dispatch loadSavedSearch(savedSearch,true)| ReduxFS
  FVS -->|onEditClick -> set currentSavedSearch| useCurrent
  FVS -->|onEditClick -> set showModal true| useShow

  Modal -->|props savedSearch = currentSavedSearch| useCurrent
  Modal -->|props show = showModal| useShow
  Modal -->|hide -> dispatch resetSearchAndFilters(true)| ReduxFB
  Modal -->|hide -> set showModal false & clear currentSavedSearch| useShow & useCurrent
  Modal -->|deleteSearch(id) -> dispatch deleteSearch(id)| ReduxFS
  Modal -->|deleteSearch(id) -> set deletingSavedSearchId(id)| useDeleting

  ReduxFS -->|loadSavedSearch, fetchSavedSearches, deleteSearch| FVSSP
  ReduxFB -->|resetSearchAndFilters| FVSSP

  classDef state fill:#f9f,stroke:#333,stroke-width:1px;
  class useShow,useCurrent,useDeleting state
```

> SVG rendering failed for this diagram.
