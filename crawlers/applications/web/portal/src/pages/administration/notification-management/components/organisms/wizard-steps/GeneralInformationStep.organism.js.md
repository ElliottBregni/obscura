# Diagram: web/portal/src/pages/administration/notification-management/components/organisms/wizard-steps/GeneralInformationStep.organism.js


> Auto-generated by Obscura crawlers

## Diagram 1

```mermaid
classDiagram
    class GeneralInformationStep {
        +bool isEditing
        +func setIsNextButtonDisabled
        +string notificationRuleName
        +func setDeliveryActions
        +array deliveryActions
        +object userDefaultCommunication
        +func setNotificationRuleName
        +object unsubscribeIdMap
        -state recipientList
        -state notificationNameLocal
        +handleStep()
        +addRecipientHandler()
        +removeRecipientHandler(idx)
        +setRecipient(idx)
    }
    class assignAndMapUnsubscribeIdToDeliveryActions {
        +(recipientList, unsubscribeIdMap) : array
    }
    class Hooks {
        +useState()
        +useEffect()
        +useTranslation()
        +useWizard()
    }
    class ValidationUtils {
        +isValidEmail()
        +isPossiblePhoneNumber()
    }
    class UIComponents {
        +Text
        +TextInput
        +InteractiveList
        +RecipientInput
    }
    GeneralInformationStep --> assignAndMapUnsubscribeIdToDeliveryActions : uses
    GeneralInformationStep --> Hooks : uses
    GeneralInformationStep --> ValidationUtils : validates recipients
    GeneralInformationStep --> UIComponents : renders
    assignAndMapUnsubscribeIdToDeliveryActions ..> "uuidv4()" : generates IDs
```

> SVG rendering failed for this diagram.

## Diagram 2

```mermaid
flowchart TB
    Props["Component Props\n(isEditing, setIsNextButtonDisabled,\nnotificationRuleName, setDeliveryActions,\ndeliveryActions, userDefaultCommunication,\nsetNotificationRuleName, unsubscribeIdMap)"]
    Init["Init: derive userDefaultEmail/Phone\ninitialize recipientList state from deliveryActions"]
    DefaultRecipients["Populate default recipients\nif recipientList empty and !isEditing"]
    UI["Render UI\nText, TextInput,\nInteractiveList -> RecipientInput(s)"]
    RecipientListState["recipientList (state)"]
    NameState["notificationNameLocal (state)"]
    Validation["useEffect: validate recipients\n- clean empty entries\n- check email/phone formats\n- ensure uniqueness\n- require name and >=1 recipient\n-> setIsNextButtonDisabled"]
    HandleStep["handleStep() (wizard)\n- assign unsubscribeIds via\n  assignAndMapUnsubscribeIdToDeliveryActions\n- call setDeliveryActions\n- call setNotificationRuleName"]
    AddRemove["addRecipientHandler / removeRecipientHandler\nupdate recipientList state"]
    Props --> Init
    Init --> RecipientListState
    Init --> NameState
    RecipientListState --> UI
    NameState --> UI
    UI --> AddRemove
    AddRemove --> RecipientListState
    RecipientListState --> Validation
    NameState --> Validation
    Validation --> Props : setIsNextButtonDisabled
    RecipientListState --> HandleStep
    NameState --> HandleStep
    HandleStep --> Props : setDeliveryActions / setNotificationRuleName
    assignAndMapUnsubscribeIdToDeliveryActions["assignAndMapUnsubscribeIdToDeliveryActions"] --- HandleStep
```

> SVG rendering failed for this diagram.
