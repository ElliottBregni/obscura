# Diagram: web/portal/src/pages/administration/notification-management/NotificationManagement.page.container.js


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
classDiagram
class NotificationManagement {
}
class ConnectedNotificationManagement {
}
class mapStateToProps {
  +notificationRuleList
  +notificationRuleListLoading
  +pageIdx
  +pageSize
  +pageCount
  +isNextButtonDisabled
  +createOrUpdateNotificationRuleStatus
  +allUserInputData
  +editNotificationRuleApiStatus
}
class mapDispatchToProps {
  +fetchNotificationRuleList(pageIdx,pageSize)
  +setPagination(pageIdx,pageSize)
  +setIsNextButtonDisabled(isNextButtonDisabled)
  +deleteNotification(notificationId)
  +resetWizardState()
  +createOrUpdateNotificationRule(allUserInputData,isEditing)
  +fetchIndividualNotification(notificationId)
}
class NotificationManagementState {
  class selectors {
    +getNotificationRuleList(state)
    +getPageSize(state)
    +getPageIdx(state)
    +getTotalPages(state)
    +getNotificationRuleListLoading(state)
  }
  class actionCreators {
    +fetchNotificationRuleList(pageIdx,pageSize)
    +setPagination(pageIdx,pageSize)
    +deleteNotification(notificationId)
  }
}
class NotificationManagementWizardState {
  class selectors {
    +getIsNextButtonDisabled(state)
    +getCreateOrUpdateNotificationRuleStatus(state)
    +getAllUserInputData(state)
    +getEditNotificationRuleApiStatus(state)
  }
  class actionCreators {
    +setIsNextButtonDisabled(isNextButtonDisabled)
    +resetWizardState()
    +createOrUpdateNotificationRule(allUserInputData,isEditing)
    +fetchIndividualNotification(notificationId)
  }
}

mapStateToProps ..> NotificationManagementState.selectors : uses
mapStateToProps ..> NotificationManagementWizardState.selectors : uses
mapDispatchToProps ..> NotificationManagementState.actionCreators : uses
mapDispatchToProps ..> NotificationManagementWizardState.actionCreators : uses
ConnectedNotificationManagement o-- NotificationManagement : wraps
ConnectedNotificationManagement ..> mapStateToProps
ConnectedNotificationManagement ..> mapDispatchToProps
```

> SVG rendering failed for this diagram.
