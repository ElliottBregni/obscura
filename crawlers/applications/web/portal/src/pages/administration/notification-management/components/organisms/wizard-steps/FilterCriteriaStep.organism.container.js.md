# Diagram: web/portal/src/pages/administration/notification-management/components/organisms/wizard-steps/FilterCriteriaStep.organism.container.js


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
classDiagram
    class FilterCriteriaStep {
        +render()
    }
    class ConnectedFilterCriteriaStep {
    }
    class connect {
        +connect(mapStateToProps, mapDispatchToProps)
    }
    class mapStateToProps {
        +filterCriteria(state)
        +eventType(state)
    }
    class mapDispatchToProps {
        +setFilterCriteria(filters)
        +setIsNextButtonDisabled(flag)
    }
    class NotificationManagementWizardState {
        class selectors {
            +getFilterCriteria(state)
            +getEventType(state)
        }
        class actionCreators {
            +setFilterCriteria(filters)
            +setIsNextButtonDisabled(flag)
        }
    }
    class Dispatch {
        +dispatch(action)
    }

    connect --> ConnectedFilterCriteriaStep : returns
    ConnectedFilterCriteriaStep --> FilterCriteriaStep : wraps
    ConnectedFilterCriteriaStep --> mapStateToProps : uses
    ConnectedFilterCriteriaStep --> mapDispatchToProps : uses

    mapStateToProps --> NotificationManagementWizardState.selectors : calls
    mapDispatchToProps --> NotificationManagementWizardState.actionCreators : uses
    mapDispatchToProps --> Dispatch : calls
    Dispatch --> NotificationManagementWizardState.actionCreators : triggers
```

> SVG rendering failed for this diagram.
