# Diagram: web/portal/src/pages/administration/notification-management/components/organisms/wizard-steps/ConfirmationStep.organism.container.js


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
flowchart LR
  A[ConfirmationStep (React Component)] -->|connected via| B[connect(mapStateToProps, mapDispatchToProps)]
  B --> C[Default Export]
  subgraph StateModule[NotificationManagementWizardState]
    S1[getAllUserInputData(selector)]
    S2[getCreateOrUpdateNotificationRuleStatus(selector)]
    AC[createOrUpdateNotificationRule(actionCreator)]
  end
  B -->|reads selectors| S1
  B -->|reads selectors| S2
  B -->|calls action creator| AC
  AC -->|dispatched| D[dispatch]
  D -->|updates| Store[Redux Store]
  S1 -->|reads from| Store
  S2 -->|reads from| Store
```

> SVG rendering failed for this diagram.
