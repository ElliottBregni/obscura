# Diagram: web/portal/src/pages/administration/notification-management/tests/interactiveList.test.tsx


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
graph LR
  IL[InteractiveList]
  props[Props<br/>children, addButtonText, maxItems, addButtonHandler, removeButtonHandler]
  btnAdd[Add Item Button<br/>(data-testid: button-add-item)]
  btnRemove[Remove Item Button<br/>(data-testid: button-remove-item)]
  testAddRender["Test: render Add when items &lt; max"]
  testAddNotRender["Test: not render Add when items = max"]
  testAddClick["Test: add handler called on click"]
  testRemoveRender["Test: render Remove when items &gt; 1"]
  testRemoveClick["Test: remove handler called on click"]
  testRemoveNotRender["Test: not render Remove when items = 1"]

  props --> IL
  IL --> btnAdd
  IL --> btnRemove
  btnAdd -->|onClick| props_add[addButtonHandler]
  btnRemove -->|onClick| props_remove[removeButtonHandler]

  testAddRender -->|renders| btnAdd
  testAddNotRender -->|doesn't render| btnAdd
  testAddClick -->|fireEvent.click| btnAdd
  testAddClick -->|expects| props_add

  testRemoveRender -->|renders| btnRemove
  testRemoveClick -->|fireEvent.click| btnRemove
  testRemoveClick -->|expects| props_remove
  testRemoveNotRender -->|doesn't render| btnRemove
```

> SVG rendering failed for this diagram.
