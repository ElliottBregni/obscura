# Diagram: web/portal/src/pages/administration/notification-management/components/molecules/RecipientInput.molecule.js


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
flowchart LR
  A[RecipientInput Component] --> B[props: recipient, setRecipient]
  A --> C[useTranslation("notification-management")]
  A --> D[translateRecipientType(type)]
  D -->|type === "Email"| E[returns t("Email")]
  D -->|type === "SMS"| F[returns t("SMS")]
  A --> G[Select]
  G --> H[value: {value: recipient.type, label: translateRecipientType(recipient.type)}]
  G --> I[options: Email, SMS]
  G --> J[onChange -> setRecipient({...recipient, value: conditional, type: newValue.value})]
  A --> K{recipient.type === "Email?"}
  K -->|Yes| L[TextInput]
  L --> M[props: placeholder, value, onChange -> setRecipient({...recipient, value: newValue})]
  K -->|No| N[PhoneNumberInput]
  N --> O[props: defaultCountry, placeholder, value, onChange -> setRecipient({...recipient, value: newValue ?? ""})]
  B --> P[recipient: {type, value, unsubscribeId}]
  B --> Q[setRecipient: function]
  C --> R[t function for translations]
  style A fill:#f9f,stroke:#333,stroke-width:2px
  style G fill:#bbf
  style L fill:#bfb
  style N fill:#bfb
  style D fill:#ffd]
```

> SVG rendering failed for this diagram.
