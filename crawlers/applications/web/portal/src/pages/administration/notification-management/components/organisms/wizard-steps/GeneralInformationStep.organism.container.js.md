# Diagram: web/portal/src/pages/administration/notification-management/components/organisms/wizard-steps/GeneralInformationStep.organism.container.js


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
classDiagram
    class GeneralInformationStep {
        +userDefaultCommunication
        +deliveryActions
        +notificationRuleName
        +unsubscribeIdMap
        +setNotificationRuleName()
        +setIsNextButtonDisabled()
        +setDeliveryActions()
    }

    class ConnectWrapper {
        +mapStateToProps(state)
        +mapDispatchToProps(dispatch)
    }

    class NotificationManagementWizardState {
        class selectors {
            +getNotificationRuleName(state)
            +getDeliveryActions(state)
            +getUnsubscribeIdMap(state)
        }
        class actionCreators {
            +setNotificationRuleName(payload)
            +setIsNextButtonDisabled(payload)
            +setDeliveryActions(payload)
        }
    }

    class ProfileState {
        class selectors {
            +getUserPreferences(state)
        }
    }

    class ReduxState {
        +state
    }

    class Dispatch {
        +dispatch(action)
    }

    ReduxState --> NotificationManagementWizardState.selectors : uses
    ReduxState --> ProfileState.selectors : uses
    NotificationManagementWizardState --> NotificationManagementWizardState.selectors
    NotificationManagementWizardState --> NotificationManagementWizardState.actionCreators
    ProfileState --> ProfileState.selectors

    ConnectWrapper --> NotificationManagementWizardState.selectors : reads
    ConnectWrapper --> ProfileState.selectors : reads
    ConnectWrapper --> NotificationManagementWizardState.actionCreators : binds
    ConnectWrapper --> ReduxState : receives
    ConnectWrapper --> Dispatch : receives

    ConnectWrapper --> GeneralInformationStep : connects as child
    Dispatch --> NotificationManagementWizardState.actionCreators : invokes
    NotificationManagementWizardState.selectors --> ConnectWrapper : provides selector functions
    ProfileState.selectors --> ConnectWrapper : provides selector functions

    GeneralInformationStep ..> NotificationManagementWizardState.actionCreators : calls dispatch via props
    GeneralInformationStep ..> NotificationManagementWizardState.selectors : receives derived props
    GeneralInformationStep ..> ProfileState.selectors : receives userDefaultCommunication
```

> SVG rendering failed for this diagram.
