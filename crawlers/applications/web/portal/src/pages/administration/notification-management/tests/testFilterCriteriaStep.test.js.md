# Diagram: web/portal/src/pages/administration/notification-management/tests/testFilterCriteriaStep.test.js


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
flowchart TD
  Start([Start]) --> IsEmpty{Is input empty?}
  IsEmpty -- Yes --> ValidEmpty[Valid]
  IsEmpty -- No --> IsNumeric{Contains only digits?}
  IsNumeric -- No --> ContainsLetters{Contains letters or units?}
  ContainsLetters -- Yes --> InvalidLetters[Invalid: strings/units]
  ContainsLetters -- No --> InvalidOther[Invalid]
  IsNumeric -- Yes --> IsInteger{Is integer (no decimal point)?}
  IsInteger -- No --> InvalidDecimal[Invalid: decimal]
  IsInteger -- Yes --> IsNonNegative{Is non-negative?}
  IsNonNegative -- No --> InvalidNegative[Invalid: negative]
  IsNonNegative -- Yes --> IsPositiveNonZero{Is > 0?}
  IsPositiveNonZero -- No --> InvalidZero[Invalid: zero]
  IsPositiveNonZero -- Yes --> LengthCheck{Number of digits <= 4?}
  LengthCheck -- No --> InvalidTooLong[Invalid: more than 4 digits]
  LengthCheck -- Yes --> RangeCheck{Value between 1 and 9999 inclusive?}
  RangeCheck -- Yes --> ValidNumber[Valid]
  RangeCheck -- No --> InvalidRange[Invalid]
  ValidEmpty --> End([End])
  ValidNumber --> End
  InvalidLetters --> End
  InvalidOther --> End
  InvalidDecimal --> End
  InvalidNegative --> End
  InvalidZero --> End
  InvalidTooLong --> End
  InvalidRange --> End
```

> SVG rendering failed for this diagram.
