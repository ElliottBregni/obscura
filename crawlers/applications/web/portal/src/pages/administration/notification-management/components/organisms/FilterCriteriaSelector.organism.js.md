# Diagram: web/portal/src/pages/administration/notification-management/components/organisms/FilterCriteriaSelector.organism.js


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
flowchart LR
  Props[Props: availableFilters, filterCriterion, filterCriteriaSelectHandler, eventType] --> FCS[FilterCriteriaSelector]
  FCS --> SelectComp[Select (react-select)]
  SelectComp -->|onChange: find availableFilter by value| HandlerInvoke[(filterCriteriaSelectHandler(filter, null))]
  FCS --> Div2[Div2: options area (enabled if selectedFilterType)]
  Div2 --> Decision{selectedFilterType === FilterTypesEnum.MILES_OUT?}
  Decision -->|Yes| MilesForm[Form.Control]
  MilesForm -->|onChange -> filterCriteriaSelectHandler(null, [value])| HandlerInvoke2[(filterCriteriaSelectHandler(null, [value]))]
  Decision -->|No| AsyncSelect[SelectAsync]
  AsyncSelect -->|onChange -> filterCriteriaSelectHandler(null, values)| HandlerInvoke2
  AsyncSelect -->|loadOptions(query, loadedOptions)| LoadFn[getFilterOptionListLoadOptions(selectedFilterType, query, loadedOptions, eventType)]
  FCS --> Translations[useTranslation & useNotificationManagementTranslations]
  Translations -->|label mapping| SelectComp
  Translations -->|placeholders / messages| MilesForm
  Translations -->|placeholders / messages| AsyncSelect
```

> SVG rendering failed for this diagram.
