# Diagram: web/portal/src/pages/administration/notification-management/tests/testDeliveryActionMapping.test.tsx


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
flowchart LR
  Start([Start]) --> ForEach{For each deliveryAction}
  ForEach --> IsEmail{type == "Email"?}
  IsEmail -- yes --> EmailNotEmpty{value != ""?}
  EmailNotEmpty -- yes --> ValidEmail{valid email format?}
  ValidEmail -- yes --> MapEmail[map to {method: "email", target: value, unsubscribeId}]
  ValidEmail -- no --> SkipEmail[skip email]
  EmailNotEmpty -- no --> SkipEmptyEmail[skip empty email]
  IsEmail -- no --> IsSMS{type == "SMS"?}
  IsSMS -- yes --> SMSNotEmpty{value != ""?}
  SMSNotEmpty -- yes --> Normalize[normalize phone (remove spaces, leading +)] --> ValidPhone{valid phone length/format?}
  ValidPhone -- yes --> MapSMS[map to {method: "sms", target: normalizedValue, unsubscribeId}]
  ValidPhone -- no --> SkipSMS[skip invalid sms]
  SMSNotEmpty -- no --> SkipEmptySMS[skip empty sms]
  IsSMS -- no --> SkipUnknown[skip unknown type]
  MapEmail --> Collect[add to result array]
  MapSMS --> Collect
  SkipEmail --> Continue[continue]
  SkipSMS --> Continue
  SkipEmptyEmail --> Continue
  SkipEmptySMS --> Continue
  SkipUnknown --> Continue
  Collect --> Continue
  Continue --> EndDecision{more actions?}
  EndDecision -- yes --> ForEach
  EndDecision -- no --> End([Return filtered mapped actions])
```

> SVG rendering failed for this diagram.
