# Diagram: web/portal/src/pages/administration/redux/ShipmentReportArrivalDepartureState.js


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
flowchart TD
  Imports[Imports]
  subgraph Modules
    M_moment["moment"]
    M_lodash["lodash (_ )"]
    M_apiUrl["api-url (apiUrl)"]
    M_buildFetch["vendor/signal-utils/build-fetch-duck (buildFetchDuck)"]
  end
  Imports --> Modules

  CONSTS[Constants]
  STORE["STORE_MOUNT_POINT = \"adminTool\""]
  URL["SHIPMENT_EVENTS_URL = apiUrl(\"/shipping/post_bulk_shipment_updates\")"]
  CONSTS --> STORE
  CONSTS --> URL

  BuildDuck["reportArrivalDepartureDuck = buildFetchDuck(STORE_MOUNT_POINT, \"shipmentReportArrivalDeparture\")"]
  M_buildFetch --> BuildDuck
  STORE --> BuildDuck

  subgraph Actions
    RAD["reportArrivalDeparture(data, onSuccess)"]
    ClearRAD["clearReportArrivalDeparture()"]
  end

  BuildDuck --> RAD
  BuildDuck --> ClearRAD

  RAD --> Payload["payload { comments, event, eventDate, shipmentIds, stopCode }"]
  RAD --> DispatchRAD["returns (dispatch) => dispatch(reportArrivalDepartureDuck.fetch(...))"]
  Payload --> DispatchRAD
  URL --> DispatchRAD
  DispatchRAD --> FetchCall["reportArrivalDepartureDuck.fetch(SHIPMENT_EVENTS_URL, {method: POST, data: payload}, _.noop, onSuccess)"]
  M_lodash --> FetchCall
  M_moment --> Payload

  ClearRAD --> DispatchClear["returns (dispatch) => dispatch(reportArrivalDepartureDuck.clear())"]
  DispatchClear --> ClearCall["reportArrivalDepartureDuck.clear()"]

  ExportObj["export default { mountPoint, actionCreators, selectors, reducer }"]
  ExportObj -->|mountPoint| STORE
  ExportObj -->|actionCreators| RAD
  ExportObj -->|actionCreators| ClearRAD
  ExportObj -->|selectors|getShipmentReportArrivalDepartureData = reportArrivalDepartureDuck.selectors.getData
  ExportObj -->|reducer| reportArrivalDepartureDuck.reducer
  BuildDuck --> ExportObj
```

> SVG rendering failed for this diagram.
