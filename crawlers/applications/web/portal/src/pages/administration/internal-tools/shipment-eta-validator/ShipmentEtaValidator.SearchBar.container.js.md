# Diagram: web/portal/src/pages/administration/internal-tools/shipment-eta-validator/ShipmentEtaValidator.SearchBar.container.js


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
flowchart LR
  SearchBarComponent[SearchBar Component] -->|connected via connect| Connect[connect(mapStateToProps, mapDispatchToProps, mergeProps)]
  subgraph StateToProps["mapStateToProps"]
    direction LR
    GetAuth[getAuthorization(state)] -->|auth| StateProps[props.auth]
    GetSolution[getSolutionId(state)] -->|solutionId| StateProps
    SBSelectors[(SearchBarState.selectors)]
    SBSelectors --> GetSearchText[getSearchText(state)] -->|searchText| StateProps
    SBSelectors --> GetSearchCategory[getSearchCategory(state)] -->|searchCategory| StateProps
    SBSelectors --> GetHasChanged[getHasSearchCriteriaChanged(state)] -->|hasSearchCriteriaChanged| StateProps
    SBSelectors --> GetPrereq[getAreAllPrerequisiteFiltersSelected(state)] -->|areAllPrerequisiteFiltersSelected| StateProps
  end
  subgraph DispatchToProps["mapDispatchToProps"]
    direction LR
    SBActions[(SearchBarState.actionCreators)]
    SBActions --> AC_setCategory[setSearchCategoryForKey] -->|setSearchCategory(key) -> dispatch| DispatchProps[dispatch props]
    SBActions --> AC_setText[setSearchText] -->|setSearchText(text,ignore) -> dispatch| DispatchProps
    SBActions --> AC_clearSaved[clearSavedSearch] -->|clearSavedSearch() -> dispatch| DispatchProps
    SBActions --> AC_clearText[clearSearchText] -->|clearSearchText() -> dispatch| DispatchProps
    SBActions --> AC_reset[resetSearchBar] -->|resetSearchBar() -> dispatch| DispatchProps
    SBActions --> AC_searchEntities[searchEntities] -->|searchEntities(solId,...) -> dispatch| DispatchProps
    SBActions --> AC_searchVins[searchVins] -->|searchVins(query) -> dispatch| DispatchProps
  end
  Connect --> StateToProps
  Connect --> DispatchToProps
  Connect --> Merge[mergeProps(stateProps, dispatchProps, ownProps)]
  Merge -->|returns combined props with isShowingAdvancedSearchButton:false| SearchBarComponent
  OwnProps[ownProps] --> Merge
  StateProps --> Merge
  DispatchProps --> Merge
```

> SVG rendering failed for this diagram.
