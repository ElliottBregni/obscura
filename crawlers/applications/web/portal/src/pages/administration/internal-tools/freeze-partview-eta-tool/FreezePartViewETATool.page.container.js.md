# Diagram: web/portal/src/pages/administration/internal-tools/freeze-partview-eta-tool/FreezePartViewETATool.page.container.js


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
flowchart LR
  FPVETAState[FreezePartViewETAState]
  mapStateToProps[mapStateToProps(state)]
  getRequestData[selectors.getRequestData(state) -> requestData]
  FreezePartViewETAComp[FreezePartViewETA]
  freezePartViewEtas[actionCreators.freezePartViewEtas()]
  requestDataClear[actionCreators.requestDataClear()]
  connectFn[connect(mapStateToProps, {freezePartViewEtas, requestDataClear})]

  FPVETAState -->|provides selector| getRequestData
  mapStateToProps -->|calls| getRequestData
  getRequestData -->|maps to prop requestData| FreezePartViewETAComp
  FPVETAState -->|provides actionCreator| freezePartViewEtas
  FPVETAState -->|provides actionCreator| requestDataClear
  freezePartViewEtas -->|passed as prop| FreezePartViewETAComp
  requestDataClear -->|passed as prop| FreezePartViewETAComp
  connectFn --> FreezePartViewETAComp
```

> SVG rendering failed for this diagram.
