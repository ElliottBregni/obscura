# Diagram: web/portal/src/pages/administration/internal-tools/dpu-admin-tool/components/DPU.VinHistory.columns.js


> Auto-generated by Obscura crawlers

## Diagram 1

```mermaid
flowchart LR
  subgraph Hook
    UC[useColumns()] 
  end
  UC -->|calls| T[useTranslation("driveaway-search")]
  UC -->|returns array of| COLS[Columns Array]
  COLS --> C1[Column: Event time]
  COLS --> C2[Column: Status]
  COLS --> C3[Column: DPU Location]
  COLS --> C4[Column: Last Milestone]
  C1 -->|Cell ->| DateTimeCell[DateTimeCell component]
  C1 -->|accessor ->| eventTs[eventTs]
  C2 -->|Cell ->| StatusHistoryStatusCell[StatusHistoryStatusCell component]
  C2 -->|accessor ->| statusObj[ { status, denyReasonCode } ]
  C3 -->|Cell ->| DpuLocationCell[DpuLocationCell component]
  C3 -->|accessor ->| dpuLocation[dpuLocation]
  C4 -->|Cell ->| LastMileStoneCell[LastMileStoneCell component]
  C4 -->|accessor ->| milestone[milestone]
```

> SVG rendering failed for this diagram.

## Diagram 2

```mermaid
classDiagram
  class useColumns {
    <<hook>>
    +Array columns
    +calls useTranslation(namespace)
  }
  class useTranslation {
    +t(key)
  }
  class DateTimeCell {
    +localize
    +dateTime
  }
  class DpuLocationCell {
    +render(dpuLocation)
  }
  class LastMileStoneCell {
    +render(milestone)
  }
  class StatusHistoryStatusCell {
    +render({status, denyReasonCode})
  }
  useColumns --> useTranslation : uses
  useColumns --> DateTimeCell : returns cell
  useColumns --> DpuLocationCell : returns cell
  useColumns --> LastMileStoneCell : returns cell
  useColumns --> StatusHistoryStatusCell : returns cell
```

> SVG rendering failed for this diagram.
