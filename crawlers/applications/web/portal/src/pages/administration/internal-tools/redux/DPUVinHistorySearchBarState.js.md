# Diagram: web/portal/src/pages/administration/internal-tools/redux/DPUVinHistorySearchBarState.js


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
flowchart LR
  subgraph Imports
    A(getBasicQueryString\nfrom search-filter-query-strings)
    B(apiUrl)
    C(chainReducers)
    D(buildSearchBarState)
    E(searchVins, vinsReducer\nfrom VinSearchStateExtensions)
    F(qs)
  end

  A -->|used by| QB[SEARCH_CATEGORIES.queryBuilder]
  F -->|parse/stringify| Params[queryString params manipulation]
  B -->|builds URL| URL[/api URL construction/]
  Params --> URL
  URL -->|passed to| DispatchCall[dispatch(duck.fetch(url))]
  DispatchCall -->|performed in| FetchVinHistory[fetchVinHistory(queryString, solutionId, duck, dispatch, state)]

  QB --> SEARCH_CATEGORIES[SEARCH_CATEGORIES config]
  SEARCH_CATEGORIES -->|provided to| BuildState[buildSearchBarState(...)]
  D --> BuildState
  BuildState --> SearchBarStateObj[SearchBarState (object)]

  subgraph ActionsSelectorsReducers
    SV(searchVins action)
    CV(clearVinHistoryEntities actionCreator)
    Sel1(getVins selector)
    Sel2(getVinsLoading selector)
    Sel3(getVinHistorySearchResults selector)
    R(driveAwayVinHistorySearchBarReducer)
    VR(vinsReducer)
    CR(chainReducers)
  end

  E --> SV
  CV -->|dispatches| CLEAR[CLEAR_VIN_HISTORY_ENTITIES action]
  SV --> SearchBarStateObj
  CV --> SearchBarStateObj
  Sel1 --> SearchBarStateObj
  Sel2 --> SearchBarStateObj
  Sel3 --> SearchBarStateObj
  R --> SearchBarStateObj
  VR -->|combined with| SearchBarStateObj
  CR -->|combines| ChainResult[SearchBarState.reducer = chainReducers([..., vinsReducer])]
  ChainResult --> Export[export default SearchBarState]

  SearchBarStateObj --> ChainResult
  FetchVinHistory --> SearchBarStateObj
  CLEAR --> R

  classDef impl fill:#f9f,stroke:#333,stroke-width:1px
  class FetchVinHistory,clearVinHistoryEntities,SearchBarStateObj,R,VR impl
```

> SVG rendering failed for this diagram.
