# Diagram: web/portal/src/pages/administration/internal-tools/shipment-eta-validator/ShipmentEtaValidator.SearchFilters.container.js


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
classDiagram
    class FilterSection {
        <<React Component>>
        +props
    }
    class ConnectedFilterSection {
        <<Connected Component>>
        +props (ownProps, stateProps, dispatchProps, filtersMetadata)
    }
    class connect {
        +mapStateToProps
        +actionCreators
        +mergeProps
    }
    class mapStateToProps {
        +auth
        +solutionId
        +searchFilters
        +hasSearchCriteriaChanged
        +areAllPrerequisiteFiltersSelected
    }
    class mergeProps {
        +combine ownProps
        +combine stateProps
        +combine dispatchProps
        +filtersMetadata = FILTERS
    }
    class actionCreators {
        +setSearchFilter()
        +clearSearchFilter()
        +searchEntities()
        +resetSavedSearch()
        +clearSavedSearch()
    }
    class ShipmentViewSearchBarState {
        class selectors {
            +getSearchFilters(state)
            +getHasSearchCriteriaChanged(state)
            +getAreAllPrerequisiteFiltersSelected(state)
        }
        class actionCreators {
            +setSearchFilter()
            +clearSearchFilter()
            +searchEntities()
            +resetSavedSearch()
            +clearSavedSearch()
        }
    }
    class FILTERS {
        <<const>>
    }
    class OrganizationsState {
        +getSolutionId(state)
    }
    class AuthorizationSelectors {
        +getAuthorization(state)
    }

    connect --> FilterSection : wraps
    connect --> ConnectedFilterSection : returns
    ConnectedFilterSection ..> FilterSection : renders
    ConnectedFilterSection --> mapStateToProps : uses
    ConnectedFilterSection --> actionCreators : binds
    ConnectedFilterSection --> mergeProps : applies
    mapStateToProps ..> AuthorizationSelectors : calls getAuthorization(state)
    mapStateToProps ..> OrganizationsState : calls getSolutionId(state)
    mapStateToProps ..> ShipmentViewSearchBarState.selectors : reads
    actionCreators <-- ShipmentViewSearchBarState.actionCreators : sourced from
    mergeProps ..> FILTERS : sets filtersMetadata
    ShipmentViewSearchBarState.selectors ..> ShipmentViewSearchBarState : member of
    ShipmentViewSearchBarState.actionCreators ..> ShipmentViewSearchBarState : member of
```

> SVG rendering failed for this diagram.
