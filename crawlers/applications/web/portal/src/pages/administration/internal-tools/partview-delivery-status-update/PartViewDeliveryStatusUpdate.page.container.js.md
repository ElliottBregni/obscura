# Diagram: web/portal/src/pages/administration/internal-tools/partview-delivery-status-update/PartViewDeliveryStatusUpdate.page.container.js


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
flowchart LR
  subgraph ReduxStateModule["PartViewDeliveryStatusUpdateState (module)"]
    PVSelectors["selectors.getRequestData(state)"]
    PVActions["actionCreators.fetchPVDeliveryStatusUpdate(formData,fileName)"]
    PVClear["actionCreators.requestDataClear"]
  end

  subgraph ComponentFile["PartViewDeliveryStatusUpdate.page"]
    Component["PartViewDeliveryStatusUpdate (React Component)"]
  end

  mapState["mapStateToProps(state)"]
  connectFunc["connect(mapStateToProps, actionCreators)"]

  subgraph ActionCreatorsObj["actionCreators (object)"]
    fetchAction["fetchPVDeliveryStatusUpdate(formData,fileName) -> (dispatch)"]
    clearAction["requestDataClear"]
  end

  State["Redux store (state)"]

  State -->|reads| PVSelectors
  PVSelectors -->|returns requestData| mapState
  mapState -->|provides props { requestData }| connectFunc
  connectFunc -->|wraps| Component

  Component -->|calls fetchPVDeliveryStatusUpdate| fetchAction
  Component -->|calls requestDataClear| clearAction
  fetchAction -->|dispatches| PVActions
  clearAction -->|references| PVClear
  PVActions -->|updates| State
  PVClear -->|updates| State
```

> SVG rendering failed for this diagram.
