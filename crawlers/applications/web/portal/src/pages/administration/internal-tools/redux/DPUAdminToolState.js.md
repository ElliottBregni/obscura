# Diagram: web/portal/src/pages/administration/internal-tools/redux/DPUAdminToolState.js


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
flowchart LR
  subgraph Modules
    A[apiUrl] -->|uses| URLfunc[getDPUAdminToolUrl(solutionId)]
    B[axios] -->|post| AxiosPost
    C[buildFetchDuck] --> duck[duck]
    D[chainReducers] --> chain[chainReducers([...])]
  end

  URLfunc --> SubmitAction[submitAction(solutionId,payload)]
  URLfunc --> SubmitBulk[submitBulkAction(solutionId,payload)]
  SubmitAction -->|dispatch SUMBIT_ACTION| DispatchStart1((SUMBIT_ACTION))
  SubmitBulk -->|dispatch SUMBIT_ACTION| DispatchStart2((SUMBIT_ACTION))

  DispatchStart1 --> CallAxios1[axios.post(url,payload, {params:{disregardCurrentStatus:true}})]
  DispatchStart2 --> CallAxios2[axios.post(url,payload, config)]
  CallAxios1 --> Response1{response}
  CallAxios2 --> Response2{response}

  Response1 -->|no response.data.status && status != 204| Throw1[/throw Error("Missing data")/]
  Response2 -->|no response.data.status && status != 204| Throw2[/throw Error("Missing data")/]

  Response1 -->|status === "FAILED"| CheckVins1{vins_and_reasons?}
  Response2 -->|status === "FAILED"| CheckVins2{vins_and_reasons?}
  CheckVins1 -->|missing| Throw1
  CheckVins2 -->|missing| Throw2
  CheckVins1 -->|present| DispatchNotAll1((SUBMIT_ACTION_NOT_ALL_SUCCESS))
  CheckVins2 -->|present| DispatchNotAll2((SUBMIT_ACTION_NOT_ALL_SUCCESS))

  Response1 -->|status !== "FAILED"| DispatchSucc1((SUMBIT_ACTION_SUCCEEDED))
  Response2 -->|status !== "FAILED"| DispatchSucc2((SUMBIT_ACTION_SUCCEEDED))

  CallAxios1 -.catch.-> Catch1[/console.error & setTimeout dispatch SUMBIT_ACTION_FAILED/]
  CallAxios2 -.catch.-> Catch2[/console.error & setTimeout dispatch SUMBIT_ACTION_FAILED/]
  Catch1 --> DispatchFail1((SUMBIT_ACTION_FAILED))
  Catch2 --> DispatchFail2((SUMBIT_ACTION_FAILED))

  DispatchNotAll1 -->|payload vins_and_reasons| StoreResults((SUBMIT_ACTION_NOT_ALL_SUCCESS))
  DispatchNotAll2 -->|payload vins_and_reasons| StoreResults

  subgraph Reducer[DPUAdminToolReducer]
    RS0[initialState {actionStatus:null, requestResults:null}]
    RS0 -->|CLEAR_ACTION_STATUS| RS1[actionStatus:null, requestResults:null]
    RS0 -->|SUMBIT_ACTION| RS2[actionStatus:"IN_PROGRESS"]
    RS0 -->|SUMBIT_ACTION_SUCCEEDED| RS3[actionStatus:"SUCCESS"]
    RS0 -->|SUMBIT_ACTION_FAILED| RS4[actionStatus:"FAILED"]
    RS0 -->|SUBMIT_ACTION_NOT_ALL_SUCCESS| RS5[actionStatus:"NOT_ALL_SUCCESS", requestResults:payload]
  end

  DispatchStart1 --> Reducer
  DispatchStart2 --> Reducer
  DispatchSucc1 --> Reducer
  DispatchSucc2 --> Reducer
  DispatchFail1 --> Reducer
  DispatchFail2 --> Reducer
  DispatchNotAll1 --> Reducer
  DispatchNotAll2 --> Reducer

  subgraph Export
    DPUAdminToolState[default export DPUAdminToolState]
    DPUAdminToolState -->|mountPoint| STORE_MOUNT_POINT["DPUAdminTools"]
    DPUAdminToolState -->|actionCreators| AC[getActionStatus, clearActionStatus, submitAction, submitBulkAction]
    DPUAdminToolState -->|selectors| Sel[getActionStatus]
    DPUAdminToolState -->|reducer| CombinedReducer[chainReducers([DPUAdminToolReducer, duck.reducer])]
  end

  duck --> CombinedReducer
  DPUAdminToolState --> Reducer
```

> SVG rendering failed for this diagram.
