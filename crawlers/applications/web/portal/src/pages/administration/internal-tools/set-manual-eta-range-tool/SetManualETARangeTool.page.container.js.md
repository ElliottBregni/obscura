# Diagram: web/portal/src/pages/administration/internal-tools/set-manual-eta-range-tool/SetManualETARangeTool.page.container.js


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
classDiagram
    class SetManualETARangeTool {
        +render()
    }
    class SetManualETARangeToolState {
        <<module>>
        +selectors
        +actionCreators
    }
    class OrganizationsState {
        <<module>>
        +getSolutionId(state)
    }
    class mapStateToProps {
        +actionStatus(state)
        +requestResults(state)
        +solutionId(state)
    }
    class mapDispatchToProps {
        +clearActionStatus()
        +submitBulkAction(solutionId, payload)
    }
    class connect {
        +connect(mapStateToProps, mapDispatchToProps)(Component)
    }

    SetManualETARangeToolState --|> mapStateToProps : provides selectors
    OrganizationsState --|> mapStateToProps : provides getSolutionId
    mapStateToProps --> SetManualETARangeTool : supplies props (actionStatus, requestResults, solutionId)

    SetManualETARangeToolState --|> mapDispatchToProps : provides actionCreators
    mapDispatchToProps --> SetManualETARangeTool : supplies dispatch props (clearActionStatus, submitBulkAction)

    mapStateToProps --> connect : arg
    mapDispatchToProps --> connect : arg
    SetManualETARangeTool --> connect : wrapped by
    connect --> "ExportedComponent" : returns connected component
```

> SVG rendering failed for this diagram.
