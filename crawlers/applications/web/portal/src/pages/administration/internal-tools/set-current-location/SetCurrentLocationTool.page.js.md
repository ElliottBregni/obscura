# Diagram: web/portal/src/pages/administration/internal-tools/set-current-location/SetCurrentLocationTool.page.js


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
classDiagram
class SetCurrentLocationOverridePage {
  - solutionId
  - initialState
  + vin
  + locationCode
  + importFileCsvData
  + importFile
  + showResultModal
  + isBulkSubmitEnabled()
  + isSubmitEnabled()
  + onFileLoad(file)
  + onBulkSubmitHandler()
  + onSingleSubmitHandler()
}
class UploadAllSuccessModalBody {
  + render()
}
class UploadErrorModalBody {
  + render()
}
class UploadNotAllSuccessModalBody {
  - columns
  - requestResults
  + render()
}
class UploadInProgressModalBody {
  + render()
}
class DialogModal {
  + show
  + onHide()
  + isLoading
  + title
}
class DownloadCsvLink {
  - data
  - headers
  - filename
  + render()
}
class BaseTable {
  - theme
  - columns
  - data
  + render()
}
class Hooks {
  + useOverrideLocationMutation()
  + useNotAllSuccessfulColumns()
  + useSetTitleOnMount()
  + useTranslation()
}
SetCurrentLocationOverridePage ..> Hooks : uses
SetCurrentLocationOverridePage o-- DialogModal : composes
SetCurrentLocationOverridePage o-- DownloadCsvLink : composes
SetCurrentLocationOverridePage o-- BaseTable : composes
DialogModal <|-- UploadAllSuccessModalBody
DialogModal <|-- UploadErrorModalBody
DialogModal <|-- UploadNotAllSuccessModalBody
DialogModal <|-- UploadInProgressModalBody
UploadNotAllSuccessModalBody o-- BaseTable : displays
SetCurrentLocationOverridePage --> UploadAllSuccessModalBody : may render
SetCurrentLocationOverridePage --> UploadErrorModalBody : may render
SetCurrentLocationOverridePage --> UploadNotAllSuccessModalBody : may render
SetCurrentLocationOverridePage --> UploadInProgressModalBody : may render
SetCurrentLocationOverridePage ..> DownloadCsvLink : provides csvTemplate
SetCurrentLocationOverridePage ..> FileReader : reads csv file
Note forSetCurrentLocationOverridePage "Handles bulk and single VIN submission, form state, validation (VIN regex), and mutation calls"
```

> SVG rendering failed for this diagram.
