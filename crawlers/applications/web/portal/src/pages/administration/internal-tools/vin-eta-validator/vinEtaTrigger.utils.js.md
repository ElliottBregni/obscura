# Diagram: web/portal/src/pages/administration/internal-tools/vin-eta-validator/vinEtaTrigger.utils.js


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
flowchart LR
  subgraph ConvertEtaTrigger
    CT[convertEtaTriggerToTableRows(etaTrigger, t)]
    Rows[rows array]
    CT --> Rows
    CT --> D(displayNAIfNull)
    CT --> G[getDayOfWeekFromIndex]
    CT --> A[getAverageName]
    CT --> CarrierObj[carrier { name, scac }]
    CT --> NextPlannedLocation[type: location]
  end

  D -->|value !== null → value| NA_OK[value or t("N/A")]
  A -->|if nil → t("N/A")| Avg_Default[t("N/A")]
  A -->|GT3 → "Greater than 3"| Avg_GT3
  A -->|LT3 → "Less than 3"| Avg_LT3

  subgraph FormatResult
    FR[formatResult(result, entity)]
    FR -->|result.error == true| FR_Error[Return object with all fields null]
    FR -->|result.error == false| FR_OK[Return mapped object from result fields]
  end

  style CT fill:#f9f,stroke:#333,stroke-width:1px
  style FR fill:#bbf,stroke:#333,stroke-width:1px
  Rows -->|contains rows with name/value/type| Table[Search Results Table]
```

> SVG rendering failed for this diagram.
