# Diagram: web/portal/src/pages/administration/internal-tools/vin-eta-validator/VinEtaValidator.SearchFilters.container.js


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
flowchart LR
  Connect[connect] --> FilterSection[FilterSection Component]
  Connect --> mapStateToProps[mapStateToProps]
  Connect --> actionCreatorsObj[actionCreators]
  Connect --> mergeProps[mergeProps]

  mapStateToProps --> getAuthorization[getAuthorization(state)]
  mapStateToProps --> getSolutionId[getSolutionId(state)]
  mapStateToProps --> VinViewSelectors[VinViewSearchBarState.selectors]
  VinViewSelectors --> getSearchFilters[getSearchFilters(state)]
  VinViewSelectors --> getHasSearchCriteriaChanged[getHasSearchCriteriaChanged(state)]
  VinViewSelectors --> getAreAllPrerequisiteFiltersSelected[getAreAllPrerequisiteFiltersSelected(state)]

  actionCreatorsObj --> setSearchFilter[setSearchFilter]
  actionCreatorsObj --> clearSearchFilter[clearSearchFilter]
  actionCreatorsObj --> searchEntities[searchEntities]
  actionCreatorsObj --> resetSavedSearch[resetSavedSearch]
  actionCreatorsObj --> clearSavedSearch[clearSavedSearch]

  mergeProps --> FILTERS[FILTERS constant]
  mergeProps --> ownProps[ownProps]
  mergeProps --> stateProps[stateProps]
  mergeProps --> dispatchProps[dispatchProps]

  FilterSection --> UI[Rendered UI]
  FILTERS --> FiltersMetadata[filtersMetadata]
  FiltersMetadata --> FilterSection
```

> SVG rendering failed for this diagram.
