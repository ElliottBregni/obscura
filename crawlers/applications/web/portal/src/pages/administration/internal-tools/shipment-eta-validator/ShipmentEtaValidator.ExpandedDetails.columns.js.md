# Diagram: web/portal/src/pages/administration/internal-tools/shipment-eta-validator/ShipmentEtaValidator.ExpandedDetails.columns.js


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
classDiagram
    class ValueCell {
        +props
        +render()
    }
    class ValuePropShape {
        +value: any
    }
    class IconComponent {
        +src
        +color
        +css
    }
    class TextComponent {
        +children
    }
    class Colors {
        +tables.tableHeadingText
    }
    class useExpandedDetailColumns {
        +t()
        +returns columns[]
    }
    class useTranslation {
        +useTranslation(namespace)
    }

    ValueCell o-- ValuePropShape : accepts
    ValueCell --> IconComponent : renders when value falsy
    ValueCell --> TextComponent : renders when value truthy
    IconComponent ..> Colors : uses color
    useExpandedDetailColumns --> useTranslation : calls
    useExpandedDetailColumns --> ValueCell : references as Cell
    useExpandedDetailColumns ..> "Array of Column objects" : returns
    class "Array of Column objects" {
        +Header
        +accessor
        +Cell
    }
```

> SVG rendering failed for this diagram.
