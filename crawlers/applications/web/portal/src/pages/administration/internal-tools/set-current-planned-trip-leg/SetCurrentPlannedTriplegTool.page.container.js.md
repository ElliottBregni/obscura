# Diagram: web/portal/src/pages/administration/internal-tools/set-current-planned-trip-leg/SetCurrentPlannedTriplegTool.page.container.js


> Auto-generated by Obscura crawlers

## Diagram 1

```mermaid
classDiagram
    class State {
    }
    class getSolutionId {
        <<function>>
        +getSolutionId(state)
    }
    class mapStateToProps {
        <<function>>
        +mapStateToProps(state)
        +returns { solutionId }
    }
    class SetCurrentPlannedTripLegPage {
        <<Component>>
        +props.solutionId
    }
    class connect {
        <<HOC>>
        +connect(mapStateToProps, null)
    }
    getSolutionId --> State : reads
    mapStateToProps --> getSolutionId : calls
    connect --> mapStateToProps : uses
    connect --> SetCurrentPlannedTripLegPage : wraps
    SetCurrentPlannedTripLegPage ..> mapStateToProps : receives solutionId
```

> SVG rendering failed for this diagram.

## Diagram 2

```mermaid
flowchart TD
    State["Redux State"] -->|state| GS[getSolutionId()]
    GS -->|solutionId| MS[mapStateToProps(state)]
    MS -->|props| CN[connect(mapStateToProps,null)]
    CN -->|connected component| SCP[SetCurrentPlannedTripLegPage]
    SCP -->|uses prop| PropSolution[solutionId]
```

> SVG rendering failed for this diagram.
