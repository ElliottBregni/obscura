# Diagram: web/portal/src/pages/administration/internal-tools/partview-delivery-status-update/PartViewDeliveryStatusUpdate.page.js


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
classDiagram
    class PartViewDeliveryStatusUpdate {
        +props: fetchPVDeliveryStatusUpdate
        +props: requestData
        +props: requestDataClear()
        -state: packagesDelivered { importCsv: {} }
        -state: importFile: string
        -const: title
        -const: csvTooltip
        +useEffect_cleanup(): void
        +convertJsonToCsv(jsonData): string
        +onFileLoad(event): void
        +alertMessage (useMemo)
        +render(): JSX
    }

    class csvTemplate {
        +fileName: string
        +headers: Array
        +data: Array
    }

    class XLSX {
        +read(data, opts)
        +utils
    }

    class FileReader {
        +readAsText(file)
        +onload(event)
    }

    class Hooks {
        +useTranslation()
        +useSetTitleOnMount()
        +useState()
        +useEffect()
        +useMemo()
    }

    class UIComponents {
        +PageHeader(title)
        +DownloadCsvLink(data, headers, label, filename)
        +Tooltip(placement, tooltipChildren)
        +Icon(src)
        +Button(variant, disabled, onClick)
        +Alert(variant, show)
        +FormControl
        +FormGroup
        +InputGroup
        +Text
    }

    PartViewDeliveryStatusUpdate --> Hooks : uses
    PartViewDeliveryStatusUpdate --> UIComponents : composes
    PartViewDeliveryStatusUpdate --> csvTemplate : reads
    PartViewDeliveryStatusUpdate --> XLSX : uses
    PartViewDeliveryStatusUpdate --> FileReader : instantiates
    PartViewDeliveryStatusUpdate --> fetchPVDeliveryStatusUpdate : calls
    PartViewDeliveryStatusUpdate --> requestDataClear : calls
    PartViewDeliveryStatusUpdate ..> Alert : produces alertMessage
    FileReader ..> PartViewDeliveryStatusUpdate : onload -> onFileLoad
    XLSX ..> PartViewDeliveryStatusUpdate : sheet_to_json -> convertJsonToCsv
    csvTemplate o-- "1" PartViewDeliveryStatusUpdate : download template
    UIComponents --> Icon : uses
    UIComponents --> Tooltip : uses
    UIComponents --> DownloadCsvLink : offers template download
```

> SVG rendering failed for this diagram.
