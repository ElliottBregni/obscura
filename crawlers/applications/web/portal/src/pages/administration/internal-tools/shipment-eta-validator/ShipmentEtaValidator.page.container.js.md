# Diagram: web/portal/src/pages/administration/internal-tools/shipment-eta-validator/ShipmentEtaValidator.page.container.js


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
classDiagram
class ShipmentEtaValidatorPage
class mapStateToProps {
  +solutionId
  +hasSearchCriteriaChanged
  +areAllPrerequisiteFiltersSelected
  +totalCount
  +selectedVin
  +data
  +isLoading
  +isExporting
  +exportIdentifier
  +exportName
  +exportFailed
}
class ShipmentEtaValidatorSearchBarState <<module>>
class OrganizationsState {
  +getSolutionId()
}
class selectors {
  +getHasSearchCriteriaChanged()
  +getAreAllPrerequisiteFiltersSelected()
  +getTotalEntities()
  +getSearchResults()
  +getIsLoading()
  +getIsExporting()
  +getExportIdentifier()
  +getExportName()
  +getExportFailed()
  +getSelectedEntity()
}
class actionCreators {
  +searchEntities()
  +resetSearchAndFilters()
  +exportEntities()
  +resetExport()
}
class connect {
  +connect(mapStateToProps, actionCreators)
}

ShipmentEtaValidatorSearchBarState o-- selectors
ShipmentEtaValidatorSearchBarState o-- actionCreators
mapStateToProps --> selectors : uses
mapStateToProps --> OrganizationsState : uses getSolutionId()
connect --> mapStateToProps
connect --> actionCreators
connect --> ShipmentEtaValidatorPage : wraps
```

> SVG rendering failed for this diagram.
