# Diagram: web/portal/src/pages/administration/internal-tools/redux/FreezePartViewETAToolState.js


> Auto-generated by Obscura crawlers

## Diagram 1

```mermaid
flowchart TD
  A[freezePartViewEtas(payload)] --> B[getSolutionId(getState())]
  B --> C[getFreezePartViewEtasUrl(solutionId)]
  C --> D[build request config]
  D --> E[dispatch(duck.fetch(url, config))]
  E --> F[duck.fetch -> network POST /eta-shipments/override/partview/solution/:id]
  subgraph Duck
    E2[duck.fetch] --> G[duck.reducer]
    E2 --> H[duck.selectors.getData]
    I[duck.clear()] --> J[dispatch(duck.clear())]
  end
  F --> G
  click G href "javascript:void(0)" "duck.reducer"
```

> SVG rendering failed for this diagram.

## Diagram 2

```mermaid
classDiagram
  class FreezePartViewETAState {
    +mountPoint: "freezePartViewEtas"
    +reducer: freezePartViewEtasReducer
    +actionCreators: { freezePartViewEtas, requestDataClear }
    +selectors: { getRequestData }
  }
  class duck {
    +fetch(url, config)
    +clear()
    +reducer
    +selectors
  }
  FreezePartViewETAState --> duck : uses
  FreezePartViewETAState "1" o-- "1" freezePartViewEtasReducer : reducer
  FreezePartViewETAState "1" o-- "1" requestDataClear : actionCreators
  FreezePartViewETAState "1" o-- "1" getRequestData : selectors
```

> SVG rendering failed for this diagram.
