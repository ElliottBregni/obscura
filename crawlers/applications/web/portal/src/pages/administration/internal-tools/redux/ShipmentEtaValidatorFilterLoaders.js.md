# Diagram: web/portal/src/pages/administration/internal-tools/redux/ShipmentEtaValidatorFilterLoaders.js


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
flowchart LR
  API[apiUrl (import)]
  Builder[buildAsyncFilterOptionsState (import)]
  Config[config object]
  Shipment[shipmentEtaValidatorVinOptionsState]

  API -->|used by|getUrlFunc[getUrl(solutionId) -> apiUrl("/entity/solution/${solutionId}/entity")]
  getUrlFunc --> Config
  Config --> topic["topic: \"shipmentEtaValidatorVinFilter\""]
  Config --> queryParam["queryParam: \"entityId\""]
  Config --> pageSize["pageSize: 20"]
  Config --> getResponseData["getResponseData(data) -> data?.data ?? []"]
  Config --> transformResult["transformResult(entity)"]
  transformResult --> resultObj["{ label: entity.id, value: entity.id, entity }"]

  Builder -->|called with| Config
  Builder -->|returns| Shipment
  Shipment -->|holds| Config
```

> SVG rendering failed for this diagram.
