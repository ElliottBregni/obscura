# Diagram: web/portal/src/pages/administration/internal-tools/react-queries/OverrideCurrentLocation.ts


> Auto-generated by Obscura crawlers

## Diagram 1

```mermaid
classDiagram
    class useOverrideLocationMutation {
        <<hook>>
        +fetch({solutionId, payload, isBulkUpload})
    }
    class createMutationRequestHook {
        <<factory>>
    }
    class axios {
        +post(url, payload, config)
    }
    class apiUrl {
        +(path): string
    }
    class moment {
        +tz.guess(): string
    }
    class vinUpdateToolResponseSchema
    class currentLocationOverridePayloadSchema
    class BaseErrorResponse

    useOverrideLocationMutation --> createMutationRequestHook : created by
    useOverrideLocationMutation --> axios : calls
    useOverrideLocationMutation --> apiUrl : builds URL
    useOverrideLocationMutation --> moment : reads timezone
    useOverrideLocationMutation --> vinUpdateToolResponseSchema : returns
    useOverrideLocationMutation --> currentLocationOverridePayloadSchema : accepts
    createMutationRequestHook --> BaseErrorResponse
```

> SVG rendering failed for this diagram.

## Diagram 2

```mermaid
flowchart TD
    A[Start: useOverrideLocationMutation.fetch called] --> B{isBulkUpload?}
    B -- yes --> C[Set Content-Type: text/csv]
    B -- no --> D[Set Content-Type: application/json]
    C --> E[headers: {x-time-zone, Content-Type}]
    D --> E
    E --> F[config = { headers }]
    F --> G[url = apiUrl(`/entity/solution/${solutionId}/current-location-override`)]
    G --> H[axios.post(url, payload, config)]
    H --> I[await response]
    I --> J[return res.data]
    H --> K[network/error -> BaseErrorResponse]
```

> SVG rendering failed for this diagram.
