# Diagram: web/portal/src/pages/administration/internal-tools/freeze-partview-eta-tool/FreezePartViewETATool.page.js


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
flowchart LR
  A[User selects or drops file] --> B[FileReader]
  B --> C(onFileLoad)
  C --> D[XLSX.read workbook]
  D --> E[Select first sheet]
  E --> F[XLSX.utils.sheet_to_json -> jsonData]
  F --> G[convertJsonToCsv(jsonData)]
  G --> H[setPackages.importCsv]
  H --> I[Button Upload clicked]
  I --> J[freezePartViewEtas(packages.importCsv)]
  J --> K[API request -> requestData]
  K --> L[getAlertProps(requestData)]
  L --> M[Alert shown with message]
  subgraph UI
    A
    I
    M
  end
  subgraph Utils
    D
    F
    G
  end
  subgraph State
    H
    K
  end
```

> SVG rendering failed for this diagram.
