# Diagram: web/portal/src/pages/administration/internal-tools/freeze-eta/FreezeETA.page.container.js


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
flowchart LR
  subgraph Redux_Module["../redux/FreezeETAState"]
    A(freezeEtas)
    B(clearActionStatus)
    C(pushFreezeEtasView)
    D(getActionStatus)
  end

  subgraph Component_File["./FreezeETA.page"]
    E[FreezeETA Component]
  end

  F[mapStateToProps(state)]
  F -->|calls| D
  D -->|returns| G[actionStatus value]
  F -->|maps to prop| H[actionStatus prop]

  subgraph Connect_Call
    I[connect(mapStateToProps, { freezeEtas, clearActionStatus, pushFreezeEtasView })]
    I --> J[Connected FreezeETA (default export)]
  end

  A -->|action creator| I
  B -->|action creator| I
  C -->|action creator| I
  H -->|passed into| I
  I -->|wraps| E
  E -->|uses props| H
  E -->|dispatches| A
  E -->|dispatches| B
  E -->|dispatches| C
```

> SVG rendering failed for this diagram.
