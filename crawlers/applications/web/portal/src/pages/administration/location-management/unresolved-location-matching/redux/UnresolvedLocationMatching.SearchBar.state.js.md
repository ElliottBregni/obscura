# Diagram: web/portal/src/pages/administration/location-management/unresolved-location-matching/redux/UnresolvedLocationMatching.SearchBar.state.js


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
classDiagram
class APIUrl {
  <<module>>
  +apiUrl(path)
}
class SearchOptions {
  <<module>>
  +SEARCH_CATEGORIES
  +FILTERS
}
class SearchBarStateBuilder {
  <<module>>
  +buildSearchBarState(mountPoint, categories, filters, fetchFn, [], options)
}
class locationsUrl {
  +locationsUrl(_ignored_solutionId, queryString)
}
class fetchSearch {
  +fetchSearch(queryString = "", _ignored_solutionId, duck, dispatch)
}
class LocationMatchingViewSearchBarState {
  <<export>>
  +instance
  +defaultSort: "name"
  +STORE_MOUNT_POINT: "fvLocationsMatchingViewSearch"
}

APIUrl <.. locationsUrl : calls apiUrl()
locationsUrl --> fetchSearch : used by
fetchSearch --> "duck.fetch()" : calls
fetchSearch --> dispatch : dispatches action
SearchOptions --> LocationMatchingViewSearchBarState : provides SEARCH_CATEGORIES & FILTERS
SearchBarStateBuilder --> LocationMatchingViewSearchBarState : buildSearchBarState(...) creates instance
fetchSearch --> LocationMatchingViewSearchBarState : provided as fetch callback
LocationMatchingViewSearchBarState ..> locationsUrl : relies on URL builder
```

> SVG rendering failed for this diagram.
