# Diagram: web/portal/src/pages/administration/location-management/unresolved-locations/LocationManagement.UnresolvedLocations.page.js


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
flowchart LR
  Page[LocationResolutionListPage]
  Alert[ExportAlert]
  Meta[TableMetaTotalRecords]
  Download[DownloadDataButton]
  Select[SelectFilter]
  Table[UnresolvedLocationTable]
  Modal[ExportModal]
  fetch[fetchUnresolvedLocationsSummary()]
  exportFn[exportLocations()]
  setSearch[setSearchFilter()]
  setPag[setPagination()]
  setSrt[setSort()]
  pushView[pushLocationMatchingView()]
  setShipment[setShipmentWithUnresolvedLocation()]
  resetExp[resetExport()]

  Page --> Alert
  Page --> Meta
  Page --> Download
  Page --> Select
  Page --> Table
  Page --> Modal

  Page --- fetch
  Download -->|onClick calls| exportFn
  Select -->|onChange calls| setSearch
  Select -->|uses options| AddressOptions[{"true: Valid Address","false: Invalid Address"}]
  Table -->|props: data, isLoading, page, pageSize, totalPages| Page
  Table -->|callbacks| setPag
  Table -->|callbacks| setSrt
  Table -->|rowClick ->| pushView
  Table -->|rowClick clears ->| setShipment

  Modal -->|props| exportIdentifier[exportIdentifier]
  Modal -->|props| exportName[exportName]
  Modal -->|props| resetExp

  fetch -->|initial effect| Page
  exportFn --> Modal
  setSearch --> fetch

  classDef fn fill:#f9f,stroke:#333,stroke-width:1px;
  class fetch,exportFn,setSearch,setPag,setSrt,pushView,setShipment,resetExp fn;
```

> SVG rendering failed for this diagram.
