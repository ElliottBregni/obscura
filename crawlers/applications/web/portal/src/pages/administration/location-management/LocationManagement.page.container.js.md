# Diagram: web/portal/src/pages/administration/location-management/LocationManagement.page.container.js


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
flowchart TD
  State[Redux State]
  getAuth[getAuthorization(state)]
  getOrg[getActiveOrganization(state)]
  map[mapStateToProps(state)\\n{ authorization, activeOrganization }]
  connect[connect(mapStateToProps)]
  View[LocationManagementView]
  Export[default export]

  State --> getAuth
  State --> getOrg
  getAuth --> map
  getOrg --> map
  map --> connect
  connect --> View
  connect --> Export
```

> SVG rendering failed for this diagram.
