# Diagram: web/portal/src/pages/administration/location-management/locations/search/LocationSearchStateExtensions.js


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
flowchart LR
  subgraph Actions
    SL[searchLocations(query)] -->|if query.length &ge;2| DISPATCH_FETCH[FETCH_LOCATIONS dispatch]
    DISPATCH_FETCH --> AX[axios.get(url)]
    AX --> RESP[response]
    RESP --> BUILD1[buildSearchLocationOptions(data)]
    BUILD1 --> DISPATCH_RECEIVE[RECEIVE_LOCATIONS dispatch (payload: locationOptions)]
  end

  subgraph AsyncAction
    ASL[asyncSearchLocations(query, paginate)] --> AX2[axios.get(url)]
    AX2 --> RESP2[response]
    RESP2 --> PARSE{paginate?}
    PARSE -->|true| EXTRACT1[data = response.data.data or response.data.destination_location]
    PARSE -->|false| EXTRACT2[data = response.data]
    EXTRACT1 --> BUILD2[buildSearchLocationOptions(data, selectType)]
    EXTRACT2 --> BUILD2
    BUILD2 --> RETURN[{ return { options: locationOptions, hasMore } }]
  end

  subgraph Utilities
    BUILD1 --> USES_STATES[Uses StatesUS lookup via lodash _.find]
    BUILD2 --> USES_STATES
    USES_STATES --> OPTION_OBJ[Creates option objects: value,label,id,code,name,address,state,stateName,city,country]
    OPTION_OBJ -->|on error| EMPTY[returns {}]
  end

  subgraph Reducer
    REDUCER[locationsReducer(state, action)] -->|on FETCH_LOCATIONS| STATE_FETCHED[set locationsLoading: true, locations: []]
    REDUCER -->|on RECEIVE_LOCATIONS| STATE_RECEIVED[set locations: action.payload, locationsLoading: false]
    STATE_FETCHED --> STORE[locations state]
    STATE_RECEIVED --> STORE
  end

  DISPATCH_RECEIVE --> REDUCER
  RETURN -->|used by Async Select components| UI[Async Select / Typeahead components]
  UI -->|selectType influences| BUILD2
  STORE -->|read by UI| UI
```

> SVG rendering failed for this diagram.
