# Diagram: web/portal/src/pages/administration/location-management/search/GeofenceFilter.js


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
classDiagram
class GeofenceFilter {
  +onChange(value)
}
class SelectFilter {
  +placeholder
  +options
  +onChange(option)
}
class geofenceFilterOptions <<array>> {
  +Point
  +Polygon
  +MultiPolygon
}
class i18n {
  +t(key): string
}
class PropTypes {
  +func
}
GeofenceFilter --> SelectFilter : renders/uses
GeofenceFilter --> geofenceFilterOptions : passes options
geofenceFilterOptions --> i18n : labels via t()
SelectFilter --> GeofenceFilter : calls onChange(option)
GeofenceFilter .. PropTypes : propTypes = { onChange: PropTypes.func }
```

> SVG rendering failed for this diagram.
