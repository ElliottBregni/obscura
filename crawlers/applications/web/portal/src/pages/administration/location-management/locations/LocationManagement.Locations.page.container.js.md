# Diagram: web/portal/src/pages/administration/location-management/locations/LocationManagement.Locations.page.container.js


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
flowchart LR
  subgraph ReduxState
    STATE[Global Redux State]
  end

  subgraph Selectors
    LADS[LadsState.selectors.getLadsList(state)]
    AUTH[getAuthorization(state)]
    SOL[getSolutionId(state)]
    ORG[getActiveOrganization(state)]
    LS_SELECTORS[LocationSearchBarState.selectors\n(getSearchResults,...,getExportFailed)]
  end

  subgraph ActionCreators
    LS_ACTIONS[LocationSearchBarState.actionCreators\n(setPagination,setSort,setSearchFilter,\nclearSearchText,clearSearchFilters,clearSearchFilter,\nsearchEntities,exportSearch,resetExport)]
    MATCH_ACTIONS[LocationMatchingState.actionCreators\n(setShipmentWithUnresolvedLocation)]
  end

  STATE -->|provides| LADS
  STATE -->|provides| AUTH
  STATE -->|provides| SOL
  STATE -->|provides| ORG
  STATE -->|provides| LS_SELECTORS

  LS_SELECTORS -->|maps to| Props_State[mapStateToProps props]
  LADS --> Props_State
  AUTH --> Props_State
  SOL --> Props_State
  ORG --> Props_State
  STATE -->|router| Router[routerLocation: state.location] --> Props_State

  LS_ACTIONS -->|used by| DispatchFns[mapDispatchToProps functions]
  MATCH_ACTIONS --> DispatchFns

  subgraph DispatchProps
    create[pushCreateLocationScreen]
    edit[pushEditLocationScreen]
    match[pushLocationMatchingView]
    pagination[setPagination]
    sort[setSort]
    filter[setSearchFilter]
    clearText[clearSearchText]
    clearFilters[clearSearchFilters]
    clearFilter[clearSearchFilter]
    search[searchLocations]
    setShipment[setShipmentWithUnresolvedLocation]
    export[exportLocations]
    reset[resetExport]
  end

  DispatchFns -->|creates| create
  DispatchFns --> edit
  DispatchFns --> match
  DispatchFns --> pagination
  DispatchFns --> sort
  DispatchFns --> filter
  DispatchFns --> clearText
  DispatchFns --> clearFilters
  DispatchFns --> clearFilter
  DispatchFns --> search
  DispatchFns --> setShipment
  DispatchFns --> export
  DispatchFns --> reset

  Props_State --> LocationsView[LocationsView Component]
  DispatchProps --> LocationsView

  connect[(connect(mapStateToProps, mapDispatchToProps))]
  Props_State --- connect
  DispatchProps --- connect
  connect --> LocationsView
```

> SVG rendering failed for this diagram.
