# Diagram: web/portal/src/pages/administration/location-management/locations/LocationManagement.Locations.page.js


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
classDiagram
    class LocationsView {
        +props: locationSearchResults, clearSearchText, searchLocations, solutionId
        +props: routerLocation, clearSearchFilters, setSearchFilter, activeOrganization
        +props: searchFilters, lads, clearSearchFilter, totalLocations
        +props: pushCreateLocationScreen, pushEditLocationScreen, isLoading
        +props: page, pageSize, totalPages, setPagination
        +props: defaultSortColumn, defaultReverseSort, sortColumn, reverseSort, setSort
        +props: canUserManageLocations, exportIdentifier, exportName, isExporting
        +props: exportFailed, exportLocations, resetExport
        -state: filters, showModal
        -methods: applyTableFilters(), filterResolvedLocations(filtered), resetSearchFilters()
        -lifecycle: useEffect(routerLocation), useEffect(searchFilters)
    }

    class AddLocationButton {
        +props: onAddNewLocation, className
        -render: Button (onClick -> onAddNewLocation)
    }

    class ImportLocationsButton {
        +props: onImportLocations, className
        -render: Button (onClick -> onImportLocations)
    }

    class BaseTable {
        +props: columns, data, isLoading, rowClickHandler
        +props: pagination props, sorting props, filters, onFilterChange
    }

    class ExportModal {
        +props: exportIdentifier, exportName, resetExport
    }

    class ImportLocationsModal {
        +props: show, hide
    }

    class ExportAlert {
        +props: exportFailed, className
    }

    class DownloadDataButton {
        +props: onClick, isExporting, isDisabled, data-qa
    }

    class SearchBarContainer {
        +props: isShowingAdvancedSearchButton
    }

    class Utility_getLadByLabelName <<utility>> {
        +getLadByLabelName(name, lads)
    }

    class TableFilterMap <<data>> {
        +name: "name_search"
        +address: "full_address_search"
        +code: "code_search"
        +organization: "customer_id"
        +type: "organization_lad_id"
        +child_count: "isParent"
        +geofence_type: "geofenceType"
        +parent_code: "parent_code"
    }

    LocationsView --> AddLocationButton : renders
    LocationsView --> ImportLocationsButton : renders
    LocationsView --> BaseTable : renders
    LocationsView --> ExportModal : includes
    LocationsView --> ImportLocationsModal : includes
    LocationsView --> ExportAlert : renders
    LocationsView --> DownloadDataButton : renders
    LocationsView --> SearchBarContainer : renders
    LocationsView ..> Utility_getLadByLabelName : uses
    LocationsView ..> TableFilterMap : uses
    BaseTable ..> AddLocationButton : receives columns (Location*Column)
    Utility_getLadByLabelName <.. TableFilterMap : resolves LAD labels
    AddLocationButton --> Button : uses
    ImportLocationsButton --> Button : uses
```

> SVG rendering failed for this diagram.
