# Diagram: web/portal/src/pages/administration/location-management/search/ParentFilter.js


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
classDiagram
class ParentFilter {
  +onChange
  +render()
}
class SelectFilter {
  +placeholder
  +options
  +onChange(option)
}
class PropTypes
class i18n {
  +t(key)
}
class parentFilterOptions {
  +[{value,label}, {value,label}]
}

ParentFilter --> SelectFilter : renders/uses
ParentFilter --> parentFilterOptions : reads
ParentFilter --> i18n : calls t() for labels
ParentFilter ..|> PropTypes : defines propTypes
SelectFilter --> ParentFilter : calls onChange(option) where option?.value is passed
```

> SVG rendering failed for this diagram.
