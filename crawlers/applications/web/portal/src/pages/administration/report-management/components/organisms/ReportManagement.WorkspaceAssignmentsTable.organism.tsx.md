# Diagram: web/portal/src/pages/administration/report-management/components/organisms/ReportManagement.WorkspaceAssignmentsTable.organism.tsx


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
classDiagram
    class WorkspaceRow {
        +string workspaceId
        +string workspaceName
    }
    class ReportManagementWorkspaceAssignmentsTableProps {
        +WorkspaceRow[] rows
        +Function onRemove(workspaceId)
    }
    class ReportManagementWorkspaceAssignmentsTable <<React Component>> {
        +props: ReportManagementWorkspaceAssignmentsTableProps
        -t: function
        -columns: Array
        +render()
    }
    class useTranslation <<Hook>> {
        +t(key): string
    }
    class useMemo <<Hook>> {
        +memoize(fn, deps)
    }
    class BaseTable <<Organism>> {
        +columns
        +data
        +isLoading
        +showPagination
    }
    class Button <<Atom>> {
        +onClick(event)
        +onMouseDown(event)
    }
    class Icon <<Atom>> {
        +src
        +size
    }

    WorkspaceRow --> ReportManagementWorkspaceAssignmentsTableProps : used in
    ReportManagementWorkspaceAssignmentsTableProps --> ReportManagementWorkspaceAssignmentsTable : passed to
    ReportManagementWorkspaceAssignmentsTable ..> useTranslation : uses
    ReportManagementWorkspaceAssignmentsTable ..> useMemo : uses
    ReportManagementWorkspaceAssignmentsTable --> BaseTable : renders
    ReportManagementWorkspaceAssignmentsTable --> Button : contains (remove action)
    ReportManagementWorkspaceAssignmentsTable --> Icon : contains (faTrashCan)
    Button --> Icon : contains
    ReportManagementWorkspaceAssignmentsTable : onRemove(workspaceId)
    ReportManagementWorkspaceAssignmentsTable : columns (Workspace, Actions)
    ReportManagementWorkspaceAssignmentsTable : returns null if rows.length == 0
```

> SVG rendering failed for this diagram.
