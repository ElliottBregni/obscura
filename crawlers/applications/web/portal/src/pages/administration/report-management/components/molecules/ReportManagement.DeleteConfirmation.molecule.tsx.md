# Diagram: web/portal/src/pages/administration/report-management/components/molecules/ReportManagement.DeleteConfirmation.molecule.tsx


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
classDiagram
    class DeleteReportConfirmation {
        +boolean show
        +AdminReportDTO|null report
        +onConfirm(force?: boolean): void
        +onCancel(): void
        -string confirmationText
        -isConfirmationValid(): boolean
        -hasEmailScheduleDependencies: boolean
        -hasWorkspaceDependencies: boolean
        -hasDependencies: boolean
        +handleConfirm(): void
        +handleCancel(): void
    }

    class AdminReportDTO {
        +string name
        +int reportNumber
        +int emailScheduleCount
        +Array workspaceAssignments
    }

    class ReportManagementConfirmModal {
        +boolean show
        +string title
        +ReactNode body
        +string confirmLabel
        +string confirmVariant
        +boolean confirmDisabled
        +onConfirm(): void
        +onCancel(): void
    }

    class Alert {
        +enum AlertVariant
        +variant
        +show: boolean
    }

    class Text {
        +boolean bold
    }

    class Colors {
        +reportManagement
    }

    DeleteReportConfirmation --> ReportManagementConfirmModal : renders
    DeleteReportConfirmation --> Alert : conditionally renders
    DeleteReportConfirmation --> Text : uses for content
    DeleteReportConfirmation --> Colors : uses style border
    DeleteReportConfirmation --> AdminReportDTO : accepts as prop
    ReportManagementConfirmModal <--|-- DeleteReportConfirmation : composes
    Alert <.. Text : contains
    AdminReportDTO o-- "workspaceAssignments" : contains
    Note right of DeleteReportConfirmation : uses useTranslation("admin-reports") and useState for confirmationText
```

> SVG rendering failed for this diagram.
