# Diagram: web/portal/src/pages/administration/report-management/hooks/useTagManagement.ts


> Auto-generated by Obscura crawlers

## Diagram 1

```mermaid
flowchart LR
  subgraph Fetching
    A[fetchTags()] --> B[call axios.get]
    B --> C[getLocalApiUrl("/reporting/admin/reports/tags")]
    B --> D[response.data]
    D --> E[z.array(adminTagSummarySchema)]
    E --> F[tagsSchema.parse(response.data)]
    F --> G[AdminTagSummaryDTO[] returned]
  end
  subgraph Hook
    H[useTagsData()] --> I[useQuery({ queryKey: ["admin","tags"], queryFn: fetchTags, staleTime: QUERY_STALE_TIME, gcTime: QUERY_GC_TIME })]
    I --> J[query object]
    J --> K[...query spread returned]
    J --> L[isLoading: Boolean(query.isLoading)]
  end
  H --> A
  style Fetching fill:#f9f,stroke:#333,stroke-width:1px
  style Hook fill:#9f9,stroke:#333,stroke-width:1px
```

> SVG rendering failed for this diagram.

## Diagram 2

```mermaid
classDiagram
  class fetchTags {
    <<function>>
    +Promise<AdminTagSummaryDTO[]>
  }
  class getLocalApiUrl {
    <<function>>
    +string
  }
  class axios {
    <<module>>
    +get(url)
  }
  class z {
    <<module>>
    +array(schema)
  }
  class adminTagSummarySchema {
    <<zod schema>>
  }
  class AdminTagSummaryDTO {
    <<type>>
  }
  class useQuery {
    <<hook>>
    +useQuery<TData, TError>(options)
  }
  class useTagsData {
    <<hook>>
    +returns { ...query, isLoading: Boolean }
  }
  class constants {
    QUERY_STALE_TIME
    QUERY_GC_TIME
  }
  fetchTags --> axios : uses
  fetchTags --> getLocalApiUrl : uses
  fetchTags --> z : uses
  z --> adminTagSummarySchema : composes
  adminTagSummarySchema --> AdminTagSummaryDTO : validates
  useTagsData --> useQuery : uses
  useQuery --> fetchTags : queryFn
  useTagsData --> constants : uses
```

> SVG rendering failed for this diagram.
