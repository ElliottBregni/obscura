# Diagram: web/portal/src/pages/administration/report-management/hooks/useSearchableOrganizationsData.ts


> Auto-generated by Obscura crawlers

## Diagram 1

```mermaid
flowchart LR
  A[Caller Component] -->|calls| B[useOrganizationOptions(search?)]
  B --> C{uses hook}
  C --> D[useSearchableOrganizationsData(search?)]
  D -->|queryKey: ["admin","organizations","search",trimmedSearch]| E[useInfiniteQuery]
  E -->|queryFn| F[fetchOrganizations(params)]
  F -->|axios.get(url, params)| G[getLocalApiUrl("/reporting/admin/organizations")]
  G --> H[API Server]
  H -->|response.data| I[paginatedAdminOrganizationsSchema.parse(data)]
  I -->|returns PaginatedAdminOrganizationsDTO| F
  F -->|pages| E
  E -->|data.pages| D
  D -->|provides| B
  B -->|maps pages.content| J[options: {value,label}[]]
  B -->|exposes| K[fetchNextPage, hasNextPage, isLoading]
```

> SVG rendering failed for this diagram.

## Diagram 2

```mermaid
classDiagram
  class PaginatedAdminOrganizationsDTO {
    +number page
    +number totalPages
    +AdminOrganizationDTO[] content
  }
  class AdminOrganizationDTO {
    +string organizationId
    +string organizationName
    +...otherFields
  }
  class FetchOrganizations {
    +fetchOrganizations({pageParam, queryKey}): PaginatedAdminOrganizationsDTO
  }
  class UseSearchableOrganizationsData {
    +useSearchableOrganizationsData(search?): UseInfiniteQueryResult
  }
  class UseOrganizationOptions {
    +useOrganizationOptions(search?): {options, fetchNextPage, hasNextPage, isLoading}
  }
  PaginatedAdminOrganizationsDTO "1" o-- "*" AdminOrganizationDTO : contains
  UseOrganizationOptions --> UseSearchableOrganizationsData : calls
  UseSearchableOrganizationsData --> FetchOrganizations : uses
  FetchOrganizations --> PaginatedAdminOrganizationsDTO : returns
```

> SVG rendering failed for this diagram.
