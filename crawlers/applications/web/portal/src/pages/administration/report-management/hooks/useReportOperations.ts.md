# Diagram: web/portal/src/pages/administration/report-management/hooks/useReportOperations.ts


> Auto-generated by Obscura crawlers

## Diagram 1

```mermaid
flowchart TD
  UI["UI: Trigger delete"] --> MU[useDeleteReport mutation]
  MU --> BUILD[Build URL with reportId and optional force param]
  BUILD --> FETCH[fetch DELETE /api/reporting/admin/reports/:reportId]
  FETCH --> RESP{response.ok?}
  RESP -- Yes --> INV[queryClient.invalidateQueries("admin-reports")]
  RESP -- No --> STATUS{status === 400?}
  STATUS -- Yes --> THROW400["throw Error: 'Cannot delete report due to dependencies'"]
  STATUS -- No --> THROWGEN["throw Error: 'Failed to delete report'"]
  RESP -- ErrorHandler --> LOG["console.error('Failed to delete report:', error.message)"]
```

> SVG rendering failed for this diagram.

## Diagram 2

```mermaid
flowchart TD
  UI2["UI: Trigger republish"] --> MU2[useRepublishReport mutation]
  MU2 --> FETCH2[fetch POST /api/reporting/admin/reports/:reportId/republish]
  FETCH2 --> RESP2{response.ok?}
  RESP2 -- Yes --> INV1[queryClient.invalidateQueries('admin-reports')]
  RESP2 -- Yes --> INV2[queryClient.invalidateQueries(['admin-report', reportId])]
  RESP2 -- No --> STATUS2{status === 400?}
  STATUS2 -- Yes --> THROW400R["throw Error: 'Cannot republish disabled report without force flag'"]
  STATUS2 -- No --> THROWGENR["throw Error: 'Failed to republish report'"]
  RESP2 -- ErrorHandler --> LOG2["console.error('Failed to republish report:', error.message)"]
```

> SVG rendering failed for this diagram.
