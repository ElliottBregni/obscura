# Diagram: web/portal/src/pages/administration/report-management/components/organisms/ReportManagement.OrganizationPermissions.organism.tsx


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
classDiagram
class ReportManagementOrganizationPermissions {
  +selectedReportMode: ReportMode
  +onChangeMode(mode: ReportMode): void
  +orgOptions: Array<{label, value}>
  +onOrgTextChange(search: string): void
  +hasMoreOrgs: boolean
  +fetchNextOrgs(): void
  +isLoadingOrgs: boolean
  +selectedOrgIds: string[]
  +onChangeSelectedOrgs(orgIds: string[]): void
  +onAddOrganizations(orgIds: string[]): Promise|void
  +assignments: OrganizationAssignment[]
  +onRemoveOrg(workspaceId: string, organizationId: string): void
  +onChangeOrgMode(workspaceId: string, organizationId: string, mode: ReportMode): void
  -handleRemoveOrg(workspaceId, organizationId): void
  -tableData: TableRow[]
  -columns: Array
}
class OrganizationAssignment {
  +workspaceId: string
  +workspaceName: string
  +organizations: Organization[]
}
class Organization {
  +organizationId: string
  +organizationName: string
  +reportMode?: ReportMode
}
class TableRow {
  +workspaceId: string
  +workspaceName: string
  +organizationId: string
  +organizationName: string
  +reportMode: ReportMode
}
class UI_Components {
  +SearchableSelectList
  +Button
  +Icon
  +Text
  +BaseTable
}
ReportManagementOrganizationPermissions "1" --> "many" OrganizationAssignment : assignments
OrganizationAssignment "1" --> "many" Organization : organizations
ReportManagementOrganizationPermissions "1" --> "many" TableRow : tableData
ReportManagementOrganizationPermissions ..> UI_Components : uses
UI_Components --> Button : renders
UI_Components --> SearchableSelectList : renders
UI_Components --> BaseTable : renders
ReportManagementOrganizationPermissions : builds columns with Cells (Workspace, Organizations, Report Mode, Actions)
ReportManagementOrganizationPermissions : actions -> Button(onMouseDown -> handleRemoveOrg)
```

> SVG rendering failed for this diagram.
