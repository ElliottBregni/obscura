# Diagram: web/portal/src/pages/administration/report-management/hooks/useReportManagementData.ts


> Auto-generated by Obscura crawlers

## Diagram 1

```mermaid
classDiagram
class DTOs {
  <<namespace>>
  AdminReportSearchDTO
  PaginatedAdminReportsDTO
  AdminReportDTO
  AdminReportCreateDTO
  AdminReportUpdateDTO
  RepublishOptionsDTO
}
class Schemas {
  <<namespace>>
  adminReportSearchSchema
  paginatedAdminReportsSchema
  adminReportSchema
  adminReportCreateSchema
  adminReportUpdateSchema
  republishOptionsSchema
}
class AxiosClient {
  <<library>>
  axios.get()
  axios.post()
  axios.put()
  axios.delete()
}
class APIHelpers {
  getLocalApiUrl(path)
}
class Fetchers {
  fetchAdminReports(criteria)
  fetchAdminReportById(id)
  fetchAdminReportDetailById(id, include)
  createAdminReport(report)
  updateAdminReport(id, report)
  deleteAdminReport(id, force)
  republishAdminReport(id, options)
}
class Hooks {
  useAdminReportsData(searchCriteria)
  useAdminReportById(id)
  useAdminReportDetailById(id)
  useDeleteReport()
  useRepublishReport()
}
DTOs --> Schemas : validated by
Fetchers --> AxiosClient : uses
Fetchers --> APIHelpers : builds URL with
Fetchers --> Schemas : parses response via
Hooks --> Fetchers : call
Hooks --> "react-query" : useQuery / useMutation
Hooks --> "QueryClient" : invalidateQueries onSuccess
Schemas <-- DTOs : define structure
```

> SVG rendering failed for this diagram.

## Diagram 2

```mermaid
flowchart LR
  subgraph "Fetch Flow"
    A[searchCriteria: AdminReportSearchDTO] -->|validated| B[fetchAdminReports(criteria)]
    B -->|axios.get / response| C[paginatedAdminReportsSchema.parse(response.data)]
    C --> D[useQuery returns {isLoading, data, error, refetch}]
  end
  subgraph "Mutations"
    E[createAdminReport(report)] -->|validate & axios.post| F[adminReportSchema.parse(response)]
    G[updateAdminReport(id, report)] -->|validate & axios.put| F
    H[deleteAdminReport(id, force)] -->|axios.delete| I[void]
    J[republishAdminReport(id, options)] -->|validate & axios.post| I
    F & I --> K[onSuccess -> queryClient.invalidateQueries("admin-reports","reportManagementAuditLogs")]
    D --> L[returned hooks: createReport/updateReport/deleteReport/republishReport & statuses]
  end
```

> SVG rendering failed for this diagram.
