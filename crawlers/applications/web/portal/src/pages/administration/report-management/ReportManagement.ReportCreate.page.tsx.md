# Diagram: web/portal/src/pages/administration/report-management/ReportManagement.ReportCreate.page.tsx


> Auto-generated by Obscura crawlers

## Diagram 1

```mermaid
classDiagram
class ReportManagementReportCreatePage {
  +t:string
  +dispatch:function
  +initialValues:AdminReportCreateDTO
  +formState
  +setValue(field, value)
  +getFieldProps(name)
  +handleFormSubmit(event)
  +createReport(values, callbacks)
  +isCreating:boolean
  +workspaces:Array
  +isWorkspacesLoading:boolean
  +workspacesError:Error|undefined
  +workspaceOptions:Array<Option>
  +toastState:{show,message,bg}
  +triggerToast(message,bg)
}
class ReportManagementPageHeaderWithBack
class ReportManagementSectionCard
class TextInputValidated
class ReportManagementSelectWithPortal
class Button
class Toast
class useFormValidation
class useAdminReportsData
class useWorkspacesData
class useSetTitleOnMount

ReportManagementReportCreatePage --> ReportManagementPageHeaderWithBack : renders
ReportManagementReportCreatePage --> ReportManagementSectionCard : renders
ReportManagementSectionCard --> TextInputValidated : contains
ReportManagementSectionCard --> ReportManagementSelectWithPortal : contains
ReportManagementSectionCard --> Button : contains
ReportManagementReportCreatePage --> Toast : renders
ReportManagementReportCreatePage --> useFormValidation : uses
ReportManagementReportCreatePage --> useAdminReportsData : uses
ReportManagementReportCreatePage --> useWorkspacesData : uses
ReportManagementReportCreatePage --> useSetTitleOnMount : uses
```

> SVG rendering failed for this diagram.

## Diagram 2

```mermaid
flowchart LR
  U[User] -->|enter values| Form[Form fields (reportNumber, name, details, source, originalReportName, workspace)]
  Form -->|submit| Submit[handleFormSubmit]
  Submit --> Create[createReport(values)]
  Create -->|onSuccess(created)| OnSuccess{created.id != null?}
  OnSuccess -->|yes| DispatchDetail[dispatch REPORT_MANAGEMENT_DETAIL with reportId]
  OnSuccess -->|no| DispatchList[dispatch REPORT_MANAGEMENT]
  Create -->|onError(err)| OnError[catch error]
  OnError --> ComputeMsg[compute errorMessage from AxiosError or err]
  ComputeMsg --> ToastTrigger1[triggerToast(message, "danger")]
  useWorkspaces[useWorkspacesData] -->|returns| Workspaces[workspaces / isLoading / error]
  Workspaces --> WorkspaceOptions[workspacesToOptions(workspaces)]
  WorkspaceOptions --> ReportManagementSelectWithPortal
  Workspaces -->|error| ToastTrigger2[triggerToast(loadWorkspacesError, "warning")]
  ToastTrigger1 --> ToastComponent[Toast show message variant]
  ToastTrigger2 --> ToastComponent
  DispatchDetail --> Navigation[Navigate to report detail]
  DispatchList --> Navigation[Navigate to reports list]
```

> SVG rendering failed for this diagram.
