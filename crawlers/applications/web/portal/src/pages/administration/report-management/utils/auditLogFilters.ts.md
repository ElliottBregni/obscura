# Diagram: web/portal/src/pages/administration/report-management/utils/auditLogFilters.ts


> Auto-generated by Obscura crawlers

## Diagram 1

```mermaid
classDiagram
class AuditLogFilterOptions {
  +Array~{value:string,label:string}~ entityTypes
  +Array~{value:string,label:string}~ operationTypes
}
class ReportManagementAuditLogSearchDTO {
  +Number page
  +Number size
  +String entityType
  +String operationType
  +String actorId
  +String startDate
  +String endDate
  +String sort
}
class useAuditLogFilterOptions {
  +getEntityTypeOptions()
  +getOperationTypeOptions()
  +getSortOptions()
}
class buildSearchCriteria {
  +(filters: Partial<ReportManagementAuditLogSearchDTO>) : ReportManagementAuditLogSearchDTO
}
class clearFilters {
  +() : ReportManagementAuditLogSearchDTO
}
class hasActiveFilters {
  +(criteria: ReportManagementAuditLogSearchDTO) : boolean
}
useAuditLogFilterOptions --> AuditLogFilterOptions : provides
buildSearchCriteria --> ReportManagementAuditLogSearchDTO : returns
clearFilters --> buildSearchCriteria : calls
hasActiveFilters --> ReportManagementAuditLogSearchDTO : reads
```

> SVG rendering failed for this diagram.

## Diagram 2

```mermaid
graph TD
  A[filters: Partial<ReportManagementAuditLogSearchDTO>] --> B{has properties?}
  B -->|page defined| P1[page = filters.page]
  B -->|page undefined| P2[page = 0]
  B -->|size defined| S1[size = filters.size]
  B -->|size undefined| S2[size = 20]
  B --> E1[entityType = filters.entityType || undefined]
  B --> O1[operationType = filters.operationType || undefined]
  B --> AC1[actorId = filters.actorId || undefined]
  B --> SD1[startDate = filters.startDate || undefined]
  B --> ED1[endDate = filters.endDate || undefined]
  B --> R1[sort = filters.sort ?? "createdAt,desc"]
  P1 --> Z[Result DTO]
  P2 --> Z
  S1 --> Z
  S2 --> Z
  E1 --> Z
  O1 --> Z
  AC1 --> Z
  SD1 --> Z
  ED1 --> Z
  R1 --> Z
  Z[ReportManagementAuditLogSearchDTO returned]
```

> SVG rendering failed for this diagram.
