# Diagram: web/portal/src/pages/administration/report-management/components/organisms/ReportManagement.RepublishModal.organism.tsx


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
flowchart LR
  Component[RepublishReportModal]
  Component --> Props[Props: show, report, onConfirm(options), onCancel(), isLoading?]
  Component --> State[State]
  State --> F[forceRepublish: boolean]
  State --> U[updateTimestamp: boolean = true]
  State --> S[sendNotification: boolean = true]

  Component --> Handlers[Handlers]
  Handlers --> HC[handleConfirm()]
  Handlers --> HCan[handleCancel()]
  HC -->|builds options| Options[RepublishOptionsDTO]
  Options -->|calls| OnConfirm[onConfirm(options)]
  HCan -->|resets state| Reset[setForceRepublish(false), setUpdateTimestamp(true), setSendNotification(true)]
  HCan -->|calls| OnCancel[onCancel()]

  Component --> UI[DialogModal]
  UI --> Title[title: "Republish Report"]
  UI --> Buttons[Submit / Cancel buttons]
  Buttons --> SubmitDisabled{submitButtonDisabled?}
  SubmitDisabled -->|isLoading true| Disabled1[disabled]
  SubmitDisabled -->|needsForceFlag && !forceRepublish| Disabled2[disabled]
  Buttons -->|onSubmit| HC

  UI --> ConditionalReport{report exists?}
  ConditionalReport -->|yes| Details[Report Details]
  Details --> Name[Name: report.name]
  Details --> Number[Number: report.reportNumber]
  Details --> Status[Status: report.enabled ? Enabled : Disabled]

  UI --> OptionsGroup[Republish Options]
  OptionsGroup --> UpdateChk[Checkbox: Update report timestamp (checked=updateTimestamp)]
  UpdateChk -->|onChange| U
  OptionsGroup --> SendChk[Checkbox: Send notifications (checked=sendNotification)]
  SendChk -->|onChange| S

  ConditionalReport --> NeedsForceFlag{needsForceFlag = report?.enabled === false}
  NeedsForceFlag -->|true| ForceAlert[Alert (Warning) with Force checkbox]
  ForceAlert --> ForceChk[Checkbox: Force republish (checked=forceRepublish)]
  ForceChk -->|onChange| F

  classDef comp fill:#f9f,stroke:#333,stroke-width:1px;
  class Component,UI comp
```

> SVG rendering failed for this diagram.
