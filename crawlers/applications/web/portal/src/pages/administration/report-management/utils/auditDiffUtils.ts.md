# Diagram: web/portal/src/pages/administration/report-management/utils/auditDiffUtils.ts


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
flowchart TD
  A[compareObjects(oldObj, newObj, path="")] --> B{oldObj && newObj?}
  B -->|No, both null/undefined| C[return []]
  B -->|No, oldObj null, newObj present| D[for each key in newObj -> push ADDED DiffResult(field, undefined, newValue)]
  B -->|No, newObj null, oldObj present| E[for each key in oldObj -> push REMOVED DiffResult(field, oldValue, undefined)]
  B -->|Yes| F[build allKeys = union(keys(oldObj), keys(newObj))]
  F --> G[for each key in allKeys]
  G --> H{key in oldObj?}
  H -->|No| I[push ADDED DiffResult(field, undefined, newValue)]
  H -->|Yes| J{key in newObj?}
  J -->|No| K[push REMOVED DiffResult(field, oldValue, undefined)]
  J -->|Yes| L[determine oldValue = a[key], newValue = b[key]]
  L --> M{isObject(oldValue) && isObject(newValue)?}
  M -->|Yes| N[recurse: compareObjects(oldValue, newValue, fieldPath) -> append diffs]
  M -->|No| O{Array.isArray(oldValue) && Array.isArray(newValue)?}
  O -->|Yes| P{arraysEqual(oldValue, newValue)?}
  P -->|No| Q[push CHANGED DiffResult(field, oldValue, newValue)]
  P -->|Yes| R[no diff]
  O -->|No| S{oldValue !== newValue?}
  S -->|Yes| Q
  S -->|No| R
  Q --> T[continue]
  I --> T
  K --> T
  N --> T
  D --> U[return diffs]
  E --> U
  T --> U[after loop return diffs]

  subgraph Helpers
    isObj[isObject(value): value !== null && typeof value === "object" && !Array.isArray(value)]
    arrEq[arraysEqual(a,b): compare lengths; iterate; use isObject/arraysEqual/!== rules]
    fmtVal[formatValue(value): null -> "null", else JSON.stringify or String(value)]
    fmtField[formatFieldName(fieldPath): split '.', Title Case parts, join " → "]
  end

  U -->|uses| isObj
  U -->|uses| arrEq
  U -->|uses| fmtVal
  U -->|uses| fmtField
```

> SVG rendering failed for this diagram.
