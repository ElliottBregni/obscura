# Diagram: web/portal/src/pages/administration/admin-tools/shipment-report-arrival-departure/ShipmentReportArrivalDeparture.page.js


> Auto-generated by Obscura crawlers

## Diagram 1

```mermaid
classDiagram
class ShipmentReportArrivalDeparture {
  +reportArrivalDeparture(Func)
  +clearReportArrivalDeparture(Func)
  +shipmentReportArrivalDepartureData(Object)
  -data {stopCode, importText, eventCode, eventTime, comments}
  -showAlert: boolean
  -isSubmitLoading: boolean
  -responseStatusCode: number|null
  -errorResponse: object|null
  +onStopChange(obj)
  +onEventChange(obj)
  +onEventTimeChange(dateTime)
  +onCommentsChange(event)
  +textChangeHandler(event)
  +onSubmitClick()
  +canSubmit(): boolean
}
class SelectField
class DateTimePicker
class Alert
class Button
class Icon
class useSetTitleOnMount
class useTrackWithMixpanelOnce
ShipmentReportArrivalDeparture --> SelectField : uses
ShipmentReportArrivalDeparture --> DateTimePicker : uses
ShipmentReportArrivalDeparture --> Alert : uses
ShipmentReportArrivalDeparture --> Button : uses
ShipmentReportArrivalDeparture --> Icon : uses
ShipmentReportArrivalDeparture --> useSetTitleOnMount : hook
ShipmentReportArrivalDeparture --> useTrackWithMixpanelOnce : hook
class EventOption {
  +label: string
  +value: string
  +stopCode: "O"|"D"
}
ShipmentReportArrivalDeparture --> EventOption : eventDropdownOptions
```

> SVG rendering failed for this diagram.

## Diagram 2

```mermaid
flowchart TD
  A[User opens page] --> B[useSetTitleOnMount & Mixpanel track]
  B --> C[Initial state set: data=initialState, showAlert=false]
  C --> D[User selects Stop (O/D)]
  D --> E[eventOptions filtered by stopCode]
  D --> F[User enters Shipment IDs textarea]
  E --> G[User selects Event code]
  G --> H[User picks Event Time using DateTimePicker]
  H --> I[User optionally enters Comments]
  F & G & H & D --> J[canSubmit computed]
  J -->|true| K[Click Submit button]
  K --> L[onSubmitClick -> parse shipmentIds and call reportArrivalDeparture]
  L --> M[API response sets shipmentReportArrivalDepartureData.status]
  M --> N[useEffect triggers showAlert=true]
  M --> O{status === 201 or 408}
  O -->|201 or 408| P[data reset to initialState]
  N --> Q[Alert displays Success/Warning/Error based on status]
  Q --> R[User dismisses Alert -> showAlert=false]
```

> SVG rendering failed for this diagram.
