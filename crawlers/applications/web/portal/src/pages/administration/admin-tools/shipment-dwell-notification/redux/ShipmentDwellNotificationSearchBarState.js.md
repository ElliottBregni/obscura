# Diagram: web/portal/src/pages/administration/admin-tools/shipment-dwell-notification/redux/ShipmentDwellNotificationSearchBarState.js


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
flowchart LR
  A[import moment from "moment"] --> B[guess timezone: moment.tz.guess()]
  C[import buildSearchBarState] --> F[buildSearchBarState(...)]
  D[import apiUrl] --> E[/apiUrl("/notification/dwell/search")/] --> SHIPMENT_DWELL_URL[SHIPMENT_DWELL_URL]
  G[import FILTERS] --> H[FILTERS]
  SHIPMENT_DWELL_URL -->|used in| I[fetchSearch(queryString, solutionId, duck, dispatch)]
  I --> J{build url}
  J --> J1["`${SHIPMENT_DWELL_URL}?prefill=0&${queryString}`"]
  I --> K[config.headers = { 'x-time-zone': moment.tz.guess() }]
  I --> L[transformResponse(response) -> { meta, data: response.results }]
  I --> M[dispatch(duck.fetch(url, config, transformResponse))]
  F --> N[default export]
  N -->|args| O[STORE_MOUNT_POINT = "shipmentDwellSearch"]
  O --> P[[] (initial state)]
  P --> H
  H --> I
  I --> Q[[] (additional options)]
  Q --> R[{ defaultSort: "location_name", reverseSort: false }]
```

> SVG rendering failed for this diagram.
