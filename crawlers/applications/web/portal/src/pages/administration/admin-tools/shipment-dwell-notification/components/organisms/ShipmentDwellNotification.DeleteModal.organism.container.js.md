# Diagram: web/portal/src/pages/administration/admin-tools/shipment-dwell-notification/components/organisms/ShipmentDwellNotification.DeleteModal.organism.container.js


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
classDiagram
    class EditShipmentDwellNotificationState {
        <<module>>
        +getLocation(state)
        +getRecipients(state)
        +getIsPrefillDataLoading(state)
        +getIsModifyDwellNotificationLoading(state)
        +modifyDwellNotification(...)
        +fetchPrefillData(...)
    }

    class ShipmentDwellNotificationSearchBarState {
        <<module>>
        +searchEntities(...)
    }

    class mapStateToProps {
        <<function>>
        +(state) : { location, recipients, isPrefillDataLoading, isModifyDwellNotificationLoading }
    }

    class actionCreators {
        <<object>>
        +modifyDwellNotification
        +fetchPrefillData
        +searchEntities
    }

    class ShipmentDwellNotificationDeleteModal {
        <<React Component>>
    }

    class connect {
        <<HOC>>
        +connect(mapStateToProps, actionCreators)(Component)
    }

    class ExportDefault {
        <<export>>
        +ConnectedComponent
    }

    EditShipmentDwellNotificationState --> mapStateToProps : selectors used by
    mapStateToProps --> ShipmentDwellNotificationDeleteModal : maps props for
    EditShipmentDwellNotificationState --> actionCreators : provides modifyDwellNotification\nfetchPrefillData
    ShipmentDwellNotificationSearchBarState --> actionCreators : provides searchEntities
    actionCreators --> connect : passed to
    mapStateToProps --> connect : passed to
    connect --> ExportDefault : returns
    connect --> ShipmentDwellNotificationDeleteModal : wraps
    ExportDefault --> ShipmentDwellNotificationDeleteModal : connected version of
```

> SVG rendering failed for this diagram.
