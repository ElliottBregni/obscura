# Diagram: web/portal/src/pages/administration/admin-tools/shipment-eta-override/ShipmentETAOverride.page.container.js


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
classDiagram
    class ShipmentETAOverride {
        <<React Component>>
    }
    class connect {
        +Function(mapStateToProps, actionCreators)
    }
    class mapStateToProps {
        +activeOrganization
        +carriers
        +carriersData
        +delayOptionsData
        +reportDelayData
    }
    class OrganizationsState {
        +getActiveOrganization(state)
    }
    class DomainDataState {
        class selectors {
            +selectCarriersAlphabetically(state)
            +getCarriersData(state)
        }
    }
    class EditShipmentState {
        class selectors {
            +getDelayOptionsData(state)
            +getReportDelayData(state)
        }
        class actionCreators {
            +fetchDelayOptions()
            +reportDelay()
            +clearReportDelayInState()
        }
    }
    mapStateToProps --> OrganizationsState : uses
    mapStateToProps --> DomainDataState.selectors : uses
    mapStateToProps --> EditShipmentState.selectors : uses
    ShipmentETAOverride <-- connect : wrapped by
    connect ..> mapStateToProps : receives
    connect ..> EditShipmentState.actionCreators : receives
    EditShipmentState.actionCreators --> EditShipmentState : defined in
    DomainDataState.selectors --> DomainDataState : defined in
    EditShipmentState.selectors --> EditShipmentState : defined in
    OrganizationsState --> getActiveOrganization : exposes
    DomainDataState --> selectors : exposes
    EditShipmentState --> selectors : exposes
    EditShipmentState --> actionCreators : exposes
```

> SVG rendering failed for this diagram.
