# Diagram: web/portal/src/pages/administration/admin-tools/AdminTools.page.container.js


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
flowchart LR
  subgraph modules
    A[getAuthorization\n(modules/auth/AuthorizationSelectors)]
    B[redirectToRoute\n(redux/utils)]
    C[AdministrativeToolsView\n(AdminTools.page)]
  end

  subgraph mapping
    M[mapStateToProps(state)]
  end

  A -->|calls| M
  M -->|returns prop: auth| Conn[connect(mapStateToProps, { redirectToRoute })]
  B -->|provided as dispatch prop| Conn
  Conn -->|wraps| C
  Conn -->|export default| Export[ConnectedAdministrativeToolsView]
```

> SVG rendering failed for this diagram.
