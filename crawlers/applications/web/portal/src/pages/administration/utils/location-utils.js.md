# Diagram: web/portal/src/pages/administration/utils/location-utils.js


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
classDiagram
class useGetCountryNameByCountryCode {
  +countryCode
  +returns string
}
class useGetShipperNameById {
  +id
  +returns string
}
class useSelector {
  +selector
}
class DomainDataState {
  selectors
}
class Lodash_find {
  +collection, predicate -> item
}

useGetCountryNameByCountryCode --> useSelector : calls
useGetCountryNameByCountryCode --> Lodash_find : uses
useGetShipperNameById --> useSelector : calls
useGetShipperNameById --> Lodash_find : uses
useSelector --> DomainDataState : invokes selectors.getCountries / selectors.getShippers
DomainDataState ..> "selectors.getCountries" 
DomainDataState ..> "selectors.getShippers"
```

> SVG rendering failed for this diagram.
