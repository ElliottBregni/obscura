# Diagram: web/portal/src/pages/profile/components/UserPreferenceForm.organism.js


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
graph LR
  UT[useTranslation("profile")] --> UPF[UserPreferenceForm]
  SL[SupportedLanguages] --> LOpts[languagePreferenceOptions]
  LOpts --> SelectFieldInput
  TimeOpts[timePreferenceOptions] --> SelectFieldInput
  DateOpts[datePreferenceOptions] --> SelectFieldInput

  subgraph SelectFieldInputComponent
    SFI[SelectFieldInput]
    SF[SelectField]
    SFI --> SF
    SFI -->|props: value,onChange,options| SF
  end

  UPF -->|renders| SFI_L[SelectFieldInput (Language)]
  UPF -->|renders| SFI_D[SelectFieldInput (Date)]
  UPF -->|renders| SFI_T[SelectFieldInput (Time)]
  UPF -->|renders| TXT[Text]
  UPF -->|renders| BTN[Button]

  SFI_L -->|options=languagePreferenceOptions| LOpts
  SFI_D -->|options=datePreferenceOptions| DateOpts
  SFI_T -->|options=timePreferenceOptions| TimeOpts

  UPF -->|uses state| StateLang[languagePreference/useState]
  UPF -->|uses state| StateDate[dateFormatPreference/useState]
  UPF -->|uses state| StateTime[timeFormatPreference/useState]

  SFI_L -->|onChange:selectedOption.value| StateLang
  SFI_D -->|onChange:selectedOption.value| StateDate
  SFI_T -->|onChange:selectedOption.value| StateTime

  BTN -->|onClick| Submit[submitPreferences()]
  Submit -->|calls| SetPreferences[setPreferences({...})]
  SetPreferences -->|updates external| External[userPreferences setter]

  UPF -->|reads| Props[userPreferences, setPreferences prop]
  Props -->|provides initial| StateLang
  Props -->|provides initial| StateDate
  Props -->|provides initial| StateTime

  Colors[Colors.background.LIGHT_GRAY] -->|used in css| UPF
  Text -->|displays| Labels[profile labels via t()]
  UT -->|t()| Labels

  classDef comp fill:#f9f,stroke:#333,stroke-width:1px;
  class UPF,SFI_L,SFI_D,SFI_T,SFI,SF,BTN,TXT comp
```

> SVG rendering failed for this diagram.
