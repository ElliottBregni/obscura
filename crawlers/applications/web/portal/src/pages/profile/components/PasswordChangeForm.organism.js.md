# Diagram: web/portal/src/pages/profile/components/PasswordChangeForm.organism.js


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
classDiagram
class FormInput {
  +props: type, handler, label, placeholder, controlType, value
  +render()
}
class PasswordChangeForm {
  +props: passwordStatus, resetPasswordForm, changePassword
  -newPassword
  -confirmPassword
  +eventHandler(value, type)
  +render()
}
class AuthenticationUtils {
  +userId
  +userEmail
}
class PasswordUtils {
  +doesPassContainEmailOrUsername()
  +isPassLengthValid()
  +isPassMatchValid()
  +isPassRegexValid()
}
class PasswordChecks {
  +props: isLengthValid, isPasswordsMatch, isRegexValid, doesPassContainEmailOrUsername
}
class UIAtoms {
  +Button
  +Alert
  +Text
}
class React {
  +useState()
  +useEffect()
  +useTranslation()
}
class ReactBootstrap {
  +Form
  +FormControl
  +FormGroup
  +FormLabel
}
class Styles {
  +Colors
  +FlexDiv
}

PasswordChangeForm --> FormInput : uses
PasswordChangeForm --> PasswordChecks : includes
PasswordChangeForm --> UIAtoms : uses
PasswordChangeForm --> AuthenticationUtils : reads
PasswordChangeForm --> PasswordUtils : validates with
PasswordChangeForm --> React : hooks
FormInput --> ReactBootstrap : composes
FormInput --> Styles : styled(FlexDiv)
UIAtoms --> Styles : styling
PasswordChecks --> PasswordUtils : consumes
AuthenticationUtils ..> PasswordChangeForm : userEmail/userId read
FormInput <--|-- PasswordChangeForm : instantiates
Styles <-- Colors : provides
ReactBootstrap <|-- Form : provides components
```

> SVG rendering failed for this diagram.
