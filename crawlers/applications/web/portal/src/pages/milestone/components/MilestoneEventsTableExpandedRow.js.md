# Diagram: web/portal/src/pages/milestone/components/MilestoneEventsTableExpandedRow.js


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
classDiagram
class RevealedDetailsTable {
  +original
  +detailsData
  +partnerRows()
  +statusRefRows()
  +categoryIdsRow
  +rows
  +rowElems()
  +render()
}
class MilestoneDashboardExpandedRow {
  +props
  +row
  +original
  +statusUpdateId
  +data
  +loading
  +render()
}
class useStatusUpdateDetails {
  +statusUpdateId
  +dispatch()
  +useEffect()
  +useSelector()
  +returns {data, loading}
}
class StatusUpdateDetailState {
  <<module>>
  +actionCreators.fetchStatusUpdateDetails()
  +selectors.getStatusUpdateDetailsForId()
}
class LocationAddressComplex {
  +address1
  +address2
  +city
  +state
  +postalCode
  +country
}
class IdBox {
  +children
}
class Loader {
  +loaded
  +left
  +scale
}
class Table {
  +tbody
}
class Colors {
  +text.GRAY
}
class PropTypes {}
class lodash {
  +get()
  +isNil()
}
class react_redux {
  +useDispatch()
  +useSelector()
}
class react_i18next {
  +useTranslation()
}

RevealedDetailsTable --> Table : renders
RevealedDetailsTable --> IdBox : uses for location name
RevealedDetailsTable --> LocationAddressComplex : uses for location address
RevealedDetailsTable --> Colors : uses addressColor
RevealedDetailsTable --> PropTypes : defines propTypes
RevealedDetailsTable --> lodash : uses get/isNil
MilestoneDashboardExpandedRow --> RevealedDetailsTable : composes
MilestoneDashboardExpandedRow --> Loader : shows loading state
MilestoneDashboardExpandedRow --> react_i18next : uses useTranslation
MilestoneDashboardExpandedRow --> useStatusUpdateDetails : obtains data
useStatusUpdateDetails --> react_redux : uses useDispatch/useSelector
useStatusUpdateDetails --> StatusUpdateDetailState : dispatches fetchStatusUpdateDetails / uses selector
StatusUpdateDetailState --> useStatusUpdateDetails : provides actions/selectors
react_redux ..> RevealedDetailsTable : hook dependency
react_i18next ..> MilestoneDashboardExpandedRow : hook dependency
lodash ..> RevealedDetailsTable : utility functions
PropTypes ..> RevealedDetailsTable : prop validation
PropTypes ..> MilestoneDashboardExpandedRow : prop validation
```

> SVG rendering failed for this diagram.
