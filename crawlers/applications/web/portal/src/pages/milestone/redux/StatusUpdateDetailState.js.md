# Diagram: web/portal/src/pages/milestone/redux/StatusUpdateDetailState.js


> Auto-generated by Obscura crawlers

## Diagram 1

```mermaid
classDiagram
    class StatusUpdateModule {
        +mountPoint: "statusUpdateDetails"
        +actionCreators.fetchStatusUpdateDetails(statusUpdateId)
        +selectors.getAllStatusUpdateDetails(state)
        +selectors.getStatusUpdateDetailsForId(id)(state)
        +reducer(state, action)
    }
    class Duck {
        +fetch(url, headers, callback)
        +reducer(state, action)
    }
    class Reducer {
        +initialState = { index: {} }
        +produce(state, action)
    }
    class Selectors {
        +getAllStatusUpdateDetails(state)
        +getStatusUpdateDetailsForId(id)(state)
    }
    class ActionCreators {
        +fetchStatusUpdateDetails(statusUpdateId)
    }
    class Utils {
        +milestonesUrl(...)
        +axiosConfigHeaders()
    }

    StatusUpdateModule --> Duck : buildFetchDuck(STORE_MOUNT_POINT)
    StatusUpdateModule --> Reducer : combines (chainReducers)
    StatusUpdateModule --> ActionCreators
    StatusUpdateModule --> Selectors
    ActionCreators --> Utils : calls milestonesUrl, axiosConfigHeaders
    ActionCreators --> Duck : calls duck.fetch(...)
    Duck --> Reducer : provides reducer to chainReducers
    Reducer --> Selectors : reads state.index
    Selectors --> StatusUpdateModule : exposed selectors
```

> SVG rendering failed for this diagram.

## Diagram 2

```mermaid
flowchart TD
    A[Start: fetchStatusUpdateDetails(statusUpdateId)] --> B[compute url = milestonesUrl(..., statusUpdateId)]
    B --> C[dispatch { type: REQUEST_UPDATE_DETAILS, payload: { statusUpdateId } }]
    C --> D[call duck.fetch(url, axiosConfigHeaders(), responseHandler)]
    D --> E{duck.fetch performs HTTP request}
    E -->|success| F[responseData returned to responseHandler]
    F --> G[dispatch { type: RECEIVE_UPDATE_DETAILS, payload: { statusUpdateId, data: responseData } }]
    G --> H[reducer (REQUEST_UPDATE_DETAILS): draft.index[id] = { loading: true }]
    H --> I[reducer (RECEIVE_UPDATE_DETAILS): draft.index[id] = { loading: false, data }]
    I --> J[selectors reflect updated state]
    J --> K[End]
```

> SVG rendering failed for this diagram.
