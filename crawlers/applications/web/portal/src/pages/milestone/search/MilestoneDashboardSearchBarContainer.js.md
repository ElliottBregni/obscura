# Diagram: web/portal/src/pages/milestone/search/MilestoneDashboardSearchBarContainer.js


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
flowchart LR
  subgraph ReduxSelectors
    A1[getSolutionId(state)]
    A2[getSearchText(state)]
    A3[getSearchCategory(state)]
    A4[getIgnoreSearchCategory(state)]
    A5[getHasSearchCriteriaChanged(state)]
    A6[getTypeaheadOptionsMetadata(state)]
    A7[getExportIdentifier(state)]
    A8[getExportName(state)]
    A9[getIsExporting(state)]
  end

  subgraph SearchBarStateSelectors["SearchBarState.selectors"]
    S1[getSearchText]
    S2[getSearchCategory]
    S3[getIgnoreSearchCategory]
    S4[getTypeaheadOptionsMetadata]
    S5[getHasSearchCriteriaChanged]
    S6[getExportIdentifier]
    S7[getExportName]
    S8[getIsExporting]
  end

  subgraph ActionCreators["SearchBarState.actionCreators"]
    C1[setSearchCategoryForKey(key)]
    C2[setSearchText(text, ignoreValueChange, true)]
    C3[clearSearchText]
    C4[resetSearchBar]
    C5[clearSavedSearch]
    C6[searchEntities(solutionId)]
  end

  subgraph MilestoneDomain["MilestoneDomainDataState.actionCreators"]
    M1[fetchDomainData(solutionId)]
  end

  %% mapStateToProps flow
  A1 -->|maps to| MS_solutionId[solutionId]
  S1 --> A2
  S2 --> A3
  S3 --> A4
  S4 --> A6
  S5 --> A5
  S6 --> A7
  S7 --> A8
  S8 --> A9

  A2 -->|maps to| MS_searchText[searchText]
  A3 -->|maps to| MS_searchCategory[searchCategory]
  A4 -->|maps to| MS_ignoreSearchCategory[ignoreSearchCategory]
  A5 -->|maps to| MS_hasSearchCriteriaChanged[hasSearchCriteriaChanged]
  A6 -->|maps to| MS_typeaheadOptionsMetadata[typeaheadOptionsMetadata]
  A7 -->|maps to| MS_getExportIdentifier[getExportIdentifier]
  A8 -->|maps to| MS_getExportName[getExportName]
  A9 -->|maps to| MS_isExporting[isExporting]

  subgraph MapState["mapStateToProps"]
    MS_solutionId
    MS_searchText
    MS_searchCategory
    MS_ignoreSearchCategory
    MS_hasSearchCriteriaChanged
    MS_typeaheadOptionsMetadata
    MS_getExportIdentifier
    MS_getExportName
    MS_isExporting
  end

  %% mapDispatchToProps flow
  C1 -->|used in| MD_setSearchCategory[setSearchCategory(key) -> dispatch]
  C2 -->|used in| MD_setSearchText[setSearchText(text, ignoreValueChange) -> dispatch]
  C3 -->|used in| MD_clearSearchText[clearSearchText() -> dispatch]
  C4 -->|used in| MD_resetSearchBar[resetSearchBar() -> dispatch]
  C5 -->|used in| MD_clearSavedSearch[clearSavedSearch() -> dispatch]
  C6 -->|used in| MD_searchEntities[searchEntities(solutionId) -> dispatch]
  M1 -->|used in| MD_fetchDomainData[fetchDomainData(solutionId) -> dispatch]

  subgraph MapDispatch["mapDispatchToProps"]
    MD_setSearchCategory
    MD_setSearchText
    MD_clearSearchText
    MD_resetSearchBar
    MD_clearSavedSearch
    MD_searchEntities
    MD_fetchDomainData
  end

  %% Connect to SearchBar
  MapState -->|props| SearchBarComponent[SearchBar Component]
  MapDispatch -->|props| SearchBarComponent
  SearchBarComponent -->|wrapped by| Connect["connect(mapStateToProps, mapDispatchToProps)"]
  Connect -->|export default| Export[Default Export]
```

> SVG rendering failed for this diagram.
