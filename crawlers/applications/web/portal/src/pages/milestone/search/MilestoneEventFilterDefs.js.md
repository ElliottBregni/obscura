# Diagram: web/portal/src/pages/milestone/search/MilestoneEventFilterDefs.js


> Auto-generated by Obscura crawlers

## Diagram 1

```mermaid
classDiagram
    class getTimestampRangeQueryString {
        +queryKey_IGNORED
        +timeRange {from,to}
        +returns string
    }
    class FILTERS {
        +Array<Filter>
    }
    class Filter {
        +queryKey
        +Component?
        +isMulti?
        +label
        +optionsState?
        +optionsGetter?
        +queryBuilder
        +hideFuzzySearch?
        +requiredOrgTypes?
        +hideSelectEmpty?
        +hideSelectAll?
    }
    class AsyncSelectFilterButton
    class DateRangeFilterButton
    class OrganizationType
    class milestoneDashboardLocationsOptionsState
    class getBasicQueryString
    class getBasicQueryStringFilterValuePriority
    class getMultiSelectQueryString
    getTimestampRangeQueryString --> moment : uses
    FILTERS --> Filter : contains
    Filter <|-- AsyncSelectFilterButton
    Filter <|-- DateRangeFilterButton
    Filter --> getBasicQueryString
    Filter --> getBasicQueryStringFilterValuePriority
    Filter --> getMultiSelectQueryString
    Filter --> OrganizationType
    Filter --> milestoneDashboardLocationsOptionsState
```

> SVG rendering failed for this diagram.

## Diagram 2

```mermaid
flowchart TD
    A[Start: build query] --> B{Filter selected?}
    B -- No --> Z[Return empty query string]
    B -- Yes --> C[Identify filter definition in FILTERS]
    C --> D{Has Component?}
    D -- AsyncSelect --> E[Render AsyncSelectFilterButton\nload options from optionsState/optionsGetter]
    D -- DateRange --> F[Render DateRangeFilterButton\nget timeRange input]
    D -- None --> G[Use optionsGetter to select value]
    E --> H[Pass selected value to queryBuilder]
    F --> I[getTimestampRangeQueryString\nif timeRange null -> return \"\"]
    I --> J{timeRange.from?}
    J -- Yes --> K[append &milestoneStartTime=unix(from)]
    J -- No --> L[skip start]
    I --> M{timeRange.to?}
    M -- Yes --> N[append &milestoneEndTime=unix(to)]
    M -- No --> O[skip end]
    K --> P[Return combined qs]
    N --> P
    H --> Q[Other queryBuilders\n(getBasicQueryString / getMultiSelectQueryString / getBasicQueryStringFilterValuePriority)]
    Q --> P
    G --> Q
    P --> R[Final query string appended to request]
```

> SVG rendering failed for this diagram.
