# Diagram: web/portal/src/pages/healthcare/details/Healthcare.Details.page.js


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
graph TD
  HD[HealthcareDetails Component]
  UT[useTranslation("healthcare")]
  ST[useSetTitleOnMount]
  SD[useSetDescriptionOnMount]
  MX[useTrackWithMixpanelOnce("Viewed Page: Healthcare")]
  EF[useEffect - on searchValue change]
  MSR[MapSearchResults]
  SB[HealthcareSearchBarContainer]
  SM[SimpleMap (forcedMapPlatform="MAPSINDOORS")]
  DP[HealthcareDetailsPanel]
  MCT[MapCoordinateType.SELECTED_COORDINATE]
  Props[Props (solutionId, searchValue, fetchAssetDetails, fetchAssetPositionUpdates, clearAsset, clearCoordinatesByType, addCoordinate, updateEntityPositionUpdate, ...)]

  HD -->|reads| Props
  HD --> UT
  HD --> ST
  HD --> SD
  HD --> MX
  HD --> EF
  HD --> MSR
  MSR -->|search slot| SB
  MSR -->|map slot| SM
  MSR -->|results slot| DP
  HD --> MCT

  EF -->|if searchValue truthy| Fetch[fetchAssetDetails(solutionId, searchValue)\nfetchAssetPositionUpdates(solutionId, searchValue)]
  EF -->|else| Clear[clearAsset(); clearCoordinatesByType([MapCoordinateType.SELECTED_COORDINATE])]
  Props -->|provides| Fetch
  Props -->|provides| Clear
  Props -->|passed to DP| DP
  DP -->|uses| Props
  SB -->|props| SBProps[isShowingFilters=false\npreventSearchOnChange=true\nmaxTypeaheadResults=Infinity]
  SM -->|prop| MapProp[forcedMapPlatform="MAPSINDOORS"]

  classDef comp fill:#f9f,stroke:#333,stroke-width:1px;
  class HD,MSR,SB,SM,DP comp
```

> SVG rendering failed for this diagram.
