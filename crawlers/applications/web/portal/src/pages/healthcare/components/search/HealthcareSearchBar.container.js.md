# Diagram: web/portal/src/pages/healthcare/components/search/HealthcareSearchBar.container.js


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
flowchart LR
  ReduxState[Redux State]
  HSBSelectors[HealthcareSearchBarState.selectors\ngetSearchText, getSearchValue, getSearchCategory,\ngetIgnoreSearchCategory, getTypeaheadOptionsMetadata]
  HDSelectors[HealthcareDomainData.selectors\ngetAssets]
  Orgs[getSolutionId\n(modules/organizations/OrganizationsState)]
  mapState[mapStateToProps]
  HSBActions[HealthcareSearchBarState.actionCreators\nsetSearchCategoryForKey, setSearchText, setSearchValue,\nclearSearchText, resetSearchBar, clearSavedSearch,\nsearchEntities, clickedSearchOrEnter]
  HDActions[HealthcareDomainData.actionCreators\nfetchDomainData]
  mapDispatch[mapDispatchToProps]
  Dispatch[dispatch]
  Connect[connect(mapStateToProps, mapDispatchToProps)]
  SearchBar[SearchBar component]
  User[User interactions / UI events]

  ReduxState --> HSBSelectors --> mapState
  ReduxState --> HDSelectors --> mapState
  ReduxState --> Orgs --> mapState
  mapState --> Connect

  HSBActions --> mapDispatch
  HDActions --> mapDispatch
  mapDispatch --> Connect

  Connect --> SearchBar
  User --> SearchBar
  SearchBar -->|calls prop action| mapDispatch
  mapDispatch -->|invokes| Dispatch
  Dispatch -->|routes to| HSBActions
  Dispatch -->|routes to| HDActions
```

> SVG rendering failed for this diagram.
