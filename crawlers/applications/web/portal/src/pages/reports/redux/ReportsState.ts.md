# Diagram: web/portal/src/pages/reports/redux/ReportsState.ts


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
flowchart LR
  subgraph Actions
    FR[fetchReports()] -->|dispatch FETCH_REPORTS| AX1[axios.get(REPORTS_URL)]
    FR -->|then RECEIVE_REPORTS| RR[RECEIVE_REPORTS]
    FRT[fetchReport(reportId)] -->|dispatch FETCH_REPORT| AX2[axios.get(REPORTS_URL/{reportId})]
    FRT -->|then RECEIVE_REPORT| RR
    FF[fetchFilteredReport(reportId, filterSetId)] -->|dispatch FETCH_REPORT| AX3[axios.get(REPORTS_URL/{reportId}/{filterSetId})]
    FF -->|then RECEIVE_REPORT| RR
    SR[saveReport(payload)] -->|dispatch REPORT_SAVING| AX4[axios.post(REPORT_URL)]
    SR -->|then REPORT_SAVED| RS[REPORT_SAVED]
    SFR[saveFilteredReport(reportId,payload)] -->|dispatch REPORT_SAVING| AX5[axios.post(REPORT_URL/{reportId})]
    SFR -->|then REPORT_SAVED| RS
    UR[updateReport(reportId,payload)] -->|dispatch REPORT_UPDATING| AX6[axios.patch(REPORT_URL/{reportId})]
    UR -->|then REPORT_UPDATED| RU[REPORT_UPDATED]
    UFR[updateFilteredReport(reportId,filterSetId,payload)] -->|dispatch REPORT_UPDATING| AX7[axios.patch(REPORT_URL/{reportId}/{filterSetId})]
    UFR -->|then REPORT_UPDATED| RU
    DR[deleteReport(reportId)] -->|dispatch REPORT_DELETING| AX8[axios.delete(REPORT_URL/{reportId})]
    DR -->|then REPORT_DELETED| RD[REPORT_DELETED]
    DFR[deleteFilteredReport(reportId,filterSetId)] -->|dispatch REPORT_DELETING| AX9[axios.delete(REPORT_URL/{reportId}/{filterSetId})]
    DFR -->|then REPORT_DELETED| RD
  end

  subgraph Reducer[ReportsReducer(state, action)]
    direction TB
    S0[initialState]
    FETCH_R[FETCH_REPORTS] -->|sets isLoading=true| S_Loading[isLoading=true]
    RECEIVE_R[RECEIVE_REPORTS] -->|mapReport -> reports, isLoading=false| S_Reports[reports updated]
    FETCH_RP[FETCH_REPORT] -->|sets isLoading=true| S_Loading
    RECEIVE_RP[RECEIVE_REPORT] -->|mapReport -> report[0] or null, isLoading=false| S_Report[report updated]
    SAVING[REPORT_SAVING] -->|isSaving=true| S_Saving[isSaving=true]
    SAVED[REPORT_SAVED] -->|isSaving=false| S_Saved[isSaving=false]
    UPDATING[REPORT_UPDATING] -->|isEditing=true| S_Editing[isEditing=true]
    UPDATED[REPORT_UPDATED] -->|isEditing=false| S_Updated[isEditing=false]
    DELETING[REPORT_DELETING] -->|isDeleting=true| S_Deleting[isDeleting=true]
    DELETED[REPORT_DELETED] -->|isDeleting=false| S_Deleted[isDeleting=false]
  end

  %% Connect actions to reducer cases
  RR -->|action RECEIVE_REPORTS| RECEIVE_R
  RR -->|action RECEIVE_REPORT| RECEIVE_RP
  RS -->|action REPORT_SAVED| SAVED
  RU -->|action REPORT_UPDATED| UPDATED
  RD -->|action REPORT_DELETED| DELETED
  AX1 -->|response data| RR
  AX2 -->|response data| RR
  AX3 -->|response data| RR

  subgraph Selectors
    SRc[getReports(state)] -->|reads| S_Reports
    SS[getReportsSorting(state)] -->|reads| S0
    SG[getReport(state)] -->|reads| S_Report
    SIL[getIsLoading(state)] -->|reads| S_Loading
    SIS[getIsSaving(state)] -->|reads| S_Saving
    SIE[getIsEditing(state)] -->|reads| S_Editing
    SID[getIsDeleting(state)] -->|reads| S_Deleting
    SRI[getSelectedReportId(state)] -->|uses lodash.get| Location[location.payload.reportId]
    SFID[getSelectedFilterSetId(state)] -->|uses lodash.get| Location2[location.payload.filterSetId]
  end

  subgraph Module
    M[ReportsStateModule]
    M -->|mountPoint| MP[reports]
    M -->|actionCreators| Actions
    M -->|selectors| Selectors
    M -->|reducer| Reducer
  end
```

> SVG rendering failed for this diagram.
