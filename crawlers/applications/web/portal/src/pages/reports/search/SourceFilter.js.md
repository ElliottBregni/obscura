# Diagram: web/portal/src/pages/reports/search/SourceFilter.js


> Auto-generated by Obscura crawlers

## Diagram 1

```mermaid
classDiagram
class SourceFilter {
  +onChange(func)
  +value(string)
  -reportSourceOptions
  -t(key):string
}
class SelectFilter <<component>> {
  +placeholder
  +value
  +isClearable
  +options
  +onChange(func)
}
class PropTypes <<library>>
class useTranslation <<hook>>

SourceFilter --> SelectFilter : uses
SourceFilter .. PropTypes : defines propTypes
SourceFilter .. useTranslation : calls
SelectFilter <-- "option objects" : receives
```

> SVG rendering failed for this diagram.

## Diagram 2

```mermaid
flowchart LR
  Props[Props: onChange, value] --> SF[SourceFilter Component]
  SF --> BuildOptions[Build reportSourceOptions]
  BuildOptions --> Select[SelectFilter component]
  Select --> OnChangeHandler[Local onChange handler]
  OnChangeHandler --> PropsOnChange[props.onChange(option?.value)]
  SF -.-> useTranslation[useTranslation('reports')] 
  BuildOptions -->|uses t()| useTranslation
```

> SVG rendering failed for this diagram.
