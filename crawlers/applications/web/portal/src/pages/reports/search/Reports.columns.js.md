# Diagram: web/portal/src/pages/reports/search/Reports.columns.js


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
flowchart TD
  GetColumns[GetColumns(t, reportType, hasReportBuilderPrivilege, hasManageSharedReportsPrivilege, hasManageReportDirectory, isFVAdmin, actionHandler)]
  BuildColumns[Build columns array]
  GetColumns --> BuildColumns
  BuildColumns --> NameCol[Report Name column (accessor: name, Cell shows New badge)]
  BuildColumns --> DescCol[Description column (accessor: description)]
  BuildColumns --> CreatedByDecision{reportType == SHARED_REPORTS?}
  CreatedByDecision -->|yes| CreatedByCol[Created By column]
  CreatedByDecision -->|no| SkipCreatedBy[skip]
  BuildColumns --> SourceDecision{reportType == SHARED_REPORTS OR MY_REPORTS?}
  SourceDecision -->|yes| SourceCol[Source column (fixSource tooltip)]
  SourceDecision -->|no| SkipSource[skip]
  BuildColumns --> LastModDecision{reportType == SHARED_REPORTS OR MY_REPORTS?}
  LastModDecision -->|yes| LastModCol[Last Modified column (localizedDate/time)]
  LastModDecision -->|no| SkipLastMod[skip]
  BuildColumns --> ActionsDecision{reportType != REPORT_BUILDER?}
  ActionsDecision -->|yes| ActionsCol[Actions column -> ActionsCell]
  ActionsDecision -->|no| NoActions[No user actions]

  %% ActionsCell internals
  ActionsCol --> ActionsCellComp[ActionsCell(props.value, props.row)]
  ActionsCellComp --> IsDeletingState[useState: isDeleting]
  ActionsCellComp --> DirCheck{directory?}
  DirCheck -->|yes| DirAllowed{(reportType == CORE && isFVAdmin) OR (reportType == SHARED && (isFVAdmin OR hasManageReportDirectory)) OR (reportType == MY)}
  DirAllowed -->|true| DirButtons[LinkButton: Edit Folder | Delete Folder]
  DirAllowed -->|false| DirNoButtons[No folder actions]
  DirCheck -->|no| NotDirFlow[report (not a directory)]
  NotDirFlow --> ReportPerms{reportType and privileges combinations}
  ReportPerms -->|Edit report condition| EditReport[LinkButton: Edit Report (source==='report_builder' && allowed)]
  ReportPerms -->|Update filters condition| UpdateFilters[LinkButton: Update Filters (filterSet && allowed)]
  ReportPerms -->|Create filtered from core| CreateFilterSet[LinkButton: Create Filtered Report (CORE_REPORTS && createFilterSet)]
  ReportPerms -->|Move| MoveReport[LinkButton: Move Report (allowed types)]
  ReportPerms -->|Send Email / Schedule| EmailActions[LinkButtons: Send Email, Schedule Email (badge if emailScheduleCount>0)]
  ReportPerms -->|Delete variants| DeleteReport[LinkButton: Delete Report / Delete Filtered Report (sets isDeleting and calls actionHandler)]

  %% utility nodes
  SourceCol --> TooltipComp[OverlayTrigger + Tooltip (originalReportName or filterSet)]
  LastModCol --> DateTimeFmt[localizedDateFormatter + localizedTimeFormatter]
  ActionsCellComp --> LoaderNode[Loader shows while !isDeleting]

  style GetColumns fill:#f9f,stroke:#333,stroke-width:1px
  style ActionsCellComp fill:#efe,stroke:#333,stroke-width:1px
```

> SVG rendering failed for this diagram.
