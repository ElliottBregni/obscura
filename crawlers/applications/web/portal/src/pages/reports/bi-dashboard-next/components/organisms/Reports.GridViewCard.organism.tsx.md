# Diagram: web/portal/src/pages/reports/bi-dashboard-next/components/organisms/Reports.GridViewCard.organism.tsx


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
graph LR
  A[ReportsGridViewCard] -->|uses| B(useTranslation)
  A -->|uses| C(useDispatch)
  A -->|uses| D(useReportTags)
  A -->|uses| E(useReportFilterSetTags)
  A -->|uses| F(useReportFilterSetFavorites)
  A -->|uses| G(useReportFavorites)
  A -->|uses| H(useReportScheduleModals)
  A -->|uses| I(useDeleteFilterSet)
  A -->|calls| J[axios.get(getLocalApiUrl(...))]
  A -->|renders| K[Chip]
  A -->|renders| L[Icon]
  A -->|renders| M[Text]
  A -->|renders| N[DialogModal]
  A -->|renders| O[Dropdown]
  A -->|renders| P[AddTagButton]
  A -->|renders| Q[SelectScheduleModal]
  A -->|renders| R[EmailScheduleModal]
  A -->|renders| S[EmailReportModal]
  A -->|renders| T[ReportFilterSetModalContainer]

  subgraph Hooks
    D
    E
    F
    G
    H
    I
  end

  subgraph UI_Elements
    K
    L
    M
    N
    O
    P
    Q
    R
    S
    T
  end

  %% Data and events
  A -->|loads screenshot| J
  J -->|on success| U[Screenshot URL stored in state]
  J -->|on error| V[Screenshot error state]

  %% Actions
  A -->|onTagDelete ->| W[confirmDeleteTag]
  W -->|if FILTER_SET| E
  W -->|else if REPORT| D

  A -->|onFavoriteToggle ->| X[toggleFavorite]
  X -->|if FILTER_SET| F
  X -->|if REPORT| G

  A -->|onCardClick ->| Y[dispatch REPORT_DETAIL / FILTERED_REPORT_DETAIL]
  Y --> C

  %% Modals control
  H -->|provides| Z{schedule modal controls}
  A --> Z
  A -->|open/close modals| Q
  A -->|open/close modals| R
  A -->|open/close modals| S
  A -->|open/close modals| T

  style A fill:#f9f,stroke:#333,stroke-width:2px
  style Hooks fill:#efe,stroke:#333
  style UI_Elements fill:#eef,stroke:#333
```

> SVG rendering failed for this diagram.
