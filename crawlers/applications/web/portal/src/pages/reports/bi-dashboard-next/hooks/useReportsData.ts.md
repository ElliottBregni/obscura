# Diagram: web/portal/src/pages/reports/bi-dashboard-next/hooks/useReportsData.ts


> Auto-generated by Obscura crawlers

## Diagram 1

```mermaid
flowchart LR
  subgraph API
    A[getLocalApiUrl("/reporting/reports")] -->|URL| B[axios.get("/reporting/reports", params)]
    C[getLocalApiUrl("/reporting/reports/distinct-tags")] -->|URL| D[axios.get("/reporting/reports/distinct-tags")]
  end
  subgraph Fetchers
    FR[fetchReports(context)] -->|calls| B
    FT[fetchDistinctTags()] -->|calls| D
    FR -->|returns| PDTO[PaginatedUnifiedReportsDTO]
    FT -->|returns| DT[DistinctTagDTO[]]
  end
  subgraph ReactQuery
    IQ[useInfiniteQuery(queryKey: ["reports", criteria, sortBy, sortDirection], queryFn: fetchReports)] 
    Q[useQuery(queryKey: ["distinctTags"], queryFn: fetchDistinctTags)]
    IQ -->|uses fetchReports| FR
    Q -->|uses fetchDistinctTags| FT
  end
  subgraph Hook
    UR[useReportsData(criteria, sortBy, sortDirection)] -->|calls| IQ
    UR -->|calls| Q
    IQ -->|provides| UI[isLoading, data.pages, fetchNextPage, hasNextPage, isFetchingNextPage, refetch, error, isError]
    Q -->|provides| DTQ[distinctTagsQuery (data, isLoading, error)]
    UI -->|unifiedItems = data.pages.flatMap(page.content)| ITEMS[unifiedItems]
    UR --> ITEMS
    UR --> DT[distinctTags]
  end
```

> SVG rendering failed for this diagram.

## Diagram 2

```mermaid
classDiagram
  class FetchReportsContext {
    <<interface>>
    + queryKey: [string, ReportCriteriaDTO?, string?, string?]
    + pageParam: any
  }
  class PaginatedUnifiedReportsDTO {
    + page: number
    + totalPages: number
    + content: any[]
    + size: number
    + totalElements: number
  }
  class ReportCriteriaDTO {
    + reportFilterSetPairsIn?: any
    + [otherProps]: any
  }
  class DistinctTagDTO {
    + id: string
    + name: string
  }
  FetchReportsContext <|-- QueryFunctionContext
  PaginatedUnifiedReportsDTO <-- PaginatedUnifiedReportsDTO : "returned by fetchReports"
  ReportCriteriaDTO <-- "used in" FetchReportsContext
  DistinctTagDTO <-- "elements of" DistinctTagDTO[]
```

> SVG rendering failed for this diagram.
