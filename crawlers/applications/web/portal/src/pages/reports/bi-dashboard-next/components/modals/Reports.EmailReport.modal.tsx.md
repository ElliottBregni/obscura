# Diagram: web/portal/src/pages/reports/bi-dashboard-next/components/modals/Reports.EmailReport.modal.tsx


> Auto-generated by Obscura crawlers

## Diagram 1

```mermaid
classDiagram
class EmailReportModal {
  +boolean show
  +Function hide()
  +UnifiedReportItemDTO item
  +useSendEmailReport sendEmailMutation
  +EmailFormState data
  +Dispatch<EmailFormAction> dispatch
  +string errorText
  +boolean showError
  +handleSendEmail()
}
class EmailForm {
  +string email
  +string body
  +Dispatch<EmailFormAction> dispatch
}
class FormButton {
  +string actionType
  +Function clickHandler()
  +boolean disabled
  +string label
}
class Modal {
  +string backdrop
  +boolean show
  +Function onHide()
  +Header Header
  +Body Body
  +Footer Footer
}
class UnifiedReportItemDTO
class useSendEmailReport {
  +mutate(payload)
  +reset()
  +boolean isPending
  +boolean isSuccess
  +boolean isError
  +object error
}
class formDataReducer {
  +EmailFormState(state, EmailFormAction) : EmailFormState
}
class validateEmailForm {
  +(data, t) : {error:boolean, errorText:string}
}
class formatEmail {
  +(emails:string) : string[]
}
EmailReportModal --> Modal : renders
EmailReportModal o-- EmailForm : contains
EmailReportModal --> useSendEmailReport : uses
EmailReportModal --> formDataReducer : manages state via
EmailForm --> formDataReducer : dispatches actions to
EmailReportModal --> validateEmailForm : validates input
EmailReportModal --> formatEmail : formats email input
EmailReportModal --> FormButton : renders
EmailForm --|> React.Component
FormButton --|> React.Component
```

> SVG rendering failed for this diagram.

## Diagram 2

```mermaid
flowchart TD
  A[Open Email Report Modal (show=true)] --> B[Initialize useReducer with initialEmailFormState]
  B --> C{User fills form}
  C --> D[Dispatch EMAIL/BODY actions -> formDataReducer updates state]
  D --> E[User clicks Send button -> handleSendEmail]
  E --> F[validateEmailForm(data)]
  F -- invalid --> G[Set errorText & showError -> display Alert]
  F -- valid --> H[formatEmail(data.email) -> payload]
  H --> I[sendEmailMutation.mutate({item, sendTo, body})]
  I --> J{sendEmailMutation result}
  J -- isSuccess --> K[hide() -> close Modal]
  J -- isError --> L[set errorText from error -> setShowError(true) -> show Alert]
  K --> M[resetMutation on modal close resets state]
  L --> M
```

> SVG rendering failed for this diagram.
