# Diagram: web/portal/src/pages/reports/bi-dashboard-next/hooks/useNewlyPublishedNotifications.ts


> Auto-generated by Obscura crawlers

## Diagram 1

```mermaid
classDiagram
class NewlyPublishedNotification {
  +string id
  +string reportNumber
  +string reportName
  +string[] matchingTags
  +string publishedDate
  +boolean read
}
class ApiNotification {
  +number id
  +string type
  +record context
  +string eventTs
  +boolean read
}
class NewlyPublishedApiNotification {
  +number id
  +literal type = "newly_published_report"
  +context {
    string reportNumber
    string reportName
    string[]? matchingTags
    string? auth0Id
    string? message
  }
  +string eventTs
  +boolean read
}
class ApiResponse {
  +ApiNotification[]? data
}
class Functions {
  +fetchNewlyPublishedNotifications()
  +markNotificationAsRead(notificationId)
  +isOlderThan72Hours(dateString)
  +useNewlyPublishedNotifications()
}
ApiResponse --> ApiNotification : contains
NewlyPublishedApiNotification --|> ApiNotification
Functions ..> NewlyPublishedNotification : returns
Functions ..> ApiResponse : parses
Functions ..> NewlyPublishedApiNotification : parses
NewlyPublishedApiNotification "1" --> "1" NewlyPublishedNotification : maps to
```

> SVG rendering failed for this diagram.

## Diagram 2

```mermaid
flowchart LR
  subgraph FetchAndParse
    A[axios.get /notification] --> B[apiResponseSchema.parse(response.data)]
    B --> C[filter type === newly_published_report]
    C --> D[map -> newlyPublishedApiNotificationSchema.parse]
    D --> E[map -> newlyPublishedNotificationSchema.parse]
  end
  subgraph SelectAndAutoRead
    E --> F[useQuery select: data -> unreadNotifications]
    F --> G[filter isOlderThan72Hours -> notificationsToAutoRead]
    G --> H[forEach -> markNotificationAsRead(id)]
    F --> I[recentUnreadNotifications = unread - old]
    I --> J[return recentUnreadNotifications]
  end
  subgraph Dismiss
    K[dismissReportNotifications(notificationIds)] --> L[Promise.all(markNotificationAsRead)]
    L --> M[query.refetch()]
  end
  A --> FetchAndParse
  E --> SelectAndAutoRead
  J -->|provided by hook| useNewlyPublishedNotifications[useNewlyPublishedNotifications hook]
  useNewlyPublishedNotifications --> Dismiss
```

> SVG rendering failed for this diagram.
