# Diagram: web/portal/src/pages/reports/bi-dashboard-next/components/organisms/Reports.ListViewRow.organism.tsx


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
classDiagram
    class ReportsListViewRow {
        +UnifiedReportItemDTO item
        +Interpolation<Theme> css?
        +(onTagClick)?: function
        +string className?
        -reportId: number|null
        -deletingTagId: number|null
        -showDeleteConfirmation: boolean
        -tagToDelete: number|null
        -showActionsDropdown: boolean
        -showFilterSetModal: boolean
        -showDeleteFilterSetConfirmation: boolean
        -showSelectScheduleModal: boolean
        -showEmailScheduleModal: boolean
        -showEmailReportModal: boolean
        -schedules
        -selectedSchedule
        -isLoading: boolean
        +handleRowClick(e)
        +handleDeleteTag(tagId)
        +confirmDeleteTag()
        +confirmDeleteFilterSet()
        +handleFavoriteToggle(e)
        +handleHideEmailReportModal()
        +handleHideSelectScheduleModal()
        +handleHideEmailScheduleModal()
    }

    class HooksAndAPIs {
        +useTranslation()
        +useDispatch()
        +useReportTags()
        +useReportFilterSetTags()
        +useReportFilterSetFavorites()
        +useReportFavorites()
        +useReportScheduleModals()
        +useDeleteFilterSet()
    }

    class ChildComponents {
        +Chip
        +Icon
        +Text
        +AddTagButton
        +DialogModal
        +Dropdown
        +EmailScheduleModal
        +SelectScheduleModal
        +EmailReportModal
        +ReportFilterSetModalContainer
    }

    class EnumsAndTypes {
        +FontSize
        +EmailScheduleDTO
        +UnifiedReportItemDTO
    }

    ReportsListViewRow --> HooksAndAPIs : uses
    ReportsListViewRow --> ChildComponents : renders
    ReportsListViewRow --> EnumsAndTypes : references

    class Tag {
        +id: number
        +tagName: string
        +onDelete()
        +onClick()
    }

    class FavoriteToggle {
        +toggleFilterSetFavorite(id, favorited)
        +toggleReportFavorite(id, favorited)
        +isTogglingFilterSetFavorite
        +isTogglingReportFavorite
    }

    ReportsListViewRow --> Tag : "maps tags to" 
    ReportsListViewRow --> FavoriteToggle : "uses for favorites"

    %% UI actions and flows
    class ActionsDropdown {
        +createFilterSet()
        +editFilterSet()
        +deleteFilterSet()
    }

    ChildComponents --> ActionsDropdown : Dropdown.Item entries
    ActionsDropdown --> ReportFilterSetModalContainer : opens modal
    ActionsDropdown --> ReportsListViewRow : triggers setShowDeleteFilterSetConfirmation

    class ScheduleModals {
        +handleShowScheduleModal()
        +handleShowCreateScheduleModal()
        +handleShowUpdateScheduleModal()
        +showSelectScheduleModal
        +showEmailScheduleModal
        +showEmailReportModal
    }

    HooksAndAPIs --> ScheduleModals : useReportScheduleModals returns
    ReportsListViewRow --> ScheduleModals : calls handlers

    %% Data flows for delete tag/filterset
    ReportsListViewRow ..> "deleteReportTag" : calls when REPORT
    ReportsListViewRow ..> "deleteReportFilterSetTag" : calls when FILTER_SET
    ReportsListViewRow ..> "deleteFilterSetMutation" : mutateAsync(item.id)

    note for ReportsListViewRow "Stateless wrapper with local UI state, uses hooks for data mutations and renders child components"
```

> SVG rendering failed for this diagram.
