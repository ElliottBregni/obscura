# Diagram: web/portal/src/pages/reports/bi-dashboard-next/components/organisms/Reports.AddTagDialog.organism.tsx


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
flowchart LR
  A[AddTagDialog Component]
  A --> B[inputValue (useState)]
  A --> C[tags (useState)]
  A --> D[showError (useState)]
  A --> E[errorMessage (useState)]
  A --> F[inputRef (useRef)]
  A --> G[resolvedEntityType (derived)]
  A --> H[reportTags (useReportTags hook)]
  A --> I[filterSetTagsHooks (useReportFilterSetTags hook)]
  H --> J[reportTags.createMultipleTags()]
  I --> K[createMultipleReportFilterSetTags()]
  G --> L{resolvedEntityType === REPORT?}
  L -- yes --> M[require reportId number]
  L -- no  --> N[require filterSetId number]
  A --> O[isCreatingTag (combined flag)]
  A --> P[createMultipleTagsError (combined)]
  P --> Q[useEffect -> setShowError / setErrorMessage]
  A --> R[addTag(tag)]
  R --> C
  A --> S[removeTag(index)]
  S --> C
  A --> T[handleKeyDown(e)]
  T --> |Enter or comma| R
  T --> |Backspace empty| S
  A --> U[handleInputChange(e)]
  U --> |contains ,| R
  U --> |else| B
  A --> V[handleSubmit(e)]
  V --> |gathers finalTags| Z[prepare tagDataArray]
  Z --> L
  L -- yes & has reportId --> J
  L -- no & has filterSetId --> K
  L -- missing id --> X[console.error required id]
  V --> C[clear tags]
  V --> B[clear inputValue]
  V --> Y[onClose()]
```

> SVG rendering failed for this diagram.
