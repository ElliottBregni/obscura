# Diagram: web/portal/src/pages/reports/bi-dashboard-next/hooks/useSendEmailReport.ts


> Auto-generated by Obscura crawlers

## Diagram 1

```mermaid
classDiagram
class SendEmailReportRequest {
  <<interface>>
  +UnifiedReportItemDTO item
  +string[] sendTo
  +string body
  +string timezone?
}
class SendEmailReportResponse {
  <<interface>>
  +boolean success
  +string message
}
class SendEmailReportError {
  <<interface>>
  +string message
  +number status?
}
class UnifiedReportItemDTO {
  <<external>>
  +string id
  +string? reportId
  +string type
}
class axios {
  <<external>>
  +post(url, body)
  +isAxiosError(error)
}
class createMutationRequestHook {
  <<factory>>
}
class getLocalApiUrl {
  <<function>>
  +getLocalApiUrl(path)
}
SendEmailReportRequest --|> UnifiedReportItemDTO : uses
SendEmailReportError <|-- Error
SendEmailReportResponse <-- sendEmailReport : returns
sendEmailReport ..> axios : calls
sendEmailReport ..> getLocalApiUrl : calls
useSendEmailReport ..> createMutationRequestHook : createdBy
useSendEmailReport ..> sendEmailReport : fetch: sendEmailReport
```

> SVG rendering failed for this diagram.

## Diagram 2

```mermaid
flowchart TD
  A[Start: sendEmailReport(request)] --> B{request.item.type == "FILTER_SET"?}
  B -- Yes --> C[reportId = request.item.reportId]
  B -- No --> D[reportId = request.item.id]
  C --> E[reportFilterSetId = request.item.id]
  D --> F[reportFilterSetId = undefined]
  E --> G[Build requestWithTimezone: {reportId, reportFilterSetId, sendTo, body, timezone}]
  F --> G
  G --> H[Call axios.post(getLocalApiUrl("/reporting/email-schedules/send-email"), requestWithTimezone)]
  H --> I{Request succeeds?}
  I -- Yes --> J[Return response.data (SendEmailReportResponse)]
  I -- No --> K{axios.isAxiosError(error) and error.response?.data?}
  K -- Yes --> L[Create customError with message and status; throw SendEmailReportError]
  K -- No --> M[Re-throw original error]
  J --> Z[End]
  L --> Z
  M --> Z
```

> SVG rendering failed for this diagram.
