# Diagram: web/portal/src/pages/reports/bi-dashboard-next/hooks/useReportFavorites.ts


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
flowchart TD
  A[useReportFavorites hook] --> B[toggleFavorite(reportId, favorited)]
  B --> C{isTogglingFavorite[reportId]?}
  C -- Yes --> D[Return early]
  C -- No --> E[setIsTogglingFavorite(reportId -> true)]
  E --> F[queryClient.setQueriesData -> optimistic update]
  F --> G[Try HTTP request]
  G --> H{favorited?}
  H -- Yes --> I[axios.delete(/reporting/reports/:id/favorite)]
  H -- No --> J[axios.post(/reporting/reports/:id/favorite)]
  I --> K[queryClient.invalidateQueries(reports)]
  J --> K
  G -.error.-> L[Catch: console.error]
  L --> M[queryClient.setQueriesData -> revert optimistic change]
  M --> N[queryClient.invalidateQueries(reports)]
  K --> O[Finally: setIsTogglingFavorite(reportId -> false)]
  N --> O
  D --> O
  O --> P[Return { toggleFavorite, isTogglingFavorite }]
```

> SVG rendering failed for this diagram.
