# Diagram: web/portal/src/pages/reports/bi-dashboard/components/ReportSubFolderDataTable.js


> Auto-generated by Obscura crawlers

## Diagram 1

```mermaid
classDiagram
    class ReportSubFolderDataTable {
        +fetchSubFolderData()
        +getSubFolderByQualifier()
        +directoryId: number
        +reportType: string
        +appliedFilters: array
        +sortColumn: string
        +reverseSort: bool
        +mapFilterSet(report)
        +formattedData[]
    }
    class BaseTable {
        +columns[]
        +data[]
        +isLoading: bool
        +rowClickHandler(row, cell)
    }
    class Hooks {
        +useEffect()
        +useTranslation()
        +useSelector()
    }
    class ColumnsModule {
        +getColumns()
        +treeMiddleAndEndColumn
    }
    ReportSubFolderDataTable --> Hooks : uses
    ReportSubFolderDataTable --> BaseTable : renders
    ReportSubFolderDataTable --> ColumnsModule : imports
    ReportSubFolderDataTable o-- "formattedData" : produces
    ReportSubFolderDataTable : PropTypes defined
    BaseTable <-- ColumnsModule : provides columns
    Hooks ..> "reportList" : useSelector returns
    ReportSubFolderDataTable ..> BaseTable : passes formattedData,isLoading,columns
    ReportSubFolderDataTable : rowClickHandler logic (filterSet -> pushFilteredReportDetailScreen, else -> pushReportDetailScreen)
```

> SVG rendering failed for this diagram.

## Diagram 2

```mermaid
flowchart TD
    Props[Props passed to component] --> Component[ReportSubFolderDataTable]
    Component --> CheckDir{directoryId defined?}
    CheckDir -- Yes --> Fetch[fetchSubFolderData(directoryId, reportType, appliedFilters, sortColumn, reverseSort)]
    CheckDir -- No --> SkipFetch[no fetch]
    Fetch --> Store[reportList in Redux]
    Store --> Selector[useSelector(getSubFolderByQualifier(directoryId))]
    Selector --> ReportListData[reportLisData (array) | isLoading flag]
    ReportListData --> Map[mapFilterSet(report) for reports with filter_set]
    Map --> Format[formattedData = reportLisData.map(...)]
    Format --> BaseTableNode[BaseTable columns/data/isLoading]
    BaseTableNode --> RowClick[on row click]
    RowClick --> ActionCheck{cell.column.id === "user-actions" or "reportId"?}
    ActionCheck -- True --> NoOp[return; no navigation]
    ActionCheck -- False --> DirectoryCheck{row.original.directory?}
    DirectoryCheck -- True --> DirNoNav[return; directory present]
    DirectoryCheck -- False --> FilterSetCheck{filterSetId present?}
    FilterSetCheck -- True --> PushFiltered[pushFilteredReportDetailScreen(reportId, filterSetId)]
    FilterSetCheck -- False --> PushNormal[pushReportDetailScreen(reportId)]
    PushFiltered --> End
    PushNormal --> End
    NoOp --> End
    DirNoNav --> End
```

> SVG rendering failed for this diagram.
