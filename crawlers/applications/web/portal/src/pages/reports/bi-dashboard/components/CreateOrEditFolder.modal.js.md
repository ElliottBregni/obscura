# Diagram: web/portal/src/pages/reports/bi-dashboard/components/CreateOrEditFolder.modal.js


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
flowchart LR
  A[CreateOrEditFolderModal Component] -->|uses| B[useTranslation("reports")]
  A -->|uses| C[validateCreateFolderForm(folderName, t)]
  A -->|renders| D[Modal]
  D --> E[Modal.Header]
  D --> F[Modal.Body]
  D --> G[Modal.Footer]
  F --> H[Alert]
  F --> I[FormGroup: Folder Name]
  F --> J[FormGroup: Description]
  G --> K[FormButton: Cancel]
  G --> L[FormButton: Create/Save]

  A -->|imports| M[ReportTypes]
  A -->|props| P[createFolderModal / editFolderModal]
  A -->|props| Q[createFolderData / editFolderData]
  A -->|props| R[modalReportType, folderCategory, report, show, hide, toggleCreateAndEditModal]

  subgraph State
    S1[folderName]
    S2[folderDesc]
    S3[errorText]
    S4[showError]
    S5[hasRequestedCreate]
    S6[hasRequestedEdit]
  end
  A --> State

  click I "none" "Folder Name input updates folderName"
  click J "none" "Description input updates folderDesc"

  %% Flows
  K ---|click| D5[hide()]
  L ---|click| S7[validateCreateFolderForm -> if error showError=true]
  L ---|click| Cond{toggleCreateAndEditModal?}
  Cond -- Yes --> CallCreate[createFolderModal(folderCategory, folderName, folderDesc)]
  Cond -- No --> CallEdit[editFolderModal(report, folderCategory, folderName, folderDesc)]
  CallCreate -->|then| S5
  CallEdit -->|then| S6

  %% Effects
  S5 & Q -->|on update| RespCreate{createFolderData.isLoadingError?}
  S6 & Q -->|on update| RespEdit{editFolderData.isLoadingError?}
  RespCreate -- true --> SetErrorCreate[setErrorText("Something went wrong"), setShowError(true), setHasRequestedCreate(false)]
  RespCreate -- false -->|success| CloseCreate[modalReportType(folderCategory), hide()]
  RespEdit -- true --> SetErrorEdit[setErrorText("Something went wrong"), setShowError(true), setHasRequestedEdit(false)]
  RespEdit -- false -->|success| CloseEdit[modalReportType(folderCategory), hide()]

  %% Other behaviors
  R.show ---|false triggers| Reset[reset states folderName/folderDesc/showError/hasRequested*]
  R.toggleCreateAndEditModal ---|becomes false and report.directory exists| Prefill[setFolderName(report.directory.name), setFolderDesc(report.directory.description)]

  style A fill:#f9f,stroke:#333,stroke-width:1px
  style State fill:#efe,stroke:#333
  style D fill:#fff,stroke:#333
  style L fill:#ff9,stroke:#333
```

> SVG rendering failed for this diagram.
