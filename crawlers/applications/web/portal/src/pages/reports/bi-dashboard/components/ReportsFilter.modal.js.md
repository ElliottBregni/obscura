# Diagram: web/portal/src/pages/reports/bi-dashboard/components/ReportsFilter.modal.js


> Auto-generated by Obscura crawlers

## Diagram 1

```mermaid
classDiagram
    class ReportsFilterModal {
        +object report
        +bool show
        +func hide()
        +func setShipmentSearchFilter()
        +func clearShipmentSearchFilters()
        +func setFinishedVehicleSearchFilter()
        +func clearFinishedVehicleSearchFilters()
        +func setContainerTrackingSearchFilter()
        +func clearContainerTrackingSearchFilters()
        +object shipmentSearchFilters
        +object finishedVehicleSearchFilters
        +object containerTrackingSearchFilters
        +func fetchDomainDataFIN(solutionId)
        +func fetchDomainDataCT()
        +func fetchLocationDetails(id)
        +func saveFilteredReport(reportId, data)
        +func updateFilteredReport(reportId, filterSetId, data)
        +bool isSaving
        +bool isUpdating
        +string solutionId
        +array locations
        +func modalReportType(type)
        +string reportType
        --
        -filteredReport : {name, saveLocation, description}
        -error : string
        -startedSaving : bool
        -isDisabled : bool
        -prevReportCategory : string
        +inputHandler(value)
        +clearForm()
        +onCancel()
        +onSave()
    }
    class Tabs
    class Modal
    class TextInput
    class SelectInput
    class FormButton
    class AuthenticationUtils
    class buildSearchLocationOptions
    ReportsFilterModal --> Tabs : uses
    ReportsFilterModal --> Modal : uses
    ReportsFilterModal --> TextInput : uses
    ReportsFilterModal --> SelectInput : uses
    ReportsFilterModal --> FormButton : uses
    ReportsFilterModal --> AuthenticationUtils : reads userEmail
    ReportsFilterModal --> buildSearchLocationOptions : calls
```

> SVG rendering failed for this diagram.

## Diagram 2

```mermaid
flowchart TD
    A[Mount ReportsFilterModal] --> B{fetchDomainDataFIN exists?}
    B -- yes --> C[fetchDomainDataFIN(solutionId)]
    B -- no --> D[skip FIN fetch]
    A --> E[fetchDomainDataCT()]
    A --> F[initialize filteredReport -> initFilteredReport]
    G[Prop: report arrives/changes] --> H{report.filterSet?}
    H -- true --> I[setFilteredReport from filterSet]
    I --> J[for each shipment filter]
    J --> K{key is origin/destination?}
    K -- yes --> L[fetchLocationDetails(id) for each id]
    K -- no --> M[setShipmentSearchFilter(key, value)]
    H -- false --> N[setFilteredReport name/description from report]
    O[locations & report.filterSet present] --> P[map locations -> buildSearchLocationOptions -> setShipmentSearchFilter(origin/destination)]
    Q[User clicks Save] --> R[validate name and saveLocation]
    R -- invalid --> S[setError and abort]
    R -- valid --> T[normalize filters (shipmentPayload ids/value)]
    T --> U{report.filterSet exists?}
    U -- true --> V[updateFilteredReport(report.reportId, filterSetId, data)]
    U -- false --> W[saveFilteredReport(report.reportId, data)]
    X[When isSaving/isUpdating toggles] --> Y[startedSaving state toggles]
    Y --> Z[if startedSaving completed and prevReportCategory matches reportType -> modalReportType(reportType) and hide()]
```

> SVG rendering failed for this diagram.
