# Diagram: web/portal/src/pages/reports/bi-dashboard/components/ReportsTable.js


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
classDiagram
class ReportsTable {
  +reportSchedules:Object
  +solutionId:String
  +type:String
  +tableFilterMap:Object
  +filtersRef:Object
  +reportsResults:Array
  +reportsIsLoading:Bool
  +page:int
  +pageSize:int
  +totalPages:int
  +reportsSortColumn:String
  +reportsReverseSort:Bool
  +searchReports()
  +setSearchFilter()
  +clearSearchText()
  +clearSearchFilters()
  +clearSearchFilter()
  +setSort()
  +setPagination()
  +pushReportDetailScreen()
  +pushFilteredReportDetailScreen()
  +applyTableFilters()
  +onFilterChange()
  +debouncedFilterReports()
  +includeScheduleWithReport()
}
class BaseTable {
  +data
  +isLoading
  +columns
  +pagination
  +sorting
  +rowClickHandler()
  +subComponent()
  +filters
}
class ReportSchedulesState {
  <<module>>
  +helpers
}
class getSchedulesKey {
  <<helper>>
  +getSchedulesKey(reportId, filterSetId)
}
class ExpanderTreeSubcomponent {
  +ExpanderTreeSubcomponent(row, filtersRef, sortCol, revSort, getSubFolderByQualifier, fetchSubFolderData, pushReportDetailScreen, pushFilteredReportDetailScreen)
}
class ReportsColumns {
  +getColumns(t,type,hasReportBuilderPrivilege,hasManageSharedReportsPrivilege,hasManageReportDirectory,isFVAdmin,actionHandler)
}
class ReportUtils {
  +expandedReport(filtersRef, reportsResults)
}
class debouncePromise {
  <<lib>>
  +debouncePromise(fn, wait)
}
ReportsTable --> BaseTable : renders
ReportsTable ..> ReportsColumns : calls getColumns
ReportsTable ..> ReportUtils : uses expandedReport
ReportsTable ..> ExpanderTreeSubcomponent : uses as subComponent
ReportsTable ..> debouncePromise : creates debouncedFilterReports
ReportSchedulesState --> getSchedulesKey : exposes
ReportsTable ..> ReportSchedulesState : uses helpers.getSchedulesKey
ReportsTable ..> getSchedulesKey : calls
ReportsTable ..> ExpanderTreeSubcomponent : passes filtersRef and navigation callbacks
BaseTable --> ExpanderTreeSubcomponent : instantiates in subComponent
ReportsTable ..> ReportsColumns : depends on translation and privileges
ReportsTable o-- "filtersRef" : holds current filters
ReportsTable ..> searchReports : triggers on mount and filter changes
ReportsTable ..> setPagination : updates pagination on page change
ReportsTable ..> setSort : updates sort on sort change
```

> SVG rendering failed for this diagram.
