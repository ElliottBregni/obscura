# Diagram: web/portal/src/pages/reports/utils/getLocalApiUrl.ts


> Auto-generated by Obscura crawlers

## Diagram 1

```mermaid
flowchart TD
    A[Call getLocalApiUrl(path)] --> B{env.REACT_APP_USE_LOCAL_REPORTS_API?}
    B -- true --> C[Construct new URL(path, "http://localhost:8500")]
    C --> D[return url.toString()]
    B -- false --> E[call customerApiUrl(path)]
    E --> F[return customerApiUrl result]
    D --> G[End]
    F --> G[End]
```

> SVG rendering failed for this diagram.

## Diagram 2

```mermaid
classDiagram
    class getLocalApiUrl {
      +getLocalApiUrl(path: string): string
    }
    getLocalApiUrl ..> "api-url" : uses
    getLocalApiUrl ..> "env" : reads
```

> SVG rendering failed for this diagram.
