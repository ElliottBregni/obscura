# Diagram: web/portal/src/pages/reports/utils/report.utils.js


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
classDiagram
class mapFilterSet {
  <<function>>
  +filterSetId
  +name
  +description
  +createdBy
  +lastUpdated
  +createdOn
  +private
  +filters
  +isNew
}
class FilterSet {
  +filter_set_id
  +name
  +description
  +email
  +last_updated_at
  +created_at
  +private
  +filters
  +new
}

class mapDirectoryReport {
  <<function>>
  +directoryId
  +name
  +description
  +type
  +level
  +isReport
  +parentId
  +createdAt
  +creatorActorId
  +creatorOrgId
  +updatedAt
}
class Directory {
  +id
  +name
  +description
  +type
  +level
  +parent_id
  +created_at
  +creator_actor_id
  +creator_org_id
  +updated_at
}

class mapReport {
  <<function>>
  +name
  +index
  +accessToken
  +embedUrl
  +reportId
  +description
  +groupName
  +workspaceId
  +lastUpdated
  +private
  +isNew
  +createdBy
  +source
  +originalReportName
  +filterSet
  +directory
  +filtersDetail
  +createFilterSet
  +emailScheduleCount
}
class Report {
  +name
  +token
  +embedUrl
  +report_id
  +description
  +group_name
  +workspace_id
  +last_updated_at
  +private
  +new
  +email
  +source
  +original_report_name
  +filter_set
  +directory
  +filters_detail
  +create_filter_set
  +email_schedule_count
}

class expandedReport {
  <<function>>
  +filters
  +reportsResults
  +returns map(index:boolean) or null
}

mapFilterSet --> FilterSet : reads
mapDirectoryReport --> Directory : reads
mapReport --> Report : reads
mapReport ..> mapFilterSet : calls
mapReport ..> mapDirectoryReport : calls
mapReport --> FilterSet : returns (filterSet)
mapReport --> Directory : returns (directory)
expandedReport --> Report : iterates
expandedReport --> "BooleanMap" : returns

FilterSet <|-- FilterSetData
Directory <|-- DirectoryData
Report <|-- ReportData
```

> SVG rendering failed for this diagram.
