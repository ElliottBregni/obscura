# Diagram: web/portal/src/pages/reports/report-detail/components/ReportsComponent.js


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
classDiagram
class ReportsComponent {
  <<React.Component>>
  +propTypes
  -component
  -rootElement
  -reportEmbedded: bool
  -state: { config, showSaveLocationModal, reportData, isEditMode }
  +constructor(props)
  +componentDidMount()
  +componentDidUpdate(prevProps)
  +componentWillUnmount()
  +embed(config)
  +onLoadedTriggered()
  +onRetrievedFilters(existingFilters, specifiedFilters)
  +onSavedTriggered(event)
  +updateConfig(report)
  +validateConfig(config)
  +render()
}
class PowerBIService {
  +embed(rootElement, config)
  +reset(rootElement)
  +factories
}
class ReportEditDetails {
  <<React.Component>>
  +show
  +onHide()
  +reportData
  +isEditMode
  +saveReport()
  +updateReport()
}
class Privileges {
  +BUILD_REPORT
}
class PbiModels {
  +ViewMode
  +Permissions
  +TokenType
  +validateReportLoad(config)
}
ReportsComponent --> PowerBIService : uses (powerbi.service.Service)
ReportsComponent --> ReportEditDetails : renders
ReportsComponent ..> PbiModels : reads (pbi.models)
ReportsComponent ..> Privileges : checks authorization.hasPrivileges()
PowerBIService ..> PbiModels : provides models and factories
ReportEditDetails <-- ReportsComponent : passes show, reportData, isEditMode, save/update props
```

> SVG rendering failed for this diagram.
