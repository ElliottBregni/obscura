# Diagram: web/portal/src/pages/reports/report-builder/ReportBuilder.page.js


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
classDiagram
    class ReportBuilder {
        +func fetchReports()
        +func pushReportDetailScreen(reportId)
        +reports []
        +isLoading bool
        +useEffect: calls fetchReports()
        +useTrackWithMixpanelOnce("Viewed Page: BI / Report Builder / Dashboard")
        +useSetTitleOnMount(title)
        +useSetDescriptionOnMount(description)
        +render()
    }

    class Loader {
        +prop loaded
    }

    class Section {
        <<styled section>>
    }

    class PanelGroup {
        +prop collapsible
        +Header
        +Content
    }

    class PanelGroupHeader {
        +title
        +titleStyle
        +titleSize
        +iconSize
        +tooltipPosition
        +postTitleIcon
        +postTitleIconTooltip
    }

    class PanelGroupContent {
        +style
    }

    class BaseTable {
        +theme
        +data
        +columns
        +pageSize
        +rowClickHandler(row, cell)
    }

    class Themes {
        <<enum>>
    }

    class getColumns {
        +func getColumns(t, context, a, b)
    }

    class useTranslation {
        +func t(key)
    }

    class useTrackWithMixpanelOnce {
        +func track(event)
    }

    class useSetTitleOnMount {
        +func setTitle(title)
    }

    class useSetDescriptionOnMount {
        +func setDescription(desc)
    }

    class FontSize {
        <<enum>>
        size18
        size16
    }

    ReportBuilder --> Loader : renders
    ReportBuilder --> Section : contains
    ReportBuilder --> PanelGroup : conditionally renders
    PanelGroup --> PanelGroupHeader : has
    PanelGroup --> PanelGroupContent : has
    PanelGroupContent --> BaseTable : contains
    ReportBuilder --> getColumns : calls
    ReportBuilder --> useTranslation : uses
    ReportBuilder --> useTrackWithMixpanelOnce : uses
    ReportBuilder --> useSetTitleOnMount : uses
    ReportBuilder --> useSetDescriptionOnMount : uses
    BaseTable --> Themes : uses
    PanelGroupHeader ..> FontSize : uses
    ReportBuilder ..> PanelGroupHeader : provides props
    BaseTable --> ReportBuilder : invokes rowClickHandler to call pushReportDetailScreen
    ReportBuilder o-- "filtered reports" : reportBuilderReport (reports filtered by groupName == "Report Builder")
```

> SVG rendering failed for this diagram.
