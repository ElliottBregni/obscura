# Diagram: web/portal/src/pages/reports/report-builder/ReportBuilder.page.container.js


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
flowchart TD
  ReportBuilder["ReportBuilder (page)"]
  Connected["connect(mapStateToProps, mapDispatchToProps)"]
  Store["Redux Store"]
  MapState["mapStateToProps(state)"]
  MapDispatch["mapDispatchToProps(dispatch)"]
  ReportsSelectors["ReportsState.selectors"]
  GetReports["getReports(state)"]
  GetIsLoading["getIsLoading(state)"]
  ReportsActions["ReportsState.actionCreators"]
  FetchReportsAction["fetchReports()"]
  TitleActions["TitleState.actionCreators"]
  SetTitle["setTitle(title)"]
  SetDescription["setDescription(description)"]
  PushDetail["dispatch({ type: 'REPORT_DETAIL', payload: { reportId } })"]

  ReportBuilder --> Connected
  Connected --> MapState
  Connected --> MapDispatch
  Store -->|state| MapState
  MapState -->|calls| ReportsSelectors
  ReportsSelectors --> GetReports
  ReportsSelectors --> GetIsLoading
  GetReports -->|returns reports| MapState
  GetIsLoading -->|returns isLoading| MapState

  MapDispatch -->|dispatches| Store
  MapDispatch -->|uses| ReportsActions
  ReportsActions --> FetchReportsAction
  MapDispatch -->|fetchReports() -> dispatch(fetchReports())| FetchReportsAction

  MapDispatch -->|uses| TitleActions
  TitleActions --> SetTitle
  TitleActions --> SetDescription
  MapDispatch -->|setTitle(title) -> dispatch(setTitle)| SetTitle
  MapDispatch -->|setDescription(description) -> dispatch(setDescription)| SetDescription
  MapDispatch -->|pushReportDetailScreen(reportId) -> dispatch(REPORT_DETAIL)| PushDetail

  Connected -->|exports| Exported["Default export: Connected ReportBuilder"]
```

> SVG rendering failed for this diagram.
