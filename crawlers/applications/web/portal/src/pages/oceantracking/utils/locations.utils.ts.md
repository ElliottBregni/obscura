# Diagram: web/portal/src/pages/oceantracking/utils/locations.utils.ts


> Auto-generated by Obscura crawlers

## Diagram 1

```mermaid
classDiagram
    class Location {
        +arrivalTs?: string
        +actualArrival?: string
        +departureTs?: string
        +actualDeparture?: string
        +address: string
        +city: string
        +code: string
        +country: string
        +eta: string|null
        +name: string
        +scheduledArrivalWindow: any[]
        +scheduledDeliveryWindow: any[]
        +scheduledPickupWindow: any[]
        +state: string
        +zipCode: string
    }
    class ContainerDetails {
        +LifecycleState: string
    }
    class Update {
        +id: string
        +datetime: string
        +eventDatetime: string
        +code: string
        +locationName: string
        +locationCode: string
        +latitude: number
        +longitude: number
        +comments: string
        +updateDescription: string
        +eventType: string
        +city: string
        +state: string
        +country: string
        +eventTs: string
        +eventCode: string
    }
    class Data {
        +Triplegs: Object[]
        +updates: Update[]
    }
    class transformLocation {
        +(location: Location, type: string, containerDetails: ContainerDetails) : object|null
    }
    class getStatusByEventAndMode {
        +(milestoneCode: string, mode: string, t: Function) : string
    }
    class getTranslatedSource {
        +(type: string, t: Function) : string|null
    }
    class getLegWithLatestUpdate {
        +(actualTriplegs: any[], latestUpdate: {code:string, locationCode:string}, sortedUpdates: Update[]) : any
    }
    class useCurrentPositionDetails {
        +(data: Data, isShipmentsLoaded: boolean, shipmentsForTriplegs: object) : object|null
    }
    Data "1" o-- "*" Update : updates
    useCurrentPositionDetails ..> Data : reads
    useCurrentPositionDetails ..> getLegWithLatestUpdate : calls
    useCurrentPositionDetails ..> getStatusByEventAndMode : calls
    useCurrentPositionDetails ..> getTranslatedSource : calls
    transformLocation ..> Location : reads
    transformLocation ..> ContainerDetails : reads
```

> SVG rendering failed for this diagram.

## Diagram 2

```mermaid
flowchart TD
    Start([Start]) --> CheckLoaded{isShipmentsLoaded?}
    CheckLoaded -- No --> EndNull1([return null])
    CheckLoaded -- Yes --> GetShipments[Get shipmentActualTripLegs from shipmentsForTriplegs]
    GetActualTriplegs[Get actualTriplegs = getFilteredTriplegsByType(data.Triplegs, "actual")]
    GetShipments --> GetActualTriplegs
    GetActualTriplegs --> HasUpdates{data.updates.length > 0?}
    HasUpdates -- No --> EndNull2([return null])
    HasUpdates -- Yes --> FilterRO[allUpdates = data.updates.filter(code !== "RO")]
    FilterRO --> HasAllUpdates{allUpdates.length > 0?}
    HasAllUpdates -- No --> EndNull3([return null])
    HasAllUpdates -- Yes --> SortUpdates[sortedUpdates = orderBy(allUpdates, "eventDatetime")]
    SortUpdates --> LatestUpdate[latestUpdate = sortedUpdates[last]]
    LatestUpdate --> LegWithLatest[getLegWithLatestUpdate(actualTriplegs, latestUpdate, sortedUpdates)]
    LegWithLatest --> CheckDP{latestUpdate.code === "DP" && shipmentsForTriplegs.hasOwnProperty(legWithLatest.creatorShipmentId)?}
    CheckDP -- Yes --> FindLatestActual[latestActualTripLeg = find shipmentActual matching creator_shipment_id]
    FindLatestActual --> ShipmentLocation[shipmentCurrentLocationDetail = latestActualTripLeg.current_location]
    FindLatestActual --> LatestModeShipment[latestMode = latestActualTripLeg.mode_name]
    ShipmentLocation --> ReturnShipment[getCurrentLegModeAndLocation(latestMode, shipmentCurrentLocationDetail, latestUpdate, "shipmentActual")]
    LatestModeShipment --> ReturnShipment
    CheckDP -- No --> LatestModePart[latestMode = legWithLatestUpdate.mode]
    LatestModePart --> ReturnPart[getCurrentLegModeAndLocation(latestMode, null, latestUpdate, "partViewActual")]
    ReturnShipment --> End([Return Current Position Details])
    ReturnPart --> End
```

> SVG rendering failed for this diagram.
