# Diagram: web/portal/src/pages/oceantracking/redux/OceanTrackingWatchedPackagesSubscriptionState.ts


> Auto-generated by Obscura crawlers

## Diagram 1

```mermaid
classDiagram
    class buildSubscriptionState {
        <<factory>>
        +topic
        +systemType
        +subscriptionType
        +getUrl()
        +getSubscribeeId()
        +getAdditionalRequestConfig(requestType, subscribee)
        +onChange(requestType, data, state)
        +updateUrl
        +fetchSubscriptionOnSuccess
    }

    class OceanTrackingPackagesSubscriptionState {
        +topic: "OceanTrackingWatchedPackages"
        +systemType: "oceanTracking"
        +subscriptionType: "update"
        +getUrl()
        +getSubscribeeId()
        +getAdditionalRequestConfig()
        +onChange()
        +updateUrl: false
        +fetchSubscriptionOnSuccess: false
    }

    class apiUrl {
        +(path): string
    }

    class ProfileState {
        class selectors {
            +getUserPreferences(state)
        }
        class actionCreators {
            +setPreferences(preferences)
        }
    }

    buildSubscriptionState <|-- OceanTrackingPackagesSubscriptionState : created_by
    OceanTrackingPackagesSubscriptionState --> apiUrl : calls getUrl()
    OceanTrackingPackagesSubscriptionState --> ProfileState.selectors : reads userPreferences
    OceanTrackingPackagesSubscriptionState --> ProfileState.actionCreators : returns setPreferences action
```

> SVG rendering failed for this diagram.

## Diagram 2

```mermaid
flowchart TD
    A[onChange(requestType, data, state)] --> B[ProfileState.selectors.getUserPreferences(state)]
    B --> C{userPreferences}
    C --> D[Merge defaultCommunication.partviewAlertMe with data]
    D --> E[preferences (updated)]
    E --> F[ProfileState.actionCreators.setPreferences(preferences)]
    F --> G[Dispatch/Return setPreferences action]
```

> SVG rendering failed for this diagram.
