# Diagram: web/portal/src/pages/oceantracking/redux/OceanTracking.SavedSearchCardsState.js


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
flowchart LR
  SRC[Source Module: OceanTrackingSavedSearchCardsState]
  subgraph Constants
    STORE[STORE_MOUNT_POINT = "oceanTrackingSavedSearchCards"]
    FETCH[FETCH_SAVED_SEARCH_CARD_DATA]
    RECEIVE[RECEIVE_SAVED_SEARCH_CARD_DATA]
    ERROR[ERROR_SAVED_SEARCH_CARD_DATA]
    CANCELMSG["cancelMessage = \"CANCELED\""]
  end
  subgraph Utils
    FORMAT[formatResponse(response)]
    QS[getSavedSearchQueryString(savedSearch)]
    TOKENS[tokens (cancel callbacks)]
  end
  subgraph Actions
    FETCH_ACT[fetchSavedSearchCardData(savedSearch)]
    GET_SEL[getSavedSearchCardData(id)]
  end
  subgraph Network
    AXIOS[axios request -> apiUrl(/partview/app/opensearch/search)]
  end
  subgraph Redux
    DISPATCH[dispatch(actions)]
    REDUCER[reducer(state, action)]
    STORE_STATE[State @ STORE_MOUNT_POINT]
  end
  SRC --> Constants
  SRC --> Utils
  SRC --> Actions
  Actions -->|build qs| QS
  QS -->|returns query string| AXIOS
  Actions -->|manage tokens| TOKENS
  FETCH_ACT -->|dispatch FETCH action| DISPATCH
  FETCH_ACT -->|create axios request| AXIOS
  AXIOS -->|on success| FORMAT
  FORMAT -->|map response| DISPATCH
  AXIOS -->|on error (not canceled)| DISPATCH
  DISPATCH --> REDUCER
  REDUCER --> STORE_STATE
  GET_SEL -->|selector reads| STORE_STATE
  STORE_STATE -->|export| SRC
  SRC -->|export default| OceanTrackingSavedSearchCardsState[OceanTrackingSavedSearchCardsState]
```

> SVG rendering failed for this diagram.
