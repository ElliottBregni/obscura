# Diagram: web/portal/src/pages/oceantracking/components/search/OceanTracking.SearchFilters.container.js


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
flowchart LR
  OTSBS[OceanTrackingSearchBarState]
  OTSBS -->|selectors.getSearchFilters(state)| mapStateToProps
  OTSBS -->|selectors.getHasSearchCriteriaChanged(state)| mapStateToProps
  OTSBS -->|selectors.getCanUserSearch(state)| mapStateToProps
  OTSBS -->|actionCreators| ActionCreators{{actionCreators}}
  subgraph Actions
    setSearchFilter["setSearchFilter"]
    clearSearchFilter["clearSearchFilter"]
    searchEntities["searchEntities"]
    resetSavedSearch["resetSavedSearch"]
    clearSavedSearch["clearSavedSearch"]
  end
  ActionCreators --> setSearchFilter
  ActionCreators --> clearSearchFilter
  ActionCreators --> searchEntities
  ActionCreators --> resetSavedSearch
  ActionCreators --> clearSavedSearch

  FILTERS["FILTERS (OceanTracking.searchOptions)"]
  FILTERS --> mergeProps

  mapStateToProps --> connectFn[connect(mapStateToProps, actionCreators, mergeProps)]
  ActionCreators --> connectFn
  mergeProps --> connectFn
  connectFn --> FilterSection["FilterSection (components/search-bar)"]
  FilterSection -->|default export| Export[Default Export]
  mergeProps -->|adds filtersMetadata: FILTERS| FilterSection
  mapStateToProps -->|provides searchFilters, hasSearchCriteriaChanged, canUserSearch| FilterSection
  ActionCreators -->|dispatch props| FilterSection
```

> SVG rendering failed for this diagram.
