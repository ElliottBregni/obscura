# Diagram: web/portal/src/pages/oceantracking/details/components/organisms/ExceptionTable.organism.js


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
classDiagram
    class ExceptionTable {
        +exceptionList:Array
        +columns
        +render()
    }
    class useColumns {
        +t(key):string
        +getFullTranslatedNameForException(obj):string
        +return columns:Array
    }
    class BaseTable {
        +data
        +columns
        +theme
        +disablePagination
    }
    class TimeCell {
        +dateTime
        +localize:Boolean
    }
    class Text {
        +block:Boolean
        +size
    }
    class Themes {
        <<enumeration>>
        LIGHT
        DARK
    }
    class useTranslation {
        +useTranslation(ns):{t}
    }
    class useOceanTrackingExceptionLabel {
        +getFullTranslatedNameForException(obj):string
    }
    class PropTypes {
        <<library>>
    }
    class ExceptionShape {
        +name:String
        +reasonCode:String
        +comment:String
        +datetime:String
        +eventDatetime:String
        +longitude:Number
        +latitude:Number
        +city:String
        +state:String
        +categoryCode:String
        +resolvedDateTime:String
    }
    class lodash {
        <<library>>
        isNil(value)
    }

    ExceptionTable ..> useColumns : calls
    ExceptionTable --> BaseTable : renders
    ExceptionTable ..> Themes : uses
    useColumns ..> useTranslation : calls
    useColumns ..> useOceanTrackingExceptionLabel : calls
    useColumns ..> Text : uses
    useColumns ..> TimeCell : uses
    useColumns ..> lodash : uses
    ExceptionTable ..|> PropTypes : validates
    PropTypes ..> ExceptionShape : defines
    ExceptionTable o-- ExceptionShape : accepts instances of
```

> SVG rendering failed for this diagram.
