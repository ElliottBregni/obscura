# Diagram: web/portal/src/pages/oceantracking/details/components/organisms/ContainerOrderDetailsTable.organism.js


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
classDiagram
class ContainerOrderDetailsTable {
  +orders: array
  +render()
  +defaultSortColumn = "OrderTimestamp"
  +defaultReverseSort = true
}
class useColumns {
  +results: Column[]
  +t(key): string
}
class Column {
  +Header: string
  +accessor: string
  +Cell(cell): ReactNode
}
class BaseTable {
  +data-qa: string
  +theme: Themes
  +columns: Column[]
  +data: array
  +disablePagination: boolean
  +render()
}
class Text {
  +block: boolean
  +size: FontSize
  +render(children)
}
class TimeCell {
  +dateTime: string
  +localize: boolean
  +render()
}
class Themes {
  <<enumeration>>
  LIGHT
}
class FontSize {
  <<enumeration>>
  size12
}
class PropTypes {
  +arrayOf()
  +shape()
  +string
}
class useTranslation {
  +useTranslation(namespace): { t(key): string }
}

ContainerOrderDetailsTable --> useColumns : "calls"
ContainerOrderDetailsTable --> BaseTable : "renders"
ContainerOrderDetailsTable ..> PropTypes : "defines propTypes"
BaseTable ..> Themes : "uses"
useColumns --> useTranslation : "uses"
useColumns --> Column : "returns 2 columns"
Column --> Text : "Cell -> uses Text (OrderNumber)"
Column --> TimeCell : "Cell -> uses TimeCell (OrderTimestamp)"
Text ..> FontSize : "size"
TimeCell ..> FontSize : "none"
PropTypes --> Column : "validates shape"
```

> SVG rendering failed for this diagram.
