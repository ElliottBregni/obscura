# Diagram: web/portal/src/pages/oceantracking/details/components/organisms/ContainerDetailsTable.organism.js


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
classDiagram
class ContainerDetailsTable {
  +orderPriority: string
  +commodityCode: string
  +shipmentNumberAndLoadCode: array
  +references: array
  +packageDetailsList: array
  +columns: array
  +render(): BaseTable
}
class useColumns {
  +t(): string
  +return: Column[]
}
class Column {
  +Header: string
  +accessor: string
  +Cell(cellInfo): JSX
}
class BaseTable {
  +props: {columns, data, theme, disablePagination}
}
class Text {
  +block: boolean
  +size: FontSize
}
class useTranslation {
  +t(key: string): string
}
class Themes {
  <<enumeration>>
  LIGHT
}
class FontSize {
  <<enumeration>>
  size12
}
class packageDetailsList {
  +items: array
  +push(item)
}
class references {
  +length: number
  +forEach(fn)
}

ContainerDetailsTable --> useColumns : calls
ContainerDetailsTable --> useTranslation : calls
ContainerDetailsTable --> packageDetailsList : builds
ContainerDetailsTable --> BaseTable : renders
useColumns --> Column : returns
Column ..> Text : uses
ContainerDetailsTable ..> Themes : uses
ContainerDetailsTable ..> FontSize : uses
packageDetailsList ..> references : populated from
useTranslation ..> useColumns : used in Headers
useTranslation ..> ContainerDetailsTable : used for labels
```

> SVG rendering failed for this diagram.
