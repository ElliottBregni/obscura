# Diagram: web/portal/src/pages/oceantracking/details/components/organisms/ContainerDetailsPanelGroup.organism.js


> Auto-generated by Obscura crawlers

## Diagram 1

```mermaid
classDiagram
    class ContainerDetailsPanelGroup {
        +trackingNumber
        +type
        +trailerEquipmentNumber
        +status
        +weight
        +weightUnits
        +exceptions
        +watch
        +isSetWatchPackageLoading
        +onWatchChange()
        +subscribeeDetails
        +subscription
        +isSubscriptionLoading
        +fetchPackageDetails()
        +addCoordinate()
        +clearCoordinatesByType()
    }
    class translateStatus {
        +status, t
        +returns translated status
    }
    class useEffect_hook {
        +depends on currentPositionDetails, trackingNumber, addCoordinate
    }
    class formattedExceptions {
        +map(exception) -> {id,type,typeName}
    }
    ContainerDetailsPanelGroup --> translateStatus : uses
    ContainerDetailsPanelGroup --> useEffect_hook : registers
    ContainerDetailsPanelGroup --> formattedExceptions : computes
    ContainerDetailsPanelGroup --> PanelGroup : renders
    ContainerDetailsPanelGroup --> Dropdown : renders
    ContainerDetailsPanelGroup --> WatchToggle : renders
    ContainerDetailsPanelGroup --> ExceptionsBox : renders
    ContainerDetailsPanelGroup --> AlertMeModal : renders
    ContainerDetailsPanelGroup --> DeliverPackageModal : renders
    ContainerDetailsPanelGroup --> DetailPanelGroupItem : renders
    ContainerDetailsPanelGroup --> Icon : renders
    ContainerDetailsPanelGroup --> Text : renders
    ContainerDetailsPanelGroup ..> MapCoordinateType : references
    ContainerDetailsPanelGroup ..> CurrentLocationStatus : references
```

> SVG rendering failed for this diagram.

## Diagram 2

```mermaid
flowchart LR
    A[ContainerDetailsPanelGroup] --> B[PanelGroup]
    B --> BHeader[PanelGroup.Header: Details, Actions]
    BHeader --> Dropdown[Dropdown: Actions]
    Dropdown --> AlertMe[Dropdown.Item -> open AlertMeModal]
    Dropdown --> DeliverItem[Dropdown.Item -> open DeliverPackageModal]
    BHeader --> WatchToggleComp[WatchToggle]
    B --> Content[PanelGroup.Content]
    Content --> LeftCol[FlexColDiv]
    LeftCol --> Tracking[DetailPanelGroupItem: Tracking #]
    LeftCol --> Type[DetailPanelGroupItem: Type]
    LeftCol --> ContainerNum[DetailPanelGroupItem: Container #]
    LeftCol --> Status[DetailPanelGroupItem: Status (translateStatus)]
    LeftCol --> Weight[DetailPanelGroupItem: Weight]
    LeftCol --> CurrentLocationBlock{currentPositionDetails present?}
    CurrentLocationBlock -->|yes| LocationItem[DetailPanelGroupItem: Current Location + Icon(faBox)]
    CurrentLocationBlock -->|yes| SourceItem[DetailPanelGroupItem: Source]
    Content --> RightCol[FlexRowDiv]
    RightCol --> ExceptionsBoxComp[ExceptionsBox(formattedExceptions)]
    A --> AlertMeModalComp[AlertMeModal(show=showAlertMeModal)]
    A --> DeliverPackageModalComp[DeliverPackageModal(show=showDeliverPackageModal)]
    useEffect_hook --> AddCoordinate[addCoordinate(MapCoordinateType.CURRENT_LOCATION_PACKAGE,...)]
    LocationItem --> ToggleMap[Icon click -> addCoordinate / clearCoordinatesByType]
```

> SVG rendering failed for this diagram.
