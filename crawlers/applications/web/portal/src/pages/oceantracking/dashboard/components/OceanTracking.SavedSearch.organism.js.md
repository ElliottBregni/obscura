# Diagram: web/portal/src/pages/oceantracking/dashboard/components/OceanTracking.SavedSearch.organism.js


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
flowchart LR
  OTS[OceanTrackingSavedSearch] --> SSP[SavedSearchPanel]
  OTS --> DC[DonutChart]
  OTS --> ECG[ExceptionCountGroup]
  OTS -->|uses| useTranslation["useTranslation('ocean-tracking')"]
  OTS -->|uses| useDispatch[useDispatch()]
  OTS -->|selects| SelectExceptions[OceanTrackingDashboardState.selectors.getExceptionsListData]
  OTS -->|selects| SelectCard[OceanTrackingSavedSearchCardsState.selectors.getSavedSearchCardData(savedSearch.id)]
  SelectExceptions --> ExceptionsList[exceptionsListData.exceptionList]
  SelectCard --> Counts[counts]
  Counts --> Total[totalCount = counts.active ?? 0]
  Counts --> Delivered[deliveredCount = counts.delivered ?? 0]
  ExceptionsList --> Exceptions[getExceptionsDataForWidget(exceptionsList, counts.exceptions, totalCount)]
  Exceptions --> ChartData[getExceptionChartData(exceptions, totalCount, sumBy(exceptions,'count'))]
  ChartData --> DC
  OTS -->|dispatches| FetchCard[fetchSavedSearchCardData(savedSearch)]
  OTS -->|dispatches| loadSavedSearch[loadSavedSearch(savedSearch)]
  OTS -->|dispatches| searchEntities[searchEntities()]
  OTS -->|dispatches| setSearchFilter[setSearchFilter(key, value)]
  DC -->|onClick| handleActive[handleActiveClick()]
  ECG -->|onClick| handleDelivered[handleDeliveredClick()]
  handleActive --> loadSavedSearch
  handleActive --> checkLifecycle[checkSavedSearchLifeCycleState()]
  checkLifecycle -->|has lifecycleState| setSearchFilter
  checkLifecycle -->|no lifecycleState| setSearchFilterActive[setSearchFilter("lifecycleState", [CREATED_OR_PACKAGED, IN_ROUTE, DELAYED])]
  handleDelivered --> loadSavedSearch
  handleDelivered --> setSearchFilterDelivered[setSearchFilter("lifecycleState", [DELIVERED])]
  style OTS fill:#f9f,stroke:#333,stroke-width:1px
  classDef redux fill:#fffbcc,stroke:#b8860b
  class SelectExceptions,SelectCard,FetchCard,loadSavedSearch,searchEntities,setSearchFilter,checkLifecycle redux
```

> SVG rendering failed for this diagram.
