# Diagram: web/portal/src/pages/error/redux/Error.state.js


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
flowchart TD
  A[createRedirectAction(routeActionType)] --> B[returns (errorMessage) => ...]
  B --> C[returns (dispatch) => ...]
  C --> D[dispatch({ type: routeActionType, payload: { errorMessage } })]
  subgraph Exports
    E[actionCreators]
    F[redirectTo404 = createRedirectAction("NOT_FOUND")]
    G[redirectTo403 = createRedirectAction("ACCESS_FORBIDDEN_ERROR")]
    E --> F
    E --> G
  end
  F -->|calls| A
  G -->|calls| A
```

> SVG rendering failed for this diagram.
