# Diagram: web/portal/src/pages/error/Error.page.container.js


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
classDiagram
    class ErrorView {
        <<React Component>>
    }
    class connect {
        +HOC(mapStateToProps, mapDispatchToProps)
    }
    class mapStateToProps {
        +state
        +location
        +errorMessage
    }
    class mapDispatchToProps {
        +dispatch
        +setTitle(title)
    }
    class TitleState {
        +actionCreators
    }
    class ActionCreators {
        +setTitle(title)
    }
    class AppState {
        +location
    }
    class Location {
        +payload
    }
    class Payload {
        +errorMessage
    }

    connect --> ErrorView : wraps
    connect --> mapStateToProps
    connect --> mapDispatchToProps
    mapStateToProps --> AppState : reads
    AppState --> Location : has
    Location --> Payload : has
    Payload --> "errorMessage" : contains
    mapStateToProps ..> Location : selects location
    mapStateToProps ..> Payload : selects errorMessage
    mapDispatchToProps --> TitleState : uses
    TitleState --> ActionCreators : exposes
    mapDispatchToProps ..> ActionCreators : calls setTitle
    mapDispatchToProps ..> "dispatch" : dispatches setTitle
```

> SVG rendering failed for this diagram.
