# Diagram: web/portal/src/pages/carrierview/search/CarrierView.Search.page.container.js


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
flowchart LR
  CVSearch["CarrierViewSearch<br/>(React Component)"]
  Connect["connect(mapStateToProps, mapDispatchToProps)"]
  CVSearch -->|default export| Connect

  Connect --> MapState["mapStateToProps(state)"]
  Connect --> MapDispatch["mapDispatchToProps(dispatch)"]

  MapState --> AuthSelector["getAuthorization(state)"]
  AuthSelector --> AuthFeatures["Features<br/>(ITSS)"]
  MapState --> Selectors["CarrierViewSearchBarState.selectors"]
  Selectors --> SelectedSaved["getSelectedSavedSearch()"]
  Selectors --> VinResults["getSearchResults()"]
  Selectors --> ShowAdvanced["getShowAdvancedSearch()"]
  Selectors --> IsLoading["getIsLoading() / getIsLoadingError()"]
  Selectors --> Pagination["getPage(), getPageSize(), getTotalPageCountForSearch()"]
  Selectors --> Totals["getTotalCountForSearch(), getTotalCountForSearchIsLoading()"]
  Selectors --> ExportInfo["getExportIdentifier(), getExportName(), getIsExporting(), getExportFailed()"]

  MapDispatch --> DispatchNode["dispatch(action)"]
  MapDispatch --> SavedActions["CarrierViewSavedSearchState.actionCreators"]
  MapDispatch --> SearchBarActions["CarrierViewSearchBarState.actionCreators"]

  DispatchNode -->|loadSavedSearch(savedSearch)| SavedActions
  DispatchNode -->|pushVinViewDetailsPage(internalId)| PushDetails["{ type: CARRIERVIEW_DETAILS, payload }"]
  DispatchNode -->|toggleShowFilters(show)| SearchBarActions
  DispatchNode -->|setPagination(page,pageSize)| SearchBarActions
  DispatchNode -->|setSort(column,reverse)| SearchBarActions
  DispatchNode -->|exportEntities()| SearchBarActions
  DispatchNode -->|clearExportErrors()| SearchBarActions
  DispatchNode -->|resetSearch() -> resetSearchAndFilters() & searchEntities()| SearchBarActions

  style Connect fill:#f9f,stroke:#333,stroke-width:1px
  style MapState fill:#eef,stroke:#333
  style MapDispatch fill:#eef,stroke:#333
  style Selectors fill:#efe,stroke:#333
  style SearchBarActions fill:#ffe,stroke:#333
  style SavedActions fill:#ffe,stroke:#333
```

> SVG rendering failed for this diagram.
