# Diagram: web/portal/src/pages/carrierview/components/search/CarrierView.SearchBar.container.js


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
classDiagram
    class SearchBar {
        <<React Component>>
    }
    class Connect {
        +mapStateToProps
        +actionCreators
    }
    class mapStateToProps {
        +(state) : props
    }
    class actionCreators {
        +setSearchCategory
        +setSearchText
        +clearSearchText
        +resetSearchBar
        +searchEntities
        +clearSavedSearch
        +fetchDomainData
    }
    class SearchBarState {
        class selectors
        class actionCreators
    }
    class CarrierViewDomainDataState {
        class selectors
        class actionCreators
    }
    class AuthorizationSelectors {
        +getAuthorization(state)
    }

    SearchBar <|-- Connect : wrapped by
    Connect o-- mapStateToProps : uses
    Connect o-- actionCreators : uses
    mapStateToProps --> SearchBarState.selectors : reads
    mapStateToProps --> CarrierViewDomainDataState.selectors : reads
    mapStateToProps --> AuthorizationSelectors : calls getAuthorization
    actionCreators --> SearchBarState.actionCreators : forwards set/clear/reset/search actions
    actionCreators --> CarrierViewDomainDataState.actionCreators : forwards fetchDomainData
    actionCreators --> "dispatch wrapper" : setSearchText(text, ignore) dispatches setSearchText(...)
```

> SVG rendering failed for this diagram.
