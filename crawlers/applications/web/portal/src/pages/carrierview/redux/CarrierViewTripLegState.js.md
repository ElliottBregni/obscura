# Diagram: web/portal/src/pages/carrierview/redux/CarrierViewTripLegState.js


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
classDiagram
class Constants {
  +STORE_MOUNT_POINT: "carrierViewTripLeg"
  +RECEIVE_ACTUAL_TRIP_LEG
  +RECEIVE_PLANNED_TRIP_LEG
  +RECEIVE_ACTUAL_TRIP_LEG_PROGRESS_SUCCESS
  +RECEIVE_ACTUAL_TRIP_LEG_PROGRESS_FAILURE
  +FETCH_ACTUAL_TRIP_LEG_PROGRESS
}
class ActionCreators {
  +fetchActualTripLeg(solutionId, entityId)
  +fetchPlannedTripLeg(solutionId, entityId)
  +fetchActualTripLegProgressUpdates(actualTripLegInternalId, internalEntityId)
}
class API {
  +apiUrl(path)
  +axios.get(url, { params })
}
class Selectors {
  +getActualTripLeg(state)
  +getPlannedTripLeg(state)
  +getLegLocationCodeString(leg)
  +getCombinedTripLegs(state)
}
class StateShape {
  +planned: { tripLegs: [] }
  +actual: { tripLegs: [] }
}
class Reducer {
  +CarrierViewTripLegReducer(state, action)
  +handles: RECEIVE_ACTUAL_TRIP_LEG, RECEIVE_PLANNED_TRIP_LEG,
  +FETCH_ACTUAL_TRIP_LEG_PROGRESS, RECEIVE_ACTUAL_TRIP_LEG_PROGRESS_SUCCESS,
  +RECEIVE_ACTUAL_TRIP_LEG_PROGRESS_FAILURE
}
Constants <|-- ActionCreators
ActionCreators --> API : uses
ActionCreators --> Reducer : dispatches actions
API --> ActionCreators : returns responses
Selectors --> StateShape : reads
Reducer --> StateShape : updates
StateShape <-- CarrierViewTripLegState : exposes
class CarrierViewTripLegState {
  +mountPoint
  +actionCreators
  +selectors
  +reducer
}
CarrierViewTripLegState --> ActionCreators
CarrierViewTripLegState --> Selectors
CarrierViewTripLegState --> Reducer
```

> SVG rendering failed for this diagram.
