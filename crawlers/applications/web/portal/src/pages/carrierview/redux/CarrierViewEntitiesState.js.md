# Diagram: web/portal/src/pages/carrierview/redux/CarrierViewEntitiesState.js


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
flowchart TD
  subgraph API["API & URLs"]
    A[getEntityInternalUrl()] -->|calls| ApiEndpoint[/api/entity/internal/ (apiUrl)/]
    B[customerApiUrl("/entity-personalization/.../notes/batch/get")] --> BatchEndpoint[/customer-api/.../notes/batch/get/]
  end

  subgraph Actions["Action Creators"]
    FC[fetchEntityCount()] -->|dispatch REQUEST_ENTITY_COUNT| REQ_COUNT[REQUEST_ENTITY_COUNT]
    FC -->|axios.get(countUrl)| AX1[axios.get(countUrl, config)]
    AX1 -->|then dispatch RECEIVE_ENTITY_COUNT| RCV_COUNT[RECEIVE_ENTITY_COUNT]
    FC -->|catch error| THROW1[throw Error]

    FW[fetchWatchedVins()] -->|dispatch REQUEST_WATCHED_ENTITIES| REQ_WATCH[REQUEST_WATCHED_ENTITIES]
    FW -->|axios.get(url, {params})| AX2[axios.get(url, {params})]
    AX2 -->|map to entityData & axios.post(batchUrl)| AX3[axios.post(BatchEndpoint, {entities})]
    AX3 -->|then transformWatchedVinsResponse| TRANS[transformWatchedVinsResponse(watchedVins,labelData)]
    TRANS -->|dispatch RECEIVE_WATCHED_ENTITIES| RCV_WATCH[RECEIVE_WATCHED_ENTITIES]
    AX3 -->|catch labelError -> dispatch RECEIVE_WATCHED_ENTITIES with raw data| RCV_WATCH
    AX2 -->|catch error -> dispatch RECEIVE_WATCHED_ENTITIES_ERROR| RCV_WATCH_ERR[RECEIVE_WATCHED_ENTITIES_ERROR]

    FP[fetchWatchedEntitiesTotalPages()] -->|dispatch REQUEST_WATCHED_ENTITIES_COUNT| REQ_WATCH_CNT[REQUEST_WATCHED_ENTITIES_COUNT]
    FP -->|axios.get(url,{params,headers})| AX4[axios.get(url,{params,headers})]
    AX4 -->|then dispatch RECEIVE_WATCHED_ENTITIES_COUNT| RCV_WATCH_CNT[RECEIVE_WATCHED_ENTITIES_COUNT]
    AX4 -->|catch error -> dispatch RECEIVE_WATCHED_ENTITIES_COUNT_ERROR| RCV_WATCH_CNT_ERR[RECEIVE_WATCHED_ENTITIES_COUNT_ERROR]

    FD[fetchEntityDeliveredCount()] -->|dispatch REQUEST_DELIVERED_ENTITY_COUNT| REQ_DELIV[REQUEST_DELIVERED_ENTITY_COUNT]
    FD -->|axios.get(countUrl, config)| AX5[axios.get(countUrl, config)]
    AX5 -->|then dispatch RECEIVE_DELIVERED_ENTITY_COUNT| RCV_DELIV[RECEIVE_DELIVERED_ENTITY_COUNT]
    AX5 -->|catch error| THROW2[throw Error]

    UV[updateVinLabel(solutionId,externalId,data,isDelete)] -->|dispatch UPDATING_VIN_LABELING| UPD[UPDATING_VIN_LABELING]
    UV -->|axios.put or axios.delete| AX6[axios.put/delete(url,data)]
    AX6 -->|then dispatch VIN_LABELING_SUCCESS| VIN_OK[VIN_LABELING_SUCCESS]
    AX6 -->|catch -> dispatch VIN_LABELING_ERROR & throw| VIN_ERR[VIN_LABELING_ERROR]

    SV[setWatchedVinsPageIndex(pageIndex)] -->|returns action| SET_IDX[SET_WATCHED_ENTITIES_PAGE_INDEX]
    SR[searchVINs(query)] -->|if short -> return empty| SR_SHORT[options:[]]
    SR -->|axios.get(url,{params})| AX7[axios.get(url,{params})]
    AX7 -->|then buildSearchVinOptions| BUILD[buildSearchVinOptions(data)] --> SR_OK[options:vinOptions]
  end

  subgraph Utils["Utility Functions"]
    TRANSFUNC[transformWatchedVinsResponse(watchedVins,labelData)]
    BUILDFUNC[buildSearchVinOptions(data)]
  end

  subgraph Reducer["CarrierViewEntityReducer (state)"]
    INIT[initialState]
    REQ_COUNT -->|handled| S1[entityCountLoading: true]
    RCV_COUNT -->|handled| S2[entityCount, entityCountLoading:false]
    REQ_DELIV -->|handled| S3[entityDeliveredCountLoading:true]
    RCV_DELIV -->|handled| S4[entityDeliveredCount, entityDeliveredCountLoading:false]
    REQ_WATCH -->|handled| S5[watchedEntitiesLoading:true]
    RCV_WATCH -->|handled| S6[watchedEntities, watchedEntitiesLoading:false]
    REQ_WATCH_CNT -->|handled| S7[watchedEntitiesCountLoading:true]
    RCV_WATCH_CNT -->|handled| S8[watchedEntitiesPageCount, watchedEntitiesCountLoading:false]
    SET_IDX -->|handled| S9[watchedEntitiesPageIndex]
    UPD -->|handled| S10[isUpdatingLabel:true,isSuccess:false,isError:false]
    VIN_OK -->|handled| S11[isUpdatingLabel:false,isSuccess:true,isError:false]
    VIN_ERR -->|handled| S12[isUpdatingLabel:false,isSuccess:false,isError:true]
    RESET[RESET] -->|handled| S13[isUpdatingLabel:false,isSuccess:false,isError:false]
  end

  %% connections between sections
  ApiEndpoint --> AX1
  ApiEndpoint --> AX2
  ApiEndpoint --> AX4
  ApiEndpoint --> AX5
  BatchEndpoint --> AX3
  AX2 --> TRANSFUNC
  AX3 --> TRANSFUNC
  TRANSFUNC --> RCV_WATCH
  BUILDFUNC --> SR_OK

  classDef api fill:#f9f,stroke:#333,stroke-width:1px;
  class ApiEndpoint,BatchEndpoint api
```

> SVG rendering failed for this diagram.
