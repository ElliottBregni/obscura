# Diagram: web/portal/src/pages/carrierview/redux/CarrierViewSearchFilterLoaders.js


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
classDiagram
class AsyncFilterBuilder {
  <<function>> buildAsyncFilterOptionsState()
}
class CarrierViewDomainData {
  <<module>>
  currentPositionCodesUrl()
  productTypesUrl()
}
class currentPositionsCodeOptionsState {
  +topic: "carrierViewCurrentPositionCodeFilter"
  +getUrl(solutionId, state): currentPositionCodesUrl()
  +pageSize: 20
  +getResponseData(data): data?.currentPositionCodes ?? []
  +transformResult(result): { label: result.value, value: result.id }
}
class productTypeOptionsState {
  +topic: "carrierViewProductTypeFilter"
  +getUrl(solutionId, state): productTypesUrl()
  +pageSize: 20
  +getResponseData(data): data?.productTypes ?? []
  +transformResult(option): { label: option, value: option }
}
AsyncFilterBuilder <|-- currentPositionsCodeOptionsState
AsyncFilterBuilder <|-- productTypeOptionsState
CarrierViewDomainData --> currentPositionsCodeOptionsState : currentPositionCodesUrl()
CarrierViewDomainData --> productTypeOptionsState : productTypesUrl()
```

> SVG rendering failed for this diagram.
