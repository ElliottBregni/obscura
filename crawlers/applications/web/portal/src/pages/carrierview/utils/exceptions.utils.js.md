# Diagram: web/portal/src/pages/carrierview/utils/exceptions.utils.js


> Auto-generated by Obscura crawlers

## Diagram 1

```mermaid
flowchart LR
  subgraph getExceptionData
    A[exceptions: Array] --> M[map over exceptions]
    B[exceptionTypes: Array] --> F[forEach exceptionTypes]
    C[entityCount: Number] --> M
    M --> U[updatedExceptions with name and percentage]
    F -->|if reason_code not found in updatedExceptions| P[push missing type with count 0 & percentage 0]
    U --> Q[updatedExceptions (after forEach)]
    P --> Q
    Q --> R[return updatedExceptions]
  end
```

> SVG rendering failed for this diagram.

## Diagram 2

```mermaid
flowchart LR
  subgraph getIconData
    X[exceptionType: String] --> S{switch on exceptionType}
    S -->|Delivered| D[return { type: IconType.FontAwesome, src: faTruckContainer }]
    S -->|default| N[return null]
  end
```

> SVG rendering failed for this diagram.
