# Diagram: web/portal/src/pages/carrierview/utils/carrierViewDetailsTabDefinitions.js


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
flowchart LR
  subgraph Imports
    A1[filteredCombinedExceptions()]
    A2[getDiagnosticsData()]
    A3[getOrderDetails()]
    A4[getVinEquipment()]
  end

  B[getVinTabs(params)] -->|calls| A1
  B -->|calls| A3
  B -->|calls| A4
  B -->|calls| A2
  A1 --> F[filterException]
  A3 --> G[orderDetails]
  A4 --> H[vinEquipment]
  A2 --> I[diagnosticsData]

  F & G & H & I --> J[data object]
  J --> K[vinTabsArray(t, data, connectedCarCoords, showDiagnosticDataTab, showConnectedCarTab, hasTripPlan)]
  K --> L[array: tabs]
  B --> M[returns { array, data }]
  L --> M
  J --> M

  subgraph vinTabsArray
    direction TB
    V1[alwaysShow = true]
    V2[tabs = []]
    V3[addTab(condition, tab)]
    V3 -->|if condition true| V4[tabs.push(tab)]
    V4 --> V2
    V5[Invocations]
    V5 --> V6[hasTripPlan -> Trip Plan tab]
    V5 --> V7[alwaysShow -> Updates tab]
    V5 --> V8[alwaysShow -> Active Exceptions tab]
    V5 --> V9[alwaysShow -> Coordinates tab]
    V5 --> V10[orderDetails.length>0 -> Order Details tab]
    V5 --> V11[alwaysShow -> VIN Details tab]
    V5 --> V12[showConnectedCarTab && connectedCarCoords.length>0 -> Connected Car tab]
    V5 --> V13[showDiagnosticDataTab && diagnosticsData.length>0 -> Diagnostics Data tab]
    V6 --> V3
    V7 --> V3
    V8 --> V3
    V9 --> V3
    V10 --> V3
    V11 --> V3
    V12 --> V3
    V13 --> V3
    V2 -->|returns| L
  end
```

> SVG rendering failed for this diagram.
