# Diagram: web/portal/src/pages/partview/shared/PackageMilestoneCell.tsx


> Auto-generated by Obscura crawlers

## Diagram 1

```mermaid
flowchart LR
  A[PackageMilestoneCell] --> K(useTranslation("partview-search"))
  A --> L(usePartViewMilestone(code))
  A --> B{status ?}
  B -->|yes| C[Text (bold) - "Status: " + status]
  B -->|no| D[no status rendered]
  A --> E{milestone.description ?}
  E -->|yes| F[Text (bold) - milestone.description]
  E -->|no| G[no milestone description]
  A --> H{location.name ?}
  H -->|yes| I[Text (block) - location.name]
  H -->|no| J[no location]
  A --> M{dateTime ?}
  M -->|yes| N[Text (bold) - "Event Time:"]
  N --> O[DateTime component (dateTime, localize, plain, fallback)]
  O --> P[Text (fallback) - "N/A"]
  M -->|no| Q[no dateTime]
```

> SVG rendering failed for this diagram.

## Diagram 2

```mermaid
classDiagram
  class PackageMilestoneCellProps {
    +value: Value
  }
  class Value {
    +code: string
    +location: Location
    +dateTime: string
    +status: string
  }
  class Location {
    +name: string
  }
  class PackageMilestoneCell {
    +render(): JSX
  }
  class Text {
    +props
  }
  class DateTime {
    +dateTime: string
    +localize: boolean
    +plain: boolean
    +className: string
    +fallback: JSX
  }
  class usePartViewMilestone {
    +(code: string): Milestone | undefined
  }
  class useTranslation {
    +(ns?: string): { t(key: string): string }
  }
  PackageMilestoneCellProps --> Value
  Value --> Location
  PackageMilestoneCell --> PackageMilestoneCellProps
  PackageMilestoneCell ..> useTranslation : uses
  PackageMilestoneCell ..> usePartViewMilestone : uses
  PackageMilestoneCell --> Text
  PackageMilestoneCell --> DateTime
```

> SVG rendering failed for this diagram.
