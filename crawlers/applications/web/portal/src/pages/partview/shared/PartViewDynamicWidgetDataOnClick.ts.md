# Diagram: web/portal/src/pages/partview/shared/PartViewDynamicWidgetDataOnClick.ts


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
flowchart TD
  Start([Start]) --> Clear[/"clearSearchFilters()"/]
  Clear --> CheckFilter{filterValues\nexists?}
  CheckFilter -- Yes --> ForLoop[[for each key in\nfilterValues]]
  ForLoop --> SetPerKey["setSearchFilter(key, filterValues[key])"]
  SetPerKey --> ForLoopEnd([done])
  ForLoopEnd --> Search["searchEntities()"]
  CheckFilter -- No --> BuildTable[[tableData = widgetData.tableData.map(item => item.rows).flat()]]
  BuildTable --> Combine[[combinedRowFilters = []\nfor each r in tableData -> push {label: r.label, value: r.code}]]
  Combine --> SetCombined["setSearchFilter(widgetData.dataType, combinedRowFilters)"]
  SetCombined --> Search
  Search --> End([End])
```

> SVG rendering failed for this diagram.
