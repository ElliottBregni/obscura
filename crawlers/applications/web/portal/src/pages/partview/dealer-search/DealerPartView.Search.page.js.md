# Diagram: web/portal/src/pages/partview/dealer-search/DealerPartView.Search.page.js


> Auto-generated by Obscura crawlers

## Diagram 1

```mermaid
classDiagram
    class DealerPartViewSearch {
        +props
        +useTranslation()
        +useSetTitleOnMount()
        +useTrackWithMixpanelOnce()
        +useEffect()
        +rowClickHandler(row,cell)
        +useColumns({isPartSeller: true})
        +return <Search />
    }
    class PropTypes {
        +isLoading: bool
        +loadSavedSearch: func
        +sortColumn: string
        +reverseSort: bool
        +page: number
        +pageSize: number
        +pushPartViewDetailsPage: func
        +savedSearch: object
        +setPagination: func
        +setSort: func
        +resetSearch: func
        +exportEntities: func
        +showFilters: bool
        +toggleShowFilters: func
        +totalEntities: number
        +totalPages: number
        +dealerPartViewSearchResults: array
        +isExporting: bool
        +exportIdentifier: string
        +exportName: string
        +exportFailed: bool
        +clearExportErrors: func
        +resetExport: func
        +selectedFvOrgId: string
        +isDealerOrg: bool
        +loadingError: shape
    }
    class SearchComponent {
        +isLoading
        +searchResults
        +hideSearchResults
        +hideSearchResultsMessage
        +totalCount
        +totalCountIsLoading
        +totalCountTitle
        +savedSearch
        +loadSavedSearch
        +SavedSearchModalContainer
        +resetSearch
        +showFilters
        +toggleShowFilters
        +SearchBarContainer
        +FiltersContainer
        +exportProps
        +tableProps
    }
    DealerPartViewSearch --> PropTypes : accepts
    DealerPartViewSearch --> SearchComponent : renders
    DealerPartViewSearch ..> useColumns : uses
    DealerPartViewSearch ..> useTranslation : uses
    DealerPartViewSearch ..> useSetTitleOnMount : uses
    DealerPartViewSearch ..> useTrackWithMixpanelOnce : tracks
```

> SVG rendering failed for this diagram.

## Diagram 2

```mermaid
flowchart LR
    subgraph Hooks
        A[useTranslation("partview-search")] --> B[useSetTitleOnMount(t("Search Results"))]
        B --> C[useTrackWithMixpanelOnce("Viewed Page: PartView / Dealer Search")]
        C --> D[useEffect -> setSort(defaultSort)]
    end
    subgraph DataInputs
        props[Props object] --> p1[savedSearch]
        props --> p2[dealerPartViewSearchResults]
        props --> p3[isLoading]
        props --> p4[showFilters]
        props --> p5[selectedFvOrgId]
        props --> p6[loadingError]
    end
    subgraph Logic
        L1[determine timeOutMessage] --> L2[updatedPvSearchResults]
        L3[rowClickHandler] --> |if WATCH_COLUMN_ID clicked| L4[return]
        L3 --> |else| L5[pushPartViewDetailsPage(TrackingNumber)]
        columns[useColumns({isPartSeller:true})] --> SearchComp
    end
    subgraph UI
        SearchComp[<Search /> component] --> TableProps
        SearchComp --> ExportProps
    end
    p2 --> L2
    p6 --> L1
    p5 --> SearchComp
    p4 --> SearchComp
    p3 --> SearchComp
    L2 --> SearchComp
    L3 --> SearchComp
    ExportProps[exportProps{exportEntities,...}] --> SearchComp
    TableProps[tableProps{data,columns,isLoading,rowClickHandler,page,pageSize,setPagination,setSort,...}] --> SearchComp
```

> SVG rendering failed for this diagram.
