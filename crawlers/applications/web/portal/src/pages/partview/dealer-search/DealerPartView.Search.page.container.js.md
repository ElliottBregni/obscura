# Diagram: web/portal/src/pages/partview/dealer-search/DealerPartView.Search.page.container.js


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
classDiagram
class DealerPartViewSearch {
}
class connect {
}
class mapStateToProps {
  +savedSearch
  +dealerPartViewSearchResults
  +showFilters
  +isLoading
  +sortColumn
  +reverseSort
  +page
  +pageSize
  +totalPages
  +totalEntities
  +exportIdentifier
  +exportName
  +isExporting
  +exportFailed
  +selectedFvOrgId
  +isDealerOrg
  +loadingError
}
class mapDispatchToProps {
  +loadSavedSearch(savedSearch)
  +pushPartViewDetailsPage(trackingNumber)
  +toggleShowFilters(showFilters)
  +setPagination(page,pageSize)
  +setSort(sortColumn,reverse)
  +exportEntities()
  +clearExportErrors()
  +resetSearch()
  +resetExport()
}
class DealerPartViewSearchBarState {
  <<module>>
  +selectors
  +actionCreators
}
class DealerPartViewSavedSearchState {
  <<module>>
  +actionCreators
}
class Authorization {
  +getAuthorization(state)
  +hasOrganizationTypes(types)
}
class OrganizationType {
  <<enum>>
  +DEALER
}
class defaultSort {
  +column
  +order
}

mapStateToProps ..> DealerPartViewSearchBarState : uses selectors
mapDispatchToProps ..> DealerPartViewSearchBarState : uses actionCreators
mapDispatchToProps ..> DealerPartViewSavedSearchState : uses loadSavedSearch
mapStateToProps ..> Authorization : calls getAuthorization
mapStateToProps ..> OrganizationType : checks DEALER
mapDispatchToProps ..> defaultSort : uses defaultSort for setSort reset
connect o-- mapStateToProps
connect o-- mapDispatchToProps
connect --> DealerPartViewSearch
mapDispatchToProps ..> "DEALER_PARTVIEW_DETAILS action" : dispatches payload
```

> SVG rendering failed for this diagram.
