# Diagram: web/portal/src/pages/partview/dealer-partview-dashboard/DealerPartView.Dashboard.page.container.js


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
classDiagram
  class DealerPartViewDashboard {
  }
  class connect_HOC {
    +mapStateToProps(state)
    +mapDispatchToProps(dispatch)
  }
  DealerPartViewDashboard <|-- connect_HOC

  class mapStateToProps {
    +selectedFvOrgId
    +exceptionsListRequestData
    +watchedTableIsLoading
    +watchedTableResults
    +watchedTablePageIndex
    +watchedTablePageSize
    +watchedTablePageCount
    +isDealerOrg
    +userPreferencesTimeZone
    +pageConfig
    +dynamicWidgetData
    +dynamicWidgetDataIsLoading
  }
  class mapDispatchToProps {
    +fetchWatchedPackages(hasDealerView,fvOrgId)
    +setWatchedTablePageIndex(index)
    +searchEntities(solutionId)
    +setSearchFilter(key,value)
    +clearSearchFilters()
    +resetSearchBar()
    +resetSavedSearch()
    +fetchDynamicWidgetData(dynamicWidgetData,fvOrgId)
  }
  connect_HOC --> mapStateToProps
  connect_HOC --> mapDispatchToProps

  class AuthorizationSelectors {
    +getAuthorization(state)
  }
  class DealerPartViewSearchBarState {
    class selectors {
      +getSelectedFvOrgId(state)
    }
    class actionCreators {
      +searchEntities(solutionId)
      +setSearchFilter(key,value)
      +clearSearchFilters()
      +resetSearchBar()
      +resetSavedSearch()
    }
  }
  class PartViewDashboardState {
    class selectors {
      +getExceptionsListRequestData(state)
      +getWatchedTableIsLoading(state)
      +getWatchedTableResults(state)
      +getWatchedTablePageIndex(state)
      +getWatchedTablePageSize(state)
      +getWatchedTablePageCount(state)
      +getDynamicWidgetData(state)
      +getDynamicWidgetDataIsLoading(state)
    }
    class actionCreators {
      +fetchWatchedPackages(hasDealerView,fvOrgId)
      +setWatchedTablePageIndex(index)
      +fetchDynamicWidgetData(dynamicWidgetData,fvOrgId)
    }
  }
  class ProfileState {
    class selectors {
      +getUserPreferences(state)
    }
  }
  class AppConfigurationState {
    class selectors {
      +getPageConfigForRoute(state)
    }
  }
  class OrganizationType {
    <<enumeration>>
    DEALER
  }

  mapStateToProps ..> AuthorizationSelectors : uses
  mapStateToProps ..> DealerPartViewSearchBarState.selectors : uses
  mapStateToProps ..> PartViewDashboardState.selectors : uses
  mapStateToProps ..> ProfileState.selectors : uses
  mapStateToProps ..> AppConfigurationState.selectors : uses
  mapStateToProps ..> OrganizationType : checks

  mapDispatchToProps ..> DealerPartViewSearchBarState.actionCreators : dispatches
  mapDispatchToProps ..> PartViewDashboardState.actionCreators : dispatches

  AuthorizationSelectors --> OrganizationType : provides auth.hasOrganizationTypes([...])
  ProfileState.selectors --> mapStateToProps : provides timezone
  AppConfigurationState.selectors --> mapStateToProps : provides pageConfig
  PartViewDashboardState.selectors --> mapStateToProps : provides watched & dynamic widget data
  DealerPartViewSearchBarState.selectors --> mapStateToProps : provides selectedFvOrgId

  DealerPartViewDashboard <-- connect_HOC : exportedConnectedComponent
```

> SVG rendering failed for this diagram.
