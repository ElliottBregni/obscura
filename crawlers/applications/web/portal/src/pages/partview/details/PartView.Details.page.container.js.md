# Diagram: web/portal/src/pages/partview/details/PartView.Details.page.container.js


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
classDiagram
    class PartViewDetails {
        +trackingNumber
        +trackingNumberUUID
        +isLoading
        +packageDetails
        +orders
        +parts
        +comments
        +coordinates
        +subscription
        +shipmentComments
        +packageDeliveryEvent
        +auth
        +pageConfig
        +fetchPackageDetails()
        +setWatchPackage()
        +fetchComments()
        +addComment()
        +fetchSubscription()
        +addCoordinate()
        +fetchShipmentComments()
        +setPackageDeliveryEvent()
        +redirectTo404()
    }

    class connect_HOC {
        +mapStateToProps()
        +actionCreators
    }

    class mapStateToProps {
        +returns props mapping from many selectors
    }

    class actionCreators {
        +fetchPackageDetails()
        +setWatchPackage()
        +fetchParts()
        +fetchReferences()
        +fetchShipmentsForTriplegs()
        +clearAllTriplegDetails()
        +clearPackageDetails()
        +fetchComments()
        +addComment()
        +markCommentsRead()
        +updateComment()
        +cancelUpdateComment()
        +cancelAddComment()
        +fetchNotification()
        +addCoordinate()
        +clearCoordinatesByType()
        +clearAllCoordinates()
        +fetchSubscription()
        +updateSubscription()
        +subscribe()
        +unsubscribe()
        +fetchShipmentComments()
        +addShipmentComment()
        +cancelAddShipmentComment()
        +updateShipmentComment()
        +cancelUpdateShipmentComment()
        +markShipmentCommentsRead()
        +addBatchShipmentComments()
        +clearBatchShipmentComments()
        +setPackageDeliveryEvent()
        +resetSetPackageDeliveryEvent()
        +redirectTo404()
        +clearPartViewSearchFilters()
        +clearDealerSearchFilters()
    }

    class PartViewEntityDetailsState {
        <<module>>
        +selectors.getContainerUUID()
        +selectors.getIsLoading()
        +selectors.getPackageDetails()
        +selectors.getOrders()
        +selectors.getParts()
        +selectors.getReferences()
        +selectors.getMeta()
        +selectors.getPartsIsLoading()
        +selectors.getIsShipmentsLoaded()
        +selectors.getShipmentsForTriplegs()
        +selectors.getCoordinates()
        +selectors.getComments()
        +selectors.getIsFetchingComments()
        +actionCreators.*
    }

    class PartViewSubscriptionState {
        <<module>>
        +selectors.getSubscription()
        +selectors.getIsSubscriptionLoading()
        +selectors.getSubscriptionRequestError()
        +actionCreators.*
    }

    class ShipmentsStatusState {
        <<module>>
        +selectors.getComments()
        +selectors.getIsFetchingComments()
        +selectors.getIsBatchCommentInProgress()
        +actionCreators.*
    }

    class MapState {
        <<module>>
        +selectors.getMapCoordinate(type)
        +actionCreators.*
    }

    class NotificationsState {
        <<module>>
        +actionCreators.fetchNotification()
    }

    class AppConfigurationState {
        <<module>>
        +selectors.getIsPageConfigLoading()
        +selectors.getPageConfigForRoute()
    }

    class DomainDataState {
        <<module>>
        +selectors.getShipmentModes()
    }

    class PartViewDeliverPackageState {
        <<module>>
        +selectors.getPackageDeliveryEvent()
        +actionCreators.*
    }

    class ErrorState {
        <<module>>
        +actionCreators.redirectTo404()
    }

    %% connections
    connect_HOC --> PartViewDetails : returns enhanced component
    connect_HOC --> mapStateToProps
    connect_HOC --> actionCreators

    PartViewDetails --> mapStateToProps : receives props
    PartViewDetails --> actionCreators : receives action functions

    mapStateToProps ..> PartViewEntityDetailsState : uses selectors
    mapStateToProps ..> AppConfigurationState : uses selectors
    mapStateToProps ..> MapState : uses selectors
    mapStateToProps ..> PartViewSubscriptionState : uses selectors
    mapStateToProps ..> ShipmentsStatusState : uses selectors
    mapStateToProps ..> DomainDataState : uses selectors
    mapStateToProps ..> PartViewDeliverPackageState : uses selectors
    mapStateToProps ..> ErrorState : (indirect)

    actionCreators ..> PartViewEntityDetailsState : imports actionCreators
    actionCreators ..> PartViewSubscriptionState : imports actionCreators
    actionCreators ..> MapState : imports actionCreators
    actionCreators ..> ShipmentsStatusState : imports actionCreators
    actionCreators ..> PartViewDeliverPackageState : imports actionCreators
    actionCreators ..> NotificationsState : imports fetchNotification
    actionCreators ..> ErrorState : imports redirectTo404
    actionCreators ..> PartViewSearchBarState : imports clearSearchFilters
    actionCreators ..> DealerPartViewSearchBarState : imports clearSearchFilters

    %% grouping
    class PartViewEntityDetailsState,PartViewSubscriptionState,ShipmentsStatusState,MapState,NotificationsState,AppConfigurationState,DomainDataState,PartViewDeliverPackageState,ErrorState,PartViewSearchBarState,DealerPartViewSearchBarState {
        <<redux modules>>
    }
```

> SVG rendering failed for this diagram.
