# Diagram: web/portal/src/pages/partview/dashboard/components/organisms/PartView.BackOrderTable.organism.js


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
classDiagram
    class PartViewBackOrderTable {
        +fetchBackOrderedParts()
        +isLoading: bool
        +backorderParts: array
        +pageIndex: number
        +pageSize: number
        +pageCount: number
        +setPageIndex()
        +searchEntities()
        +setSearchFilter()
        +clearSearchFilters()
        +setSearchText()
        -t(): string
        -columns
        +useEffect(fetchBackOrderedParts, [fetchBackOrderedParts, pageIndex])
    }

    class useColumns {
        +(opts): columns
    }

    class PanelGroup {
        +Header(title)
        +Content(style)
    }

    class BaseTable {
        +data-qa
        +theme
        +columns
        +data
        +isLoading
        +showPagination
        +showPageSizeDropdown
        +isManualPagination
        +pageCount
        +pageSize
        +pageIndex
        +onPageChange(newPage)
    }

    PartViewBackOrderTable --> useColumns : calls
    PartViewBackOrderTable *-- PanelGroup : renders
    PanelGroup o-- PanelGroup.Header : contains
    PanelGroup o-- PanelGroup.Content : contains
    PanelGroup.Content *-- BaseTable : renders
    PartViewBackOrderTable --> BaseTable : supplies props/props mapping
    PartViewBackOrderTable ..> "i18n:t" : uses
    PartViewBackOrderTable ..> fetchBackOrderedParts : depends on (effect)
    BaseTable --> Themes : uses enum/theme
    note right of PartViewBackOrderTable : PropTypes validation enforces required functions and types
```

> SVG rendering failed for this diagram.
