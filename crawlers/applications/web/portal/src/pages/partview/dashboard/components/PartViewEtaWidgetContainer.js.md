# Diagram: web/portal/src/pages/partview/dashboard/components/PartViewEtaWidgetContainer.js


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
flowchart TD
  subgraph Components
    PVW[PartViewEtaWidget]
    CONNECT[connect(mapStateToProps, actionCreators)]
  end

  subgraph StateModules
    PVD[PartViewDashboardState]
    PVS[PartViewSearchBarState]
  end

  subgraph Selectors
    GETETA[getEtaCountsRequestData]
  end

  subgraph ActionCreators
    FETCH[fetchEtaCounts]
    SEARCH[searchEntities]
    SET[setSearchFilter]
    CLEAR[clearSearchFilters]
    RESET[resetSearchBar]
  end

  PVD -->|exports selectors| GETETA
  GETETA -->|used by| mapStateToProps[mapStateToProps(state)]
  mapStateToProps -->|provides prop etaCountsData| CONNECT
  FETCH -->|from PVD.actionCreators| ActionCreators
  PVD -->|exports actionCreators| FETCH
  PVS -->|exports actionCreators| SEARCH
  PVS -->|exports actionCreators| SET
  PVS -->|exports actionCreators| CLEAR
  PVS -->|exports actionCreators| RESET
  SEARCH --> ActionCreators
  SET --> ActionCreators
  CLEAR --> ActionCreators
  RESET --> ActionCreators
  ActionCreators -->|passed to| CONNECT
  CONNECT -->|wraps| PVW
```

> SVG rendering failed for this diagram.
