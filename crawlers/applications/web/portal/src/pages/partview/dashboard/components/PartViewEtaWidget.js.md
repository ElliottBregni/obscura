# Diagram: web/portal/src/pages/partview/dashboard/components/PartViewEtaWidget.js


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
classDiagram
class ShowETADate {
  +value: string
  +render()
}
class PartViewEtaWidget {
  +isLoading: bool
  +data: array
  +handleEtaClick(): void
  +render()
}
class PartViewEtaTableWidget {
  +title: string
  +columns: array
  +loaded: bool
  +chartData: array
  +rowClickHandler(): void
}
class useTranslation {
  +t(key: string): string
}
class convertCountToLocaleString {
  +format(n: number): string
}
class faQuestionCircle
class PropTypes
class React

PartViewEtaWidget --> PartViewEtaTableWidget : uses
PartViewEtaWidget --> ShowETADate : uses (in columns.Cell)
PartViewEtaWidget --> useTranslation : calls
ShowETADate --> useTranslation : calls
PartViewEtaWidget --> convertCountToLocaleString : calls (accessor)
PartViewEtaWidget --> faQuestionCircle : uses (postTitleIcon)
ShowETADate ..|> PropTypes : propTypes(value:string)
PartViewEtaWidget ..|> PropTypes : propTypes(isLoading,bool; data,array; handleEtaClick,func)
PartViewEtaWidget --> React : renders with
ShowETADate --> React : returns JSX
```

> SVG rendering failed for this diagram.
