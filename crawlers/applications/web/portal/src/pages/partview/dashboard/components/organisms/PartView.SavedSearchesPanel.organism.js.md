# Diagram: web/portal/src/pages/partview/dashboard/components/organisms/PartView.SavedSearchesPanel.organism.js


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
classDiagram
    class PartViewSavedSearchesPanel {
        +useSelector(savedSearches)
        +useSelector(getIsSavedSearchLoading)
        +useDispatch()
        +useState(showModal)
        +useState(currentSavedSearch)
        +useState(deletingSavedSearchId)
        +useEffect(fetchSavedSearches)
        +render()
    }

    class SavedSearchesPanel {
        +props: savedSearches
        +props: isLoading
        +props: onAddSavedSearch()
        +props: CardComponent
        +props: getCardProps(savedSearch)
        +props: contentBackgroundColor
    }

    class PartViewSavedSearch {
        +props: savedSearch
        +props: isDeleting
        +props: onEditClick()
    }

    class PartViewEditSavedSearchModalContainer {
        +props: savedSearch
        +props: show
        +props: hide()
        +props: deleteSearch(id)
    }

    class PartViewSavedSearchState {
        class selectors {
            +getSavedSearches()
            +getIsLoading()
        }
        class actionCreators {
            +fetchSavedSearches()
            +loadSavedSearch(savedSearch, flag)
            +deleteSearch(id)
        }
    }

    class PartViewSearchBarState {
        class actionCreators {
            +resetSearchAndFilters(flag)
        }
    }

    class Colors {
        <<enum>>
        +background.WHITE
    }

    PartViewSavedSearchesPanel --> SavedSearchesPanel : renders
    PartViewSavedSearchesPanel --> PartViewEditSavedSearchModalContainer : renders
    SavedSearchesPanel --> PartViewSavedSearch : uses as CardComponent
    PartViewSavedSearchesPanel --> PartViewSavedSearchState.actionCreators : dispatches fetch/load/delete
    PartViewSavedSearchesPanel --> PartViewSavedSearchState.selectors : reads getSavedSearches/getIsLoading
    PartViewSavedSearchesPanel --> PartViewSearchBarState.actionCreators : dispatches resetSearchAndFilters
    PartViewSavedSearchesPanel --> Colors : uses contentBackgroundColor
    PartViewSavedSearchesPanel o-- "state" : showModal
    PartViewSavedSearchesPanel o-- "state" : currentSavedSearch
    PartViewSavedSearchesPanel o-- "state" : deletingSavedSearchId
    SavedSearchesPanel --> Colors : may receive contentBackgroundColor
```

> SVG rendering failed for this diagram.
