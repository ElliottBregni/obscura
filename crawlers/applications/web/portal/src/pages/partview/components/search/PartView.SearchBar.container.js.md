# Diagram: web/portal/src/pages/partview/components/search/PartView.SearchBar.container.js


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
flowchart TD
  SearchBarComponent[SearchBar Component]
  Connect[connect(mapStateToProps, actionCreators)]
  SearchBarStateModule[SearchBarState\n(selectors, actionCreators)]
  PartViewShipmentState[partViewShipmentIdOptionsState\n(selectors, actionCreators)]
  PartViewPartNumberState[partViewPartNumberOptionsState\n(selectors, actionCreators)]
  PartViewOrderNumberState[partViewOrderNumberOptionsState\n(selectors, actionCreators)]
  PartViewPackageState[partViewPackageNumberOptionsState\n(selectors, actionCreators)]
  PartViewBOLState[partViewBillOfLadingOptionsState\n(selectors, actionCreators)]

  Connect -->|wraps| SearchBarComponent
  Connect -->|reads from| SearchBarStateModule
  Connect -->|reads from| PartViewShipmentState
  Connect -->|reads from| PartViewPartNumberState
  Connect -->|reads from| PartViewOrderNumberState
  Connect -->|reads from| PartViewPackageState
  Connect -->|reads from| PartViewBOLState

  subgraph PropsFromState
    searchText["searchText = getSearchText(state)"]
    searchCategory["searchCategory = getSearchCategory(state)"]
    ignoreSearchCategory["ignoreSearchCategory = getIgnoreSearchCategory(state)"]
    typeaheadMeta["typeaheadOptionsMetadata = getTypeaheadOptionsMetadata(state)"]
    hasChanged["hasSearchCriteriaChanged = getHasSearchCriteriaChanged(state)"]
    canUserSearch["canUserSearch = getCanUserSearch(state)"]
    shipmentResults["shipmentIdOptions = selectors.getResults(state)"]
    shipmentLoading["isShipmentIdOptionsLoading = selectors.getIsLoading(state)"]
    partResults["partNumberOptions = selectors.getResults(state)"]
    partLoading["isPartNumberOptionsLoading = selectors.getIsLoading(state)"]
    orderResults["orderNumberOptions = selectors.getResults(state)"]
    orderLoading["isOrderNumberOptionsLoading = selectors.getIsLoading(state)"]
    packageResults["packageNumberOptions = selectors.getResults(state)"]
    packageLoading["isPackageNumberOptionsLoading = selectors.getIsLoading(state)"]
    bolResults["billOfLadingOptions = selectors.getResults(state)"]
    bolLoading["isBillOfLadingOptionsLoading = selectors.getIsLoading(state)"]
  end

  SearchBarComponent -->|receives| PropsFromState

  subgraph ActionCreators
    setSearchCategory["setSearchCategoryForKey -> setSearchCategory"]
    setSearchTextAC["setSearchText(text, ignore) -> dispatch(setSearchText(...))"]
    clearSearchText["clearSearchText"]
    resetSearchBar["resetSearchBar"]
    searchEntities["searchEntities"]
    clearSavedSearch["clearSavedSearch"]
    fetchShipment["fetchShipmentIdOptions(query,max) -> dispatch(partViewShipmentIdOptionsState.fetch)"]
    fetchPart["fetchPartNumberOptions(query,max) -> dispatch(partViewPartNumberOptionsState.fetch)"]
    fetchOrder["fetchOrderNumberOptions(query,max) -> dispatch(partViewOrderNumberOptionsState.fetch)"]
    fetchPackage["fetchPackageNumberOptions(query,max) -> dispatch(partViewPackageNumberOptionsState.fetch)"]
    fetchBOL["fetchBillOfLadingOptions(query,max) -> dispatch(partViewBillOfLadingOptionsState.fetch)"]
  end

  Connect -->|injects| ActionCreators
  setSearchCategory --> SearchBarComponent
  setSearchTextAC --> SearchBarComponent
  clearSearchText --> SearchBarComponent
  resetSearchBar --> SearchBarComponent
  searchEntities --> SearchBarComponent
  clearSavedSearch --> SearchBarComponent
  fetchShipment --> SearchBarComponent
  fetchPart --> SearchBarComponent
  fetchOrder --> SearchBarComponent
  fetchPackage --> SearchBarComponent
  fetchBOL --> SearchBarComponent

  PartViewShipmentState -->|provides fetch| fetchShipment
  PartViewPartNumberState -->|provides fetch| fetchPart
  PartViewOrderNumberState -->|provides fetch| fetchOrder
  PartViewPackageState -->|provides fetch| fetchPackage
  PartViewBOLState -->|provides fetch| fetchBOL

  SearchBarStateModule -->|provides selectors| PropsFromState
  PartViewShipmentState -->|provides selectors| shipmentResults
  PartViewShipmentState -->|provides selectors| shipmentLoading
  PartViewPartNumberState -->|provides selectors| partResults
  PartViewPartNumberState -->|provides selectors| partLoading
  PartViewOrderNumberState -->|provides selectors| orderResults
  PartViewOrderNumberState -->|provides selectors| orderLoading
  PartViewPackageState -->|provides selectors| packageResults
  PartViewPackageState -->|provides selectors| packageLoading
  PartViewBOLState -->|provides selectors| bolResults
  PartViewBOLState -->|provides selectors| bolLoading
```

> SVG rendering failed for this diagram.
