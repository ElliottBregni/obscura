# Diagram: web/portal/src/pages/partview/components/search/PartView.SearchFilters.container.js


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
classDiagram
    class PartViewSearchBarState {
        +selectors
        +actionCreators
    }
    class Selectors {
        +getSearchFilters(state)
        +getHasSearchCriteriaChanged(state)
        +getAreAllPrerequisiteFiltersSelected(state)
        +getCanUserSearch(state)
    }
    class ActionCreators {
        +setSearchFilter()
        +clearSearchFilter()
        +searchEntities()
        +resetSavedSearch()
        +clearSavedSearch()
    }
    class FilterSection {
        <<React Component>>
    }
    class mapStateToProps {
        +(state) => {searchFilters, hasSearchCriteriaChanged, areAllPrerequisiteFiltersSelected, canUserSearch}
    }
    class mergeProps {
        +(stateProps, dispatchProps, ownProps) => mergedProps
    }
    class FILTERS {
        <<constant>>
    }
    class connect {
        +connect(mapStateToProps, actionCreators, mergeProps)(FilterSection)
    }

    PartViewSearchBarState --> Selectors : exposes
    PartViewSearchBarState --> ActionCreators : exposes
    Selectors ..> mapStateToProps : used by
    ActionCreators ..> connect : passed as
    mapStateToProps --> connect : passed as
    mergeProps --> connect : passed as
    FILTERS --> mergeProps : injected as filtersMetadata
    connect --> FilterSection : returns connected component
    connect --> "Export" : default export of connected FilterSection
```

> SVG rendering failed for this diagram.
