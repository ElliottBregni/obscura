# Diagram: web/portal/src/pages/partview/components/search/PartView.EditSavedSearchModal.container.js


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
classDiagram
    class EditSavedSearchModal {
    }
    class SearchBarContainer {
    }
    class FilterSectionContainer {
    }
    class PartViewSearchBarState {
        <<module>>
        +getSearchFilters(state)
        +getSearchCategory(state)
        +getSearchText(state)
    }
    class PartViewSavedSearchState {
        <<module>>
        +saveSearch(payload)
        +editSearch(payload)
    }
    class mapStateToProps {
        +searchFilters
        +searchCategory
        +searchText
        +SearchBarContainer
        +FilterSectionContainer
    }
    class actionCreators {
        +saveSearch
        +editSearch
    }
    class connect{{HOC}} {
        +(mapStateToProps, actionCreators)(Component)
    }

    mapStateToProps ..> PartViewSearchBarState : uses selectors
    mapStateToProps --> SearchBarContainer : injects prop
    mapStateToProps --> FilterSectionContainer : injects prop
    actionCreators ..> PartViewSavedSearchState : re-exports actions
    connect ..> mapStateToProps
    connect ..> actionCreators
    connect --> EditSavedSearchModal : wraps component
    EditSavedSearchModal ..|> connect : enhanced by
```

> SVG rendering failed for this diagram.
