# Diagram: web/portal/src/pages/partview/utils/watchedPackageUtils.js


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
flowchart LR
  A[searchResult (list)] --> B[map over results]
  C[watchedPackageList (list)] --> D[filter to matching packageID]
  B --> E{for each result}
  E -->|compute finalResult| D
  D --> F{finalResult === result.id?}
  F -- Yes --> G[return {...result, Watched: true}]
  F -- No --> H[return result]
  G --> I[collect into new list]
  H --> I
  I --> J[updatedWatchedPackages output]
```

> SVG rendering failed for this diagram.
