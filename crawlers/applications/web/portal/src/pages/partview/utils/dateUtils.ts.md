# Diagram: web/portal/src/pages/partview/utils/dateUtils.ts


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
flowchart TD
  A[Start: getFormattedDate(value, numberOfAddDay=0, convertToutc=false, dateFormat="YYYY-MM-DD", startOf?)] --> B[Set currentDate = value]
  B --> C{numberOfAddDay > 0?}
  C -- Yes --> D[currentDate.add(numberOfAddDay, "days")]
  C -- No --> E[No change]
  D --> F{startOf provided?}
  E --> F
  F -- Yes --> G[currentDate.startOf("day")]
  F -- No --> H[No change]
  G --> I{convertToutc?}
  H --> I
  I -- Yes --> J[currentDate = value.utc()]
  I -- No --> K[No change]
  J --> L[return currentDate.format(dateFormat)]
  K --> L
  L --> Z[End]
```

> SVG rendering failed for this diagram.
