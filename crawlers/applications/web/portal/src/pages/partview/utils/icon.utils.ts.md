# Diagram: web/portal/src/pages/partview/utils/icon.utils.ts


> Auto-generated by Obscura crawlers

## Diagram 1

```mermaid
classDiagram
class filterIconDefinitions {
  +Record<string,IconDefinition> filter(obj)
  +filters entries with "icon" property
}
class ICON_REGISTRY {
  <<constant>>
  +Record<string,IconDefinition>
}
class getIconData {
  +{ type: IconType; src: IconDefinition; color: string } | null getIconData(code, color)
}
class Colors
class IconType
class IconDefinition
class light
class solid

filterIconDefinitions --> IconDefinition : returns entries
ICON_REGISTRY <-- filterIconDefinitions : populated from light and solid
getIconData --> ICON_REGISTRY : looks up code
getIconData --> IconType : returns FontAwesome
getIconData --> Colors : uses DEFAULT
light --|> ICON_REGISTRY
solid --|> ICON_REGISTRY
```

> SVG rendering failed for this diagram.

## Diagram 2

```mermaid
flowchart LR
InputCode(("Input: code, optional color")) --> Lookup{Lookup ICON_REGISTRY[code]}
Lookup -- not found --> ReturnNull[/"Return null"/]
Lookup -- found --> BuildObj[Build result object]
BuildObj --> SetType["type: IconType.FontAwesome"]
BuildObj --> SetSrc["src: matching IconDefinition"]
BuildObj --> SetColor["color: supplied color or Colors.exceptions.DEFAULT"]
BuildObj --> ReturnObj[/"Return object"/]
ReturnNull --> End((End))
ReturnObj --> End((End))
```

> SVG rendering failed for this diagram.
