# Diagram: web/portal/src/pages/partview/utils/milestone.utils.ts


> Auto-generated by Obscura crawlers

## Diagram 1

```mermaid
classDiagram
    class PartViewMilestoneCode {
      <<enumeration>>
      ARRIVED: "AR"
      DEPARTED: "DP"
      DELIVERED: "FD"
      REOPEN: "RO"
      AVAILABLE_FOR_PICKUP: "APU"
    }
    class Location {
      +string address
      +string code
      +string country
      +string name
      +string state
    }
    class LastMilestone {
      +string datetime
      +string eventCode
      +Location location
    }
    class LatestUpdate {
      +string code
      +Location location
      +string dateTime
      +string status
    }
    class getDescriptionFromMilestoneCode {
      +(code: string, locationName: string) : string|null
    }
    class usePartViewMilestone {
      +(code: string) : {description: string|null}
    }
    class getRecentLastUpdate {
      +(lastMilestone: LastMilestone, lastUpdate: LastMilestone, lifecycleState: string) : LatestUpdate|null
    }
    PartViewMilestoneCode --|> getDescriptionFromMilestoneCode : uses
    PartViewMilestoneCode --|> usePartViewMilestone : uses
    LastMilestone --> getRecentLastUpdate
    LatestUpdate <-- getRecentLastUpdate
    Location --> LastMilestone
    Location --> LatestUpdate
```

> SVG rendering failed for this diagram.

## Diagram 2

```mermaid
flowchart TD
    A[Start: getRecentLastUpdate] --> B{lastMilestone?.datetime?}
    B -- No --> C{lastUpdate?.datetime?}
    C -- No --> D[Return latestMilestone (null values allowed)]
    C -- Yes --> E[Return latestUpdate]
    B -- Yes --> F{lastUpdate?.datetime?}
    F -- No --> G[Return latestMilestone]
    F -- Yes --> H[Compute timeDifference = getDifferenceBetweenTimestamps(lastMilestone.datetime, lastUpdate.datetime)]
    H --> I{timeDifference > 0?}
    I -- Yes --> J[Return latestMilestone]
    I -- No --> K[Return latestUpdate]
    D --> L[End]
    E --> L
    G --> L
    J --> L
    K --> L
```

> SVG rendering failed for this diagram.
