# Diagram: web/portal/src/pages/partview/search/PartView.Search.page.js


> Auto-generated by Obscura crawlers

## Diagram 1

```mermaid
classDiagram
class PartViewSearch {
  +props
  +useTranslation()
  +useSetTitleOnMount()
  +useTrackWithMixpanelOnce()
  +useEffect()
  +rowClickHandler(row, cell)
  +columns
  +render()
}
class Search {
  +isLoading
  +searchResults
  +totalCount
  +savedSearch
  +SavedSearchModalContainer
  +SearchBarContainer
  +FiltersContainer
  +exportProps
  +tableProps
}
class Props {
  +isLoading: bool
  +loadSavedSearch: func
  +sortColumn: string
  +reverseSort: bool
  +page: number
  +pageSize: number
  +pushPartViewDetailsPage: func
  +savedSearch: object
  +setPagination: func
  +setSort: func
  +resetSearch: func
  +exportEntities: func
  +showFilters: bool
  +toggleShowFilters: func
  +totalEntities: number
  +totalEntitiesIsLoading: bool
  +totalPages: number
  +partViewSearchResults: array
  +isExporting: bool
  +exportIdentifier: string
  +exportName: string
  +exportFailed: bool
  +clearExportErrors: func
  +resetExport: func
  +loadingError: object
}
PartViewSearch --> Props : accepts
PartViewSearch "1" o-- "1" Search : renders
PartViewSearch ..> "useColumns" : uses
PartViewSearch ..> "useTranslation" : uses
PartViewSearch ..> "useSetTitleOnMount" : uses
PartViewSearch ..> "useTrackWithMixpanelOnce" : uses
PartViewSearch ..> "useEffect" : uses
```

> SVG rendering failed for this diagram.

## Diagram 2

```mermaid
flowchart TD
  A[PartViewSearch Component] --> B[useTranslation("partview-search")]
  A --> C[useSetTitleOnMount(t("Search Results"))]
  A --> D[useTrackWithMixpanelOnce("Viewed Page: PartView / Search")]
  A --> E[useColumns({ isPartSeller: false })]
  A --> F[useEffect -> setSort(defaultSort.column, defaultSort.order)]
  A --> G[rowClickHandler(row, cell)]
  G -->|if cell.column.id === WATCH_COLUMN_ID| H[return (no navigation)]
  G -->|else| I[pushPartViewDetailsPage(row.original.TrackingNumber)]
  A --> J[compute timeOutMessage from loadingError]
  A --> K[updatedPvSearchResults = loadingError.status===408 ? [] : partViewSearchResults]
  A --> L[Render Search component with props]
  L --> M[exportProps, tableProps, SavedSearchModalContainer, SearchBarContainer, FiltersContainer]
  L --> N[tableProps.rowClickHandler = rowClickHandler]
  L --> O[tableProps.onPageChange -> setPagination(newPage, pageSize)]
  L --> P[tableProps.onPageSizeChange -> setPagination(0, newPageSize)]
  L --> Q[tableProps.onPageSort -> setSort(column, isReverse)]
```

> SVG rendering failed for this diagram.
