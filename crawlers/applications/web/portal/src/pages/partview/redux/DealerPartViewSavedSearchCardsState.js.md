# Diagram: web/portal/src/pages/partview/redux/DealerPartViewSavedSearchCardsState.js


> Auto-generated by Obscura crawlers

## Diagram 1

```mermaid
classDiagram
class DealerPartViewSavedSearchCardsState {
  +mountPoint: string
  +actionCreators: object
  +selectors: object
  +reducer: function
}
class reducer {
  +state: object
  +action: object
  +returns: object
}
class fetchSavedSearchCardData {
  +savedSearch: object
  +includeAPU: boolean
  +returns: function(dispatch)
}
class formatResponse {
  +response: object
  +returns: object
}
class getSavedSearchQueryString {
  +savedSearch: object
  +returns: string
}
class tokens {
  <<constant>>
}
class Constants {
  +STORE_MOUNT_POINT: string
  +FETCH_SAVED_SEARCH_CARD_DATA: string
  +RECEIVE_SAVED_SEARCH_CARD_DATA: string
  +ERROR_SAVED_SEARCH_CARD_DATA: string
}
DealerPartViewSavedSearchCardsState --> reducer
DealerPartViewSavedSearchCardsState --> fetchSavedSearchCardData : actionCreators
DealerPartViewSavedSearchCardsState --> getSavedSearchCardData : selectors
fetchSavedSearchCardData --> getSavedSearchQueryString
fetchSavedSearchCardData --> formatResponse
fetchSavedSearchCardData --> tokens
reducer --> Constants
formatResponse ..> "data shape" : produces
getSavedSearchQueryString ..> "QS string" : produces
```

> SVG rendering failed for this diagram.

## Diagram 2

```mermaid
flowchart TD
  A[Start fetchSavedSearchCardData(savedSearch, includeAPU)] --> B{tokens[savedSearch.id] exists?}
  B -- yes --> C[call cancelRequest("CANCELED") on previous token]
  B -- no --> D[no cancellation]
  C --> E[reset tokens[savedSearch.id] = {}]
  D --> E
  E --> F[dispatch FETCH_SAVED_SEARCH_CARD_DATA (id)]
  F --> G[clone savedSearch, determine isBatch, build params via getSavedSearchQueryString]
  G --> H{lifecycleState present?}
  H -- no --> I[set lifecycleState from PackageStatusOption list (include APU if includeAPU true)]
  H -- yes --> J[keep existing lifecycleState]
  I --> K{isBatch?}
  J --> K
  K -- yes --> L[prepare POST body with batchList and append batchType]
  K -- no --> M[use GET with query string]
  L --> N[create axios request with CancelToken saved to tokens[savedSearch.id].cancelRequest]
  M --> N
  N --> O[request.then -> formatResponse(response)]
  O --> P[dispatch RECEIVE_SAVED_SEARCH_CARD_DATA(id, data)]
  N --> Q[request.catch -> if not canceled log error]
  Q --> R[dispatch ERROR_SAVED_SEARCH_CARD_DATA(id, "error")]
  P --> S[End]
  R --> S
```

> SVG rendering failed for this diagram.
