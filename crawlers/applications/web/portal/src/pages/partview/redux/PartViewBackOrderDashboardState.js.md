# Diagram: web/portal/src/pages/partview/redux/PartViewBackOrderDashboardState.js


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
classDiagram
class PartViewBackOrderDashboardState {
  +mountPoint: "partViewBackOrderDashboard"
  +actionCreators
  +selectors
  +reducer
}
class backOrderedPartDuck {
  +fetch(url, config)
  +selectors
  +reducer
}
class Actions {
  +SET_BACKORDER_TABLE_PAGE_INDEX
  +getScopedActionName(actionName)
  +setBackOrderTablePageIndex(index)
  +fetchBackOrderedParts()
}
class Selectors {
  +getBackOrderTableIsLoading(state)
  +geBackOrderTableResults(state)
  +getBackOrderTablePageIndex(state)
  +getBackOrderTablePageSize(state)
  +getBackOrderTablePageCount(state)
}
class partViewBackOrderDashboardReducer {
  +partViewBackOrderDashboardReducer(state, action)
}
class chainReducers {
  +chainReducers(reducers[])
}

PartViewBackOrderDashboardState --> backOrderedPartDuck : includes
PartViewBackOrderDashboardState --> Actions : exposes
PartViewBackOrderDashboardState --> Selectors : exposes
PartViewBackOrderDashboardState --> chainReducers : reducer = chainReducers([...])
backOrderedPartDuck --> backOrderedPartDuck : selectors
Actions ..> backOrderedPartDuck : dispatches fetch(url, config)
Selectors ..> backOrderedPartDuck : reads selectors.getData(state)
chainReducers --> backOrderedPartDuck : composes reducer
chainReducers --> partViewBackOrderDashboardReducer : composes reducer
Actions ..> partViewBackOrderDashboardReducer : SET_BACKORDER_TABLE_PAGE_INDEX consumed by
partViewBackOrderDashboardReducer "0" --|> partViewBackOrderDashboardReducer : initialState(backOrderTablePageIndex:0)
```

> SVG rendering failed for this diagram.
