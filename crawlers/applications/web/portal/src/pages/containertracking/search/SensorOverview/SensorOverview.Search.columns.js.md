# Diagram: web/portal/src/pages/containertracking/search/SensorOverview/SensorOverview.Search.columns.js


> Auto-generated by Obscura crawlers

## Diagram 1

```mermaid
classDiagram
    class SensorID {
        +props.value: string
        +render(): Text
    }
    class LocationCell {
        +props.value: {locationName, locationCode}
        +render(): Text
    }
    class SensorStatusCell {
        +props.value: string
        +render(): Text
    }
    class LastReceivedReading {
        +props.value: string
        +render(): DateTime | null
    }
    class useColumns {
        +useTranslation()
        +returns: Array<Column>
    }
    class DateTime {
        +props: {plain, localize, dateTime, fallback}
    }
    class Text {
        +props.children
    }
    SensorID --> Text
    LocationCell --> Text
    SensorStatusCell --> Text
    LastReceivedReading --> DateTime
    LastReceivedReading --> Text
    useColumns --> SensorID
    useColumns --> LocationCell
    useColumns --> SensorStatusCell
    useColumns --> LastReceivedReading
```

> SVG rendering failed for this diagram.

## Diagram 2

```mermaid
flowchart TD
    A[Data row d] -->|accessor: sensorId| B[Sensor ID Column]
    A -->|accessor: locationCode (composed)| C[Location Column]
    A -->|accessor: status| D[Status Column]
    A -->|accessor: lastReadTime| E[Last Received Reading Column]
    B --> F[SensorID Component]
    C --> G[LocationCell Component]
    D --> H[SensorStatusCell Component]
    E --> I[LastReceivedReading Component]
    F --> J[Text render]
    G --> J
    H --> J
    I -->|value present| K[DateTime render]
    I -->|value absent| L[Text: N/A via useTranslation]
    K --> J
```

> SVG rendering failed for this diagram.
