# Diagram: web/portal/src/pages/containertracking/search/SensorOverview/SensorOverviewSearchFiltersContainer.js


> Auto-generated by Obscura crawlers

## Diagram 1

```mermaid
classDiagram
    class FilterSection {
        +props
    }
    class Connect_HOC {
        +mapStateToProps(state)
        +actionCreators
        +mergeProps(stateProps, dispatchProps, ownProps)
    }
    class SensoreOverViewSearchBarState {
        <<module>>
        +selectors.getSearchFilters(state)
        +selectors.getHasSearchCriteriaChanged(state)
        +actionCreators.setSearchFilter()
        +actionCreators.clearSearchFilter()
        +actionCreators.searchEntities()
        +actionCreators.resetSavedSearch()
        +actionCreators.clearSavedSearch()
    }
    class SensorOverviewDomainDataState {
        <<module>>
        +selectors.getSensorStatusOptions(state)
    }
    class FILTERS {
        <<constant>>
    }
    Connect_HOC --> FilterSection : wraps
    Connect_HOC ..> SensoreOverViewSearchBarState : uses selectors & actionCreators
    Connect_HOC ..> SensorOverviewDomainDataState : uses selectors
    Connect_HOC ..> FILTERS : injects as filtersMetadata
    SensoreOverViewSearchBarState --> "selectors.getSearchFilters" 
    SensoreOverViewSearchBarState --> "selectors.getHasSearchCriteriaChanged"
    SensorOverviewDomainDataState --> "selectors.getSensorStatusOptions"
```

> SVG rendering failed for this diagram.

## Diagram 2

```mermaid
flowchart LR
    subgraph ReduxState
        state[(state)]
    end
    subgraph Modules
        SOVSS[SensoreOverViewSearchBarState]
        SODDS[SensorOverviewDomainDataState]
        FILT[FILTERS]
    end
    state -->|getSearchFilters| SOVSS
    state -->|getHasSearchCriteriaChanged| SOVSS
    state -->|getSensorStatusOptions| SODDS
    SOVSS -->|actionCreators| Actions((setSearchFilter<br/>clearSearchFilter<br/>searchEntities<br/>resetSavedSearch<br/>clearSavedSearch))
    Actions -->|bound to dispatch| Dispatch[dispatch]
    Dispatch -->|passed as props| FilterSection[FilterSection Component]
    SODDS -->|sensorStatusOptions.data| FilterSection
    FILT -->|filtersMetadata| FilterSection
    state -->|mapStateToProps -> props| Connect[connect(mapStateToProps,...)]
    Connect -->|returns HOC| FilterSection
```

> SVG rendering failed for this diagram.
