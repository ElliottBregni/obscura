# Diagram: web/portal/src/pages/containertracking/search/SensorOverview/SensorOverviewSearch.page.container.js


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
flowchart LR
  subgraph Modules
    A[modules/organizations/OrganizationsState\ngetFeatureData()] 
    B[pages/containertracking/utils/ContainerTrackingUtils\ngetSolutionId()] 
    C[pages/containertracking/redux/SensorOverviewSearchBarState\nselectors & actionCreators]
    D[pages/containertracking/search/SensorOverview/SensorOverview.Search.page\nSensorOverviewSearch]
  end

  state[(Redux state)]
  state -->|getFeatureData(state)| A
  A -->|featureData| B
  B -->|solutionId| mapState

  state -->|selectors.getSearchResults(state)| S_searchResults[searchResults]
  state -->|selectors.getShowAdvancedSearch(state)| S_showFilters[showFilters]
  state -->|selectors.getIsLoading(state)| S_isLoading[isLoading]
  state -->|selectors.getSortColumn(state)| S_sortColumn[sortColumn]
  state -->|selectors.getReverseSort(state)| S_reverseSort[reverseSort]
  state -->|selectors.getPage(state)| S_page[page]
  state -->|selectors.getPageSize(state)| S_pageSize[pageSize]
  state -->|selectors.getTotalPages(state)| S_totalPages[totalPages]
  state -->|selectors.getTotalEntities(state)| S_totalCount[totalCount]
  state -->|selectors.getExportIdentifier(state)| S_exportIdentifier[exportIdentifier]
  state -->|selectors.getExportName(state)| S_exportName[exportName]
  state -->|selectors.getIsExporting(state)| S_isExporting[isExporting]
  state -->|selectors.getExportFailed(state)| S_exportFailed[exportFailed]

  mapState[mapStateToProps] --> S_searchResults
  mapState --> S_showFilters
  mapState --> S_isLoading
  mapState --> S_sortColumn
  mapState --> S_reverseSort
  mapState --> S_page
  mapState --> S_pageSize
  mapState --> S_totalPages
  mapState --> S_totalCount
  mapState --> S_exportIdentifier
  mapState --> S_exportName
  mapState --> S_isExporting
  mapState --> S_exportFailed
  mapState -->|solutionId: getSolutionId(getFeatureData(state))| solutionId[solutionId]

  subgraph Dispatchers
    D_toggle[toggleShowFilters(showFilters)]
    D_setPagination[setPagination(page,pageSize)]
    D_setSort[setSort(sortColumn,reverse)]
    D_exportEntities[exportEntities()]
    D_clearExportErrors[clearExportErrors()]
    D_resetSearch[resetSearch()]
    D_searchEntities[searchEntities()]
    D_resetExport[resetExport()]
    D_resetSearchBar[resetSearchBar()]
  end

  C -->|actionCreators.toggleShowFilters| D_toggle
  C -->|actionCreators.setPagination| D_setPagination
  C -->|actionCreators.setSort| D_setSort
  C -->|actionCreators.exportSearch| D_exportEntities
  C -->|actionCreators.clearExportErrors| D_clearExportErrors
  C -->|actionCreators.resetSearchAndFilters| D_resetSearch
  C -->|actionCreators.searchEntities| D_searchEntities
  C -->|actionCreators.resetExport| D_resetExport
  C -->|actionCreators.resetSearchBar| D_resetSearchBar

  mapDispatch[mapDispatchToProps] --> D_toggle
  mapDispatch --> D_setPagination
  mapDispatch --> D_setSort
  mapDispatch --> D_exportEntities
  mapDispatch --> D_clearExportErrors
  mapDispatch --> D_resetSearch
  mapDispatch --> D_searchEntities
  mapDispatch --> D_resetExport
  mapDispatch --> D_resetSearchBar

  merge[mergeProps] --> mapState
  merge --> mapDispatch
  merge --> ownProps[ownProps]
  merge --> mergedExport[exportEntities() uses solutionId]
  merge --> mergedReset[resetSearch() uses solutionId]

  solutionId --> mergedExport
  solutionId --> mergedReset

  mergedExport -->|passed as prop| D_exportEntities
  mergedReset -->|passed as prop| D_resetSearch

  merge -->|connected component| Connected[connect(...)(SensorOverviewSearch)]
  Connected --> D
  D -->|receives props| mapState
  D -->|receives dispatch props| mapDispatch
  D -->|receives merged props| merge
```

> SVG rendering failed for this diagram.
