# Diagram: web/portal/src/pages/containertracking/modules/domain-data/SensorOverviewSearchFilterLoaders.js


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
graph LR
  A[locationOptionsState] --> B[buildAsyncFilterOptionsState]
  B --> C{config}
  C --> D[topic: "sensorLocationFilter"]
  C --> E[url: apiUrl(`/containertracking/api/location?filter_name=${FilterNameEnum.SENSOR_LOCATIONS}`)]
  C --> F[pageSize: 5]
  C --> G[getResponseData: (data) => data?.data ?? []]
  C --> H[transformResult: (item) => { label: `${item.location_name} (${item.location_code})`, value: item.location_code }]
  E --> I[api-url module]
  B --> J[AsyncFilterOptionsStateBuilder]
  J --> K[components/search-bar]
  subgraph DataFlow
    L[API Response] --> G
    L --> H
    G --> M[Filtered Data Array]
    M --> H
    H --> A
  end
```

> SVG rendering failed for this diagram.
