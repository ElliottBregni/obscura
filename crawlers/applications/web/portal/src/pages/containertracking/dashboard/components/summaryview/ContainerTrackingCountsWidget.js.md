# Diagram: web/portal/src/pages/containertracking/dashboard/components/summaryview/ContainerTrackingCountsWidget.js


> Auto-generated by Obscura crawlers

## Diagram 1

```mermaid
classDiagram
    class ContainerTrackingCountsWidget {
        +containerTotals
        +isContainerTotalsLoading
        +searchEntities()
        +setSearchFilter()
        +getPercentage(count,totalCount)
        +getExceptionGroups(...)
        +handleClickException({name})
        +render()
    }
    class ExceptionsPanel {
        +Chart(...)
        +exceptionGroups
        +handleClickException
    }
    class Separator
    class MediaQueries
    class Colors
    class getIconData
    class getFontSize
    ContainerTrackingCountsWidget --> ExceptionsPanel : uses
    ContainerTrackingCountsWidget --> Separator : uses
    ContainerTrackingCountsWidget --> MediaQueries : uses
    ContainerTrackingCountsWidget --> Colors : reads
    ContainerTrackingCountsWidget --> getIconData : calls
    ContainerTrackingCountsWidget --> getFontSize : calls
```

> SVG rendering failed for this diagram.

## Diagram 2

```mermaid
flowchart TD
    A[Props: containerTotals, isLoading, searchEntities, setSearchFilter] --> B[Initialize texts via useTranslation]
    B --> C[Extract counts from containerTotals]
    C --> D[fontSize = getFontSize(...)]
    C --> E[getExceptionGroups(...) returns exception groups]
    E --> F[totalContainerChart & onsiteContainerCharts structures]
    F --> G[TotalContainerChart -> ExceptionsPanel.Chart (total)]
    F --> H[onsiteContainerCharts.map -> ExceptionsPanel.Chart (per group)]
    G --> I[onClick -> searchEntities()]
    H --> J[onClick -> setSearchFilter(...) then searchEntities()]
    K[handleClickException(name)] -->|Unaccounted| L[setSearchFilter("activeExceptions",["Unaccounted"]) & searchEntities()]
    K -->|Excessive Dwell| M[setSearchFilter("activeExceptions",["Excessive Dwell"]) & searchEntities()]
    K -->|No Update 30+ Days| N[setSearchFilter("activeExceptions",["No Update 30+ Days"]) & searchEntities()]
    K -->|Off Route| O[setSearchFilter("activeExceptions",["Off Route"]) & searchEntities()]
    K -->|Battery Level| P[setSearchFilter("activeExceptions",["Battery Level"]) & searchEntities()]
    subgraph UI
      G
      H
      SeparatorElem[Separator with MediaQueries styles]
    end
    F --> UI
```

> SVG rendering failed for this diagram.
