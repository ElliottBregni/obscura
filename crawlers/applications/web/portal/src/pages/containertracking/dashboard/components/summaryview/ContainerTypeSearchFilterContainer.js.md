# Diagram: web/portal/src/pages/containertracking/dashboard/components/summaryview/ContainerTypeSearchFilterContainer.js


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
flowchart LR
  subgraph ReduxState["Redux State"]
    state[State]
  end

  subgraph CTModule["ContainerTrackingContainerTypeTableState"]
    selectors[selectors.getSearchFilters()]
    ac_set[setSearchFilter]
    ac_clear[clearSearchFilter]
    ac_search[searchEntities]
    ac_reset[resetSavedSearch]
    ac_clearSaved[clearSavedSearch]
  end

  subgraph LocalCode["This Module"]
    mapStateToProps[mapStateToProps]
    actionCreatorsWrappers[actionCreators (wrappers)]
    mergePropsFunc[mergeProps(stateProps, dispatchProps, ownProps)]
    constants[CONTAINER_TYPE_FILTERS]
    connectCall[connect(...)(FilterSection)]
    FilterSectionComp[FilterSection]
  end

  state --> selectors
  selectors --> mapStateToProps
  ac_set --> actionCreatorsWrappers
  ac_clear --> actionCreatorsWrappers
  ac_search --> actionCreatorsWrappers
  ac_reset --> actionCreatorsWrappers
  ac_clearSaved --> actionCreatorsWrappers

  mapStateToProps --> connectCall
  actionCreatorsWrappers --> connectCall
  mergePropsFunc --> connectCall
  constants --> mergePropsFunc
  connectCall --> FilterSectionComp

  click selectors "javascript:undefined" "selectors.getSearchFilters"
  click ac_set "javascript:undefined" "setContainerTypeSearchFilter"
  click ac_clear "javascript:undefined" "clearContainerTypeSearchFilter"
  click ac_search "javascript:undefined" "searchEntities"
  click constants "javascript:undefined" "CONTAINER_TYPE_FILTERS"
```

> SVG rendering failed for this diagram.
