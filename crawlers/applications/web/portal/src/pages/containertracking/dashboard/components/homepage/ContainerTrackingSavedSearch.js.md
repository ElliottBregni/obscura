# Diagram: web/portal/src/pages/containertracking/dashboard/components/homepage/ContainerTrackingSavedSearch.js


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
classDiagram
    class ContainerTrackingSavedSearch {
        +savedSearch
        +onEditClick()
        +isDeleting
        +loadFullSavedSearch()
        +handleExceptionClick(exception)
        +handleOnSiteClick(onsiteData)
        +getExceptionCount(fvId)
        +getLatestExceptionCount(fvId)
        +getExceptionGroups({lastestExceptionOnly})
        +getOnsiteCounts()
    }
    class SavedSearchPanel {
        +savedSearch
        +onSearchClick()
        +onEditClick()
        +isLoading
        +isDeleting
    }
    class DonutChart {
        +data
        +handler()
        +label
        +total
    }
    class ExceptionCountGroup {
        +exceptions
        +vertical
        +exceptionDirection
        +clickHandler()
        +showNumberSeparator
    }
    class ContainerTrackingSavedSearchCardsState {
        +selectors
        +actionCreators
        +fetchSavedSearchCardData()
    }
    class ContainerTrackingSavedSearchState {
        +actionCreators
        +loadSavedSearch()
    }
    class ContainerTrackingSearchBarState {
        +actionCreators
        +searchEntities()
    }
    class Utils {
        +getIconData(name)
        +getExceptionChartData(groups, totalCount, latestCount)
    }
    class Redux {
        +useDispatch()
        +useSelector()
    }
    ContainerTrackingSavedSearch --> SavedSearchPanel : renders
    ContainerTrackingSavedSearch --> DonutChart : uses
    ContainerTrackingSavedSearch --> ExceptionCountGroup : uses
    ContainerTrackingSavedSearch --> ContainerTrackingSavedSearchCardsState : selects data from
    ContainerTrackingSavedSearch --> ContainerTrackingSavedSearchState : dispatches loadSavedSearch
    ContainerTrackingSavedSearch --> ContainerTrackingSearchBarState : dispatches searchEntities
    ContainerTrackingSavedSearch --> Utils : calls getIconData/getExceptionChartData
    ContainerTrackingSavedSearch ..> Redux : uses hooks
    SavedSearchPanel <|-- ContainerTrackingSavedSearch
    DonutChart <|-- ContainerTrackingSavedSearch
    ExceptionCountGroup <|-- ContainerTrackingSavedSearch
    ContainerTrackingSavedSearchCardsState o-- "selectors" : getSavedSearchCardData
    ContainerTrackingSavedSearchCardsState o-- "actionCreators" : fetchSavedSearchCardData
    ContainerTrackingSavedSearchState o-- "actionCreators" : loadSavedSearch
    ContainerTrackingSearchBarState o-- "actionCreators" : searchEntities
```

> SVG rendering failed for this diagram.
