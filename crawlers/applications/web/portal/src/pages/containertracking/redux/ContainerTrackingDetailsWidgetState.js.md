# Diagram: web/portal/src/pages/containertracking/redux/ContainerTrackingDetailsWidgetState.js


> Auto-generated by Obscura crawlers

## Diagram 1

```mermaid
classDiagram
class ContainerTrackingDetailsWidgetState {
  +mountPoint
  +actionCreators
  +selectors
  +reducer
}
class ActionCreators {
  +fetchShipmentDetails()
  +fetchContainerDetails()
  +fetchComments()
  +addComment()
  +addBatchComments()
  +updateComment()
  +markCommentsRead()
  +fetchContainerMedia()
  +clearContainerMedia()
  +fetchContainerHistory()
  +fetchContainerActiveExceptions()
  +fetchContainerSupplierLocationDetails()
  +clearContainerDetails()
  +watchContainerActions()
}
class Selectors {
  +getContainerDetails()
  +getContainerMedia()
  +getContainerPositionUpdates()
  +getLocationDetails()
  +getSupplierLocationDetails()
  +getContainerHistory()
  +getShipmentDetails()
  +getChildShipmentDetails()
  +getIsFetchingComments()
  +getComments()
  +getBatchCommentFlags()
}
class Reducer {
  +ContainerTrackingDetailsWidgetReducer(state, action)
}
class InitialState {
  +containerDetails
  +containerMedia
  +containerPositionUpdates
  +containerHistory
  +comments
  +flags...
}
class API {
  +axiosConfig()
  +containerDetailsUrl(id)
  +containerPositionUpdatesUrl(id)
  +containerHistoryApiUrl(id)
  +containerActiveExceptionsApiUrl(id)
  +watchContainerUrl(id)
  +shipmentDetailsUrl({shipmentID,scac})
}
class Constants {
  +STORE_MOUNT_POINT
  +APPLICATION_BASE_URL
  +CONTAINER_IMAGE_BASE_URL
  +ActionTypeConstants...
}
ContainerTrackingDetailsWidgetState --> ActionCreators : exposes
ContainerTrackingDetailsWidgetState --> Selectors : exposes
ContainerTrackingDetailsWidgetState --> Reducer : uses
Reducer --> InitialState : initializes with
ActionCreators --> API : calls
ActionCreators --> Reducer : dispatches actions
Selectors --> InitialState : reads from
Constants ..> API : used by
```

> SVG rendering failed for this diagram.

## Diagram 2

```mermaid
flowchart LR
  subgraph UI
    U[UI Component / Widget]
  end
  subgraph Actions
    A1[dispatch(FETCH_SHIPMENT_DETAILS)]
    A2[dispatch(REQUEST_CONTAINER_DETAILS)]
    A3[dispatch(FETCH_CONTAINER_COMMENTS)]
    A4[dispatch(SUBMIT_NEW_COMMENT)]
  end
  subgraph AsyncCalls
    X1[/axios.get(containerDetailsUrl)\n.then dispatch RECEIVE_CONTAINER_DETAILS/]
    X2[/axios.get(positionUpdates)\n.then dispatch RECEIVE_CONTAINER_OTHER_ALL/]
    X3[/axios.post(comment)\n.then dispatch RECEIVE_NEW_COMMENT/]
    X4[/axios.post/watch or delete\n.then dispatch CONTAINER_UPDATE_SUCCESS/]
  end
  subgraph Store
    R[ContainerTrackingDetailsWidgetReducer]
    S[State (containerDetails, comments, media, flags...)]
  end
  U -->|user action| A1
  U -->|user action| A3
  A1 --> X1
  A1 --> X2
  A3 --> X3
  A4 --> X3
  X1 -->|RECEIVE_CONTAINER_DETAILS| R
  X2 -->|RECEIVE_CONTAINER_OTHER_ALL| R
  X3 -->|RECEIVE_NEW_COMMENT / RECEIVE_CONTAINER_COMMENTS| R
  X4 -->|CONTAINER_UPDATE_SUCCESS| R
  R --> S
  S -->|selectors read| U
  style X1 stroke-dasharray: 5 5
  style X2 stroke-dasharray: 5 5
  style X3 stroke-dasharray: 5 5
  style X4 stroke-dasharray: 5 5
```

> SVG rendering failed for this diagram.
