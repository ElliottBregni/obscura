# Diagram: web/portal/src/pages/containertracking/redux/ContainerTrackingCountsWidgetState.js


> Auto-generated by Obscura crawlers

## Diagram 1

```mermaid
flowchart LR
  subgraph Actions
    FC[fetchContainerTotals()] -->|dispatch| REQ[REQUEST_CONTAINER_TOTALS]
    FC --> AX[axios.get(BASE_URL, axiosConfig())]
    AX -->|on success| REC[RECEIVE_CONTAINER_TOTALS]
    AX -->|on error| LOG[console.log(err)]
  end
  REQ -->|handled by| RDR[ContainerTrackingCountsWidgetReducer]
  REC -->|payload ->| RDR
  RDR -->|returns new state| STATE[Store: containerTrackingCountsWidget]
  STATE --> SEL1[getContainerTotals(state)]
  STATE --> SEL2[getContainerTotalsLoadingFlag(state)]
  classDef actions fill:#f9f,stroke:#333,stroke-width:1px;
  class FC,REQ,AX,REC,LOG actions
  classDef reducer fill:#bbf,stroke:#333;
  class RDR reducer
  classDef store fill:#bfb,stroke:#333;
  class STATE store
  classDef selectors fill:#ffd,stroke:#333;
  class SEL1,SEL2 selectors
```

> SVG rendering failed for this diagram.

## Diagram 2

```mermaid
classDiagram
  class ContainerTrackingCountsWidgetState {
    +String mountPoint = "containerTrackingCountsWidget"
    +actionCreators
    +selectors
    +reducer
  }
  class ActionCreators {
    +fetchContainerTotals()
  }
  class Selectors {
    +getContainerTotals(state)
    +getContainerTotalsLoadingFlag(state)
  }
  class Reducer {
    +ContainerTrackingCountsWidgetReducer(state, action)
  }
  ContainerTrackingCountsWidgetState "1" --> "1" ActionCreators : has
  ContainerTrackingCountsWidgetState "1" --> "1" Selectors : has
  ContainerTrackingCountsWidgetState "1" --> "1" Reducer : has
  ActionCreators ..> Reducer : dispatches
  Selectors ..> ContainerTrackingCountsWidgetState : read state fields
  Reducer : initialState {containerTotals: {}, isContainerTotalsLoading: false}
```

> SVG rendering failed for this diagram.
