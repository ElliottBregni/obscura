# Diagram: web/portal/src/pages/containertracking/redux/SensorOverviewSearchBarState.js


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
classDiagram
    class Module {
        +STORE_MOUNT_POINT
        +entitiesUrl(_ignored_solutionId, queryString)
        +axiosConfig()
        +fetchSearch(queryString, _ignored_solutionId, duck, dispatch)
        +SensorOverviewSearchBarState
    }

    class apiUrl {
        <<import>>
        +apiUrl(path)
    }

    class moment {
        <<import>>
        +tz.guess()
    }

    class _ {
        <<import>>
        +partial(fn, ...args)
    }

    class buildSearchBarState {
        <<import>>
        +buildSearchBarState(mountPoint, categories, filters, fetchFn, arr, opts)
    }

    class SEARCH_OPTIONS {
        <<import>>
        +SEARCH_CATEGORIES
        +FILTERS
    }

    class SensorOverviewSearchBarState {
        +actionCreators
        +selectors
        +export default
    }

    Module "1" --> "1" apiUrl : uses
    Module "1" --> "1" moment : uses
    Module "1" --> "1" _ : uses
    Module "1" --> "1" buildSearchBarState : constructs
    Module "1" --> "1" SEARCH_OPTIONS : imports

    apiUrl <.. entitiesUrl : called_by
    moment <.. axiosConfig : called_by
    axiosConfig --|> Module : returns_config
    entitiesUrl --|> Module : builds_url
    fetchSearch --|> Module : uses_entitiesUrl/axiosConfig
    fetchSearch ..> "duck.fetch" : dispatches
    buildSearchBarState --> SensorOverviewSearchBarState : returns
    SensorOverviewSearchBarState o-- "actionCreators" : contains
    SensorOverviewSearchBarState o-- "selectors" : contains
    _ ..> SensorOverviewSearchBarState : used_for_partial
    entitiesUrl ..> SensorOverviewSearchBarState : passed_to_exportSearch
```

> SVG rendering failed for this diagram.
