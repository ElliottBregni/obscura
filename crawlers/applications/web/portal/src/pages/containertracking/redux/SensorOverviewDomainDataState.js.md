# Diagram: web/portal/src/pages/containertracking/redux/SensorOverviewDomainDataState.js


> Auto-generated by Obscura crawlers

## Diagram 1

```mermaid
classDiagram
    class SensorOverviewModule {
        +STORE_MOUNT_POINT: "sensorOverview"
        +fetchDomainData()
        +SensorOverviewDomainDataState
    }
    class duck {
        +fetch(url, options, transform)
        +reducer
        +selectors
    }
    class apiUrl {
        +call(path): url
    }
    class transform {
        +map data -> {label, value}[]
    }
    class SensorOverviewDomainDataState {
        +mountPoint
        +reducer
        +actionCreators.fetchDomainData()
        +selectors.getSensorStatusOptions
    }
    SensorOverviewModule --> duck : creates
    SensorOverviewModule --> apiUrl : uses
    SensorOverviewModule --> SensorOverviewDomainDataState : exports
    fetchDomainData --> apiUrl : builds URL
    fetchDomainData --> duck : calls fetch()
    duck --> SensorOverviewDomainDataState : provides reducer/selectors
    duck ..> transform : passes to fetch()
```

> SVG rendering failed for this diagram.

## Diagram 2

```mermaid
flowchart TD
    A[Module Init] --> B[buildFetchDuck("sensorOverview")]
    B --> C[duck {fetch,reducer,selectors}]
    A --> D[fetchDomainData()]
    D --> E[apiUrl("/containertracking/api/filtervalues")]
    E --> F[network request to API]
    F --> G[duck.fetch(url, {params}, transform)]
    G --> H[transform(data) => sensorOptions.map -> {label, value}]
    H --> I[duck stores data]
    I --> J[selectors.getData() -> getSensorStatusOptions]
    J --> K[UI consumes getSensorStatusOptions]
```

> SVG rendering failed for this diagram.
