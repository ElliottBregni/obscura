# Diagram: web/portal/src/pages/containertracking/container-management/components/tag-management/ContainerTracking.Management.SearchBar.container.js


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
flowchart LR
  subgraph Components
    SB[SearchBar Component]
  end

  subgraph ReduxBinding
    Connect[connect(mapStateToProps, actionCreators)]
    MTP[mapStateToProps]
    AC[actionCreators object]
  end

  subgraph Sources
    SBS[SearchBarState.selectors]
    SBA[SearchBarState.actionCreators]
    CTD[ContainerTrackingDomainData.actionCreators]
    ORG[getFeatureData / getCurrentOrganizationId]
    AUTH[getAuthorization]
  end

  SB -->|wrapped by| Connect
  Connect --> MTP
  Connect --> AC

  MTP -->|reads selectors| SBS
  MTP -->|calls| ORG
  MTP -->|calls| AUTH
  SBS -->|provides| getSearchText[getSearchText]
  SBS -->|provides| getSearchCategory[getSearchCategory]
  SBS -->|provides| getIgnoreSearchCategory[getIgnoreSearchCategory]
  SBS -->|provides| getTypeaheadOptionsMetadata[getTypeaheadOptionsMetadata]
  SBS -->|provides| getHasSearchCriteriaChanged[getHasSearchCriteriaChanged]

  MTP -->|outputs prop domainDataArgs| DomainArgs[domainDataArgs]
  DomainArgs -->|uses| ORG

  AC -->|maps| setSearchCategoryForKey[setSearchCategoryForKey]
  AC -->|wraps| setSearchTextWrapper[setSearchText(text, ignoreValueChange) -> dispatch]
  AC -->|includes| clearSearchText[clearSearchText]
  AC -->|includes| resetSearchBar[resetSearchBar]
  AC -->|includes| searchEntities[searchEntities]
  AC -->|includes| clearSavedSearch[clearSavedSearch]
  AC -->|includes| fetchDomainData[fetchDomainData]

  SBA -->|exports| setSearchCategoryForKey
  SBA -->|exports| setSearchText
  SBA -->|exports| clearSearchText
  SBA -->|exports| resetSearchBar
  SBA -->|exports| searchEntities
  SBA -->|exports| clearSavedSearch

  CTD -->|exports| fetchDomainData

  style SB fill:#f9f,stroke:#333,stroke-width:1px
  style Connect fill:#bbf,stroke:#333
  style MTP fill:#efe,stroke:#333
  style AC fill:#efe,stroke:#333
  style SBS fill:#ffd,stroke:#333
  style SBA fill:#ffd,stroke:#333
  style CTD fill:#ffd,stroke:#333
  style ORG fill:#fff,stroke:#333
  style AUTH fill:#fff,stroke:#333
```

> SVG rendering failed for this diagram.
