# Diagram: web/portal/src/pages/containertracking/container-management/components/RouteManagement.AsyncSelect.js


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
classDiagram
    class CheckboxOption {
        +props
        +render(): JSX
    }
    class Checkbox {
        +id
        +checked
    }
    class Text {
        +as
        +htmlFor
    }
    class Colors {
        <<static>>
        +background
    }
    class AsyncPaginate {
        +isDisabled
        +components
        +isMulti
        +value
        +loadOptions()
        +onChange()
    }
    class RouteManagementAsyncSelect {
        +loadOptions
        +onInputChange
        +values
        +onChange
        +inputValue
        +isDisabled
        +closeMenuOnSelect
        +menuPosition
        +render(): JSX
    }

    RouteManagementAsyncSelect --> AsyncPaginate : uses
    RouteManagementAsyncSelect ..> CheckboxOption : components.Option
    CheckboxOption --> Checkbox : renders
    CheckboxOption --> Text : renders
    CheckboxOption --> Colors : reads background
    AsyncPaginate <|-- RouteManagementAsyncSelect : configured instance
    RouteManagementAsyncSelect o-- "props" : accepts
    CheckboxOption o-- "props.innerProps" : receives
    Colors --|> background : contains
```

> SVG rendering failed for this diagram.
