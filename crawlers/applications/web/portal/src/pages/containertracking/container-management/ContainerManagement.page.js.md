# Diagram: web/portal/src/pages/containertracking/container-management/ContainerManagement.page.js


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
classDiagram
    class ContainerTrackingManagement {
        +props: searchResults, isLoading, page, pageSize, totalPages
        +props: setRoutePagination, routeSearchResults, routeIsLoading, routePage
        +props: routePageSize, routeTotalPages, setContainerPagination
        +props: savedStatus, updateData, resetFailedCases
        +props: batchUploadAction, clearBatchUpload, batchUploadResult
        +props: batchUploadError, batchUploadLoading, resetSavedCases
        +props: resetSavedStatus, savedCases, isSaving, failure
        +props: searchRouteEntities, searchContainerEntities
        -state: showNewRoutePopup, showBatchUploadPopup, showEditRoutePopup
        -state: popupData { locations, containers, defaultBucketName }
        -handlers: onNewRouteClick(), onBatchUploadClick(), editRouteHandler()
        -lifecycle: useSetTitleOnMount(), useSetDescriptionOnMount(), useEffect()
    }

    class Dashboard {
        +Tabs, TabList, Tab, TabPanel
    }

    class BaseTable {
        +props: columns, isLoading, data, pagination, pageIndex, pageCount, pageSize
        +events: onPageChange(), onPageSizeChange()
    }

    class RouteManagementPopup {
        +props: locations, containers, defaultBucketName, show, isEdit
        +props: hide(), savedStatus, updateData, resetFailedCases
        +props: isSaving, resetSavedCases, resetSavedStatus, savedCases, failure
        +props: resetPopupData()
    }

    class BatchUploadPopup {
        +props: show, hide(), batchUploadAction, clearBatchUpload
        +props: batchUploadResult, batchUploadError, batchUploadLoading
    }

    class ContainerTrackingManagementSearchBarContainer
    class ContainerTrackingRouteManagementSearchBarContainer
    class SearchButtonContainer
    class Button
    class Text
    class Icon
    class Colors
    class getManagementColumns
    class getRouteManagementColumns
    class useTranslation
    class useSetTitleOnMount
    class useSetDescriptionOnMount

    ContainerTrackingManagement --> Dashboard : renders
    ContainerTrackingManagement --> BaseTable : renders (container table)
    ContainerTrackingManagement --> BaseTable : renders (route table)
    ContainerTrackingManagement --> RouteManagementPopup : opens/hides
    ContainerTrackingManagement --> BatchUploadPopup : opens/hides
    ContainerTrackingManagement --> ContainerTrackingManagementSearchBarContainer : renders
    ContainerTrackingManagement --> ContainerTrackingRouteManagementSearchBarContainer : renders
    ContainerTrackingManagement --> SearchButtonContainer : uses
    ContainerTrackingManagement --> Button : uses
    ContainerTrackingManagement --> Text : uses
    ContainerTrackingManagement --> Icon : uses
    ContainerTrackingManagement --> Colors : uses
    ContainerTrackingManagement --> getManagementColumns : supplies t -> columns
    ContainerTrackingManagement --> getRouteManagementColumns : supplies t, editRouteHandler -> columns
    ContainerTrackingManagement --> useTranslation : uses t()
    ContainerTrackingManagement --> useSetTitleOnMount : calls
    ContainerTrackingManagement --> useSetDescriptionOnMount : calls
    ContainerTrackingManagement ..> RouteManagementPopup : passes popupData, handlers
    ContainerTrackingManagement ..> BatchUploadPopup : passes batch actions and state
    Dashboard --> Dashboard.Tabs : contains
    Dashboard.Tabs --> Dashboard.TabList : contains
    Dashboard.TabList --> Dashboard.Tab : contains
    Dashboard.Tabs --> Dashboard.TabPanel : contains
    getManagementColumns --> BaseTable : produces columns
    getRouteManagementColumns --> BaseTable : produces columns
```

> SVG rendering failed for this diagram.
