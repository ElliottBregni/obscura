# Diagram: web/portal/src/pages/containertracking/details/components/ContainerTrackingActionNav.js


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
flowchart LR
  subgraph ReactComponent
    AN[ActionNav]
  end
  AN -->|uses| useState["useState(isOpen)"]
  AN -->|uses| useTranslation["useTranslation('container-tracking')"]
  AN -->|renders| DIV[div(onClick stopPropagation)]
  DIV --> DropdownComp[Dropdown (variant=primary, id=action-selector, color=Colors.tableHeadingBackground, className=float-end)]
  DropdownComp -->|prop: open| isOpenState[(isOpen)]
  DropdownComp -->|prop: text| TransText["t('container-tracking:Actions')"]
  DropdownComp -->|onToggle ->| ActionToggle[actionToggle(): setIsOpen(!isOpen)]
  DropdownComp --> Item[Dropdown.Item]
  Item -->|onClick ->| EventHandlerCall[eventHandler('MANAGE_LOCATIONS')]
  AN -->|propTypes| PropTypesBox[PropTypes: { eventHandler: func.isRequired }]
```

> SVG rendering failed for this diagram.
