# Diagram: web/portal/src/pages/subscribe/subscribe.page.container.js


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
flowchart LR
  Store[Redux Store]
  SubscribeComponent[Subscribe Component]
  Connect[connect(mapStateToProps, actionCreators)]
  SubscribeStateModule[SubscribeState Module]
  Selectors[Selectors]
  ActionCreators[Action Creators]
  mapStateToPropsFunc[mapStateToProps]
  Props[Props passed to Subscribe]

  Store -->|state| mapStateToPropsFunc
  mapStateToPropsFunc -->|maps| Props

  subgraph MappedProps["mapped state props"]
    guid["guid: state.location.payload.guid"]
    category["category: state.location.payload.category"]
    subscription["subscription: SubscribeState.selectors.getSubscription(state)"]
    organizationImageConfig["organizationImageConfig: SubscribeState.selectors.getOrganizationImageConfig(state)"]
    isFetchingSubscription["isFetchingSubscription: SubscribeState.selectors.getIsFetchingSubscription(state)"]
    isFetchSubscriptionError["isFetchSubscriptionError: SubscribeState.selectors.getIsFetchSubscriptionError(state)"]
    subscriptionErrorMessage["subscriptionErrorMessage: SubscribeState.selectors.getFetchSubscriptionErrorMessage(state)"]
    isSubscribeComplete["isSubscribeComplete: SubscribeState.selectors.getIsSubscribeComplete(state)"]
    isSubscribing["isSubscribing: SubscribeState.selectors.getIsSubscribing(state)"]
    isSubscribeError["isSubscribeError: SubscribeState.selectors.getIsSubscribeError(state)"]
    subscribeErrorMessage["subscribeErrorMessage: SubscribeState.selectors.getSubscribeErrorMessage(state)"]
  end

  Props --> SubscribeComponent
  Connect --> SubscribeComponent
  Connect --> mapStateToPropsFunc
  Connect --> ActionCreators

  SubscribeStateModule --> Selectors
  SubscribeStateModule --> ActionCreators

  Selectors --> subscription
  Selectors --> organizationImageConfig
  Selectors --> isFetchingSubscription
  Selectors --> isFetchSubscriptionError
  Selectors --> subscriptionErrorMessage
  Selectors --> isSubscribeComplete
  Selectors --> isSubscribing
  Selectors --> isSubscribeError
  Selectors --> subscribeErrorMessage

  ActionCreators --> resetSubscribe["resetSubscribe()"]
  ActionCreators --> fetchSubscription["fetchSubscription()"]
  ActionCreators --> subscribeAction["subscribe()"]
  ActionCreators --> fetchOrganizationImages["fetchOrganizationImages()"]

  actionCreatorsObj[actionCreators object]
  actionCreatorsObj --> resetSubscribe
  actionCreatorsObj --> fetchSubscription
  actionCreatorsObj --> subscribeAction
  actionCreatorsObj --> fetchOrganizationImages

  Store -.->|dispatch| ActionCreators
  SubscribeComponent -->|calls| ActionCreators
```

> SVG rendering failed for this diagram.
