# Diagram: web/portal/src/pages/loginloading/redirect-helper.ts


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
flowchart LR
  Start((Request with pathname)) --> DealerCheck[getDealerRedirect(pathname)]
  Start --> HealthcareCheck[getHealthCareRedirect(pathname)]
  Start --> PartnerCheck[getPartnerRedirect(pathname)]

  subgraph Dealer["Dealer Redirect Logic"]
    DealerCheck --> DCheck{pathname includes any allowedPaths?}
    DAllowed[Allowed paths:
      PART_VIEW.DEALER_PARTVIEW_DASHBOARD
      PART_VIEW.DEALER_PARTVIEW_SEARCH
      PART_VIEW.DEALER_PARTVIEW_DETAILS
      FINISHED_VEHICLE.VINVIEW_DASHBOARD
      FINISHED_VEHICLE.VINVIEW_SEARCH
      /vinview/details
      /driveaway
      ADMINISTRATION.USER_MANAGEMENT
      USER.PROFILE
      /documentation
      /reports]:::list
    DCheck -->|yes| DNull[Return null]
    DCheck -->|no| DVin[/finished-vehicle/vinview-dashboard]
    DealerCheck --> DAllowed
  end

  subgraph Healthcare["Healthcare Redirect Logic"]
    HealthcareCheck --> HCheck{pathname includes any healthcareAllowedPaths?}
    HAllowed[Allowed paths:
      /healthcare-container-tracking
      /healthcare-tracking-management
      /manage/users
      /manage/locations
      /profile
      /reports]:::list
    HCheck -->|yes| HNull[Return null]
    HCheck -->|no| HDash[/healthcare-container-tracking]
    HealthcareCheck --> HAllowed
  end

  subgraph Partner["Partner Redirect Logic"]
    PartnerCheck --> PCheck{pathname includes any partnerAllowedPaths?}
    PAllowed[Allowed paths:
      /partners
      /partnerview/details
      /manage/users
      /documentation
      /container-tracking
      /container-tracking/search
      /container-tracking/details/:id
      /inventoryview
      /inventoryview/details
      /damageview
      /damageview/search
      /damageview/details
      /compliance
      /compliance/reports/:reportId
      /reports]:::list
    PCheck -->|yes| PNull[Return null]
    PCheck -->|no| PDash[/partners]
    PartnerCheck --> PAllowed
  end

  classDef list fill:#f9f,stroke:#333,stroke-width:0.5,font-size:12px;padding:6px;
```

> SVG rendering failed for this diagram.
