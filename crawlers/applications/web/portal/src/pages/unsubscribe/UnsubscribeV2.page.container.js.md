# Diagram: web/portal/src/pages/unsubscribe/UnsubscribeV2.page.container.js


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
classDiagram
    class UnsubscribeV2 {
        <<React Component>>
    }
    class UnsubscribeV2State {
        selectors
        actionCreators
    }
    class mapStateToProps {
        +ruleUuid
        +unsubscribeUuid
        +isUnsubscribing()
        +isUnsubscribeComplete()
        +isUnsubscribeError()
        +unsubscribeErrorMessage()
    }
    class actionCreators {
        +resetUnsubscribe()
        +unsubscribe()
    }
    class State {
        location.payload.ruleUuid
        location.payload.unsubscribeUuid
    }
    class connect_HOC {
        +connect(mapStateToProps, actionCreators)
    }
    UnsubscribeV2State "1" o-- "1" UnsubscribeV2 : provides
    mapStateToProps ..> State : reads
    mapStateToProps ..> UnsubscribeV2State : calls selectors
    actionCreators ..> UnsubscribeV2State : sourced from actionCreators
    connect_HOC --> mapStateToProps : uses
    connect_HOC --> actionCreators : uses
    connect_HOC --> UnsubscribeV2 : wraps
    connect_HOC --> "ConnectedUnsubscribeV2" : returns
    class ConnectedUnsubscribeV2 {
        <<default export>>
    }
```

> SVG rendering failed for this diagram.
