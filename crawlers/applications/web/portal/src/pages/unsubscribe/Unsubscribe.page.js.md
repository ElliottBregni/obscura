# Diagram: web/portal/src/pages/unsubscribe/Unsubscribe.page.js


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
flowchart LR
  A[Component Mount] --> B[resetUnsubscribe()]
  B --> C{guid?}
  C -- yes --> D[fetchSubscription(guid)]
  C -- no --> E[setHasData(false)]
  B --> F{category?}
  F -- no --> E
  D --> G[isFetchingSubscription? render spinner]
  D --> H[subscription received]
  H --> I[fetchOrganizationImages(subscription.organization_id)]
  H --> J{category === "email" && !recipient_email?}
  J -- yes --> E
  H --> K{category === "phone" && !mobile_number?}
  K -- yes --> E
  H --> L{!reference_id?}
  L -- yes --> E
  H --> M[determine service from reference_type]
  M --> N[render form when hasData && subscription && !isFetchingSubscription]
  N --> O{isUnsubscribeComplete?}
  O -- no --> P[show details + disabled TextInput + Submit Button]
  P --> Q[onSubmit -> startUnsubscribe()]
  Q --> R[unsubscribe(guid, category) -> isUnsubscribing]
  R --> S{success?}
  S -- yes --> T[isUnsubscribeComplete = true -> show success Alert]
  S -- no --> U[isUnsubscribeError -> show error Alert]
  G --> N
  E --> V[show Danger Alert: Invalid options]
  V --> end
  U --> end
  T --> end
```

> SVG rendering failed for this diagram.
