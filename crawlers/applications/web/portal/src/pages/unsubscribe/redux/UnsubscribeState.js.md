# Diagram: web/portal/src/pages/unsubscribe/redux/UnsubscribeState.js


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
classDiagram
    class Constants {
        +UNSUBSCRIBE_URL
        +ORGANIZATIONS_URL
        +STORE_MOUNT_POINT
        +UNSUBSCRIBE_RESET
        +FETCH_SUBSCRIPTION_STARTED
        +FETCH_SUBSCRIPTION_SUCCESS
        +FETCH_SUBSCRIPTION_FAILED
        +UNSUBSCRIBE_STARTED
        +UNSUBSCRIBE_SUCCESS
        +UNSUBSCRIBE_FAILED
        +FETCH_ORGANIZATION_IMAGES
    }

    class InitialState {
        +subscription
        +isFetchingSubscription
        +isFetchSubscriptionError
        +fetchSubscriptionErrorMessage
        +isUnsubscribeComplete
        +isUnsubscribing
        +isUnsubscribeError
        +unsubscribeErrorMessage
    }

    class ActionCreators {
        +resetUnsubscribe(dispatch)
        +fetchSubscription(guid)(dispatch)
        +fetchOrganizationImages(orgId)(dispatch)
        +unsubscribe(guid, category)(dispatch)
    }

    class Selectors {
        +getSubscription(state)
        +getIsFetchingSubscription(state)
        +getIsFetchSubscriptionError(state)
        +getFetchSubscriptionErrorMessage(state)
        +getOrganizationImageConfig(state)
        +getIsUnsubscribeComplete(state)
        +getIsUnsubscribing(state)
        +getIsUnsubscribeError(state)
        +getUnsubscribeErrorMessage(state)
    }

    class UnsubscribeReducer {
        +UnsubscribeReducer(state, action)
    }

    class UnsubscribeState {
        +mountPoint
        +actionCreators
        +selectors
        +reducer
    }

    class Axios {
        +get(url)
        +post(url, payload)
    }

    Constants --> ActionCreators : used by
    ActionCreators ..> Axios : calls
    ActionCreators --> UnsubscribeReducer : dispatches actions to
    ActionCreators --> Constants : reference URLs and action types
    UnsubscribeReducer --> InitialState : initializes from
    UnsubscribeState --> ActionCreators : exposes
    UnsubscribeState --> Selectors : exposes
    UnsubscribeState --> UnsubscribeReducer : exposes
    Selectors --> InitialState : read fields from
    Axios <..> Constants : constructs URLs with
    InitialState <|-- UnsubscribeReducer : returns state shaped like
    class Authentication <<interface>>
    note right of UnsubscribeReducer : Handles action types:\nUNSUBSCRIBE_RESET, FETCH_SUBSCRIPTION_*,\nFETCH_ORGANIZATION_IMAGES, UNSUBSCRIBE_*
```

> SVG rendering failed for this diagram.
