# Diagram: web/portal/src/pages/damageview/components/search/DamageView.searchOptions.js


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
classDiagram
    class "search-filter-query-strings" {
        +getBasicQueryString()
        +getMultiSelectQueryString()
        +getDateRangeQueryString()
        +getNQueryStringFilterValuePriority()
    }
    class "FilterButtons" {
        +AsyncSelectFilterButton
        +MultiSelectFilterButton
        +NFilterButton
        +DateRangeFilterButton
    }
    class "Organization.const" {
        +OrganizationType
    }
    class "SearchableSelectList" {
        +SELECT_ALL_OPTION_VALUE
        +SELECT_EMPTY_OPTION_VALUE
    }
    class "DamageViewLoaders" {
        +damageAreaOptionsState
        +damageViewAssigneesOptionsState
        +damageViewPartnersOptionsState
        +currentPositionsCodeOptionsState
        +productTypeOptionsState
        +lastMilestoneOptionsState
        +stolenVinOptionsState
    }
    class "translation.utils" {
        +useDamageViewStatusTranslation()
    }

    class SEARCH_CATEGORIES {
        +[{queryKey, label, placeholder, queryBuilder, loaderProperty, property, loadingProperty}]
    }
    class SEARCH_FILTERS {
        +[{queryKey, label, optionsGetter, Component, queryBuilder, ...}]
    }
    class SubmissionsByStatusFilters {
        +[{queryKey, Component, isMulti, label, optionsState, queryBuilder}]
    }

    "SEARCH_CATEGORIES" --> "search-filter-query-strings" : uses queryBuilder
    "SEARCH_FILTERS" --> "search-filter-query-strings" : uses getMultiSelect/getDateRange/getN/getBasic
    "SEARCH_FILTERS" --> "FilterButtons" : uses Component types
    "SEARCH_FILTERS" --> "DamageViewLoaders" : references optionsState
    "SEARCH_FILTERS" --> "SearchableSelectList" : checks SELECT_ALL/EMPTY
    "SEARCH_FILTERS" --> "Organization.const" : requiredOrgTypes
    "SEARCH_FILTERS" --> "translation.utils" : uses translation hook
    "SubmissionsByStatusFilters" --> "FilterButtons" : uses AsyncSelectFilterButton
    "SubmissionsByStatusFilters" --> "DamageViewLoaders" : uses productTypeOptionsState

    %% Detail relationships for key filters
    class StatusFilter {
        +queryKey: "status"
        +Component: MultiSelectFilterButton
        +queryBuilder: getMultiSelectQueryString
        +hideSelectEmpty: true
    }
    class InProgressStatusFilter {
        +queryKey: "inProgressStatus"
        +Component: MultiSelectFilterButton
        +queryBuilder: custom builder (handles SELECT_ALL/EMPTY)
    }
    class AssigneeFilter {
        +queryKey: "assignee"
        +Component: AsyncSelectFilterButton
        +isMulti: true
        +optionsState: damageViewAssigneesOptionsState
    }
    class DateRangeFilter {
        +queryKey: "submission_date"
        +Component: DateRangeFilterButton
        +queryBuilder: getDateRangeQueryString
        +isValueValid: custom validation using lodash
    }
    class NFilter {
        +queryKey: ["currentPositionTypes","currentPositionCodes"]
        +Component: NFilterButton
        +queryBuilder: getNQueryStringFilterValuePriority
    }
    class DamageAreaFilter {
        +queryKey: "damageArea"
        +Component: AsyncSelectFilterButton
        +isMulti: true
        +queryBuilder: custom builder (handles SELECT_ALL/EMPTY)
    }

    "SEARCH_FILTERS" --> StatusFilter
    "SEARCH_FILTERS" --> InProgressStatusFilter
    "SEARCH_FILTERS" --> AssigneeFilter
    "SEARCH_FILTERS" --> DateRangeFilter
    "SEARCH_FILTERS" --> NFilter
    "SEARCH_FILTERS" --> DamageAreaFilter

    StatusFilter --> "translation.utils" : translates labels
    InProgressStatusFilter --> "translation.utils" : translates labels
    DateRangeFilter --> "lodash" : uses _.isNil()
```

> SVG rendering failed for this diagram.
