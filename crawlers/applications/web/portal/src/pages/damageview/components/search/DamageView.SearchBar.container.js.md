# Diagram: web/portal/src/pages/damageview/components/search/DamageView.SearchBar.container.js


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
graph LR
  SearchBarComponent[SearchBar Component]
  Connect[connect HOC]
  MapState[mapStateToProps]
  MapDispatch[mapDispatchToProps]
  MergeProps[mergeProps]
  ReduxStore[Redux Store (state)]
  SearchBarStateSelectors[SearchBarState.selectors]
  SearchBarStateActions[SearchBarState.actionCreators]
  DamageDomainActions[damageViewDomainData.actionCreators]
  OrganizationsSelectors[getSolutionId]
  AuthSelectors[getAuthorization]

  SearchBarComponent -->|wrapped by| Connect
  Connect --> MapState
  Connect --> MapDispatch
  Connect --> MergeProps
  MapState --> SearchBarStateSelectors
  MapState --> OrganizationsSelectors
  MapState --> AuthSelectors
  MapState -->|reads from| ReduxStore
  MapDispatch --> SearchBarStateActions
  MapDispatch --> DamageDomainActions
  MapDispatch -->|dispatches to| ReduxStore
  MergeProps -->|combines| MapState
  MergeProps -->|combines| MapDispatch
  MergeProps -->|includes| OwnProps[ownProps]
  MergeProps --> SearchBarComponent

  subgraph StateSelectors [Selectors]
    getSearchText[getSearchText]
    getSearchCategory[getSearchCategory]
    getIgnoreSearchCategory[getIgnoreSearchCategory]
    getTypeaheadOptionsMetadata[getTypeaheadOptionsMetadata]
    getHasSearchCriteriaChanged[getHasSearchCriteriaChanged]
    getVins[getVins]
    getVinsLoading[getVinsLoading]
    getCanUserSearch[getCanUserSearch]
  end

  SearchBarStateSelectors --> getSearchText
  SearchBarStateSelectors --> getSearchCategory
  SearchBarStateSelectors --> getIgnoreSearchCategory
  SearchBarStateSelectors --> getTypeaheadOptionsMetadata
  SearchBarStateSelectors --> getHasSearchCriteriaChanged
  SearchBarStateSelectors --> getVins
  SearchBarStateSelectors --> getVinsLoading
  SearchBarStateSelectors --> getCanUserSearch

  subgraph ActionCreators [Action Creators]
    setSearchCategoryForKey[setSearchCategoryForKey]
    setSearchTextAct[setSearchText]
    clearSearchText[clearSearchText]
    resetSearchBar[resetSearchBar]
    searchEntities[searchEntities]
    clearSavedSearch[clearSavedSearch]
    searchVins[searchVins]
    fetchDomainData[fetchDomainData]
  end

  SearchBarStateActions --> setSearchCategoryForKey
  SearchBarStateActions --> setSearchTextAct
  SearchBarStateActions --> clearSearchText
  SearchBarStateActions --> resetSearchBar
  SearchBarStateActions --> searchEntities
  SearchBarStateActions --> clearSavedSearch
  SearchBarStateActions --> searchVins
  DamageDomainActions --> fetchDomainData

  MapState -->|provides props| StateProps[State Props]
  StateProps --> auth[auth = getAuthorization(state)]
  StateProps --> typeahead[typeaheadOptionsMetadata]
  StateProps --> searchText[searchText]
  StateProps --> searchCategory[searchCategory]
  StateProps --> ignoreSearchCategory[ignoreSearchCategory]
  StateProps --> hasSearchCriteriaChanged[hasSearchCriteriaChanged]
  StateProps --> solutionId[solutionId = getSolutionId(state)]
  StateProps --> vins[vins]
  StateProps --> vinsLoading[vinsLoading]
  StateProps --> canUserSearch[canUserSearch]

  MapDispatch -->|provides props| DispatchProps[Dispatch Props]
  DispatchProps --> setSearchCategory[setSearchCategory(key)]
  DispatchProps --> setSearchTextFn[setSearchText(text, ignoreValueChange)]
  DispatchProps --> clearSavedSearchFn[clearSavedSearch()]
  DispatchProps --> clearSearchTextFn[clearSearchText()]
  DispatchProps --> resetSearchBarFn[resetSearchBar()]
  DispatchProps --> searchEntitiesFn[searchEntities(solutionId, resetPagination, preventRedirect)]
  DispatchProps --> searchVinsFn[searchVins(query)]
  DispatchProps --> fetchDomainDataFn[fetchDomainData(solutionId)]

  StateProps --> MergeProps
  DispatchProps --> MergeProps
  OwnProps --> MergeProps
  MergeProps -->|final props| SearchBarComponent
```

> SVG rendering failed for this diagram.
