# Diagram: web/portal/src/pages/damageview/redux/DamageViewSearchBarState.js


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
classDiagram
    class Module {
        +STORE_MOUNT_POINT: string
        +APPLICATION_BASE_URL: string
    }

    class Imports {
        +_ : lodash
        +moment
        +axios
        +qs
        +SEARCH_CATEGORIES
        +SEARCH_FILTERS
        +buildSearchBarState()
        +searchVins()
        +vinsReducer()
        +chainReducers()
        +apiUrl()
    }

    class Utilities {
        +entitiesUrl(queryString)
        +watchUrl(id)
    }

    class Actions {
        +fetchSearch(queryString, solutionId, duck, dispatch, state)
        +watchActions(solutionId, id, watch=true)
        +saveBatchAssigned(selectedAssignee, selectedIds)
    }

    class Selectors {
        +getVins(state)
        +getVinsLoading(state)
    }

    class SearchBarState {
        +actionCreators
        +selectors
        +reducer
    }

    Module --> Imports : "depends on"
    Module --> Utilities : "defines"
    Module --> Actions : "defines"
    Module --> Selectors : "defines"
    Module --> SearchBarState : "creates"

    Imports --> Utilities : "uses apiUrl, qs, moment"
    Utilities --> Actions : "used by"
    Actions --> SearchBarState : "attached to actionCreators"
    Selectors --> SearchBarState : "attached to selectors"
    SearchBarState o-- "vinsReducer" : "reducer composition"
    SearchBarState ..> buildSearchBarState : "constructed by"
    SearchBarState ..> chainReducers : "chainReducers([reducer, vinsReducer])"
    Actions ..> axios : "HTTP client"
    Utilities ..> apiUrl : "builds URLs"
    Actions ..> qs : "parse/stringify"
    Actions ..> moment : "tz.guess()"

    class entitiesUrl {
        +returns URL string
    }
    class watchUrl {
        +returns watch endpoint
    }
    class fetchSearch {
        +dispatches duck.fetch(url, config)
        +dispatches DAMAGE_VIEW_SEARCH action
    }
    class watchActions {
        +POST/DELETE watch endpoint
        +dispatches searchEntities on success
    }
    class saveBatchAssigned {
        +POST batch assignment
    }
    class getVins {
        +returns vins array
    }
    class getVinsLoading {
        +returns vinsLoading boolean
    }

    Utilities --> entitiesUrl
    Utilities --> watchUrl
    Actions --> fetchSearch
    Actions --> watchActions
    Actions --> saveBatchAssigned
    Selectors --> getVins
    Selectors --> getVinsLoading

    %% relationships summarizing exports
    SearchBarState "1" --o "1" Module : "export default"
```

> SVG rendering failed for this diagram.
