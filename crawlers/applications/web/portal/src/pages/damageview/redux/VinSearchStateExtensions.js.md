# Diagram: web/portal/src/pages/damageview/redux/VinSearchStateExtensions.js


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
flowchart LR
  A[searchVins(query)] --> B{solutionId && isShipperRole?}
  B -- yes --> C[use solution-specific vinsUrl(query)]
  B -- no --> D[use default vinsUrl(query)]
  C --> E{query.length < 4?}
  D --> E
  E -- yes --> F[return null]
  E -- no --> G[dispatch FETCH_VINS]
  G --> H[build URL via getUrl(query)]
  H --> I[request = () => axios.get(url)]
  I --> J[debounceRequest = debouncePromise(request, 500)]
  J --> K[debounceRequest() -> Promise]
  K --> L{response received?}
  L -- success --> M[extract response.data.vins or []]
  M --> N[vinOptions = data.map(vin => vin[0])]
  N --> O[dispatch RECEIVE_VINS payload: vinOptions]
  L -- error --> P[console.error(err) && dispatch RECEIVE_VINS payload: []]
  O --> R[vinsReducer handles RECEIVE_VINS]
  G --> Q[vinsReducer handles FETCH_VINS]
  Q --> S[state: vinsLoading = true; vins = []]
  R --> T[state: vinsLoading = false; vins = action.payload]
  S --> U[store updated]
  T --> U
```

> SVG rendering failed for this diagram.
