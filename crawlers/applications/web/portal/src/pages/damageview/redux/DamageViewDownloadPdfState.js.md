# Diagram: web/portal/src/pages/damageview/redux/DamageViewDownloadPdfState.js


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
flowchart LR
  A[exportAsyncPDF(submissionId, locale, fileName)] --> B[getState -> getSolutionId]
  B --> C{solutionId?}
  C -->|yes| D[baseUrl = /damageview/solution_id/{solutionId}/submission/{submissionId}?]
  C -->|no| E[baseUrl = /damageview/submission/{submissionId}?]
  D --> F[queryParams: asyncExport=true,fileName,locale]
  E --> F
  F --> G[url = baseUrl + qs.stringify(queryParams)]
  G --> H[axios.get(url, axiosConfig)]
  A --> I[dispatch(EXPORT_REQUEST)]
  H -->|fulfilled| J[response.data.pdfIdentifier -> identifier]
  J --> K[dispatch(EXPORT_IDENTIFIER_RECEIVED {identifier, exportName})]
  K --> L[dispatch(EXPORT_SEARCH_SUCCEEDED)]
  H -->|rejected| M[error]
  M --> N[dispatch(EXPORT_SEARCH_FAILED)]
  subgraph Reducer [ExportPDFReducer state transitions]
    R0[(initialState)]
    R0 -->|RESET_EXPORT| R1[isExporting=false, exportFailed=false, exportIdentifier="", exportName=""]
    R0 -->|EXPORT_REQUEST| R2[isExporting=true, exportFailed=false]
    R2 -->|EXPORT_IDENTIFIER_RECEIVED| R3[exportIdentifier=action.identifier, exportName=action.exportName, isExporting=false]
    R2 -->|EXPORT_SEARCH_FAILED| R4[isExporting=false, exportIdentifier="", exportName="", exportFailed=true]
    R4 -->|CLEAR_EXPORT_ERRORS| R5[exportFailed=false]
  end
  K --> R3
  I --> R2
  N --> R4
  note right of H: axiosConfig.headers include x-time-zone and accept: application/pdf
```

> SVG rendering failed for this diagram.
