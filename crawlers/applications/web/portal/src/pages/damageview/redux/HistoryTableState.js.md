# Diagram: web/portal/src/pages/damageview/redux/HistoryTableState.js


> Auto-generated by Obscura crawlers

## Diagram 1

```mermaid
classDiagram
    class Module {
        +fetchDamageHistoryUrl(submission_id, queryString)
        +transformResponse(response)
        +fetchDamageHistory(queryString, submission_id, duck, dispatch)
        +DamageViewHistoryTableState
    }
    class SearchBarBuilder {
        +buildSearchBarState(mountPoint, [], [], fetchFunc, [], {})
    }
    Module --> SearchBarBuilder : uses
    Module : STORE_MOUNT_POINT = "damageViewHistoryTable"
    Module : export default DamageViewHistoryTableState
```

> SVG rendering failed for this diagram.

## Diagram 2

```mermaid
flowchart TD
    A[fetchDamageHistory(queryString, submission_id, duck, dispatch)] -->|calls| B[fetchDamageHistoryUrl(submission_id, queryString)]
    A -->|calls| C[dispatch(duck.fetch(url, {}, transformResponse))]
    B --> D[customerApiUrl(...)]
    C --> E[duck.fetch(url, {}, transformResponse)]
    E -->|on response| F[transformResponse(response)]
    F --> G[map response.data.items -> updatedData]
    G --> H{item.eventType === "CREATE"}
    H -->|yes| I[push status Submitted change object]
    H -->|no| J[push original changes]
    F --> K[return { data: updatedData, meta: response.meta }]
    L[DamageViewHistoryTableState] -->|created by| M[buildSearchBarState(STORE_MOUNT_POINT, [], [], fetchDamageHistory, [], {})]
```

> SVG rendering failed for this diagram.
