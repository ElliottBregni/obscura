# Diagram: web/portal/src/pages/damageview/dashboard/components/DamageView.ImageUploader.container.js


> Auto-generated by Obscura crawlers

## Diagram 1

```mermaid
classDiagram
class ImageUploaderContainer {
  +props: data, imagesData, category, formData, setFormData, className
  -state: files, commentData
  -hooks: useEffect, useState, useDamageViewFormLabelTranslation
  +render(): li element
}
class DamageViewImageUploader {
  +props: files, setFiles, setCommentData, commentData, imagesData, name, onChange, style
}
class CustomLabel {
  +styled span
}
class Colors {
  +text.RED
}
ImageUploaderContainer --> DamageViewImageUploader : uses
ImageUploaderContainer --> CustomLabel : renders when data.isRequired
ImageUploaderContainer --> Colors : references
ImageUploaderContainer ..> "useDamageViewFormLabelTranslation" : calls
ImageUploaderContainer o-- "useState(files)" : files
ImageUploaderContainer o-- "useState(commentData)" : commentData
DamageViewImageUploader <.. ImageUploaderContainer : onChange callback
```

> SVG rendering failed for this diagram.

## Diagram 2

```mermaid
flowchart TD
  A[props: data, imagesData, category, formData, setFormData] --> B{initialize state}
  B --> C[files = imagesData[category].files]
  B --> D[commentData = imagesData[category].comments]
  C --> E[render label: CustomLabel if data.isRequired else span]
  D --> E
  E --> F[DamageViewImageUploader receives props]
  F --> G[User updates images or comments]
  G --> H[onChange(name, val) invoked]
  H --> I{_.isEqual(formData.uploadPhotos[name], val)?}
  I -- no --> J[setFormData({ ...formData.uploadPhotos, [name]: { ...val } })]
  I -- yes --> K[no update]
```

> SVG rendering failed for this diagram.
