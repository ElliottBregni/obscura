# Diagram: web/portal/src/pages/damageview/dashboard/components/DamageView.WatchedSubmissionsWidget.js


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
flowchart LR
  A[WatchedSubmissionsWidget] -->|renders| B[PanelGroup]
  B --> C[PanelGroup.Header]
  B --> D[PanelGroup.Content]
  D --> E[BaseTable]
  A -->|uses| F[useTranslation("damage-view")]
  A -->|uses| G[useState: unwatchedSubmissions]
  A -->|usesEffect on mount| H[fetchWatchedSubmissionsData(solutionId)]
  E -->|props: data, columns, theme, isLoading, pagination...| I[useWatchedSubmissionColumns]
  I -->|depends on| J[solutionId, exportSubmissonData, userPreferences, toggleWatch]
  A -->|defines| K[toggleWatch(submission)]
  K -->|updates| G
  K -->|clears/sets| L[unwatchTimers[submission]]
  L -->|after 2000ms calls| H
  L -->|after 2000ms calls| M[fetchMySubmissionsData(solutionId)]
  E -->|onPageChange| N[setWatchedSubmissionsPageIndex(solutionId, newPage, pageSize)]
  E -->|rowClickHandler| O{cell.column.id === "watch"?}
  O -- yes --> P[no-op]
  O -- no --> Q{row.original.id?}
  Q -- no --> P
  Q -- yes --> R[pushDamageViewDetailsPage(row.original.id)]
  E -->|rowDisabledHandler| S{unwatchedSubmissions.includes(submissionId)}
  style A fill:#f9f,stroke:#333,stroke-width:1px
  style E fill:#fffbcc,stroke:#333,stroke-width:1px
```

> SVG rendering failed for this diagram.
