# Diagram: web/portal/src/pages/damageview/dashboard/components/DamageView.Dashboard.columns.js


> Auto-generated by Obscura crawlers

## Diagram 1

```mermaid
classDiagram
    class useWatchedSubmissionColumns {
        +solutionId
        +exportSubmissonData
        +userPreferences
        +toggleHandler
        +returns columns[]
    }
    class useMySubmissionColumns {
        +solutionId
        +exportSubmissonData
        +userPreferences
        +toggleHandler
        +returns columns[]
    }
    class WatchToggle {
        +checked
        +onClick(e)
        +onChange(newValue)
        +iconSize
        +color
        +checkedColor
    }
    class DamageViewWatchedSubmissionsWidgetState {
        +actionCreators
    }
    class Cells {
        +SubmissionIDCell()
        +VinsCell()
        +StatusCell()
        +AssigneeCell()
        +SubmitterCell()
        +SubmissionDateCell()
        +InspectionDateCell()
        +ReportingLocationCell()
        +LastMilestoneCell()
        +InProgressStatusCell()
        +StolenVinCell()
    }
    class Helpers {
        +getLastMilestones(vins, t)
        +getVins(vins)
    }
    class Hooks {
        +useDispatch()
        +useTranslation(ns)
    }

    useWatchedSubmissionColumns --> Hooks : uses
    useMySubmissionColumns --> useWatchedSubmissionColumns : composes
    useWatchedSubmissionColumns --> WatchToggle : renders
    WatchToggle --> DamageViewWatchedSubmissionsWidgetState : dispatches actions
    useWatchedSubmissionColumns --> Cells : assigns Cell components
    Cells --> Helpers : calls
    Helpers ..> "VIN data" : transforms
```

> SVG rendering failed for this diagram.

## Diagram 2

```mermaid
flowchart TD
    A[Table Row] --> B{Column: Watch}
    B --> C[WatchToggle component]
    C -->|onChange(newWatchValue)| D[dispatch watch action]
    D --> E[DamageViewWatchedSubmissionsWidgetState.actionCreators.watchActions]
    E --> F[API / State Update]
    F --> G{toggleHandler provided?}
    G -->|yes| H[toggleHandler(submissionId)]
    G -->|no| I[no-op]
    subgraph ColumnRender
        J[SubmissionIDCell]
        K[VinsCell]
        L[LastMilestoneCell]
        M[StatusCell]
        N[AssigneeCell]
    end
    A --> ColumnRender
    K --> Helpers2[getVins/getLastMilestones]
    Helpers2 --> L
```

> SVG rendering failed for this diagram.
