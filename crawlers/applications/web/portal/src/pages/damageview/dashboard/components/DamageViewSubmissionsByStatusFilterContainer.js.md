# Diagram: web/portal/src/pages/damageview/dashboard/components/DamageViewSubmissionsByStatusFilterContainer.js


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
flowchart LR
  subgraph Redux
    State[Application State]
    DamageState[DamageViewSubmissionsByStatusState]
    DamageSelectors[selectors.getSearchFilters()]
    DamageActions[actionCreators: setSearchFilter, clearSearchFilter, searchEntities, resetSavedSearch, clearSavedSearch]
  end

  subgraph LocalModule
    mapStateToProps[mapStateToProps(state)]
    actionCreatorsWrapper[actionCreators]
    mergeProps[mergeProps(stateProps, dispatchProps, ownProps)]
    SubmissionsByStatusFilters[SubmissionsByStatusFilters]
    FilterSectionComp[FilterSection Component]
  end

  State -->|passed to| DamageSelectors
  DamageState --> DamageSelectors
  DamageState --> DamageActions

  DamageSelectors -->|provides searchFilters| mapStateToProps
  mapStateToProps -->|returns searchFilters prop| mergeProps

  DamageActions --> actionCreatorsWrapper

  actionCreatorsWrapper --> setSearchFilterWrapper[setSearchFilter(key,value) → dispatch(setSubmissionsByStatusSearchFilter) & dispatch(searchEntities)]
  actionCreatorsWrapper --> clearSearchFilterWrapper[clearSearchFilter(key) → dispatch(clearSubmissionsByStatusSearchFilter) & dispatch(searchEntities)]
  actionCreatorsWrapper --> searchEntitiesFn[searchEntities]
  actionCreatorsWrapper --> resetSavedSearchFn[resetSavedSearch]
  actionCreatorsWrapper --> clearSavedSearchFn[clearSavedSearch]

  setSearchFilterWrapper -->|dispatches| DamageActions
  clearSearchFilterWrapper -->|dispatches| DamageActions
  searchEntitiesFn -->|dispatches| DamageActions

  mergeProps -->|includes filtersMetadata| SubmissionsByStatusFilters
  mergeProps -->|merges props| FilterSectionComp

  connect[connect(mapStateToProps, actionCreators, mergeProps)] --> FilterSectionComp
  mapStateToProps --> connect
  actionCreatorsWrapper --> connect
  mergeProps --> connect
```

> SVG rendering failed for this diagram.
