# Diagram: web/portal/src/pages/damageview/dashboard/components/DamageView.MySubmissionsWidget.js


> Auto-generated by Obscura crawlers

## Diagram 1

```mermaid
classDiagram
class MySubmissionsWidget {
  +string solutionId
  +bool isLoading
  +array mySubmissionsData
  +func fetchWatchedSubmissionsData()
  +func fetchMySubmissionsData()
  +number mySubmissionsPageIndex
  +number mySubmissionsPageSize
  +number mySubmissionsPageCount
  +func setMySubmissionsPageIndex()
  +func exportSubmissonData()
  +object userPreferences
  +func pushDamageViewDetailsPage()
  +string sortColumn
  +bool reverseSort
  +string defaultSortColumn
  +bool defaultReverseSort
  +func setSort()
  -array unwatchedSubmissions
  -object unwatchTimers
  +toggleWatch(submission)
  +useEffect(fetchMySubmissionsData)
}
class PanelGroup {
  +Header(title)
  +Content(children)
}
class BaseTable {
  +data
  +columns
  +theme
  +isLoading
  +showPagination
  +isManualPagination
  +isManualSorting
  +pageIndex
  +pageSize
  +pageCount
  +sortColumn
  +reverseSort
  +defaultSortColumn
  +defaultReverseSort
  +onPageChange()
  +onPageSort()
  +rowClickHandler()
  +rowDisabledHandler()
}
class useMySubmissionColumns {
  +(solutionId, exportSubmissonData, userPreferences, toggleWatch) : columns
}
MySubmissionsWidget --> PanelGroup : renders
MySubmissionsWidget --> BaseTable : uses
MySubmissionsWidget --> useMySubmissionColumns : invokes
MySubmissionsWidget o-- "unwatchedSubmissions" : state
MySubmissionsWidget o-- "unwatchTimers" : timers
```

> SVG rendering failed for this diagram.

## Diagram 2

```mermaid
flowchart TD
  A[User clicks watch checkbox] --> B{Is timer active for submission?}
  B -- Yes --> C[clearTimeout(timer)]
  B -- No --> D[continue]
  D --> E{Submission currently unwatched?}
  E -- Yes --> F[remove submission from unwatchedSubmissions]
  E -- No --> G[add submission to unwatchedSubmissions]
  F --> H[set unwatchedSubmissions state]
  G --> H
  H --> I[setTimeout(2000)]
  I --> J[fetchWatchedSubmissionsData(solutionId)]
  J --> K[fetchMySubmissionsData(solutionId)]
  K --> L[set unwatchedSubmissions([])]
  I -. stores timer .-> M[unwatchTimers[submission]]
```

> SVG rendering failed for this diagram.
