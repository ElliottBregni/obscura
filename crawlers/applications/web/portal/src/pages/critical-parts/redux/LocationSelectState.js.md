# Diagram: web/portal/src/pages/critical-parts/redux/LocationSelectState.js


> Auto-generated by Obscura crawlers

## Diagram 1

```mermaid
flowchart LR
  FL[fetchLocations(currentVal, searchText, pageNumber, pageSize=20)]
  FBY[fetchLocationById(locationId)]
  FCP[fetchCpEnabled(locationId)]
  API[apiUrl("/location/...")]
  AX[axios.get(url, { params })]
  AX2[axios.get(url)]
  MAP[map response.data.data → {label, value, data}]
  META[response.data.meta]
  RET_OK[return { options, hasMore, additional }]
  RET_ERR[return { options: [], hasMore: false, additional }]
  RET_BYID[return { label, value, data } | null]
  RET_CP[return enabled | null]

  FL -->|construct url| API
  FL -->|call axios.get with params| AX
  AX --> MAP
  AX --> META
  MAP --> RET_OK
  META --> RET_OK
  AX -->|throws| RET_ERR
  FL -.-> RET_ERR

  FBY -->|construct url with id| API
  FBY -->|call axios.get| AX2
  AX2 -->|on success| RET_BYID
  AX2 -->|throws| RET_BYID

  FCP -->|construct feature url| API
  FCP -->|call axios.get| AX2
  AX2 -->|response.data.enabled| RET_CP
  AX2 -->|throws| RET_CP
```

> SVG rendering failed for this diagram.

## Diagram 2

```mermaid
sequenceDiagram
  participant C as Caller
  participant FL as fetchLocations
  participant API as apiUrl
  participant AX as axios
  participant Mapper as map
  participant Return as ReturnObject

  C->>FL: invoke with currentVal, searchText, pageNumber
  FL->>API: build /location/locations URL
  FL->>AX: GET url with params
  AX-->>FL: response (data, meta) or error
  alt success
    FL->>Mapper: map response.data.data to options
    FL->>Return: compute hasMore from meta
    FL-->>C: return { options, hasMore, additional }
  else error
    FL-->>C: return { options: [], hasMore: false, additional }
```

> SVG rendering failed for this diagram.
