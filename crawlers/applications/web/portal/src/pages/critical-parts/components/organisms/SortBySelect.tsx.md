# Diagram: web/portal/src/pages/critical-parts/components/organisms/SortBySelect.tsx


> Auto-generated by Obscura crawlers

## Diagram 1

```mermaid
classDiagram
    class SortKey {
        +string value
        +string label
    }
    class SortBySelect {
        +props: SortBySelectProps
        +t(): string
        +sortKeys: SortKey[]
        +sortKeyHashMap: {string:SortKey}
        +setDir: (boolean) => void
        +dir: boolean
        +label(color?:string): object
        +styles: StylesConfig
        +SingleValue(children, props): JSX.Element
        +render(): JSX.Element
    }
    class SelectAsync {
        +isSearchable: boolean
        +options: SortKey[]
        +onChange(selected): void
        +value: SortKey
        +styles: StylesConfig
        +components: object
    }
    class Button {
        +onMouseDown(event): void
        +size: string
    }
    class NoSeparator {
        +render(): null
    }
    class Constants {
        +CP_SORT_BY_RUN_OUT_BY = "runOutBy"
        +CP_SORT_BY_MUST_SHIP_BY = "mustShipBy"
        +CP_SORT_DIRECTION_ASCENDING = false
        +CP_SORT_DIRECTION_DESCENDING = true
    }
    SortBySelect o-- SortKey : uses >
    SortBySelect --> SelectAsync : renders
    SortBySelect --> Button : uses in SingleValue
    SelectAsync ..|> components.Component : integrates
    SortBySelect --> NoSeparator : supplies as IndicatorSeparator
    SortBySelect --> Constants : references
```

> SVG rendering failed for this diagram.

## Diagram 2

```mermaid
graph TD
    A[SortBySelect Component] --> B[initialize t via useTranslation]
    A --> C[build sortKeys array]
    C --> D[create sortKeyHashMap]
    A --> E[define label(style) helper]
    A --> F[define styles config]
    A --> G[define SingleValue wrapper]
    G --> H[Button inside SingleValue]
    H -- onMouseDown --> I[toggles direction via setDirection(!dir)]
    A --> J[Render SelectAsync]
    J --> K[props: isSearchable=false, options=sortKeys, value=sortKeyHashMap[props.sort]]
    J --> L[onChange -> props.setSort(selected?.value)]
    J --> M[components: { IndicatorSeparator: NoSeparator, SingleValue }]
    subgraph User interaction
        U[User selects option] --> J
        U2[User clicks sort direction Button] --> H
    end
    I --> A[SortBySelect state updated]
```

> SVG rendering failed for this diagram.
