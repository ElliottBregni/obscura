# Diagram: web/portal/src/pages/critical-parts/components/organisms/LocationSelect.js


> Auto-generated by Obscura crawlers

## Diagram 1

```mermaid
classDiagram
class LocationSelect {
  +object selectedLocation
  +func setSelectedLocation()
  +string defaultLocationId
  -string searchText
  -useEffect(loadDefaultLocation)
  -async loadOptions(currentVal, loadedOptions, additionalOptions)
  -formatOptionLabel(data, {context})
}
class AsyncPaginate {
  +prop debounceTimeout
  +prop isMulti
  +prop cacheOptions
  +prop onInputChange(val)
  +prop placeholder
  +prop value
  +prop loadOptions
  +prop formatOptionLabel
  +prop onChange(selectedData)
}
class fetchLocations {
  <<function>>
}
class queryClient {
  +fetchQuery({queryKey, queryFn})
}
LocationSelect "1" --> "1" AsyncPaginate : renders
LocationSelect "1" --> "1" queryClient : uses
LocationSelect "1" --> "1" fetchLocations : calls
AsyncPaginate --> LocationSelect : invokes loadOptions
AsyncPaginate --> LocationSelect : invokes formatOptionLabel
LocationSelect ..> PropTypes : validates props
```

> SVG rendering failed for this diagram.

## Diagram 2

```mermaid
flowchart LR
  Render[Render LocationSelect] --> CheckSelected{selectedLocation present?}
  CheckSelected -- Yes --> SkipDefault[Skip loading default]
  CheckSelected -- No --> LoadDefault[Call loadDefaultLocation()]
  LoadDefault --> HasDefaultId{defaultLocationId provided?}
  HasDefaultId -- No --> FetchFirst[fetchLocations("", "", 0, 1)]
  FetchFirst --> HasOptions{response.options exists?}
  HasOptions -- Yes --> SetFirst[setSelectedLocation(firstOption)]
  HasOptions -- No --> LogError[console.error("No locations available")]
  Render --> Async[AsyncPaginate UI]
  Async --> Input[onInputChange -> setSearchText]
  Async --> Request[loadOptions called by AsyncPaginate]
  Request --> Query[queryClient.fetchQuery(queryKey, queryFn)]
  Query --> fetchLocationsCall[fetchLocations(currentVal, searchText, pageNumber)]
  Query --> ReturnOptions[return {options, hasMore, additional}]
  Async --> Select[onChange -> setSelectedLocation(selectedData)]
  Async --> FormatLabel[formatOptionLabel used for value/context]
```

> SVG rendering failed for this diagram.
