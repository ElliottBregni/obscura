# Diagram: web/portal/src/pages/critical-parts/search/CriticalParts.Search.page.container.js


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
flowchart LR
  subgraph React
    CPS[CriticalPartsSearch]
    CONNECT[connect(mapStateToProps, mapDispatchToProps)]
  end

  subgraph ReduxSelectors["Selectors / State Getters"]
    UPIsLoading[ProfileState.selectors.getUserPreferencesIsLoading]
    UPIsLoadingError[ProfileState.selectors.getUserPreferencesIsLoadingError]
    UP[ProfileState.selectors.getUserPreferences]
    getSearchText[getSearchText]
    getSortColumn[getSortColumn]
    getSortReverse[getSortReverse]
    getFiltersValue[getFiltersValue]
  end

  subgraph ReduxActions["Action Creators / Dispatchers"]
    getUserPrefs[ProfileState.actionCreators.getUserPreferences]
    setPreferences[ProfileState.actionCreators.setPreferences]
    setSortCol[setSortColumn]
    setSortRev[setSortReverse]
    setSelectedLoc[setSelectedLocationCode]
    setResetFilters[setResetFilters]
  end

  CONNECT -->|wraps| CPS
  CONNECT -->|maps state via| mapStateToProps[mapStateToProps]
  CONNECT -->|maps dispatch via| mapDispatchToProps[mapDispatchToProps]

  mapStateToProps --> UPIsLoading
  mapStateToProps --> UPIsLoadingError
  mapStateToProps --> UP
  mapStateToProps --> getSearchText
  mapStateToProps --> getSortColumn
  mapStateToProps --> getSortReverse
  mapStateToProps --> getFiltersValue

  mapDispatchToProps --> getUserPrefs
  mapDispatchToProps --> setPreferences
  mapDispatchToProps --> setSortCol
  mapDispatchToProps --> setSortRev
  mapDispatchToProps --> setSelectedLoc
  mapDispatchToProps --> setResetFilters

  getSearchText -->|provides| CPS
  getSortColumn -->|provides| CPS
  getSortReverse -->|provides| CPS
  getFiltersValue -->|provides| CPS
  UPIsLoading -->|provides| CPS
  UPIsLoadingError -->|provides| CPS
  UP -->|provides| CPS

  CPS -->|calls| getUserPrefs
  CPS -->|calls| setPreferences
  CPS -->|calls| setSortCol
  CPS -->|calls| setSortRev
  CPS -->|calls| setSelectedLoc
  CPS -->|calls| setResetFilters
```

> SVG rendering failed for this diagram.
