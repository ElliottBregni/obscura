# Diagram: web/portal/src/pages/critical-parts/search/components/CriticalParts.filterLoaders.js


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
classDiagram
    class buildAsyncFilterOptionsState {
        <<function>>
        +topic
        +getUrl(solutionId, state)
        +pageSize
        +getResponseData(data)
        +transformResult(item)
    }

    class fuCodeFilterOptionsState {
        <<const (result)>>
        +topic: "fuCodeCPFilter"
        +pageSize: 20
        +getUrl(solutionId, state)
        +getResponseData(data)
        +transformResult(item)
    }

    class customerApiUrl {
        <<function>>
        +(path)
    }

    class CriticalPartsState {
        <<module>>
        selectors
    }

    class selectors {
        +getSelectedLocationCode(state)
    }

    buildAsyncFilterOptionsState <|-- fuCodeFilterOptionsState
    fuCodeFilterOptionsState --> customerApiUrl : calls with path
    fuCodeFilterOptionsState --> selectors : calls getSelectedLocationCode(state)
    CriticalPartsState --> selectors
    fuCodeFilterOptionsState : getResponseData -> data?.partGroups ?? []
    fuCodeFilterOptionsState : transformResult -> { label: item, value: item }
```

> SVG rendering failed for this diagram.
