# Diagram: web/portal/src/pages/surgicaltotetracking/modules/domain-data/SurgicalToteTrackingFilterLoaders.js


> Auto-generated by Obscura crawlers

## Diagram 1

```mermaid
flowchart LR
    Builder[buildAsyncFilterOptionsState] --> LOC[locationOptionsState]
    LOC --> T_topic[topic: "hsttLocationFilter"]
    LOC --> U_url[url: "/containertracking/api/location"]
    LOC --> P_params[getAdditionalParams() → {filter_name: "location_code"}]
    LOC --> H_headers[additionalHeaders: {"x-target-feature": Features.SURGICAL_TOTE_TRACKING}]
    LOC --> S_page[pageSize: 5]
    LOC --> R_response[getResponseData(data) → data?.data ?? []]
    LOC --> X_transform[transformResult(item) → { label: `${item.location_name} (${item.location_code})`, value: item.location_code }]
```

> SVG rendering failed for this diagram.

## Diagram 2

```mermaid
flowchart LR
    Builder2[buildAsyncFilterOptionsState] --> UDI[uniqueDeviceIdOptionsState]
    UDI --> T2_topic[topic: "hsttUniqueDeviceIdFilter"]
    UDI --> U2_url[url: "/containertracking/api/contents"]
    UDI --> H2_headers[additionalHeaders: {"x-target-feature": Features.SURGICAL_TOTE_TRACKING}]
    UDI --> S2_page[pageSize: 20]
    UDI --> R2_response[getResponseData(data) → data?.data ?? []]
    UDI --> X2_transform[transformResult(item) → { label: item.value, value: item.value }]
```

> SVG rendering failed for this diagram.
