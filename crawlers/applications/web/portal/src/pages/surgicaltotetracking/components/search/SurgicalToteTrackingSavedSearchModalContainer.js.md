# Diagram: web/portal/src/pages/surgicaltotetracking/components/search/SurgicalToteTrackingSavedSearchModalContainer.js


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
graph LR
  subgraph ReduxStates
    STSBarState[SurgicalToteTrackingSearchBarState.selectors]
    STSSavedState[SurgicalToteTrackingSavedSearchState.actionCreators]
  end

  subgraph Utils
    getFeatureData["getFeatureData(state)"]
    getSolutionId["getSolutionId(featureData)"]
  end

  subgraph Containers
    SearchBarContainer[SearchBarContainer]
    FilterSectionContainer[FilterSectionContainer]
  end

  mapState[mapStateToProps()]
  mapDispatch[mapDispatchToProps()]

  EditModal[EditSavedSearchModal]
  Connect[connect(mapStateToProps, mapDispatchToProps)]

  STSBarState -->|getSearchFilters|getSearchFilters
  STSBarState -->|getSearchCategory|getSearchCategory
  STSBarState -->|getSearchText|getSearchText

  getSearchFilters[getSearchFilters(state)] --> mapState
  getSearchCategory[getSearchCategory(state)] --> mapState
  getSearchText[getSearchText(state)] --> mapState
  SearchBarContainer --> mapState
  FilterSectionContainer --> mapState
  mapState --> Connect

  STSSavedState -->|saveSearch| saveSearchFn[saveSearch]
  STSSavedState -->|editSearch| editSearchFn[editSearch]
  saveSearchFn --> mapDispatch
  editSearchFn --> mapDispatch

  mapDispatch -->|provides saveSearch(name,search)| SaveCaller[saveSearch(name, search)]
  mapDispatch -->|provides editSearch(id,name,search)| EditCaller[editSearch(id, name, search)]

  SaveCaller --> saveSearchDispatch[saveSearchDispatch(name, search)]
  saveSearchDispatch -->|calls|getState[getState()]
  saveSearchDispatch -->|calls|getFeatureData
  getFeatureData --> getSolutionId
  getSolutionId -->|returns solution_id| saveSearchDispatch
  saveSearchDispatch -->|dispatch(saveSearch(name, search, solution_id))| STSSavedState

  Connect --> EditModal
  EditModal -->|receives props from| mapState
  EditModal -->|receives dispatchers from| mapDispatch
```

> SVG rendering failed for this diagram.
