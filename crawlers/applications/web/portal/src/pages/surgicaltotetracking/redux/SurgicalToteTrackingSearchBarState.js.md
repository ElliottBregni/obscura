# Diagram: web/portal/src/pages/surgicaltotetracking/redux/SurgicalToteTrackingSearchBarState.js


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
classDiagram
    class File {
        +STORE_MOUNT_POINT: string
        +entitiesUrl(solutionId, queryString)
        +batchSearchUrl(solutionId, queryString, batchType)
        +axiosConfig()
        +fetchSearch(queryString, solutionId, duck, dispatch, state, preventRedirect)
        +getTotalCountForSearch(state)
        +getTotalCountForSearchIsLoading(state)
        +getTotalPageCountForSearch(state)
        +sttSearchBarReducer(state, action)
        +SearchBarState
    }

    class Imports {
        +moment
        +lodash (_)
        +apiUrl
        +buildSearchBarState
        +SEARCH_CATEGORIES
        +FILTERS
        +Features
    }

    class URLs {
        +entitiesUrl(solutionId, queryString)
        +batchSearchUrl(solutionId, queryString, batchType)
    }

    class AxiosConfig {
        +headers
    }

    class Fetch {
        +dispatch fetch
        +dispatch redirect
    }

    class Selectors {
        +getTotalCountForSearch
        +getTotalCountForSearchIsLoading
        +getTotalPageCountForSearch
    }

    class Reducer {
        +sttSearchBarReducer
        +state shape
    }

    class SearchBarState {
        +actionCreators
        +selectors
        +exportSearch (partial)
    }

    Imports --> File : provides
    File o-- URLs : defines
    File o-- AxiosConfig : defines
    File o-- Fetch : defines
    File o-- Selectors : defines
    File o-- Reducer : defines
    File --> SearchBarState : builds using buildSearchBarState()
    SearchBarState *-- "1" Reducer : includes
    SearchBarState --> Selectors : extends
    SearchBarState --> "actionCreators" : extends
    SearchBarState.actionCreators ..> AxiosConfig : uses (exportSearch headers)
    Fetch --> Imports : uses moment, Features in headers and redirects
    URLs --> apiUrl : uses
    AxiosConfig --> moment : uses
    SearchBarState : export default
```

> SVG rendering failed for this diagram.
