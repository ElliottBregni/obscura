# Diagram: web/portal/src/pages/surgicaltotetracking/redux/SurgicalToteTrackingSelectors.js


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
flowchart TD
    Start([getSurgicalToteTrackingSolutionId(state)]) --> A[getFeatureData(state)]
    A --> B{Array.isArray(featureData) && featureData.length > 0}
    B -- Yes --> C[featureData[0].solution ?? null]
    B -- No --> D[null]
    C --> End([return])
    D --> End
```

> SVG rendering failed for this diagram.
