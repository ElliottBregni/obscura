# Diagram: web/portal/src/pages/surgicaltotetracking/redux/SurgicalToteTrackingDetailsWidgetState.js


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
classDiagram
class SurgicalToteTrackingDetailsWidgetState {
  +mountPoint
  +actionCreators
  +selectors
  +reducer
}
class ActionCreators {
  +clearToteMedia()
  +fetchToteDetails(id)
  +fetchToteActiveExceptions(id)
  +fetchToteMedia(type, solutionId)
  +fetchToteSupplierLocationDetails(code)
  +fetchToteHistory(id)
  +clearToteDetails()
  +watchToteActions(id, watch)
  +fetchComments(toteId,pageNumber,pageSize)
  +addComment(toteId,data)
  +addBatchComments(data,isCsvFormat,solutionId)
  +clearBatchComments()
  +cancelAddComment(fakeCommentId)
  +updateComment(toteId,commentId,updatedData)
  +cancelUpdateComment(commentId)
  +markCommentsRead(toteId,datetime)
}
class Selectors {
  +getToteDetails(state)
  +getToteDetailsLoadingFlag(state)
  +getToteMedia(state)
  +getTotePositionUpdates(state)
  +getLocationDetails(state)
  +getLocationDetailsLoadingFlag(state)
  +getSupplierLocationDetails(state)
  +getSupplierLocationDetailsLoadingFlag(state)
  +getToteHistory(state)
  +getSTTActiveExceptions(state)
  +getBothDetailsLoadingFlag(state)
  +getIsFetchingComments(state)
  +getComments(state)
  +getIsBatchCommentInProgress(state)
  +getIsBatchCommentSuccessful(state)
  +getIsBatchCommentFailed(state)
}
class Reducer {
  +initialState
  +SurgicalToteTrackingDetailsWidgetReducer(state,action)
}
class API {
  +APPLICATION_BASE_URL
  +surgicalToteDetailsUrl(id)
  +surgicalTotePositionUpdateUrl(id)
  +surgicalToteActiveExceptions(id)
  +surgicalToteHistoryApiUrl(id)
  +watchToteUrl(id)
  +TOTE_IMAGE_BASE_URL
}
class AxiosConfig {
  +axiosConfig()
  +headers: x-target-feature
}
class ActionTypes {
  +REQUEST_TOTE_DETAILS
  +RECEIVE_TOTE_DETAILS
  +REQUEST_TOTE_ACTIVE_EXCEPTIONS
  +RECEIVE_TOTE_ACTIVE_EXCEPTIONS
  +RECEIVE_TOTE_MEDIA
  +CLEAR_TOTE_MEDIA
  +REQUEST_TOTE_POSITION_UPDATES
  +REQUEST_TOTE_LOCATION_DETAILS
  +RECEIVE_TOTE_OTHER_ALL
  +REQUEST_TOTE_SUPPLIER_LOCATION_DETAILS
  +RETRIEVE_TOTE_SUPPLIER_LOCATION_DETAILS
  +CLEAR_TOTE_DETAILS
  +REQUEST_TOTE_HISTORY
  +RETRIEVE_TOTE_HISTORY
  +TOTE_UPDATE_SUCCESS
  +FETCH_TOTE_COMMENTS
  +FETCH_TOTE_COMMENTS_FAILED
  +RECEIVE_TOTE_COMMENTS
  +SUBMIT_NEW_COMMENT
  +RECEIVE_NEW_COMMENT
  +SUBMIT_NEW_COMMENT_FAILED
  +CANCEL_NEW_COMMENT
  +SET_IS_UPDATING_COMMENT
  +SET_IS_UPDATING_COMMENT_FAILED
  +CANCEL_UPDATE_COMMENT
  +SUBMIT_BATCH_COMMENTS
  +SUBMIT_BATCH_COMMENTS_SUCCESS
  +SUBMIT_BATCH_COMMENTS_FAILED
  +SUBMIT_BATCH_COMMENTS_RESET
  +START_BOTH_DETAILS_LOADING
  +STOP_BOTH_DETAILS_LOADING
}
SurgicalToteTrackingDetailsWidgetState --> ActionCreators : exposes
SurgicalToteTrackingDetailsWidgetState --> Selectors : exposes
SurgicalToteTrackingDetailsWidgetState --> Reducer : exposes
ActionCreators --> API : calls
ActionCreators --> AxiosConfig : uses
ActionCreators --> ActionTypes : dispatches
Reducer --> ActionTypes : handles
Selectors --> SurgicalToteTrackingDetailsWidgetState : reads state
API --> ApplicationConfig : reads environment
AxiosConfig ..> "modules/auth/Authorization" : uses Features.SURGICAL_TOTE_TRACKING
note right of ActionCreators : fetchToteDetails sequences: REQUEST_TOTE_* -> RECEIVE_TOTE_DETAILS -> RECEIVE_TOTE_OTHER_ALL / STOP_BOTH_DETAILS_LOADING
note left of Reducer : comment flow handles optimistic updates: SUBMIT_NEW_COMMENT -> RECEIVE_NEW_COMMENT / SUBMIT_NEW_COMMENT_FAILED -> CANCEL_NEW_COMMENT
```

> SVG rendering failed for this diagram.
