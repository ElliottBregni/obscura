# Diagram: web/portal/src/pages/surgicaltotetracking/redux/SurgicalToteTrackingSavedSearchState.js


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
flowchart LR
  A[buildSavedSearchState] --> B[export default savedSearchState]
  B --> C{config}
  C --> D[topic: "sttSavedSearch"]
  C --> E[savedSearchUrl: apiUrl("/preferences-ng/search-history")]
  C --> F[searchType: (state) => getSurgicalToteTrackingSolutionId(state)]
  C --> G[searchCategories: SEARCH_CATEGORIES]
  C --> H[isSearchTypeFunction: true]
  subgraph Actions
    I[clearSearchFilters] -->|maps to| J[SearchState.actionCreators.clearSearchFilters]
    K[setSearchCategory] -->|maps to| L[SearchState.actionCreators.setSearchCategory]
    M[setSearchText] -->|maps to| N[SearchState.actionCreators.setSearchText]
    O[setSearchFilter] -->|maps to| P[SearchState.actionCreators.setSearchFilter]
  end
  C --> Actions
  Q[imports]
  Q --> R["buildSavedSearchState from components/saved-search/SavedSearchStateBuilder"]
  Q --> S["SEARCH_CATEGORIES from ../components/search/SurgicalToteTracking.Search.Options"]
  Q --> T["apiUrl from api-url"]
  Q --> U["SearchState from ./SurgicalToteTrackingSearchBarState"]
  Q --> V["getSurgicalToteTrackingSolutionId from ./SurgicalToteTrackingSelectors"]
  R --> A
  S --> G
  T --> E
  U --> J
  U --> L
  U --> N
  U --> P
  V --> F
```

> SVG rendering failed for this diagram.
