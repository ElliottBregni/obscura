# Diagram: web/portal/src/pages/surgicaltotetracking/details/components/SurgicalToteTrackingExceptionsTab.js


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
classDiagram
    class SurgicalToteTrackingExceptionsTab {
        +sttActiveExceptions: array
        +headers: array
        +rows: ReactElement[]
        +render()
    }
    class RowDetails {
        +row: object
        +render()
    }
    class DetailsTable {
        +headers: array
        +rows: ReactElement[]
        +render()
    }
    class DateTime {
        +stack: boolean
        +plain: boolean
        +localize: boolean
        +dateTime: string
        +render()
    }
    class DateTime_Time {
        +bold: boolean
    }
    class DateTime_Timezone {
        +bold: boolean
    }
    class useTranslation {
        +t(namespace: string): function
    }
    SurgicalToteTrackingExceptionsTab --> DetailsTable : uses
    SurgicalToteTrackingExceptionsTab --> RowDetails : renders many
    RowDetails --> DateTime : composes/uses
    DateTime "1" o-- "2" DateTime_Time : has
    DateTime "1" o-- "2" DateTime_Timezone : has
    SurgicalToteTrackingExceptionsTab ..> useTranslation : calls
    RowDetails : propTypes -> row: object
    SurgicalToteTrackingExceptionsTab : propTypes -> sttActiveExceptions: array
```

> SVG rendering failed for this diagram.
