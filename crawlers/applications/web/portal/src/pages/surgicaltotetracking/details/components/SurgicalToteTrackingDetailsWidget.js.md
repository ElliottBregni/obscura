# Diagram: web/portal/src/pages/surgicaltotetracking/details/components/SurgicalToteTrackingDetailsWidget.js


> Auto-generated by Obscura crawlers

## Diagram 1

```mermaid
classDiagram
    class SurgicalToteTrackingDetailsWidget {
        +toteDetails
        +toteMedia
        +sttActiveExceptions
        +watch
        +onWatchChange()
        +letterForLad({name}, status)
    }
    SurgicalToteTrackingDetailsWidget --> PanelGroup
    SurgicalToteTrackingDetailsWidget --> DetailPanelGroupItem
    SurgicalToteTrackingDetailsWidget --> ShowMoreList
    SurgicalToteTrackingDetailsWidget --> Chiclet
    SurgicalToteTrackingDetailsWidget --> Image
    SurgicalToteTrackingDetailsWidget --> ExceptionsBox
    SurgicalToteTrackingDetailsWidget --> WatchToggle
    SurgicalToteTrackingDetailsWidget --> PhotoPlaceholder
    SurgicalToteTrackingDetailsWidget --> localizedDateFormatter
    SurgicalToteTrackingDetailsWidget --> localizedTimeFormatter
```

> SVG rendering failed for this diagram.

## Diagram 2

```mermaid
flowchart TD
    A[Start render SurgicalToteTrackingDetailsWidget] --> B{toteDetails present?}
    B -- no --> C[Render PanelGroup header with WatchToggle only]
    B -- yes --> D[Extract tote fields: toteId,status,location,eventTs,homeLocation,contents]
    D --> E[Compute codeLetter := letterForLad(locationType, status)]
    E --> F[Compute color based on status or colorForLad]
    D --> G{contents list length > 0?}
    G -- yes --> H[Render ShowMoreList with UDIs]
    G -- no --> I[Skip UDI list]
    D --> J{locationCode or locationName or eventTs?}
    J -- yes --> K[Render Current Location DetailPanelGroupItem with Chiclet and formatted date/time]
    J -- no --> L[Skip Current Location block]
    D --> M{homeLocation present?}
    M -- yes --> N[Render Home Location DetailPanelGroupItem with address fields]
    M -- no --> O[Skip Home Location block]
    E --> P[Render Chiclet if codeLetter]
    D --> Q[Render Image with PhotoPlaceholder]
    D --> R[Render ExceptionsBox with sttActiveExceptions]
    C --> S[End render]
    H --> S
    I --> S
    K --> S
    N --> S
    P --> S
    Q --> S
    R --> S
```

> SVG rendering failed for this diagram.
