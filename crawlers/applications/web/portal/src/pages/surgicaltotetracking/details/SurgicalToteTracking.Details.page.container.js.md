# Diagram: web/portal/src/pages/surgicaltotetracking/details/SurgicalToteTracking.Details.page.container.js


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
classDiagram
    class SurgicalToteTrackingDetails {
        <<React Component>>
    }
    class mapStateToProps {
        +function(state)
        +returns props
    }
    class mergeProps {
        +function(stateProps, dispatchProps, ownProps)
    }
    class connectFn {
        +connect(mapStateToProps, actionCreators, mergeProps)
    }
    class SurgicalToteTrackingDetailsWidgetState {
        class selectors {
            +getToteDetails(state)
            +getSTTActiveExceptions(state)
            +getToteDetailsLoadingFlag(state)
            +getToteMedia(state)
            +getTotePositionUpdates(state)
            +getLocationDetails(state)
            +getLocationDetailsLoadingFlag(state)
            +getToteHistory(state)
            +getIsFetchingComments(state)
            +getComments(state)
        }
        class actionCreators {
            +clearToteMedia()
            +fetchToteDetails()
            +fetchToteActiveExceptions()
            +fetchToteMedia()
            +fetchToteSupplierLocationDetails()
            +clearToteDetails()
            +fetchToteHistory()
            +watchToteActions()
            +fetchComments()
            +addComment()
            +cancelAddComment()
            +updateComment()
            +cancelUpdateComment()
            +markCommentsRead()
        }
    }
    class MapState {
        class selectors {
            +getMapCoordinate(type)(state)
        }
        class actionCreators {
            +addCoordinate()
            +clearCoordinatesByType()
            +clearAllCoordinates()
        }
    }
    class NotificationsState {
        class actionCreators {
            +fetchNotification()
        }
    }
    class OrganizationsState {
        +getActiveOrganization(state)
        +getSolutionIdByFeatureName(state,featureName)
    }
    class MapCoordinateType {
        <<enum>>
        +SELECTED_COORDINATE
    }

    %% relationships
    mapStateToProps --> OrganizationsState : uses
    mapStateToProps --> SurgicalToteTrackingDetailsWidgetState.selectors : uses
    mapStateToProps --> MapState.selectors : uses getMapCoordinate
    mapStateToProps --> MapCoordinateType : reads SELECTED_COORDINATE
    mapStateToProps --> NotificationsState : (none direct) 

    SurgicalToteTrackingDetailsWidgetState.actionCreators --> actionCreators : contributes
    MapState.actionCreators --> actionCreators : contributes
    NotificationsState.actionCreators --> actionCreators : contributes

    actionCreators <|-- SurgicalToteTrackingDetailsWidgetState.actionCreators
    actionCreators <|-- MapState.actionCreators
    actionCreators <|-- NotificationsState.actionCreators

    connectFn --> mapStateToProps
    connectFn --> actionCreators
    connectFn --> mergeProps
    connectFn --> SurgicalToteTrackingDetails : returns connected component

    SurgicalToteTrackingDetails <-- connectFn : default export
```

> SVG rendering failed for this diagram.
