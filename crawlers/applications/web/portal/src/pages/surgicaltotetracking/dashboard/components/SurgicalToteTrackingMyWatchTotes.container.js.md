# Diagram: web/portal/src/pages/surgicaltotetracking/dashboard/components/SurgicalToteTrackingMyWatchTotes.container.js


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
flowchart TD
  subgraph ReduxState["SurgicalToteTrackingMyWatchedTotesState"]
    SSelectors["selectors\n- getWatchedTotes()\n- getWatchedTotesLoading()\n- getWatchedTotesPageIndex()\n- getWatchedTotesPageSize()\n- getWatchedTotesPageCount()"]
    SActions["actionCreators\n- fetchWatchedTotes()\n- setWatchedTotesPageIndex()"]
  end

  MapState["mapStateToProps(state)"]
  MapDispatch["mapDispatchToProps(dispatch)"]
  Connect["connect(mapStateToProps, mapDispatchToProps)"]
  Component["SurgicalToteTrackingMyWatchedTotes\n(React Component)"]

  state["Redux store\n(state)"]
  dispatch["dispatch"]

  state -->|passed into| MapState
  SSelectors -->|reads selectors| MapState
  MapState -->|returns props:\nwatchedToteArray,\nwatchedTotesLoading,\nwatchedTotesPageIndex,\nwatchedTotesPageSize,\nwatchedTotesPageCount| Connect

  MapDispatch -->|uses actionCreators| SActions
  SActions -->|provides creators| MapDispatch
  dispatch -->|passed into| MapDispatch
  MapDispatch -->|returns props:\nfetchWatchedTotes(),\nsetWatchedTotesPageIndex(pageIndex),\nsetSelectedToteId(uuid -> SURGICAL_TOTE_TRACKING_DETAILS)| Connect

  Connect -->|wraps| Component
  Component -->|reads props| MapState
  Component -->|calls props that dispatch| MapDispatch
```

> SVG rendering failed for this diagram.
