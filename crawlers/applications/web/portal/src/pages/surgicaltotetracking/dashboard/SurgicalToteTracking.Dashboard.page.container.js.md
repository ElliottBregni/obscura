# Diagram: web/portal/src/pages/surgicaltotetracking/dashboard/SurgicalToteTracking.Dashboard.page.container.js


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
flowchart LR
  subgraph ReduxModules
    STCW[SurgicalToteTrackingCountsWidgetState]
    STSB[SurgicalToteTrackingSearchBarState]
  end

  subgraph Selectors
    STCW_sel[getTotesTotals<br/>getTotesTotalsLoadingFlag<br/>getExceptionTypes<br/>getExceptionTypesIsLoadingFlag]
  end

  subgraph ActionCreators
    STCW_ac[fetchTotesTotals<br/>fetchExceptionTypes]
    STSB_ac[searchEntities<br/>setSearchFilter<br/>resetSearchAndFilters]
  end

  State[Application State] --> STCW_sel
  STCW --> STCW_sel
  STSB --> STSB_ac

  STCW_sel -->|maps| mapStateToProps[mapStateToProps]
  STCW_ac --> actionCreators[actionCreators]
  STSB_ac --> actionCreators

  mapStateToProps --> propsState[props (totesTotals, isTotesTotalsLoading, exceptionTypes, isExceptionTypesIsLoading)]
  actionCreators --> connectFn[connect(mapStateToProps, actionCreators, mergeProps)]

  mapMerge[mergeProps(stateProps, dispatchProps, ownProps) => {...}] 
  connectFn --> mergeCall[mergeProps merges props]
  propsState --> mergeCall
  actionCreators --> mergeCall

  mergeCall --> SurgicalToteDashboard[SurgicalToteDashboard (connected component)]
```

> SVG rendering failed for this diagram.
