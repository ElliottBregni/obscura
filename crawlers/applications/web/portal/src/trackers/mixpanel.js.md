# Diagram: web/portal/src/trackers/mixpanel.js


> Auto-generated by Obscura crawlers

## Diagram 1

```mermaid
classDiagram
    class MixpanelUtils {
        +isMixpanelEnabled()
        +register(values={})
        +track(event, properties={})
        +identify(id, properties)
        +alias(id, properties)
    }
    class ModuleScope {
        -mixpanel = null
        +initMixpanel()
        +useTrackWithMixpanelOnce(event, properties={})
    }
    class ApplicationConfig {
        +MIXPANEL_TOKEN
    }
    class mixpanelLib {
        +init(token, options)
    }
    ApplicationConfig --> ModuleScope : provides MIXPANEL_TOKEN
    ModuleScope --> mixpanelLib : calls init(token, options)
    mixpanelLib o-- ModuleScope : loaded callback sets mixpanel
    ModuleScope --> MixpanelUtils : exposes mixpanel to
    MixpanelUtils --> ModuleScope : reads mixpanel variable
    ModuleScope ..> MixpanelUtils : useTrackWithMixpanelOnce calls track
```

> SVG rendering failed for this diagram.

## Diagram 2

```mermaid
flowchart TD
    A[initMixpanel] --> B{MIXPANEL_TOKEN?}
    B -- No --> C[return false]
    B -- Yes --> D[mixpanelLib.init(token, options)]
    D --> E[options.loaded(loadedMixpanel)]
    E --> F[set module-level mixpanel = loadedMixpanel]
    F --> G[Mixpanel initialized]
    G --> H[MixpanelUtils.isMixpanelEnabled()]
    H --> I{mixpanel present?}
    I -- No --> J[methods return false]
    I -- Yes --> K[MixpanelUtils.track/identify/alias/register call mixpanel API]
    L[useTrackWithMixpanelOnce hook] --> M[on mount -> MixpanelUtils.track(event, properties)]
    M --> K
```

> SVG rendering failed for this diagram.
