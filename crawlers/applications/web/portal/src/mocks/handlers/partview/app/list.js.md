# Diagram: web/portal/src/mocks/handlers/partview/app/list.js


> Auto-generated by Obscura crawlers

## Diagram 1

```mermaid
flowchart TD
  subgraph ListHandlers
    CL[Client] --> LURL["/partview/app/list/:topic"]
    LURL --> LHandler[rest.get handler]
    LHandler --> LPAGE{req.url.searchParams.get("pageSize")?}
    LPAGE --|yes|--> LPAGINATE["createPaginatedResponseBody(req, list)"]
    LPAGE --|no|--> LDIRECT["response: { data: list }"]
    LPAGINATE --> LRES[res(ctx.json(responseBody))]
    LDIRECT --> LRES
  end

  subgraph BatchSearch
    BCL[Client] --> BURL["/partview/app/search"]
    BURL --> BHandler[rest.post handler]
    BHandler --> BBUILD["construct responseBody: [shipments...]"]
    BBUILD --> BDETAILS["shipment fields: id, Watched, TrackingNumber, LifecycleState, LastMilestone, OriginDetail, DestinationDetail, PartList, Orders, ExceptionList, ActiveExceptionList"]
    BBUILD --> BUUID[uuidv4() ids]
    BHandler --> BMOMENT[moment() timestamps]
    BBUILD --> BRES[res(ctx.json({ meta: { currentPage, totalCount, totalPages }, data: responseBody }))]
  end

  subgraph InitLists
    MAP[initial list definitions] --> CONV[convertStringToApiListItem(value)]
    CONV --> UUUID[uuidv4()]
    CONV --> ITEM["{ id, label, value }"]
    MAP --> CARR["carrier list objects with uuidv4()"]
  end

  LURL -.-> MAP
  BURL -.-> MAP
  LPAGINATE -.-> CREATEPAG["createPaginatedResponseBody implementation"]
```

> SVG rendering failed for this diagram.

## Diagram 2

```mermaid
classDiagram
  class createListHandler {
    +(topic, list)
    +returns: rest.get handler
    +uses: apiUrl
  }
  class convertStringToApiListItem {
    +(value)
    +returns: { id, label, value }
    +uses: uuidv4
  }
  class createPaginatedResponseBody
  class handleBatchSearch {
    +rest.post('/partview/app/search')
    +returns: { meta, data }
    +uses: uuidv4, moment
  }
  class Exports
  Exports : +handleGetShipmentIdOptions()
  Exports : +handleOrderNumberOptions()
  Exports : +handleGetPartNameOptions()
  Exports : +handleGetPartCarrierOptions()
  Exports : +handleGetPartNumberOptions()
  Exports : +handleGetPackageStatusOptions()
  Exports : +handleGetExceptionOptions()
  Exports : +handleGetOrderPriorityOptions()
  Exports : +handleGetTrackingNumberOptions()
  Exports : +handleGetBillOfLadingOptions()
  Exports : +handleGetDestinationDealerOptions()
  Exports : +handleBatchSearch()
  Exports : +handleGetFinalMileOriginOptions()

  createListHandler <|-- Exports : creates handlers
  convertStringToApiListItem --> uuidv4 : uses
  createListHandler --> apiUrl : uses
  handleBatchSearch --> uuidv4 : uses
  handleBatchSearch --> moment : uses
  createListHandler --> createPaginatedResponseBody : calls when pageSize present
```

> SVG rendering failed for this diagram.
