# Diagram: web/portal/src/mocks/handlers/preferences-ng/search-history.js


> Auto-generated by Obscura crawlers

## Diagram 1

```mermaid
flowchart TD
  subgraph DataStore
    lastId[(lastId)]
    savedSearches[(savedSearches)]
    URL["/preferences-ng/search-history"]
  end
  subgraph Handlers
    GET["handleGetSavedSearches (GET)"]
    POST["handleCreateSavedSearch (POST)"]
    PUT["handleUpdateSavedSearch (PUT)"]
    DELETE["handleDeleteSavedSearch (DELETE)"]
  end
  GET -->|filter by searchtype| savedSearches
  GET -->|respond 200 JSON(filteredSearches)| GETResp[(200 JSON)]
  POST -->|lastId += 1| lastId
  POST -->|create savedSearch and push| savedSearches
  POST -->|respond 200 JSON([savedSearch])| POSTResp[(200 JSON)]
  PUT -->|findIndex by id| savedSearches
  PUT -->|if not found -> 404 no records updated| PUT404((404 no records updated))
  PUT -->|if found -> update savedSearches[index]| savedSearches
  PUT -->|respond 200 JSON([req.body])| PUTResp[(200 JSON)]
  DELETE -->|findIndex by id| savedSearches
  DELETE -->|if not found -> 404 no records deleted| DEL404((404 no records deleted))
  DELETE -->|if found -> splice savedSearches| savedSearches
  DELETE -->|respond 200 OK| DELResp[(200 OK)]
```

> SVG rendering failed for this diagram.

## Diagram 2

```mermaid
sequenceDiagram
  participant Client
  participant MSW as MSW_Handler
  participant Store as SavedSearchStore

  Client->>MSW: GET /preferences-ng/search-history?searchtype=...
  MSW->>Store: filter by searchtype
  Store-->>MSW: filteredSearches
  MSW-->>Client: 200 JSON(filteredSearches)

  Client->>MSW: POST /preferences-ng/search-history (body)
  MSW->>Store: lastId += 1; create savedSearch
  Store-->>MSW: [savedSearch]
  MSW-->>Client: 200 JSON([savedSearch])

  Client->>MSW: PUT /preferences-ng/search-history/:id (body)
  MSW->>Store: findIndex by id
  alt found
    MSW->>Store: update savedSearches[index] with req.body
    Store-->>MSW: updatedRecord
    MSW-->>Client: 200 JSON([req.body])
  else not found
    MSW-->>Client: 404 no records updated
  end

  Client->>MSW: DELETE /preferences-ng/search-history/:id
  MSW->>Store: findIndex by id
  alt found
    MSW->>Store: splice savedSearches[index]
    MSW-->>Client: 200 OK
  else not found
    MSW-->>Client: 404 no records deleted
  end
```

> SVG rendering failed for this diagram.
