# Diagram: web/portal/src/mocks/handlers/shipping/post-bulk-shipment-updates.js


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
flowchart TD
    REST[rest.post handler: handlePostBulkShipmentUpdates]
    REST --> REQ[POST /shipping/post_bulk_shipment_updates]
    REQ --> PARSE[Extract req.body.shipmentIds string]
    PARSE --> SPLIT[Split string by "," → shipmentIds array]
    SPLIT --> FIND[Call findDuplicates(shipmentIds)]
    FIND --> DUP{duplicates.length > 0?}
    DUP -- Yes --> SET_400[Set status = 400]
    SET_400 --> BUILD_ERR[Build responseBody.error with duplicate IDs message]
    BUILD_ERR --> GEN_UUID[Generate reference = uuidv4()]
    GEN_UUID --> RESP[Return res(ctx.delay(1000), ctx.json(responseBody), ctx.status(status))]
    DUP -- No --> SET_201[Set status = 201; responseBody = ""]
    SET_201 --> RESP
```

> SVG rendering failed for this diagram.
