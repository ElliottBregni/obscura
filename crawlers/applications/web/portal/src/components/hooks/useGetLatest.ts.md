# Diagram: web/portal/src/components/hooks/useGetLatest.ts


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
flowchart LR
  A[useGetLatest(value)] --> B[React.useRef<T>()\n(ref)]
  B --> C[ref.current = value\n(every render)]
  B --> D[React.useCallback(() => ref.current as T, [])\n(callback)]
  D --> E[return callback]
  E --> F[Calling callback() -> returns ref.current]
```

> SVG rendering failed for this diagram.
