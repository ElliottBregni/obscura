# Diagram: web/portal/src/components/hooks/useSubcomponentOverride.js


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
flowchart LR
A[useSubcomponentOverride(children, defaultChild)] --> B{children exists?}
B -- No --> G[return defaultChild]
B -- Yes --> C[React.Children.toArray(children)]
C --> D[find child where child.type === defaultChild.type]
D --> E{passedInChild found?}
E -- No --> G
E -- Yes --> H[defaultProps = defaultChild.props ?? {}]
H --> I[passedInProps = passedInChild.props ?? {}]
I --> J[cloned = React.cloneElement(defaultChild, {...defaultProps, ...passedInProps})]
J --> K[return cloned]
```

> SVG rendering failed for this diagram.
