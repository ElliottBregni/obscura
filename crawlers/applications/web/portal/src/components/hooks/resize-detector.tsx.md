# Diagram: web/portal/src/components/hooks/resize-detector.tsx


> Auto-generated by Obscura crawlers

## Diagram 1

```mermaid
classDiagram
    class useResizeDetector {
        +ref: Ref<T>
        +size: {height?: number; width?: number}
        +useMemo()
    }
    class withResizeDetector {
        +Extend(WrappedComponent)
        +ComponentWithResizeDetector(props)
    }
    class ComponentWithResizeDetector {
        +render()
        +displayName
    }
    class WrappedComponent {
        +props
    }
    class ReactResizeDetector {
        +height
        +width
        +ref
    }
    ComponentWithResizeDetector --> useResizeDetector : uses
    useResizeDetector --> ReactResizeDetector : delegates to
    ComponentWithResizeDetector --> WrappedComponent : renders / passes innerRef & size
    withResizeDetector --> ComponentWithResizeDetector : returns
    WrappedComponent <|-- ComponentWithResizeDetector
```

> SVG rendering failed for this diagram.

## Diagram 2

```mermaid
flowchart TD
    Start[withResizeDetector<T>()] --> ExtendFunc[Extend(WrappedComponent)]
    ExtendFunc --> Component[ComponentWithResizeDetector]
    Component --> Hook[useResizeDetector<T>()]
    Hook --> Dep[react-resize-detector dependency]
    Dep --> Size{height, width}
    Hook --> Ref[ref]
    Component --> Wrapped[WrappedComponent]
    Wrapped -->|props + innerRef + size| Component
    Ref --> Wrapped
    Size --> Wrapped
```

> SVG rendering failed for this diagram.
