# Diagram: web/portal/src/components/utils/exceptions.utils.js


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
flowchart TD
  Start([getExceptionChartData(exceptions, totalCount, totalExceptionsCount)])
  Start --> Check{!exceptions or totalCount === 0?}
  Check -- Yes --> ReturnNone[/"Return: [{ x: 'none', y: 1, fill: Colors.background.GRAY }]"/]
  Check -- No --> BuildArray[/"Build array:\n[...exceptions, { name: 'No Exception', count: totalCount - totalExceptionsCount || 0, icon: { color: Colors.highlight.GREEN } }]"/]
  BuildArray --> MapLambda[/".map(exception => { x: exception.name, y: (exception.count/totalCount)*100 or 0, fill: exception.icon?.color })"/]
  MapLambda --> ReturnMapped[/"Return: mapped array of { x, y, fill }"/]
  ReturnNone --> End([End])
  ReturnMapped --> End
```

> SVG rendering failed for this diagram.
