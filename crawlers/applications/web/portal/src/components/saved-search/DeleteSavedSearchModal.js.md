# Diagram: web/portal/src/components/saved-search/DeleteSavedSearchModal.js


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
classDiagram
class DeleteSavedSearchModal {
  +bool show
  +function hide()
  +function handleConfirm()
  +t(key): string
}
class Modal
class ModalHeader {
  +title: string
}
class ModalBody
class ModalFooter
class Button {
  +variant: string
  +onClick(): void
}
class Colors {
  +background
}
class useTranslation {
  +(ns: string): { t(key:string): string }
}

DeleteSavedSearchModal --> Modal : renders
Modal *-- ModalHeader : contains
Modal *-- ModalBody : contains
Modal *-- ModalFooter : contains
ModalFooter o-- Button : contains
DeleteSavedSearchModal ..> Button : renders
DeleteSavedSearchModal ..> Colors : uses for styles
DeleteSavedSearchModal ..> useTranslation : uses for i18n
```

> SVG rendering failed for this diagram.
