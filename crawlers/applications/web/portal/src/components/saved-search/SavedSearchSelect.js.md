# Diagram: web/portal/src/components/saved-search/SavedSearchSelect.js


> Auto-generated by Obscura crawlers

## Diagram 1

```mermaid
classDiagram
    class SavedSearchSelect <<Component>> {
        +bool readOnly
        +object savedSearch
        +array savedSearches
        +func selectSavedSearch(id)
        +string solutionId
        +func exportSearch(id)
        +func resetSavedSearch()
        +func deleteSearch(id)
        +func fetchSavedSearches()
        +func loadSavedSearch(obj)
        +object EditModal
        +func DeleteModal
        +func onChange(value)
    }
    class SaveCurrentSearch <<Component>> {
        +func changeHandler(selected)
        +func deleteConfirmHandler()
        +object savedSearch
        +func editHandler()
        +array savedSearchOptions
    }
    class EditSavedSearchButtons <<Component>> {
        +bool disabled
        +func onEdit()
        +func onDelete()
    }
    class DownloadDataButton <<Component>> {
        +func onClick()
    }
    class SelectField <<Component>> {
        +array options
        +any stateValue
        +func onChange(selected)
        +string placeholder
    }
    class EditModal <<Component>> {
        +bool show
        +func hide()
        +object savedSearch
    }
    class DeleteModal <<Component>> {
        +bool show
        +func hide()
        +func handleConfirm()
    }

    SavedSearchSelect --> SaveCurrentSearch : contains
    SavedSearchSelect --> DownloadDataButton : contains
    SavedSearchSelect --> SelectField : uses
    SavedSearchSelect --> EditModal : renders
    SavedSearchSelect --> DeleteModal : renders
    SaveCurrentSearch --> EditSavedSearchButtons : uses when savedSearch
    SaveCurrentSearch --> SelectField : renders
    EditSavedSearchButtons ..> TiPencil : uses icon
    EditSavedSearchButtons ..> TiTrash : uses icon
```

> SVG rendering failed for this diagram.

## Diagram 2

```mermaid
flowchart LR
    A[App mounts] --> B[SavedSearchSelect]
    B -->|calls on mount| C[fetchSavedSearches()]
    C --> D[populate savedSearches]
    D --> B
    B --> E{readOnly?}
    E -- false --> F[Show DownloadDataButton]
    F --> G[User clicks export] --> H[exportSearch(solutionId)]
    E -- false --> I[Show SaveCurrentSearch]
    I --> J{savedSearch exists?}
    J -- no --> K[Show Save Current Search Button]
    K --> L[User clicks -> editHandler -> show EditModal]
    J -- yes --> M[Show EditSavedSearchButtons]
    M --> N[onEdit -> show EditModal]
    M --> O[onDelete -> show DeleteModal]
    O --> P[confirmDelete -> deleteSearch(id) + resetSavedSearch()]
    I --> Q[SelectField]
    Q --> R[onChange -> selectedSavedSearch -> selectSavedSearch(obj)]
    B --> S[EditModal hide -> setShowEditSearchModal(false)]
    B --> T[DeleteModal hide -> setShowDeleteSearchModal(false)]
```

> SVG rendering failed for this diagram.
