# Diagram: web/portal/src/components/atoms/Tooltip.atom.js


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
classDiagram
    class Tooltip {
        +placement = "bottom"
        +theme = TooltipTheme.Default
        +trigger = ["hover","focus"]
        +showDelayInMs = 0
        +hideDelayInMs = 0
        +tooltipChildren
        +style = {}
        +className
        +children
        +render()
    }
    class OverlayTrigger
    class BootstrapTooltip
    class TooltipTheme {
        <<enum>>
        +Default
        +Danger
    }
    class Colors {
        <<module>>
        +highlight
    }
    class positionFixed {
        <<const>>
        +position: "fixed"
    }
    class dangerStyles {
        <<const>>
        +".tooltip-arrow::before"
        +".tooltip-inner"
        +position: "fixed"
    }
    class PropTypes {
        <<library>>
    }

    Tooltip --> OverlayTrigger : uses
    Tooltip --> BootstrapTooltip : renders
    Tooltip --> TooltipTheme : reads
    Tooltip --> Colors : reads
    Tooltip --> positionFixed : uses
    Tooltip --> dangerStyles : uses
    Tooltip ..|> PropTypes : validates with
    TooltipTheme <-- Colors : theming (uses highlight.RED)
```

> SVG rendering failed for this diagram.
