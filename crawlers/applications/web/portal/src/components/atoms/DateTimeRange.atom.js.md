# Diagram: web/portal/src/components/atoms/DateTimeRange.atom.js


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
classDiagram
class DateTimeRange {
  +from: string
  +to: string
  +localize: bool
  +stack: bool
  +plain: bool
  +showTime: bool = true
  +size: number = FontSize.size14
  +style: object
  +"data-qa": string
  +children: node
  +render(): JSX.Element | null
}
class DateTime {
  +dateTime: string
  +plain: bool
  +stack: bool
  +localize: bool
  +size: number
  +showTime: bool
}
class Text {
  +style: object
  +content: string
}
class FontSize {
  <<enumeration>>
  size14
}
class parseDateTime {
  +parse(input:string, localize:bool): { isValid: bool, parsed: Date }
}
class useTranslation {
  +t(key:string): string
}
class useSubcomponentOverride {
  +override(children, fallback): ReactElement
}
class PropTypes {
  +string
  +bool
  +number
  +object
  +node
}
DateTimeRange --> DateTime : composes
DateTimeRange --> Text : composes
DateTimeRange ..> parseDateTime : calls
DateTimeRange ..> useTranslation : uses
DateTimeRange ..> useSubcomponentOverride : uses
DateTimeRange ..> FontSize : references
DateTimeRange ..> PropTypes : declares propTypes
```

> SVG rendering failed for this diagram.
