# Diagram: web/portal/src/components/atoms/Button.atom.stories.js


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
classDiagram
    class Button {
        <<React Component>>
        +props
        +render()
    }
    class Icon {
        <<React Component>>
        +src
        +render()
    }
    class Colors {
        <<Module>>
        +background
    }
    class Storybook_File {
        <<Storybook Stories>>
        +Template(args)
        +Default
        +Disabled
        +WithIcon
        +Types
        +Variants
        +Sizes
        +Animations
        +OnClick
    }
    class ButtonTypes {
        <<const array>>
        +[ "button","reset","submit" ]
    }
    class ButtonVariants {
        <<const array>>
        +[ many variants ]
    }
    class ButtonSizes {
        <<const array>>
        +[ {label,key}, ... ]
    }
    class faAngleDoubleRight {
        <<icon import>>
    }

    Storybook_File --> Button : uses
    Storybook_File --> Icon : uses
    Storybook_File --> Colors : uses
    Storybook_File --> ButtonTypes : references
    Storybook_File --> ButtonVariants : references
    Storybook_File --> ButtonSizes : references
    WithIcon --> faAngleDoubleRight : imports
    Storybook_File ..> faAngleDoubleRight : references
    Template --> Button : returns
    Types ..> ButtonTypes : iterates
    Variants ..> ButtonVariants : iterates
    Variants ..> Colors : reads background
    Sizes ..> ButtonSizes : iterates
    OnClick ..> "alert()" : calls
```

> SVG rendering failed for this diagram.
