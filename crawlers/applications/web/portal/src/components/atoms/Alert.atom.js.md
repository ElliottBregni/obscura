# Diagram: web/portal/src/components/atoms/Alert.atom.js


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
classDiagram
    class Alert {
        +show: bool = false
        +variant: string = AlertVariant.Primary
        +dismissible: bool = false
        +onDismiss: func = _.noop
        +style: object = {}
        +className: string = ""
        +data-qa: string = ""
        +children: any (required)
        +...restProps
        +render(): BootstrapAlert
    }
    class BootstrapAlert
    class PropTypes
    class lodash
    class AlertVariant

    Alert --> BootstrapAlert : renders/returns
    Alert ..> PropTypes : uses for prop validation
    Alert ..> lodash : uses _.noop
    Alert --> AlertVariant : reads default variant and re-exports
    BootstrapAlert <|-- Alert : wraps component
```

> SVG rendering failed for this diagram.
