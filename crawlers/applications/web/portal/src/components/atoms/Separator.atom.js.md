# Diagram: web/portal/src/components/atoms/Separator.atom.js


> Auto-generated by Obscura crawlers

## Diagram 1

```mermaid
classDiagram
class Separator {
  +color: string = Colors.background.LIGHT_GRAY
  +orientation: SeparatorOrientation = VERTICAL
  +text: string
  +style: object
  +className: string
  +render(): JSX.Element
}
class SeparatorOrientation {
  <<enumeration>>
  VERTICAL
  HORIZONTAL
}
class Colors {
  background: { LIGHT_GRAY: string }
}
class PropTypes

Separator --> SeparatorOrientation
Separator --> Colors
Separator ..|> PropTypes
```

> SVG rendering failed for this diagram.

## Diagram 2

```mermaid
flowchart LR
Start((Start)) --> Check{orientation === SeparatorOrientation.VERTICAL?}
Check -- Yes --> VerticalDiv[div(className)\ncss: border-left: 1px solid color\nstyle={style}]
Check -- No --> HorizontalDiv[div(className)\ncss: display:flex; line-height:1em; color; margin\n&:before/&:after -> background-color:color; height:1px\nstyle={style}]
HorizontalDiv --> HasText{text ?}
HasText -- Yes --> TextNode[div(css: padding: 0 0.5em)\ncontains text]
HasText -- No --> NoText((no text))
VerticalDiv --> End((Return vertical div))
TextNode --> End((Return horizontal div with text))
NoText --> End((Return horizontal divider))
```

> SVG rendering failed for this diagram.
