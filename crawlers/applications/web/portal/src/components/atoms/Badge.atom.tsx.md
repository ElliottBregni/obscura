# Diagram: web/portal/src/components/atoms/Badge.atom.tsx


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
classDiagram
    interface BadgeProps {
        +children: React.ReactNode
        +size?: "small"|"medium"
        +customStyles?: Interpolation<Theme>
    }

    class Badge {
        +props: BadgeProps
        +render(): JSX.Element
    }

    class getBadgeStyle {
        <<function>>
        +(size: "small"|"medium"): SerializedStyles
    }

    class defaultBadgeStyles {
        <<constant>>
        +backgroundColor: string
        +color: string
    }

    class sizeStyles {
        <<constant>>
        +small: {fontSize,padding,borderRadius}
        +medium: {fontSize,padding,borderRadius}
    }

    class Colors {
        <<module>>
        +notifications: object
    }

    BadgeProps <|.. Badge
    Badge --> getBadgeStyle : uses
    Badge --> defaultBadgeStyles : composes
    Badge --> sizeStyles : reads
    getBadgeStyle --> defaultBadgeStyles : spreads
    getBadgeStyle --> sizeStyles : spreads
    getBadgeStyle --> Colors : references
    Colors ..> defaultBadgeStyles : provides color source
```

> SVG rendering failed for this diagram.
