# Diagram: web/portal/src/components/atoms/DateTime.atom.tsx


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
classDiagram
class DateTime {
  +DateTimeProps props
  +render()
  -baseText
  -smallText
  -timeComponent
  -timezoneComponent
  -dateComponent
}
class "DateTime.Time" {
  +TimeProps props
  +render()
}
class "DateTime.Timezone" {
  +TimezoneProps props
  +render()
}
class "DateTime.Date" {
  +DateProps props
  +render()
}
class Text {
  +SharedTextProps props
  +render()
}
class parseDateTime {
  +parse(dateTime, localize, timezonePreference)
  --> {date, time, timezone, isValid, a11yTs}
}
class useSubcomponentOverride {
  +useSubcomponentOverride(children, defaultComponent)
  +returns ReactNode
}
class Colors <<enum>> {
  +text.GRAY
}
DateTime "1" o-- "DateTime.Time" : uses
DateTime "1" o-- "DateTime.Timezone" : uses
DateTime "1" o-- "DateTime.Date" : uses
"DateTime.Time" --> Text : composes
"DateTime.Timezone" --> Text : composes
"DateTime.Date" --> Text : composes
DateTime --> parseDateTime : calls
DateTime --> useSubcomponentOverride : calls
DateTime --> Colors : reads
parseDateTime ..> "DateTime.Time" : provides time
parseDateTime ..> "DateTime.Timezone" : provides timezone
parseDateTime ..> "DateTime.Date" : provides date
note for DateTime "Validations: showDate & showTime cannot both be false; handles fallback on invalid date"
```

> SVG rendering failed for this diagram.
