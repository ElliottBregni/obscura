# Diagram: web/portal/src/components/atoms/ScrollArea.atom.tsx


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
classDiagram
class ScrollArea {
  +autoHide: boolean = false
  +className: string
  +style: CSSProperties = {}
  +children: ReactNode
  +handleWidth: "11px"
  +handleColor: "rgba(0, 0, 0, 0.5)"
  +theme: "os-theme-scroll-area"
  +themeConfig: Object
  +options: Object
  +render(): OverlayScrollbarsComponent
}
class ScrollAreaProps {
  +autoHide?: boolean
  +className?: string
  +style?: CSSProperties
}
class OverlayScrollbarsComponent {
  +css: Object
  +className: string
  +style: CSSProperties
  +options: Object
  +defer: boolean
  +children: ReactNode
}
class themeConfig {
  +--os-size: string
  +--os-padding-perpendicular: string
  +--os-padding-axis: string
  +--os-track-bg: string
  +--os-track-bg-hover: string
  +--os-track-bg-active: string
  +--os-track-border-radius: string
  +--os-handle-bg: string
  +--os-handle-min-size: string
  +--os-handle-perpendicular-size: string
  +--os-handle-interactive-area-offset: string
}
class options {
  +paddingAbsolute: boolean
  +showNativeOverlaidScrollbars: boolean
  +update: Object
  +overflow: Object
  +scrollbars: Object
}
ScrollArea "1" o-- "1" ScrollAreaProps : accepts
ScrollArea --> OverlayScrollbarsComponent : renders
ScrollArea ..> themeConfig : defines
ScrollArea ..> options : defines
OverlayScrollbarsComponent ..> themeConfig : applies CSS theme
OverlayScrollbarsComponent ..> options : consumes configuration
options "1" o-- "1" scrollbars : contains
scrollbars : theme, visibility, autoHide, autoHideDelay, autoHideSuspend, dragScroll, clickScroll, pointers
options : update (elementEvents, debounce, attributes, ignoreMutation), overflow (x,y)
```

> SVG rendering failed for this diagram.
