# Diagram: web/portal/src/components/atoms/SelectAsync.atom.stories.tsx


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
classDiagram
class SelectAsync {
  +props: SelectAsyncProps
  +render()
}
class SelectAsyncProps {
  +loadOptions(inputValue): Promise
  +isMulti: boolean
  +showCheckbox: boolean
  +closeMenuOnSelect: boolean
  +hideSelectedOptions: boolean
  +onChange: function
}
class Template {
  +render(args): JSX.Element
}
class Default {
  +args
}
class ShowCheckBox {
  +args
}
class promiseOptions {
  +(inputValue): Promise<{options, hasMore}>
}
class defaultOptions {
  +Array<{value, label}>
}

Template --> SelectAsync : uses
Default --> Template : bind
ShowCheckBox --> Template : bind
SelectAsync ..> SelectAsyncProps : accepts
promiseOptions ..> defaultOptions : filters
Default --> promiseOptions : loadOptions
ShowCheckBox --> promiseOptions : loadOptions
```

> SVG rendering failed for this diagram.
