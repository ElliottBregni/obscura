# Diagram: web/portal/src/components/atoms/ScrollArea.atom.stories.tsx


> Auto-generated by Obscura crawlers

## Diagram 1

```mermaid
classDiagram
    class ScrollArea {
        +ScrollAreaProps
        +children
        +style
    }
    class Template {
        +args: ScrollAreaProps
        +render()
    }
    class TallSection {
        +render()
        -Array[0..99] items
    }
    class StoryExports {
        +Default
        +WithChildWithOverflow
    }
    ScrollArea <|-- Template : uses
    Template --> ScrollArea : returns
    StoryExports --> Template : binds
    StoryExports --> TallSection : uses as children
    TallSection ..> "div" : renders
    ScrollArea ..> "div (container)" : wraps children
```

> SVG rendering failed for this diagram.

## Diagram 2

```mermaid
flowchart TD
    A[Storybook Story: Atoms/ScrollArea] --> B[Template(args)]
    B --> C[ScrollArea Component]
    C --> D{props}
    D -->|children: TallSection| E[TallSection]
    D -->|style: {maxHeight:200}| F[Inline style]
    E --> G[div (100 items)]
    subgraph Stories
        H[Default] -->|args.children = TallSection| B
        I[WithChildWithOverflow] -->|args.children = div(maxHeight:200) -> TallSection| B
    end
```

> SVG rendering failed for this diagram.
