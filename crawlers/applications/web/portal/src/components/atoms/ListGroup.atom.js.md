# Diagram: web/portal/src/components/atoms/ListGroup.atom.js


> Auto-generated by Obscura crawlers

## Diagram 1

```mermaid
classDiagram
    class ListGroup {
        +children
        +className
        +render()
    }
    class ListGroupItem {
        +onClick
        +styles
        +children
        +render()
    }
    class BSListGroup
    class BSListGroupItem
    ListGroup --> BSListGroup : renders
    ListGroupItem --> BSListGroupItem : renders
    ListGroupItem ..|> ListGroup : attached as Item
    ListGroupItem o-- "onClick?: func" : prop
    ListGroup o-- "className?: string" : prop
    ListGroup o-- "children: any" : prop
    ListGroupItem o-- "styles?: object" : prop
```

> SVG rendering failed for this diagram.

## Diagram 2

```mermaid
flowchart TD
    A[App or Parent] --> B[ListGroup]
    B --> C[BSListGroup]
    C --> D[ListGroup.Item (ListGroupItem)]
    D --> E[BSListGroup.Item]
    A -->|passes children| B
    A -->|passes className| B
    A -->|passes onClick/styles| D
    E -->|onClick triggers| F[clickHandler]
```

> SVG rendering failed for this diagram.
