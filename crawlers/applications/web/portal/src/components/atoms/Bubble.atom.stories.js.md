# Diagram: web/portal/src/components/atoms/Bubble.atom.stories.js


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
classDiagram
    class StoryModule {
        <<module>>
        +title: "Atoms/Bubble"
        +component: Bubble
        +argTypes
    }
    class Bubble {
        <<React Component>>
        +props
    }
    class FontSize {
        <<enum>>
        +size16
        +size36
        +...options
    }
    class Colors {
        <<const>>
        +text.VERY_LIGHT_GRAY
        +text.YELLOW
        +background.DARK_BLUE
        +background.DARKER_BLUE
    }
    class Template {
        <<function>>
        +(args) : JSX.Element
    }
    class NumberStory {
        <<Story>>
        +args: { children: 12 }
    }
    class TextStory {
        <<Story>>
        +args: { children: "This is text inside the bubble" }
    }
    class CustomColorsAndSizeStory {
        <<Story>>
        +args: { children: "Displaying custom colors", color, background, size }
    }

    StoryModule --> Bubble : component
    StoryModule --> FontSize : uses
    StoryModule --> Colors : uses
    StoryModule --> Template : defines
    Template --> Bubble : renders
    NumberStory --|> Template : bind
    TextStory --|> Template : bind
    CustomColorsAndSizeStory --|> Template : bind
    CustomColorsAndSizeStory --> Colors : references
    StoryModule ..> NumberStory : exports
    StoryModule ..> TextStory : exports
    StoryModule ..> CustomColorsAndSizeStory : exports
```

> SVG rendering failed for this diagram.
