# Diagram: web/portal/src/components/selectors.js


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
flowchart LR
A[alfBy(sel, key)] --> R[createSelector (from reselect)]
R --> S[invoke sel → vals]
S --> L[_.sortBy(vals, key) (from lodash)]
L --> O[sorted array (returned)]
A ---|uses key| K[key parameter] 
K --> L
```

> SVG rendering failed for this diagram.
