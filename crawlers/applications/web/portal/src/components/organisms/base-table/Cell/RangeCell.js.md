# Diagram: web/portal/src/components/organisms/base-table/Cell/RangeCell.js


> Auto-generated by Obscura crawlers

## Diagram 1

```mermaid
classDiagram
    class RangeCell {
        +t(func) : required
        +dateRange?: { start?: dateTimePropType, end?: dateTimePropType }
        +render()
    }
    class dateTimePropType {
        +date: string
        +time: string
    }
    RangeCell --> dateTimePropType : "start / end use"
```

> SVG rendering failed for this diagram.

## Diagram 2

```mermaid
flowchart TD
    A[Start] --> B{dateRange present?}
    B -- No --> C[Return null]
    B -- Yes --> D[Container: flex row]
    D --> E{start present?}
    D --> G{end present?}
    E -- Yes --> F1[Start column\n- time\n- date]
    E -- No --> F2[no start block]
    G -- Yes --> H1[End column\n- time\n- date]
    G -- No --> H2[no end block]
    F1 --> I{start && end?}
    H1 --> I
    I -- Yes --> J[Center "to" text\n(margin, light gray)]
    I -- No --> K[No "to" text]
```

> SVG rendering failed for this diagram.
