# Diagram: web/portal/src/components/organisms/base-table/Filter/SelectFilter.js


> Auto-generated by Obscura crawlers

## Diagram 1

```mermaid
classDiagram
class SelectFilter {
  +isClearable: boolean = true
  +remainingProps: object
  +styles: object
  +render(): ReactElement
  +propTypes = Select.propTypes
}
class Select <<External Library>> {
  +props
  +styles
}
class Styles {
  +container(provided): object
  +control(provided): object
  +indicatorsContainer(provided): object
}
SelectFilter --> Select : renders
SelectFilter --> Styles : provides styles
SelectFilter ..> Select : reuses propTypes
```

> SVG rendering failed for this diagram.

## Diagram 2

```mermaid
flowchart TD
    A[Start] --> B[Call SelectFilter(props)]
    B --> C{isClearable in props?}
    C -- yes --> D[use provided isClearable]
    C -- no --> E[use default isClearable = true]
    D --> F[merge remainingProps]
    E --> F
    F --> G[build styles object\n(container, control, indicatorsContainer)]
    G --> H[Render <Select\nisClearable={isClearable}\n{...remainingProps}\nstyles={styles} />]
    H --> I[End]
```

> SVG rendering failed for this diagram.
