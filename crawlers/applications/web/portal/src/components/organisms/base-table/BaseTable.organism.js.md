# Diagram: web/portal/src/components/organisms/base-table/BaseTable.organism.js


> Auto-generated by Obscura crawlers

## Diagram 1

```mermaid
classDiagram
    class BaseTable {
        +props
        +render()
        -useTable(...)
        -useEffect(updatePage)
        -updatePage()
        -updatePageDebounced
    }
    class DefaultColumnFilter {
        +column
        +render()
    }
    class Themes {
        <<enumeration>>
        DEFAULT
        DARK
        LIGHT
        LIGHT_CHILD
    }
    class useCustomControlledState {
        +(tableState, props)
        -pagination
        -sortBy
        -filters
    }
    class ScrollArea
    class Tooltip
    class ThemeProvider
    class Styles
    class FontAwesomeIcon
    class Alert

    BaseTable --> ThemeProvider : uses
    ThemeProvider --> Styles : wraps
    BaseTable --> ScrollArea : contains
    BaseTable --> Tooltip : uses
    BaseTable --> Alert : uses
    BaseTable --> FontAwesomeIcon : uses
    BaseTable --> DefaultColumnFilter : defaultColumn.Filter
    BaseTable --> useCustomControlledState : useControlledState
    BaseTable --> Themes : default theme enum
    DefaultColumnFilter ..> _: lodash.debounce
    useCustomControlledState ..> _: useMemo
    BaseTable ..> _: useTable, useFilters, useSortBy, usePagination, useFlexLayout, useResizeColumns, useExpanded
    BaseTable o-- "headers[]" : renders
    BaseTable o-- "page[]" : renders rows
    BaseTable --> ScrollArea : scrollToTableTop()
```

> SVG rendering failed for this diagram.

## Diagram 2

```mermaid
flowchart TD
    subgraph Components
        A[BaseTable] 
        B[DefaultColumnFilter]
        C[ScrollArea]
        D[Tooltip]
        E[Pagination Controls]
    end

    subgraph Hooks/Utilities
        H1[useTable + plugins]
        H2[useCustomControlledState]
        H3[useTranslation]
        L[lodash utils]
    end

    A --> H1
    H1 --> H2
    A --> H3
    A --> B
    A --> C
    A --> D
    A --> E
    A --> L

    H1 -->|provides| headers
    H1 -->|provides| page
    H1 -->|provides| prepareRow
    headers -->|render header cells| A
    page -->|render row cells| A
    E -->|calls| H1 : nextPage / previousPage / gotoPage / setPageSize
    B -->|debounces| L
    A -->|onFilterChange| props
    A -->|onPageChange / onPageSizeChange| props

    clickHandlers((Row click))
    A --> clickHandlers
    clickHandlers -->|calls| props.rowClickHandler
    A -->|uses| scrollToTableTop()
```

> SVG rendering failed for this diagram.
