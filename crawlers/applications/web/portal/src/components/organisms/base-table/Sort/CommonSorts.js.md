# Diagram: web/portal/src/components/organisms/base-table/Sort/CommonSorts.js


> Auto-generated by Obscura crawlers

## Diagram 1

```mermaid
classDiagram
    class sortByAddress {
        +rowA
        +rowB
        +columnId
        +returns Number
    }
    class sortByTimestamp {
        +rowA
        +rowB
        +columnId
        +returns Number
    }
    class formatTimestamp {
        +value
        +returns {date, time}
    }
    sortByAddress --> formatTimestamp : (no)
    sortByTimestamp --> formatTimestamp
```

> SVG rendering failed for this diagram.

## Diagram 2

```mermaid
flowchart TD
    A[sortByAddress(rowA, rowB, columnId)] --> B{both addresses empty/null?}
    B -- yes --> C[return 0]
    B -- no --> D{address1 empty/null?}
    D -- yes --> E[return 1]
    D -- no --> F{address2 empty/null?}
    F -- yes --> G[return -1]
    F -- no --> H{address1 > address2?}
    H -- yes --> I[return 1]
    H -- no --> J[return -1]
```

> SVG rendering failed for this diagram.

## Diagram 3

```mermaid
flowchart TD
    K[sortByTimestamp(rowA, rowB, columnId)] --> L[ts1 = formatTimestamp(rowA.values[columnId])]
    K --> M[ts2 = formatTimestamp(rowB.values[columnId])]
    L --> N{date1 > date2?}
    M --> N
    N -- yes --> O[return 1]
    N -- no --> P{date1 < date2?}
    P -- yes --> Q[return -1]
    P -- no --> R{time1 > time2?}
    R -- yes --> S[return 1]
    R -- no --> T{time1 < time2?}
    T -- yes --> U[return -1]
    T -- no --> V[return 0]
```

> SVG rendering failed for this diagram.
