# Diagram: web/portal/src/components/organisms/ArrivalAndDeparturePanelGroup.organism.js


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
classDiagram
    class transformLocation {
        +location:Object
        +returns:locationData
    }
    class getLocationDataFromShipments {
        +shipments:Array
        +returns:{origin,destination,calculatedEta,frozenEta,isFvEta}
    }
    class ArrivalAndDeparturePanelGroup {
        +props:origin,destination,eta,visibleFields,...
        +render():JSX
        +SubHeaders
        +RouteSubHeader
        +TripPlanIdSubHeader
        +LastUpdateSubHeader
    }
    class EtaBox {
        +props:eta,warningMessage,isFvEta,etaWindow,onlyShowEtaDate,showEtaAtTimezone,heldExceptionDateTime
        +render():JSX
    }
    class ArrivalDepartureTimes {
        +props:location,availableForUnloadTs,completeStateChangeTs,...
        +render():JSX
    }
    class ScheduledArrivalWindow {
        +props:location,hideScheduledTimes
        +render():JSX
    }
    class TableSlice {
        +props:title,children,style
        +render():JSX
    }
    class LocationDetails {
        +props:title,location,center,style
        +render():JSX
    }
    class ContentHeader {
        +props:title,icon,style
        +render():JSX
    }
    class SubHeaders {
        +children
        +render():JSX|null
    }
    class RouteSubHeader {
        +props:routeId,onTimePercentage
        +render():JSX|null
    }
    class TripPlanIdSubHeader {
        +props:tripPlanId
        +render():JSX|null
    }
    class LastUpdateSubHeader {
        +props:location,ts
        +render():JSX|null
    }

    %% relationships
    transformLocation --> LocationDetails : provides data for
    getLocationDataFromShipments --> ArrivalAndDeparturePanelGroup : supplies origin/destination
    ArrivalAndDeparturePanelGroup --> EtaBox : composes
    ArrivalAndDeparturePanelGroup --> SubHeaders : uses
    ArrivalAndDeparturePanelGroup --> RouteSubHeader : exposes
    ArrivalAndDeparturePanelGroup --> TripPlanIdSubHeader : exposes
    ArrivalAndDeparturePanelGroup --> LastUpdateSubHeader : exposes
    ArrivalAndDeparturePanelGroup --> LocationDetails : composes
    ArrivalAndDeparturePanelGroup --> ScheduledArrivalWindow : composes
    ArrivalAndDeparturePanelGroup --> ArrivalDepartureTimes : composes
    ArrivalAndDeparturePanelGroup --> ContentHeader : composes
    ScheduledArrivalWindow --> TableSlice : composes
    ArrivalDepartureTimes --> TableSlice : composes
    EtaBox --> Icon : uses
    EtaBox --> DateTime : uses
    LocationDetails --> LocationAddressComplex : uses
```

> SVG rendering failed for this diagram.
