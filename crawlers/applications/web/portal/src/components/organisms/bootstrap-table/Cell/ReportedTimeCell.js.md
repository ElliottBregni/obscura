# Diagram: web/portal/src/components/organisms/bootstrap-table/Cell/ReportedTimeCell.js


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
flowchart TD
  A[coord] --> B[startFormatted\nlocalizedTimeFormatter(coord.start)]
  A --> C[endFormatted\nlocalizedTimeFormatter(coord.end)]
  A --> I[groupedIds.length > 1]
  B --> D{startFormatted != endFormatted\nAND groupedIds.length > 1}
  C --> D
  I --> D
  D -- yes --> E[timeFormatted = startFormatted - endFormatted]
  D -- no --> F[timeFormatted = startFormatted]
  E --> G[BoldCell]
  F --> G
  G --> H[Rendered output (BoldCell children: timeFormatted)]
```

> SVG rendering failed for this diagram.
