# Diagram: web/portal/src/components/molecules/TripProgessBar.molecule.js


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
flowchart TD
  Start([TimelineMad Component]) --> CheckPlanned{isPlannedLegFvGenerated?}
  CheckPlanned -- Yes --> SetProgress50[progress = 50]
  SetProgress50 --> ComputePosition[Set positionStyles: position:absolute]
  ComputePosition --> PlannedTooltip[Render Tooltip (borderRadius:50%, bg: LIGHT_GRAY, border:3px solid grey)]
  PlannedTooltip --> IconNode[Icon src=LogoMark, type=LocalImage, size=22]
  CheckPlanned -- No --> ComputePosition2[Set positionStyles: position:absolute]
  ComputePosition2 --> ProgressLT100{progress < 100?}
  ProgressLT100 -- Yes --> TopRule[positionStyles.top = `${progress}%`]
  ProgressLT100 -- No --> ProgressEQ100{progress === 100?}
  ProgressEQ100 -- Yes --> BottomRule[positionStyles.bottom = 0]
  ProgressEQ100 -- No --> NoTopBottom[(no top/bottom set)]
  TopRule --> CheckInBetween{progress > 0 and progress < 100?}
  BottomRule --> CheckInBetween
  NoTopBottom --> CheckInBetween
  CheckInBetween -- Yes --> TransformRule[positionStyles.transform = "translateY(-50%)"]
  CheckInBetween -- No --> NoTransform[no transform]
  TransformRule --> RenderMad[Render Tooltip placement="top"]
  NoTransform --> RenderMad
  RenderMad --> MadChicletNode[MadChiclet(shipmentMode=mode, stopMode=mode, hideBorder=hideBorder, height=22, width=22)]
  MadChicletNode --> End([Rendered UI])
```

> SVG rendering failed for this diagram.
