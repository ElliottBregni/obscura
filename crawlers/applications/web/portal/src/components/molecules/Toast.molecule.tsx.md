# Diagram: web/portal/src/components/molecules/Toast.molecule.tsx


> Auto-generated by Obscura crawlers

## Diagram 1

```mermaid
classDiagram
    class Toast {
        +boolean show
        +ToastVariant variant
        +string title
        +ReactNode children
        +onClose()
        +position
        +autoHide
        +delay
        +maxWidth
    }
    class Icon
    class Text
    class createPortal
    class getVariantIcon
    class getVariantStyles
    class getPositionStyles

    Toast --> Icon : renders
    Toast --> Text : renders
    Toast --> createPortal : mounts toastElement
    Toast ..> getVariantIcon : calls
    Toast ..> getVariantStyles : calls
    Toast ..> getPositionStyles : calls
    Icon <|-- "fa* icons" : source
    getVariantStyles --> Colors : reads
    getPositionStyles --> "position constants" : returns CSS
```

> SVG rendering failed for this diagram.

## Diagram 2

```mermaid
flowchart TD
    A[show === false?] -->|yes| B[Return null]
    A -->|no| C[Compute variantStyles, variantIcon, positionStyles]
    C --> D{autoHide && onClose && show}
    D -->|yes| E[setTimeout(delay) -> onClose()]
    D -->|no| F[No auto-hide timer]
    C --> G[Build toastElement JSX]
    G --> H[Includes Icon, Title?, Children, CloseButton?]
    H --> I{onClose provided?}
    I -->|yes| J[Close button onClick -> onClose()]
    I -->|no| K[No close button]
    G --> L[createPortal(toastElement, document.body)]
    E --> L
    J --> L
```

> SVG rendering failed for this diagram.
