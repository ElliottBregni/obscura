# Diagram: web/portal/src/components/molecules/DialogModal.molecule.js


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
flowchart LR
Props[Props] --> DialogModal[DialogModal]
DialogModal --> Modal[Modal (react-bootstrap)]
Modal --> Header[Modal.Header<br/>(rendered if title)]
Modal --> Body[Modal.Body<br/>(children)]
Modal --> Footer[Modal.Footer]

Props -->|show| Modal
Props -->|onHide| Modal
Props -->|title| Header
Props -->|children| Body
Props -->|footerContent| Footer

Footer -->|footerContent present| RenderFooter[Render footerContent]
Footer -->|else| FooterLogic[Footer logic]

subgraph FooterLogicBranch
  FooterLogic -->|loadStatus == "success" \nOR "failed"| OkBranch[Ok button -> onHide]
  FooterLogic -->|else if isLoading === false| ButtonsBranch[Cancel + Submit buttons]
  FooterLogic -->|else| Empty[No footer buttons]
end

ButtonsBranch --> CancelBtn[Cancel Button -> onHide]
ButtonsBranch --> SubmitBtn[Submit Button -> onSubmit]
SubmitBtn -->|disabled if submitButtonDisabled| SubmitDisabled[Disabled?]
SubmitBtn -->|if isSubmitting| Spinner[Icon faSpinner (spin)]
Spinner --> SubmitBtn
SubmitBtn -->|else| SubmitText[submitButtonText]
```

> SVG rendering failed for this diagram.
