# Diagram: web/portal/src/components/molecules/VerticalStackedBarChart.molecule.stories.js


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
graph TD
  Story[Story: "Molecules/VerticalStackedBarChart"]
  DefaultComp[Default (story component)]
  Vertical[VerticalStackedBarChart (component)]
  Args[Default.args (props)]
  Data[Data → 5 location objects]
  Bars[Bars → nonShippable (Aging, #d5473f), shippable (Dwell, #46b630)]
  BarStyle[barStyle → data & labels]
  CrossAxis[crossAxisStyle]
  AxisStyle[axisStyle]
  Flyout[flyoutStyle]
  Handlers[Handlers → onClickBar(), onClickLegend()]
  XAccessor[xAccessor → "name"]
  DefaultComp -->|renders| Vertical
  Story -->|defines| DefaultComp
  DefaultComp -->|provides props| Args
  Args --> Data
  Args --> Bars
  Args --> BarStyle
  Args --> CrossAxis
  Args --> AxisStyle
  Args --> Flyout
  Args --> Handlers
  Args --> XAccessor
  Data -->|binds to| Vertical
  Bars -->|stacked bars config| Vertical
  BarStyle --> Vertical
  CrossAxis --> Vertical
  AxisStyle --> Vertical
  Flyout --> Vertical
  Handlers --> Vertical
```

> SVG rendering failed for this diagram.
