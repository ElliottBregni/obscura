# Diagram: web/portal/src/components/molecules/LocationAddressBlock.molecule.js


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
flowchart LR
  Props[Props\n(name,address1,address2,city,state,country,postalCode,defaultTextColor,addressColor,style,lookupCountryNameFromCode)]
  Hook[useGetCountryNameByCountryCode(country) --> countryName]
  Component[LocationAddressComplex Component]
  Props --> Component
  Component --> Hook
  Component --> Container[div\n(flex column, color=defaultTextColor)]
  Container --> NameCond{name ? render Name block : skip}
  NameCond -->|true| IdBoxNode[IdBox\n(name.value, name.rightContent)]
  Container --> Addr1Cond{address1 ?}
  Addr1Cond -->|true| Addr1[span\n(address1, color=addressColor)]
  Container --> Addr2Cond{address2 ?}
  Addr2Cond -->|true| Addr2[span\n(address2, color=addressColor)]
  Container --> CityStatePostal[span\n(city?, state?, postalCode?)]
  CityStatePostal --> CityCond{city ?}
  CityCond -->|true| City[span\n(city)]
  CityStatePostal --> CommaCond{city && state ?}
  CommaCond -->|true| Comma[span(",")]
  CityStatePostal --> StateCond{state ?}
  StateCond -->|true| State[span\n(state)]
  CityStatePostal --> PostalCond{postalCode ?}
  PostalCond -->|true| Postal[span\n(postalCode)]
  Container --> CountryCond{country ?}
  CountryCond -->|true| CountryDisplay[span\n(lookup ? countryName : country)]
  Props -->|default| ColorsNode[Colors constants\n(defaultTextColor, addressColor)] 
  ColorsNode --> Container
```

> SVG rendering failed for this diagram.
