# Diagram: web/portal/src/components/molecules/TextInputValidated.molecule.tsx


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
flowchart LR
  Props[Props<br/>(value, required, minLength, maxLength, pattern, email, url, number, customValidation, asyncValidation, errorMessage, showErrorOn, showSuccessState, label, helpText)]
  Props --> Validation[Validation Logic<br/>(sync checks & customValidation)]
  Validation -->|result string| ValidationError[validationError (string)]
  Validation -->|no error| Valid[isValid = true]
  ValidationError --> IsValidFalse[isValid = false]
  ValidationError --> ShouldShowError{shouldShowError?<br/>(touched && validationError && showErrorOn in [change, blur])}
  ShouldShowError -->|true| ShowErrorNode[Render Error Text]
  ShouldShowError -->|false| MaybeHelp{helpText present?}
  MaybeHelp -->|true| ShowHelp[Render Help Text]
  MaybeHelp -->|false| Nothing[No helper shown]

  Valid --> UIState{showSuccessState && value && touched?}
  UIState -->|true| ShowSuccess[Input border GREEN]
  UIState -->|false| NormalInput[Input default style]

  Events((User Events))
  Events -->|onChange -> handleChange(newValue)| HandleChange[handleChange]
  HandleChange --> Props
  HandleChange --> Validation
  HandleChange -->|if asyncValidation & newValue| AsyncVal[asyncValidation Promise]
  AsyncVal -->|string result| SetAsyncError[setAsyncError(result)]
  AsyncVal -->|false| SetAsyncErrorFalse[setAsyncError(errorMessage || "Invalid value")]
  AsyncVal -->|true/valid| ClearAsyncError[setAsyncError("")]
  SetAsyncError --> Validation
  SetAsyncErrorFalse --> Validation
  ClearAsyncError --> Validation

  Events -->|onClick -> handleClick| HandleClick[handleClick]
  HandleClick -->|if showErrorOn == "blur"| SetTouched[touched = true]
  SetTouched --> ShouldShowError
  SetTouched --> UIState

  ValidationError -->|if present| OnErrorEffect[useEffect -> onError(validationError)]
  Valid -->|on change| OnValidationChangeEffect[useEffect -> onValidationChange(isValid, validationError)]
  Valid --> OnValidationCompleteEffect[useEffect -> onValidationComplete(isValid, validationError)]
  OnErrorEffect --> ExternalCallbacks[External Callbacks]
  OnValidationChangeEffect --> ExternalCallbacks
  OnValidationCompleteEffect --> ExternalCallbacks

  Label[label present?] -->|true| RenderLabel[Render Label with required *]
  RenderLabel --> UIContainer[Div containing label, TextInput, helper/error]

  UIContainer --> TextInputComp[TextInput component<br/>(props: value, defaultValue, onChange, onClick, placeholder, disabled, readOnly, maxLength, style)]
  TextInputComp --> Events

  classDef ui fill:#f9f,stroke:#333,stroke-width:1px;
  class UIContainer,TextInputComp,ShowError,ShowHelp,RenderLabel ui;
```

> SVG rendering failed for this diagram.
