# Diagram: web/portal/src/components/map-search-results/ContentSection.js


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
classDiagram
class ContentSection <<React Component>> {
  +children: PropTypes.node (required)
  +isMobile: PropTypes.bool (required)
  +isShowingOverlay: PropTypes.bool (required)
  +render(): JSX.Element
}
class FlexDiv <<Styled Element>> {
  +flex: 1
  +display: "flex"
  +flexDirection: "row | column"
  +position: "relative"
  +overflow: "hidden"
}
class ContentSectionOverlay <<React Component>> {
  +render(): JSX.Element
}
ContentSection --> FlexDiv : wraps/uses
ContentSection --> ContentSectionOverlay : conditionally renders (isShowingOverlay ?)
ContentSection ..> PropTypes : uses
FlexDiv <-- ContentSection : provides container for children
```

> SVG rendering failed for this diagram.
