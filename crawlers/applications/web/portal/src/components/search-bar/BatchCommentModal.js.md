# Diagram: web/portal/src/components/search-bar/BatchCommentModal.js


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
classDiagram
    class BatchCommentModal {
        <<React Component>>
        +props: show: bool
        +props: hide(): func
        +props: addBatchComments(): func
        +props: clearBatchComments(): func
        +props: shareableOrg: object
        +props: shareableText: string
        +props: shareableTooltipText: string
        +props: isBatchCommentInProgress: bool
        +props: isBatchCommentSuccessful: bool
        +props: isBatchCommentFailed: bool
        +props: batchCsvExample: {data, headers}
        +props: batchImportLabel: string
        +props: shippers: array
        +props: shipperOrgId: number
        --
        +state: importFile
        +state: importFileCsvData: string
        +state: comment: string
        +state: importIds: string
        +state: isSharing: bool
        +state: importShipperOrgId
        --
        +useEffect(resetOnShow)
        +useEffect(resetOnSuccess)
        +getTooltipDisplay(): JSX
        +onFileLoad(fileEvent)
        +getBatchData(): object
    }

    class Modal {
        <<UI Container>>
        +Header()
        +Body()
        +Footer()
    }

    class SelectInput {
        <<Form Input>>
        +label
        +options
        +value
        +onChange()
    }

    class DownloadCsvLink {
        <<Utility>>
        +data
        +headers
        +label
        +filename
    }

    class Tooltip {
        <<UI>>
        +placement
        +tooltipChildren
    }

    class Button {
        <<UI Button>>
        +variant
        +onClick()
        +disabled
        +pulse
    }

    class Checkbox {
        <<UI Checkbox>>
        +checked
        +onChange()
        +disabled
    }

    class FormControl {
        <<Form Control>>
        +type
        +value
        +onChange()
        +as
        +rows
    }

    class Alert {
        <<UI Alert>>
        +variant
        +show
        +dismissible
        +onDismiss()
    }

    class Icon {
        <<UI Icon>>
        +src
        +spin
    }

    BatchCommentModal --> Modal : uses
    BatchCommentModal --> SelectInput : uses
    BatchCommentModal --> DownloadCsvLink : uses
    BatchCommentModal --> Tooltip : uses
    BatchCommentModal --> Button : uses
    BatchCommentModal --> Checkbox : uses
    BatchCommentModal --> FormControl : uses
    BatchCommentModal --> Alert : uses
    BatchCommentModal --> Icon : uses
    BatchCommentModal ..> FileReader : uses

    %% Data/logic relationships
    BatchCommentModal o-- "shipperOptions" : derives
    BatchCommentModal ..> "batchCsvExample.data" : reads
    BatchCommentModal ..> "batchCsvExample.headers" : reads
```

> SVG rendering failed for this diagram.
