# Diagram: web/portal/src/components/search-bar/AsyncCategoryOptionsStateBuilder.js


> Auto-generated by Obscura crawlers

## Diagram 1

```mermaid
flowchart LR
  A[buildAsyncCategoryOptionsState(config)] --> B{Validate config}
  B -->|missing topic| E1[Throw Error: Missing topic]
  B -->|missing url and getUrl| E2[Throw Error: Missing url/getUrl]
  B --> C[Select axiosRequest based on method & debounceWaitTimeInMs]
  C --> C1[method == GET -> axios.get or debounced axios.get]
  C --> C2[method == POST -> axios.post or debounced axios.post]
  C --> C3|else| E3[Throw Error: Unsupported request method]
  C1 --> D[Define action types: FETCH, RECEIVE, FETCH_ERROR]
  C2 --> D
  D --> F[getRequestUrl(state)]
  F --> G[fetch(query, pageSize)]
  G --> H{method == GET?}
  H -->|Yes| I[Build config with params {pageNumber:0,pageSize,(queryParam:query?)} and headers]
  H -->|No| J[Build requestBody {q, pagination:{size,searchAfter:null}}]
  I --> K[axiosRequest(requestUrl, config)]
  J --> L[axiosRequest(requestUrl, requestBody)]
  K --> M[.then -> getResponseData(response.data) -> map transformResult -> dispatch RECEIVE]
  K --> N[.catch -> console.error -> dispatch FETCH_ERROR]
  L --> M
  L --> N
  M --> O[Reducer updates state.results and isLoading=false]
  N --> P[Reducer sets results=[] and isLoading=false]
  G --> Q[Selectors: getResults(state) / getIsLoading(state)]
  Q --> R[Returned object {mountPoint, actionCreators:{fetch}, selectors, reducer}]
```

> SVG rendering failed for this diagram.

## Diagram 2

```mermaid
classDiagram
  class buildAsyncCategoryOptionsState {
    +function(config)
  }
  class ReturnedObject {
    +mountPoint: string
    +actionCreators: object
    +selectors: object
    +reducer: function
  }
  class ActionCreators {
    +fetch(query, pageSize)
  }
  class Selectors {
    +getResults(state)
    +getIsLoading(state)
  }
  class Reducer {
    +reducer(state, action)
    -initialState {results:[], isLoading:false}
  }
  buildAsyncCategoryOptionsState --> ReturnedObject
  ReturnedObject --> ActionCreators
  ReturnedObject --> Selectors
  ReturnedObject --> Reducer
  ActionCreators --> Reducer : dispatches FETCH/RECEIVE/FETCH_ERROR
  Reducer --> Selectors : read state[topic]
```

> SVG rendering failed for this diagram.
