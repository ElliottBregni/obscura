# Diagram: web/portal/src/components/templates/SplashPage.template.stories.tsx


> Auto-generated by Obscura crawlers

## Diagram 1

```mermaid
classDiagram
class Story {
}
class SplashPage {
  +render(args)
}
class Icon {
  +type
  +altText
  +src
}
class IconType {
  <<enum>>
  +LocalImage
}
class LogoIcon {
  <<asset>>
}
class Template {
  +bind()
  +args
}
class LogoWithText {
  +args
}
Template --> SplashPage : uses
LogoWithText --> Template : boundFrom
LogoWithText ..> LogoIcon : imports
IconType <|-- Icon
SplashPage ..> Icon : renders
LogoWithText --> "children (JSX)"
```

> SVG rendering failed for this diagram.

## Diagram 2

```mermaid
flowchart LR
T[Template (Story)] --> S[SplashPage]
LWT[LogoWithText (export)] --> T
LWT --> A[args.children]
A --> D[div maxWidth:500px]
D --> I[Icon component]
I --> IT[IconType.LocalImage]
I --> LI[LogoIcon.svg]
D --> Text[Lorem ipsum ... (static text)]
LWT -.-> Export[export const LogoWithText]
```

> SVG rendering failed for this diagram.
