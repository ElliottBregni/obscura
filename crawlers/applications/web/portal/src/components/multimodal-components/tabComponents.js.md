# Diagram: web/portal/src/components/multimodal-components/tabComponents.js


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
classDiagram
    class ResponsiveLegTabLabel {
        +children
        +render()
    }
    class endToEndTab {
        +eventHandler
        +logo
        +altText
        +imageStyle
        +style
        +returns Tabs.Tab
    }
    class legTabs {
        +childSummaries
        +childDetails
        +eventHandler
        +isShipmentDetails
        +onClick()
        +getTabLabel()
        +returns [Tabs.Tab]
    }
    class "Tabs.Tab" {
        +style
        +key
        +onClick(evt)
        +data-qa
    }
    class Image {
        +type
        +media
        +altText
        +style
        +imageStyle
    }
    class EndToEndTabIcon
    class Translation {
        +ns
        +(t) => ReactNode
    }
    class MadChicletCSS {
        +shipmentMode
        +stopMode
        +width
        +height
    }
    class ShowForSmallAndUp
    class PropTypes

    ResponsiveLegTabLabel --> ShowForSmallAndUp : wraps
    ResponsiveLegTabLabel --> LegTabLabel : renders
    endToEndTab --> "Tabs.Tab" : returns
    endToEndTab --> Image : uses (when logo)
    endToEndTab --> EndToEndTabIcon : uses (when no logo)
    endToEndTab --> Translation : uses
    legTabs --> "Tabs.Tab" : returns array of
    legTabs --> MadChicletCSS : uses
    legTabs --> Translation : uses (for default label)
    endToEndTab ..> PropTypes : validates
    ResponsiveLegTabLabel ..> PropTypes : validates
    Image <-- "components/atoms/Image.atom" : implemented in
    MadChicletCSS <-- "components/chiclets" : implemented in
```

> SVG rendering failed for this diagram.
