# Diagram: web/portal/src/modules/mt-search/MetalTrackingSavedSearchContainer.js


> Auto-generated by Obscura crawlers

## Diagram 1

```mermaid
classDiagram
class SavedSearchSelect {
  +props: fvSearch...
  +savedSearch
  +savedSearches
  +searchObj
  +searchFilters
  +searchCategory
  +searchText
  +solutionId
  +EditModal
  +DeleteModal
  +fetchSavedSearches()
  +loadSavedSearch(savedSearch)
  +saveSearch(name, searchObj)
  +editSearch(id,name,searchObj)
  +deleteSearch(id)
  +exportSearch(t,solutionId)
  +selectSavedSearch(item)
  +resetSavedSearch()
}
class Connect {
  +mapStateToProps(state)
  +mapDispatchToProps(dispatch)
}
class MetalTrackingSavedSearchState {
  <<module>>
  +selectors
  +actionCreators
}
class MetalTrackingSearchBarState {
  <<module>>
  +selectors
  +actionCreators
}
class OrganizationsState {
  <<module>>
  +getSolutionId(state)
}
class EditSavedSearchModalContainer {
  <<component>>
}
class DeleteSavedSearchModal {
  <<component>>
}
Connect --> SavedSearchSelect : connects
SavedSearchSelect ..> MetalTrackingSavedSearchState : uses selectors\nand actionCreators
SavedSearchSelect ..> MetalTrackingSearchBarState : uses selectors\nand actionCreators
SavedSearchSelect ..> OrganizationsState : uses getSolutionId
SavedSearchSelect ..> EditSavedSearchModalContainer : passes as EditModal
SavedSearchSelect ..> DeleteSavedSearchModal : passes as DeleteModal
MetalTrackingSavedSearchState o-- "selectors" : provides
MetalTrackingSavedSearchState o-- "actionCreators" : provides
MetalTrackingSearchBarState o-- "selectors" : provides
MetalTrackingSearchBarState o-- "actionCreators" : provides
```

> SVG rendering failed for this diagram.

## Diagram 2

```mermaid
flowchart LR
  subgraph ReduxState
    state[fvSearch...] 
    mtsb[MetalTrackingSearchBarState.selectors]
    mtsv[MetalTrackingSavedSearchState.selectors]
    orgs[getSolutionId]
  end
  subgraph Components
    CSS[SavedSearchSelect]
    EditModal[EditSavedSearchModalContainer]
    DeleteModal[DeleteSavedSearchModal]
  end
  subgraph Actions
    MTS_actions[MetalTrackingSavedSearchState.actionCreators]
    MTB_actions[MetalTrackingSearchBarState.actionCreators]
  end
  state -->|spread into props| CSS
  mtsb -->|getSelectedSavedSearch, getSearchFilters, getSearchCategory, getSearchText| CSS
  mtsv -->|getSavedSearches| CSS
  orgs -->|solutionId| CSS
  CSS -->|uses EditModal prop| EditModal
  CSS -->|uses DeleteModal prop| DeleteModal
  CSS -->|dispatch fetchSavedSearches()| MTS_actions
  CSS -->|dispatch loadSavedSearch()| MTS_actions
  CSS -->|dispatch saveSearch()| MTS_actions
  CSS -->|dispatch editSearch()| MTS_actions
  CSS -->|dispatch deleteSearch()| MTS_actions
  CSS -->|dispatch exportEntities()| MTB_actions
  CSS -->|dispatch selectSavedSearch()/resetSavedSearch()| MTB_actions
```

> SVG rendering failed for this diagram.
