# Diagram: web/portal/src/modules/mt-search/MetalTrackingEditSavedSearchModalContainer.js


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
flowchart LR
  Connect["connect(mapStateToProps, mapDispatchToProps)"] --> EditSavedSearchModal[EditSavedSearchModal]
  mapStateToProps["mapStateToProps(state)"] --> Connect
  mapDispatchToProps["mapDispatchToProps(dispatch)"] --> Connect

  MetalTrackingSearchBarState["MetalTrackingSearchBarState"] -->|selectors.getSearchFilters(state)| getSearchFilters[getSearchFilters(state)]
  MetalTrackingSearchBarState -->|selectors.getSearchCategory(state)| getSearchCategory[getSearchCategory(state)]
  MetalTrackingSearchBarState -->|selectors.getSearchText(state)| getSearchText[getSearchText(state)]

  getSearchFilters -->|provides searchFilters prop| mapStateToProps
  getSearchCategory -->|provides searchCategory prop| mapStateToProps
  getSearchText -->|provides searchText prop| mapStateToProps

  SearchBarContainer["SearchBarContainer"] -->|passed as prop| mapStateToProps
  FilterSectionContainer["FilterSectionContainer"] -->|passed as prop| mapStateToProps

  MetalTrackingSavedSearchState["MetalTrackingSavedSearchState"] --> actionCreators["actionCreators"]
  actionCreators -->|saveSearch(name, search)| saveSearch[saveSearch(name, search)]
  actionCreators -->|editSearch(id, name, search)| editSearch[editSearch(id, name, search)]

  saveSearch -->|dispatched via dispatch(...)| mapDispatchToProps
  editSearch -->|dispatched via dispatch(...)| mapDispatchToProps
```

> SVG rendering failed for this diagram.
