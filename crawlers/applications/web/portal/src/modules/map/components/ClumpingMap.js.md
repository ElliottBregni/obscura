# Diagram: web/portal/src/modules/map/components/ClumpingMap.js


> Auto-generated by Obscura crawlers

## Diagram 1

```mermaid
classDiagram
    class SimpleMap {
        <<abstract>>
        +initMap()
        +initAll()
        +createAndAddMapMarker(id, position, z, data, clickable, svg, h, w, dh, dw, ox, oy)
        +addMapMarkerEventListener(marker, event, handler)
        +addMapEventListener(event, handler)
        +clearMapMarkers(prefix)
        +getMapZoom()
        +setMapZoom(z)
        +setMapCenter(position)
        +isMapViewBoundsContainLatLng(lat, lng)
        +zoomSingleLocation(location)
        +zoomLocations(locations)
        +createAndAddMapInfoBubble(position, dom)
    }

    class ClumpingMap {
        +groups: MapGroups
        +_groupsOrderedLocationsCountByZoomLevelCache
        +state: { mapLocations, hasUpdatedOnce }
        +constructor(props)
        +initMap()
        +initAll()
        +componentDidMount()
        +componentDidUpdate(prevProps)
        +loadLocations(onLocationsLoaded)
        +getVisibleLocations()
        +onMarkerLocationClick(event, googleMapsMarker)
        +onMarkerGroupClick(e, googleMapsMarker)
        +onMapViewChange()
        +_onMapViewChangeBubbleEvent()
        +onMarkerMouseEnter(event, googleMapsMarker)
        +onMarkerMouseOut()
        +addLocationMarker(position, markerId, label, data)
        +addGroupMarker(id, position, count)
        +generateAndRedrawGroups()
        +drawGroups(groups)
        +clearDrawnGroups()
        +clearDrawnLocations()
        +recenterMap()
        +showPopupForLocation(location)
        +showPopupForGroup(group, selectedLocation)
        +_getInfoPopupSingleLocationDOMObject(location)
        +_getInfoPopupMultipleLocationsDOMObject(locations, selectedLocation)
        +allowRegroupOfUngroupedMarkers()
        +computeMarkersGroupings(locations)
    }

    class MapGroups {
        +constructor(zoom?)
        +addUngroupedLocation(location)
        +addLocationToExistentGroup(location)
        +addNewGroupWithOneLocation(location)
        +groupUngroupedSameLocationInTheSameGroup()
        +positionGroupsInTheCenterOfItsLocations()
        +get(id)
        +getGroupByLocation(location)
        +ungroup(groupId)
        +keys()
        +values()
    }

    ClumpingMap --|> SimpleMap : extends
    ClumpingMap o-- MapGroups : uses
    ClumpingMap ..> PinChicletSVG : renders
    ClumpingMap ..> CircleChicletSVG : renders
    ClumpingMap ..> Colors : reads
    ClumpingMap ..> MapState : maps state
    ClumpingMap ..> PropTypes : declares
```

> SVG rendering failed for this diagram.

## Diagram 2

```mermaid
flowchart TD
    A[Props: mapLocations] --> B{loadLocations detects change?}
    B -- yes --> C[enrichMapLocations & setState(mapLocations)]
    C --> D[_onMapViewChangeBubbleEvent -> onMapViewChange(prop clients)]
    C --> E[generateAndRedrawGroups]
    E --> F[computeMarkersGroupings(getVisibleLocations())]
    F --> G[clearDrawnGroups & clearDrawnLocations]
    G --> H[drawGroups(groups)]
    H --> I{for each group}
    I -->|single location| J[addLocationMarker]
    I -->|sameLocationGroup| K[addLocationMarker with number]
    I -->|multiple| L[addGroupMarker]
    click L callback --> M[onMarkerGroupClick]
    click J callback --> N[onMarkerLocationClick]
    M --> O{group size <= 50?}
    O -- yes --> P[groups.ungroup(id) & clearMapMarkers & zoomLocations]
    O -- no --> Q[setMapCenter & setMapZoom(+1) & generateAndRedrawGroups]
    N --> R[showPopupForLocation]
```

> SVG rendering failed for this diagram.
