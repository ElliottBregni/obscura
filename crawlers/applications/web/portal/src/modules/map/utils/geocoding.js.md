# Diagram: web/portal/src/modules/map/utils/geocoding.js


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
flowchart LR
  A[geocode(mapPlatform, mapTypeOverride, hereMapsPlatform, googleMaps, params)] --> B{mapType}
  B -->|HERE| C[hereMapsPlatform.getSearchService().geocode(q=address, limit=1)]
  C --> D{result.items[0].position exists?}
  D -->|yes| E[resultCallback({lat, lng})]
  D -->|no| F[resultCallback({})]
  B -->|GOOGLE| G[new googleMaps.Geocoder().geocode(address)]
  G --> H{status === "OK"}
  H -->|yes| I{results.length > 0}
  I -->|yes| J[resultCallback({lat, lng})]
  I -->|no| F
  H -->|no| K[errorCallback(results)]
  subgraph reverseGeocodeFlow
    R1[reverseGeocode(mapPlatform, mapTypeOverride, hereMapsPlatform, googleMaps, lat, lng)] --> R2{mapType}
    R2 -->|HERE| R3[hereMapsPlatform.getSearchService().reverseGeocode(in=circle:lat,lng;r=150, limit=1)]
    R3 --> R4{response.items[0].address exists?}
    R4 -->|no| R5[resultCallback(null)]
    R4 -->|yes| R6[resultCallback({City, Country, County, District, Label, PostalCode, State})]
    R2 -->|GOOGLE| R7[new googleMaps.LatLng(lat,lng); new googleMaps.Geocoder().geocode(location=latLng)]
    R7 --> R8{status === "OK"}
    R8 -->|no| R9[errorCallback(response)]
    R8 -->|yes| R10{response[0] exists?}
    R10 -->|no| R5
    R10 -->|yes| R11[reduce address_components into addressCombined starting from DEFAULT_GEOCODING + Label]
    R11 --> R6
  end
```

> SVG rendering failed for this diagram.
