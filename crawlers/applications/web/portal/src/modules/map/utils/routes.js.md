# Diagram: web/portal/src/modules/map/utils/routes.js


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
flowchart TD
  A[calculateRoute(mapPlatform, mapTypeOverride, hereMaps, hereMapsPlatform, googleMaps, numWaypoints, params, resultCallback, errorCallback)] --> B{Determine mapType}
  B -->|mapTypeOverride?.toUpperCase() or mapPlatform?.toUpperCase()| C{mapType == "HERE"}
  C -->|yes| H1[params["origin"] = params["waypoint0"]]
  H1 --> H2[params["destination"] = params["waypoint{numWaypoints-1}"]]
  H2 --> H3{numWaypoints > 2}
  H3 -->|yes| H4[Collect intermediate waypoints -> intermediateVia array]
  H3 -->|no| H5[intermediateVia = []]
  H4 --> H6{intermediateVia.length > 0}
  H5 --> H6
  H6 -->|yes| H7[params["via"] = new hereMaps.service.Url.MultiValueQueryParameter(intermediateVia)]
  H6 -->|no| H8[skip via param]
  H7 --> H9[for i in 0..numWaypoints-1: delete params["waypoint{i}"]]
  H8 --> H9
  H9 --> H10[router = hereMapsPlatform.getRoutingService(null, 8)]
  H10 --> H11[router.calculateRoute(params, resultCallback, errorCallback)]
  C -->|no| G{mapType == "GOOGLE"}
  G -->|yes| G1[origin = params["waypoint0"]]
  G1 --> G2[destination = params["waypoint{numWaypoints-1}"]]
  G2 --> G3[Initialize waypoints = []]
  G3 --> G4[for i in 0..numWaypoints-1: if i==0 or i==numWaypoints-1 skip]
  G4 --> G5[parse waypoint -> lat,lng -> waypoints.push({location: new googleMaps.LatLng(lat,lng)})]
  G5 --> G6[directionsService = new googleMaps.DirectionsService()]
  G6 --> G7[directionsService.route({origin, destination, waypoints, travelMode: "DRIVING"}, callback)]
  G7 --> G8{callback status === "OK"}
  G8 -->|yes| G9[resultCallback(response)]
  G8 -->|no| G10[errorCallback(response)]
  C -->|neither| Z[No-op / mapType null]
```

> SVG rendering failed for this diagram.
