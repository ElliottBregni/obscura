# Diagram: web/portal/src/modules/map/utils/RoutingMapUtils.js


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
flowchart TD
  HASH_PRECISION["HASH_PRECISION = 4"]
  hashNum["hashNum(numstring)"]
  stopPositionStringify["stopPositionStringify(stop)"]
  shipmentUpdateStringify["shipmentUpdateStringify(pos)"]
  stopHasLocation["stopHasLocation(stop)"]
  getStopLatLong["getStopLatLong(stop)"]
  updateHasLocation["updateHasLocation(update)"]
  getUpdateLatLong["getUpdateLatLong(update)"]
  getStopModes["getStopModes(stops, index, defaultMode)"]
  getPositionsForShipments["getPositionsForShipments(shipmentHash)"]
  getPositionsForMilestones["getPositionsForMilestones(coordinates)"]
  consoleLog["console.log(\"Stop without location details:\", stop)"]

  HASH_PRECISION -->|used by| hashNum
  stopPositionStringify -->|reads| stopHasLocation
  shipmentUpdateStringify -->|formats| getUpdateLatLong

  stopHasLocation -->|guards| getStopLatLong
  getStopLatLong -->|returns lat/lng| getPositionsForShipments
  stopHasLocation -->|checked in| getPositionsForShipments
  getPositionsForShipments -->|pushes positions| consoleLog
  getPositionsForShipments -->|returns| "Positions[]"

  updateHasLocation -->|guards| getUpdateLatLong
  updateHasLocation -->|used in mapping| getPositionsForMilestones
  getPositionsForMilestones -->|maps coordinates to| "milestoneCoordinates[]"
  getPositionsForMilestones -->|uses| updateHasLocation

  getStopModes -->|reads stops array| getPositionsForShipments
  getStopModes -->|produces| "stopMode / nextStopMode"
```

> SVG rendering failed for this diagram.
