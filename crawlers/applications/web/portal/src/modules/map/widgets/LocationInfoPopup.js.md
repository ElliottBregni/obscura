# Diagram: web/portal/src/modules/map/widgets/LocationInfoPopup.js


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
classDiagram
    class LocationInfoPopup {
        +props: data, lad, type, eventHandler, isLatestUpdate
        +renderLocationContent()
        +renderMilestoneContent()
        +renderConnectedCarContent()
    }
    class Chiclet
    class LadMilestoneChiclet
    class LinkButton
    class FlexDiv
    class FlexColDiv
    class DateTime {
        <<atom>>
        +Time
        +Timezone
    }
    class Text {
        <<atom>>
    }
    class Colors {
        +comments
    }
    class MapPreference
    class formattedStartCase()
    class getCurrentVehicleLocation()

    LocationInfoPopup --> Chiclet : uses
    LocationInfoPopup --> LadMilestoneChiclet : uses
    LocationInfoPopup --> LinkButton : uses
    LocationInfoPopup --> FlexDiv : uses
    LocationInfoPopup --> FlexColDiv : uses
    LocationInfoPopup --> DateTime : uses
    LocationInfoPopup --> Text : uses
    LocationInfoPopup --> Colors : uses
    LocationInfoPopup --> MapPreference : checks
    LocationInfoPopup --> formattedStartCase : calls
    LocationInfoPopup --> getCurrentVehicleLocation : calls

    Chiclet <|-- LadMilestoneChiclet
    DateTime "1" o-- "2" DateTime.Time : contains
    DateTime "1" o-- "2" DateTime.Timezone : contains
```

> SVG rendering failed for this diagram.
