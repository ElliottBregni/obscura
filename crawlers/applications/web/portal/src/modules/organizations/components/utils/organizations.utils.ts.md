# Diagram: web/portal/src/modules/organizations/components/utils/organizations.utils.ts


> Auto-generated by Obscura crawlers

## Diagram 1

```mermaid
classDiagram
    class useGrantedFeaturesOptions {
        +t: function
        +useMemo(): Array<Option>
    }
    class useManageableFeaturesOptions {
        +t: function
        +manageableFeatureOptions: Array<Option>
        +selectedManageableFeatures: Array<Option>|undefined
        +useMemo(): {manageableFeatureOptions, selectedManageableFeatures}
    }
    class useTranslation {
        +useTranslation(namespaces): {t}
    }
    class useMemo {
        +useMemo(fn, deps)
    }
    class Option {
        +label: string
        +value: string
    }
    useGrantedFeaturesOptions --> useTranslation : calls
    useGrantedFeaturesOptions --> useMemo : uses
    useManageableFeaturesOptions --> useTranslation : calls
    useManageableFeaturesOptions --> useMemo : uses
    useManageableFeaturesOptions --> Option : produces
    useGrantedFeaturesOptions --> Option : produces
```

> SVG rendering failed for this diagram.

## Diagram 2

```mermaid
flowchart TD
    A[Start: call useManageableFeaturesOptions(features, isCarrier, isPartner, isDealer)] --> B{isDealer?}
    B -- Yes --> C[dealerOptions list]
    B -- No --> D{isCarrier?}
    D -- Yes --> E[carrierOptions list]
    D -- No --> F{isPartner?}
    F -- Yes --> G[partnerOptions list]
    F -- No --> H[[] empty list]
    C --> I[manageableFeatureOptions]
    E --> I
    G --> I
    H --> I
    I --> J{features provided and non-empty?}
    J -- No --> K[selectedManageableFeatures = undefined]
    J -- Yes --> L[filter manageableFeatureOptions by features includes(option.value)]
    L --> M{filtered.length > 0?}
    M -- Yes --> N[selectedManageableFeatures = filtered]
    M -- No --> K
    K --> O[Return {manageableFeatureOptions, selectedManageableFeatures}]
    N --> O
```

> SVG rendering failed for this diagram.
