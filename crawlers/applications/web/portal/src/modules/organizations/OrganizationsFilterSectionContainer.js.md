# Diagram: web/portal/src/modules/organizations/OrganizationsFilterSectionContainer.js


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
flowchart LR
  subgraph Imports
    A1[OrganizationsSearchBarState]
    A2[FILTERS]
    A3[getAuthorization]
    A4[FilterSection (components/search-bar/FilterSection)]
  end

  subgraph State_and_Actions
    S1[selectors.getSearchFilters]
    S2[selectors.getHasSearchCriteriaChanged]
    AC[actionCreators:{ setSearchFilter, clearSearchFilter, clearSavedSearch, searchEntities }]
  end

  A1 --> S1
  A1 --> S2
  A1 --> AC
  A3 --> MapState

  MapState[mapStateToProps(state) ]
  S1 --> MapState
  S2 --> MapState
  A3 --> MapState
  MapState --> Connect

  AC --> ActionCreatorsObj[actionCreators object]
  ActionCreatorsObj --> Connect

  MergeProps[mergeProps(stateProps, dispatchProps, ownProps)]
  A2 --> MergeProps
  MapState --> MergeProps
  ActionCreatorsObj --> MergeProps
  MergeProps --> Connect

  Connect[connect(mapStateToProps, actionCreators, mergeProps)]
  Connect --> FilterComp[FilterSection]
  FilterComp --> Export[export default FilterSection]
```

> SVG rendering failed for this diagram.
