# Diagram: web/portal/src/modules/organizations/OrganizationsSearchBarState.js


> Auto-generated by Obscura crawlers

## Diagram 1

```mermaid
flowchart LR
    A[Caller] --> B[fetchSearch(queryString, solutionId, duck, dispatch)]
    B --> C[build URL: ORGANIZATIONS_URL + queryString + paginate/locationCount/allOrgs]
    B --> D[config.headers = { \"x-time-zone\": moment.tz.guess() }]
    B --> E[transformResponse(response) -> { meta, data: response.response }]
    B --> F[dispatch( duck.fetch(url, config, transformResponse) )]
    F --> G[duck.fetch -> HTTP GET to ORGANIZATIONS_URL]
    G --> H[API: /iam/organizations]
    H --> I[Response] --> E
```

> SVG rendering failed for this diagram.

## Diagram 2

```mermaid
classDiagram
    class Module {
        +const STORE_MOUNT_POINT: "organizationSearch"
        +const ORGANIZATIONS_URL: apiUrl("/iam/organizations")
        +function fetchSearch(queryString="", solutionId, duck, dispatch)
        +default export: buildSearchBarState(...)
    }
    class Imports {
        +moment
        +apiUrl
        +buildSearchBarState
        +SEARCH_CATEGORIES
        +FILTERS
    }
    Module --> Imports
    Module ..> "buildSearchBarState(...)": uses
    Module : defaultSort = "org_name"
    Module : reverseSort = false
```

> SVG rendering failed for this diagram.
