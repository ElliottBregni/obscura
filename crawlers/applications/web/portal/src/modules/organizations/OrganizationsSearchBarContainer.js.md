# Diagram: web/portal/src/modules/organizations/OrganizationsSearchBarContainer.js


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
flowchart LR
  A[File: this module] -->|import| B(OrganizationsSearchBarState)
  A -->|import| C[SearchBar (components/search-bar/SearchBar)]
  B --> D{selectors}
  D --> E[getSearchText(state)]
  D --> F[getSearchCategory(state)]
  D --> G[getTypeaheadOptionsMetadata(state)]
  D --> H[getHasSearchCriteriaChanged(state)]
  B --> I{actionCreators}
  I --> J[setSearchText]
  I --> K[clearSearchText]
  I --> L[resetSearchBar]
  I --> M[setSearchCategoryForKey]
  I --> N[searchEntities]
  I --> O[clearSavedSearch]
  A --> P[mapStateToProps]
  P -->|uses selectors| E
  P -->|uses selectors| F
  P -->|uses selectors| G
  P -->|uses selectors| H
  P --> Q{returns props}
  Q --> R[searchText]
  Q --> S[searchCategory]
  Q --> T[typeaheadOptionsMetadata]
  Q --> U[hasSearchCriteriaChanged]
  R --- E
  S --- F
  T --- G
  U --- H
  A --> V[actionCreators object]
  V --> J
  V --> K
  V --> L
  V -->|setSearchCategory:| M
  V --> N
  V --> O
  A -->|connect(mapStateToProps, actionCreators)(SearchBar)| W[Default export: ConnectedSearchBar]
  W --> C
```

> SVG rendering failed for this diagram.
