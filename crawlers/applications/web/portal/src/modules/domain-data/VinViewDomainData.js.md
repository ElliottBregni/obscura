# Diagram: web/portal/src/modules/domain-data/VinViewDomainData.js


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
classDiagram
    class VinViewState {
        +mountPoint
        +actionCreators
        +selectors
        +reducer
    }
    class Ducks {
        +vinViewDomainDuck
        +vinViewProductTypeDuck
        +vinViewPositionTypesDuck
        +vinViewOrderTypeDuck
        +vinViewOrderNumberDuck
        +vinViewExceptionTypesDuck
        +vinViewOpenSearchLifecyleStatesDuck
        +vinViewOpenSearchProductTypeDuck
        +vinViewOpenSearchPositionTypesDuck
        +vinViewOpenSearchOrderTypeDuck
        +vinViewOpenSearchOrderNumberDuck
        +vinViewOpenSearchExceptionTypesDuck
        +vinViewOpenCarriersDuck
    }
    class ActionCreators {
        +fetchDomainData()
        +fetchVinViewSystemConfigForOpenSearch()
    }
    class Selectors {
        +getExceptionTypes()
        +selectExceptionTypesAlphabetically()
        +getProductTypes()
        +getLifeCycleStates()
        +getVINs()
        +getOrderTypes()
        +getOrderNumbers()
        +getCarriers()
        +getDomainDataLoading()
        +getCurrentPositionTypes()
        +getOpenSearchLifeCycleStates()
        +getOpenSearchProductTypes()
        +getOpenSearchCurrentPositionTypes()
        +getOpenSearchOrderTypes()
        +getOpenSearchOrderNumbers()
        +getOpenSearchExceptionTypes()
        +getOpenSearchCarriers()
        +getIsVinViewOpenSearchConfigured()
        +getIsVinViewOpenSearchisLoading()
    }
    class URLs {
        +currentPositionCodesUrl(dealerOrgId)
        +productTypesUrl(dealerOrgId)
        +locationsUrl(dealerOrgId)
        +lastMilestoneUrl(dealerOrgId)
        +soldToUrl(dealerOrgId)
        +finCodeUrl(dealerOrgId)
        +customerprovidedExteriorColorUrl(dealerOrgId)
        +customerprovidedInteriorColorUrl(dealerOrgId)
        +customerprovidedDoorTypeUrl(dealerOrgId)
        +customerprovidedPowertrainUrl(dealerOrgId)
        +endUserFinCodeUrl(dealerOrgId)
        +originUrl(dealerOrgId)
        +externalStateUrl(dealerOrg)
        +getFilterURL(dealerOrgFvId, filterName)
    }
    class Helpers {
        +buildFetchDuck(name)
        +createSelectorFromProperty(property,parent,sort)
        +axiosConfigHeaders()
        +apiUrl(path)
        +customerApiUrl(path)
        +isDealer(org)
        +isShipper(org)
    }
    class DomainFetchers {
        +fetchDomainDataFilters(dispatch,dealerOrgId)
        +fetchDomainDataFiltersOpenSearch(dispatch,dealerOrgFvId)
        +fetchDealerOrganizations(query,loadedResults)
    }
    VinViewState --> Ducks : uses
    VinViewState --> ActionCreators : exposes
    VinViewState --> Selectors : exposes
    VinViewState --> "vinviewSystemConfigReducer" : includes
    ActionCreators --> DomainFetchers : calls
    DomainFetchers --> Ducks : dispatches.fetch()
    DomainFetchers --> URLs : uses apiUrl/getFilterURL
    DomainFetchers --> Helpers : uses axiosConfigHeaders,moment,buildFetchDuck
    Selectors --> Helpers : uses createSelectorFromProperty, alfBy
    URLs --> Helpers : uses apiUrl, customerApiUrl
    Ducks ..> Helpers : created by buildFetchDuck
    fetchVinViewSystemConfigForOpenSearch --> URLs : builds system configuration URL
    Helpers ..> "moment.tz.guess()" : uses
    class vinviewSystemConfigReducer {
        +isLoading
        +vinviewFeatureFlagConfig
        +reducer(action)
    }
    VinViewState --> vinviewSystemConfigReducer : reducer includes
```

> SVG rendering failed for this diagram.
