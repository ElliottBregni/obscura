# Diagram: web/portal/src/modules/domain-data/ShipmentsDomainDataState.js


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
classDiagram
class Module {
  +STORE_MOUNT_POINT
  +TRAILER_EQUIPMENT_NUMBERS_URL
  +SERVICE_CODE_URL
  +... (many URL constants)
  +SHIPMENT_EVENTS_FILTER_OPTION_NAMES
  +fetchDomainData()
  +selectors
  +reducer
}
class Ducks {
  +exceptionTypeDuck
  +statusTypeDuck
  +shipmentIDDuck
  +routeIDDuck
  +shipmentTypesDuck
  +orderNumbersDuck
  +tripPlanDuck
  +assetIdsDuck
  +trainIdsDuck
  +lineOfBusinessDuck
  +serviceCodesDuck
  +buildFetchDuck(name)
}
class fetchDomainData {
  +dispatch(fetch(url))
}
class Selectors {
  +selectShipmentModesAlphabetically
  +getExceptionTypes
  +selectExceptionTypesAlphabetically
  +getStatusTypes
  +selectStatusTypesAlphabetically
  +selectShipmentEventsAlphabetically
  +getShipmentIDs
  +getParts
  +getRouteIDs
  +getShipmentTypes
  +getOrderNumbers
  +getTripPlanNumbers
  +getAssetIds
  +getTrainIds
  +getLineOfBusiness
  +getServiceCodes
}
class DomainDataState {
  +selectors.getShipmentModes()
}
class Reducer {
  +combineReducers({...})
  +exceptionTypes
  +statusTypes
  +shipmentIDs
  +routeIDs
  +shipmentTypes
  +orderNumbers
  +tripPlanNumbers
  +assetIds
  +trainIds
  +lineOfBusiness
  +serviceCodes
}

Module --> Ducks : creates
Module --> fetchDomainData : provides
fetchDomainData ..> Ducks : dispatches to
Module --> Selectors : exposes
Selectors --> DomainDataState : composes with
Module --> Reducer : exposes
Reducer --> Ducks : uses reducers from
Ducks --> "api endpoints" : fetch from URLs
Module ..> "EXPORT DEFAULT OBJECT" : default export contains module members
```

> SVG rendering failed for this diagram.
