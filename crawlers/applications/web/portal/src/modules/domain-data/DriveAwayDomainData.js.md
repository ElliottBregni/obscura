# Diagram: web/portal/src/modules/domain-data/DriveAwayDomainData.js


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
classDiagram
    class DriveAwayModule {
        +locationsUrl(solutionId)
        +ddaDealerRegionUrl(solutionId)
        +ddaDealerZoneUrl(solutionId)
        +ddaDealerDistrictUrl(solutionId)
        +customersUrl(solutionId)
        +dealersUrl(solutionId, state)
        +statusUrl()
        +ddaProductTypeUrl(solutionId, state)
        +lastMilestoneUrl(solutionId)
        +destinationUrl()
        +fetchDomainData(solutionId)
        +createSelectorFromProperty(property,parent,sort)
        +getStatusStates(state)
        +getCarriers(state)
        +getDomainDataLoading(state)
        -STORE_MOUNT_POINT = "driveAwayDomainData"
    }

    class buildFetchDuck <<library>> {
        +buildFetchDuck(name)
    }

    class driveAwayDomainDuck {
        +fetch(url, options)
        +reducer
    }

    class combineReducers <<library>> {
        +combineReducers(map)
    }

    class apiUrl <<library>> {
        +apiUrl(path)
    }

    class isShipper <<library>> {
        +isShipper(activeOrganization)
    }

    class lodash__ <<library>> {
        +isString(x)
        +isObject(x)
        +sortBy(arr, key)
    }

    DriveAwayModule --> apiUrl : calls
    DriveAwayModule --> isShipper : checks role
    DriveAwayModule --> lodash__ : uses for sorting checks
    DriveAwayModule --> buildFetchDuck : constructs driveAwayDomainDuck
    buildFetchDuck --> driveAwayDomainDuck : returns
    DriveAwayModule --> driveAwayDomainDuck : dispatches fetch(url, params)
    DriveAwayModule --> combineReducers : composes reducer
    combineReducers --> driveAwayDomainDuck : uses reducer as domainData
    driveAwayDomainDuck --> "domainData slice" : provides reducer and fetch behavior
    DriveAwayModule : selectors read from STORE_MOUNT_POINT -> domainData.data[...]
```

> SVG rendering failed for this diagram.
