# Diagram: web/portal/src/modules/auth/FederatedUserPreferenceModalState.js


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
flowchart LR
  CONST_STORE[STORE_MOUNT_POINT = "federatedPreferenceModal"]
  CONST_ACTION[UPDATE_POPUP_SHOW = "${STORE_MOUNT_POINT}/UPDATE_POPUP_SHOW"]
  A[setPopupShow(show)\n(action creator)]
  B[dispatch({ type: UPDATE_POPUP_SHOW,\n payload: { show } })]
  R[PreferenceModalReducer(state, action)\ninitialState: { popupShow: false }]
  S[getPopupShow(state)\n→ state.federatedPreferenceModal?.popupShow ?? false]
  STATE_NODE[state.federatedPreferenceModal\n{ popupShow }]
  MODULE[FederatedUserPreferenceModalState\n(mountPoint, actionCreators, selectors, reducer)]

  CONST_STORE --> CONST_ACTION
  A --> B
  B --> R
  R --> STATE_NODE
  STATE_NODE --> S
  CONST_STORE --> MODULE
  A --> MODULE
  S --> MODULE
  R --> MODULE
```

> SVG rendering failed for this diagram.
