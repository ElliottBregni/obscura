# Diagram: web/portal/src/modules/auth/FederatedUserPreferenceUpdateModalContainer.js


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
graph LR
  Component[FederatedUserEmailUpdateModal]
  Connect[connect(mapStateToProps, actionCreators, mergeProps)]
  MapState[mapStateToProps]
  Actions[actionCreators]
  MergeProps[mergeProps]
  OwnProps[ownProps]

  StateSlice1[FederatedUserPreferenceModalState]
  StateSlice2[ProfileState]

  Selectors1[getPopupShow(state)]
  Selectors2[getUserPreferences(state)]

  ActionCreator1[setPopupShow(show)]
  ActionCreator2[setPreferences(preferences)]

  StateSlice1 -->|provides selectors| Selectors1
  StateSlice2 -->|provides selectors| Selectors2
  Selectors1 -->|used by| MapState
  Selectors2 -->|used by| MapState

  StateSlice1 -->|provides actionCreators| ActionCreator1
  StateSlice2 -->|provides actionCreators| ActionCreator2
  ActionCreator1 -->|included in| Actions
  ActionCreator2 -->|included in| Actions

  MapState -->|mapped into| Connect
  Actions -->|bound into| Connect
  MergeProps -->|merge function| Connect

  MapState -->|inputs| MergeProps
  Actions -->|inputs (dispatchProps)| MergeProps
  OwnProps -->|inputs| MergeProps
  MergeProps -->|returns props to| Connect

  Connect -->|wraps| Component
```

> SVG rendering failed for this diagram.
