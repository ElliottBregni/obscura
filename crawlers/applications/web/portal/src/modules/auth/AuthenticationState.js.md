# Diagram: web/portal/src/modules/auth/AuthenticationState.js


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
classDiagram
class Module {
  +mountPoint = "auth"
  +actionCreators
  +selectors
  +reducer
}
class Constants <<constant>> {
  +STORE_MOUNT_POINT = "auth"
  +LOGIN_LOADING = "auth/LOGIN_LOADING"
}
class ActionCreator <<function>> {
  +setIsLoginLoading(isLoginLoading)
  +dispatch({ type: LOGIN_LOADING, payload })
}
class Selector <<function>> {
  +getIsLoginLoading(state)
}
class Reducer <<function>> {
  +AuthReducer(state, action)
  +initialState = { isLoginLoading: false }
}
class State {
  +isLoginLoading: boolean = false
}

Module --> Constants
Module --> ActionCreator
Module --> Selector
Module --> Reducer

ActionCreator ..> Constants : uses
ActionCreator ..> Reducer : dispatches action
Reducer --> State : returns updated state
Selector ..> State : reads isLoginLoading
```

> SVG rendering failed for this diagram.
