# Diagram: web/portal/src/modules/auth/authentication.js


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
classDiagram
class Authentication {
  - _federatedRedirectOverrideState
  + constructor()
  + handleAuthentication(code, orgId)
  + handleFederatedRedirectState()
  + handleSamlAuthentication()
  + isAuthenticated()
  + isLoggingIn()
  + login(orgId)
  + logout(isManual=false)
  + setSession(authResult)
  + setLastRequestedUrl(url)
  + setIsLoggingIn(flag)
  + setIsSilentAuthentication(flag)
  + setHasRequestedFederatedUserEmail(flag)
  + getIsSilentAuthentication()
  + getLastRequestedUrl()
  + getIdToken()
  + getAccessToken()
  + getExpiresAt()
  + getHasRequestedFederatedUserEmail()
  + getDecodedToken()
  + getDecodedIdToken()
  + getEnvAuth0BaseUri()
  + getEnvAuth0ClientId()
  + getEnvRedirectUri(orgId=null)
  + getAuthHeaders()
  + getUserProfile()
  + getOrgIdQs(orgId)
  + getAuthorizeQueryString(options, orgId)
  + installAuthInterceptor()
  + userProfile
  + userId
  + authenticationStrategy
  + userEmail
  + userUsername
  + userPicture
  + userMixpanelPeopleProperties
  + getFederatedRedirectOverrideState()
}
class ConnectionStrategy {
  <<enumeration>>
  AUTH0
  SAML
  OIDC
}
class BrowserStorage {
  + accessToken
  + idToken
  + expiresAt
  + isLoggingIn
  + currentOrganization
  + isSilentAuthentication
  + solutionId
  + federatedState
  + federationData
  + locationTimezone
  + hasRequestedFederatedUserEmail
  + userRoleOverride
}
class ApplicationConfig {
  + IS_PUBLIC
  + ENVIRONMENT
}
class Axios {
  + create()
  + post(url, params)
  + interceptors
}
class MixpanelUtils {
  + identify(userId, props)
}
class apiUrl {
  + (path)
}
class decodeJsonWebToken {
  + (token)
}
class UserAuthorizationNamespace {
}
Authentication --> BrowserStorage : reads/writes
Authentication --> ApplicationConfig : reads
Authentication ..> Axios : makes requests
Authentication --> MixpanelUtils : identify()
Authentication ..> apiUrl : posts to /iam/login-audit
Authentication ..> decodeJsonWebToken : decodes tokens
Authentication ..> UserAuthorizationNamespace : reads profile namespace
ConnectionStrategy <|-- Authentication : uses
note right of Authentication : AuthenticationUtils is a single exported instance
object AuthenticationUtils {
  <<instance>>
}
```

> SVG rendering failed for this diagram.
