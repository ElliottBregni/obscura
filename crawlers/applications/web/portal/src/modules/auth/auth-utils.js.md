# Diagram: web/portal/src/modules/auth/auth-utils.js


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
flowchart LR
  I[import jwtDecode from "jwt-decode"] --> A[decodeJsonWebToken(jwtString)]
  A --> B{Try to decode}
  B --> C[jwtDecode(jwtString)]
  C --> D[decoded = jwtDecode result]
  B -->|throws| E[decoded = null]
  D --> F[return decoded]
  E --> F
```

> SVG rendering failed for this diagram.
