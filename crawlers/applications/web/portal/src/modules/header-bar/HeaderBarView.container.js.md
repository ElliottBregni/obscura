# Diagram: web/portal/src/modules/header-bar/HeaderBarView.container.js


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
graph TD
  subgraph ReactRedux
    CONNECT[connect]
  end

  subgraph Components
    HeaderBar[HeaderBar]
  end

  subgraph StateModules
    Orgs["modules/organizations/OrganizationsState"]
    Auth["modules/auth/AuthorizationSelectors"]
    MapState["modules/map/MapState"]
    TitleState["shared/redux/TitleState"]
    HeaderSearch["./HeaderSearchBarState"]
  end

  subgraph Selectors
    getActiveOrganization[getActiveOrganization()]
    getCurrentOrganizationId[getCurrentOrganizationId()]
    getOrganizationImageConfig[getOrganizationImageConfig()]
    getAuthorization[getAuthorization()]
    Map_getMapTypeOverride[getMapTypeOverride()]
    Title_getTitle[getTitle()]
    Title_getSubTitle[getSubTitle()]
    Title_getDescription[getDescription()]
    Title_getOpenSearchVis[getOpenSearchToggleSwitchVisibility()]
    Title_getOpenSearchState[getOpenSearchToggleState()]
    HeaderSearch_getResults[getSearchResults()]
  end

  subgraph ActionCreators
    setCurrentOrganization[setCurrentOrganization(id)]
    setActiveOrganization[setActiveOrganization(org)]
    Map_setMapTypeOverride[MapState.setMapTypeOverride(mapType)]
    HeaderSearch_searchEntities[HeaderSearchBarState.searchEntities()]
    Title_setToggleState[TitleState.setToggleSwitchState(flag)]
  end

  %% imports to selectors
  Orgs --> getActiveOrganization
  Orgs --> getCurrentOrganizationId
  Orgs --> getOrganizationImageConfig
  Auth --> getAuthorization
  MapState --> Map_getMapTypeOverride
  TitleState --> Title_getTitle
  TitleState --> Title_getSubTitle
  TitleState --> Title_getDescription
  TitleState --> Title_getOpenSearchVis
  TitleState --> Title_getOpenSearchState
  HeaderSearch --> HeaderSearch_getResults

  %% action creators source
  Orgs --> setCurrentOrganization
  Orgs --> setActiveOrganization
  MapState --> Map_setMapTypeOverride
  HeaderSearch --> HeaderSearch_searchEntities
  TitleState --> Title_setToggleState

  %% mapStateToProps collects selectors
  mapStateToProps[mapStateToProps(state)]
  getActiveOrganization --> mapStateToProps
  getCurrentOrganizationId --> mapStateToProps
  getOrganizationImageConfig --> mapStateToProps
  getAuthorization --> mapStateToProps
  Map_getMapTypeOverride --> mapStateToProps
  Title_getTitle --> mapStateToProps
  Title_getSubTitle --> mapStateToProps
  Title_getDescription --> mapStateToProps
  Title_getOpenSearchVis --> mapStateToProps
  Title_getOpenSearchState --> mapStateToProps
  HeaderSearch_getResults --> mapStateToProps

  %% mapDispatchToProps collects actions
  mapDispatchToProps[mapDispatchToProps(dispatch)]
  setCurrentOrganization --> mapDispatchToProps
  setActiveOrganization --> mapDispatchToProps
  Map_setMapTypeOverride --> mapDispatchToProps
  HeaderSearch_searchEntities --> mapDispatchToProps
  Title_setToggleState --> mapDispatchToProps

  %% connect ties them to HeaderBar
  CONNECT --> mapStateToProps
  CONNECT --> mapDispatchToProps
  CONNECT --> HeaderBar
  mapStateToProps --> CONNECT
  mapDispatchToProps --> CONNECT
  HeaderBar -->|uses props| mapStateToProps
  HeaderBar -->|dispatches| mapDispatchToProps
```

> SVG rendering failed for this diagram.
