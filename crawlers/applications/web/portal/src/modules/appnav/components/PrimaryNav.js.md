# Diagram: web/portal/src/modules/appnav/components/PrimaryNav.js


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
flowchart LR
  Props[Props: sidebarMinimize, currentLocation] --> PrimaryNav[PrimaryNav Component]
  PrimaryNav --> Selectors{useSelector}
  Selectors --> Authorization[getAuthorization -> authorization]
  Selectors --> FeatureData[getFeatureData -> featureData]
  Selectors --> ActiveOrg[getActiveOrganization -> activeOrganization]
  Selectors --> ClosestRoute[getClosestVisibleRouteMapEntry -> closestRouteMapEntry]
  PrimaryNav --> State[useState: openGroups]
  PrimaryNav --> Effect[useEffect (on featureData)]
  Effect --> StorageOpen[getNavBarOpenGroupsFromStorage()]
  Effect --> RouteGroups[privateRouteGroupsMap entries]
  RouteGroups --> DefaultOpenCalc[getShouldSectionBeOpenByDefault(defaultOpen, authorization)]
  DefaultOpenCalc --> OpenByDefault[openGroupsByDefault]
  RouteGroups --> OpenByLocation[filter by currentLocation => openGroupsByCurrentLocation]
  StorageOpen --> MergeOpen[_.uniq([...storage, ...openByDefault, ...openByLocation])]
  MergeOpen --> State
  PrimaryNav --> GroupsRender[map over privateRouteGroupsMap]
  GroupsRender --> SkipHidden{navGroup.hidden?}
  SkipHidden -- true --> Null1[null]
  SkipHidden -- false --> GroupData[groupData(authorization, activeOrganization, navGroup)]
  GroupData --> Links[getNavLinks(links, groupKey, closestRouteMapEntry, authorization)]
  Links --> NavLinkElems[NavLink elements]
  GroupData --> IsAuthorized[authorization.isAuthorized(..., true)]
  IsAuthorized -- false --> Null2[null]
  IsAuthorized -- true --> RenderNavGroup[Render NavGroup]
  RenderNavGroup --> NavGroupComponent[NavGroup: receives groupKey, isOpen, toggleOpenGroup, sidebarMinimize]
  NavGroupComponent --> NavLinkElems
  PrimaryNav --> DocumentationNavComponent[DocumentationNav]
  DocumentationNavComponent --> DocIsOpen[isOpen = openGroups includes DOCUMENTATION_GROUP_KEY]
  PrimaryNav --> ToggleFunc[toggleOpenGroup(groupKey)]
  ToggleFunc --> UpdateLocal[update openGroups state]
  ToggleFunc --> UpdateStorage[setNavBarOpenGroupsInStorage(newOpenGroups)]
  getNavLinksFunc[getNavLinks function] --> NavLinkConstruction[creates NavLink with compositeKey, actionType, isActiveScreen, auth]
  getShouldSection[getShouldSectionBeOpenByDefault] --> AuthorizationChecks[authorization.isAuthorized for each condition]
  subgraph ExternalModules
    lodash[_ (lodash)]
    utils_nav[getNavBarOpenGroupsFromStorage / setNavBarOpenGroupsInStorage]
    routes[privateRouteGroupsMap]
    routeSelectors[getClosestVisibleRouteMapEntry]
  end
  PrimaryNav --> ExternalModules
```

> SVG rendering failed for this diagram.
