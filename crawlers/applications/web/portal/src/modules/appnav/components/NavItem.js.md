# Diagram: web/portal/src/modules/appnav/components/NavItem.js


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
classDiagram
    class NavItem {
        +isActiveNavItem: bool = false
        +sidebarMinimize: bool
        +screen: object
        +auth: object
        +translatedTitle: string
        +rolloverElem: JSX|null
        +render(): JSX
    }
    class NavDiv {
        <<styled.div>>
        +padding: "5px 10px 5px 42px"
        +position: "relative"
        +fontSize: "1.02em"
        +color: Colors.text.HIT_GRAY
        +hoverColor: "white"
        +hoverRolloverVisibility: "visible"
        +backgroundColor(props.isActiveNavItem)
    }
    class useTranslation {
        +t(key:string): string
    }
    class translateNavLabelLookup {
        +translateNavLabelLookup(t, actionType, auth): string
    }
    class Colors {
        class background {
            DARK_HIGHLIGHT_GRAY
        }
        class text {
            HIT_GRAY
        }
    }
    class PropTypes {
        +bool
        +shape(obj)
        +object
        +string
    }

    NavItem --> NavDiv : contains
    NavItem ..> useTranslation : calls
    NavItem ..> translateNavLabelLookup : uses
    NavItem ..> Colors : reads
    NavItem ..> PropTypes : validates props
    NavDiv ..> Colors : reads background/text colors
```

> SVG rendering failed for this diagram.
