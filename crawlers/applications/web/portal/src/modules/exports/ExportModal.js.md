# Diagram: web/portal/src/modules/exports/ExportModal.js


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
flowchart LR
  Props[Props: exportIdentifier, exportName, exportType] --> CheckProps{exportIdentifier & exportName?}
  CheckProps -- Yes --> DispatchAdd[dispatch(addExport(...))]
  CheckProps -- No --> HideReset[setShowExportModal = false<br/>resetExport()]
  DispatchAdd --> ShowModal[showExportModal = true<br/>Modal displayed]
  DispatchAdd --> FetchNotif[dispatch(fetchNotification(pageSize=10,refresh=true,page=0))]
  ShowModal --> GetStatus[useSelector(getExport(exportIdentifier))\nexportStatus]
  GetStatus --> IsReady{exportStatus === READY?}
  IsReady -- Yes --> ReadyMsg["Show: 1 hour to download"]
  IsReady -- No --> WaitingMsg["Show: export in progress message"]
  exportError["exportStatus == TIMEOUT or ERROR"] -->|effect| CloseOnError[setShowExportModal = false<br/>resetExport()]
  GetStatus --> exportError
  ReadyAction[Buttons when READY] --> DownloadBtn[Download (enabled)]
  ReadyAction --> MinimizeBtn[Minimize (disabled when READY)]
  ReadyAction --> CancelBtn[Cancel]
  WaitingAction[Buttons when NOT READY] --> DownloadBtnDisabled[Download (disabled)]
  WaitingAction --> MinimizeBtnEnabled[Minimize (enabled)]
  WaitingAction --> CancelBtn
  IsReady -- Yes --> ReadyAction
  IsReady -- No --> WaitingAction
  DownloadBtn --> FetchDownload[dispatch(fetchExportDownload(exportIdentifier, exportType))]
  FetchDownload --> HideResetAfter[setShowExportModal = false<br/>resetExport()]
  MinimizeBtnEnabled --> HideResetAfter
  MinimizeBtn --> HideResetAfter
  CancelBtn --> UpdateCancel[dispatch(updateExport(exportIdentifier, {status: CANCELED}))]
  UpdateCancel --> HideResetAfter
  HideReset --> End[Modal hidden]
  HideResetAfter --> End
  CloseOnError --> End
```

> SVG rendering failed for this diagram.
