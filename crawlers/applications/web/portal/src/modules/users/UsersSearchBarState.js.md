# Diagram: web/portal/src/modules/users/UsersSearchBarState.js


> Auto-generated by Obscura crawlers

## Diagram 1

```mermaid
classDiagram
    class UsersSearchBarState {
        +STORE_MOUNT_POINT: "userSearch"
        +SEARCH_CATEGORIES
        +defaultSort: "email"
        +reverseSort: false
        +actionCreators
    }
    class fetchSearch {
        +(queryString = "", solutionId, duck, dispatch)
    }
    class getOrganizationsUrl {
        +(solutionId, queryString)
    }
    class BrowserStorage {
        +currentOrganization
    }
    class apiUrl {
        +(path)
    }
    class transformResponse {
        +(response)
    }
    UsersSearchBarState --> fetchSearch : uses
    fetchSearch --> getOrganizationsUrl : calls
    getOrganizationsUrl --> BrowserStorage : reads currentOrganization
    getOrganizationsUrl --> apiUrl : builds URL
    fetchSearch --> transformResponse : defines
    fetchSearch --> UsersSearchBarState : dispatches duck.fetch
    UsersSearchBarState o-- "actionCreators" : contains
    UsersSearchBarState : exportSearch() = partial(exportEntities, getOrganizationsUrl, null, headers, "fv-users")
```

> SVG rendering failed for this diagram.

## Diagram 2

```mermaid
flowchart TD
    F[fetchSearch(queryString, solutionId, duck, dispatch)] --> G[getOrganizationsUrl(solutionId, queryString)]
    G --> H[/apiUrl(`/iam/organizations/${organizationId}/members?${queryString}`)/]
    H --> I[return url]
    F --> J[config = { headers: { "x-time-zone": moment.tz.guess() } }]
    F --> K[transformResponse(response) => { meta: response.meta, data: response.response }]
    F --> L[dispatch(duck.fetch(url, config, transformResponse))]
    J --> L
    K --> L
    G -->|reads| M[BrowserStorage.currentOrganization]
    style H stroke:#333,stroke-width:1px
    style L stroke:#0b0,stroke-width:2px,stroke-dasharray: 5 5
```

> SVG rendering failed for this diagram.
