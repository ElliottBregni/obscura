# Diagram: web/portal/src/modules/users/UsersView.js


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
classDiagram
    class UsersView {
        +props
        +state: showModal {modalMode, user}
        +useEffect(): fetchRoles(), fetchOrganizationMembers()
        +onPageChange(newPage)
        +onPageSizeChange(newPageSize)
        +onPageSort(column, isReverse)
        +rowActionHandler(action, val)
    }

    class ModalMode {
        <<enumeration>>
        CREATE
        UPDATE
        DELETE
        BLOCK
        UNBLOCK
        RESET
        IMPORT
        DETAILS
    }

    class Hooks {
        +useSetTitleOnMount()
        +useSetDescriptionOnMount()
        +useTrackWithMixpanelOnce()
    }

    class DataFetchers {
        +fetchRoles()
        +fetchOrganizationMembers()
    }

    class PaginationAndSort {
        +setMembersPagination(orgId, page, pageSize)
        +setMembersSort(orgId, column, isReverse)
    }

    class Exporting {
        +exportEntities()
        +isExporting
        +exportIdentifier
        +exportName
        +resetExport()
        +clearExportErrors()
        +exportFailed
    }

    class UIComponents {
        +Loader
        +ExportAlert
        +DownloadDataButton
        +ExportModal
        +SearchBarContainer
        +Button (Add, Import)
        +UsersTable
        +AddUserModal
        +ImportUsersModal
        +UserActiveModal
        +EditUserModal
        +DeleteUserModal
        +UserPasswordModal
        +DetailsUserModal
    }

    UsersView --> Hooks : uses
    UsersView --> DataFetchers : calls
    UsersView --> PaginationAndSort : calls
    UsersView --> Exporting : uses
    UsersView --> UIComponents : renders
    UsersView --> ModalMode : sets modalMode
    UIComponents "1" o-- "*" UsersView : contains
    UsersView <.. UsersTable : provides data, handlers
    UsersTable --> UsersView : onPageChange/onPageSizeChange/onPageSort
    Exporting --> ExportModal : provides export data
    Button --> UsersView : onClick -> setShowModal(CREATE/IMPORT)
    UsersView ..> DataFetchers : fetch on mount
    UsersView ..> PaginationAndSort : update pagination/sort via callbacks
    ModalMode <|-- UsersView : uses enum values
```

> SVG rendering failed for this diagram.
