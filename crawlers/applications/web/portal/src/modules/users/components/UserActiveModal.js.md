# Diagram: web/portal/src/modules/users/components/UserActiveModal.js


> Auto-generated by Obscura crawlers

## Diagram 1

```mermaid
classDiagram
class UserActiveModal {
  +props: show, hide, user, activeOrganization, actionStatus
  +props: blockUser(), unblockUser(), clearActionStatus()
  -state: updateUser { email, isActive }
  +useEffect(setUserFromProps)
  +shouldShowFailureAlert(actionStatus)
  +shouldShowSuccessAlert(actionStatus)
  +getAlertMessage(actionStatus)
  +onHide()
  +render()
}
class Modal {
  +Header()
  +Body()
  +Footer()
}
class ReadOnlyInput
class FormButton
class Icon
class Alert
class UsersState {
  +actionCreators.blockUser()
  +actionCreators.unblockUser()
  +actionCreators.clearActionStatus()
  +UsersActionStatus
}
class OrganizationsState {
  +getActiveOrganization(state)
}
UserActiveModal *-- Modal
UserActiveModal --> ReadOnlyInput
UserActiveModal --> FormButton
UserActiveModal --> Icon
UserActiveModal --> Alert
UserActiveModal ..> UsersState : uses
UserActiveModal ..> OrganizationsState : uses
UsersState --> UsersActionStatus
```

> SVG rendering failed for this diagram.

## Diagram 2

```mermaid
flowchart LR
  A[Show Modal? (prop: show)] -->|true| B[Modal.Header: title based on updateUser.isActive]
  B --> C[Modal.Body]
  C --> C1[ReadOnlyInput: Organization <- activeOrganization.org_name]
  C --> C2[ReadOnlyInput: Email <- updateUser.email]
  C --> C3{actionStatus present?}
  C3 -->|success| C4[Alert: Success message]
  C3 -->|failure| C5[Alert: Error message]
  C --> D[Modal.Footer]
  D --> D1[FormButton: Cancel/Close -> onHide()]
  D --> D2[FormButton: Mark Active/Inactive -> clickHandler()]
  D2 --> E{updateUser.isActive?}
  E -->|true| F[blockUser(user, activeOrganization.organization_id)]
  E -->|false| G[unblockUser(user, activeOrganization.organization_id)]
  A -->|false| H[Modal hidden]
```

> SVG rendering failed for this diagram.
