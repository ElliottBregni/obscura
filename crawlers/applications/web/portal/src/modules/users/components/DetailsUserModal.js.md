# Diagram: web/portal/src/modules/users/components/DetailsUserModal.js


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
classDiagram
class DetailsUserModal {
  +bool show
  +func hide()
  +object user
  +object activeOrganization
  +func fetchUserDetails(orgId, userId)
  +object userDetailsRequestData
  +render()
}
class Modal
class ReadOnlyInput
class Loader
class parseDateTime {
  +func parse(date, withTime)
}
class getActiveOrganization {
  +func (state)
}
class UsersState {
  class selectors {
    +func getUserDetailsRequestData(state)
  }
  class actionCreators {
    +func fetchUserDetails(orgId, userId)
  }
}
class mapStateToProps {
  +func(state)
}
class mapDispatchToProps {
  +fetchUserDetails
}
class connect {
  +func(mapStateToProps, mapDispatchToProps)(Component)
}

DetailsUserModal --> Modal : uses
DetailsUserModal --> ReadOnlyInput : renders
DetailsUserModal --> Loader : shows loading
DetailsUserModal ..> parseDateTime : calls
mapStateToProps ..> getActiveOrganization : reads
mapStateToProps ..> UsersState.selectors : reads
mapDispatchToProps ..> UsersState.actionCreators : dispatches
connect --> DetailsUserModal : wraps
connect ..> mapStateToProps
connect ..> mapDispatchToProps
```

> SVG rendering failed for this diagram.
