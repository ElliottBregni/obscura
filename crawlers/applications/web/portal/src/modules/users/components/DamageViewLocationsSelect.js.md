# Diagram: web/portal/src/modules/users/components/DamageViewLocationsSelect.js


> Auto-generated by Obscura crawlers

## Diagram 1

```mermaid
flowchart TD
  User["User (browser)"] -->|interacts| AsyncPaginate[AsyncPaginate]
  AsyncPaginate -->|calls loadOptions -> loadLocations| loadLocations[loadLocations(currentVal,pageNumber)]
  loadLocations -->|HTTP GET| Axios[axios.get(url)]
  Axios -->|returns response.data| Transform[map -> {label,value,assignee}]
  Transform -->|returns options| AsyncPaginate
  AsyncPaginate -->|onChange| FilterSelected[filter selected locations by assignee/overrideIds]
  FilterSelected -->|calls| setSelectedLocations[setSelectedLocations([...])]
  CheckboxOption -->|Override button click| SetShow[setShow(true)]
  CheckboxOption -->|Override button click| SetOverrideLocation[setOverrideLocation(data)]
  SetShow --> Modal[Modal (confirmation)]
  SetOverrideLocation --> Modal
  Modal -->|Accept click| onChangeAccept[onChangeAccept()]
  onChangeAccept -->|adds overrideLocation| setOverrideLocations[setOverrideLocations([...])]
  onChangeAccept -->|also adds to selected| setSelectedLocations
  Modal -->|Cancel click| hideHandler[hideHandler() --> setShow(false)]
  subgraph State
    locationInputValue
    overrideLocations
    overrideLocation
    menuIsOpen
    show
    previousUser
    overrideIds
  end
  AsyncPaginate --- State
  CheckboxOption --- State
```

> SVG rendering failed for this diagram.

## Diagram 2

```mermaid
classDiagram
  class DamageViewLocationsSelect {
    +string user
    +array selectedLocations
    +func setSelectedLocations
    +bool disabled
    -string locationInputValue
    -array overrideLocations
    -object overrideLocation
    -bool menuIsOpen
    -bool show
    -string previousUser
    -string overrideLocationName
    -array overrideIds
    +hideHandler()
    +onChangeAccept()
    +loadLocations(currentVal,pageNumber)
    +onMenuOpen()
    +onMenuClose()
    +CheckboxOption(props)
  }
  class AsyncPaginate {
    +loadOptions()
    +onChange()
    +onInputChange()
    +components.Option
  }
  class Axios {
    +get(url)
  }
  class Modal {
    +show
    +ModalBody
    +ModalFooter
  }
  DamageViewLocationsSelect "1" -- "1" AsyncPaginate : uses
  DamageViewLocationsSelect "1" -- "1" Modal : renders
  loadLocations ..> Axios : calls
  DamageViewLocationsSelect o-- "states" : manages
  CheckboxOption --> DamageViewLocationsSelect : calls setShow / setOverrideLocation
  Modal --> DamageViewLocationsSelect : invokes onChangeAccept on accept
```

> SVG rendering failed for this diagram.
