# Diagram: web/portal/src/modules/geofence-edit/geofence-coordinates.js


> Auto-generated by Obscura crawlers

## Diagram 1

```mermaid
flowchart TD
  A[geofenceWithUpdatedCoordinates(geofence, newData)] --> B[getGeofenceType(geofence)]
  B --> |POLYGONAL| C[Return: {...geofence, geometry: {...geometry, coordinates: newData}}]
  B --> |MULTIPOLYGON| D[Return: {...geofence, geometry: newData}]
  B --> |RADIAL| E[Return: {...geofence, geometry: {...geometry, coordinates: newData}}]
  C --> F[END]
  D --> F
  E --> F
```

> SVG rendering failed for this diagram.

## Diagram 2

```mermaid
flowchart TD
  subgraph LatValidation
    L1[isLatValid(lat)]
    L1 --> L2{_.isNumber(lat)?}
    L2 -- No --> LfalseLat[false]
    L2 -- Yes --> L3{_.isNaN(lat)?}
    L3 -- Yes --> LfalseLat
    L3 -- No --> L4{lat > 90 or lat < -90?}
    L4 -- Yes --> LfalseLat
    L4 -- No --> LtrueLat[true]
  end

  subgraph LngValidation
    G1[isLngValid(lng)]
    G1 --> G2{_.isNumber(lng)?}
    G2 -- No --> GfalseLng[false]
    G2 -- Yes --> G3{_.isNaN(lng)?}
    G3 -- Yes --> GfalseLng
    G3 -- No --> G4{lng > 180 or lng < -180?}
    G4 -- Yes --> GfalseLng
    G4 -- No --> GtrueLng[true]
  end

  subgraph CoordValidation
    C1[isCoordValid(lat, lng)]
    C1 --> C2[isLatValid(lat)]
    C1 --> C3[isLngValid(lng)]
    C2 --> C4{result}
    C3 --> C5{result}
    C4 --> C6[AND]
    C5 --> C6
    C6 --> Ctrue[true or false]
  end

  LtrueLat --> C1
  LfalseLat --> C1
  GtrueLng --> C1
  GfalseLng --> C1
```

> SVG rendering failed for this diagram.
