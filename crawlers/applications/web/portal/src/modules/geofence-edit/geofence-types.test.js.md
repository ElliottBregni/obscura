# Diagram: web/portal/src/modules/geofence-edit/geofence-types.test.js


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
classDiagram
class geofence-types {
  +getType(fence)
  +getRadialCenter(fence)
  +isRadialFenceValid(lat,lng,radius)
  +isRadialFenceValidForFenceData(fence)
  +isFenceValid(fence)
  +RADIAL
  +POLYGONAL
}
class radialFenceData {
  +geometry: { type: "Point", coordinates: [lng, lat] }
  +properties: { buffer: radius }
  +annArborRadialData
  +annArborRadialFence
  +radialTestCases
}
class polygonalFenceData {
  +geometry: { type: "Point", coordinates: [[...lngLats]] }
  +properties: { buffer: 0, center: undefined }
  +annArborFencePolygonal
  +zaggedFencePolygonal
  +polygonalTestCases
}
class geofenceMissingCenterPoint {
  +type: "Feature"
  +geometry: { type: "Point", coordinates: [100,42] }
  +properties: { buffer:1609.344, center:{latitude:null, longitude:null} }
}
class Tests {
  +radial fixture validity()
  +radial fence validity()
  +radial fence missing with depopulated center prop()
  +get radial center()
  +get radial center missing centerpoint()
  +polygonal fixture validity()
  +radial to polygonal()
}

geofence-types --> radialFenceData : validates
geofence-types --> polygonalFenceData : validates
Tests --> radialFenceData : constructs/tests
Tests --> polygonalFenceData : constructs/tests
Tests --> geofence-types : calls
geofenceMissingCenterPoint --> geofence-types : validatedBy
radialFenceData --> geofenceMissingCenterPoint : example of missing center
```

> SVG rendering failed for this diagram.
