# Diagram: web/portal/src/modules/geofence-edit/GeofenceEditRadialForm.js


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
flowchart LR
  Data[props.data\n(data.geofence)] --> ExtractCoords[Extract coordinates\n[lng, lat]]
  Data --> Buffer[data.geofence.properties.buffer]
  Buffer --> ConvertMetersToMiles[math.unit(buffer, "m")\n.to("mile")]
  ConvertMetersToMiles --> Round[_.round(..., 5)]
  Round --> SetRadius[state: setRadiusMiles(radiusMiles)]
  SetRadius --> Render[Render FlexRow with inputs]
  Render --> LatInput[NumberInput: Latitude\nvalue=lat]
  Render --> LngInput[NumberInput: Longitude\nvalue=lng]
  Render --> RadiusInput[StandardInput: Radius (mi)\nvalue=radiusMiles]
  LatInput -- onChange --> BuildNewGeofenceLat[geofenceWithUpdatedCoordinates(\ngeofence, [lng, parsedLat])]
  LngInput -- onChange --> BuildNewGeofenceLng[geofenceWithUpdatedCoordinates(\ngeofence, [parsedLng, lat])]
  BuildNewGeofenceLat --> UpdateGeofence[updateGeofence(newGeofence)]
  BuildNewGeofenceLng --> UpdateGeofence
  RadiusInput -- onChange --> SetRadiusLocal[setRadiusMiles(valMiles)]
  RadiusInput -- onChange --> ConvertMilesToMeters[math.unit(valMiles, "mile").to("m")]
  ConvertMilesToMeters --> MergeBuffer[Create newGeofence with\nproperties.buffer = meters]
  MergeBuffer --> UpdateGeofence
  ReadOnly[isReadOnly prop] -.-> LatInput
  ReadOnly -.-> LngInput
  ReadOnly -.-> RadiusInput
  Helper[geofenceWithUpdatedCoordinates]\n-- used by --> BuildNewGeofenceLat
  Helper -- used by --> BuildNewGeofenceLng
```

> SVG rendering failed for this diagram.
