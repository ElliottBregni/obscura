# Diagram: web/portal/src/modules/shipment-detail/AddAssetModal.js


> Auto-generated by Obscura crawlers

## Diagram 1

```mermaid
classDiagram
class EditShipmentRow {
  +label: string
  +value: any
  +valueKey: string
  +isTime: bool
  +systemValue: string
  +updateField(field, val)
}
class AddAssetModal {
  +show: bool
  +hide()
  +shipment: object
  +assignAsset(...)
  +assignTrailer(...)
  -data: {assetID, trailerNumber, effectiveDate}
  -noteValue: string
  -initialData: ref
  +handleOnEnter()
  +handleOnChangeNote(evt)
  +updateField(field, val)
}
class Modal {
  +Header()
  +Body()
  +Footer()
}
class DateTimePicker {
  +onChange(value)
  +value
}
class FormControl {
  +type
  +value
  +onChange(evt)
}
class Button {
  +onClick()
  +disabled
}
class FlexColDiv {}
class Colors {
  +background
  +highlight
}
AddAssetModal --> Modal
AddAssetModal --> EditShipmentRow : uses 3 instances
EditShipmentRow --> DateTimePicker : when isTime
EditShipmentRow --> FormControl : when !isTime
AddAssetModal --> Button
AddAssetModal --> FlexColDiv
AddAssetModal .. Colors
```

> SVG rendering failed for this diagram.

## Diagram 2

```mermaid
flowchart TD
  A[AddAssetModal mounted] --> B[useState data initialized]
  B --> C[Render Modal]
  C --> D[Render EditShipmentRow: Asset ID]
  C --> E[Render EditShipmentRow: Effective Date]
  C --> F[Render EditShipmentRow: Trailer Number]
  E --> G[DateTimePicker] 
  D & F --> H[FormControl inputs]
  H --> I[onChange -> updateField(valueKey, value)]
  G --> I[onChange -> updateField(valueKey, value)]
  I --> J[setData(updated)]
  J --> K[userChangedData computed]
  K --> L[Submit Button enabled if changed]
  L --> M{onClick Submit}
  M -->|assetID present| N[assignAsset(shipment, organization, assetID, effectiveDate, noteValue)]
  M -->|trailerNumber present| O[assignTrailer(shipment, trailerNumber)]
  M --> P[hide() modal closed]
```

> SVG rendering failed for this diagram.
