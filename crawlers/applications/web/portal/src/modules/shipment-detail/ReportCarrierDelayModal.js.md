# Diagram: web/portal/src/modules/shipment-detail/ReportCarrierDelayModal.js


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
flowchart TD
  RC[ReportCarrierDelayModal] --> PropsR[Props: show, hide, isOrgTypeShipper, fetchDelayOptions, reportDelayOptionsList, reportDelayOptionsIsLoading, reportDelayOptionsIsLoadingError, reportDelay, isSubmitLoading, responseStatusCode, errorResponse]
  RC --> StateR[State: reasonCode, newEta, comments, shipmentIDs{importText, importShipmentIDs}, showAlert]
  RC --> EffectsR[Effects: useEffect(fetchDelayOptions), useEffect(responseStatusCode -> showAlert), useEffect(reset on 201/408)]
  RC --> UIR[UI: Modal(Header, Body, Footer) -> SelectField, DateTimePicker, Comments textarea, Batch textarea (if shipper), Alerts, Close & Submit Buttons]
  RC --> HandlersR[Handlers: onReasonChange, onNewEtaChange, onCommentsChange, textChangeHandler, onSubmitClick, onModalClose, resetStopData]
  HandlersR -->|onSubmitClick uses| reportDelayFunc[reportDelay(reasonCode,newEta,comments,maybe shipmentIDs)]
  StateR -->|affects| CanSubmit[canSubmit = !reportDelayOptionsIsLoading && !isSubmitLoading && reasonCode != null && newEta != null]
  CanSubmit -->|controls| SubmitBtn[Submit Button disabled=!canSubmit or shows spinner if isSubmitLoading]
  UIR -->|shows| AlertsR[Alerts: loading/error from reportDelayOptions, response status alert (201/408/other)]
  PropsR -->|isOrgTypeShipper true shows| BatchText[Batch Shipment IDs textarea]
  RC --> ClearLink[ClearCarrierDelayModal]
  ClearLink --> PropsC[Props: show, hide, clearDelay, isSubmitLoading, errorMessage]
  ClearLink --> UIC[UI: Modal(Header: Clear Delay, Body: confirmation text, optional error Alert, Footer: Close & Submit)]
  UIC --> HandlersC[Handler: onSubmitClick -> clearDelay(hide)]
  HandlersC -->|invokes| clearDelayFunc[clearDelay]
  style RC fill:#f9f,stroke:#333,stroke-width:1px
  style ClearLink fill:#eef,stroke:#333,stroke-width:1px
```

> SVG rendering failed for this diagram.
