# Diagram: web/portal/src/modules/shipment-detail/BillOfLadingModal.tsx


> Auto-generated by Obscura crawlers

## Diagram 1

```mermaid
classDiagram
class BillOfLadingModal {
  +bool show
  +func hide()
  +func updateShipment(shipment, payload, fetchShipmentDetails)
  +any shipment
  +array organizations
  +func clearEditShipmentStatus()
  +func fetchShipmentDetails()
  +bool isSubmitLoading
  +string errorMessages
  +string responseStatusCode
  +string bolText
  +bool showAlert
  +useEffect(reset bolText on show change)
  +useEffect(clearEditShipmentStatus on mount if responseStatusCode)
  +useEffect(reset bolText on responseStatusCode change)
  +useEffect(setShowAlert on errorMessages or responseStatusCode)
  +func submitForm()
  +func closeModal()
}
class AlertMessages {
  +string responseStatusCode
  +bool showAlert
  +func hideAlert()
  +useTranslation t
}
class BolTextProps {
  +shipmentReferences: array
}
class AlertMessagesProps {
  +string responseStatusCode
  +bool showAlert
  +func hideAlert()
}
class BillOfLadingModalProps {
  +bool show
  +func hide()
  +func updateShipment(shipment, payload, fetchShipmentDetails)
  +any shipment
  +array organizations
  +func clearEditShipmentStatus()
  +func fetchShipmentDetails()
  +bool isSubmitLoading
  +string errorMessages
  +string responseStatusCode
}
BillOfLadingModal "1" o-- "1" AlertMessages : uses
BillOfLadingModalProps <|-- BillOfLadingModal
AlertMessagesProps <|-- AlertMessages
BillOfLadingModal "1" o-- "1" BolTextProps : builds payload
AlertMessages ..> "useTranslation" : uses
BillOfLadingModal ..> "react useState/useEffect" : lifecycle
```

> SVG rendering failed for this diagram.

## Diagram 2

```mermaid
flowchart LR
  A[User types BOL text in textarea] --> B[setBolText updates bolText state]
  B --> C{Submit button clicked}
  C -->|disabled if empty or loading| D[No action]
  C -->|enabled| E[submitForm()]
  E --> F[formattedBolText = bolText.split(newlines)]
  F --> G[Build payload.shipmentReferences with qualifier "bol" and values]
  G --> H[call updateShipment(shipment, payload, fetchShipmentDetails)]
  H --> I[Server response -> responseStatusCode or errorMessages]
  I --> J{responseStatusCode or errorMessages present?}
  J -->|yes| K[setShowAlert(true) -> AlertMessages shown]
  J -->|yes| L[clearEditShipmentStatus() called (if responseStatusCode on mount)]
  K --> M[User dismisses alert or closeModal()]
  M --> N[hide() called and showAlert set false]
```

> SVG rendering failed for this diagram.
