# Diagram: web/portal/src/modules/shipment-detail/CancelShipmentModal.js


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
flowchart LR
  A[CancelShipmentModal Component] -->|receives props| P[props: show, hide, shipment, cancelShipment]
  A --> T[useTranslation("shipment-details")]
  A --> S[useState noteValue = ""]
  S --> UC[userChangedData = noteValue.length > 0]
  A --> H1[handleOnChangeNote(event): setNoteValue(event.target.value)]
  A --> H2[handleOnEnter(): setNoteValue("")]
  A --> M[Modal]
  M --> MH[Modal.Header: title = t("Cancel Shipment")]
  M --> MB[Modal.Body]
  M --> MF[Modal.Footer]
  MB --> FG[FormGroup]
  FG --> FL[FormLabel: "Reason"]
  FG --> FC[FormControl textarea]
  FC -->|onChange| H1
  MF --> B1[Button: "Cancel"]
  MF --> B2[Button: "Submit"]
  B1 -->|onClick| P_hide[hide()]
  B2 -->|disabled={!userChangedData}| P_submit[onClick -> cancelShipment(shipment, noteValue) & hide()]
  P --> P_hide
  P --> P_submit
  style A fill:#f9f,stroke:#333,stroke-width:1px
  style M fill:#eef,stroke:#333
  style S fill:#efe,stroke:#333
  style B2 fill:#cfc,stroke:#333
```

> SVG rendering failed for this diagram.
