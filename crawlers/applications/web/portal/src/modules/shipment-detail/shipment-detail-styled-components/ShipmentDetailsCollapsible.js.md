# Diagram: web/portal/src/modules/shipment-detail/shipment-detail-styled-components/ShipmentDetailsCollapsible.js


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
classDiagram
    class ShipmentDetailsCollapsible {
        <<component>>
        +extraHeaderContent
        +shipmentId
        +assetId
        +mode
        +showMadChiclet
        +status
        +shipmentType
        +shipperName
        +carrierName
        +carrierScac
        +currentRoad
        +trailerAndEquipmentNumbers
        +serviceCode
        +showException
        +exception
        +isBackOrder
        +isOffRoute
        +isNoActivity
        +railTrainId
        +loadedStatus
        +etaTimeCategory
        +render()
    }
    class PanelGroup {
        <<ui>>
        +Header
        +Content
    }
    class DetailItem {
        <<ui>>
        +label
        +children
    }
    class ShowMoreList {
        <<ui>>
        +title
        +list
        +visibleItemCount
    }
    class MadChicletCSS {
        <<ui>>
        +shipmentMode
        +stopMode
    }
    class Tooltip {
        <<ui>>
        +tooltipChildren
        +children
    }
    class useShipmentTranslation {
        <<hook>>
        +getTranslatedMode()
    }
    class useShipmentExceptionTranslation {
        <<hook>>
        +getTranslatedShipmentException()
    }
    class useEtaTimeCategory {
        <<hook>>
        +(etaTimeCategory, mode) -> {color, label, formattedDuration}
    }
    class getColorForException {
        <<helper>>
        +(exception) -> color
    }
    class getLoadedStatusDisplayValue {
        <<helper>>
        +(loadedStatus, t) -> string
    }
    ShipmentDetailsCollapsible --> PanelGroup : uses
    ShipmentDetailsCollapsible --> DetailItem : composes many
    ShipmentDetailsCollapsible --> ShowMoreList : uses
    ShipmentDetailsCollapsible --> MadChicletCSS : conditionally renders
    ShipmentDetailsCollapsible --> Tooltip : conditionally wraps DetailItem
    ShipmentDetailsCollapsible ..> useShipmentTranslation : calls
    ShipmentDetailsCollapsible ..> useShipmentExceptionTranslation : calls
    ShipmentDetailsCollapsible ..> useEtaTimeCategory : calls
    ShipmentDetailsCollapsible ..> getColorForException : calls
    ShipmentDetailsCollapsible ..> getLoadedStatusDisplayValue : calls
```

> SVG rendering failed for this diagram.
