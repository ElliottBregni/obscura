# Diagram: web/portal/src/modules/shipment-detail/shipment-detail-styled-components/ShipmentInfoBox.js


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
classDiagram
    class ShipmentInfoBox {
        +title
        +children
        +border
        +styles
        +titleStyles
        +"data-qa"
        +render()
    }
    class React
    class Colors {
        <<module>>
        background
    }
    ShipmentInfoBox --> Colors : imports/uses
    ShipmentInfoBox ..> React : JSX runtime
    ShipmentInfoBox o-- "div (container)" : renders
    class "div (container)" {
        -borderStyle
        -borderRadius
        -width
        -padding
        -color
    }
    "div (container)" o-- "div (title)" : contains
    class "div (title)" {
        -fontSize
        -marginBottom
    }
    "div (container)" o-- "div (children)" : contains
    class "div (children)" {
        -color: Colors.background.DARK_BLUE
    }
```

> SVG rendering failed for this diagram.
