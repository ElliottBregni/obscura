# Diagram: web/portal/src/modules/mt-dashboard/mt-dashboard-components/ExpiringRacks.js


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
classDiagram
    class ExpiringRacks {
        +props: solutionId, metalTrackingEntities, setSearchFilter
        +render()
    }
    class expiringCounts {
        +racks -> counts
        +returns counts
    }
    class expiringChartData {
        +counts -> chartData
    }
    class DonutChart {
        +data, handler, label, total, styles
    }
    class RackTotalWidget {
        +count, label, color, handler, icon?
    }
    class PanelGroup {
        <<component>>
        +Header(title)
        +Content(children)
    }
    class FlexDiv {
        +css
    }
    class FlexColDiv {
        +css
    }
    class Colors {
        +background, highlight
    }
    class MediaQueries {
        +mediumAndUp
    }
    class MdDone {
        <<icon>>
    }
    class useTranslation {
        +t(namespace) -> string
    }

    ExpiringRacks --> expiringCounts : calls
    ExpiringRacks --> expiringChartData : calls
    expiringChartData --> Colors : reads
    ExpiringRacks --> PanelGroup : composes
    PanelGroup --> DonutChart : contains
    PanelGroup --> RackTotalWidget : contains (multiple)
    ExpiringRacks --> FlexDiv : uses
    ExpiringRacks --> FlexColDiv : uses
    RackTotalWidget --> MdDone : may use icon
    ExpiringRacks --> useTranslation : uses
    ExpiringRacks ..> MediaQueries : uses for responsive css
    ExpiringRacks ..> Colors : uses for widget colors
    expiringCounts --> "racks" : iterates over
    expiringChartData --> "chart segments" : returns array of segments
```

> SVG rendering failed for this diagram.
