# Diagram: web/portal/src/modules/documentation/TryApiView.js


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
classDiagram
class TryApiView {
  +apiGroup
  +apiSample
  +data
  +apiNameOptions
  +apiTypeOptions
  +requestSampleOptions
  +selectedApiName()
  +selectedApiType()
  +selectedRequestSample()
  +username
  +password
  +constructTryApi(sample)
  +constructSelectors(group)
  +apiSelectHandler(type, val)
  +requestUpdateHandler(section, name, val)
  +tryApiHandler()
}
class AuthorizationCredentials {
  +username
  +password
  +onUsernameChange(value)
  +onPasswordChange(value)
}
class ApiSelect
class ApiRequest {
  +data
  +eventHandler()
  +submitHandler()
  +isLoading
}
class ApiResponse {
  +tryResponse
}
class PageHeader {
  +title
  +description
}
class StandardInput {
  +label
  +value
  +onChange()
}
TryApiView --> ApiSelect : contains
TryApiView --> AuthorizationCredentials : contains
TryApiView --> ApiRequest : contains
TryApiView --> ApiResponse : contains
TryApiView --> PageHeader : uses
AuthorizationCredentials --> StandardInput : renders
TryApiView ..> "useState/useEffect/useTranslation" : uses
TryApiView ..> "useSetTitleOnMount" : uses
TryApiView ..> "useTrackWithMixpanelOnce" : uses
```

> SVG rendering failed for this diagram.
