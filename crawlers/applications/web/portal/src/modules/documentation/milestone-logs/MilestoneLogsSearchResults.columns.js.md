# Diagram: web/portal/src/modules/documentation/milestone-logs/MilestoneLogsSearchResults.columns.js


> Auto-generated by Obscura crawlers

## Diagram 1

```mermaid
classDiagram
    class CellRenderer {
        +value: string
        +render()
    }
    class MilestoneCommentsAndReferencesCell {
        +value: {vin, comments, references}
        +render()
    }
    class StatusCellRenderer {
        +value: string
        +render()
    }
    class MilestoneReferencesButton {
        +vin
        +references
    }
    class RotatingExpanderCell
    class DateTimeCell {
        +dateTime
        +localize
    }
    class Text {
        +block
    }
    class Colors {
        +highlight.RED
    }

    CellRenderer --> Text : uses (optionally)
    MilestoneCommentsAndReferencesCell --> MilestoneReferencesButton : renders
    MilestoneCommentsAndReferencesCell --> Text : renders comments
    StatusCellRenderer --> Colors : reads highlight.RED
    DateTimeCell <|-- DateTimeCell
    RotatingExpanderCell <|-- RotatingExpanderCell
```

> SVG rendering failed for this diagram.

## Diagram 2

```mermaid
graph LR
    A[getColumns(t)] --> B[columns = []]
    B --> C[push expander column]
    C --> D[push VIN column]
    D --> E[push Milestone Event column]
    E --> F[push Milestone Status Code column]
    F --> G[push Location column]
    G --> H[push Timestamp column (DateTimeCell)]
    H --> I[push Milestone Comments and References column]
    I --> J[push Status column (StatusCellRenderer)]
    J --> K[return columns]
```

> SVG rendering failed for this diagram.
