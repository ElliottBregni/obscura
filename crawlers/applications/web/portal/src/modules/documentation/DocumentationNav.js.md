# Diagram: web/portal/src/modules/documentation/DocumentationNav.js


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
flowchart LR
  Props[Props: isOpen, toggleOpenGroup, sidebarMinimize] --> Component[DocumentationNav Component]
  Component --> Hooks{Hooks}
  Hooks --> useTranslation[useTranslation -> t]
  Hooks --> useSelector_docCategory[useSelector(DocumentationState.selectors.getCategorySlug) -> docCategory]
  Hooks --> useSelector_location[useSelector(getCurrentLocation) -> currentLocation]
  Hooks --> useSelector_activeContent[useSelector(DocumentationState.selectors.getContent) -> activeContent]
  Hooks --> useSelector_contentNav[useSelector(DocumentationState.selectors.getContentNav) -> contentNav]
  Hooks --> useSelector_activeOrg[useSelector(getActiveOrganization) -> activeOrganization]
  Hooks --> useDispatch[useDispatch -> dispatch]
  useEffectBlock[useEffect: on mount / activeOrganization change] -->|if activeOrganization not empty| dispatchFetch[dispatch(DocumentationState.actionCreators.fetchContentNav())]
  Hooks --> useEffectBlock
  contentNav --> navOptions{{navOptions = _.get(contentNav, "options", [])}}
  navOptions --> activeOptionCalc[Determine activeOption using docRoutesSet, activeContent, MATCHER_FOR_DOC_CATEGORY]
  activeOptionCalc --> getNavLinks[getNavLinks(t, navOptions, activeOption) -> links]
  getNavLinks --> NavLinkList[Array of <NavLink> elements]
  NavLinkList --> RenderCheck{links exist?}
  RenderCheck -->|no| ReturnNull[returns null]
  RenderCheck -->|yes| RenderNavGroup[Render <NavGroup> with props: groupKey, title, icon, isOpen, toggleOpenGroup, sidebarMinimize]
  RenderNavGroup --> ContainsLinks[NavGroup contains NavLinkList children]
  DOCUMENTATION_GROUP[DOCUMENTATION constant] --> docGroup[docGroup = privateRouteGroupsMap["DOCUMENTATION"]]
  docGroup --> docRoutesSet[docRoutesSet = Set(Object.keys(docGroup.routes))]
  MATCHERS[MATCHER_FOR_DOC_CATEGORY mapping] --> activeOptionCalc
  Component --> PropTypesDef[PropTypes validation for isOpen, toggleOpenGroup, sidebarMinimize]
  Component --> Export[export default DocumentationNav]
```

> SVG rendering failed for this diagram.
