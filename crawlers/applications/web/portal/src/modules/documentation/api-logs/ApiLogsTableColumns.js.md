# Diagram: web/portal/src/modules/documentation/api-logs/ApiLogsTableColumns.js


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
classDiagram
    class CellRenderer {
        +props
        +render()
    }
    class StatusCellRenderer {
        +props
        +render()
    }
    class ShipmentIdCellRenderer {
        +value
        +shipmentInternalId
        +pushShipmentDetailView()
        +render()
    }
    class getColumns {
        +t
        +auth
        +pushShipmentDetailView
        +returns columns[]
    }
    class RotatingExpanderCell
    class Button
    class Text
    class Colors
    class PropTypes

    CellRenderer ..> PropTypes : propTypes uses
    StatusCellRenderer ..> PropTypes : propTypes uses
    ShipmentIdCellRenderer ..> PropTypes : propTypes uses
    ShipmentIdCellRenderer --> Button : uses
    ShipmentIdCellRenderer --> Text : uses
    StatusCellRenderer --> Colors : reads RED highlight
    getColumns --> RotatingExpanderCell : sets as Cell
    getColumns --> CellRenderer : sets as Cell
    getColumns --> StatusCellRenderer : sets as Cell
    getColumns --> ShipmentIdCellRenderer : sets as Cell
    getColumns --> Button : (rendered within ShipmentId)
    getColumns --> Text : (rendered within ShipmentId)
    getColumns o-- "Array" : returns
    RotatingExpanderCell <|-- RotatingExpanderCell : external component reference
```

> SVG rendering failed for this diagram.
