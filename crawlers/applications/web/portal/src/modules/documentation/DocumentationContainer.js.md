# Diagram: web/portal/src/modules/documentation/DocumentationContainer.js


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
classDiagram
    class ContentView {
        +filename
        +baseUrl
        +isLoading
        +content
        +fetchContent(filename)
        +pushDocumentView(fileName, fileType, baseUrl)
    }
    class mapStateToProps {
        +state
        +-> filename
        +-> baseUrl
        +-> isLoading
        +-> content
    }
    class mapDispatchToProps {
        +dispatch
        +-> fetchContent(filename)
        +-> pushDocumentView(fileName,fileType,baseUrl)
    }
    class DocumentationState {
        class selectors {
            +getContentStatus(state)
            +getContent(state)
        }
        class actionCreators {
            +fetchContent(filename)
            +pushDocumentView(fileName,fileType,baseUrl)
        }
    }
    class AppState {
        +location.payload.filename
        +location.payload.baseUrl
    }
    mapStateToProps --> AppState : reads
    mapStateToProps --> DocumentationState.selectors : calls
    mapDispatchToProps --> DocumentationState.actionCreators : uses
    ContentView <-- mapStateToProps : receives props
    ContentView <-- mapDispatchToProps : receives dispatchers
    class connect {
        +connect(mapStateToProps,mapDispatchToProps)
    }
    connect --> ContentView : wraps
    connect --> mapStateToProps
    connect --> mapDispatchToProps
```

> SVG rendering failed for this diagram.
