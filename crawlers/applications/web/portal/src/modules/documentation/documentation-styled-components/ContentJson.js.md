# Diagram: web/portal/src/modules/documentation/documentation-styled-components/ContentJson.js


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
classDiagram
    class ContentJson {
        +object content
        +func pushDocumentView(file, type, baseUrl)
        +render()
    }
    class CellRenderer {
        +props.value: string
        +render()
    }
    class LinkRenderer {
        +props.value: string
        +render()
    }
    class externalLinksColumns {
        +func(columns): columnsConfig[]
    }
    class LinkPanels {
        +title
        +children
    }
    class LinkPanel {
        +title
        +description
        +onClick()
    }
    class BaseTable {
        +data
        +columns
        +isLoading
    }
    class Text {
        +block
        +size
        +color
    }
    class useTranslation {
        +func(namespace): {t}
    }
    class uuidv4 {
        +func(): uuid
    }
    class lodash {
        +values(obj): array
    }
    class Colors {
        +background
    }

    ContentJson o-- LinkPanels : uses
    ContentJson o-- BaseTable : renders
    ContentJson ..> useTranslation : calls
    ContentJson ..> uuidv4 : generates keys
    ContentJson ..> lodash : uses _.values
    ContentJson ..> Colors : uses
    LinkPanels <|-- LinkPanel : contains
    externalLinksColumns ..> CellRenderer : returns Cell for string
    externalLinksColumns ..> LinkRenderer : returns Cell for hyperlink
    BaseTable ..> externalLinksColumns : receives columns
    LinkPanel ..> ContentJson : invokes pushDocumentView onClick
    CellRenderer --> Text : renders text (inline)
    LinkRenderer --> Text : renders anchor (inline)
```

> SVG rendering failed for this diagram.
