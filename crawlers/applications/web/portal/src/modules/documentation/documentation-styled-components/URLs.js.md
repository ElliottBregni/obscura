# Diagram: web/portal/src/modules/documentation/documentation-styled-components/URLs.js


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
classDiagram
class URLs {
  +title: string
  +servers: array
  +method: string
  +path: string
  +render(): JSX
}
class SectionHeader {
  +title: string
  +render(): JSX
}
class UrlBuilder {
  +method: string
  +path: string
  +serverUrl: string
  +serverDescription: string
  +render(): JSX
}
class lodash {
  +values(obj): array
}
class paths {
  +paths(servers, method, path): UrlBuilder[]
}

URLs --> SectionHeader : "renders"
URLs --> paths : "calls"
paths --> lodash : "uses _.values"
paths --> UrlBuilder : "creates instances of"
UrlBuilder <-- SectionHeader : "separate component"
note right of URLs : exported as default
```

> SVG rendering failed for this diagram.
