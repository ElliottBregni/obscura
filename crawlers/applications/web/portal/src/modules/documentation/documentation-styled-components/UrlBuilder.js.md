# Diagram: web/portal/src/modules/documentation/documentation-styled-components/UrlBuilder.js


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
classDiagram
    class UrlBuilder {
        +method: string
        +path: string
        +serverUrl: string
        +serverDescription: string
        +finalUrl: string
        +render(): JSX.Element
    }
    class withTranslation {
        <<HOC>>
        +(names: Array<string>): Function
    }
    class fieldCss {
        <<object>>
        +border
        +boxShadow
        +fontWeight
        +marginBottom
        +padding
    }
    class badgeCss {
        <<object>>
        +backgroundColor
        +borderRadius
        +color
        +marginRight
        +padding
        +textTransform
    }
    class serverCss {
        <<object>>
        +float
        +fontWeight
        +marginRight
        +fontSize
    }
    class Colors {
        <<module>>
        +highlight.BRIGHT_BLUE
    }
    class PropTypes {
        <<module>>
        +string
    }
    class Export {
        <<default export>>
        +UrlBuilderTranslated
    }

    Colors --> badgeCss : uses
    fieldCss --> UrlBuilder : applied to
    badgeCss --> UrlBuilder : applied to
    serverCss --> UrlBuilder : applied to
    PropTypes --> UrlBuilder : validates props
    UrlBuilder "1" --> "1" withTranslation : wrapped by
    withTranslation --> Export : returns
    UrlBuilder --> Export : exported as wrapped component
    UrlBuilder : finalUrl = "https://" + serverUrl + path
```

> SVG rendering failed for this diagram.
