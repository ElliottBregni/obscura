# Diagram: web/portal/src/modules/documentation/ContentView.js


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
flowchart LR
  A[ContentView props:\nfilename, baseUrl, content,\nisLoading, pushDocumentView, fetchContent] --> B[useTranslation(\"documentation\") -> t]
  A --> C[useSetTitleOnMount(t(\"documentation:Training & Resources\"))]
  A --> D[useTrackWithMixpanelOnce(`Viewed Page: Docs / Content / ${filename}`)]
  A --> E[useEffect: if !baseUrl then fetchContent(filename)]
  A --> F[compute flags]
  F --> F1{isJson?\n(filename includes .json\n&& content is object\n&& !Array.isArray(content))}
  F --> F2{isUrl?\n(baseUrl && baseUrl.length > 0)}
  F --> F3{isPdf?\n(filename includes .pdf\n&& (isUrl || content is string))}
  F --> F4{isHtml?\n(filename includes .htm\n&& content is string)}
  A --> G[Render DOM: <div css=contentCss>]
  G --> H[Loader loaded={!isLoading}]
  H -->|if isJson true| J[ContentJson\n(content, pushDocumentView)]
  H -->|if isJson false| I[no JSON content rendered]
  G --> K{isPdf && !isUrl?}
  K -->|true| L[ContentPdf\n(filename, content)]
  K -->|false| M[no ContentPdf]
  G --> N{isPdf && isUrl?}
  N -->|true| O[LinkPdf\n(filename, baseUrl)]
  N -->|false| P[no LinkPdf]
  G --> Q{isHtml?}
  Q -->|true| R[ContentHtml\n(content)]
  Q -->|false| S[no HTML content]
  style A fill:#f9f,stroke:#333,stroke-width:1px
  style G fill:#eef,stroke:#333
  style H fill:#fff7cc,stroke:#333
  style J fill:#d4f7d4
  style L fill:#d4e7ff
  style O fill:#ffe4d4
  style R fill:#f0d4ff
```

> SVG rendering failed for this diagram.
