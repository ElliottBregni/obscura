# Diagram: web/portal/src/redux/store.js


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
flowchart LR
  subgraph Imports
    A[redux: applyMiddleware, combineReducers, legacy_createStore as createStore]
    B[redux-devtools: composeWithDevTools]
    C[redux-thunk: thunkMiddleware]
    D[redux-logger: createLogger]
    E[redux-first-router: connectRoutes]
    F[history: createBrowserHistory]
    G[qs: querySerializer]
    H[routesMap]
    I[ApplicationConfig]
    J[Many reducer modules...]
  end

  A -->|used in| Configure[configureStore(preloadedState)]
  B -->|used in| Configure
  C -->|added to| Middlewares[appMiddlewares]
  D -->|conditionally added to| Middlewares
  E -->|returns reducer,middleware,enhancer| ConnectResult[connectRoutes(routesMap,...)]
  F -->|passed to| ConnectResult
  G -->|passed to| ConnectResult
  H -->|passed to| ConnectResult
  I -->|controls logger| Configure

  J -->|collected into| AppReducers[appReducers object]
  ConnectResult -->|reducer & middleware & enhancer| Configure
  Middlewares -->|combined with route middleware| Applied[appliedMiddlewares = applyMiddleware(middleware, ...appMiddlewares)]
  Applied -->|combined| Enhancers[enhancers = composeWithDevTools(enhancer, appliedMiddlewares)]
  AppReducers -->|spread into| CombinedReducers[reducers = combineReducers({ location: reducer, ...appReducers })]
  CombinedReducers -->|passed to| CreateStoreCall[createStore(reducers, preloadedState, enhancers)]
  CreateStoreCall -->|returns| StoreObj[store]
  StoreObj -->|returned by| Configure

  classDef mod fill:#f9f,stroke:#333,stroke-width:1px
  class Imports,J,AppReducers mod
```

> SVG rendering failed for this diagram.
