# Diagram: web/portal/.gitlab-ci.yml


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
flowchart LR
  subgraph BaseJob[".web-portal (base)"]
    B_Image["image: node:18.17.1-alpine3.17@sha256..."]
    B_Interruptible["interruptible: true"]
    B_Vars["variables:\nWEB_PORTAL_HOME=applications/web/portal"]
  end

  P["web-portal:prettier"]
  P -->|extends| BaseJob
  P --> Stage["stage: lint"]
  P --> Scripts["script"]
  Scripts --> S1["cd $WEB_PORTAL_HOME"]
  Scripts --> S2["npm install --global prettier@3.0.3"]
  Scripts --> S3["yarn run lint:prettier"]
  P --> Rules["rules: changes"]
  Rules --> ChangeFiles["$WEB_PORTAL_HOME/**/*.{js,jsx,ts,tsx,scss}"]
  Rules --> PrettierConfig["$WEB_PORTAL_HOME/.prettierrc"]
  Rules --> PrettierIgnore["$WEB_PORTAL_HOME/.prettierignore"]

  subgraph OptionalCache["(commented: optional cache & jobs)"]
    C_Base[".web-portal:with-cache\nextends .web-portal\ncache:key(paths: yarn.lock)\npaths: $WEB_PORTAL_HOME/node_modules"]
    C_Install["web-portal:install\nstage: setup\nscript: yarn install --frozen-lockfile\ncache: pull-push\nrules: changes yarn.lock"]
    C_PostInstall[".web-portal:post-install\nextends .web-portal-with-cache\nbefore_script: reference install script"]
    C_Eslint["web-portal:eslint\nextends .web-portal-post-install\nstage: lint\nscript: yarn run lint:eslint"]
    P -.-> C_Base
    C_Base --> C_Install
    C_Install --> C_PostInstall
    C_PostInstall --> C_Eslint
  end

  style OptionalCache stroke-dasharray: 5 5,fill:none
  classDef meta fill:#f9f,stroke:#333,stroke-width:1px;
  class BaseJob meta,OptionalCache meta
```

> SVG rendering failed for this diagram.
