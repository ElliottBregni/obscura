# Diagram: web/portal/config-overrides.js


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
flowchart TD
  Webpack[Webpack] -->|calls| Override[module.exports = override(config, env)]
  Override -->|param| ConfigIn[config]
  Override -->|param| EnvIn[env]
  Override --> Process[do stuff with the webpack config]
  Process -->|returns| ConfigOut[config]
```

> SVG rendering failed for this diagram.
