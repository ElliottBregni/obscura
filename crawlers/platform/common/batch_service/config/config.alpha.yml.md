# Diagram: common/batch_service/config/config.alpha.yml


> Auto-generated by Obscura crawlers

## Diagram 1

```mermaid
flowchart TD
    A[Crawl Entry (crawl.py)] --> B[Crawlers (crawlers.py, crawlers/)]
    B --> C[Agents (agents/)]
    C --> D[Backend (backend/)]
    D --> E[Sync & CLI (sync.py, sdk/)]
    E --> F[Storage & Repos (repos/)]
    B --> G[Examples & Tests (examples/, tests/)]
    style A fill:#f9f,stroke:#333,stroke-width:2px
    style D fill:#bbf,stroke:#333,stroke-width:1px
```

> SVG rendering failed for this diagram.

## Diagram 2

```mermaid
classDiagram
    class Crawl {
        +run()
        +parse_args()
    }
    class CrawlerModule {
        +discover()
        +fetch()
    }
    class Agent {
        +start()
        +schedule()
    }
    class Backend {
        +serve()
        +store()
    }
    class Sync {
        +sync_changes()
    }
    class SDK {
        +client()
    }
    Crawl --> CrawlerModule : uses
    CrawlerModule --> Agent : delegates to
    Agent --> Backend : reports to
    Backend --> Sync : triggers
    Sync --> SDK : uses
    Backend --> SDK : exposes API
    note left of Crawl : entry points: crawl.py, crawl.py CLI
```

> SVG rendering failed for this diagram.
