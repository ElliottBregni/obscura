# Diagram: common/batch_service/batch_service/post_batch_message.py


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
flowchart TD
  A[lambda_handler(event, context, audit_refs)] -->|establish| B[DB_CONN.establish_connection()]
  B --> C[cursor = DB_CONN.cursor]
  A --> D[get_user_id(event)]
  A --> E[auth.get_organization_id(event)]
  D --> F[update audit_refs with USER_ID]
  E --> F
  A --> G[get_identifier(event)]
  A --> H[get_comments(event)]
  A --> I[get_shared_data(event)]
  I --> J[is_valid_msg_body(shared_data)]
  J -->|valid| K[for each comment in comments]
  K --> L[assemble_additional_data(event, shared_data, comment)]
  L --> M[deepcopy(event) & remove body/pathParameters]
  L --> N[assemble_path_parameters(shared_data, reference_id)]
  N --> N1{target_service}
  N1 -->|entity| N2[set solution_id, entity_id]
  N1 -->|shipment| N3[set shipment_id, shipper_org_id]
  N1 -->|Container Tracking| N4[set containerId]
  L --> O[build new body with reference_id, system_name, text, shared_with, batch_flag]
  L --> P[return additional_data]
  K --> Q[create_batch_message(cursor, user_id, org_id, identifier, target_lambda, target_service, reference_id, json)]
  Q --> R[collect results]
  R --> S[make_response({"batch_group_id": identifier}, 200)]
  J -->|invalid| T[raise BadRequestError]
  A --> U[exception handler]
  U --> V[make_error_response(event, context, message, resource, client_message, 500)]
  subgraph Helpers
    GH1[get_event_body(event)]
    GH2[make_response(payload, status)]
    GH3[make_error_response(...)]
    GH4[create_batch_message(...)]
    GH5[uuid.uuid4()]
  end
  get_event_body_call[get_event_body used by get_identifier/get_comments/get_shared_data] --> GH1
  G -->|uses| GH5
  Q -->|calls| GH4
  S -->|uses| GH2
  V -->|uses| GH3
```

> SVG rendering failed for this diagram.
