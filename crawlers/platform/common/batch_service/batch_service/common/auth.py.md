# Diagram: common/batch_service/batch_service/common/auth.py


> Auto-generated by Obscura crawlers

## Diagram 1

```mermaid
flowchart TD
Start((Start)) --> TryBlock[Try]
TryBlock --> Extract[Extract user_id from event["requestContext"]["authorizer"]["user_id"]]
Extract --> HasID{"user_id present?"}
HasID -- No --> KeyErrorHandle[Raise fv.error.ForbiddenError: "User information could not be found on the event"]
KeyErrorHandle --> ErrorEnd((Error))
HasID -- Yes --> ParseAttempt[Attempt to parse/split user_id if needed]
ParseAttempt --> SplitDecision{"full == False?"}
ParseAttempt -- error --> AttrHandle[Raise fv.error.ForbiddenError: "User information was invalid"]
AttrHandle --> ErrorEnd
SplitDecision -- True --> ReturnStripped[Return user_id.split("|")[1]]
SplitDecision -- False --> ReturnFull[Return full user_id]
ReturnStripped --> End((Return))
ReturnFull --> End
```

> SVG rendering failed for this diagram.

## Diagram 2

```mermaid
classDiagram
class "fv.aws.lambdas" {
  +module
}
class "fv.db" {
  +module
}
class "fv.dm" {
  +module
}
class "fv.log" {
  +module
}
class "fv.secrets" {
  +module
}
class "fv.aws.lambdas.constants" {
  +TEST_CLIENT_ID
  +STAGING_CLIENT_ID
  +PROD_CLIENT_ID
}
class "get_user_id" {
  +get_user_id(event, full=False)
  +returns stripped or full user id
  +raises fv.error.ForbiddenError
}
class "fv.error.ForbiddenError" {
  +Exception
}
"fv.aws.lambdas" --> "get_user_id" : defines
"fv.aws.lambdas" --> "fv.aws.lambdas.constants" : imports
"fv.aws.lambdas" --> "fv.db" : imports/uses
"fv.aws.lambdas" --> "fv.dm" : imports/uses
"fv.aws.lambdas" --> "fv.log" : imports/uses
"fv.aws.lambdas" --> "fv.secrets" : imports/uses
"get_user_id" --> "fv.error.ForbiddenError" : may raise
```

> SVG rendering failed for this diagram.
