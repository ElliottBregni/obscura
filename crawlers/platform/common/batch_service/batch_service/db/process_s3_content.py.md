# Diagram: common/batch_service/batch_service/db/process_s3_content.py


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
flowchart TD
    Start((Start)) --> InputParams[/"Input params: cursor, uuid, user_id, filename, target_lambda, url, status"/]
    InputParams --> BuildData[Build data dict]
    BuildData --> PrepareQuery[Prepare SQL query string (INSERT INTO process_s3_content ...)]
    PrepareQuery --> TryBlock{Try to execute}
    TryBlock --> LogInfo[logger.info(cursor.mogrify(query, data).decode())]
    LogInfo --> Mogrify[query = cursor.mogrify(query, data)]
    Mogrify --> Execute[cursor.execute(query)]
    Execute --> End((End))
    TryBlock -->|Exception caught| Except[except Exception as e]
    Except --> Raise[raise_generic_exception(e)]
    Raise --> End
```

> SVG rendering failed for this diagram.
