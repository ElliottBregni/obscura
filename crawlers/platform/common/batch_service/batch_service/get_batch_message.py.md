# Diagram: common/batch_service/batch_service/get_batch_message.py


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
flowchart LR
A[lambda_handler(event, context, audit_refs)] --> B[get_path_parameter(event, "id")]
A --> C[DB_CONN.establish_connection()]
A --> D[cursor = DB_CONN.cursor]
A --> E[get_user_id(event)]
A --> F[get_organization_id(event)]
E --> G[audit_refs.update(USER_ID, SOLUTION_ID, ORGANIZATION_ID)]
F --> G
D --> H[get_batch_message(cursor, batch_id)]
H --> I{batch exists?}
I -- yes --> J[make_response({batch}, 200, content_type="application/json")]
I -- no --> K[raise fv.error.NotFoundError(message, message)]
```

> SVG rendering failed for this diagram.
