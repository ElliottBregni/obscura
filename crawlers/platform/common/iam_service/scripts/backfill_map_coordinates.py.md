# Diagram: common/iam_service/scripts/backfill_map_coordinates.py


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
classDiagram
    class UpdateMapCoordinates {
        +cursor
        +process_csv_file(csv_file)
        +get_region_map_coordinates(region)
        +set_map_coordinates(org_fv_id, map_coordinates)
    }
    class FvDatabaseConnector
    class SecretNames
    class DB_CONN_SHIPMENTS
    class parse_arguments
    class main
    class CSV_File
    class organizations_table

    DB_CONN_SHIPMENTS --> FvDatabaseConnector : instanceOf
    DB_CONN_SHIPMENTS --> SecretNames : uses
    UpdateMapCoordinates --> DB_CONN_SHIPMENTS : uses.cursor
    UpdateMapCoordinates --> organizations_table : updates
    parse_arguments --> main : returns args
    main --> UpdateMapCoordinates : creates
    main --> UpdateMapCoordinates : calls process_csv_file
    UpdateMapCoordinates --> CSV_File : reads via csv.DictReader
    UpdateMapCoordinates : set_map_coordinates --> organizations_table : executes UPDATE query
    UpdateMapCoordinates : get_region_map_coordinates --> returns "POINT(...)"
```

> SVG rendering failed for this diagram.
