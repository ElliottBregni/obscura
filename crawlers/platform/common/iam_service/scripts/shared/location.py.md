# Diagram: common/iam_service/scripts/shared/location.py


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
flowchart TD
    A[call get_shipper_location_id_by_code(customer_org_id, location_code)] --> B[cursor.execute(SQL query, params)]
    B --> C[cursor.fetchone()]
    C -->|row found| D[return row.id]
    C -->|no row| E[return None]
```

> SVG rendering failed for this diagram.
