# Diagram: common/iam_service/scripts/shared/location_reference.py


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
flowchart LR
A[insert_into_location_reference(location_id, qualifier, value)] --> B[shipment_cursor.execute(SQL, params)]
B --> C{SQL Statement}
C --> D[INSERT INTO location.location_reference(location_id, qualifier, value)]
D --> G[location.location_reference table]
C --> E[ON CONFLICT (location_id, qualifier) DO UPDATE SET value = EXCLUDED.value]
B --> F[params: {"location_id","qualifier","value"}]
```

> SVG rendering failed for this diagram.
