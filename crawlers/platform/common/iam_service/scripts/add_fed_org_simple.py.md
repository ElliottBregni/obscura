# Diagram: common/iam_service/scripts/add_fed_org_simple.py


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
flowchart LR
  subgraph ModuleLoad
    DB_CONN[FvDatabaseConnector("add_fed_simple", SecretNames.DATABASE)]
    Imports[Imports: argparse, csv, fv.db, scripts.update_federated_orgs, fv.secrets]
  end

  Start[Start]
  Start --> ParseArgs(parse_arguments())
  ParseArgs -->|returns args| OpenFile[open(args.file, encoding="utf-8-sig")]
  OpenFile --> ReadCSV[list(csv.DictReader(f))]
  ReadCSV --> ForEach{for org in orgs}
  ForEach --> PrintOrg[print(org)]
  ForEach --> ExtractFields[org_code = org["code"]\norg_name = org["name"]]
  ExtractFields --> AddOrUpdate[add_or_update_org(org_code, org_name, fv_id=None)]
  AddOrUpdate --> DetermineID{organization_id in org_new?}
  DetermineID -->|yes| OrgID1[org_new["organization_id"]]
  DetermineID -->|no| OrgID2[org_new["id"]]
  OrgID1 --> Insert[insert_to_federation(DB_CONN.cursor, org_code, org_id)]
  OrgID2 --> Insert
  Insert --> ForEach
  ForEach --> End[End]

  DB_CONN --> Insert
  Imports --> ModuleLoad
```

> SVG rendering failed for this diagram.
