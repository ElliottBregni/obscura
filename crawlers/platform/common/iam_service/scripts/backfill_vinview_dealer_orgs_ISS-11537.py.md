# Diagram: common/iam_service/scripts/backfill_vinview_dealer_orgs_ISS-11537.py


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
flowchart TD
  A[script entry: __main__] -->|calls| B(parse_arguments)
  A --> C{customer_org_fv_id present?}
  C -->|yes| D[get_org_id_from_fv_id(cursor, fv_id)]
  D -->|queries organizations| DB1[(DB_CONN.cursor)]
  D -->|returns id| E[customer_org_id]
  C -->|no| Z[raise BadRequestError]
  A --> F[open CSV file -> rows]
  F --> G[for each row]
  G --> H[get_existing_org(cursor, org_name)]
  H -->|queries organizations| DB1
  H -->|if found| I[dealer_org_id exists]
  H -->|if not found| J[add_or_update_org(name) -> create org]
  J -->|returns| I
  I --> K[insert_into_federation_association(cursor, db_org_id, federated_org_id)]
  K -->|inserts into federation_association| DB1
  I --> L[insert_into_location_grant(cursor, location_code, customer_org_id, dealer_org_id, actor_id)]
  L -->|mogrify & execute| DB1
  L -->|returns| M[grant_ids]
  G --> N[after loop -> backfill_visibility_grants(cursor, actor_id, fv_id, grant_ids)]
  N -->|prompts to run sync function and then inserts| O[DB entity operations]
  O -->|queries active_entity, location, location_grant and inserts visibility_grant| DB2[(DB_CONN_ENTITY.cursor)]
  subgraph DBs
    DB1
    DB2
  end
  style DB1 fill:#f9f,stroke:#333,stroke-width:2px
  style DB2 fill:#bbf,stroke:#333,stroke-width:2px
```

> SVG rendering failed for this diagram.
