# Diagram: common/iam_service/scripts/migrate_org_assocs.py


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
flowchart TD
  Start(("Start"))
  StartTime["compute start time"]
  GetAuth0["authorization_api.get_all_roles()\nand authorization_api.get_all_permissions()"]
  DBConn["fv.db.get_connection(config)\nfv.db.get_cursor(conn)"]
  InsertPriv["insert_privileges(cursor, privileges)"]
  RoleLoop["For each role"]
  InsertRole["insert_role(cursor, role)"]
  InsertRolePriv["insert_role_privilege(cursor, role, db_role, db_privs)"]
  NameMap["auth0_to_name(role_permissions, permissions)"]
  InsertAuth0Users["insert_auth0_users(cursor, role_priv)"]
  GetGroups["authorization_api.get_all_groups()"]
  GetUserRoles["authorization_api.get_user_roles(group_user)"]
  UpdateUserAPI["management_api.update_user(group_user, payload)"]
  InsertUserDB["INSERT INTO \"user\" (auth0_id, role_id, organization_id) ..."]
  Commit["cursor.execute('COMMIT')"]
  EndTime["compute end time"]
  End(("Finished"))

  Start --> StartTime --> GetAuth0 --> DBConn --> InsertPriv --> RoleLoop --> InsertAuth0Users --> EndTime --> End
  RoleLoop --> InsertRole --> InsertRolePriv --> NameMap --> RoleLoop
  InsertAuth0Users --> GetGroups --> GetUserRoles --> UpdateUserAPI --> InsertUserDB --> Commit --> InsertAuth0Users
```

> SVG rendering failed for this diagram.
