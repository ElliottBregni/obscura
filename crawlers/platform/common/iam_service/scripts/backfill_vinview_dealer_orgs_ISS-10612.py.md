# Diagram: common/iam_service/scripts/backfill_vinview_dealer_orgs_ISS-10612.py


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
flowchart TD
    A[Start: __main__] --> B[parse_arguments()]
    B --> C{customer_org_fv_id provided}
    C -->|yes| D[get_org_id_from_fv_id(DB_CONN.cursor)]
    D -->|found| E[actor_id = backfill_...-YYYYMMDD]
    D -->|not found| F[raise fv.error.BadRequestError]
    E --> G[open CSV file rows]
    G --> H[for each row]
    H --> I[get_existing_org(DB_CONN.cursor, dealer_name)]
    I -->|exists| J[dealer_org_id]
    I -->|not exists| K[add_or_update_org(None, name) -> res]
    K --> L[dealer_org_id = res.organization_id]
    L --> M{dealer_org_id present?}
    M -->|no| N[logging.error and continue]
    M -->|yes| O[insert_into_federation_association(DB_CONN.cursor, dealer_org_id, fed_id)]
    O --> P[insert_into_location_grant(DB_CONN.cursor, location_code, customer_org_id, dealer_org_id, actor_id)]
    H --> Q[end for loop]
    Q --> R[backfill_visibility_grants(DB_CONN_ENTITY.cursor, actor_id, customer_org_fv_id)]
    R --> S[Done]

    subgraph DBs
      DB1[DB_CONN -> FvDatabaseConnector(backfill_vinview_dealer_orgs_ISS-10612, SecretNames.DATABASE)]
      DB2[DB_CONN_ENTITY -> FvDatabaseConnector(backfill_vinview_dealer_orgs_ISS-10612, prod/entity/database_rds_lambda)]
    end

    D --> DB1
    I --> DB1
    O --> DB1
    P --> DB1
    R --> DB2

    style DBs fill:#f9f,stroke:#333,stroke-width:1px
```

> SVG rendering failed for this diagram.
