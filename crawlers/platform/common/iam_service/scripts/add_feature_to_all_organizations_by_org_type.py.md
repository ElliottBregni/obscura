# Diagram: common/iam_service/scripts/add_feature_to_all_organizations_by_org_type.py


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
flowchart LR
  A[__main__ start] --> B[Instantiate FvDatabaseConnector db_conn]
  A --> C[parse_arguments()]
  C --> D[args(org_type_codes, features)]
  B --> E[main(db_conn, args)]
  D --> E
  E --> F[db_conn.establish_connection()]
  E --> G[cursor = db_conn.get_cursor()]
  G --> H[org_types = args.org_type_codes.split(",")]
  G --> I[features = args.features.split(",")]
  H --> J[for each org_type]
  I --> K[for each feature]
  J --> L[add_features_by_org_type_code(cursor, feature, org_type)]
  K --> L
  L --> M[get_all_organizations_without_feature(cursor, org_type, feature)]
  M --> N[Execute SQL query via cursor.execute(query, params)]
  N --> O[cursor.fetchall() -> org_fv_ids]
  O --> P[for each org in org_fv_ids]
  P --> Q[solution = format org.fv_id and feature]
  Q --> R[add_solution(AddSolutionParams(solution, org.fv_id, feature, None))]
  R --> S[solution recorded in DB via scripts.add_solution]
  style A fill:#f9f,stroke:#333,stroke-width:2px
  style B fill:#bbf,stroke:#333
  style C fill:#bbf,stroke:#333
  style E fill:#bfb,stroke:#333
  style L fill:#ffd,stroke:#333
```

> SVG rendering failed for this diagram.
