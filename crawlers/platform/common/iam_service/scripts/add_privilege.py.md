# Diagram: common/iam_service/scripts/add_privilege.py


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
flowchart TD
    A[main(args)] -->|calls| B[parse_arguments()]
    A --> C{args.roles, args.privileges}
    C --> D[split & strip roles -> roles set]
    C --> E[split & strip privileges -> privs set]
    A --> F[DB_CONN.cursor]
    F --> G[for priv in privs]
    G --> H[try db_stuff(cursor, priv, roles)]
    H --> I[db_stuff(cursor, priv, roles)]
    I --> J[roles_db.insert_privilege(cursor, priv)]
    I --> K[roles_db.add_privilege_to_role(cursor, role, priv)]
    I --> L[users_db.get_users(cursor) -> users]
    H -->|except Exception| M[DB_CONN.establish_connection()]
    M --> F
    G --> N[roles_db.insert_privilege(cursor, priv) (again)]
    N --> O[roles_db.add_privilege_to_role(cursor, role, priv) (again)]
    G --> P[users_db.get_users(cursor) -> users]
    P --> Q[for each user (auth0_id, user_data)]
    Q --> R{roles.intersection(user_roles)}
    R -->|empty| S[unchanged_count += 1]
    R -->|non-empty| T[changed_count += 1]
    T --> U[prepare payload with app_metadata.privileges = list(user_privs)]
    U --> V[management_api.update_user(auth0_id, payload)]
    Q --> W[sys.stdout write progress]
    subgraph ExternalModules
        DB_CONN[DB_CONN: fv.db.FvDatabaseConnector]
        roles_db[roles_db: iam_service.v1.db.roles]
        users_db[users_db: iam_service.v1.db.users]
        management_api[management_api: iam_service.vendor.auth0.management_api]
        secrets[SecretNames: fv.secrets]
    end
    DB_CONN -->|constructed with| secrets
    A -->|uses| ExternalModules
```

> SVG rendering failed for this diagram.
