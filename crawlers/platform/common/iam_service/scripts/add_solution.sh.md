# Diagram: common/iam_service/scripts/add_solution.sh


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
flowchart TD
  Start([Start]) --> SetVars[/"Set FEATURE, ORG_NAME, REPO_PATH"/]
  SetVars --> ExportPYTHONPATH[/"Export PYTHONPATH:\nplatform/common/iam_service\nplatform/common/fv/python"/]
  ExportPYTHONPATH --> Prompt[/"Prompt: Are you sure? (y/n)"/]
  Prompt --> Decision{Reply matches\n^[Yy]$?}
  Decision -- No --> Exit([Exit code 1])
  Decision -- Yes --> RunPy[/Run: python3 add_solution.py\nwith -s "${ORG_NAME}_${FEATURE}" -o "$ORG_NAME" -f "$FEATURE"/]
  RunPy --> End([End])
```

> SVG rendering failed for this diagram.
