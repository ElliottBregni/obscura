# Diagram: common/iam_service/scripts/common.py


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
flowchart LR
  A[get_all_users(organization_id)] --> B[Initialize all_users = []\npage = 0]
  B --> C{Loop: True}
  C --> D[Call management_api.get_all_users(page, organization_id)]
  D --> E[Receive paged_users (tuple/list)\npaged_users[1] = users_page]
  E --> F{paged_users[1] exists?}
  F -- Yes --> G[Append users_page to all_users]
  G --> H{len(users_page) < 100?}
  H -- Yes --> I[Break loop]
  H -- No --> J[page += 1\nContinue loop]
  F -- No --> I
  I --> K[Return all_users]
  C --> D
  
  subgraph OrgQuery [get_all_orgs(cursor)]
    O1[get_all_orgs(cursor)] --> O2[cursor.execute(SQL SELECT DISTINCT ... JOIN ...)]
    O2 --> O3[cursor.fetchall()]
    O3 --> O4[Return results]
  end
```

> SVG rendering failed for this diagram.
