# Diagram: common/iam_service/scripts/add_remove_certain_privs.py


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
flowchart TD
  A[Script Start\nimports: fv.db, fv.utilities,\nscripts.add_privilege (ap), SecretNames] --> B[DB_CONN = FvDatabaseConnector("add_remove_certain_privs", SecretNames.DATABASE)]
  B --> C[get_privileges_for_role(role)]
  B --> D[get_all_privs_no_sys()]
  C --> C1[open cursor\nexecute SELECT p.name ... WHERE r.name=%(role)s]
  C1 --> C2[return list of privilege names]
  D --> D1[open cursor\nexecute SELECT distinct p.name ... WHERE r.name not in SYSTEMS]
  D1 --> D2[return list of privilege names]
  E[__main__ sequence] --> E1[org_user_privs = get_privileges_for_role("ORGANIZATION_USER")]
  E1 --> E2[args = dotdict({privileges: join(org_user_privs), roles: "FINISHED_VEHICLE_ADC,REPORT_BUILDER_USER"})]
  E2 --> E3[ap.main(args)]
  E --> F1[fv_privs = get_privileges_for_role("FINISHED_VEHICLE_USER")]
  F1 --> F2[args = dotdict({privileges: join(fv_privs), roles: "FINISHED_VEHICLE_ADC"})]
  F2 --> F3[ap.main(args)]
  E --> G1[all_privs = get_all_privs_no_sys()]
  G1 --> G2[args = dotdict({privileges: join(all_privs), roles: "ORGANIZATION_ADMIN"})]
  G2 --> G3[ap.main(args)]
  style A fill:#f9f,stroke:#333,stroke-width:1px
  style B fill:#ffebcd
  style C fill:#e6f7ff
  style D fill:#e6f7ff
  style E fill:#e8ffe6
  classDef db fill:#fff2cc,stroke:#b8860b;
  class B db
```

> SVG rendering failed for this diagram.
