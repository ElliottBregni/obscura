# Diagram: common/iam_service/scripts/copy_users.py


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
flowchart LR
    A[Start: __main__] --> B[Set AWS_STAGE = "staging"]
    B --> C[get_configuration()]
    C --> D[fv.db.get_connection(config)]
    D --> E[fv.db.get_cursor(connection)]
    E --> F[Execute SELECT * FROM users WHERE last_name ILIKE '%API%' (staging)]
    F --> G[users = cursor.fetchall()]
    G --> H[Set AWS_STAGE = "test"]
    H --> I[get_configuration()]
    I --> J[fv.db.get_connection(config) (test)]
    J --> K[fv.db.get_cursor(connection) (test)]
    K --> L[Execute SELECT * FROM users WHERE last_name ILIKE '%API%' (test)]
    L --> M[users_s = cursor.fetchall()]
    M --> N[Execute "START TRANSACTION"]
    N --> O[Loop: for user in users]
    O --> P[user = user._asdict()]
    P --> Q[INSERT INTO users (...) VALUES (...) ON CONFLICT (email) DO UPDATE ... RETURNING *]
    Q --> R[Optional: count increment]
    R --> S[End Transaction / Commit implied]
    S --> T[End]
```

> SVG rendering failed for this diagram.
