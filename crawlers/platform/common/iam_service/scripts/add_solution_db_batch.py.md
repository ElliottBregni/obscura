# Diagram: common/iam_service/scripts/add_solution_db_batch.py


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
flowchart TD
  A[Start: __main__] --> B[parse_arguments()]
  B --> C[DB_CONN.get_cursor()]
  C --> D[get_org_fv_ids(cursor, from_feature)]
  D --> E{org_fv_ids empty?}
  E -- Yes --> F[Print info: No orgs found\nReturn]
  E -- No --> G[Print discovered summary]
  G --> H[Set failures = []]
  H --> I[For each fv_id in org_fv_ids]
  I --> J[Create AddSolutionParams(solution, org_id=fv_id, feature, remove)]
  J --> K[call add_solution_main(params)]
  K --> L{Exception?}
  L -- BadRequestError --> M[Log warn, append to failures, continue]
  L -- Other Exception --> N[Log warn with type, append to failures, continue]
  L -- No Exception --> O[Continue to next fv_id]
  O --> I
  M --> I
  N --> I
  I --> P{after loop: failures exist?}
  P -- Yes --> Q[Print summary with failures and list]
  P -- No --> R[Print summary completed successfully]
  Q --> S[End]
  R --> S[End]
```

> SVG rendering failed for this diagram.
