# Diagram: common/iam_service/api_documentation/IamService.yaml


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
classDiagram
    class IAM_Service {
        +title: "IAM Service APIs"
        +version: "1.0.0-oas3"
    }
    class Servers {
        +Dev
        +Dev2
        +QA
        +QA2
        +Test
        +Staging1
        +Prod-A
        +Prod-B
    }
    IAM_Service --> Servers

    %% Endpoints
    class Org_ExternalId_Path {
        +GET /organizations/{organizationId}/external_id/{externalIdType}/{externalId}
        -200: OrganizationMapping (example)
        -400,403
    }
    class Org_ExternalCodes {
        +GET /organizations/{organization_id}/external-code-definition
        -200: ExternalCodeDefinitionSchema
        -400,401,404,500
    }
    class Org_Mapping_List {
        +GET /organizations/external_mapping_list
        -200: OrganizationMappingListSchema
        -400,403
    }
    class User_Preference {
        +GET /v1/preference/
        -200: UserPreferenceResponseSchema
        -400,403,404
    }
    class Branding {
        +GET /branding
        +PUT /branding
        -GET 200: BrandingOptionsSchema
        -PUT 200/201: UserPreferenceResponseSchema
        -400,403,404,500
    }
    class Organizations {
        +GET /organizations
        -200: OrganizationListSchema or OrganizationListCondensedVersionSchema
        -400,403
    }
    class Create_Solution {
        +POST /create_solution
        -200,400,401,500
    }

    IAM_Service --> Org_ExternalId_Path
    IAM_Service --> Org_ExternalCodes
    IAM_Service --> Org_Mapping_List
    IAM_Service --> User_Preference
    IAM_Service --> Branding
    IAM_Service --> Organizations
    IAM_Service --> Create_Solution

    %% Schemas / Models
    class UserPreferenceInputSchema {
        +locale: string
        +dateformat: enum
        +timeformat: enum
        +timezone: string
    }
    class UserPreferenceResponseSchema {
        +locale: string
        +dateformat: enum
        +timeformat: enum
        +timezone: string
        +units_of_metrics: UnitsOfMeasurement
    }
    class UnitsOfMeasurement {
        +system: enum
        +mass: enum
        +pressure: enum
        +volume: enum
        +distance: enum
        +temperature: enum
    }
    class OrganizationMappingListSchema {
        +pageInfo: PageInfoSchema
        +data: OrganizationMappingItem[]
    }
    class OrganizationMappingItem {
        +name: string
        +code: string
    }
    class PageInfoSchema {
        +currentPage: integer
        +totalPages: integer
        +totalCount: integer
    }
    class ExternalCodeDefinitionSchema {
        +externalCodes: ExternalCodeItem[]
    }
    class ExternalCodeItem {
        +name: string
        +externalCode: string
    }
    class OrganizationListSchema {
        +organization_id: integer
        +org_name: string
        +org_type: string
        +fv_id: string
        +auth0_id: string
        +contact_name: string
        +deleted_at: string
        +map_type: string
        +phone_no: string
        +scac: string
        +email_address: string
        +no_of_user: integer
        +federated_org_ids: string[]
    }
    class OrganizationListCondensedVersionSchema {
        +organization_id: integer
        +org_name: string
        +org_type: string
        +fv_id: string
        +deleted_at: string
        +map_type: string
        +scac: string
        +no_of_locations: integer
        +count: integer
        +federated_org_ids: string[]
        +map_coordinates: MapCoordinates
    }
    class MapCoordinates {
        +lat: number
        +lng: number
    }
    class BrandingOptionsSchema {
        +code: string
        +name: string
        +imageURL: string
        +logoURL: string
    }

    %% Associations
    User_Preference --> UserPreferenceResponseSchema
    Branding --> BrandingOptionsSchema
    Branding --> UserPreferenceInputSchema
    Org_Mapping_List --> OrganizationMappingListSchema
    Org_ExternalCodes --> ExternalCodeDefinitionSchema
    Org_ExternalId_Path --> OrganizationMappingItem
    Organizations --> OrganizationListSchema
    Organizations --> OrganizationListCondensedVersionSchema
    UserPreferenceResponseSchema --> UnitsOfMeasurement
    OrganizationMappingListSchema --> PageInfoSchema
    OrganizationListCondensedVersionSchema --> MapCoordinates
    ExternalCodeDefinitionSchema --> ExternalCodeItem

    %% Security
    class Security {
        +BasicAuth: http basic
    }
    IAM_Service --> Security
```

> SVG rendering failed for this diagram.
