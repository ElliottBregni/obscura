# Diagram: common/iam_service/tests/__init__.py


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
flowchart TD
    M[Module: get_rand.py] --> I[imports: binascii, os]
    I --> F[get_rand(length)]
    F --> U[os.urandom(length) -> bytes]
    U --> B[binascii.b2a_hex(bytes) -> bytes_hex]
    B --> D[.decode("ascii") -> str]
    D --> R[return hex string]
```

> SVG rendering failed for this diagram.
