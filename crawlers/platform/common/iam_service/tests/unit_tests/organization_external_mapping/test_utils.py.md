# Diagram: common/iam_service/tests/unit_tests/organization_external_mapping/test_utils.py


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
flowchart TD
  Tests["Unit Tests (pytest & unittest)"] -->|calls| GQP[get_query_pagination(request)]
  Tests -->|calls| GRP[get_response_pagination(request, total_count)]
  subgraph get_query_pagination
    req_req[request.page_number, request.page_size]
    calc_offset["offset = page_number * page_size"]
    calc_limit["limit = page_size"]
    out_q["returns (offset, limit)"]
    req_req --> calc_offset
    req_req --> calc_limit
    calc_offset --> out_q
    calc_limit --> out_q
  end
  subgraph get_response_pagination
    req2_req[request.page_number, request.page_size, total_count]
    calc_totalPages["totalPages = ceil(total_count / page_size) if total_count>0 else 0"]
    build_pageInfo["pageInfo = {currentPage, totalCount, totalPages}"]
    out_r["returns pageInfo dict"]
    req2_req --> calc_totalPages
    req2_req --> build_pageInfo
    calc_totalPages --> build_pageInfo
    build_pageInfo --> out_r
  end
  GQP --> get_query_pagination
  GRP --> get_response_pagination
  Tests -->|asserts| Assertions["Asserts expected outputs for parameterized cases"]
```

> SVG rendering failed for this diagram.
