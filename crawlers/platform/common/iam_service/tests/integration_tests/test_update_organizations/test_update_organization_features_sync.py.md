# Diagram: common/iam_service/tests/integration_tests/test_update_organizations/test_update_organization_features_sync.py


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
classDiagram
class OrgType {
  <<enum>>
  SHIPPER
  CARRIER
  PARTNER
  DEALER
}
class Feature {
  <<enum (imported)>>
}
class unittest.TestCase
class UpdateOrganizationFeaturesSyncTestCase {
  +setUpClass()
  +setUp()
  +tearDown()
  +tearDownClass()
  +test_add_feature_on_update()
  +test_remove_feature_on_update()
  +test_add_and_remove_features_simultaneously()
  +test_remove_all_manageable_features()
  +test_no_change_when_features_not_in_payload()
  +test_features_idempotent_update()
  +_new_fv_id(prefix)
  +_get_profile_id_by_type(org_type)
  +_create_org(fv_id, org_type, scac)
  +_update_org(org_id, fv_id, features, features_to_remove)
  +_expected_external_id(fv_id, feature_name)
  +assertFeature(feature_name, fv_id, should_exist)
}
class FvDatabaseConnector {
  +establish_connection()
  +get_cursor()
}
class FakeLambdaEvent {
  +set_http_method(m)
  +set_organization_id(id)
  +set_org_profiles(list)
  +set_body(body)
  +update_path_parameters(params)
  +to_aws_event()
}
class LambdaContext {
  +LambdaContext(function_name)
}
class create_organizations_lambda
class update_organizations_lambda

unittest.TestCase <|-- UpdateOrganizationFeaturesSyncTestCase
UpdateOrganizationFeaturesSyncTestCase o-- FvDatabaseConnector : db_conn
UpdateOrganizationFeaturesSyncTestCase --> FakeLambdaEvent : builds events
UpdateOrganizationFeaturesSyncTestCase --> LambdaContext : invokes lambdas with context
UpdateOrganizationFeaturesSyncTestCase --> create_organizations_lambda : calls create_org_lambda
UpdateOrganizationFeaturesSyncTestCase --> update_organizations_lambda : calls update_org_lambda
UpdateOrganizationFeaturesSyncTestCase --> OrgType : uses
UpdateOrganizationFeaturesSyncTestCase --> Feature : uses
FvDatabaseConnector ..> "postgres organizations/solution/feature tables" : executes SQL queries
```

> SVG rendering failed for this diagram.
