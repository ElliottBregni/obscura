# Diagram: common/iam_service/tests/test_organization/test_org_features.py


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
classDiagram
    class FakeOrganizationFeaturesDAO {
        - current_features: dict
        + __init__()
        + get_current_features(organization_id, manageable_features)
        + set_current_features(organization_id, features)
    }

    class OrganizationFeaturesService {
        - dao
        - manageable_features
        - cursor
        + __init__(dao, manageable_features, cursor=None)
        + get_added_features(organization_id, new_features)
        + get_removed_features(organization_id, new_features)
        + set_removed_features(org_fv_id, org_id, new_features)
        + delete_solution(org_id, feature)
    }

    class TestOrganizationFeatureManager {
        + setUp()
        + test_added_features_return_empty_list_if_new_features_equal_preexisting_features()
        + test_added_features_do_not_return_new_features_that_are_not_manageable()
        + test_added_features_return_features_that_are_not_currently_enabled_and_are_manageable()
        + test_added_features_ignores_order()
        + test_added_features_return_empty_list_if_new_features_is_empty_list()
        + test_none_features_return_empty_list()
        + test_removed_features_return_empty_list_if_new_features_equal_preexisting_features()
        + test_removed_features_do_not_return_new_features_that_are_not_manageable()
        + test_removed_features_return_features_that_are_currently_enabled_and_are_manageable_and_not_in_new_features()
        + test_removed_features_ignores_order()
        + test_removed_features_returns_all_manageable_features_if_new_features_is_empty_list()
    }

    class TestSetRemovedFeatures {
        - manageable_features
        - dao
        - mock_cursor
        + setUp()
        + test_set_removed_features_deletes_features_in_both_current_and_new_list()
        + test_set_removed_features_returns_empty_list_when_no_features_to_remove()
        + test_set_removed_features_returns_empty_list_when_cursor_is_none()
        + test_set_removed_features_removes_multiple_features_when_in_both_lists()
    }

    FakeOrganizationFeaturesDAO --> OrganizationFeaturesService : provides DAO
    TestOrganizationFeatureManager --> FakeOrganizationFeaturesDAO : uses
    TestOrganizationFeatureManager --> OrganizationFeaturesService : constructs
    TestSetRemovedFeatures --> FakeOrganizationFeaturesDAO : uses
    TestSetRemovedFeatures --> OrganizationFeaturesService : constructs
    OrganizationFeaturesService o-- "feature_db, solution_db" : interacts with external DBs
```

> SVG rendering failed for this diagram.
