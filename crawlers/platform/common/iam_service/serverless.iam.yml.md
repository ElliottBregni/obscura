# Diagram: common/iam_service/serverless.iam.yml


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
flowchart TD
  subgraph AWS
    APIGW[API Gateway (iam base path)]
    subgraph Lambdas[Lambda Functions]
      get_user["get_user\n(create, update, delete, block/unblock,\norganizations, roles, prefs, tickets, etc)"]
    end
    IAMRole[IAM Role\n(managed policies + statements)]
    S3[S3 Bucket]
    SQS[SQS Queue]
    SNS[SNS Topic]
    Secrets[Secrets Manager]
    SSM[SSM Parameter Store]
    KMS[KMS Key]
    Datadog[Datadog]
    Auth0[Auth0]
  end

  APIGW -->|HTTP events| get_user
  get_user -->|assumed by| IAMRole
  IAMRole -->|allows| S3
  IAMRole -->|allows| SQS
  IAMRole -->|allows| SNS
  IAMRole -->|allows| Secrets
  IAMRole -->|allows get| SSM
  IAMRole -->|allows decrypt| KMS
  get_user -->|calls| Auth0
  get_user -->|publishes| SNS
  get_user -->|reads/writes| S3
  get_user -->|sends/receives| SQS
  get_user -->|gets secrets| Secrets
  get_user -->|reads params| SSM
  get_user -->|decrypts with| KMS
  get_user -->|reports traces| Datadog

  subgraph Policies
    DenyLogs["Deny logs: logs:CreateLogGroup/CreateLogStream/PutLogEvents\n(for prod-b/staging1/test patterns)"]
  end
  IAMRole --> Policies
  Datadog -.->|env vars configure| get_user
  Secrets -->|secret arn used for datadog| Datadog
  SQS -.->|event source (auth0 update)| get_user
  SNS -.->|CreateTopic/Publish allowed| IAMRole
```

> SVG rendering failed for this diagram.
