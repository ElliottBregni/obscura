# Diagram: common/iam_service/iam_service/v1/power_bi/utilities.py


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
flowchart TD
  subgraph API_calls
    A[get_reports(workspace_id, access_token)]
    F[generate_report_token(workspace_id, report_id, access_token, fv_id, core_dataset_id, report_mode)]
    N[generate_token(request_body, access_token)]
  end

  A -->|calls| ReqGet[requests.get(".../groups/{workspace_id}/reports", headers)]
  ReqGet --> CondGet{reports_res.status_code == 200}
  CondGet -->|true| ReturnReports[return reports_res.json()["value"]]
  CondGet -->|false| RaiseGet[raise fv.error.ForbiddenError(err_message)]

  F -->|calls| ReqPostGen[requests.post(".../reports/{report_id}/GenerateToken", json, headers)]
  ReqPostGen --> CondGen{token_res.status_code == 200}
  CondGen -->|true| ReturnGenToken[return token_res.json()["token"]]
  CondGen -->|false| RaiseGen[raise fv.error.ForbiddenError(err_message)]

  N -->|calls| ReqPostAll[requests.post(".../GenerateToken", json=request_body, headers)]
  ReqPostAll --> CondAll{token_res.status_code == 200}
  CondAll -->|true| ReturnAllToken[return token_res.json()["token"]]
  CondAll -->|false| RaiseAll[raise fv.error.ForbiddenError(err_message)]

  subgraph Builders
    K[generate_report_token_request_body(fv_id, reports)]
    K --> BuildLoop[for report in reports: determine report_mode; add report to request; add dataset to datasets; add identity]
    BuildLoop --> ReturnRequest[return request]
  end

  subgraph Validation
    S[check_source(source)]
    S --> Validate[if source not in [c.value for c in constants.Source]]
    Validate -->|invalid| RaiseBadReq[raise fv.error.BadRequestError(error)]
    Validate -->|valid| ReturnOk[return None]
  end

  %% shared resources
  constants[constants (BI_Mode, Source)]
  fv_error[fv.error (ForbiddenError, BadRequestError)]

  F -->|uses| constants
  K -->|uses| constants
  S -->|uses| constants

  RaiseGet --> fv_error
  RaiseGen --> fv_error
  RaiseAll --> fv_error
  RaiseBadReq --> fv_error
```

> SVG rendering failed for this diagram.
