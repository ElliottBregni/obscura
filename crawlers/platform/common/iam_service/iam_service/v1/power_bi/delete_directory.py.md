# Diagram: common/iam_service/iam_service/v1/power_bi/delete_directory.py


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
flowchart TD
    subgraph AWS_Lambda_Decorator
        MANDATORY["fv.aws.lambdas.mandatory_lambda_handling()"]
    end

    subgraph Lambda_Function
        LH[lambda_handler(event, context, audit_refs)]
    end

    MANDATORY --> LH
    Event[Event] --> LH

    LH --> |"cursor = DB_CONN.cursor"| DB_CONN[(DB_CONN: FvDatabaseConnector)]
    DB_CONN --> CURSOR[(cursor)]

    LH --> |"directory_number = get_path_parameter(event, 'directory_number')"| GetPath[get_path_parameter]
    LH --> |"organization_id = auth.get_organization_id(event)"| Auth[auth.get_organization_id]

    GetPath --> PID[directory_number]
    Auth --> ORG[organization_id]

    LH --> |"deleted_directory = process_delete_directory(cursor, directory_number, organization_id)"| Process[iam_service.v1.db.directory.process_delete_directory]
    CURSOR --> Process
    PID --> Process
    ORG --> Process

    Process --> Decision{deleted_directory ?}
    Decision -->|yes| Return["fv.aws.lambdas.make_response(deleted_directory._asdict(), 200)"]
    Decision -->|no| Error["raise fv.error.BadRequestError('Unable to delete directory')"]

    Return --> Response[HTTP 200 Response]
    Error --> Exception[BadRequestError raised]
```

> SVG rendering failed for this diagram.
