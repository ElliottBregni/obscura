# Diagram: common/iam_service/iam_service/v1/power_bi/email/update_email_schedule.py


> Auto-generated by Obscura crawlers

## Diagram 1

```mermaid
flowchart TD
  Start([Event received]) --> GetBody[get_event_body(event)]
  GetBody --> AuthOrg[get_organization_id(event)]
  GetBody --> AuthUser[get_user_id(event)]
  GetBody --> PathParams[get_path_parameter(report_number, filter_set_id, creator_id, schedule_name)]
  PathParams --> PatternDetails[patternDetails = event_body.patternDetails]
  PatternDetails --> ValidatePattern{pattern in Email_Pattern?}
  ValidatePattern -- No --> BadRequest1[[BadRequestError: invalid pattern]]
  ValidatePattern -- Yes --> Emails[event_body.email]
  Emails --> ParseEmails{is string?}
  ParseEmails -- Yes --> JSONParse[json.loads(emails)]
  ParseEmails -- No --> SkipParse
  JSONParse --> CheckFormat
  SkipParse --> CheckFormat
  CheckFormat{all emails valid?}
  CheckFormat -- No --> BadRequest2[[BadRequestError: invalid email format]]
  CheckFormat -- Yes --> Trim[emails = [e.strip()]]
  PatternDetails -->|if pattern| HandleSchedule[handle_schedule(pattern, patternDetails) -> next_send_ts]
  Trim --> UpdateDB[reporting_db.update_email_schedule(DB_CONN.cursor, ...)]
  UpdateDB -->|psycopg2.IntegrityError| IntegrityHandler[[BadRequestError: Integrity error]]
  UpdateDB --> MakeResponse[fv.aws.lambdas.make_response(event_body)]
  MakeResponse --> End([Return response])
```

> SVG rendering failed for this diagram.

## Diagram 2

```mermaid
sequenceDiagram
  participant Client
  participant Lambda as lambda_handler
  participant AWS as fv.aws.lambdas
  participant Auth as auth
  participant PBI as pbi_utilities
  participant DB as reporting_db
  participant Utils as fv.utilities
  participant Error as fv.error

  Client->>Lambda: HTTP event
  Lambda->>AWS: get_event_body(event)
  Lambda->>Auth: get_organization_id(event)
  Lambda->>Auth: get_user_id(event)
  Lambda->>AWS: get_path_parameter(...) (report_number, filter_set_id, creator_id, schedule_name)
  Lambda->>PBI: Email_Pattern lookup
  Lambda->>AWS: validate pattern; if invalid -> Error.BadRequestError
  Lambda->>AWS: parse emails (json.loads if string)
  Lambda->>Utils: check_email_format for each email
  Utils-->>Lambda: validation result
  alt emails invalid
    Lambda->>Error: raise BadRequestError
  else emails valid
    Lambda->>PBI: handle_schedule(pattern, patternDetails)
    PBI-->>Lambda: next_send_ts
    Lambda->>DB: update_email_schedule(..., next_send_ts, ...)
    alt IntegrityError
      DB-->>Lambda: psycopg2.IntegrityError
      Lambda->>Error: raise BadRequestError with traceback
    else success
      Lambda->>AWS: make_response(event_body)
      AWS-->>Client: response
    end
  end
```

> SVG rendering failed for this diagram.
