# Diagram: common/iam_service/iam_service/v1/power_bi/email/send_email.py


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
flowchart TD
    A[Lambda Trigger\n(event, context, audit_refs)] --> B[get_path_parameter(report_number)]
    A --> C[get_path_parameter(filter_set_id)]
    A --> D[get_event_body(event)]
    D --> E{email field type}
    E -->|string| F[try json.loads]
    F -->|invalid| G[raise BadRequestError]
    F -->|valid| H[emails list]
    E -->|list| H
    D --> I[emailBody]
    B --> J[build_email_link(report_number, filter_set_id, org_id)]
    D --> K[get_organization_id(event)\n(org_id)]
    K --> J
    A --> L[get_actor_email(event)\n(sender_email)]
    J --> M[handle_export_lambda(event, report_number, filter_set_id)\n(excel_url)]
    I --> N[create_email_body(emailBody, website_link, sender_email, download_url=excel_url)]
    H --> O[send_email(emails, body_html=email_body)]
    N --> O
    O --> P[make_response({})\n(return)]
    style A fill:#f9f,stroke:#333,stroke-width:2px
    style P fill:#9f9,stroke:#333,stroke-width:2px
```

> SVG rendering failed for this diagram.
