# Diagram: common/iam_service/iam_service/v1/power_bi/get_token.py


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
flowchart LR
  subgraph AWS_Lambda["fv.aws.lambdas"]
    LH[mandatory_lambda_handling]
    GL[get_privileges / get_path/query_parameter / make_response]
  end
  subgraph Secrets["fv.secrets"]
    SE[Secrets]
  end
  subgraph DB["Database"]
    DB_CONN[FvDatabaseConnector DB_CONN]
    CURSOR[(cursor)]
  end
  subgraph IAM["iam_service.v1"]
    ORG_DB[organization_db.get_organization_by_id]
    REP_DB[reporting_db.get_workspaces / get_reports / get_schedules_from_report / reports_to_return]
    FILTER_DB[report_filtering_db.get_reports_by_org / get_report_filter_by_id]
  end
  subgraph POWERBI["Power BI & Utilities"]
    PBI[pbi_util.get_access_token_user_pass]
    UTIL[utilities.get_reports / generate_report_token]
    CONST[constants.BI_Mode]
  end
  subgraph AUTH["fv.aws.lambdas.auth"]
    AUTH_PRIV[auth.Privilege / get_user_id / get_organization_id]
  end
  subgraph FV["fv.*"]
    FV_ERROR[fv.error (ForbiddenError/BadRequest/NotFound)]
    FV_DB[fv.db.FvDatabaseConnector]
  end

  event[Event / Context / audit_refs] --> LH
  LH --> lambda_handler[lambda_handler]
  lambda_handler --> AUTH_PRIV
  lambda_handler --> SE
  SE --> PB_CONF[SecretNames.POWER_BI -> pb_config]
  lambda_handler --> DB_CONN
  DB_CONN --> DB_CONN_establish[establish_connection]
  DB_CONN_establish --> CURSOR
  lambda_handler --> ORG_DB
  ORG_DB --> org_data[fv_id / org_type]
  org_data --> lambda_handler
  lambda_handler --> PBI
  lambda_handler --> UTIL
  lambda_handler --> AUTH_PRIV
  lambda_handler --> REP_DB
  lambda_handler --> FILTER_DB

  lambda_handler --> add_reports_flow[add_reports_by_org / add_reports_by_type]
  add_reports_flow --> UTIL
  add_reports_flow --> REP_DB
  add_reports_flow --> AUTH_PRIV
  add_reports_flow --> FV_ERROR

  add_reports_flow --> build_rep_to_ret
  build_rep_to_ret --> FILTER_DB
  build_rep_to_ret --> add_filter_results
  build_rep_to_ret --> add_email_schedule_counts
  build_rep_to_ret --> build_dict
  build_dict --> UTIL
  build_dict --> CONST
  add_filter_results --> FILTER_DB
  add_email_schedule_counts --> REP_DB

  build_rep_to_ret --> reporting_sort[reporting_db.reports_to_return & sorting]
  reporting_sort --> REP_DB

  build_rep_to_ret --> make_response[returns reports_to_ret]
  make_response --> GL
  GL --> response[HTTP 200 response]

  style AWS_Lambda fill:#f9f,stroke:#333,stroke-width:1px
  style DB fill:#ffefc6,stroke:#333,stroke-width:1px
  style IAM fill:#c6f9ff,stroke:#333,stroke-width:1px
  style POWERBI fill:#e6e6ff,stroke:#333,stroke-width:1px
  style AUTH fill:#ffd6e6,stroke:#333,stroke-width:1px
  style FV fill:#e6ffe6,stroke:#333,stroke-width:1px
```

> SVG rendering failed for this diagram.
