# Diagram: common/iam_service/iam_service/v1/power_bi/get_directory.py


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
flowchart LR
  Start([Start]) --> InitDB[/"Init DB_CONN = FvDatabaseConnector"/]
  InitDB --> Handler{{"lambda_handler(event, context, audit_refs)"}}
  Handler --> GetCursor[/"cursor = DB_CONN.cursor"/]
  Handler --> GetQueryParams[/"Get query/path params:\n- type\n- name\n- directory_number\n- level\n- reportNumbers"/]
  Handler --> GetAuth[/"Auth calls:\n- organization_id = auth.get_organization_id(event)\n- actor_id = auth.get_user_id(event)"/]
  GetAuth --> CanManage["can_access_core_report = can_manage_directory(event)"]
  GetCursor --> CallDB[/"Call process_get_directory(...)\n(including can_access_core_report)"/]
  CallDB --> CheckResult{directorys ?}
  CheckResult -->|yes| MapDict["dir_to_ret = [i._asdict() for i in directorys]"]
  MapDict --> Return200[/fv.aws.lambdas.make_response([...], 200)/]
  CheckResult -->|no| Return404[/fv.aws.lambdas.make_response(\"No Records found\", 404)/]
  Return200 --> End([End])
  Return404 --> End
  classDef ext fill:#f9f,stroke:#333,stroke-width:1px;
  class InitDB,GetAuth,CanManage,CallDB ext
```

> SVG rendering failed for this diagram.
