# Diagram: common/iam_service/iam_service/v1/common/organizations.py


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
graph TD
  A[Event + Organization] --> B[add_granted_locations]
  B -->|calls| Invok[invokinator_location.get_location_grants]
  B -->|builds map_org_to_loc| Map[map_org_to_loc]
  Map -->|produces| Formatted[formatted_grants]
  Formatted -->|sets| OrgOut[organization["granted_locations"]]
  OrgOut -->|returns| B

  C[Cursor + fv_id + Event] --> D[get_organization_for_event_stream]
  D -->|calls| E[get_custom_organization_sql_for_event_stream]
  E -->|returns (sql, replaces)| SQL[SQL string + params]
  SQL -->|executed by| DB[cursor.execute(sql, replaces)]
  DB -->|fetchone()| Result[result]
  Result -->|if not null| OrgJSON[return result.organization_json]
  Result -->|if null| Err[fv.error.NotFoundError]

  subgraph SQL_BUILD
    E --> Q[JSON_BUILD_OBJECT SELECT]
    Q --> Solutions[solutions subquery]
    Q --> ExternalCodes[externalOrgCodes subquery]
    Q --> GrantedLocations[grantedLocations subquery]
  end

  F[Cursor + EventBody + org_fv_id] --> G[add_granted_features]
  G -->|reads features| FB[event_body["features"]]
  G --> DAO1[organization_db.OrganizationFeaturesDAO(cursor)]
  DAO1 --> Svc1[OrganizationFeaturesService]
  Svc1 -->|set_added_features(event, org_fv_id, features)| Added[added_features]
  Added --> G

  H[Cursor + features_to_remove + org_fv_id + org_id] --> K[delete_granted_features]
  K --> DAO2[organization_db.OrganizationFeaturesDAO(cursor)]
  DAO2 --> Svc2[OrganizationFeaturesService(cursor=cursor)]
  Svc2 -->|set_removed_features(org_fv_id, org_id, features_to_remove)| Removed[removed_features]
  Removed --> K

  style SQL_BUILD stroke:#bbb,stroke-dasharray: 5 5
  classDef external fill:#f9f,stroke:#333,stroke-width:1px;
  class Invok,Err external
```

> SVG rendering failed for this diagram.
