# Diagram: common/iam_service/iam_service/v1/lambdas/auth0/auth0_user_update_consumer.py


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
flowchart LR
  Event[Incoming AWS Event]
  Records[For each Record in event.Records]
  Parse[Parse record body (json.loads)]
  Auth0Id[Extract auth0_id]
  CreateHandler[Instantiate Auth0UserUpdateEventHandler<br/>DB_CONN.establish_connection()]
  Process[handler.process(auth0_id)]
  TryBlock[Try: call sync_user(DB_CONN, auth0_id)]
  Sync[sync_user(DB_CONN, auth0_id)]
  Success[Success]
  NotFoundErr[fv.error.NotFoundError caught]
  Raise[Raise NotFoundError("User {auth0_id} not found")]
  MakeResp[make_response({}, HTTP 200)]
  Return[Return HTTP 200]

  Event --> Records
  Records --> Parse
  Parse --> Auth0Id
  Auth0Id --> CreateHandler
  CreateHandler --> Process
  Process --> TryBlock
  TryBlock --> Sync
  Sync -->|succeeds| Success
  Success --> MakeResp
  MakeResp --> Return

  Sync -->|raises NotFoundError| NotFoundErr
  NotFoundErr --> Raise
  Raise -->|propagates error| Return
```

> SVG rendering failed for this diagram.
