# Diagram: common/iam_service/iam_service/v1/lambdas/utilities/__init__.py


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
flowchart TD
    A[call: split_by_page(lst)] --> B[compute n = math.ceil(len(lst) / 100)]
    B --> C[build dict comprehension]
    C --> D[for i in range(n): slice = lst[i*100:(i+1)*100]]
    D --> E{add entry i: slice to result dict}
    E --> F[return dict of pages]
```

> SVG rendering failed for this diagram.
