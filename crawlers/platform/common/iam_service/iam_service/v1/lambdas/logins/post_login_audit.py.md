# Diagram: common/iam_service/iam_service/v1/lambdas/logins/post_login_audit.py


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
flowchart TD
  Start([Start: lambda_handler invoked])
  A1[auth.get_organization_id(event)]
  A2[auth.get_actor_email(event)]
  A3[auth.get_user_phone_num(event)]
  A4[auth.get_privileges(event)]
  A5[auth.get_user_id(event)]
  T1[login_at = datetime.utcnow()]
  DB[DB_CONN.establish_connection()]
  CURS[cursor = DB_CONN.cursor]
  DecidePriv{privileges?}
  GetRole[auth.get_role_from_privs(cursor, privileges)]
  DecidePhone{phone_number?}
  Sanitize[phone_number = phone_number.replace("-", "")]
  UpdatePhone[/update_phone_number(cursor, email, phone_number)/]
  CreateLogin[logins_db.create_login_record(cursor, organization_id, user_name, user_role, login_at, auth0_id)]
  CheckRecord{login_record exists?}
  Error[/raise fv.error.UnhandledException/]
  Return[/fv.aws.lambdas.make_response(login_record)/]
  End([End])

  Start --> A1 --> A2 --> A3 --> A4 --> A5 --> T1 --> DB --> CURS
  CURS --> DecidePriv
  DecidePriv -- yes --> GetRole --> SetRole[set user_role] --> CURS
  DecidePriv -- no --> SkipRole[set user_role = None] --> CURS
  CURS --> DecidePhone
  DecidePhone -- yes --> Sanitize --> UpdatePhone --> CreateLogin
  DecidePhone -- no --> CreateLogin
  CreateLogin --> CheckRecord
  CheckRecord -- no --> Error --> End
  CheckRecord -- yes --> Return --> End

  subgraph update_phone_number_function [update_phone_number(cursor, email, phone_number)]
    UQ1["SQL: UPDATE \"user\" SET phone_number=%s WHERE email=%s"]
  end
  UpdatePhone --> UQ1
```

> SVG rendering failed for this diagram.
