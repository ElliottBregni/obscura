# Diagram: common/iam_service/iam_service/v1/lambdas/iam_temp_solution_sync.py


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
graph TD
  FV[FV Package]
  FV_DB[fv.db]
  FV_AWS[fv.aws.lambdas]
  MGMT[iam_service.vendor.auth0.management_api]
  SECRETS[SecretNames]
  DB_CONN["DB_CONN: FvDatabaseConnector('solution-sync')"]
  LH[lambda_handler(event, context, audit_refs)]
  EST[DB_CONN.establish_connection()]
  CURSOR[DB_CONN.cursor]
  GETSHIP[get_shipper_org_ids(cursor)]
  GETALL[get_all_users_by_shipper_org(cursor)]
  GETALLUSERS["management_api.get_all_users(organization_id, all_users=True)"]
  USERDICT[user_data_to_dict(users)]
  PARSE[parseData(auth, cursor)]
  UPDATE["management_api.update_user(user_id, payload)"]
  MAKE["fv.aws.lambdas.make_response({...}, 200)"]
  REQ_AUDIT["fv.aws.lambdas.request_audit(event, context)"]

  FV -->|contains| FV_DB
  FV -->|contains| FV_AWS
  LH --> REQ_AUDIT
  LH --> DB_CONN
  DB_CONN --> FV_DB
  DB_CONN --> SECRETS
  DB_CONN --> EST
  EST --> CURSOR
  LH -->|calls| EST
  LH -->|uses cursor| CURSOR
  LH --> GETALL
  GETALL --> GETSHIP
  GETSHIP -->|executes SQL on| CURSOR
  GETALL --> GETALLUSERS
  GETALLUSERS --> MGMT
  GETALL -->|returns| PARSE
  PARSE --> CURSOR
  PARSE --> USERDICT
  USERDICT -->|builds dict from| PARSE
  PARSE --> UPDATE
  UPDATE --> MGMT
  LH --> MAKE
  FV_AWS --> REQ_AUDIT
  FV_AWS --> MAKE
```

> SVG rendering failed for this diagram.
