# Diagram: common/iam_service/iam_service/v1/lambdas/solution/create_solution.py


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
flowchart TD
  Start([Lambda invoked])
  A[Establish DB connection]
  B{Get event body}
  B_no[Raise BadRequestError: "Invalid payload: body is null"]
  C[Extract fvId, featureName, solutionExternalId]
  D{fvId and featureName present?}
  D_no[Raise BadRequestError: "Invalid payload: fv_id/feature_name does not exist"]
  E[organization_db.get_org_by_fv_id(cursor, fvId)]
  E_no[Raise NotFoundError: "organization does not exist for fv_id"]
  F[org_id = organization.id]
  G[feature_db.get_feature_id_by_name(cursor, featureName)]
  G_no[Raise NotFoundError: "feature name not found"]
  H[new_record = solution_db.create_solution(cursor, feature_id, org_id, solutionExternalId)]
  I[formatted_record = format_solution(new_record)]
  J[users = users_db.get_organization_users(cursor, int(org_id))]
  K[data_to_publish_to_queue = map users to auth0_id/email objects]
  L[Create Auth0UserUpdateEventSender with data]
  M[sender.send()]
  N[Return HTTP 200 with formatted_record]
  Z[UnhandledException -> raise fv.error.UnhandledException]

  Start --> A --> B
  B -- no --> B_no --> Z
  B -- yes --> C --> D
  D -- no --> D_no --> Z
  D -- yes --> E
  E -- not found --> E_no --> Z
  E -- found --> F --> G
  G -- not found --> G_no --> Z
  G -- found --> H --> I --> J --> K --> L --> M --> N
  subgraph ErrorHandling
    Z
  end
```

> SVG rendering failed for this diagram.
