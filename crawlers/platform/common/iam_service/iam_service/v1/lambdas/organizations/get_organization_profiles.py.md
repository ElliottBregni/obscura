# Diagram: common/iam_service/iam_service/v1/lambdas/organizations/get_organization_profiles.py


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
flowchart TD
  A[Imports: fv.aws.lambdas<br/>fv.db<br/>fv.log<br/>iam_service.v1.db.organization<br/>fv.secrets] --> B[DB_CONN = FvDatabaseConnector("get_orginization_profiles", SecretNames.DATABASE)]
  B --> C{lambda_handler invoked}
  C --> D[Decorator: mandatory_lambda_handling(privs=MANAGE_USERS)]
  C --> E[DB_CONN.establish_connection()]
  E --> F[cursor = DB_CONN.cursor]
  F --> G[response = organization_db.get_all_organization_types(cursor)]
  G --> H[response = organization_db.structure_organization_profiles(response)]
  H --> I[result_payload = {"response": response}]
  I --> J[return fv.aws.lambdas.make_response(result_payload, 200)]
  D -.-> C
```

> SVG rendering failed for this diagram.
