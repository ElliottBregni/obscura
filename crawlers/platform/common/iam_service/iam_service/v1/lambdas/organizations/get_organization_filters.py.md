# Diagram: common/iam_service/iam_service/v1/lambdas/organizations/get_organization_filters.py


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
flowchart LR
  Start([Start])
  A[Establish DB_CONN_ENTITY_TRACKING]
  B[cursor = DB_CONN_ENTITY_TRACKING.cursor]
  C[Get user_org_types via auth.get_user_org_types(event)]
  D[organization_id = event.requestContext.authorizer.organization_id]
  E{organization_id exists?}
  F[Establish DB_CONN_SHIPMENTS_TRACKING]
  G[shipments_cursor = DB_CONN_SHIPMENTS_TRACKING.cursor]
  H{User is DEALER?}
  I[get_fv_org_id_dealer(cursor, organization_id)]
  J[get_fv_org_id(shipments_cursor, organization_id)]
  K[get_org_filters(cursor, fv_org_id)]
  L[Make response: {"org_filters": org_filters}, 200]
  End([End])

  Start --> A --> B --> C --> D --> E
  E -- no --> Kx([org_filters undefined]) --> L --> End
  E -- yes --> F --> G --> H
  H -- yes --> I --> K
  H -- no --> J --> K
  K --> L --> End

  subgraph DB_Resources
    DB1[DB_CONN_ENTITY_TRACKING (FvDatabaseConnector)]
    DB2[DB_CONN_SHIPMENTS_TRACKING (FvDatabaseConnector)]
    DBOrg[public.organization_filter table]
    DBOrgs[public.organizations table]
    DBLocation[LOCATION.LOCATION, LOCATION.organizations, LOCATION.location_grant]
  end

  A --> DB1
  F --> DB2
  I --> DBLocation
  J --> DBOrgs
  K --> DBOrg
```

> SVG rendering failed for this diagram.
