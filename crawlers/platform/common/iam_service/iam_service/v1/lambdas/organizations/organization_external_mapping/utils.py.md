# Diagram: common/iam_service/iam_service/v1/lambdas/organizations/organization_external_mapping/utils.py


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
flowchart LR
  Req[OrganizationExternalMappingListRequest]
  Req -->|page_number, page_size| Q[get_query_pagination(request)]
  Q -->|offset = page_number * page_size| Offset[offset]
  Q -->|limit = page_size| Limit[limit]
  Req -->|page_number, page_size, total_count| R[get_response_pagination(request, total_count)]
  R -->|total_pages = ceil(total_count / request.page_size)| TotalPages[total_pages]
  R -->|page_info = {currentPage, totalCount, totalPages}| PageInfo[page_info (dict)]
  Offset -->|returned| Q
  Limit -->|returned| Q
  PageInfo -->|returned| R
```

> SVG rendering failed for this diagram.
