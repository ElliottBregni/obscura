# Diagram: common/iam_service/iam_service/v1/lambdas/organizations/update_federated_org.py


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
flowchart LR
  A[lambda_handler(event)] --> B[gather_and_grab_all_locs]
  B --> C[get_locations(granter, loc_code)]
  C --> D[gather_data(filtered_list, loc_code_dict, ...)]
  D -->|new org needed| E[add_or_update_org]
  E --> F[insert_to_federation]
  F --> G[insert_solution_for_dda]
  D -->|location missing| H[create_loc_no_exist]
  H --> C
  D --> I[get_and_update_location]
  I --> J[locations-put lambda]
  H --> K[locations-post lambda]
  D --> L[org_loc_dict -> org_id]
  L --> M[get_granted_locs(grantee_id)]
  M --> N[process_locs(locations, existing_granted_locs)]
  N -->|to add| O[invokinator_location.add_location_grants]
  N -->|to remove| P[invokinator_location.delete_location_grants]
  O --> Q[invokinator_organization.invoke_publish_organization]
  P --> Q
  Q --> R[does_grant_exist(org_id)]
  R -->|no grants| S[remove_federation_association]
  S --> T[delete_org_if_no_fed_assoc]
  note right of D: also calls get_all_fed_org_details, form_location_references, get_organization_lad_id_dealer
  subgraph DB
    F
    G
    M
    R
    S
    T
  end
```

> SVG rendering failed for this diagram.
