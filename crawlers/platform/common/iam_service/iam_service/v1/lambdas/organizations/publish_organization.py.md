# Diagram: common/iam_service/iam_service/v1/lambdas/organizations/publish_organization.py


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
flowchart LR
  LH[lambda_handler(event, context, audit_refs)]
  DB[DB_CONN.establish_connection()]
  CURSOR[cursor = DB_CONN.cursor]
  PUB[publish_organization(event, cursor)]
  EVBODY[get_event_body(event) -> content]
  CHECK_CONTENT{content length != 0}
  FV_ID[fv_id = content.get("fv_id")]
  NO_INPUT1[BadRequestError: No valid inputs found]
  HTTP[get_http_method(event) -> http_method]
  CHECK_HTTP{http_method is not None}
  NO_INPUT2[BadRequestError: No valid inputs found]
  CONDITIONAL{http_method != "DELETE"}
  GET_ORG[iam_service.get_organization_for_event_stream(cursor, fv_id, event) -> retval]
  PUBLISH[publish_event_to_queue(retval, fv_id, http_method)]
  RESPONSE[fv.aws.lambdas.make_response(retval, 200) -> return]
  NAME_ERR[NameError -> raise NotFoundError]
  DB_ERR[psycopg2.Error -> raise DatabaseError]

  LH --> DB --> CURSOR --> PUB
  PUB --> EVBODY --> CHECK_CONTENT
  CHECK_CONTENT -->|yes| FV_ID
  CHECK_CONTENT -->|no| NO_INPUT1
  FV_ID --> HTTP --> CHECK_HTTP
  CHECK_HTTP -->|yes| CONDITIONAL
  CHECK_HTTP -->|no| NO_INPUT2
  CONDITIONAL -->|true| GET_ORG --> PUBLISH --> RESPONSE
  CONDITIONAL -->|false| PUBLISH --> RESPONSE
  PUB -->|NameError| NAME_ERR
  PUB -->|psycopg2.Error| DB_ERR
```

> SVG rendering failed for this diagram.
