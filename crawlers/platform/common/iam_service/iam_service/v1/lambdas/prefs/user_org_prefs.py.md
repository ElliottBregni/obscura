# Diagram: common/iam_service/iam_service/v1/lambdas/prefs/user_org_prefs.py


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
flowchart LR
  A[lambda_handler(event, context, audit_refs)] --> B[establish DB_CONN]
  A --> C{path contains "/org-pref"?}
  C -- yes --> D[user_auth0_id = "Organization"]
  C -- no --> E[user_auth0_id from event]
  A --> F{httpMethod in CUD?}
  F -- yes --> G[create_update_pref(event, cursor, org_id, user_auth0_id)]
  F -- no --> H[read_pref(event, cursor, org_id, user_auth0_id)]
  H --> H1[get_path_parameter(key, required=false)]
  H1 --> H2{key_pref is None?}
  H2 -- yes --> H3[prefs_db.get_pref_keys_by_org_user_id(cursor, org_id, actor_id)]
  H2 -- no --> H4[prefs_db.get_pref_by_org_user_id_access_key(cursor, org_id, actor_id, key_pref)]
  G --> G1[get_path_parameter(key, required=true)]
  G1 --> G2[determine http_put from httpMethod]
  G2 --> G3[fv.aws.lambdas.get_event_body(event) -> json_data]
  G3 --> G4[set status = ACTIVE or INACTIVE if DELETE]
  G4 --> V[validate_key(http_put, cursor, org_id, key_pref, user_auth0_id)]
  V --> V1[prefs_db.check_user_existing_entity(cursor, org_id, actor_id, key_pref)]
  V1 --> V2{http_put and user_check == False?}
  V1 --> V3{http_put == False and user_check == True?}
  V2 -- true --> X[raise BadRequestError("Invalid preference key")]
  V3 -- true --> Y[raise BadRequestError("Key already found. Use PUT Instead")]
  V2 -- false --> V4[continue]
  V3 -- false --> V4
  V4 --> P[prefs_db.persist_pref(cursor, status, actor_id, organization_id, key_pref, active_prefs)]
  P --> Q{httpMethod == DELETE?}
  Q -- yes --> R[return make_response([], 204)]
  Q -- no --> S[prefs_db.get_pref_by_org_user_id_access_key(... )]
  S --> T[return make_response(response, 200)]
```

> SVG rendering failed for this diagram.
