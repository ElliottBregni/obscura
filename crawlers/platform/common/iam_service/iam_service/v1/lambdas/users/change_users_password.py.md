# Diagram: common/iam_service/iam_service/v1/lambdas/users/change_users_password.py


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
flowchart TD
  A([lambda_handler(event, context, audit_refs)]) --> B[Unquote param_user_id from pathParameters]
  B --> C[Extract user_org and user_id from requestContext.authorizer]
  C --> D[Parse json_data from event.body]
  D --> E{Is user_id == param_user_id\nor privilege CHANGE_USER_PASSWORDS present?}
  E -->|No| F[Raise ForbiddenError: no permission]
  E -->|Yes| G{Is user_id truthy?}
  G -->|No| H[Raise BadRequestError: invalid user id]
  G -->|Yes| I{Is json_data a dict\nand keys == {"password"}?}
  I -->|No| J[Raise BadRequestError: invalid request body]
  I -->|Yes| K[Call management_api.get_user(param_user_id)]
  K --> L{status_code == HTTP 200?}
  L -->|No| M[Return make_response({"response": response}, status_code)]
  L -->|Yes| N[Create payload with password and call management_api.update_user(param_user_id, payload)]
  N --> O{(status_code from get_user) == HTTP 200?}
  O -->|No| P[response_message = "Invalid request: Error while changing user's password"]
  O -->|Yes| Q[response_message = "User's password was successfully changed"]
  P --> R[Return make_response({"response": response_message}, response.status_code)]
  Q --> R
  F --> S((End))
  H --> S
  J --> S
```

> SVG rendering failed for this diagram.
