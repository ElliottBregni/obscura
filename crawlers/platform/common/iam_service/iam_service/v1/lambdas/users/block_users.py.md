# Diagram: common/iam_service/iam_service/v1/lambdas/users/block_users.py


> Auto-generated by Obscura crawlers

## Diagram 1

```mermaid
flowchart TD
  A[API Gateway / Client] -->|invoke event| L(lambda_handler)
  L --> DB_CONN[ShipmentDatabase.connect]
  L --> U[user_id = unquote(pathParameters.user_id)]
  L --> B[json_data = event.body]
  B -->|null| Err1[BadRequestError: Missing organization_id]
  B -->|has body| C{has organization_id and user_id?}
  C -->|no| Err2[BadRequestError: missing organization_id or user_id]
  C -->|yes| Call[call change_user_organization_status(DB_CONN, org_id, False, user_id)]
  Call --> IAM[iam_service.v1.lambdas.users.change_user_organization_status]
  IAM -->|success| Resp[fv.aws.lambdas.make_response({"response":"user blocked"},200)]
  Call -->|exception| Log[logging.exception("User failed to be disabled.")]
  Log --> Err3[BadRequestError: User failed to be disabled.]
  Resp --> A
```

> SVG rendering failed for this diagram.

## Diagram 2

```mermaid
sequenceDiagram
  participant Client as Client/API
  participant Lambda as lambda_handler
  participant DB as ShipmentDatabase
  participant IAM as change_user_organization_status
  participant Logger as logging

  Client->>Lambda: invoke(event, context, audit_refs)
  Lambda->>DB: establish_connection()
  Lambda->>Lambda: user_id = unquote(event.pathParameters.user_id)
  Lambda->>Lambda: audit_refs.update(USER_ID:user_id)
  Lambda->>Lambda: json_data = event.body
  alt missing body
    Lambda->>Logger: raise BadRequestError("Missing organization_id")
    Logger-->>Client: 400 Bad Request
  else invalid payload
    Lambda->>Logger: raise BadRequestError("missing organization_id or user_id")
    Logger-->>Client: 400 Bad Request
  else valid
    Lambda->>IAM: change_user_organization_status(DB, org_id, False, user_id)
    alt success
      IAM-->>Lambda: success
      Lambda->>Client: 200 {"response":"user blocked"}
    else exception
      IAM-->>Lambda: throws Exception
      Lambda->>Logger: logging.exception("User failed to be disabled.")
      Lambda->>Client: 400 Bad Request
  end
```

> SVG rendering failed for this diagram.
