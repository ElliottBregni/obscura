# Diagram: common/iam_service/iam_service/v1/lambdas/users/create_users.py


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
flowchart TD
  subgraph Initialization
    AUTH0_CONN["AUTH0_CONNECTION (env)"]
    AUTH0_PW["AUTH0_INITIAL_USER_PASSWORD (env)"]
    DB_CONN["DB_CONN: ShipmentDatabase('create_users')"]
    FV_ORG_ID["fv_organization_id <- organization_db.get_org_by_fv_id(...)"]
  end

  subgraph Utilities
    reverse_roles["reverse_roles(roles)"]
    parse_txt_csv["parse_txt_csv(csv_text)"]
    does_group_exist["does_group_exist(groups,name)"]
    can_create["can_create(allowed_ids, allowed_names, create_roles, all_roles, fv_admin)"]
  end

  subgraph UserCreation
    create_users["create_users(...)"]
    create_user_fn["iam_service.v1.lambdas.users.create_user"]
    get_auth0_user["get_auth0_user_by_id"]
    user_has_org["user_has_organization_in_appmeta"]
    assign_dv["assign_damageview_locations"]
  end

  subgraph Lambda
    lambda_handler["lambda_handler(event,context,audit_refs)"]
    get_event_body["fv.aws.lambdas.get_event_body(event)"]
    make_response["fv.aws.lambdas.make_response(...)"]
  end

  %% Initialization flows
  DB_CONN -->|establish_connection| DB_CONN
  DB_CONN --> FV_ORG_ID

  %% Lambda flow
  lambda_handler --> get_event_body
  get_event_body --> parse_txt_csv
  lambda_handler --> get_auth0_user
  lambda_handler --> user_has_org
  lambda_handler --> roles_db_get["roles_db.get_all_roles(cursor,user_org)"]
  roles_db_get --> reverse_roles
  reverse_roles --> create_users
  parse_txt_csv --> create_users
  lambda_handler --> create_users

  %% create_users internals
  create_users --> can_create
  can_create -->|on allowed| create_user_fn
  create_users -->|calls| create_user_fn
  create_users -->|may call| assign_dv
  create_users -->|handles ConflictError| conflict["ConflictError -> pass"]

  %% external checks and final response
  lambda_handler --> make_response
  make_response --> HTTP[ "HTTP 201 CREATED" ]

  %% utility mapping
  get_auth0_user --> user_has_org
  user_has_org --> fv_admin_decision["determine fv_admin_user (bool)"]
  fv_admin_decision --> can_create

  style DB_CONN fill:#f9f,stroke:#333,stroke-width:1px
  style lambda_handler fill:#bbf,stroke:#333,stroke-width:1px
  style create_users fill:#bfb,stroke:#333,stroke-width:1px
```

> SVG rendering failed for this diagram.
