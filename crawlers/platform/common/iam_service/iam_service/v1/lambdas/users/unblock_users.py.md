# Diagram: common/iam_service/iam_service/v1/lambdas/users/unblock_users.py


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
flowchart LR
  Start([Start])
  A[establish DB connection]
  B[extract user_id from pathParameters]
  C[update audit_refs with user_id]
  D[parse json_data from event.body]
  E{json_data is None?}
  F[raise BadRequestError: Missing organization_id]
  G{user_id missing OR organization_id not in json_data?}
  H[raise BadRequestError: Missing organization_id or user_id]
  I[call change_user_organization_status(DB_CONN, organization_id, True, user_id)]
  J[return 200 response: {"response":"user unblocked"}]
  K[exception caught]
  L[log exception and raise BadRequestError: User failed to be enabled]
  End([End])

  Start --> A --> B --> C --> D --> E
  E -- yes --> F --> End
  E -- no --> G
  G -- yes --> H --> End
  G -- no --> I
  I --> J --> End
  I --> K --> L --> End
```

> SVG rendering failed for this diagram.
