# Diagram: common/iam_service/iam_service/v1/db/solution.py


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
flowchart LR
  subgraph Functions
    GS[get_solution(solution_id, include_shipper_name=False)]
    GB[get_solution_by_org_feature(organization_id, feature_name)]
    CS[create_solution(feature_id, org_id, solution_external_id)]
    DS[delete_solution_by_org_and_feature(org_id, feature_id)]
  end

  subgraph DB["Database"]
    SOL[solution]
    FEAT[feature]
    ORG[organizations]
  end

  Cursor[(cursor)]
  Logger[(logging)]

  GS -->|exec SELECT JOIN| Cursor
  GS -->|reads| SOL
  GS -->|joins on feature_id| FEAT
  GS -->|optional JOIN when include_shipper_name| ORG

  GB -->|mogrify & exec SELECT JSON| Cursor
  GB -->|reads| SOL
  GB -->|joins on feature_id and filters by name/org| FEAT
  GB --> Logger

  CS -->|exec INSERT RETURNING| Cursor
  CS -->|writes to| SOL

  DS -->|exec DELETE RETURNING| Cursor
  DS -->|deletes from| SOL

  Cursor --> SOL
  Cursor --> FEAT
  Cursor --> ORG
```

> SVG rendering failed for this diagram.
