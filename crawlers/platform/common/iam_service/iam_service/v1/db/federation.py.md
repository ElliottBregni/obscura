# Diagram: common/iam_service/iam_service/v1/db/federation.py


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
flowchart TD
  A[update_federation_association_details(federator_details)] --> B{extract federated_org_id from federator_details.bac}
  B --> C[does_federation_exist(cursor, federated_org_id)]
  C -->|exists| D[parse good_through -> expires_on]
  D --> E[UPDATE federation_association SET federator_details, expires_on WHERE federated_org_id]
  C -->|not exists| F[INSERT INTO federated_org_whitelist (federated_org_id, federator_details) ON CONFLICT DO UPDATE]
  A -->|logs query| G[logging.info(cursor.mogrify(...).decode())]
  subgraph DoesExist
    H[does_federation_exist(cursor, federated_org_id)] --> H1[execute SELECT id FROM federation_association WHERE federated_org_id = %(federated_org_id)s]
    H1 --> H2{row returned?}
    H2 -->|no| H3[return None]
    H2 -->|yes| H4[map column_names to row -> return dict]
  end
  subgraph GetDetails
    I[get_federation_association_details(cursor)] --> I1[execute SELECT federator_details FROM federation_association WHERE federator_details IS NOT NULL]
    I1 --> I2{rows returned?}
    I2 -->|no| I3[return []]
    I2 -->|yes| I4[map column_names to each row -> return list of dicts]
  end
  subgraph IsExpired
    J[is_federation_access_expired(cursor, federated_org_id)] --> J1[execute SELECT CAST(CASE WHEN expires_on > now() THEN TRUE ELSE FALSE END AS BOOLEAN) AS expired FROM federation_association WHERE federated_org_id = %(federated_org_id)s]
    J1 --> J2{row returned?}
    J2 -->|no| J3[return False]
    J2 -->|yes| J4[map column_names to row -> result]
    J4 --> J5{return True if result.expired == True else False}
  end
  E --> G
  F --> G
```

> SVG rendering failed for this diagram.
