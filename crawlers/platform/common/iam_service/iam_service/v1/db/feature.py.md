# Diagram: common/iam_service/iam_service/v1/db/feature.py


> Auto-generated by Obscura crawlers

## Diagram 1

```mermaid
flowchart TD
    A[Caller] -->|call get_features(org_id, is_admin)| B[get_features]
    B --> C{is_admin?}
    C -->|False| D[append "AND admin_only=false"]
    C -->|True| E[no admin filter]
    D --> F[construct SQL with org_id]
    E --> F
    F --> G[cursor.execute(SQL, [org_id])]
    G --> H[cursor.fetchall()]
    H --> I[map rows to dicts]
    I --> J[return list of feature dicts]
```

> SVG rendering failed for this diagram.

## Diagram 2

```mermaid
sequenceDiagram
    participant Caller
    participant get_features as GF
    participant get_feature_id_by_name as GID
    participant Cursor

    Caller->>GF: get_features(cursor, org_id, is_admin)
    GF->>Cursor: execute(SQL JOIN feature/solution WHERE organizations_id=%s and admin_only filter)
    Cursor-->>GF: rows
    GF->>GF: map rows to dicts
    GF-->>Caller: return list(dict)

    Caller->>GID: get_feature_id_by_name(cursor, feature_name)
    GID->>GID: validate parameters
    alt cursor missing
        GID-->>Caller: raise Exception("Parameter 'cursor' is required.")
    else feature_name empty
        GID-->>Caller: return None
    else
        GID->>Cursor: execute(SELECT f.id WHERE is_active=true AND name=%(feature_name)s)
        Cursor-->>GID: row or None
        GID-->>Caller: return row.id or None
```

> SVG rendering failed for this diagram.
