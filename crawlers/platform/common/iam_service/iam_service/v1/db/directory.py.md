# Diagram: common/iam_service/iam_service/v1/db/directory.py


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
flowchart TD
    PAD[process_add_directory()] -->|mogrify/execute insert| Dir[reporting.directory]
    PPD[process_patch_directory()] -->|mogrify/execute update| Dir
    PDD[process_delete_directory()] -->|cte update reporting.directory_report| DirReport[reporting.directory_report]
    PDD -->|delete reporting.directory| Dir
    AMR[add_move_report_to_directory()] -->|insert on conflict -> upsert| DirReport
    PGD[process_get_directory()] -->|select d.*| Dir
    ISD[is_sub_my_report_dir()] -->|select true as is_owner| Dir
    IMD[is_my_report_dir()] -->|select * and check type == "my_reports"| Dir
    MUR[move_report_from_update_filter_modal()] -->|update reporting.directory_report set directory_id| DirReport

    Cursor((cursor)) --- PAD
    Cursor --- PPD
    Cursor --- PDD
    Cursor --- AMR
    Cursor --- PGD
    Cursor --- ISD
    Cursor --- IMD
    Cursor --- MUR

    DirReport -->|references| Dir
    Dir -->|has fields| DirFields[(id, type, level, parent_id, creator_org_id, creator_actor_id, name, description)]
    DirReport -->|has fields| DirReportFields[(directory_id, report_id, filter_set_id, creator_org_id)]
```

> SVG rendering failed for this diagram.
