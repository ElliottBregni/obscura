# Diagram: common/support_service/support_service/get_language_templates.py


> Auto-generated by Obscura crawlers

## Diagram 1

```mermaid
flowchart LR
  Event[Incoming event] -->|invoke| LambdaHandler[lambda_handler(event, context, audit_refs)]
  LambdaHandler --> GetHeader[get_header(event, "accept-language") or DEFAULT_LANGUAGE]
  GetHeader --> NormalizeLang{"language contains '-'"}
  NormalizeLang -- Yes --> SplitLang[language.split("-")[0]]
  NormalizeLang -- No --> KeepLang[language]
  SplitLang --> BuildRequested[requested_file = f"nav/labels_{language}.json"]
  KeepLang --> BuildRequested
  BuildRequested --> GetContent[get_content(requested_file)]
  GetContent --> S3Resource[boto3.resource("s3")]
  S3Resource --> BucketName[get_bucket_name_for_stage(os.environ.get("AWS_STAGE"))]
  BucketName --> TryGet[Try: s3.Object(bucket_name, requested_file).get()]
  TryGet -->|success| ReadBody[byte_content = obj.get()["Body"].read()]
  ReadBody --> Decode[content = byte_content.decode("utf-8")]
  TryGet -->|exception| FallbackSet[requested_file = "nav/labels_en.json"]
  FallbackSet --> FallbackGet[s3.Object(bucket_name, requested_file).get()]
  FallbackGet --> FallbackRead[byte_content = obj.get()["Body"].read()]
  FallbackRead --> Decode
  Decode --> MakeResponse[fv.aws.lambdas.make_response(content)]
  MakeResponse --> Return[Return response]
```

> SVG rendering failed for this diagram.

## Diagram 2

```mermaid
classDiagram
  class module {
    +get_content(requested_file)
    +lambda_handler(event, context, audit_refs)
  }
  class boto3 {
    +resource(service_name)
  }
  class s3_Object {
    +get()
  }
  class fv_aws_lambdas {
    +mandatory_lambda_handling()
    +get_header(event, header_name)
    +make_response(content)
  }
  class support_service_common {
    +get_bucket_name_for_stage(stage)
    +DEFAULT_LANGUAGE
  }
  module --|> fv_aws_lambdas : uses
  module --|> boto3 : uses
  module --|> support_service_common : imports
  boto3 --> s3_Object : returns
  s3_Object -- get --> "byte stream"
```

> SVG rendering failed for this diagram.
