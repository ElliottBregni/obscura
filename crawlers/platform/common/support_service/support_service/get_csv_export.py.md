# Diagram: common/support_service/support_service/get_csv_export.py


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
flowchart TD
  A[lambda_handler(event, context, audit_refs)] --> B{requested_file exists\nand file_type contains\n"text/csv"?}
  B -->|yes| C[Execute SQL: select ... where uuid = requested_file]
  C --> D[res = cursor.fetchone()]
  D --> E{res found?}
  E -->|yes| F[Return JSON response with\nurl, status, uuid, type = PAGE[res.target]]
  E -->|no| G[Return JSON response\nurl: null, status: NOT_FOUND,\nuuid: requested_file, type: null]
  B -->|no| H[Execute SQL: select ... where actor_id = user_id]
  H --> I[res = cursor.fetchall()]
  I --> J{res has rows?}
  J -->|yes| K[Build all_exports list\nfrom rows -> objects with\nurl,status,uuid,type]
  K --> L[Return JSON response body=all_exports]
  J -->|no| M[Raise fv.error.BadRequestError("Export Not found")]
  F --> Z[END]
  G --> Z
  L --> Z
  M --> Z
  A --> N[get_query_parameter(event, "file") -> requested_file]
  A --> O[auth.get_user_id(event) -> user_id]
  A --> P[parse_accept_header(event) -> file_type]
  style A fill:#f9f,stroke:#333,stroke-width:2px
  style Z fill:#eee,stroke:#333,stroke-width:1px
```

> SVG rendering failed for this diagram.
