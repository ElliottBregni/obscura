# Diagram: common/support_service/serverless.support.yml


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
flowchart LR
  subgraph Provider[AWS Provider (python3.13, arm64)]
    direction TB
    VPC[VPC]
    IAM[IAM Role\n(managedPolicies + statements)]
    Datadog[Datadog Env Vars]
    Plugins[Plugins\nserverless-domain-manager\nserverless-python-requirements\nserverless-prune-plugin\nserverless-api-compression\nserverless-plugin-scripts]
  end

  subgraph API[API Gateway / HTTP Events]
    direction TB
    APIGW[API Gateway]
  end

  subgraph Functions[Lambda Functions (support_service)]
    direction TB
    get_content_nav["get_content_nav\nhandler: support_service/get_content_nav.lambda_handler"]
    get_content["get_content\nhandler: support_service/get_content.lambda_handler"]
    get_api_sample["get_api_sample\nhandler: support_service/get_api_sample.lambda_handler"]
    get_api_group["get_api_group\nhandler: support_service/get_api_group.lambda_handler"]
    get_api_logs["get_api_logs\nhandler: support_service/get_api_logs.lambda_handler"]
    get_language_templates["get_language_templates\nhandler: support_service/get_language_templates.lambda_handler"]
    get_csv_export["get_csv_export\nhandler: support_service/csv_exports/get_csv_export.lambda_handler"]
    update_csv_export["update_csv_export\nhandler: support_service/csv_exports/update_csv_export.lambda_handler"]
    customer_support_ticket["customer_support_ticket\nhandler: support_service/lambdas/customer_support_ticket.lambda_handler"]
    get_compliance_links["get_compliance_links\nhandler: support_service/lambdas/get_compliance_links.lambda_handler"]
  end

  subgraph Resources[External Resources & Permissions]
    direction TB
    S3["S3 Buckets\n(fv-doc-*, multiple envs)"]
    SSM["SSM Parameters\n(arn:aws:ssm:us-east-1:*:parameter/fv/...)"]
    KMS["KMS Key\n(Decrypt)"]
    Secrets["SecretsManager\nDatadog API Key ARN"]
    CloudWatchDeny["CloudWatch Logs (Deny for staging/test)"]
    SQS["SNS: CreateTopic / Publish"]
    S3Get["s3:GetObject"]
  end

  %% Connections
  APIGW -->|HTTP GET/POST/PATCH| get_content_nav
  APIGW --> get_content
  APIGW --> get_api_sample
  APIGW --> get_api_group
  APIGW --> get_api_logs
  APIGW --> get_language_templates
  APIGW --> get_csv_export
  APIGW --> update_csv_export
  APIGW --> customer_support_ticket
  APIGW --> get_compliance_links

  %% Lambdas to Provider
  Functions -->|uses| IAM
  Functions -->|attached to| VPC
  Functions -->|env vars| Datadog
  Functions -->|layers| Plugins

  %% Permissions & Resources usage
  get_content_nav -->|reads| S3
  get_content -->|reads| S3
  get_api_logs -->|reads| SSM
  get_csv_export -->|reads/returns| S3
  update_csv_export -->|reads/writes| S3
  update_csv_export -->|path param| APIGW
  customer_support_ticket -->|publishes| SQS
  customer_support_ticket -->|uses| Secrets
  Functions -->|kms:Decrypt| KMS
  Functions -->|ssm:GetParameter*| SSM
  Functions -->|secretsmanager:GetSecretValue| Secrets
  IAM -->|Deny logs| CloudWatchDeny

  %% Packaging & config
  subgraph PackageConfig[Packaging & Custom]
    patterns["package.patterns\nexclusions"]
    customVars["custom.globalConfig\nbase path: support"]
  end
  Provider --> PackageConfig
  Plugins --> PackageConfig
```

> SVG rendering failed for this diagram.
