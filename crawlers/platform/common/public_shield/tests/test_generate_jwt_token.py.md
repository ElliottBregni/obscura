# Diagram: common/public_shield/tests/test_generate_jwt_token.py


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
classDiagram
    class GeneratePublicShieldJwtTokenTest {
        +setUp()
        +test_event_is_valid_*
        +test_jwt_token_generation_*
        -secret: JwtSecretKeySchema
        -organization_invokinator: OrganizationInvokinator
        -token_generator: PublicShieldJwtTokenGeneratorService
    }

    class PublicShieldJwtTokenGeneratorService {
        +__init__(secret, organization_invokinator)
        +generate_jwt_token(event)
        -secret: JwtSecretKeySchema
        -organization_invokinator: OrganizationInvokinator
        -validator
    }

    class PublicShieldJwtTokenEvent {
        +from_lambda_event(event)
        +is_valid()
        -entity
        -organization_id
        -organization_fv_id
        -id
        -expirationLifetime
        -errors
    }

    class JwtSecretKeySchema {
        +__init__(algorithm, secret_key)
        -algorithm
        -secret_key
    }

    class OrganizationInvokinator {
        +get_organization_id_from_fv_id(fv_id)
    }

    class MockJwtSecret {
        +__init__()
    }

    class MockOrganizationInvokinator {
        +get_organization_id_from_fv_id(fv_id)
    }

    class ForbiddenError {
    }

    %% Inheritance
    JwtSecretKeySchema <|-- MockJwtSecret
    OrganizationInvokinator <|-- MockOrganizationInvokinator

    %% Associations / Dependencies
    GeneratePublicShieldJwtTokenTest ..> PublicShieldJwtTokenGeneratorService : uses
    GeneratePublicShieldJwtTokenTest ..> PublicShieldJwtTokenEvent : creates
    PublicShieldJwtTokenGeneratorService --> JwtSecretKeySchema : uses
    PublicShieldJwtTokenGeneratorService --> OrganizationInvokinator : uses
    PublicShieldJwtTokenGeneratorService o-- "validator" : has
    PublicShieldJwtTokenGeneratorService ..> PublicShieldJwtTokenEvent : consumes
    PublicShieldJwtTokenGeneratorService ..> ForbiddenError : may raise
    PublicShieldJwtTokenEvent ..> OrganizationInvokinator : may resolve fv id via
    MockOrganizationInvokinator --> OrganizationInvokinator
    MockJwtSecret --> JwtSecretKeySchema
```

> SVG rendering failed for this diagram.
