# Diagram: common/public_shield/handler.py


> Auto-generated by Obscura crawlers

## Diagram 1

```mermaid
classDiagram
class lambda_handler {
  +EventResponseSchema lambda_handler(EventSchema event, ContextSchema context)
}
class PublicShieldService {
  +(constructor) PublicShieldService()
  +(tuple) validate_token(token, payload)
  +get_payload(query_string_parameters)
  +run_business_layer_lambda(lambda_name, full_payload)
}
class EventSchema
class ContextSchema
class EventResponseSchema
class ValidateHandler <<decorator>>
class wrap_lambda_handler <<decorator>>
class rollbar_lambda_function <<decorator>>

lambda_handler --> EventSchema : param "event"
lambda_handler --> ContextSchema : param "context"
lambda_handler --> EventResponseSchema : returns
lambda_handler --> PublicShieldService : uses
ValidateHandler <|-- lambda_handler
wrap_lambda_handler <|-- lambda_handler
rollbar_lambda_function <|-- lambda_handler
PublicShieldService --> "validate_token(token,payload)" 
PublicShieldService --> "get_payload(query_string_parameters)"
PublicShieldService --> "run_business_layer_lambda(lambda_name,full_payload)"
```

> SVG rendering failed for this diagram.

## Diagram 2

```mermaid
flowchart LR
  subgraph Decorators
    A1[wrap_lambda_handler (fv.datadog)]
    A2[rollbar.lambda_function]
    A3[ValidateHandler]
  end
  Event[(Incoming Event : EventSchema)]
  Context((Context : ContextSchema))
  Event -->|passed to| D[lambda_handler]
  Context --> D
  A3 --> D
  A1 --> D
  A2 --> D
  D --> S[Instantiate PublicShieldService]
  S --> V{validate_token}
  V -->|calls| G[get_payload(query_string_parameters)]
  G --> V
  V --> E[returns (payload, entity)]
  E --> R[run_business_layer_lambda(lambda_name=entity, full_payload=payload)]
  R --> Resp[EventResponseSchema response]
  Resp --> End((Return response))
```

> SVG rendering failed for this diagram.
