# Diagram: common/fv/python/fv/aws/lambdas/invokinators/invokinator_custom_auth.py


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
flowchart LR
    A[invoke_get_custom_auth(auth, headers)] --> B[Call fv.aws.lambdas.get_account_number().value]
    B --> C[headers.update({"Authorization": auth})]
    C --> D[event = {"headers": headers, "methodArn": ":::us-east-1:{account_number}"}]
    D --> E[Call fv.aws.lambdas.invoke_lambda("custom-authorizer", full_payload=event)]
    E --> F[return response]
```

> SVG rendering failed for this diagram.
