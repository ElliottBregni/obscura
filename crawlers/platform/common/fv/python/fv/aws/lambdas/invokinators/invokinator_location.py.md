# Diagram: common/fv/python/fv/aws/lambdas/invokinators/invokinator_location.py


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
flowchart LR
  get_location_grants["get_location_grants(event, grantee_id, dereference, location_ids, pov)"]
  add_location_grants["add_location_grants(event, granter_id, grantee_id, location_ids, grant_type)"]
  delete_location_grants["delete_location_grants(event, grantee_id, location_ids)"]
  invoke_get_location["invoke_get_location(event, query_params, headers, accept, path_params)"]
  invoke_locations_intersections["invoke_locations_intersections(event, qsp)"]

  fv_invoke["fv.aws.lambdas.invoke_lambda(name, full_payload)"]
  auth_privs["fv.aws.lambdas.auth.Privilege"]
  qsp_const["QSP.LocationGrant constants"]

  get_location_grants -->|build invoke_event; set query params| fv_invoke
  get_location_grants -->|call 'grants-get'| fv_invoke
  fv_invoke -->|returns results| get_location_grants
  get_location_grants -->|on status>=400 raise HandledException| error1[/"HandledException (internal error)"/]

  add_location_grants -->|build invoke_event.body| fv_invoke
  add_location_grants -->|call 'grants-add'| fv_invoke
  fv_invoke -->|returns results| add_location_grants
  add_location_grants -->|on status>=400 and !=409 raise HandledException| error2[/"HandledException (internal error)"/]

  delete_location_grants -->|build query params| fv_invoke
  delete_location_grants -->|call 'grants-delete'| fv_invoke
  fv_invoke -->|returns results| delete_location_grants
  delete_location_grants -->|on status>=400 raise HandledException| error3[/"HandledException (internal error)"/]

  invoke_get_location -->|deepcopy event; set path/query/headers| fv_invoke
  invoke_get_location -->|call 'locations-get'| fv_invoke
  fv_invoke -->|returns location| invoke_get_location
  invoke_get_location -->|on status>=400 raise HandledException| error4[/"HandledException (status)"/]

  invoke_locations_intersections -->|deepcopy event; modify authorizer privileges| auth_privs
  invoke_locations_intersections -->|append MANAGE_CARRIER_LOCATIONS & MANAGE_SHIPPER_LOCATIONS| auth_privs
  invoke_locations_intersections -->|set queryStringParameters and log| fv_invoke
  invoke_locations_intersections -->|call 'locations-intersections'| fv_invoke
  fv_invoke -->|returns resp| invoke_locations_intersections
  invoke_locations_intersections -->|if status>=400 -> body = None| null_body[(None)]
  invoke_locations_intersections -->|if not qsp.full -> parse body| parsed_body[(parsed body)]
  invoke_locations_intersections -->|if qsp.full -> build {ids, locations}| full_body[{ids, locations}]

  qsp_const --> get_location_grants
  qsp_const --> add_location_grants
  qsp_const --> delete_location_grants

  style fv_invoke fill:#f9f,stroke:#333,stroke-width:1px
  style error1 fill:#faa,stroke:#900,stroke-width:1px
  style error2 fill:#faa,stroke:#900,stroke-width:1px
  style error3 fill:#faa,stroke:#900,stroke-width:1px
  style error4 fill:#faa,stroke:#900,stroke-width:1px
```

> SVG rendering failed for this diagram.
