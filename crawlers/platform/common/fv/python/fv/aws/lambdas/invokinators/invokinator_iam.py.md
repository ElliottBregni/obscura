# Diagram: common/fv/python/fv/aws/lambdas/invokinators/invokinator_iam.py


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
flowchart LR
  get_user_org_prefs["get_user_org_prefs(event, organization_id, user_auth0_id, key_pref)"]
  invoke_get_features["invoke_get_features(organization_id, lambda_level)"]
  extract_solution_id["extract_solution_id(solution_id, get_shipper_name)"]
  invoke_get_solution["invoke_get_solution(solution_id, get_shipper_name)"]
  sync_user["sync_user(auth0_user_id)"]
  extract_query_params["extract_query_params(event, query_params)"]
  invoke_get_organizations["invoke_get_organizations(event, query_params)"]
  invoke_get_organizations_members["invoke_get_organizations_members(event, query_params)"]
  extract_org_feat["extract_organization_id_feature_name(organization_id, feature_name)"]
  invoke_get_solution_by_org_feat["invoke_get_solution_by_organization_id_and_feature(organization_id, feature_name)"]
  organization_has_feature["organization_has_feature(organization_id, feature)"]

  subgraph AWS_Lambdas
    lambda_invoke["fv.aws.lambdas.invoke_lambda(name, full_payload)"]
    get_sample_admin_event["fv.aws.lambdas.get_sample_admin_event(method)"]
  end

  subgraph Auth_Cache_Error
    auth_get_org["auth.get_organization_id(event)"]
    cache_TTL["TTLCache / @cached"]
    fv_error["fv.error.HandledException"]
  end

  get_user_org_prefs -->|invokes create_user_pref| lambda_invoke
  invoke_get_features -->|invokes get_features| lambda_invoke
  extract_solution_id -->|used as cache key| cache_TTL
  invoke_get_solution -->|cached(key=extract_solution_id)| cache_TTL
  invoke_get_solution -->|invokes get_solution| lambda_invoke
  sync_user -->|uses get_sample_admin_event| get_sample_admin_event
  sync_user -->|invokes update_users| lambda_invoke
  extract_query_params -->|calls auth.get_organization_id| auth_get_org
  extract_query_params -->|returns cache key| cache_TTL
  invoke_get_organizations -->|cached(key=extract_query_params)| cache_TTL
  invoke_get_organizations -->|invokes get_organizations| lambda_invoke
  invoke_get_organizations_members -->|cached(key=extract_query_params)| cache_TTL
  invoke_get_organizations_members -->|invokes get_organizations_members| lambda_invoke
  extract_org_feat -->|used as cache key| cache_TTL
  invoke_get_solution_by_org_feat -->|cached(key=extract_organization_id_feature_name)| cache_TTL
  invoke_get_solution_by_org_feat -->|invokes get_solution| lambda_invoke
  organization_has_feature -->|calls invoke_get_solution_by_organization_id_and_feature| invoke_get_solution_by_org_feat

  %% Error handling links
  lambda_invoke -->|on error raises| fv_error
  get_sample_admin_event --> lambda_invoke
```

> SVG rendering failed for this diagram.
