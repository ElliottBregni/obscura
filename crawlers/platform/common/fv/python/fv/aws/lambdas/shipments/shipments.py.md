# Diagram: common/fv/python/fv/aws/lambdas/shipments/shipments.py


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
classDiagram
    class shipment_visibility_conditions {
        +shipment_visibility_conditions(leg_filtering=True, GLOBAL=False, has_visibility_override=False)
    }
    class create_db_ref_dict {
        +create_db_ref_dict(shipment_db_refs)
    }
    class get_quantity_from_ref {
        +get_quantity_from_ref(reference)
    }
    class insert_ref {
        +insert_ref(cursor, shipment_db_id, qualifier, value, quantity, actor_org_id=None)
    }
    class update_ref {
        +update_ref(cursor, shipment_db_id, qualifier, value, quantity, actor_org_id=None)
    }
    class delete_shipment_ref {
        +delete_shipment_ref(cursor, shipment_db_id, qualifier, value)
    }
    class check_shipment_ref_exist {
        +check_shipment_ref_exist(cursor, shipment_db_id, qualifier, value)
    }
    class update_shipment_details_with_sibiling_refs {
        +update_shipment_details_with_sibiling_refs(cursor, shipment_id, sib_refs)
    }
    class update_shipment_column {
        +update_shipment_column(cursor, shipment_id, column, value)
    }
    class update_lob {
        +update_lob(cursor, shipment_db_id, lob)
    }
    class check_if_equipment_number_exists {
        +check_if_equipment_number_exists(cursor, shipment_db_id)
    }
    class handle_specific_ref {
        +handle_specific_ref(cursor, given_ref, shipment_db_id, db_ref_dict, actor_org_id=None, allow_duplicate_equipment_num=True)
        -ref_exists(db_refs, given_value, given_quantity)
    }

    shipment_visibility_conditions --> fv_aws_constants : uses OrgTypes.DEALER
    shipment_visibility_conditions --> constants : reads constants

    create_db_ref_dict --> constants : reads DUPLICATE_REFERENCES
    create_db_ref_dict --> create_db_ref_dict : builds db_ref_dict

    get_quantity_from_ref --> (qualifier contains "part" or "rack") : parses value
    get_quantity_from_ref --> int : attempts conversion

    handle_specific_ref --> get_quantity_from_ref : calls
    handle_specific_ref --> insert_ref : may call
    handle_specific_ref --> update_ref : may call
    handle_specific_ref --> delete_shipment_ref : may call
    handle_specific_ref --> check_shipment_ref_exist : may call
    handle_specific_ref --> update_shipment_column : may call
    handle_specific_ref --> check_if_equipment_number_exists : may call
    handle_specific_ref --> constants : reads many constants
    handle_specific_ref --> util : uses strip_str

    insert_ref --> shipment_references_db : inserts row
    update_ref --> shipment_references_db : updates row
    delete_shipment_ref --> shipment_references_db : deletes row
    check_shipment_ref_exist --> shipment_references_db : selects id

    update_shipment_details_with_sibiling_refs --> shipments_db : updates shipment_details
    update_shipment_column --> shipments_db : updates column
    update_lob --> shipments_db : updates line_of_business_id and vins_added

    check_if_equipment_number_exists --> shipment_references_db : exists check
    check_if_equipment_number_exists --> constants : uses EQUIPMENT_NUMBER_QUALIFIER

    note for handle_specific_ref "behavior":
    handle_specific_ref ..> create_db_ref_dict : expects db_ref_dict produced by create_db_ref_dict

    %% external modules
    class fv_aws_constants {
        <<external>>
    }
    class constants {
        <<external>>
    }
    class util {
        <<external>>
    }
```

> SVG rendering failed for this diagram.
