# Diagram: common/fv/python/fv/aws/lambdas/auth.py


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
classDiagram
    class Privilege {
        <<enumeration>>
        +CHANGE_ACTIVE_ORGANIZATIONS
        +SUPER_PRIVILEGE
        +MANAGE_ORGANIZATIONS
        +MANAGE_CARRIER_LOCATIONS
        +MANAGE_USERS
        +VIEW_LOCATIONS
        +MANAGE_LOCATIONS
        +VIEW_SHIPMENTS
        +MANAGE_SHIPMENTS
        +LINK_LOCATIONS
        +MANAGE_SHIPPER_LOCATIONS
        +CHANGE_USER_PASSWORDS
        +MANAGE_ENTITY
        +VIEW_ENTITY
        +VIEW_LOGGING
        +VIEW_AI
        +GLOBAL_ASSET_SEARCH
        +BUILD_REPORT
        +MANAGE_SHARED_REPORTS
        +VIEW_ALL_ENTITIES
        +MANAGE_GEOFENCE_NAME
        +MANAGE_REPORT_DIRECTORY
        +MANAGE_SCAC
        +VIEW_PARTVIEW
        +VIEW_PARTVIEW_SELLER
        +FV_ADMIN_TOOL
        +VIEW_DAMAGEVIEW_DASHBOARD
        +VIEW_OCEANTRACKING_DASHBOARD
    }

    class Roles {
        <<enumeration>>
        +THIRD_PARTY
        +CARRIER_SYSTEM
        +FV_ADMIN
        +ORG_ADMIN
        +ORG_USER
        +FINISHED_VEHICLE_USER
        +FINISHED_VEHICLE_SYSTEM
        +REPORT_BUILDER_USER
        +SCAC_ADMIN
        +PARTVIEW_USER
        +PARTVIEW_SELLER
        +DAMAGEVIEW_USER
        +OCEANTRACKING_USER
    }

    class RoleIDs {
        <<enumeration>>
        +CARRIER_SYSTEM
        +FREIGHTVERIFY_ADMIN
        +ORGANIZATION_ADMIN
        +ORGANIZATION_USER
        +THIRD_PARTY_SYSTEM
        +FINISHED_VEHICLE_SYSTEM
        +FINISHED_VEHICLE_USER
        +REPORT_BUILDER_USER
        +SCAC_ADMIN
        +PARTVIEW_USER
        +PARTVIEW_SELLER
        +DAMAGEVIEW_USER
        +OCEANTRACKING_USER
    }

    class Auth0ClientID {
        <<enumeration>>
        +TEST
        +STAGING
        +PROD
    }

    class AuthType {
        <<enumeration>>
        +PRIVILEGE
        +SOLUTION
        +LOCATION_GRANT
        +ORG_TYPE
        +FEATURE
    }

    class Feature {
        <<enumeration>>
        +EVENT_API_READ_ACCESS
        +FINISHED_VEHICLE
        +PLANT_ASSET_TRACKING
        +CONNECTED_CAR
        +AUTO_DISTRIBUTION_CENTER
        +MILESTONE
        +MILESTONE_DASHBOARD
        +REPORT_BUILDER
        +MILESTONE_ETA
        +VIN_VIEW
        +CONTAINER
        +TRANSIT_TIMER
        +ITSS
        +DIAGNOSTIC_DATA
        +TIRE_PRESSURE
        +FUEL_LEVEL
        +BATTERY_LEVEL
        +SB
        +DL
        +SMALL_CONTAINER
        +FV_PLACEHOLDER_TRIP_LEGS
        +DAMAGE_VIEW
        +CUSTOMER_SUPPORT
        +FINISHED_VEHICLE_SCHEDULED_WINDOW
        +NOTIFICATION_MANAGEMENT
        +CRITICAL_PARTS
        +EXPECTED_TIME_OF_ARRIVAL
        +PARTNER_PORTAL_COMPLIANCE
        +INVENTORY_VIEW
        +DEALER_DRIVE_AWAY
        +BUSINESS_INTELLIGENCE
        +BI_DASHBOARD_NEXT
    }

    class FederatedRole {
        <<enumeration>>
        +FV
    }

    class Exceptions {
        +NonIntListException
        +NoPrivExcpetion
        +NoSolutionException
        +NoGrantException
    }

    class AuthModule {
        +get_auth_fields(event)
        +user_token_is_valid(config, email, token)
        +requires_tokenauth(config, resource)
        +convert_basic_auth(func)
        +convert_basic_auth_direct(event, context)
        +get_access_token_add_com(email, secret)
        +get_access_token(username, password, first_try=True)
        +get_actor_email(event)
        +get_actor_type(event)
        +get_privileges(event)
        +get_features(event)
        +get_user_id(event)
        +get_organization_id(event)
        +get_org_type(cursor, organization_id)
        +get_user_org_types(event)
        +get_user_phone_num(event, full=False)
        +get_role_from_privs(cursor, privileges)
        +approve_solution(user_solutions, solution_id)
        +check_privileges(event, required_privileges, solution_id=None)
        +check_intersecting_lists(lst, user_list)
        +check_location_grant(cursor, user_org, solution_id, location_code)
        +get_first_error(check_group)
        +check_check_list(check_list)
        +check_auth_wrapper(cursor, event, auth_check)
    }

    AuthModule --> Exceptions : raises
    AuthModule ..> Privilege : uses
    AuthModule ..> AuthType : uses
    AuthModule ..> Feature : uses
    AuthModule ..> Roles : references
    AuthModule ..> RoleIDs : references
    AuthModule ..> Auth0ClientID : references
    AuthModule ..> FederatedRole : references

    Exceptions <|-- NonIntListException
    Exceptions <|-- NoPrivExcpetion
    Exceptions <|-- NoSolutionException
    Exceptions <|-- NoGrantException

    FederatedRole "1" --> "many" Roles : federates
    FEDERATED_ROLES : map
    FEDERATED_ROLES --> FederatedRole
    FEDERATED_ROLES --> Roles

    class fv.aws.lambdas {
        +get_header(event, name, required=False)
        +make_client_error(event, message)
        +make_error_response(...)
        +get_url_extension(stage)
        +get_privileges(event)
    }

    AuthModule ..> fv.aws.lambdas : calls
    AuthModule ..> fv.db : uses
    AuthModule ..> fv.dm : uses
    AuthModule ..> fv.error : raises
    AuthModule ..> fv.secrets : uses
    AuthModule ..> fv.enum : defines enums
    AuthModule ..> requests : http calls

    note left of AuthModule
      Core authentication and authorization functions:
      token handling, access token retrieval, checks, and wrappers.
    end note
```

> SVG rendering failed for this diagram.
