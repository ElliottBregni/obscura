# Diagram: common/fv/python/fv/aws/lambdas/location/__init__.py


> Auto-generated by Obscura crawlers

## Diagram 1

```mermaid
flowchart TD
A[get_blacklist_locations(solution_id, location_codes)] --> B[construct full_payload]
B --> C[invoke_lambda("locations_blacklist", full_payload)]
C --> D[extract "body" from response]
D --> E[json.loads(body) -> get "blacklist"]
E --> F[return blacklist]
```

> SVG rendering failed for this diagram.

## Diagram 2

```mermaid
flowchart LR
G[get_granted_dealers_from_codes(grantor_id, location_codes)] --> H[FvDatabaseConnector("locations_shared", SecretNames.DATABASE)]
H --> I[establish_connection()]
I --> J[cursor = db_conn.cursor]
J --> K[query = cursor.mogrify(SQL with %(location_codes)s and %(grantor_id)s)]
K --> L[data = {"location_codes": tuple(location_codes.keys()), "grantor_id": grantor_id}]
K --> M[cursor.execute(query, data)]
M --> N[rows = cursor.fetchall()]
N --> O[grant_data = [row._asdict() for row in rows]]
O --> P[initialize rtn_data = []]
P --> Q[for grant in grant_data]
Q --> R[for grantee in grant.grantee_list]
R --> S[append {"grantee_id": grantee, "grant_reason": location_codes.get(grant.code), "resolved_referent_id": grant.resolved_referent_id} to rtn_data]
S --> T[return rtn_data]
```

> SVG rendering failed for this diagram.
