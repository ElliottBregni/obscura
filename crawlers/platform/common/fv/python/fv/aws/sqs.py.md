# Diagram: common/fv/python/fv/aws/sqs.py


> Auto-generated by Obscura crawlers

## Diagram 1

```mermaid
classDiagram
    class QueueMessage <<Protocol>> {
        +send(queue)
    }
    class StandardQueueMessage {
        -body
        -delay_seconds
        -attributes
        -system_attributes
        +send(queue)
    }
    class FIFOQueueMessage {
        -deduplication_id
        -group_id
        +send(queue)
    }
    class SQSQueue {
        -queue_name
        -_queue
        +send_message(message)
        +send_messages(messages)
        +get_attributes()
        +get_attribute(name)
        +get_redrive_policy()
        +get_url()
    }
    class HandleSingleSqsRecord <<Abstract>> {
        <<abstract>>
        +handle(record)
        +parse_message(record)
    }
    class SqsBatchProcessorWithPartialItemFailure {
        -handler: HandleSingleSqsRecord
        +process_batch(event)
    }
    QueueMessage <|.. StandardQueueMessage
    StandardQueueMessage <|-- FIFOQueueMessage
    SQSQueue o-- QueueMessage : "accepts"
    SqsBatchProcessorWithPartialItemFailure --> HandleSingleSqsRecord : "uses handler"
    SQSQueue ..> "boto3.resource('sqs')" : "fetches queue via"
    SqsBatchProcessorWithPartialItemFailure ..> rollbar : "reports errors to"
    HandleSingleSqsRecord ..> json : "parses message with"
    SQSQueue : "+_fetch_queue_if_not_exist()"
```

> SVG rendering failed for this diagram.

## Diagram 2

```mermaid
flowchart TD
    A[Start: process_batch(event)] --> B{Records present?}
    B -- No --> C[Return response with empty body]
    B -- Yes --> D[Iterate records]
    D --> E[Set current_message_id]
    E --> F[Try handler.handle(record)]
    F -- Success --> G[Append handler response to response_data]
    F -- Exception --> H[Log exception and rollbar.report_exc_info]
    H --> I[Create item failure object with traceback]
    I --> J[Append to failed_records]
    G --> K{More records?}
    J --> K
    K -- Yes --> D
    K -- No --> L[create_sqs_consumer_response(failed_records, response_data)]
    L --> M[Return response]
```

> SVG rendering failed for this diagram.
