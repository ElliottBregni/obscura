# Diagram: common/fv/python/fv/db/organizations.py


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
flowchart TD
    A[Start: get_org_from_fv_id(cursor, fv_id)] --> B[Build SQL query with cursor.mogrify]
    B --> C[Execute query: cursor.execute(query)]
    C --> D[Fetch result: org = cursor.fetchone()]
    D --> E{org exists?}
    E -- Yes --> F[Return org]
    E -- No --> G[Return None]
    F --> H[End]
    G --> H
```

> SVG rendering failed for this diagram.
