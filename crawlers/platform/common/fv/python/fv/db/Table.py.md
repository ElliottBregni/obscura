# Diagram: common/fv/python/fv/db/Table.py


> Auto-generated by Obscura crawlers

## Diagram 1

```mermaid
classDiagram
    class Table {
        <<base>>
        - __tablename__
        - columns
        - columns_to_filter
        - id
        - _cursor
        - _where_dict
        - _where_clause
        - _autocommit
        - _conflict_str
        + __init__(cursor, existing_row=None, autocommit=True, **kwargs)
        + update(log_no_execute=False)
        + insert(upsert=False, log_no_execute=False)
        + build_where(dct, group="id")
        + set_existing_row(dct, group)
        + to_dict()
        + _populate_object(existing_row)
        + _build_upsert(to_insert)
        + _build_update(mod_type="update")
        + _set_attr_init(name, value)
        + __setattr__(name, value)
        + __str__()
    }
    class "fv.error" {
        <<module>>
        + DatabaseError(...)
        + BadRequestError(...)
        + HandledException(...)
    }
    Table ..> "fv.error" : raises
    Table o-- "_cursor" : uses
    Table --> "DB Table" : represents
```

> SVG rendering failed for this diagram.

## Diagram 2

```mermaid
flowchart TD
    start([Start])
    check_changes{Changes set to update?}
    build_update[/_build_update()/]
    no_changes[Return (nothing to update)]
    build_set_clause[Build SET clause]
    ensure_where{Have where filters?}
    build_where[build_where(...) or use id]
    params_check{Parameters overlap?}
    mogrify[Cursor.mogrify(UPDATE ...)]
    execute_exec[Execute query]
    commit[Execute COMMIT if autocommit]
    end([End])

    start --> check_changes
    check_changes -- no --> no_changes --> end
    check_changes -- yes --> build_update --> build_set_clause --> ensure_where
    ensure_where -- missing --> build_where --> params_check
    ensure_where -- present --> params_check
    params_check -- fail --> end
    params_check -- ok --> mogrify --> execute_exec --> commit --> end
```

> SVG rendering failed for this diagram.

## Diagram 3

```mermaid
flowchart TD
    i_start([Insert / Upsert Start])
    build_insert[/_build_update("insert")/]
    no_insert[Return (nothing to insert)]
    columns_values[Prepare columns and values strings]
    upsert_check{upsert requested?}
    build_upsert[_build_upsert(to_insert)]
    mogrify_insert[Cursor.mogrify(INSERT ... RETURNING id)]
    execute_insert[Execute query]
    fetch_id[Set id from cursor.fetchone().id]
    commit_ins[Execute COMMIT if autocommit]
    end_ins([End])

    i_start --> build_insert
    build_insert -- none --> no_insert --> end_ins
    build_insert -- ok --> columns_values --> upsert_check
    upsert_check -- yes --> build_upsert --> mogrify_insert
    upsert_check -- no --> mogrify_insert
    mogrify_insert --> execute_insert --> fetch_id --> commit_ins --> end_ins
```

> SVG rendering failed for this diagram.
