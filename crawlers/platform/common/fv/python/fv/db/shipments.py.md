# Diagram: common/fv/python/fv/db/shipments.py


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
flowchart TD
  A[base_query(order_clause)] --> B[fv.db.q("""SELECT s.* FROM vw_shipment_details_location s ...""")]
  get_shipments[get_shipments(cursor, organization_id, shipment_ids)] --> D[fv.log.log_elapsed_time (decorator)]
  D --> A
  get_shipments --> E[cursor.execute(query, {"organization_id","shipment_ids"})]
  E --> F[cursor.fetchall()]
  F --> G[return rows]

  get_org_from_fv_id[get_org_from_fv_id(cursor, fv_id)] --> H[cursor.execute("""SELECT * FROM organizations WHERE fv_id=%s""", [fv_id])]
  H --> I[cursor.fetchone()]
  I --> J{org exists?}
  J -- yes --> K[return org]
  J -- no --> L[return None]
```

> SVG rendering failed for this diagram.
