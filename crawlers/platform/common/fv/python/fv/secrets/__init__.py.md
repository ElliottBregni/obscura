# Diagram: common/fv/python/fv/secrets/__init__.py


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
classDiagram
    class SecretNames <<enumeration>> {
        <<StringEnum>>
        DATABASE
        DATABASE_LOGGING
        DATABASE_PLATFORM
        HERE
        AUTH0
        ADMIN
        POWER_BI
        DATABASE_TRACKING
        DATABASE_EXILE
        ENTITY_DATABASE
        ENTITY_DATABASE_UNOPTIMIZED
        ENTITY_DATABASE_TRACKING
        ENTITY_DATABASE_DATA_EXTRACT
        SMTP
        L2_ETA
        ETA_ADMIN_USER
        AUTH0_INTEGRATION_USER
        AUTH0_INTEGRATION_TEST_USER
        CARBON_CARE
        PERSONA_DATABASE
        MILESTONE_CSV_BATCH_UPLOAD_SFTP
        JWT_SECRET_KEY
    }

    class Secrets {
        - client
        - secrets
        + __init__(region_name="us-east-1")
        + get_secrets(secrets, required=True, dev_secrets=False)
        + get_secret(secret_name, required=True, dev_secrets=False, cached=True)
        - _get_secret(secret_with_env, required=True, cached=True)
    }

    class CACHE {
        <<MyTTLCache>>
        + maxsize
        + ttl
    }

    class Functions {
        + extract_cached_params(self, secret_name, required=True, dev_secrets=False)
        + envkey(*args, **kwargs)
        + get_client(region_name)
    }

    class boto3 {
        + session
        + client(service_name, region_name)
    }

    class cachetools {
        + cached(cache)
        + keys.hashkey(*args)
    }

    class fv_utilities {
        + dev_to_staging(stage, spec_env)
        + dotdict(dict_obj)
        + MyTTLCache(maxsize, ttl)
    }

    Secrets o-- CACHE : uses
    Secrets --> SecretNames : references
    Secrets --> boto3 : uses
    Secrets --> cachetools : decorator
    Functions --> CACHE : references
    Functions --> cachetools : uses hashkey / cached
    Functions --> fv_utilities : uses dev_to_staging, dotdict, MyTTLCache
    boto3 ..> Functions : get_client creates session.client()
    cachetools ..> Functions : cached decorator / hashkey
    SecretNames <|-- Secrets : uses enum values for keys
```

> SVG rendering failed for this diagram.
