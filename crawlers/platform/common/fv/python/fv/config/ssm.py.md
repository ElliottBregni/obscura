# Diagram: common/fv/python/fv/config/ssm.py


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
classDiagram
    class ConfigurationSource {
    }

    class SSMConfigurationSource {
        - ssm_client
        - ssm_paths
        - recursive
        + __init__(ssm_paths=None, recursive=False)
        + add_ssm_parameter(config, ssm_path)
        + add_ssm_parameters_by_path(config, ssm_path, next_token=None)
        + load()
    }

    ConfigurationSource <|-- SSMConfigurationSource

    %% External dependencies (uses)
    class boto3 << (D,orchid) >>
    class botocore.exceptions << (D,orchid) >>
    class json << (D,orchid) >>
    class logging << (D,orchid) >>
    class os.path << (D,orchid) >>

    SSMConfigurationSource ..> boto3 : uses
    SSMConfigurationSource ..> "botocore.exceptions" : handles
    SSMConfigurationSource ..> json : parses
    SSMConfigurationSource ..> logging : logs
    SSMConfigurationSource ..> os.path : manipulates paths
```

> SVG rendering failed for this diagram.
