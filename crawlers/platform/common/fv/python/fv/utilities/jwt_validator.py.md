# Diagram: common/fv/python/fv/utilities/jwt_validator.py


> Auto-generated by Obscura crawlers

## Diagram 1

```mermaid
classDiagram
    class JwtSecretKeySchema {
        +secret_key: str
        +algorithm: str
    }
    class JWTValidator {
        +decoded_token: Dict[str, Any]
        +secret: JwtSecretKeySchema
        +timedelta_adapter: TypeAdapter[timedelta]
        +__init__(secret: JwtSecretKeySchema)
        +validate_signature(token: str) : None
        +validate_payload(payload: Dict[str, Any], ignore_include_parameters: List[str], ignore_exclude_parameters: List[str]) : Tuple[Dict[str, str], str]
        +encode(payload: Dict[str, Any], expiration_lifetime: str = "P50D") : str
    }
    JwtSecretKeySchema <|-- JWTValidator : has
    JWTValidator o-- "decoded_token" : Dict
    JWTValidator ..> TypeAdapter : uses
```

> SVG rendering failed for this diagram.

## Diagram 2

```mermaid
flowchart TD
    Start([Start]) --> VSig[validate_signature(token)]
    VSig -->|decoded successfully| VPayload[validate_payload(payload, ignore_include_parameters, ignore_exclude_parameters)]
    VSig -->|DecodeError or ExpiredSignatureError| Forbidden1[ForbiddenError: "Token is invalid"]
    VPayload --> RemoveExcluded[remove ignore_exclude_parameters from decoded_token]
    RemoveExcluded --> ExtractInclude[extract ignore_include_parameters into include_to_payload]
    ExtractInclude --> Compare{decoded_token == payload?}
    Compare -->|no| Forbidden2[ForbiddenError: "Payload is invalid"]
    Compare -->|yes| MergeInclude[payload.update(include_to_payload)]
    MergeInclude --> ReturnPayload[(return payload, entity)]
    EncodeStart([Encode start]) --> EncodeFn[encode(payload, expiration_lifetime)]
    EncodeFn --> ResolveLifetime[expiration_lifetime or "P50D"]
    ResolveLifetime --> ConvertDelta[timedelta_adapter.validate_python(expiration_lifetime)]
    ConvertDelta --> SetExp[payload["exp"] = datetime.utcnow() + expiration_timedelta]
    SetExp --> JWTEnc[jwt.encode(payload, key=secret.secret_key, algorithm=secret.algorithm)]
    JWTEnc --> Token((JWT token))
```

> SVG rendering failed for this diagram.
