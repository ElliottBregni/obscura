# Diagram: common/fv/python/fv/utilities/watcher_notification.py


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
flowchart TB
  Start([Start]) --> Make[/"make_internal_error(service_name, exception_detail, error_status_code, request, client_error, json_body)"/]
  Make --> Build{Build internal_error JSON}
  Build --> LogJson[/"logging.error(internal_error_log_json)"/]
  LogJson --> Trace[/"logging.error(traceback.format_exc())"/]
  Trace --> GetStage[/"aws_stage = os.environ['AWS_STAGE']"/]
  GetStage --> StageCheck{aws_stage == "dev" or aws_stage == "qa"}
  StageCheck -- yes --> TopicDev["topic_arn = arn:aws:sns:us-east-1:591447794615:{aws_stage}-{service_name}"]
  StageCheck -- no --> TopicProd["topic_arn = arn:aws:sns:us-east-1:519940785508:{aws_stage}-{service_name}"]
  TopicDev --> CreateClient[/"sns_client = boto3_client('sns')"/]
  TopicProd --> CreateClient
  CreateClient --> Publish[/"resp = sns_client.publish(TopicArn=topic_arn, Message=internal_error_log_json)"/]
  Publish --> LogWarn[/"logging.warning(f'Response from SNS publish: {resp}')"/]
  LogWarn --> End([End])
  Publish -- ClientError --> Except[/"except ClientError as ex"/]
  Except --> LogErr[/"logging.error(f'Error publishing, does topic {topic_arn} exist? Exception {ex}')"/]
  LogErr --> End
```

> SVG rendering failed for this diagram.
