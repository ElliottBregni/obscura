# Diagram: common/fv/python/fv/utilities/__init__.py


> Auto-generated by Obscura crawlers

## Diagram 1

```mermaid
classDiagram
    class dotdict {
        <<dict>>
        +__getattr__()
        +__setattr__()
        +__delattr__()
    }
    class DictionaryToObject {
        +__init__(d)
    }
    class MyTTLCache {
        <<TTLCache>>
        +__setitem__(key, value)
    }
    class Utilities {
        +is_blank(test_str)
        +is_present(test_str)
        +unescape(s)
        +add_escape_to_backslash(string, remove=False)
        +get_distance(first_coords, sec_coords)
        +straight_line_distance(p0, p1)
        +get_distance_waypoints(waypoints)
        +dev_to_staging(stage, spec_env="")
        +split_to_list(comma_sep_string)
        +is_valid_lat_long(lat, lng)
        +snake_to_camel(string)
        +snake_to_camel_recur(obj)
        +get_rand_hex(length)
        +send_multithreaded_requests(thread_function, thread_nums=4, args=[], kwargs=[])
        +fix_wildcards(query)
        +format_fv_stop_time(stop_time)
        +validate_location_update(latitude, longitude)
        +validate_lat_long(latitude, longitude)
        +strip_str(string)
        +strip_recur(obj)
        +send_email(email, body)
        +datetime_from_iso_format(date_str, end=True)
        +merge_date_and_time(date, time)
        +split_list_even(lst, max_size=1000)
        +split_dict_even(dct, max_size=1000)
        +timestamp_to_datetime(timestamp: str, to_utc: bool)
        +check_email_format(email)
        +is_timestamp(ts_str)
        +convert_str_to_datetime(date_str, formats)
        +convert_list_to_dict_by_key(lst, key)
        +ensure_tz_present(dt)
    }
    dotdict --|> dict : extends
    MyTTLCache --|> TTLCache : extends
    Utilities --> MyTTLCache : uses
    Utilities --> DictionaryToObject : creates
    Utilities --> dotdict : returns/accepts
    Utilities ..> fv.error : raises
    Utilities ..> cachetools.TTLCache : references
    Utilities ..> dateutil.parser : uses
    Utilities ..> geopy.distance : uses
    Utilities ..> html, urllib, re, math, os, binascii, threading, time, datetime : uses
```

> SVG rendering failed for this diagram.

## Diagram 2

```mermaid
flowchart TD
    A[validate_lat_long(latitude, longitude)] --> B{can convert to float?}
    B -- No --> C[return False, "Bad value for lat or long"]
    B -- Yes --> D{latitude in -90..90?}
    D -- No --> E[return False, "Invalid value for lat {latitude}"]
    D -- Yes --> F{longitude in -180..180?}
    F -- No --> G[return False, "Invalid value for longitude ({longitude})"]
    F -- Yes --> H{latitude and longitude truthy?}
    H -- Yes --> I[return True, ""]
    H -- No --> J[return False, "Invalid value for latitude,longitude ({latitude},{longitude})"]
```

> SVG rendering failed for this diagram.
