# Diagram: common/fv/python/fv/encoding/json.py


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
flowchart LR
  Start([start]) --> D1{is instance of<br/>date, datetime,<br/>or time?}
  D1 -- yes --> R1[/return value.isoformat()/]
  D1 -- no --> D2{is instance of<br/>DateTimeRange?}
  D2 -- yes --> D2a{value.lower and<br/>value.upper?}
  D2a -- yes --> R2[/return [value.lower.timestamp(), value.upper.timestamp()]/]
  D2a -- no --> R3[/return [None, None]/]
  D2 -- no --> D3{is instance of<br/>unittest.mock.MagicMock?}
  D3 -- yes --> R4[/return "MOCK"/]
  D3 -- no --> D4{is instance of<br/>decimal.Decimal?}
  D4 -- yes --> R5[/return float(value)/]
  D4 -- no --> D5{is instance of<br/>enum.Enum?}
  D5 -- yes --> R6[/return value.value/]
  D5 -- no --> D6{is instance of<br/>datetime.timedelta?}
  D6 -- yes --> A1[print value.days, value.seconds]
  A1 --> A2[hours = floor(value.seconds / 3600)]
  A2 --> A3[minutes = floor(value.seconds / 60 % 60)]
  A3 --> R7[/return formatted "DD:HH:MM"/]
  D6 -- no --> D7{is dataclass?<br/>dataclasses.is_dataclass(value)}
  D7 -- yes --> R8[/return dataclasses.asdict(value)/]
  D7 -- no --> E1[message = "Could not encode value: {value}"]
  E1 --> E2[/raise BadRequestError(message, message,<br/>http_status=HTTPStatus.INTERNAL_SERVER_ERROR.value)/]
```

> SVG rendering failed for this diagram.
