# Diagram: common/fv/python/tests/aws/test_mandatory_lambda_handling.py


> Auto-generated by Obscura crawlers

## Diagram 1

```mermaid
classDiagram
    class Context {
        +function_name: str
        +function_version: str
        +invoked_function_arn: str
        +aws_request_id: str
        +memory_limit_in_mb: str
        +get_remaining_time_in_millis(): int
    }
    class TestException
    class TestProxyV1Exception {
        +actual_http_status: int
    }
    class HandledException
    class TestMandatoryLambdaHandling {
        +test_wraps_around_a_lambda_handler()
        +test_catches_unhandled_exceptions()
        +test_handles_proxy_v1_errors()
        +test_handles_timeouts_keeps_the_original_response()
        +test_handles_proxy_v1_exceptions()
    }
    class mandatory_lambda_handling()
    Context <|-- TestMandatoryLambdaHandling
    TestProxyV1Exception --|> HandledException
    TestMandatoryLambdaHandling ..> mandatory_lambda_handling : uses
    TestMandatoryLambdaHandling ..> TestException : raises
    TestMandatoryLambdaHandling ..> TestProxyV1Exception : raises
```

> SVG rendering failed for this diagram.

## Diagram 2

```mermaid
flowchart TD
    A[Lambda Handler] --> B[mandatory_lambda_handling wrapper]
    B --> C{Handler returns or raises}
    C -->|returns normal response| D[Return response unchanged]
    C -->|raises TestException (unhandled)| E[Generate 500 response with requestId]
    C -->|raises TestProxyV1Exception| F[Convert to proxy v1 error body with reference]
    B --> G{Timeout check}
    G -->|exceeded MAX_TIMEOUT_SECONDS| H[Use original response if present]
    G -->|not exceeded| I[Proceed as normal]
    style A fill:#f9f,stroke:#333,stroke-width:1px
    style B fill:#bbf,stroke:#333,stroke-width:1px
    style E fill:#f99,stroke:#333,stroke-width:1px
    style F fill:#ffb,stroke:#333,stroke-width:1px
```

> SVG rendering failed for this diagram.
