# Diagram: common/fv/python/tests/aws/test_account_numbers.py


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
flowchart LR
  Tests[Test functions]
  TP[test_parses_stage]
  TR[test_rejects_unknown_stage]
  TD[test_defaults_to_dev]
  Tests --> TP
  Tests --> TR
  Tests --> TD

  TP --> |sets AWS_STAGE to Stage.*| AF[AccountNumber.from_stage()]
  TR --> |sets AWS_STAGE to "random"| AF
  TD --> |deletes AWS_STAGE| AF

  subgraph Mapping
    A_DEV[Stage: DEV,DEV2,QA,QA2] -->|->| ACC_DEV[AccountNumber.DEV]
    A_STAGING1[Stage: STAGING1] -->|->| ACC_STAGING[AccountNumber.STAGING]
    A_PROD[Stage: TEST,PROD_A,PROD] -->|->| ACC_PROD[AccountNumber.PROD]
    A_UNKNOWN[Stage: unknown] -->|->| ERR[ValueError raised]
    A_MISSING[Stage: missing/undefined] -->|defaults->| ACC_DEV
  end

  AF --> |if AWS_STAGE in DEV/DEV2/QA/QA2| A_DEV
  AF --> |if AWS_STAGE == STAGING1| A_STAGING1
  AF --> |if AWS_STAGE in TEST/PROD_A/PROD| A_PROD
  AF --> |if AWS_STAGE unknown| A_UNKNOWN
  AF --> |if AWS_STAGE missing| A_MISSING
```

> SVG rendering failed for this diagram.
