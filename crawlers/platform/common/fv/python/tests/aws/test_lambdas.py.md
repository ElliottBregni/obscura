# Diagram: common/fv/python/tests/aws/test_lambdas.py


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
flowchart LR
  subgraph Tests
    T1[Test: test_get_event_body]
    T2[Test: test_sanitize_event]
    T3[Test: test_sanitize_event_missing_value]
  end

  subgraph Library
    GB[get_event_body()]
    S[sanitize()]
    GH[get_header()]
    GM[get_multivalue_header()]
    E[fv.error.BadRequestError]
  end

  T1 -->|calls| GB
  GB -->|parses body (dict or JSON string)| GB_Parse[parse logic]
  GB -->|on invalid JSON (trailing comma / leading zero / missing body)| E
  T1 -->|asserts equality| GB_Parse

  T2 -->|provides event with headers & multiValueHeaders| S
  T2 -->|calls| GH
  T2 -->|calls| GM
  S -->|reads headers| Headers[headers/multiValueHeaders]
  Headers -->|replaces sensitive values| Redaction[REDACTED/N/a REDACTED]
  S -->|returns sanitized event| S_Return[sanitized_event]
  S_Return --> GH
  S_Return --> GM
  GH -->|returns single header value| HeaderVal
  GM -->|returns multivalue header list| MultiVal

  T2 -->|asserts Authorization -> "Basic REDACTED"| HeaderVal
  T2 -->|asserts X-User-Email preserved| HeaderVal
  T2 -->|asserts X-User-Token -> "REDACTED"| HeaderVal
  T2 -->|asserts multivalue redactions| MultiVal

  T3 -->|provides event with invalid auth value| S
  T3 -->|calls| GH
  T3 -->|calls| GM
  S -->|on malformed auth value| RedactionMalformed[produce "N/a REDACTED"]
  RedactionMalformed --> S_Return
  T3 -->|asserts Authorization -> "N/a REDACTED"| HeaderVal
  T3 -->|asserts multivalue -> ["N/a REDACTED"]| MultiVal
```

> SVG rendering failed for this diagram.
