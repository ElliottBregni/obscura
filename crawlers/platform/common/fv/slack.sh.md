# Diagram: common/fv/slack.sh


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
flowchart TD
    A[Script Start] --> B[post_to_slack(webhook, message)]
    B --> C[Format SLACK_MESSAGE as code block: ```message```]
    C --> D[curl POST payload: {"text": "${SLACK_ICON} ${SLACK_MESSAGE}"} to webhook URL]
    D --> E[exit 0]
```

> SVG rendering failed for this diagram.
