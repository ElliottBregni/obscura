# Diagram: common/fv/scripts/seed_here_locator.py


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
flowchart LR
  Start([Start]) --> Main[main()]
  Main --> ProcessShipments[process_shipments()]
  ProcessShipments --> Establish[DB_CONN.establish_connection()]
  Establish --> GetShipment[get_shipment(cursor, offset, PAGE_SIZE)]
  GetShipment --> ShipmentLoop{for each shipment}
  ShipmentLoop --> GetStatuses[get_shipment_statuses(cursor, shipment_id)]
  GetStatuses --> ForStatus{for each shipment_status}
  ForStatus --> CheckLongKey{check_key("current_longitude")}
  CheckLongKey -- false --> Continue1([continue])
  CheckLongKey -- true --> CheckLatKey{check_key("current_latitude")}
  CheckLatKey -- false --> Continue2([continue])
  CheckLatKey -- true --> ValidateLatLong{fv.utilities.is_valid_lat_long}
  ValidateLatLong -- false --> Error[/raise BadRequestError/]
  ValidateLatLong -- true --> ProcessRowCall[process_row(..., long, lat, source)]
  ProcessRowCall --> GetSeed[get_seed_locator(cursor,long,lat) (cached)]
  GetSeed --> SeedExists{seed_info != None && len>0}
  SeedExists -- true --> SkipWrite([pass])
  SeedExists -- false --> WriteDB[write_by_long_lat_db(...)]
  WriteDB --> DB[DB_CONN.cursor -> INSERT INTO geo_location]
  ShipmentLoop --> OffsetCheck{shipment_read >= total_count}
  OffsetCheck -- true --> EndLoop([offset = -1 -> exit loop])
  OffsetCheck -- false --> NextPage[get_shipment(cursor, offset+PAGE_SIZE, PAGE_SIZE)]
  NextPage --> ShipmentLoop
  EndLoop --> PrintTotal[print total records read]
  Error --> Stop([Stop])
```

> SVG rendering failed for this diagram.
