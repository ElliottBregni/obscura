# Diagram: common/fv/scripts/create_queue.py


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
flowchart TD
  Start([Start]) --> ParseArgs[/"parse_args()"/]
  ParseArgs --> Args{args}
  Args -->|f False| QueueNameReceived["queue_name = 'received-requests'"]
  Args -->|f True| QueueNameFailed["queue_name = 'failed-requests'"]
  QueueNameReceived --> QNStage["qn_stage = f\"{args.stage}-received-requests\""]
  QueueNameFailed --> QNStageFailed["qn_stage = f\"{args.stage}-failed-requests\""]
  QNStage --> BuildARNs["Build ARNs"]
  QNStageFailed --> BuildARNs
  BuildARNs --> SQS_ARN["sqs_arn = arn:aws:sqs:us-east-1:519940785508:{qn_stage}"]
  BuildARNs --> SNS_ARN["sns_arn = arn:aws:sns:us-east-1:519940785508:{qn_stage}"]
  SQS_Client["sqs_client = boto3.client('sqs')"] --> CreateQueue
  SNS_Client["sns_client = boto3.client('sns')"] --> CreateQueue
  CreateQueue["sqs_client.create_queue(QueueName=qn_stage, Attributes=Policy)"] --> Policy["Policy JSON with Condition ArnEquals aws:SourceArn = sns_arn"]
  Policy --> End([End])
```

> SVG rendering failed for this diagram.
