# Diagram: common/fv/serverless.fv.yml


> Auto-generated by Obscura crawlers

## Diagram 1

```mermaid
flowchart LR
    SVC[service: fvarm64]
    PROVIDER[provider: aws\nruntime: python3.13]
    ENV[AWS_STAGE, ACCOUNT_ID]
    PLUGINS[plugins\n- serverless-python-requirements\n- serverless-prune-plugin]
    VPC[vpc: ${self:custom.globalConfig.vpc}]
    PACKAGE[package patterns\n- !.serverless*/**\n- !./**\n- !__pycache__/**\n- python/**]
    LAYERS[fv layer]
    LAYER_PATH[path: ./]
    LAYER_NAME[name: fvarm64-${opt:stage}]
    CUSTOM[custom]
    CUSTOM_GLOBAL[globalConfig -> file(${env:FV_PATH}/config.global.yml)]
    PYREQS[pythonRequirements\n- dockerizePip: non-linux\n- layer: true]
    PRUNE[prune\n- automatic: true\n- includeLayers: true\n- number: 8]
    RESOURCES[resources.extensions\n- PythonRequirementsLambdaLayer: Retain\n- FvLambdaLayer: Retain]

    SVC --> PROVIDER
    PROVIDER --> ENV
    SVC --> PLUGINS
    SVC --> VPC
    SVC --> PACKAGE
    SVC --> LAYERS
    LAYERS --> LAYER_PATH
    LAYERS --> LAYER_NAME
    SVC --> CUSTOM
    CUSTOM --> CUSTOM_GLOBAL
    CUSTOM --> PYREQS
    CUSTOM --> PRUNE
    SVC --> RESOURCES
```

> SVG rendering failed for this diagram.

## Diagram 2

```mermaid
classDiagram
    class Provider {
        +name: aws
        +runtime: python3.13
        +environment: map
    }
    class LayerFV {
        +path: ./
        +name: fvarm64-${opt:stage}
        +UpdateReplacePolicy: Retain
    }
    class Custom {
        +globalConfig: file(${env:FV_PATH}/config.global.yml)
        +prune: config
        +pythonRequirements: config
    }
    class Plugins {
        +serverless-python-requirements
        +serverless-prune-plugin
    }
    class Resources {
        +PythonRequirementsLambdaLayer: Retain
        +FvLambdaLayer: Retain
    }
    Provider <|-- Provider
    LayerFV <-- Resources
    Custom <-- Provider
    Plugins <-- Provider
```

> SVG rendering failed for this diagram.
