# Diagram: common/monitoring/monitoring_tests/test_consumer.py


> Auto-generated by Obscura crawlers

## Diagram 1

```mermaid
flowchart TD
    A[Start: load_no_fail(dct, key)] --> B{key in dct?}
    B -- No --> C[Return (do nothing)]
    B -- Yes --> D[Value = dct[key]]
    D --> E{Value is str?}
    E -- Yes --> F{Is JSON string?}
    F -- Yes --> G[Parse JSON -> replace dct[key] with parsed object]
    F -- No --> H[Leave dct[key] as original string]
    E -- No --> I{Value is list?}
    I -- Yes --> J[Leave list unchanged]
    I -- No --> K[Leave value unchanged]
    G --> L[End]
    H --> L
    J --> L
    K --> L
    C --> L
```

> SVG rendering failed for this diagram.

## Diagram 2

```mermaid
flowchart LR
    A[Start: is_function_blacklisted(event)] --> B{event has "request" key?}
    B -- No --> C[Return False]
    B -- Yes --> D[req = event.request]
    D --> E{req is truthy string?}
    E -- No --> C
    E -- Yes --> F{Matches pattern "*-locations-get" OR startswith any of ["prod-","staging","dev-"] with "-locations-get"}
    F -- Yes --> G[Return True]
    F -- No --> C
```

> SVG rendering failed for this diagram.
