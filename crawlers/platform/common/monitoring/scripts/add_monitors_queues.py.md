# Diagram: common/monitoring/scripts/add_monitors_queues.py


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
flowchart TD
    components[components module]
    monitors[monitors module]
    argparse[argparse module]
    parse_args[parse_arguments()]
    main[__main__ entrypoint]
    get_lambdas[get_lambdas_from_service]
    create_received[create_sqs_monitors(stage, "received-requests")]
    create_failed[create_sqs_monitors(stage, "failed-requests")]
    args[args.stage]

    components -->|imports| get_lambdas
    components -->|imports| monitors
    components -->|imports| argparse
    argparse --> parse_args
    parse_args --> args
    main --> parse_args
    main --> create_received
    main --> create_failed
    monitors --> create_received
    monitors --> create_failed
    get_lambdas -.unused.-> components
```

> SVG rendering failed for this diagram.
