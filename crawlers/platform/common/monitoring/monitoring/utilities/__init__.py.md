# Diagram: common/monitoring/monitoring/utilities/__init__.py


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
flowchart TD
    Start([Start deep_update(d, u)]) --> Iterate["Iterate over u.items()"]
    Iterate --> IsMap{Is v a Mapping?}
    IsMap -- Yes --> Recurse[/d[k] = deep_update(d.get(k, {}), v)/]
    IsMap -- No --> Assign[/d[k] = v/]
    Recurse --> Continue{"More items?"}
    Assign --> Continue
    Continue -- Yes --> Iterate
    Continue -- No --> Return([Return d])
```

> SVG rendering failed for this diagram.
