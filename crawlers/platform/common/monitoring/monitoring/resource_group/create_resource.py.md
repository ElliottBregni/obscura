# Diagram: common/monitoring/monitoring/resource_group/create_resource.py


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
flowchart TD
    Start([Start]) --> ParseArgs[/"parse_arguments() -> stage"/]
    ParseArgs --> CreateClients{Create AWS clients}
    CreateClients -->|resource_client| ResourceClient[boto3.client("resource-groups")]
    CreateClients -->|cloud_client| CloudClient[boto3.client("cloudformation")]
    ResourceClient --> InitStacks[/"stacks = []; next_token = ''"/]
    CloudClient --> InitStacks
    InitStacks --> ListStacks[/"stack_list = cloud_client.list_stacks()"/]
    ListStacks --> CheckSummaries{stack_list["StackSummaries"]?}
    CheckSummaries -->|yes| Accumulate[/"stacks += stack_list['StackSummaries']"/]
    Accumulate --> HasNext{ "NextToken" in stack_list? }
    CheckSummaries -->|no| HasNext
    HasNext -->|yes| NextTokenSet[/"next_token = stack_list['NextToken']\nstack_list = cloud_client.list_stacks(NextToken=next_token)"/]
    NextTokenSet --> CheckSummaries
    HasNext -->|no| BuildServiceNames[/"service_with_stage = [f'{service}-{stage}' for service in SERVICES]"/]
    BuildServiceNames --> FilterStacks[/"filter stacks by name in service_with_stage and not DeletionTime"/]
    FilterStacks --> ForEachStack{for stack in stacks}
    ForEachStack --> ExtractName[/"StackName = stack['StackName'].split(f'-{stage}')[0]"/]
    ExtractName --> TryCreate[/"try: resource_client.create_group(Name=f'{stage}-{StackName}', ResourceQuery=...)" /]
    TryCreate --> CreatedLog[/"print(f'Created {stage}-{StackName}')"/]
    TryCreate -->|ClientError| ExceptPass[/"except ClientError: pass"/]
    CreatedLog --> ForEachStack
    ExceptPass --> ForEachStack
    ForEachStack --> End([End])
```

> SVG rendering failed for this diagram.
