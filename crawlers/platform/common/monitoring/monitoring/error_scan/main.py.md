# Diagram: common/monitoring/monitoring/error_scan/main.py


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
flowchart LR
  A[Start: script executed] --> B[Initialize DB_CONN = FvDatabaseConnector("scan_event_audit_error", SecretNames.DATABASE_LOGGING)]
  B --> C[logging.basicConfig(level=INFO)]
  C --> D[now = datetime.datetime.utcnow()]
  D --> E[DB_CONN.establish_connection()]
  E --> F[Create ErrorScanner(cursor=DB_CONN.get_cursor(), period=timedelta(days=1), stage=os.getenv("AWS_STAGE"))]
  F --> G[recent_errors = ErrorScanner.get_all_recent_errors()]
  G --> H{recent_errors exists?}
  H -- No --> I[Exit, no recent errors]
  H -- Yes --> J[print "Found N unique errors!"]
  J --> K[now_formatted = datetime.strftime(now, "%Y-%m-%d--%H-%M-%S")]
  K --> L[filename = expanduser("~/error-scan-" + now_formatted + ".xlsx")]
  L --> M[Create ErrorWriter(filename)]
  M --> N[ErrorWriter.write_all(recent_errors)]
  N --> O[print "Done! Wrote to {filename}"]
  O --> P[End]
```

> SVG rendering failed for this diagram.
