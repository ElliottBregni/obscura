# Diagram: common/monitoring/monitoring/add_metric.py


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
flowchart LR
  AMF[add_metric_filter(stage, func, metricName, filterPattern)] -->|put_metric_filter| PUTMF["boto3 client.put_metric_filter\n(logGroupName, filterPattern, metricTransformations)"]
  ALE[add_log_event(stage, func)] -->|calls|getSeq[get_sequence_data(logGroupName)]
  getSeq -->|describe_log_streams| DESCR["boto3 client.describe_log_streams\n(orderBy=LastEventTime, limit=1)"]
  ALE -->|uses returned token & stream| PLE[put_log_event(stage, func, logStreamName, sequence_token, curTime)]
  PLE -->|put_log_events| PUTLE["boto3 client.put_log_events\n(logEvents, sequenceToken)"]
  GM[add_metric(stage, function, metricName)] -->|put_metric_data| PUTMD["boto3 cloudwatch_client.put_metric_data\n(Namespace=CustomLambdas, MetricData)"]
  COMP[get_lambdas (components)] -.-> ALE
  getSeq -->|returns| PLE
  subgraph AWS
    PUTMF
    DESCR
    PUTLE
    PUTMD
  end
```

> SVG rendering failed for this diagram.
