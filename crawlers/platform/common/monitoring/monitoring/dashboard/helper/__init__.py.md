# Diagram: common/monitoring/monitoring/dashboard/helper/__init__.py


> Auto-generated by Obscura crawlers

## Diagram 1

```mermaid
classDiagram
    class constants {
        <<module>>
        +ORANGE_HEX
        +RED_HEX
        +BLUE_HEX
    }
    class error_json {
        +error_json(stage, lmbda, title, colors=[ORANGE_HEX,RED_HEX], width=9)
        +returns: dict {width,x,properties{metrics,title}}
    }
    class build_base_widget {
        +build_base_widget(title, x=0, width=12, metrics=[])
        +returns: dict {width,x,y,properties{metrics,title}}
    }
    class build_error {
        +build_error(stage, lmbda, error="4xx", color=None)
        +returns: list ["CustomLambdas", metricName, (optional) {color}]
    }
    class aws_json {
        +aws_json(stage, lmbda, title, metric, stat="Sum", color=BLUE_HEX, width=9)
        +returns: dict {width,x,properties{metrics,title,stat}}
    }
    constants <|-- error_json
    constants <|-- aws_json
    build_error <|-- error_json
    build_base_widget <-- error_json
    build_base_widget <-- aws_json
```

> SVG rendering failed for this diagram.

## Diagram 2

```mermaid
flowchart LR
    A[error_json(stage, lmbda, title, colors, width)]
    B[build_error(stage,lmbda,error,color)]
    C[build_base_widget(title,x,width,metrics)]
    D[aws_json(stage,lmbda,title,metric,stat,color)]
    E[constants module]
    A -->|uses| E
    D -->|uses| E
    A -->|composes metrics from| B
    A -->|returns widget dict compatible with| C
    D -->|conditional x position when metric == "Duration"| D
    D -->|returns widget dict with AWS/Lambda metric| C
```

> SVG rendering failed for this diagram.
