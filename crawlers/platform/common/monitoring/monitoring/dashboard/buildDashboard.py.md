# Diagram: common/monitoring/monitoring/dashboard/buildDashboard.py


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
flowchart LR
  Start([start]) --> Parse[/"parse_arguments()"/]
  Parse --> SetArgs{args: service, stage, -l, -r}
  SetArgs --> DetermineService{service == "all"?}
  DetermineService -- yes --> ServicesList[constants.FULL_SERVICE_NAMES]
  DetermineService -- no --> ServicesSingle[ [service] ]
  ServicesList --> ForEachService{for service in services}
  ServicesSingle --> ForEachService
  ForEachService --> BuildDashboardName{args.r ? resource-group dashboard : normal}
  BuildDashboardName -- resource --> Name1[CloudWatch-Default-{stage}-{service}]
  BuildDashboardName -- normal --> Name2[{stage}-{service}]
  BuildDashboardName --> PrintName[/print(dashboard_name)/]
  PrintName --> GetLambdas{args.l ? use SHIP_LAMBDAS : get_lambdas_from_service(stage, service)}
  GetLambdas -- use SHIP_LAMBDAS --> LambdasConst[constants.SHIP_LAMBDAS]
  GetLambdas -- fetch --> LambdasFetch[get_lambdas_from_service(stage, service) --> sorted list]
  LambdasConst --> CheckNone{len(lambdas) == 0?}
  LambdasFetch --> CheckNone
  CheckNone -- yes --> Error[/"raise ValueError('No lambdas found')"/]
  CheckNone -- no --> CreateDashboard[Dd = Dashboard(dashboard_name)]
  CreateDashboard --> IfResource{if args.r}
  IfResource -- true --> InitErrWidgets[create err_4xx_values, err_5xx_values with build_base_widget]
  IfResource -- false --> LoopLambdas{for lmbda in lambdas}
  InitErrWidgets --> LoopLambdas
  LoopLambdas --> ResourceMode{if not args.r}
  ResourceMode -- false (resource mode) --> StripName[lmbda = lmbda.split(f"{stage}-")[1]]
  StripName --> AppendErr4xx[err_4xx_values.properties.metrics.append(build_error(stage,lmbda,"4xx"))]
  AppendErr4xx --> AppendErr5xx[err_5xx_values.properties.metrics.append(build_error(stage,lmbda,"5xx"))]
  ResourceMode -- true (normal mode) --> BuildTitles[set error_title, dur_title, invo_title]
  BuildTitles --> ErrorVals[error_json(stage,lmbda,error_title)]
  BuildTitles --> DurationVals[aws_json(stage,lmbda,dur_title,"Duration",stat="Average")]
  BuildTitles --> InvocVals[aws_json(stage,lmbda,invo_title,"Invocations",stat="Sum",color=constants.GREEN_HEX,width=6)]
  ErrorVals --> PutError[Dd.put_widget(error_title, error_values)]
  DurationVals --> PutDuration[Dd.put_widget(dur_title, duration_values)]
  InvocVals --> PutInvoc[Dd.put_widget(invo_title, invo_values)]
  PutError --> ContinueLoop --> LoopEnd
  PutDuration --> ContinueLoop
  PutInvoc --> ContinueLoop
  AppendErr5xx --> ContinueLoop
  ContinueLoop --> AfterLambdas{after loop}
  AfterLambdas -- if args.r --> PutAggWidgets[Dd.put_widget(err_4xx_title, err_4xx_values) & Dd.put_widget(err_5xx_title, err_5xx_values)]
  AfterLambdas --> Finalize[Dd.put_dashboard()]
  Finalize --> End([end])
```

> SVG rendering failed for this diagram.
