# Diagram: common/monitoring/monitoring/dashboard/Dashboard.py


> Auto-generated by Obscura crawlers

## Diagram 1

```mermaid
classDiagram
    class Dashboard {
        +name
        +client_cloud
        +dashboard
        +widgets
        +__init__(name)
        +put_dashboard()
        +put_widget(title, values={})
        +get_widget(title)
        +get_widget_index(title)
        +correct_widget_json(jsonStr)
        +build_str_widgets()
        +order_widgets()
        +__str__()
    }
    class Widget
    class boto3_client
    class ClientError
    Dashboard --> Widget : contains
    Dashboard ..> boto3_client : uses
    Dashboard ..> ClientError : handles
```

> SVG rendering failed for this diagram.

## Diagram 2

```mermaid
flowchart TD
    PD[put_dashboard()] --> OW[order_widgets()]
    OW --> BS[build_str_widgets()]
    BS --> CJ[correct_widget_json(...)]
    CJ --> PUT[client_cloud.put_dashboard(...)]
    PD -->|calls| PUT
    subgraph widget_flow [put_widget/title handling]
        GW[get_widget(title)] -->|found| UP[update existing Widget]
        GW -->|not found| NW[append new Widget(values)]
    end
    PD --> widget_flow
```

> SVG rendering failed for this diagram.
