# Diagram: common/monitoring/monitoring/monitors.py


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
flowchart LR
    A[boto3.client('cloudwatch')] --> create_db_monitors
    A --> create_sqs_monitors
    A --> create_lambda_monitors

    create_db_monitors["create_db_monitors(environment, db_name, metric_name, statistic, threshold, critical=True, ComparisonOperator='GreaterThanThreshold', unit='Gigabytes')"]
    create_db_monitors --> DBAlarm[put_metric_alarm: AWS/RDS, MetricName=metric_name]
    DBAlarm --> DBDimensions[Dimensions: DBInstanceIdentifier=db_name]
    DBAlarm --> SNS[OKActions/AlarmActions -> arn:aws:sns:...:{environment}-cloudwatch-alerts]

    create_sqs_monitors["create_sqs_monitors(environment, queue_name='received-shipment-requests', threshhold_dict={})"]
    create_sqs_monitors --> SQS_Crit[put_metric_alarm: ApproximateNumberOfMessagesVisible (critical)]
    create_sqs_monitors --> SQS_Warn[put_metric_alarm: ApproximateNumberOfMessagesVisible (warning)]
    create_sqs_monitors --> SQS_NotVis_Crit[put_metric_alarm: ApproximateNumberOfMessagesNotVisible (critical)]
    create_sqs_monitors --> SQS_NotVis_Warn[put_metric_alarm: ApproximateNumberOfMessagesNotVisible (warning)]
    SQS_Crit --> SQSDimensions[Dimensions: QueueName=environment-queue_name]
    SQS_Warn --> SQSDimensions
    SQS_NotVis_Crit --> SQSDimensions
    SQS_NotVis_Warn --> SQSDimensions
    SQS_Crit --> SNS
    SQS_Warn --> SNS
    SQS_NotVis_Crit --> SNS
    SQS_NotVis_Warn --> SNS

    create_lambda_monitors["create_lambda_monitors(environment, lambda_name, telematics=False)"]
    create_lambda_monitors --> Lambda_5xx_Crit[put_metric_alarm: Namespace=CustomLambdas, MetricName={environment}-{lambda_name}-5xxErrors (critical)]
    create_lambda_monitors --> Lambda_5xx_Warn[put_metric_alarm: Namespace=CustomLambdas, MetricName={environment}-{lambda_name}-5xxErrors (warning)]
    create_lambda_monitors --> Lambda_Duration[put_metric_alarm: Namespace=AWS/Lambda, MetricName=Duration (warning)]
    Lambda_Duration --> LambdaDimensions[Dimensions: FunctionName=environment-lambda_name]
    Lambda_5xx_Crit --> SNS
    Lambda_5xx_Warn --> SNS
    Lambda_Duration --> SNS

    subgraph Thresholds
        crit_threshhold[crit_threshold: default 100000 or threshhold_dict.get('crit_threshold')]
        warning_threshold[warning_threshold: default 10000 or threshhold_dict.get('warning_threshold')]
        lambda_threshold[lambda threshold: 10000 or 180000 if telematics]
    end

    SQS_Crit --> crit_threshhold
    SQS_NotVis_Crit --> crit_threshhold
    SQS_Warn --> warning_threshold
    SQS_NotVis_Warn --> warning_threshold
    Lambda_Duration --> lambda_threshold
```

> SVG rendering failed for this diagram.
