# Diagram: common/monitoring/monitoring/lambdas/monitoring/event_dispatcher_monitor.py


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
classDiagram
    class FvDatabaseConnector {
        +establish_connection()
        +cursor
    }
    class CloudWatchClient {
        +put_metric_data(MetricData)
    }
    class QueryBuilder {
        +get_previous_interval_query(cursor, time_range_minutes)
    }
    class MetricsLogger {
        +log_metrics(pending, claimed, processing, complete, unavailable, invalid, failure, failure_rate)
    }
    class Monitor {
        +run_monitor(application_name)
    }
    class LambdaHandler {
        +lambda_handler(event, context, audit_refs)
    }
    FvDatabaseConnector --> QueryBuilder : provides cursor
    Monitor --> QueryBuilder : builds SQL query
    Monitor --> FvDatabaseConnector : establishes connection / uses cursor
    Monitor --> MetricsLogger : sends aggregated results
    MetricsLogger --> CloudWatchClient : calls put_metric_data
    LambdaHandler --> Monitor : invokes run_monitor
    MetricsLogger ..> "Environment(AWS_STAGE,RUN_LOCAL)" : reads variables
    LambdaHandler ..> "fv.aws.lambdas" : decorated by mandatory_lambda_handling
    Monitor ..> "Postgres(event_dispatch table)" : queries counts by state
```

> SVG rendering failed for this diagram.
