# Diagram: common/monitoring/monitoring/lambdas/monitoring/finished_vehicle_sync_monitor.py


> Auto-generated by Obscura crawlers

## Diagram 1

```mermaid
flowchart LR
  LH[lambda_handler(event, context, audit_refs)] --> RM[run_monitor(application_name)]
  RM --> DB[DB_CONN (FvDatabaseConnector)]
  DB --> DBE[establish_connection()]
  RM --> CURS[cursor]
  RM --> GPQ[get_previous_interval_query(cursor, time_range_minutes)]
  GPQ --> SQL[SQL RETURNED]
  CURS -->|execute(query)| SQL
  SQL -->|fetchone()| RES{results is not None?}
  RES -->|yes| EXTRACT[extract claimed, processing, pending, failure, complete, failure_rate]
  RES -->|no| ZERO[set all metrics to 0]
  EXTRACT --> LM[log_metrics(pending, claimed, processing, complete, failure, failure_rate)]
  ZERO --> LM
  LM --> CW[cloudwatch_client.put_metric_data(Namespace, MetricData)]
  CW -->|response| LOG[logging.info('metrics...')]
  subgraph AWS
    CW
  end
  subgraph DBLayer
    DB
    CURS
  end
```

> SVG rendering failed for this diagram.

## Diagram 2

```mermaid
classDiagram
  class FvDatabaseConnector {
    +establish_connection()
    +cursor
    -dsn
  }
  class boto3_client {
    +put_metric_data(Namespace, MetricData)
  }
  class ModuleFunctions {
    +get_previous_interval_query(cursor, time_range_minutes)
    +log_metrics(pending, claimed, processing, complete, failure, failure_rate)
    +run_monitor(application_name)
    +lambda_handler(event, context, audit_refs)
  }
  FvDatabaseConnector "1" <-- "1" : DB_CONN
  boto3_client <-- ModuleFunctions : uses
  ModuleFunctions ..> FvDatabaseConnector : interacts with
  ModuleFunctions ..> boto3_client : sends metrics to
  note for ModuleFunctions "Entrypoints and helpers for monitoring and metric reporting"
```

> SVG rendering failed for this diagram.
