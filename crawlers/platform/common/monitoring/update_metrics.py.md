# Diagram: common/monitoring/update_metrics.py


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
flowchart LR
    A[Start] --> B[parse_arguments()]
    B --> C[Print: Getting lambdas]
    C --> D[get_lambdas(stage) → lambdas]
    D --> E[Create cloudwatch_client = boto3.client(cloudwatch)]
    E --> F[Create logs_client = boto3.client(logs)]
    F --> G[For each func in lambdas (loop)]
    G --> H{Try}
    H --> I[add_metric_filter(logs_client, stage, func, metricName: stage-func-4xxErrors, filterPattern: 4* patterns)]
    I --> J[add_metric_filter(logs_client, stage, func, metricName: stage-func-5xxErrors, filterPattern: 5* patterns)]
    J --> K[Print: added func]
    H --> L[Except ClientError]
    L --> M[Print: Could not create metric func, error]
    K --> N[End]
    M --> N
```

> SVG rendering failed for this diagram.
