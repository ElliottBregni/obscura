# Diagram: common/comment_service/comment_service/db/user.py


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
flowchart LR
  A[get_or_create_user(reference_id, email, organization_id)] --> B[execute_query: INSERT INTO user ... RETURNING id, email]
  B --> C[user_db]
  A --> D[get_or_create_organization(organization_id)]
  D --> E[organization_db]
  C --> F[get_or_create_user_can_act_as(user_db.id, organization_db.id)]
  F --> G[get_user_can_act_as(user_id, organization_id) -> SELECT id FROM can_act_as]
  G --> H{user_can_act_as exists?}
  H -- yes --> I[return user_can_act_as]
  H -- no --> J[create_user_can_act_as(user_id, organization_id)]
  J --> K[execute_query: INSERT INTO can_act_as ... RETURNING id]
  K --> L[get_user_can_act_as(user_id, organization_id)]
  L --> I
  I --> M[return user_db]
```

> SVG rendering failed for this diagram.
