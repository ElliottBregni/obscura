# Diagram: common/comment_service/comment_service/db/object.py


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
flowchart TD
    GO[get_or_create_object(organization_id, system_name, reference_type, reference_id)]
    GO --> GET[get_object(organization_id, system_name, reference_type, reference_id)]
    GO --> CREATE[create_object(organization_id, system_name, reference_type, reference_id)]
    GET --> GS1[get_system(system_name) -> system_id]
    GET --> Q1[execute_query(SELECT id ...)]
    Q1 --> R1{result truthy?}
    R1 -- yes --> RET1[return result[0]]
    R1 -- no --> NULL1[return None]
    CREATE --> GS2[get_system(system_name) -> system_id]
    CREATE --> Q2[execute_query(INSERT ... RETURNING id)]
    Q2 --> CALL_GET[get_object(...) to fetch inserted row]
    CALL_GET --> RET2[return db row]
    GO --> OUT{db_object exists?}
    OUT -- yes --> OUT1[return db_object]
    OUT -- no --> OUT2[call create_object and return result]
```

> SVG rendering failed for this diagram.
