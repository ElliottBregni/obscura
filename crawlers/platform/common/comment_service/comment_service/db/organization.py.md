# Diagram: common/comment_service/comment_service/db/organization.py


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
graph TD
    A[get_or_create_organization(reference_id)] --> B[Cast reference_id to int]
    B --> C{organization exists?}
    C -->|Yes| D[return organization]
    C -->|No| E[create_organization(reference_id)]
    subgraph CreateFlow
        E --> F[SQL: INSERT INTO DB_SCHEMA.organization (reference_id) VALUES (%s) RETURNING id]
        F --> G[execute_query(sql, reference_id)]
        G --> H[get_organization(reference_id)]
        H --> I[SQL: SELECT id FROM DB_SCHEMA.organization WHERE reference_id = %s]
        I --> J[execute_query(sql, reference_id)]
        J --> K[if result: return result[0]]
    end
    K --> L[return organization]
    D --> M[END]
    L --> M
```

> SVG rendering failed for this diagram.
