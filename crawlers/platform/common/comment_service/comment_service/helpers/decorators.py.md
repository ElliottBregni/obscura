# Diagram: common/comment_service/comment_service/helpers/decorators.py


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
flowchart LR
  A[db_request(validate_data?, validate_query_string?)] --> B[wrapper(*args, **kwargs)]
  B --> C[build_request_object(*args, **kwargs) -> request]
  C --> D{Validate request?}
  D -->|validate_data or validate_query_string| E[_validate_data_or_raise_exception(request, validate_data, validate_query_string)]
  E --> F[Validator(request.data or params, schema) -> (valid, errors)]
  F -->|errors| G[raise ValidationError]
  G --> H[make_error_response(event, context, ... BAD_REQUEST)]
  E -->|no errors| I[get_connection() -> connection]
  I --> J[connection.establish_connection()]
  J --> K[request.cursor = connection.cursor]
  K --> L[request.user = get_or_create_user(user_id, user_email, org_id)]
  L --> M[try: response = f(request, **kwargs)]
  M -->|DatabaseError| N[make_error_response(event, context, ... DATABASE_ERROR)]
  M -->|success| O[request.cursor.commit() -> return response]
  D -->|no validation required| I
```

> SVG rendering failed for this diagram.
