# Diagram: common/comment_service/comment_service/comment_read_post.py


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
flowchart LR
    A[lambda_handler(request)] --> B[Apply decorators]
    B --> C[mandatory_lambda_handling()]
    B --> D[db_request(validations)]
    A --> E[data = request.data]
    E --> F[read_comment(user.id,\nowner_id,\norganization_id,\nsystem_name,\nreference_type,\nreference_id,\ndate_until)]
    F --> G[make_response(HTTPStatus.NO_CONTENT.value)]
    subgraph VALIDATIONS["validations"]
        V1["date_until: {type: [string], required: True}"]
        V2["system_name: {type: [string], required: True}"]
        V3["reference_type: {type: [string], required: True}"]
        V4["reference_id: {type: [string], required: True}"]
    end
    D --> VALIDATIONS
```

> SVG rendering failed for this diagram.
