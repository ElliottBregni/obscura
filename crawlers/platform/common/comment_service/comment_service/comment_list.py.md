# Diagram: common/comment_service/comment_service/comment_list.py


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
flowchart TD
  subgraph Decorators
    MAND[mandatory_lambda_handling()] --> DBREQ[db_request(validate_query_string=validations)]
  end

  Request[Incoming Request<br/>(request)] --> MAND
  DBREQ --> Handler[lambda_handler(request)]

  Handler -->|reads| Args[request.args (data)]
  Handler -->|reads| User[request.user.id]
  Handler -->|reads| Owner[request.owner_id]
  Handler -->|reads| Org[request.organization_id]

  Args -->|get PAGE_NUMBER| PageNum[page_number]
  Args -->|get PAGE_SIZE| PageSize[page_size]
  PageNum --> ListCall
  PageSize --> ListCall

  Handler -->|calls| ListCall[list_comments(user_id, owner_id, organization_id, system_name, reference_type, reference_id, page_number, page_size)]
  ListCall --> Paginated[paginated_comments]
  Paginated -->|data field| Serialize[comment_serializer(paginated_comments["data"])]
  Serialize --> PaginatedUpdated[paginated_comments (data serialized)]

  PaginatedUpdated -->|returns| MakeResp[make_response(paginated_comments, HTTPStatus.OK.value)]
  MakeResp --> Response[HTTP Response (200 OK)]

  subgraph Validations
    V_system[system_name: string, required]
    V_ref_type[reference_type: string, required]
    V_ref_id[reference_id: string, required]
    V_page_num[PAGE_NUMBER: number, optional]
    V_page_size[PAGE_SIZE: number, optional]
  end

  DBREQ --> Validations
  Validations -->|validated| Handler
```

> SVG rendering failed for this diagram.
