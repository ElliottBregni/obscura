# Diagram: common/comment_service/comment_service/serializers.py


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
flowchart LR
  CS[comment_serializer(db_comment)] --> CHECK_LIST{is instance of list?}
  CHECK_LIST -- Yes --> MAP[List comprehension: do_serialize(i) for i in db_comment]
  CHECK_LIST -- No --> CALL_DS[call do_serialize(db_comment)]
  CALL_DS --> BUILD_RESULT[build result dict with id, owner_id, text, ts, modified, email, shared_with, batch_flag]
  BUILD_RESULT --> CHECK_READ{getattr(db_comment, "read", None) is not None?}
  CHECK_READ -- Yes --> ADD_READ[add "read" to result]
  CHECK_READ -- No --> SKIP_READ[skip read]
  ADD_READ --> RETURN_RESULT[return result]
  SKIP_READ --> RETURN_RESULT
  MAP --> RETURN_LIST[return list of serialized items]
  CALL_DS --> CHECK_TS_TS[serialize ts and modified via _date_serializer]
  CHECK_TS_TS --> DS[_date_serializer(value)]
  DS --> IF_VALUE{value?}
  IF_VALUE -- True --> ISO[value.isoformat()]
  IF_VALUE -- False --> NULL[return None]
```

> SVG rendering failed for this diagram.
