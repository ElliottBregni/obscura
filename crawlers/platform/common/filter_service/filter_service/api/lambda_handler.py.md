# Diagram: common/filter_service/filter_service/api/lambda_handler.py


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
flowchart TD
  subgraph AWS_Lambda
    direction TB
    GFL[get_filter_value_list_lambda_handler]
    AUF[add_update_filter_value_lambda_handler]
  end

  decorator[(mandatory_lambda_handling)]
  decorator --> GFL
  decorator --> AUF

  GFL -->|calls| GetFilterValueListClass[GetFilterValueList]
  GetFilterValueListClass -->|prepare() → process() → get_formatted_result()| Result1[formatted result]
  Result1 -->|make_response(result, 200)| make_response1[make_response]

  AUF -->|reads event.httpMethod| HTTPVerb[HTTP verb]
  HTTPVerb -->|GET| GetFV[GetFilterValue]
  HTTPVerb -->|POST| AddFV[AddFilterValue]
  HTTPVerb -->|PUT| PutFV[PutFilterValue]
  HTTPVerb -->|PATCH| PatchFV[PatchFilterValue]
  HTTPVerb -->|DELETE| DeleteFV[DeleteFilterValue]
  HTTPVerb -->|missing| BadReq1[BadRequestError("No HTTP verb found")]

  subgraph Handlers
    GetFV
    AddFV
    PutFV
    PatchFV
    DeleteFV
  end

  GetFV -->|prepare() → process() → get_formatted_result()| Result2[formatted result]
  AddFV -->|prepare() → process() → get_formatted_result()| Result3[formatted result]
  PutFV -->|prepare() → process() → get_formatted_result()| Result4[formatted result]
  PatchFV -->|prepare() → process() → get_formatted_result()| Result5[formatted result]
  DeleteFV -->|prepare() → process() → get_formatted_result()| Result6[formatted result]

  Result2 -->|make_response(result, 200)| make_response2[make_response]
  Result3 -->|make_response(result, 201)| make_response3[make_response]
  Result4 -->|make_response(result, 200)| make_response4[make_response]
  Result5 -->|make_response(result, 200)| make_response5[make_response]
  Result6 -->|make_response(result, 200)| make_response6[make_response]

  GFL ---|AssertionError| BadReq2[BadRequestError]
  AUF ---|AssertionError| BadReq2

  classDef aws fill:#f9f,stroke:#333,stroke-width:1px;
  class AWS_Lambda aws
```

> SVG rendering failed for this diagram.
