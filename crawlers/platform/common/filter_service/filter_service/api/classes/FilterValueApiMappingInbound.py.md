# Diagram: common/filter_service/filter_service/api/classes/FilterValueApiMappingInbound.py


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
classDiagram
    class ApiMapping {
        <<abstract>>
        +set_api_name(name)
        +set_uuid_fields(fields)
        +set_string_fields(fields)
        +set_timestamp_fields(field)
    }

    class BidirectionalMap {
        +add(key, value)
    }

    class FilterValueApiMappingInbound {
        -BIDIRECTIONAL_MAP : BidirectionalMap
        +__init__()
        +set_api_mapping()
    }

    ApiMapping <|-- FilterValueApiMappingInbound
    FilterValueApiMappingInbound --> BidirectionalMap : uses/BIDIRECTIONAL_MAP
    note right of FilterValueApiMappingInbound
      BIDIRECTIONAL_MAP maps:
      "label" <-> "label"
      "value" <-> "value"
      "expiryTs" <-> "expiry_ts"
    endnote
```

> SVG rendering failed for this diagram.
