# Diagram: common/filter_service/filter_service/api/classes/PatchFilterValue.py


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
classDiagram
    class PatchFilterValue {
        +__init__()
        +prepare(aws_event)
        +process()
        +get_formatted_result()
        -application_name: str = "filter-value-list"
    }

    class FilterValueApiBaseClass {
        <<abstract>>
        +load_from_aws_event(event)
        +set_data(data)
        +get_template()
        +get_data()
    }

    class FilterValueList {
        +FilterValueList(application_name)
        +patch_filter(template)
    }

    class FilterValueApiMappingOutbound {
        +set_bidirectional_map(map)
        <<static>> BIDIRECTIONAL_MAP
    }

    class MapAction {
        <<static>>
        +proper_map_persistable_to_payload(mapping, data, flag)
    }

    PatchFilterValue --|> FilterValueApiBaseClass
    PatchFilterValue --> FilterValueList : uses BUSINESS_LAYER
    PatchFilterValue --> FilterValueApiMappingOutbound : instantiates
    PatchFilterValue --> MapAction : calls static method
    FilterValueApiMappingOutbound ..> MapAction : used by
    FilterValueList <-- "BUSINESS_LAYER instance" : instantiated with APPLICATION_NAME
    note left of PatchFilterValue : APPLICATION_NAME = "filter-value-list"
```

> SVG rendering failed for this diagram.
