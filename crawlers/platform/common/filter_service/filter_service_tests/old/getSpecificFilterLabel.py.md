# Diagram: common/filter_service/filter_service_tests/old/getSpecificFilterLabel.py


> Auto-generated by Obscura crawlers

## Diagram 1

```mermaid
classDiagram
    class DictionaryToObject {
        <<from fv_framework.utility.DictionaryToObject>>
        +__init__()
        +toObject()
    }
    class Authorizer {
        <<from localTest.core.Authorizer>>
        +setUsername(username)
        +syncFromAuth0()
    }
    class CanonicalEvent {
        <<from localTest.core.CanonicalEvent>>
        +setAuthorizer(auth)
        +setHttpMethod(method)
        +setAcceptHeader(header)
        +parseUri(uri)
        +addPathParameter(key, value)
        +prepare()
        +toAwsEvent()
    }
    class LambdaHandler {
        <<from filter_service.api.lambda_handler>>
        +add_update_filter_value_lambda_handler(event, context)
    }
    DictionaryToObject --> LambdaHandler : used to wrap config
    Authorizer --> CanonicalEvent : provides authorizer
    CanonicalEvent --> LambdaHandler : produces event
    LambdaHandler --> "retval" : returns response
```

> SVG rendering failed for this diagram.

## Diagram 2

```mermaid
flowchart TD
    Start([start]) --> CreateAuthorizer[Create Authorizer]
    CreateAuthorizer --> SetUsername[setUsername("dave.damon@freightverify.com")]
    SetUsername --> SyncAuth0[syncFromAuth0()]
    SyncAuth0 --> BuildURI[Build URI with APPLICATION_ID, tenant, filterName, label]
    BuildURI --> CreateEvent[Create CanonicalEvent and set properties]
    CreateEvent --> PrepareEvent[prepare() -> toAwsEvent()]
    PrepareEvent --> CallLambda[call add_update_filter_value_lambda_handler(event, DictionaryToObject(...))]
    CallLambda --> PrintRetval[print(retval)]
    PrintRetval --> End([end])
```

> SVG rendering failed for this diagram.
