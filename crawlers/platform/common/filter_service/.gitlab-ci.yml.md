# Diagram: common/filter_service/.gitlab-ci.yml


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
flowchart TD
  A{Changes in<br/>platform/common/filter_service/** or<br/>fv_framework python/**?}
  A -- yes --> B[Execute before_script: platform-base before_script]
  A -- no --> Z[Job skipped]
  B --> C[Run py-test login script]
  C --> D[pip install -e ${FV_REPO_DIR}/platform/fv_core/fv_framework/python]
  D --> E[Run pytest --cov-report xml:coverage.xml --cov-branch platform/common/filter_service/]
  E --> F[Generate coverage.xml]
  E --> G{Tests result}
  G -- pass --> H[Job succeeds]
  G -- fail --> I[Job fails / report errors]
```

> SVG rendering failed for this diagram.
