# Diagram: common/document_service/helm/templates/service.yaml


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
classDiagram
    class Service {
        +apiVersion: v1
        +kind: Service
        +name: document-service.fullname
        +labels: app=document-service.name
    }
    class Metadata {
        +name: {{ include "document-service.fullname" . }}
        +labels: app: {{ include "document-service.name" . }}
    }
    class Spec {
        +type: {{ .Values.serviceType | default "ClusterIP" }}
        +ports: Port[]
        +selector: Selector
    }
    class Port {
        +port: {{ .Values.port }}
        +targetPort: {{ .Values.port | default 8080 }}
    }
    class Selector {
        +app: {{ include "document-service.name" . }}
    }

    Service o-- Metadata : has
    Service o-- Spec : has
    Spec o-- Port : contains
    Spec o-- Selector : contains
```

> SVG rendering failed for this diagram.
