# Diagram: common/document_service/src/api/tests/integration/test_end_to_end.py


> Auto-generated by Obscura crawlers

## Diagram 1

```mermaid
flowchart TD
    A[Start Test: direct upload] --> B[Generate test_id]
    B --> C[Read test.pdf]
    C --> D[POST /document (direct upload) -> doc{id, documentUrl}]
    D --> E[Track upload via s3_utils]
    E --> F{verify upload successful?}
    F -- yes --> G[Assert DB row matches metadata]
    G --> H[Batch associate doc -> object_id]
    H --> I[Assert association exists in DB]
    I --> J[GET presigned download URL]
    J --> K[Assert presigned URL shape]
    K --> L[Download content and compare -> matches file_bytes]
    L --> M[PATCH /document/{doc_id} update metadata]
    M --> N[Assert DB row updated]
    N --> O[DELETE /document/{doc_id}]
    O --> P[Assert DB row deleted]
    P --> Q[Verify storage object removed]
    Q --> Z[End Test]
```

> SVG rendering failed for this diagram.

## Diagram 2

```mermaid
flowchart TD
    A2[Start Test: presigned upload] --> B2[Generate test_id]
    B2 --> C2[Request presigned upload URL]
    C2 --> D2[Assert presigned upload URL shape]
    D2 --> E2[Upload bytes to presigned URL]
    E2 --> F2[Track upload via s3_utils]
    F2 --> G2{verify upload successful?}
    G2 -- yes --> H2[POST /document/metadata -> create doc_id]
    H2 --> I2[Batch associate doc with multiple object_ids]
    I2 --> J2[For each period in PT1M, PT5M:]
    J2 --> K2[GET presigned download URL]
    K2 --> L2[Assert presigned URL shape]
    L2 --> M2[Download content and compare -> matches file_bytes]
    M2 --> N2[PATCH /document/{doc_id} update metadata]
    N2 --> O2[Assert DB row updated]
    O2 --> P2[DELETE /document/{doc_id}/associations]
    P2 --> Q2[DELETE /document/{doc_id}]
    Q2 --> R2[Verify storage object removed]
    R2 --> Z2[End Test]
```

> SVG rendering failed for this diagram.
