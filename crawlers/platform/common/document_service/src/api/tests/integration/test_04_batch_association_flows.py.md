# Diagram: common/document_service/src/api/tests/integration/test_04_batch_association_flows.py


> Auto-generated by Obscura crawlers

## Diagram 1

```mermaid
flowchart LR
  Fixture[pytest fixture: docs] --> DocA[create_document_metadata: A]
  Fixture --> DocB_conflict[create_document_metadata: B_conflict]
  DocA -->|used by| BatchAssoc[TestBatchDocumentToObjectsAssociation]
  DocB_conflict -->|used by| BatchAssoc
  BatchAssoc --> AtomicAtomic["POST /document/{id}/associations (mode=atomic)"]
  BatchAssoc --> AtomicConflict["POST /document/{id}/associations (conflict -> 409)"]
  BatchAssoc --> BestEffort["POST /document/{id}/associations (mode=best-effort)"]
  BatchAssoc --> Dups["POST /document/{id}/associations (duplicate objects)"]
  AtomicAtomic --> DB[get_associations_for_doc(session)]
  AtomicConflict -->|rollback on conflict| DB
  BestEffort -->|skip conflicts| DB
  Dups -->|dedupe| DB
  DB --> Assertions{"assertions"}
  Assertions --> AssertCount["len(refs) checks"]
  Assertions --> AssertIDs["object id set checks"]
  classDef fixtures fill:#f9f,stroke:#333,stroke-width:1px
  class Fixture,DocA,DocB_conflict fixtures
```

> SVG rendering failed for this diagram.

## Diagram 2

```mermaid
sequenceDiagram
  participant T as TestClient
  participant API as FastAPI
  participant DB as Database
  Note over T,API: Atomic batch success
  T->>API: POST /document/A/associations {OBJ-A, OBJ-B, mode: atomic}
  API->>DB: begin transaction; insert associations
  DB-->>API: inserted OBJ-A, OBJ-B
  API-->>T: 200 OK
  Note over T,API: Atomic batch conflict
  T->>API: POST /document/B_conflict/associations {OBJ-C, OBJ-D, mode: atomic}
  API->>DB: begin transaction; try insert OBJ-C (conflict)
  DB-->>API: conflict on OBJ-C
  API->>DB: rollback
  API-->>T: 409 Conflict (details: OBJ-C)
  Note over T,API: Best-effort mode
  T->>API: POST /document/B_conflict/associations {OBJ-E, OBJ-F, mode: best-effort}
  API->>DB: insert OBJ-E (conflict) -> skip; insert OBJ-F
  DB-->>API: inserted OBJ-F
  API-->>T: 200 OK (associated: OBJ-F, skipped: OBJ-E)
  Note over T,API: Deletion flows
  T->>API: DELETE /document/A/associations {OBJ-G}
  API->>DB: delete association OBJ-G
  DB-->>API: deleted; remaining OBJ-H
  API-->>T: 204 No Content
  T->>API: DELETE /document/A/associations
  API->>DB: delete all associations for document A
  DB-->>API: none remaining
  API-->>T: 204 No Content
```

> SVG rendering failed for this diagram.
