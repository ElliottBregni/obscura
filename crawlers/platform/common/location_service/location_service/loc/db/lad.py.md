# Diagram: common/location_service/location_service/loc/db/lad.py


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
flowchart LR
  subgraph Modules
    psycopg2[psycopg2]
    fv_error[fv.error]
    fv_db[fv.db]
    constants[TableVersions]
    shipments_fn[shipment_visibility_conditions()]
  end

  subgraph Functions
    insert_lad_fn[insert_lad(cursor, body, org_lad_id, org_id)]
    get_lads_fn[get_lads(cursor, organization_id, unclassified, all_lads=False)]
    get_org_lad_by_name[get_organization_lad_by_name(cursor, lad_name, lob_name)]
  end

  subgraph Database_Tables
    org_lad[location.organization_lad]
    lad[location.lad]
    lob[location.lob]
    organizations[organizations]
    active_shipment[ACTIVE_SHIPMENT]
  end

  insert_lad_fn -->|SET search_path; parse JSON| cursor
  insert_lad_fn -->|UPDATE org_lad SET name| org_lad
  insert_lad_fn -->|raises IntegrityError -> DatabaseError| fv_error
  insert_lad_fn -->|uses psycopg2 exceptions| psycopg2

  get_lads_fn -->|constructs SELECT (two variants)| cursor
  get_lads_fn -->|JOIN lad and lob| lad
  get_lads_fn --> lob
  get_lads_fn --> org_lad
  get_lads_fn -->|when all_lads uses organizations and ACTIVE_SHIPMENT| organizations
  get_lads_fn --> active_shipment
  get_lads_fn --> constants
  get_lads_fn --> shipments_fn
  get_lads_fn -->|fetchall -> transform to list/dict| results[(rows)]

  get_org_lad_by_name -->|SELECT ol.organization_id, ol.id JOIN lad,lob WHERE names| cursor
  get_org_lad_by_name --> org_lad
  get_org_lad_by_name --> lad
  get_org_lad_by_name --> lob
  get_org_lad_by_name -->|returns dict mapping organization_id->id| result_map{map}

  cursor[(cursor)]
  results -->|if all_lads True group by org_id| final_res
  results -->|else list of dicts| final_res

  style Modules fill:#f9f,stroke:#333,stroke-width:1px
  style Functions fill:#bbf,stroke:#333,stroke-width:1px
  style Database_Tables fill:#bfb,stroke:#333,stroke-width:1px
```

> SVG rendering failed for this diagram.
