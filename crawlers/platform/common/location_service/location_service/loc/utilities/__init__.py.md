# Diagram: common/location_service/location_service/loc/utilities/__init__.py


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
flowchart LR
  RB[round_boundary(coords)\n- recursively rounds floats to 5 decimals\nreturns coords] 
  MD[max_depth(l)\n- returns nesting depth\nreturns int]
  RMP[recurse_multi_polygon(valid, coords, curr_depth, depth_of_polygons)\n- recursive validator for polygons\nreturns (valid, coords, curr_depth, depth_of_polygons)]
  VMP[validate_multi_polygon(coords)\n- computes coords_depth\n- checks depth >= 3\n- calls recurse_multi_polygon\nreturns bool]

  RB -->|recursive call| RB
  RMP -->|recursive call when curr_depth != depth_of_polygons| RMP
  VMP -->|calls| MD
  VMP -->|calls| RMP
  MD -->|returns coords_depth| VMP
  RMP -->|returns validation tuple| VMP

  subgraph DepthCheck [Depth check in validate_multi_polygon]
    DC1[(coords_depth < 3?) ]
    DC1 -->|true| Fail[return False]
    DC1 -->|false| Continue[compute depth_of_polygons = coords_depth - 2]
    Continue --> RMP
  end

  VMP --> DepthCheck
  Fail --> VMP

  style RB fill:#f9f,stroke:#333,stroke-width:1px
  style MD fill:#ff9,stroke:#333,stroke-width:1px
  style RMP fill:#9ff,stroke:#333,stroke-width:1px
  style VMP fill:#9f9,stroke:#333,stroke-width:1px
```

> SVG rendering failed for this diagram.
