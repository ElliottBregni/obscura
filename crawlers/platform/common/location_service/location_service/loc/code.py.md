# Diagram: common/location_service/location_service/loc/code.py


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
flowchart TD
  Start((Start))
  ES[extract_shipper_codes(thing)]
  CTPS[code_transform_patterns: list of regexes]
  INIT_CODES[codes = [thing]]
  FOR_P_1{for each pattern p}
  MATCH1{p.match(thing) -> m?}
  POS_CODES[ pos_codes = m.groups() ]
  FOR_CODE{for each code in pos_codes}
  LEN_CHECK{len(code) < 3?}
  DUP_CHECK{code not in codes?}
  APPEND_CODE[append code to codes]
  IS_NUM{code.isnumeric()?}
  LEN9{len(code) < 9?}
  ZF9[append code.zfill(9)]
  LEN5{len(code) < 5?}
  ZF5[append code.zfill(5)]
  RETURN_CODES[/return codes/]

  GLB_START --> Start
  Start --> ES
  ES --> CTPS
  CTPS --> INIT_CODES
  INIT_CODES --> FOR_P_1
  FOR_P_1 --> MATCH1
  MATCH1 -- no --> FOR_P_1
  MATCH1 -- yes --> POS_CODES
  POS_CODES --> FOR_CODE
  FOR_CODE --> LEN_CHECK
  LEN_CHECK -- yes --> FOR_CODE
  LEN_CHECK -- no --> DUP_CHECK
  DUP_CHECK -- no --> FOR_CODE
  DUP_CHECK -- yes --> APPEND_CODE
  APPEND_CODE --> IS_NUM
  IS_NUM -- no --> FOR_CODE
  IS_NUM -- yes --> LEN9
  LEN9 -- yes --> ZF9 --> FOR_CODE
  LEN9 -- no --> LEN5
  LEN5 -- yes --> ZF5 --> FOR_CODE
  LEN5 -- no --> FOR_CODE
  FOR_P_1 --> RETURN_CODES

  %% get_shipper_location_by_code flow
  GSL[ get_shipper_location_by_code(cursor, organization_id, code) ]
  Q1["execute SELECT ... WHERE code=%s"]
  FETCH1[location = cursor.fetchone()]
  FOUND1{location?}
  RETURN_LOC1[/return location/]
  EXTRACT["candidate_codes = extract_shipper_codes(code)"]
  FOR_CC{for each cc in candidate_codes}
  LOG_TRY["logging.info('Trying code {cc}')"]
  Q2["execute SELECT ... WHERE code=%s for cc"]
  FETCH2[location = cursor.fetchone()]
  FOUND2{location?}
  RETURN_LOC2[/return location/]
  RETURN_NONE[/return None/]

  RETURN_CODES --> GSL
  GSL --> Q1
  Q1 --> FETCH1
  FETCH1 --> FOUND1
  FOUND1 -- yes --> RETURN_LOC1
  FOUND1 -- no --> EXTRACT
  EXTRACT --> FOR_CC
  FOR_CC --> LOG_TRY
  LOG_TRY --> Q2
  Q2 --> FETCH2
  FETCH2 --> FOUND2
  FOUND2 -- yes --> RETURN_LOC2
  FOUND2 -- no --> FOR_CC
  FOR_CC --> RETURN_NONE
  RETURN_LOC1 --> End((End))
  RETURN_LOC2 --> End
  RETURN_NONE --> End
```

> SVG rendering failed for this diagram.
