# Diagram: common/location_service/location_service/loc/common/csv_utils.py


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
flowchart LR
  A[create_csv(locations, event, version)] --> B[normalize locations to dicts]
  B --> C{for each location}
  C --> D[get_csv_values(event, location, version)]
  D --> H[fv.aws.lambdas.get_header(event, HeaderOptions.TIMEZONE) or \"America/Detroit\"]
  D --> I[extract geofence, lad, geometry, lad_name]
  I --> J[compute Parent, Child Location Count, Name, Location Type, Geofence Type, Geofence Count, Location Code, Address, Parent Location Code]
  J --> E[csv_dict]
  D --> E
  C --> F[append csv_dict to data list]
  F --> G[fv.aws.lambdas.csv_export(data, event=event)]
  style A fill:#f9f,stroke:#333,stroke-width:1px
  style G fill:#9f9,stroke:#333,stroke-width:1px
```

> SVG rendering failed for this diagram.
