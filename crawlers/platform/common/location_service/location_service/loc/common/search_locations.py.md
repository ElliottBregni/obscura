# Diagram: common/location_service/location_service/loc/common/search_locations.py


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
flowchart LR
  A[search_locations] --> B[establish_connection & get cursor]
  B --> C[get_locations(cursor, organization_id, event, ...)]
  C --> D[parse queryStringParameters (qsp) & owner_id]
  D --> E[determine sort_column & reverse_sort]
  E --> F[build helper getters: get_qsp/get_int_qsp/get_bool_qsp/get_list_qsp]
  F --> G[construct query_parameters dict with filters]
  G --> H{version == BASIC and uses lad/lob/near_location?}
  H -- yes --> I[set version = FULL; log info]
  H -- no --> J[continue]
  J --> K[check filtered_params; enforce pagination for large orgs]
  K --> L[determine Accept header -> app_header flag]
  L --> M{page_size or page_number provided?}
  M -- yes --> N[normalize page_size/page_number -> page_options]
  N --> O[db.get_locations(..., page_options...)]
  O --> P{is_export?}
  P -- yes --> Q[return make_response(user_locations)]
  P -- no --> R[active_count = user_locations[0].count if any else 0]
  R --> S[total_pages = ceil(active_count / page_size)]
  S --> T[loc_data_list = get_location_data_list(app_header, user_locations)]
  T --> U[response = {meta: {total_pages,current_page,total_count}, data: loc_data_list}]
  U --> V[return make_response(response)]
  M -- no --> W[db.get_locations(... without paging ...)]
  W --> X{is_export?}
  X -- yes --> Y[return make_response(user_locations)]
  X -- no --> Z[loc_data_list = get_location_data_list(app_header, user_locations)]
  Z --> AA[return make_response(loc_data_list)]
```

> SVG rendering failed for this diagram.
