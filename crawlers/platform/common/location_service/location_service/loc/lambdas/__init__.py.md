# Diagram: common/location_service/location_service/loc/lambdas/__init__.py


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
flowchart LR
  Event(["event"]) --> GLat[get_latitude(event, required=False)]
  Event --> GLon[get_longitude(event, required=False)]

  GLat --> CallLat["call fv.aws.lambdas.get_float_query_parameter(event, 'latitude', required)"]
  GLon --> CallLon["call fv.aws.lambdas.get_float_query_parameter(event, 'longitude')"]

  CallLat --> LatVal["latitude"]
  CallLon --> LonVal["longitude"]

  GLat --> ReqLat{"required and latitude ?"}
  ReqLat -->|no| RetLat["return latitude"]
  ReqLat -->|yes| InRangeLat{"-90 <= latitude <= 90 ?"}
  InRangeLat -->|yes| RetLat
  InRangeLat -->|no| ErrLat["raise fv.error.BadRequestError('Latitude must be between -90 and 90 degrees.')"]

  GLon --> ReqLon{"required and longitude ?"}
  ReqLon -->|no| RetLon["return longitude"]
  ReqLon -->|yes| InRangeLon{"-180 <= longitude <= 180 ?"}
  InRangeLon -->|yes| RetLon
  InRangeLon -->|no| ErrLon["raise fv.error.BadRequestError('Longitude must be between -180 and 180 degrees.')"]

  ErrLat --- fvError["fv.error.BadRequestError"]
  ErrLon --- fvError
  RetLat --> EndLat(["latitude returned"])
  RetLon --> EndLon(["longitude returned"])
```

> SVG rendering failed for this diagram.
