# Diagram: common/location_service/location_service/loc/lambdas/lad/lads_get.py


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
flowchart LR
  subgraph Imports
    A[fv.db] --> B[FvDatabaseConnector]
    C[fv.aws.lambdas] --> D[mandatory_lambda_handling]
    C --> E[get_boolean_query_parameter]
    C --> F[make_response]
    C --> G[auth (module)]
    H[fv.log] 
    I[location_service.loc.db.lad] --> J[get_lads]
    K[fv.aws.lambdas.constants] --> L[OrgTypes]
    M[fv.secrets] --> N[SecretNames]
  end

  A -.creates.-> DB_CONN[DB_CONN = FvDatabaseConnector("get_lads", SecretNames.DATABASE)]
  DB_CONN -->|establish_connection()| Cursor[DB_CONN.cursor]

  subgraph Lambda
    LH[lambda_handler(event, context, audit_refs)]
    LH --> Authorizer[event.requestContext.authorizer]
    Authorizer -->|check org_profiles| CheckAll[OrgTypes.CARRIER in authorizer.org_profiles?]
    LH -->|get_organization_id| UserOrg[user_org = auth.get_organization_id(event)]
    LH -->|get_boolean_query_parameter| Unclassified[unclassified = get_boolean_query_parameter(event,"unclassified",False)]
    LH --> DB_CONN
    DB_CONN --> Cursor
    Cursor -->|pass to| CallGetLads[get_lads(cursor, user_org, unclassified, all_lads=CheckAll)]
    CallGetLads --> Lads[lads]
    Lads -->|wrap| Response[fv.aws.lambdas.make_response({"lads": lads})]
    Response --> LH
  end

  D -->|decorates| LH
  G -->|provides| UserOrg
  L -->|enum| CheckAll
  N -->|provides secret| DB_CONN
```

> SVG rendering failed for this diagram.
