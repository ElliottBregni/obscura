# Diagram: common/location_service/location_service/loc/lambdas/grant/grant_location_by_code.py


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
flowchart TD
  Start([lambda_handler(event, context)]) --> ParseBody[/"get_parsed_event_body(event)"/]
  ParseBody --> Extract[/"extract granterFvId, granteeFvId, locationCode from body"/]
  Extract --> CheckLocation{locationCode != None?}
  CheckLocation -- No --> NoLocation[/return_error_response: "No location code provided."/]
  CheckLocation -- Yes --> DBConnect[/DB_CONN.establish_connection() and cursor = DB_CONN.cursor/]
  DBConnect --> GetUser[user_id = auth.get_user_id(event)]
  GetUser --> GetDetails[/return_details_for_grant_location(cursor, granter_fv_id, grantee_fv_id, location_code)/]
  GetDetails --> CheckDetails{granter_org_id AND location_id AND grantee_org_id?}
  CheckDetails -- Yes --> InsertGrant[/db.insert_grant(cursor, user_id, granter_org_id, grantee_org_id, location_id, None)/]
  InsertGrant --> CheckInsert{grant_id AND loc_id?}
  CheckInsert -- Yes --> Created[/fv.aws.lambdas.make_response(status=201, details id/location_id)/]
  CheckInsert -- No --> FailGrant[/return_error_response: "Failed to grant location."/]
  CheckDetails -- No --> DetermineMissing[/determine missing: granterFvId or granteeFvId or location code/]
  DetermineMissing --> MissingError[/return_error_response: "Provided <value> does not exist."/]
  Start -->|exception| ExceptionCatch((Exception))
  ExceptionCatch --> ExceptionResponse[/fv.aws.lambdas.make_error_response(event, context, error=exception)/]
```

> SVG rendering failed for this diagram.
