# Diagram: common/location_service/location_service/loc/lambdas/grant/get_granted_locations.py


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
classDiagram
    class GetGrantedLocationList {
        <<Service>>
        +STANDARD_COLUMNS
        +GRANTEE_COLUMNS
        +byIdGrantQuery()
        +byFvIdGrantQuery()
        +byLocationCodeAndGrantorFvID()
        +byLocationCodeAndGrantorInternalID()
        +executeQuery(query, parameters)
        +getGrantedLocationsById(parameters)
        +getGrantedLocationsByFvId(parameters)
        +getGrantedLocationsByLocationCodeAndGrantorFvID(parameters)
        +getGrantedLocationsByLocationCodeAndGrantorInternalID(parameters)
        +process(user_organization_id, grantor_ids, grantee_id)
        +process_get_grantee(grantor_id, grantor_internal_id, location_codes, feature_name)
        +location_grants_handler(event, context, audit_refs)
        +grantee_handler(event, context, audit_refs)
    }

    class DB_CONN {
        <<DatabaseConnector instance>>
        +establish_connection()
        +cursor
    }

    class fv_aws_lambdas {
        <<Module>>
        +make_response(payload, status_code)
        +get_query_parameter(event, name)
        +get_list_query_parameter(event, name)
        +parse_accept_header(event, version_options)
        +mandatory_lambda_handling(privs)
    }

    class fv_error {
        <<Module>>
        +BadRequestError(message, details)
    }

    class fv_db {
        <<Module>>
        +FvDatabaseConnector(name, secret)
    }

    class QSP_VersionOptions {
        <<Constants>>
        +LocationGrant
        +VersionOptions
    }

    class lambda_handler {
        <<Function>>
        +lambda_handler(event, context, audit_refs)
    }

    GetGrantedLocationList --> DB_CONN : uses
    GetGrantedLocationList --> fv_aws_lambdas : uses
    GetGrantedLocationList --> fv_error : raises
    DB_CONN ..|> fv_db : instance of
    lambda_handler --> GetGrantedLocationList : dispatches to
    lambda_handler --> fv_aws_lambdas : decorated by/uses
    fv_aws_lambdas ..> QSP_VersionOptions : reads constants
    note right of GetGrantedLocationList : SQL query templates (byId, byFvId,\nbyLocationCodeAndGrantorFvID,\nbyLocationCodeAndGrantorInternalID)
```

> SVG rendering failed for this diagram.
