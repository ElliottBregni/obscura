# Diagram: common/location_service/location_service/loc/lambdas/grant/grants_add.py


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
flowchart LR
  Start([Start]) --> ParseBody{Parse event body}
  ParseBody -->|get granter_id, grantee_id, location_ids, grant_type| ValidateGrantType{Grant type provided?}
  ValidateGrantType -->|yes| CheckInList{grant_type in loc_constants.GrantType?}
  ValidateGrantType -->|no| ContinueNoGrantType[Continue]
  CheckInList -->|no| BadRequest1["BadRequestError: grant type not in grant type list"]
  CheckInList -->|yes| ParseLocationIDs{location_ids is not None?}
  ParseLocationIDs -->|no| MissingFields["BadRequestError: must include granter id, grantee id, and location ids"]
  ParseLocationIDs -->|yes| LocationTypeCheck{location_ids type}
  LocationTypeCheck -->|list| LocsOk[Proceed with list]
  LocationTypeCheck -->|str| TryJSONParse[json.loads(location_ids) or BadRequestError]
  LocationTypeCheck -->|int| WrapInt[location_ids = [location_ids]]
  LocationTypeCheck -->|other| BadRequest2["BadRequestError: Location IDs could not be parsed"]
  LocsOk --> CheckRequiredFields{granter_id && grantee_id && location_ids not empty?}
  TryJSONParse --> CheckRequiredFields
  WrapInt --> CheckRequiredFields
  CheckRequiredFields -->|no| MissingFields
  CheckRequiredFields -->|yes| GetUser["user_id = auth.get_user_id(event)"]
  GetUser --> DBConnect["DB_CONN.establish_connection()"]
  DBConnect --> Cursor["cursor = DB_CONN.cursor"]
  Cursor --> ForEachLoc{for loc_id in location_ids}
  ForEachLoc --> InsertGrant["db.insert_grant(cursor, user_id, granter_id, grantee_id, loc_id, grant_type)"]
  InsertGrant --> AppendResult["results.append({id: grant_id, location_id: loc_id})"]
  AppendResult --> ForEachLoc
  ForEachLoc -->|after loop| CheckResults{results non-empty?}
  CheckResults -->|yes| Respond201["return make_response(results, status_code=201)"]
  CheckResults -->|no| RaiseException["raise Exception"]
  BadRequest1 --> EndError([End])
  BadRequest2 --> EndError
  MissingFields --> EndError
  Respond201 --> EndSuccess([End])
```

> SVG rendering failed for this diagram.
