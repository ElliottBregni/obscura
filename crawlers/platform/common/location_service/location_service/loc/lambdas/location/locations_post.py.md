# Diagram: common/location_service/location_service/loc/lambdas/location/locations_post.py


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
flowchart TD
    LH[lambda_handler(event, context, audit_refs)]
    LH --> DB_CONN_est[DB_CONN.establish_connection()]
    LH --> CURS[cursor = DB_CONN.cursor]
    LH --> ORG[auth.get_organization_id(event)]
    LH --> UID[auth.get_user_id(event)]
    LH --> BODY[fv.aws.lambdas.get_event_body(event)]
    BODY --> CHECKLIST{body is list?}
    CHECKLIST -->|Yes| LOOP[for each location_list_body]
    LOOP --> CHECK_PRIVS1[check_post_privs(cursor, event, location_list_body, user_org, user_id, category, owner_id)]
    CHECK_PRIVS1 --> PRIVS1[auth.get_privileges(event)]
    CHECK_PRIVS1 --> DB_INSERT1[db.insert_location(cursor, user_id, user_org, category, location_list_body)]
    DB_INSERT1 --> COLLECT[new_locations.append(new_location)]
    LOOP --> HANDLE_EXC1[catch Exception -> log and continue]
    CHECKLIST -->|List finished| EVAL_COUNT{len(new_locations) < 1?}
    EVAL_COUNT -->|True| RAIS_CONFLICT[fv.error.ConflictError(...)]
    EVAL_COUNT -->|False| RESP1[fv.aws.lambdas.make_response(new_locations)]
    CHECKLIST -->|No| CHECK_PRIVS2[check_post_privs(cursor, event, body, user_org, user_id, category, owner_id)]
    CHECK_PRIVS2 --> PRIVS2[auth.get_privileges(event)]
    CHECK_PRIVS2 --> DB_INSERT2[db.insert_location(cursor, user_id, user_org, category, body)]
    DB_INSERT2 --> RESP2[fv.aws.lambdas.make_response(new_location)]
    LH --> OWNER_ID[owner_id = fv.dm.extract_int(body, "owner_id", default=-1)]
    OWNER_ID --> OWNER_ADJUST{owner_id == -1?}
    OWNER_ADJUST -->|Yes| SET_OWNER[user_org -> owner_id]
    LH --> CATEGORY[category = db.get_category(cursor, owner_id)]
    CHECK_PRIVS1 -->|permission checks| FORBID1[fv.error.ForbiddenError]
    CHECK_PRIVS2 -->|permission checks| FORBID2[fv.error.ForbiddenError]
    PRIVS1 -->|uses| auth.Privilege
    PRIVS2 -->|uses| auth.Privilege
    DB_CONN_est --> DB_CONN_obj[DB_CONN (FvDatabaseConnector)]
    BODY --> SAMPLE_POLY[SAMPLE_POLYGON_REQUEST]
    BODY --> SAMPLE_RAD[SAMPLE_RADIUS_REQUEST]
    style LH fill:#f9f,stroke:#333,stroke-width:2px
    style DB_CONN_est fill:#efe,stroke:#333
    style DB_INSERT1 fill:#eef,stroke:#333
    style DB_INSERT2 fill:#eef,stroke:#333
```

> SVG rendering failed for this diagram.
