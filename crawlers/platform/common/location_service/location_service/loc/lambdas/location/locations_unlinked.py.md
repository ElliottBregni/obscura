# Diagram: common/location_service/location_service/loc/lambdas/location/locations_unlinked.py


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
flowchart LR
  A[lambda_handler(event, context, audit_refs)] --> B[auth.get_organization_id(event)]
  A --> C[fv.aws.lambdas.parse_accept_header(event, VersionOptions)]
  C --> D{requested_format includes "text/csv"?}
  D -->|yes & async export| E[fv.aws.lambdas.async_export(CSV_LAMBDAS.LOCATIONS_UNRESOLVED)]
  D -->|no or sync export| F[DB_CONN.establish_connection()]
  F --> G[cursor = DB_CONN.cursor]
  G --> H[get_locations(cursor, event, actor_organization_id, is_export)]
  H --> I[gather_search_params(event)]
  I --> J[get_qsp(field), get_int_qsp(field), get_bool_qsp(field)]
  H --> K[fv.aws.lambdas.get_query_parameter(event, QSP.SORT_COLUMN)]
  H --> L[fv.aws.lambdas.get_boolean_query_parameter(event, QSP.REVERSE_SORT)]
  H --> M[fv.aws.lambdas.get_int_query_parameter(event, PaginationOptions.PAGE_SIZE/NUMBER)]
  H --> N{sort_column valid?}
  N -->|no| O[raise BadRequestError]
  N -->|yes| P[db.get_location_unlinked(cursor, org_id, query_parameters, page_options, is_export)]
  P --> Q[count_total, user_locations]
  Q --> R{is_export?}
  R -->|yes| S[csv_utils.create_csv(user_locations, event, version) -> fv.aws.lambdas.make_response(csv)]
  R -->|no| T{pagination used?}
  T -->|yes| U[build meta + data response -> fv.aws.lambdas.make_response(response, status_code)]
  T -->|no| V[return user_locations -> fv.aws.lambdas.make_response(user_locations)]
  O --> W[fv.error.BadRequestError]
```

> SVG rendering failed for this diagram.
