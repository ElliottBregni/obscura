# Diagram: common/location_service/location_service/loc/lambdas/location/bulk_upload/repository.py


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
classDiagram
    class LocationBulkUploadRepository {
        - db_conn: FvDatabaseConnector
        - cursor
        + __init__() 
        + insert_location_progress(filename, row_num, group_id, location, status=None, comment=None) int
        + update_location_progress(job_id, row_id, current_status, future_status, comment) void
        + get_processed_count_by_group(group_id) int
        + get_failed_location_messages(group_id) List~ErrorReport~
        + get_category(owner_id) str
        + insert_location(user_id, user_org, category, location) Any
        + get_lads(organization_id, all_lads) List
        + get_lat_long_from_here(location) (lat,long)
    }

    class FvDatabaseConnector {
        + FvDatabaseConnector(name, secret_name)
        + establish_connection()
        + cursor
    }

    class SecretNames {
        <<enumeration>>
        + DATABASE
    }

    class db {
        + get_category(cursor, owner_id) str
        + insert_location(cursor, user_id, user_org, category, location_dict) Any
    }

    class lad_db {
        + get_lads(cursor, organization_id, all_lads) List
    }

    class HERE {
        + get_lat_long(location, cursor, precise=False) (lat,long)
    }

    class Location {
        + code
        + model_dump() dict
    }

    class LocationUploadStatus {
        <<enumeration>>
        + PENDING
        + COMPLETE
        + FAILURE
        + value
    }

    class ErrorReport {
        + row_number
        + reason
    }

    LocationBulkUploadRepository *-- FvDatabaseConnector : creates
    LocationBulkUploadRepository o-- "cursor" : holds
    LocationBulkUploadRepository ..> db : uses
    LocationBulkUploadRepository ..> lad_db : uses
    LocationBulkUploadRepository ..> HERE : calls
    LocationBulkUploadRepository ..> SecretNames : references
    LocationBulkUploadRepository ..> Location : accepts
    LocationBulkUploadRepository ..> LocationUploadStatus : accepts
    LocationBulkUploadRepository ..> ErrorReport : returns
    FvDatabaseConnector --> "cursor" : provides
    db ..> ErrorReport : returns
    HERE ..> (lat,long) : returns
```

> SVG rendering failed for this diagram.
