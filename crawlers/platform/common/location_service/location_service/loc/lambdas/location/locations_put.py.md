# Diagram: common/location_service/location_service/loc/lambdas/location/locations_put.py


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
flowchart TD
  Lambda[lambda_handler(event, context, audit_refs)]
  EventBody[get_event_body(event)]
  PathId[location_id = int(pathParameters.id)]
  Audit[update audit_refs with LOCATION_ID]
  DB[DB_CONN.establish_connection()]
  Cursor[cursor = DB_CONN.cursor]
  AuthOrg[auth.get_organization_id(event)]
  AuthUser[auth.get_user_id(event)]
  AuthOrgTypes[auth.get_user_org_types(event)]
  Privileges[auth.get_privileges(event)]
  OwnerId[owner_id = extract_int(event_body, "owner_id")]
  CustomerId[customer_id = extract_int(event_body, "customer_id")]
  Category[category = db.get_category(cursor, owner_id)]
  SafeGeo[safe_geofence = get_boolean_query_parameter(event, "safe_geofence")]
  PermissionChecks{Privilege checks}
  GetLocation[db.get_location(cursor, user_org, location_id)]
  Validate[validate_loc_data(db_location_data, event_body)]
  LinkCheck{if referent_id and lacks LINK_LOCATIONS}
  Forbidden1[raise ForbiddenError("link locations")]
  Forbidden2[raise ForbiddenError("update shipper locations")]
  Forbidden3[raise ForbiddenError("create locations for org")]
  Update[updated_location = db.update_location(...)]
  Response[fv.aws.lambdas.make_response(updated_location)]

  Lambda --> EventBody
  Lambda --> PathId
  Lambda --> Audit
  Lambda --> DB
  DB --> Cursor
  Lambda --> AuthOrg
  Lambda --> AuthUser
  Lambda --> AuthOrgTypes
  Lambda --> Privileges
  Lambda --> OwnerId
  Lambda --> CustomerId
  OwnerId -->|if -1 then owner=user_org| AuthOrg
  AuthOrg --> Category
  Category --> PermissionChecks
  Privileges --> PermissionChecks
  PermissionChecks -->|manage_geofence_name only| GetLocation
  GetLocation --> Validate
  EventBody --> LinkCheck
  LinkCheck -->|true and lacks privilege| Forbidden1
  PermissionChecks -->|category == "Shipper" and lacks MANAGE_SHIPPER_LOCATIONS and lacks MANAGE_GEOFENCE_NAME| Forbidden2
  PermissionChecks -->|owner_id != user_org and not allowed| Forbidden3
  PermissionChecks -->|passes| Update
  Validate -->|passes| Update
  Update --> Response
  Forbidden1 --> Response
  Forbidden2 --> Response
  Forbidden3 --> Response
```

> SVG rendering failed for this diagram.
