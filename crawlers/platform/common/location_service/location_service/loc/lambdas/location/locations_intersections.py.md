# Diagram: common/location_service/location_service/loc/lambdas/location/locations_intersections.py


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
flowchart LR
  Start([lambda_handler(event, context, audit_refs)]) --> GetParams[/"Get query parameters:\nlatitude, longitude,\nlocation_ids, lad, lob,\ncategory, full"/]
  GetParams --> CheckLadLob{(lad and not lob) or\n(not lad and lob)?}
  CheckLadLob -- Yes --> BadRequest1[/Raise BadRequestError:\n"LAD and LOB must both be present"/]
  CheckLadLob -- No --> CheckPresence{not (lad or lob or\nlocation_ids)?}
  CheckPresence -- Yes --> BadRequest2[/Raise BadRequestError:\n"Must have either LAD and LOB or location_ids"/]
  CheckPresence -- No --> UpdateAudit[Update audit_refs with\nSearchable_Ids.LOCATION_ID -> location_ids]
  UpdateAudit --> GetPrivs[Call auth.get_privileges(event)]
  GetPrivs --> GetOrg[Call auth.get_organization_id(event)]
  GetOrg --> DetermineCarrier{MANAGE_CARRIER_LOCATIONS\nin privileges?}
  DetermineCarrier -- Yes --> CarrierTrue[carrier_access = true]
  DetermineCarrier -- No --> CarrierFalse[carrier_access = false]
  CarrierTrue --> DBConnect[DB_CONN.establish_connection()]
  CarrierFalse --> DBConnect
  DBConnect --> Cursor[cursor = DB_CONN.cursor]
  Cursor --> Report[Call db.report_intersections(cursor,\nactor_organization_id, latitude, longitude,\nlocation_ids, carrier_access, lad, lob,\nget_full_location, category)]
  Report --> Response[Return fv.aws.lambdas.make_response(location_intersections)]
  BadRequest1 --> EndError1([End: Error])
  BadRequest2 --> EndError2([End: Error])
  Response --> End([End])
```

> SVG rendering failed for this diagram.
