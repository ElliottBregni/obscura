# Diagram: common/location_service/location_service/loc/lambdas/location/locations_aliases.py


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
flowchart TD
  A[Imports: fv.db, fv.log, location_service.loc.db.location as db, fv.aws.lambdas.auth, fv.aws.lambdas.constants.Searchable_Ids, fv.secrets.SecretNames] --> B[DB_CONN = fv.db.FvDatabaseConnector("grants_delete", SecretNames.DATABASE)]
  subgraph Lambda
    direction TB
    D[decorator: fv.aws.lambdas.mandatory_lambda_handling()] --> E[lambda_handler(event, context, audit_refs)]
    E --> F{extract location_id}
    F --> G[audit_refs.update({Searchable_Ids.LOCATION_ID: location_id})]
    E --> H[actor_organization_id = auth.get_organization_id(event)]
    E --> I[DB_CONN.establish_connection()]
    I --> J[cursor = DB_CONN.cursor]
    J --> K[location_aliases = db.get_alias_locs(cursor, location_id, actor_organization_id)]
    K --> L[return fv.aws.lambdas.make_response(location_aliases)]
  end
  B --> E
```

> SVG rendering failed for this diagram.
