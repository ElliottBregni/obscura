# Diagram: common/location_service/.gitlab-ci.yml


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
flowchart TD
  Start[CI Pipeline Triggered] --> CheckRules{CI_PIPELINE_SOURCE == "merge_request_event" \n AND changes in location_service or fv python files?}
  CheckRules -- yes --> RunJob[Execute location-tests job]
  CheckRules -- no --> Skip[Skip job]
  RunJob --> Venv[Create virtualenv: python -m venv venv]
  Venv --> Activate[Activate venv: source venv/bin/activate]
  Activate --> InstallReqs1[Install base requirements: pip install -r requirements.txt]
  InstallReqs1 --> InstallDevReqs[pip install -r dev-requirements.txt]
  InstallDevReqs --> InstallFV[pip install -e platform/common/fv/python]
  InstallFV --> InstallLSDev[pip install -r platform/common/location_service/dev-requirements.txt]
  InstallLSDev --> InstallLSReqs[pip install -r platform/common/location_service/requirements.txt]
  InstallLSReqs --> PreScriptDone[Before script complete]
  PreScriptDone --> LoginStep[Run referenced login script (!reference [.py-test:login, script])]
  LoginStep --> UnitTests[Run pytest unit tests\npytest --cov --cov-report xml:coverage.xml --cov-branch platform/common/location_service/tests]
  UnitTests --> ExportEnv[Export RUN_INTEGRATION_TESTS=true\nExport AWS_STAGE=dev]
  ExportEnv --> IntegrationTests[Run pytest integration tests\npytest --cov-report xml:coverage.xml --cov-branch platform/common/location_service/location_service_tests]
  UnitTests --> Coverage[Generate coverage.xml]
  IntegrationTests --> Coverage
  Coverage --> JobDone[Job Complete]
```

> SVG rendering failed for this diagram.
