# Diagram: common/location_service/config/config.staging.yml


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
classDiagram
    class Crawl {
        +run()
        +parse_args()
    }
    class Crawlers {
        +crawl_site()
        +extract_metadata()
    }
    class Backend {
        +start_server()
        +handle_request()
    }
    class SDK {
        +Client
        +call_api()
    }
    class Sync {
        +sync_repos()
    }
    Crawl --> Crawlers : uses
    Crawl --> Backend : sends data to
    Backend --> SDK : calls
    Sync --> Backend : updates
    Crawlers <-- "repos/" : reads files from
    SDK <-- "node_modules/" : depends on
```

> SVG rendering failed for this diagram.
