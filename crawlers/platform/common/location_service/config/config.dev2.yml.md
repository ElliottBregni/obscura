# Diagram: common/location_service/config/config.dev2.yml


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
classDiagram
    class Crawl {
        +run()
        +load_config()
    }
    class CrawlerManager {
        +discover()
        +crawl()
    }
    class Sync {
        +sync_repos()
    }
    class Backend {
        +start()
        +handle_request()
    }
    class Agents {
        +launch()
    }
    class Skills {
        +register()
        +execute()
    }
    class SDK {
        +client()
    }
    Crawl --> CrawlerManager : uses
    Crawl --> Backend : sends data to
    CrawlerManager --> "crawlers/*" : implements
    Sync --> Backend : updates
    Backend --> Agents : dispatches to
    Backend --> Skills : invokes
    Skills --> SDK : depends on
    Agents --> SDK : uses
```

> SVG rendering failed for this diagram.
