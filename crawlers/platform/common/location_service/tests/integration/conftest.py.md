# Diagram: common/location_service/tests/integration/conftest.py


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
flowchart LR
  A[Module imports: pytest, os, json] --> B[Evaluate WORKING_DIR]
  B --> C{CI_PIPELINE_SOURCE set?}
  C -- yes --> D["WORKING_DIR = 'platform/common/location_service/tests/integration/'"]
  C -- no --> E["WORKING_DIR = ''"]
  D --> F[Define fixture: get_granted_location_event (scope=module)]
  E --> F
  F --> G[Open file: WORKING_DIR + 'events/get_granted_location_event.json']
  G --> H[Read file contents]
  H --> I[json.loads(contents) -> test_backorder_event]
  I --> J[Return test_backorder_event]
```

> SVG rendering failed for this diagram.
