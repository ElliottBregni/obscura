# Diagram: common/location_service/tests/integration/location_get_grant_lambda_test.py


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
flowchart TD
  A[Start: test_get_grant_lambda_handler_does_not_return_inactive_grant] --> B{AWS_STAGE == 'dev'?}
  B -- No --> C[Skipped by pytest.mark.skipif]
  B -- Yes --> D[Prepare get_granted_location_event & Context(30)]
  D --> E[Call lambda_handler(get_granted_location_event, Context(30))]
  E --> F[Receive res (response)]
  F --> G{"FV4017820A_PART" in res?}
  G -- Yes --> H[Assertion fails: test fails]
  G -- No --> I[Assertion passes: "Data should be empty"]
  H --> J[End: Fail]
  I --> K[End: Pass]
```

> SVG rendering failed for this diagram.
