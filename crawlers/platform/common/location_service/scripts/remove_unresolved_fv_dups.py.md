# Diagram: common/location_service/scripts/remove_unresolved_fv_dups.py


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
flowchart TD
  A[Start\n(main)] --> B[DB_CONN.cursor]
  B --> C[get_dup_locations(cursor)]
  C --> D{For each dup location}
  D --> E[get_all_dup_locations_for_single_dup(cursor, loc)]
  E --> F[choose_loc_with_ref(all_locs)\nreturns chosen_loc_id]
  F --> G[loc_ids_to_update_delete = all_locs - chosen_loc_id]
  G --> H[Progress(len(dup_locations), skip=1)]
  H --> I[update_shipment_stop_location_id(cursor, chosen_loc_id, loc_ids_to_update)]
  I --> J[run_queries(cursor, queries, data)\n(retries up to 3 on failure)]
  J --> K[delete_dup_locations(cursor, location_ids)]
  K --> L[run_queries(cursor, queries, data)]
  L --> M[Next dup or End]
  M --> D
  M --> Z[End]

  subgraph DB
    B
  end

  subgraph Helpers
    J
    L
  end
```

> SVG rendering failed for this diagram.
