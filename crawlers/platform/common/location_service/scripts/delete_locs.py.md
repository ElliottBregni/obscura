# Diagram: common/location_service/scripts/delete_locs.py


> Auto-generated by Obscura crawlers

## Diagram 1

```mermaid
flowchart TD
    A[Start] --> B[parse_arguments()]
    B --> C{args.data provided?}
    C -- yes --> D[Open CSV -> DictReader]
    D --> E[Extract codes list]
    E --> F[helpers.split_list(codes, break_up_num)]
    F --> G[Loop over split_list]
    G --> H[Progress.output_progress(i)]
    H --> I[Build vals with code_list and org_fv_id]
    I --> J[DB: UPDATE location.location SET referent_id=null WHERE referent_id IN (SELECT id ...)]
    J --> K[DB: SELECT ufn_delete_location(id) WHERE code in code_list ...]
    K --> L{more batches?}
    L -- yes --> G
    L -- no --> M[End]
```

> SVG rendering failed for this diagram.

## Diagram 2

```mermaid
classDiagram
    class FvDatabaseConnector {
        <<module: fv.db>>
        +cursor
    }
    class Progress {
        <<module: scripts.Progress>>
        +__init__(total, skip=10)
        +output_progress(i)
    }
    class helpers {
        <<module: scripts.loc_helpers>>
        +split_list(list, n)
    }
    class SecretNames {
        <<module: fv.secrets>>
        +DATABASE
    }
    class main_module {
        +parse_arguments()
        +main()
        -DB_CONN : FvDatabaseConnector
    }
    FvDatabaseConnector "1" o-- "main_module" : DB_CONN
    Progress "1" <-- "main_module" : Prog created
    helpers <-- main_module : uses
    SecretNames <-- main_module : uses
```

> SVG rendering failed for this diagram.
