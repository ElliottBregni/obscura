# Diagram: common/location_service/scripts/dedup_and_split.py


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
flowchart LR
  Start([Start]) --> OpenFile{Open CSV file "../../gm_locations_20190911.csv"}
  OpenFile --> ReadLoop[Iterate lines with enumerate]
  ReadLoop --> CalcBatch[Compute batch_num = i // BATCH_SIZE]
  CalcBatch --> CheckBatchPrint{If i % BATCH_SIZE == 0}
  CheckBatchPrint -- yes --> PrintCreating[Print "Creating batch # {batch_num} ..."]
  CheckBatchPrint -- no --> ContinueLoop
  PrintCreating --> ContinueLoop
  ContinueLoop --> IsHeader{Is i == 0 ?}
  IsHeader -- yes --> SkipHeader[continue (skip header)]
  SkipHeader --> ReadLoop
  IsHeader -- no --> ExtractCode[code = line.split(",")[1]]
  ExtractCode --> CodeExists{Is code in codes?}
  CodeExists -- yes --> DuplicateSkip[continue (duplicate code)]
  DuplicateSkip --> ReadLoop
  CodeExists -- no --> AddCode[codes.add(code)]
  AddCode --> AppendBatch{Does batches contain batch_{batch_num}?}
  AppendBatch -- yes --> AppendLine[batches[batch_num].append(line)]
  AppendBatch -- no --> CreateBatch[batches[batch_num] = [line]]
  AppendLine --> ReadLoop
  CreateBatch --> ReadLoop
  ReadLoop -.-> EndRead[End of file loop]
  EndRead --> PrintWriting[Print "Writing out batched csv files..."]
  PrintWriting --> WriteBatches[For each batch: open file, write header, write lines]
  WriteBatches --> PrintCompleted[Print "Completed! Total time: ..."]
  PrintCompleted --> End([End])
```

> SVG rendering failed for this diagram.
