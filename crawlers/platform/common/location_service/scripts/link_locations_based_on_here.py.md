# Diagram: common/location_service/scripts/link_locations_based_on_here.py


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
flowchart TD
  Main[main()] --> ConnectDB[Connect to DB (fv.db.FvDatabaseConnector)]
  Main --> ReadCSV[Read CSV in chunks (pd.read_csv chunksize=10000)]
  ReadCSV --> Concat[Concatenate chunks (pd.concat)]
  Main --> Iterate[Iterate rows (for index,row in data.iterrows())]
  Iterate --> CheckMatched{unresolved_location_id in matched_data?}
  CheckMatched -- yes --> Continue[skip row]
  CheckMatched -- no --> ExtractUnres[extract_location_from_row(row,"unresolved")]
  CheckMatched -- no --> ExtractFord[extract_location_from_row(row,"ford")]
  ExtractUnres --> ReturnHereUnres[return_here_id(cursor, unresolved_location, ...)]
  ExtractFord --> ReturnHereFord[return_here_id(cursor, ford_location, ...)]
  ReturnHereUnres --> CompareIDs{unresolved_here_id && ford_here_id && equal?}
  ReturnHereFord --> CompareIDs
  CompareIDs -- yes --> AddMatch[matched_data[unresolved_location_id]=ford_location_id]
  Iterate --> PeriodicFlush{index%10==0 or last index?}
  PeriodicFlush -- yes --> BuildDF[Build DataFrame from matched_data]
  BuildDF --> AppendCSV[append_to_csv(df, path)]
  AppendCSV --> Iterate
  ReturnHereHere[return_here_id function internals]
  ReturnHereHere --> GetGeocode[get_geocode(location_data, cursor, no_cache=True)]
  GetGeocode --> Validate[validate_response(r)]
  Validate --> Parse[parse_response(response_json)]
  Parse --> RelevanceCheck{Relevance >= 0.6?}
  RelevanceCheck -- no --> ReturnEmpty["""return \"\"""]
  RelevanceCheck -- yes --> ReturnLocationId["return Location.LocationId"]
  ReturnHereUnres -.-> ReturnHereHere
  ReturnHereFord -.-> ReturnHereHere
```

> SVG rendering failed for this diagram.
