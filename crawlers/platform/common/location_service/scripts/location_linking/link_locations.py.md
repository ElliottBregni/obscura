# Diagram: common/location_service/scripts/location_linking/link_locations.py


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
flowchart LR
    A[Start: __main__] --> B[parse_arguments()]
    B --> C[get_shipper_id(cursor, args.shipper_id)]
    C --> D{saved_linked_locs?}
    D -- yes --> E[read CSV -> res]
    D -- no --> F[get_location_data(code_addr)] --> G[get_location_data(intersects)]
    G --> H[res = combine results -> write_csv(temp_loc_data)]
    E & H --> I[tend_to_ship(res, shipper_id)]
    I --> J[choose_referent(new_refs...) -> final_to_link, unlinked_locs]
    J --> K[get_all_tend_locs(cursor, shipper_id, overwrite, countries)]
    K --> L{countries filter?}
    L -- true & reverse geocode --> M[reverse_geocode_tenders(cursor, tend_locs)]
    L -- true & no reverse --> N[filter_by_countries(tend_locs, countries)]
    L -- false --> O[tend_locs unchanged]
    M --> P[update_location_country(cursor, loc_id, country)]
    N & O & M --> Q[build_linking_data(tend_to_ship, final_to_link, nones, full=True) -> write_csv(link_info_full.csv)]
    Q --> R[apply updates if args.o or args.u -> SQL UPDATE referent_id]
    R --> S[build_linking_data(..., full=False) -> write_csv(link_info_condensed.csv)]
    S --> T[Print start/finish times -> End]
```

> SVG rendering failed for this diagram.
