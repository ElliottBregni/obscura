# Diagram: common/location_service/scripts/location_linking/r_geocode_tenders.py


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
flowchart LR
    A[parse_arguments()] --> B{args}
    B -->|shipper_id| C[get_shipper_id(cursor, args.shipper_id)]
    B -->|countries| D[split countries into list]
    E[main] --> A
    E --> F[get_connection(SECRETS.get_secret(...), DB_APP_NAME="r_geocode_tenders")]
    F --> G[get_cursor(conn)]
    G --> C
    C --> H[get_all_tend_locs(cursor, shipper_id, countries)]
    H --> I[reverse_geocode_tenders(cursor, tend_locs)]
    subgraph modules
        M1[fv.db] 
        M2[loc_helpers]
        M3[scripts.location_linking]
        M4[fv.secrets]
    end
    F -.-> M1
    C -.-> M2
    H -.-> M3
    SECRETS[Secrets()] -.-> M4
    style SECRETS fill:#f9f,stroke:#333,stroke-width:1px
```

> SVG rendering failed for this diagram.
