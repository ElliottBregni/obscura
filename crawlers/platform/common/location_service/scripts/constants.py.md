# Diagram: common/location_service/scripts/constants.py


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
classDiagram
    class COUNTRY_CONV {
        +USA: "US"
        +MEX: "MX"
        +CAN: "CA"
        +BRA: "BR"
        +ARG: "AR"
        +KOR: "KR"
        type: dict[str->str]
    }
    class FIELD_NAMES_BASE {
        +fields: 13
        -examples: Code, LocationName, Address, City, Country, Latitude, Longitude
        type: list[str]
    }
    class FIELD_NAMES {
        +inherits FIELD_NAMES_BASE
        +additional_fields: 8
        -examples: lad_id, organization_id, postal_code, Geography, state, city, actor_id, address
        type: list[str]
    }
    class BRAZIL_GOOD_ADDRS {
        +entries: "map of id -> {Latitude, Longitude}"
        +sample_id: "901184903"
        type: dict[str->object]
    }
    class KOREAN_GOOD_ADDRS {
        +entries: "map of id -> {Latitude, Longitude}"
        +sample_id: "557804871"
        type: dict[str->object]
    }
    class LINK_FULL_DATA {
        +fields: 20
        -examples: tend_id, old_id, new_id, tend_name, old_name, new_name, tend_code, old_code, new_code, tend_addr, old_addr, new_addr, tend_city, old_city, new_city, tend_state, old_state, new_state, final_link
        type: list[str]
    }

    FIELD_NAMES_BASE <|-- FIELD_NAMES
    COUNTRY_CONV ..> FIELD_NAMES : "used to normalize Country"
    BRAZIL_GOOD_ADDRS ..> FIELD_NAMES : "provides geo for records"
    KOREAN_GOOD_ADDRS ..> FIELD_NAMES : "provides geo for records"
    LINK_FULL_DATA ..> FIELD_NAMES : "linking schema"
```

> SVG rendering failed for this diagram.
