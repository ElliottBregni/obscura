# Diagram: common/location_service/scripts/ford_location_scripts/FCSD_standardize_csv.py


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
flowchart TD
    A[parse_arguments()] -->|returns args| B[Read CSV files]
    B --> C[combined_csv]
    C --> D[standardize(fcsd_data)]
    D --> E[standardized list]
    E --> F[geocode_row(rows)]
    F --> G[helper.write_csv("FCSD_bad_updated.csv", standardized)]
    subgraph DB["Database lookup (on start)"]
        H[SECRETS.get_secret(SecretNames.DATABASE)] --> I[fv.db.get_connection(config, DB_APP_NAME)]
        I --> J[get_cursor(conn)]
        J --> K[SELECT id FROM organizations WHERE fv_id='FCSD']
        K --> L[org_id = cursor.fetchone().id]
    end
    Start((start)) --> M[__main__]
    M --> H
    M --> A
    M --> B
    style DB fill:#f9f,stroke:#333,stroke-width:1px
    classDef funcs fill:#bbf,stroke:#333
    class A,D,F,G funcs
```

> SVG rendering failed for this diagram.
