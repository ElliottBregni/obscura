# Diagram: common/location_service/scripts/ADC_capacities/add_location_capacities.py


> Auto-generated by Obscura crawlers

## Diagram 1

```mermaid
classDiagram
    class Module {
        +SECRETS: Secrets
        +database: str
        +process_csv(cursor, org_id, file_location)
        +main()
    }
    class Secrets {
        +get_secret(name)
    }
    class SecretNames
    class fv.db {
        +get_connection(...)
        +get_cursor(...)
    }
    Module --> Secrets : uses
    Module --> SecretNames : uses
    Module --> fv.db : uses
    Module : create_update_statement(code, capacity)
    Module : full_insert_stmt (str)
    Module : process loop (file rows)
    Module --> "location.location" : queries
    create_update_statement ..> fv.db : executes SQL
    process_csv ..> create_update_statement : calls
```

> SVG rendering failed for this diagram.

## Diagram 2

```mermaid
flowchart TD
    Start([Start]) --> ReadArgs{args valid?}
    ReadArgs -->|yes| OpenDB[/"Open DB connection"/]
    ReadArgs -->|no| Exit1([Exit])
    OpenDB --> OpenCursor[/Acquire cursor/]
    OpenCursor --> OpenFile[/"Open CSV file"/]
    OpenFile --> RowLoop{for each row (skip header)}
    RowLoop --> ParseRow[/"parse code, capacity"/]
    ParseRow --> Validate{valid values?}
    Validate -->|no| LogProblem["Log problem and continue"]
    LogProblem --> RowLoop
    Validate -->|yes| CallCreate[/"create_update_statement(code, capacity)"/]
    CallCreate --> AppendSQL["append to full_insert_stmt"]
    AppendSQL --> RowLoop
    RowLoop --> ExecuteFullSQL["cursor.execute(full_insert_stmt)"]
    ExecuteFullSQL --> CloseCursor[/Release cursor/]
    CloseCursor --> CloseDB[/Close DB connection/]
    CloseDB --> End([End])
```

> SVG rendering failed for this diagram.
