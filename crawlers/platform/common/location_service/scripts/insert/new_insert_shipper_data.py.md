# Diagram: common/location_service/scripts/insert/new_insert_shipper_data.py


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
classDiagram
    class Script {
        +parse_arguments()
        +combine_geofence_points(csv_data, shipper_id, customer_id)
        +clean_values(combined_csv)
        +geocode_row(row)
        +pad_code(code, pad_num=9)
        +strip_code(code)
        +get_shipper_id(fv_id)
        +partial_update(cursor, combined_dict)
        +geocode_locations(combined_dict, shipper_id, organization_id, bad_geofences, writer=None, use_coords=False, customer_id=None)
        +split_list(lst_to_split, n)
        +clean_bad_geofences(bad_geofences)
        +report_invalid_geometries(invalid_geometry)
        +build_query(args_lst, arg_uc, is_carrier)
        +update_lads(cursor, results)
        +insert_and_update(db_conn, query, invalid_geometry, is_carrier=False, retry=0)
        +get_base_file_name(full_path)
        +repl(match)
    }

    class Globals {
        +SECRETS
        +DB_APP_NAME
        +DB_CONN
        +CURSOR
        +FIELD_NAMES
        +begin
        +args
        +buffer
    }

    class RetryDecorator {
        +retry_func(f)
    }

    class ExternalServices {
        +psycopg2
        +fv.db
        +fv.HERE.HERE (here)
        +fv.error
        +fv.secrets.Secrets
        +scripts.loc_helpers
        +scripts.Progress.Progress
        +csv
        +argparse
        +os
        +time
        +re
        +math
        +logging
    }

    Script --> Globals : reads/writes
    Script --> ExternalServices : imports/uses
    Script ..> RetryDecorator : decorates get_shipper_id, partial_update, geocode_locations, update_lads, insert_and_update
    Globals --> ExternalServices : DB_CONN uses fv.db
    RetryDecorator ..> ExternalServices.psycopg2 : handles InterfaceError
    Script --> ExternalServices.fv.HERE.HERE : geocode_row calls get_lat_long
    Script --> ExternalServices.scripts.loc_helpers : change_country
    Script --> ExternalServices.scripts.Progress.Progress : Progress used in batching
    Script --> ExternalServices.csv : reads/writes CSV files
    Script --> ExternalServices.os : reads env vars and file paths

    classDB_CONN <|-- DB_CONN
    classDB_CONN {
        +establish_connection()
        +get_cursor()
    }

    Globals o-- DB_CONN : instance
    Globals o-- CURSOR : cursor

    %% Data flow relationships
    Script "1" --> "many" : combined_csv_list
    Script "1" --> "many" : combined_dict
    Script "1" --> "many" : bad_geofences
    Script "1" --> "many" : invalid_geometry

    %% Database operations
    Script --> classDB_CONN : insert_and_update calls get_cursor()
    partial_update ..> classDB_CONN : uses CURSOR.mogrify/execute
    update_lads ..> classDB_CONN : uses CURSOR.execute
    build_query --> Script : returns SQL query string
```

> SVG rendering failed for this diagram.
