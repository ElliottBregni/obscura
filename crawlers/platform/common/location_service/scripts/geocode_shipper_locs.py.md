# Diagram: common/location_service/scripts/geocode_shipper_locs.py


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
flowchart TD
    Start[Start] --> Init[Load SECRETS & DB_APP_NAME]
    Init --> Main[__main__]
    Main --> GetConfig[SECRETS.get_secret(SecretNames.DATABASE)]
    GetConfig --> Connect[get_connection(config, DB_APP_NAME)]
    Connect --> Cursor[get_cursor(conn)]
    Cursor --> Geocode[geocode_empty_locs(cursor)]
    Geocode --> ParseArg[parse_arguments()]
    Geocode --> Query[SELECT locations WHERE buffer >= 1609]
    Query --> Loop[for each loc in empty_point_locs]
    Loop --> CheckAddr{loc.address?}
    CheckAddr -- yes --> HereCall[here.get_lat_long(loc, cursor)]
    HereCall --> AppendLatLng[lat_longs.append((lat,lng))]
    AppendLatLng --> DistanceCall[get_distance(cursor, loc.id, lat, lng, org_id)]
    DistanceCall --> DistanceList[distance_list.append(...)]
    DistanceCall --> MaybeUpdate{distance > 1609 \n AND GeometryType='POINT' ?}
    MaybeUpdate -- true --> UpdateLoc[update_loc(cursor, loc.id, lat, lng, org_id)]
    MaybeUpdate -- false --> NoUpdate[Did not update]
    CheckAddr -- no --> SkipAddr[bad_addrs[...] = {...}]
    Loop --> Progress[print progress to stdout]
    Geocode --> TxStart[START TRANSACTION]
    Geocode --> Sort[distance_list = sorted(...)]
    Geocode --> PrintBad[print(bad_addrs)]
    Geocode --> ExceptionHandler[except Exception -> ROLLBACK; print(exception)]
    UpdateLoc --> Increment[count++]
    GetConfig -->|used by| Connect
    style Start fill:#f9f,stroke:#333,stroke-width:1px
    style Geocode fill:#bbf,stroke:#333,stroke-width:1px
    style UpdateLoc fill:#bfb,stroke:#333,stroke-width:1px
```

> SVG rendering failed for this diagram.
