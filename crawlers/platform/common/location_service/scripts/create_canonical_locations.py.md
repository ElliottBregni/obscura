# Diagram: common/location_service/scripts/create_canonical_locations.py


> Auto-generated by Obscura crawlers

## Diagram 1

```mermaid
flowchart TD
    A[main_generate_canonical_csv] --> B[parse_args / read CSV]
    B --> C{for each location}
    C --> D[retry_here_calls(location)]
    D --> E{HTTP OK and result valid?}
    E -- yes --> F[get_result_no_processed(response)]
    F --> G[get_addr_fields(here_location)]
    G --> H[augment location with Normalized_* fields]
    H --> I[collect locations]
    E -- no --> J[log/continue]
    I --> K[write_to_csv(csv_file_pathnorm.csv, locations)]
    K --> L[Done]
```

> SVG rendering failed for this diagram.

## Diagram 2

```mermaid
flowchart TD
    M[main] --> N[get_locations_without_canonical(DB_CONN.cursor)]
    N --> O{locations_to_add empty?}
    O -- yes --> P[exit loop]
    O -- no --> Q[pair locations with db_conn_pool via cycle]
    Q --> R[ThreadPoolExecutor(max_workers=10)]
    R --> S[executor.map -> handle_location(conn.cursor, location)]
    S --> T[handle_location]
    T --> U[normalized_location_info = retry_here_calls(location, cursor)]
    U --> V{status_code == 200 and result valid?}
    V -- no --> W[set_to_processed(cursor, location.id) & return]
    V -- yes --> X[get_result(response)]
    X --> Y[here_location = result["Location"]]
    Y --> Z[get_addr_fields(here_location)]
    Z --> AA[insert_into_canonical_location(cursor, location_dict, here_location_id, location.id)]
    AA --> AB[sets datamod.canonical_location_to_handle.processed_ts]
    AB --> AC[continue processing next location]
```

> SVG rendering failed for this diagram.
