# Diagram: common/location_service/scripts/extract_shipper_locations.py


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
flowchart TD
    A[Start: __main__] --> B[get_configuration()]
    A --> C[parse_arguments()]
    C --> D[args.shipper_id, args.file_to_write]
    A --> E[SECRETS.get_secret(SecretNames.DATABASE) -> config]
    E --> F[fv.db.get_connection(config, DB_APP_NAME)]
    F --> G[fv.db.get_cursor(conn)]
    G --> H[EXECUTE: SELECT *, St_AsText(boundary) as bound FROM location.location WHERE organization_id=%s]
    H --> I[shipper_locs = cursor.fetchall()]
    I --> J[build_lad_dict(cursor, shipper_id)]
    J --> J1[CURSOR EXECUTE: SELECT ... FROM location.location_lad JOIN organization_lad JOIN lad JOIN lob JOIN location WHERE l.organization_id=%s]
    J1 --> J2[lad_dict = {row.id: {lad_name, lob_name} ...}]
    I --> K[transform_location_data(shipper_locs, lad_dict)]
    K --> K1[for each loc in shipper_locs -> points = re.findall(...)]
    K1 --> K2[for each point -> create new_row with LocationName, Code, GeofenceNo, PointNo, Latitude, Longitude, Address, City, Province_Code, Postal_Code, Country, Lad, Lob]
    K2 --> K3[new_rows list returned]
    K3 --> L[helper.write_csv(args.file_to_write, helper.get_headers(), new_loc_data)]
    L --> M[End]

    subgraph Imports/Constants
        I1[fv.config.env, fv.db, scripts.loc_helpers, fv.secrets]
        I2[SECRETS = Secrets()]
        I3[DB_APP_NAME = "extract_shipper_locs"]
    end

    A --> Imports/Constants
    style Imports/Constants fill:#f9f,stroke:#333,stroke-width:1px
```

> SVG rendering failed for this diagram.
