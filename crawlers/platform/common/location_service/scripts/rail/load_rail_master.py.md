# Diagram: common/location_service/scripts/rail/load_rail_master.py


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
flowchart LR
  A[main] -->|configure logging, get DB config| B[get DB connection & cursor]
  B --> C[get_rail_locs(cursor)]
  C --> D[build_rail_dict(rail_locs)]
  D --> E[get organization lad ids via db_lad.get_organization_lad_by_name]
  E --> F[get_request_context(access_token)]
  F --> G[send_multithreaded_requests(locations, request_context, rail_station_ids, thread_nums)]
  G --> H{threading}
  H -->|spawn N threads| I[Thread i -> send_requests(subset, request_context, rail_station_ids)]
  I --> J[for each location]
  J --> K[set_query_params(location)]
  J --> L[make_post_body(location, organization_lad_id)]
  J --> M[Invoke Lambda: locations-get with full_payload]
  M --> N{existing_loc?}
  N -->|no| O[Invoke Lambda: locations-post]
  N -->|yes| P[Invoke Lambda: locations-put (with pathParameters.id)]
  O --> Q[handle response statusCode]
  P --> Q
  Q --> R[log errors if status not 200 or 409]
  R --> S[count++ / logging.info]

  subgraph Helpers
    CL[clean_lat_long(lat,lng)]
    CV[check_valid_coords(lat,lng)]
    MP[make_post_body(location, organization_lad_id)]
    SP[set_query_params(location)]
  end

  D -->|uses| CL
  D -->|uses| CV
  J -->|uses| SP
  J -->|uses| MP

  style A fill:#f9f,stroke:#333,stroke-width:1px
  style Helpers fill:#eef,stroke:#333,stroke-width:1px
```

> SVG rendering failed for this diagram.
