# Diagram: common/location_service/scripts/gm_location_scripts/combine_formatted_csv.py


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
flowchart TD
    Start([Start]) --> ParseArgs[/"parse_arguments() - argparse"/]
    ParseArgs --> ForFiles{For each file in\nargs.combined_data}
    ForFiles --> ReadCSV[/"csv.DictReader(open(file,\nencoding='cp1250'))"/]
    ReadCSV --> RowLoop{For each row}
    RowLoop --> CheckIDs{row["LOCATION_XID"] and\nrow["PLANT_CISCO"] present?}
    CheckIDs -- No --> SkipRow([skip row])
    CheckIDs -- Yes --> CheckFacility{row["Facility Type"] == ""?}
    CheckFacility -- Yes --> SetUnclassified([row["Facility Type"] = "Unclassified"])
    CheckFacility -- No --> NoChange([leave Facility Type])
    SetUnclassified --> AppendRow([combined_csv.append(row)])
    NoChange --> AppendRow
    SkipRow --> RowLoop
    AppendRow --> RowLoop
    RowLoop --> AfterFiles{All files processed?}
    AfterFiles -- Yes --> GetHeaders[/"loc_helpers.get_headers()"/]
    GetHeaders --> AssignHeaders([headers = [ ... predefined list ... ]])
    AssignHeaders --> WriteCSV[/"loc_helpers.write_csv(\n\"GM Gold List Combined.csv\",\nheaders, combined_csv)"/]
    WriteCSV --> End([End])
```

> SVG rendering failed for this diagram.
