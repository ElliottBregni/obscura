# Diagram: common/location_service/scripts/gm_location_scripts/combine_gm_data_OLD.py


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
flowchart LR
  A[main (__main__)] --> B[parse_arguments()]
  A --> C[open CSVs: supplier_poly, plant_poly, supplier_radial, loc_addresses]
  C --> D[build_loc_dict(supplier_poly) -> supplier_poly_dict]
  C --> E[build_loc_dict(plant_poly, code="Cisco") -> plant_poly_dict]
  C --> F[build_loc_dict(supplier_radial) -> supplier_radial_dict]
  D --> G[build_new_rows(supplier_poly_dict) -> supplier_poly_rows, geofenceNo]
  E --> H[build_new_rows(plant_poly_dict, geofenceNo) -> plant_poly_rows, geofenceNo]
  F --> I[build_new_rows(supplier_radial_dict, geofenceNo) -> supplier_radial_rows, geofenceNo]
  G --> J[convert_to_dict("LOCATION_GID", address_info) -> address_dict]
  J --> K[fill_address(rows, address_dict)]
  K --> L[sort_rows(rows)]
  G --> L
  H --> L
  I --> L
  L --> M[master_list = supplier + plant + radial rows]
  M --> N[partition into list_with_addrs and list_without_addrs]
  N --> O[dict_of_addrs, dict_of_non counts]
  O --> P[write_csv("CombinedData.csv", headers, list_with_addrs)]
  O --> Q[write_csv("LocsMissingAddr.csv", headers, list_without_addrs)]
  subgraph Helpers
    R[reduce_codes(codes)]
    S[remove_dups(supp, plant, rad) -> master_list]
    T[filter_rows(master_dict, rows)]
  end
  D -.-> R
  E -.-> R
  F -.-> R
  S --> T
  T --> S
```

> SVG rendering failed for this diagram.
