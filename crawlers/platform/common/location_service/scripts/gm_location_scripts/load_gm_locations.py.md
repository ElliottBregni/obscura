# Diagram: common/location_service/scripts/gm_location_scripts/load_gm_locations.py


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
flowchart TD
  Start([Start]) --> ParseArgs["Parse arguments (CSV path)"]
  ParseArgs --> ReadCSV["Open CSV and iterate rows"]
  ReadCSV --> BuildGroups["Accumulate fence_points per LocationName/Code"]
  BuildGroups --> EndOfFile{"Row changes or EOF?"}
  EndOfFile -- group complete --> SaveLocation["Save location tuple (name, code, code_type, points)"]
  EndOfFile -- continue --> ReadCSV
  SaveLocation --> GetConfig["Load configuration & secrets"]
  GetConfig --> DBConnect["Get DB connection (fv.db.get_connection)"]
  DBConnect --> SetSearchPath["SET search_path = location, public"]
  SetSearchPath --> PrintDBDetails["fv.db.print_db_details"]
  PrintDBDetails --> ForEachLocation["For each location_name in locations_by_name"]
  ForEachLocation --> ForEachInstance["For each location instance"]
  ForEachInstance --> CountPoints["Count points (n)"]
  CountPoints --> IsPoint1{"n == 1?"}
  IsPoint1 -- yes --> MakePoint["geometry = POINT(...) ; buffer=1609.344"]
  IsPoint1 -- no --> IsLess4{"n < 4?"}
  IsLess4 -- yes --> MakeLine["geometry = LINESTRING(...) ; buffer=1609.344"]
  IsLess4 -- no --> MakePolygon["geometry = POLYGON(...) ; buffer=0"]
  MakePoint --> ValidateCodes
  MakeLine --> ValidateCodes
  MakePolygon --> ValidateCodes
  ValidateCodes["Split codes and validate with CODE_PATTERNS by code_type"] --> CodeValid{"pattern match?"}
  CodeValid -- no --> SkipCode["Print skipping probable carrier location code"]
  CodeValid -- yes --> ForEachOrg["For each organization_fv_id in organization_fv_ids"]
  ForEachOrg --> InsertAttempt["Execute INSERT_LOCATION_STMT via cursor.execute"]
  InsertAttempt --> FetchRow["newloc = cursor.fetchone()"]
  FetchRow --> PrintAdded["Print added location"]
  InsertAttempt -->|IntegrityError (unique)| HandleUnique["If UNIQUE_VIOLATION: print ignore & rollback"]
  InsertAttempt -->|Other psycopg2.Error| HandleOther["Print error and exit(1)"]
  SkipCode --> ForEachInstance
  PrintAdded --> ForEachInstance
  HandleUnique --> ForEachInstance
  HandleOther --> End([Exit with error])
  ForEachInstance --> ForEachLocation
  ForEachLocation --> CommitDone["All locations processed"]
  CommitDone --> EndDone([End])
```

> SVG rendering failed for this diagram.
