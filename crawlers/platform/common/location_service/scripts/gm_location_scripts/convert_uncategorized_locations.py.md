# Diagram: common/location_service/scripts/gm_location_scripts/convert_uncategorized_locations.py


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
flowchart TD
    A[Script Start (__main__)] --> B[parse_arguments()]
    A --> C[read uncat_rows (csv.DictReader)]
    A --> D[read full_shipper_rows (optional csv.DictReader)]
    A --> E[SECRETS.get_secret -> fv.db.get_connection]
    E --> F[get_cursor(cursor)]
    F --> G[get_update_shipper_locs(cursor, uncat_rows, full_shipper_rows, ins, up)]
    subgraph UpdateLoop["get_update_shipper_locs"]
        G --> G1[iterate uncat_rows]
        G1 --> |has Shipper_Code| G2[format Shipper_Code.zfill(9)]
        G2 --> G3[get_tend_ship_join(cursor, row)]
        G3 --> |found| G4[match -> if up then update_referent_id(cursor, tend_id, ship_id)]
        G3 --> |not found| G5[if ins -> insert_new_loc(cursor, full_shipper_dict, shipper_code)]
        G5 --> |row_to_ins| G6[new_rows.append(row_to_ins)]
        G1 --> G7[every 15 rows -> output_progress(rate, i, len_uncat_rows)]
        G --> |end| G8[if ins -> write_csv("new_gm_locs.csv", new_rows)]
        G --> G9[print(len(match))]
    end
    subgraph InsertFlow["insert_new_loc"]
        G5 --> I1[full_shipper_dict lookup]
        I1 --> I2[build_new_dict([row]) -> new_row]
        I2 --> I3[make_geo_row(new_row)]
        I3 --> I4[get_lat_long(geo_row) -> (lat,lng)]
        I4 --> I5[new_row["Latitude"/"Longitude"]=lat/lng]
    end
    subgraph Preprocess["convert_to_dict"]
        H1[convert_to_dict("LOCATION_GID", full_shipper_rows)] --> H2[address_dict]
        G --> |if ins before loop| H1
    end
    classDef db fill:#f9f,stroke:#333;
    class E,F,G3,G4 db
```

> SVG rendering failed for this diagram.
