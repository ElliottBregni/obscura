# Diagram: common/location_service/scripts/gm_location_scripts/transform_cells.py


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
flowchart TD
    A[Start: __main__] --> B[get_configuration()]
    A --> C[parse_arguments()]
    C --> D[open CSV file<br/>csv.DictReader(args.lads_to_change)]
    D --> E[change_lads(rows)]
    E --> F[loc_helpers.write_csv(filepath, headers, new_rows)]
    B --> G[fv.config.Configuration<br/>sources: EnvironmentConfigurationSource(filters=["DB_.*"])<br/>defaults: LOG_LEVEL=INFO, DB_APPLICATION_NAME="Update GM Data"]
    subgraph transform["Row transformation"]
        E --> H[for each row:<br/>set Lob = "Assembly"<br/>set Lad = LAD_DICT[row["Lad"]]]
        H --> I[LAD_DICT: "S"->"Supplier", "L"->"Logistics Optimization Center", "A"->"Assembly Plant"]
    end
    F --> J[End]
```

> SVG rendering failed for this diagram.
