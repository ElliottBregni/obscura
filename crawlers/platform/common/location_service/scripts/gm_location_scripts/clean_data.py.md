# Diagram: common/location_service/scripts/gm_location_scripts/clean_data.py


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
flowchart LR
  A[Start] --> B[parse_arguments()]
  B --> C[open CSV & csv.DictReader]
  C --> D[remove_dups(rows)]
  D --> E[build_dct(rows)]
  E --> E1{Code prefix numeric?}
  E1 -- No --> E2[skip row]
  E1 -- Yes --> F{Code in code_dict?}
  F -- No --> G[create code_dict[Code]: rows=[row], PointNo=1, lad_code=row.lad_code]
  F -- Yes --> H{int(PointNo) <= int(existing PointNo)?}
  H -- Yes --> I{existing.lad_code == "u" or (not existing.lad_code and row.lad_code != "u" and row.lad_code)}
  I -- True --> J[replace entry: rows=[row], PointNo=1, lad_code=row.lad_code]
  I -- False --> K[do nothing]
  H -- No --> L[append row to rows; set PointNo=row.PointNo]
  G --> M[return code_dict]
  J --> M
  K --> M
  L --> M
  M --> N[remove_dups collects all rows from code_dict into master_list]
  N --> O[helper.write_csv(path, helper.get_headers(), rows)]
  O --> P[End]
```

> SVG rendering failed for this diagram.
