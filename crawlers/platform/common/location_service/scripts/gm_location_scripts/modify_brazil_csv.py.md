# Diagram: common/location_service/scripts/gm_location_scripts/modify_brazil_csv.py


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
graph TD
    A[parse_arguments()] --> B[open CSV (args.brazil_locs)]
    B --> C[brazil_locs: csv.DictReader]
    C --> D[remove_dups(old_csv)]
    D --> E[build_modified_csv(old_csv, good_addrs)]
    E --> E1[iterate suppliers]
    E1 -->|if Supplier ID in good_addrs| F[create row dict]
    F --> G[add fields: Lob,Lad,LocationName,Code,GeofenceNo,PointNo]
    G --> H[map address fields -> Address,City,Province,Postal_Code]
    H --> I[helper.change_country(l["Country"])]
    I --> J[set Latitude/Longitude from good_addrs]
    J --> K[append row to modified_dict]
    E --> L[returns modified_dict]
    subgraph Constants & Helpers
        CA[KOREAN_GOOD_ADDRS (good_addrs)]
        HB[scripts.loc_helpers (helper)]
    end
    CA --> E
    HB --> I
    HB --> M[helper.write_csv("formatted_korean_locs.csv", modified_dict)]
    L --> M
    subgraph Script Execution
        N[__main__]
        N --> A
        N --> C
        N --> CA
        N --> L
        L --> M
        M --> O[output: formatted_korean_locs.csv]
    end
```

> SVG rendering failed for this diagram.
