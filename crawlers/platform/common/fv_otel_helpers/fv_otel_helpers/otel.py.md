# Diagram: common/fv_otel_helpers/fv_otel_helpers/otel.py


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
flowchart LR
A[Start] --> B{Inputs}
B --> C[prefix: string]
B --> D[mapping: dict[string, AttributeValue]]
C --> E[For each (k, v) in mapping.items()]
D --> E
E --> F[Compute key: prefix + "." + k]
F --> G[Add entry key -> v to result dict]
G --> H[Result: dict[string, AttributeValue]]
H --> I[Return and End]
```

> SVG rendering failed for this diagram.
