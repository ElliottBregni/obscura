# Diagram: common/fv_otel_helpers/fv_otel_helpers/tests/test_structlog_setup.py


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
flowchart TD
  A[Test: test_should_show_locals (parametrized)] --> B[monkeypatch.setenv("AWS_STAGE", stage)]
  B --> C{show_locals is None?}
  C -- yes --> D[monkeypatch.delenv("SHOW_LOCALS")]
  C -- no --> E[monkeypatch.setenv("SHOW_LOCALS", show_locals)]
  D --> F[_should_show_locals() returns boolean]
  E --> F
  F --> G[assert result == expected]

  H[Test: test_configure_structlog] --> I[configure_structlog("info","unit tests")]
  I --> J[get_logger() -> logger]
  J --> K[logger.bind(bar="b", baz="c")]
  K --> L[logger.info("testing123", foo="a")]
  L --> M[assert logger.get_effective_level() == NAME_TO_LEVEL["info"]]
  L --> N[capsys.readouterr() -> out]
  N --> O[assert '"env": "unit tests"' in out]
  N --> P[assert '"foo": "a"' in out]
  N --> Q[assert '"bar": "b"' in out]
  N --> R[assert '"baz": "c"' in out]

  style A fill:#f9f,stroke:#333,stroke-width:1px
  style H fill:#ff9,stroke:#333,stroke-width:1px
```

> SVG rendering failed for this diagram.
