# Diagram: common/fv_otel_helpers/fv_otel_helpers/tests/test_otel.py


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
flowchart TD
    A[call prefix_keys(prefix, dict)] --> B{iterate keys k in dict}
    B --> C[compute new_key = prefix + "." + k]
    C --> D[assign new_dict[new_key] = dict[k]]
    D --> E[after all keys processed return new_dict]
    subgraph Example1
      I1[prefix = "foo"] --> M1[dict = {"a":1,"b":"cool","c":True}]
      M1 --> O1[output = {"foo.a":1,"foo.b":"cool","foo.c":True}]
    end
    subgraph Example2
      I2[prefix = "foo.bar"] --> M2[dict = {"a":1,"b":"cool","c":True}]
      M2 --> O2[output = {"foo.bar.a":1,"foo.bar.b":"cool","foo.bar.c":True}]
    end
    A --- I1
    A --- I2
```

> SVG rendering failed for this diagram.
