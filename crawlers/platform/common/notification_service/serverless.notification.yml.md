# Diagram: common/notification_service/serverless.notification.yml


> Auto-generated by Obscura crawlers

## Diagram 1

```mermaid
flowchart LR
  APIGW[API Gateway] -->|GET /, GET /{notification_id}| FN_get[get_notifications]
  APIGW -->|PATCH /{notification_id}/read, PATCH /read| FN_mark[mark_notifications_as_read]
  APIGW -->|POST /dwell/configure| FN_configure[configure_dwell_recipients]
  APIGW -->|GET /dwell/search| FN_search[search_dwell_recipients]
  APIGW -->|invoke| FN_create[create_notification]
  APIGW -->|invoke| FN_update[update_notification]

  subgraph Lambdas
    FN_get --> LN_get[notification_service/get_notifications.lambda_handler]
    FN_mark --> LN_mark[notification_service/mark_notifications_as_read.lambda_handler]
    FN_create --> LN_create[notification_service/create_notification.lambda_handler]
    FN_update --> LN_update[notification_service/update_notification.lambda_handler]
    FN_configure --> LN_configure[notification_service/dwell_alert_recipients.lambda_handler]
    FN_search --> LN_search[notification_service/dwell_alert_search.lambda_handler]
  end

  IAM[IAM Role (managedPolicies + statements)] ---|grants| Lambdas
  SNS[SNS] ---|Publish/CreateTopic| Lambdas
  S3[S3: email-attachments-* buckets] ---|r/w| Lambdas
  Secrets[Secrets Manager] ---|GetSecretValue| Lambdas
  Logs[CloudWatch Logs] ---|PutLogEvents| Lambdas
  VPC[VPC] ---|attached| Lambdas

  subgraph Provider[AWS / Serverless Framework]
    APIGW
    IAM
    SNS
    S3
    Secrets
    Logs
    VPC
  end
```

> SVG rendering failed for this diagram.

## Diagram 2

```mermaid
classDiagram
  class get_notifications {
    +handler: notification_service/get_notifications.lambda_handler()
    +events: GET /, GET /{notification_id}
    +layers: fv_layers
  }
  class mark_notifications_as_read {
    +handler: notification_service/mark_notifications_as_read.lambda_handler()
    +events: PATCH /{notification_id}/read, PATCH /read
    +layers: fv_layers
  }
  class create_notification {
    +handler: notification_service/create_notification.lambda_handler()
    +layers: fv_layers
  }
  class update_notification {
    +handler: notification_service/update_notification.lambda_handler()
    +layers: fv_layers
  }
  class configure_dwell_recipients {
    +handler: notification_service/dwell_alert_recipients.lambda_handler()
    +events: POST /dwell/configure
    +layers: fv_layers
  }
  class search_dwell_recipients {
    +handler: notification_service/dwell_alert_search.lambda_handler()
    +events: GET /dwell/search
    +layers: fv_layers
  }

  get_notifications --> mark_notifications_as_read : shares IAM / environment
  create_notification --> update_notification : CRUD relation
  configure_dwell_recipients --> search_dwell_recipients : dwell API group
```

> SVG rendering failed for this diagram.
