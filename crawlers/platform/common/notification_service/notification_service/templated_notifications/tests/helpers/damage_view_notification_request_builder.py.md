# Diagram: common/notification_service/notification_service/templated_notifications/tests/helpers/damage_view_notification_request_builder.py


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
classDiagram
    class NotificationRequestBuilder {
        - _request: dict
        + NotificationRequestBuilder(template_id=1)
        + _default_context(): Dict[str, Any]
        + with_solution_id(solution_id: str): NotificationRequestBuilder
        + with_status(status: str, in_progress_status: Optional[str]): NotificationRequestBuilder
        + with_vin(vin: str): NotificationRequestBuilder
        + with_assignee(assignee: str): NotificationRequestBuilder
        + with_location(code: str, name: str): NotificationRequestBuilder
        + with_damages(damages: List[Dict[str,str]]): NotificationRequestBuilder
        + with_recipients(users: List[Dict[str,Any]]): NotificationRequestBuilder
        + with_rejection_comments(comments: str): NotificationRequestBuilder
        + with_comment_text(comment: str): NotificationRequestBuilder
        + with_context_override(override: Dict[str,Any]): NotificationRequestBuilder
        + with_organization(org_name: str): NotificationRequestBuilder
        + build(): NotificationRequest
    }
    class NotificationRequest {
    }
    NotificationRequestBuilder --> NotificationRequest : build() creates
    NotificationRequestBuilder : uses nested update_dict(path, value)
    NotificationRequestBuilder : default context keys: vin, submission_id, metadata, org_details, location, damages, users, user_preference, template_id, reference_id, notification_type, source_service
```

> SVG rendering failed for this diagram.
