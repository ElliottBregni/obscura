# Diagram: common/notification_service/notification_service/db/db_user.py


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
flowchart TD
    Caller -->|calls| GetUser[get_user_data(cursor, user_email)]
    GetUser --> ExecuteSQL1[Execute SELECT SQL]
    ExecuteSQL1 --> FetchRow[row = cursor.fetchone()]
    FetchRow --> RowMissing{row is falsy?}
    RowMissing -- yes --> ReturnNone1[return None]
    RowMissing -- no --> BuildCols[column_names = [desc[0] for desc in cursor.description]]
    BuildCols --> ZipDict[result = dict(zip(column_names, row))]
    ZipDict --> ReturnDict[return result]

    Caller -->|calls| CreateUser[create_user(cursor, user_email)]
    CreateUser --> LogSQL[logging.info(INSERT SQL)]
    LogSQL --> ExecuteSQL2[Execute INSERT SQL]
    ExecuteSQL2 --> FetchResults[results = cursor.fetchone()]
    FetchResults --> ResultsMissing{results present?}
    ResultsMissing -- no --> ReturnNone2[return None]
    ResultsMissing -- yes --> ReturnID[return results[0]]
```

> SVG rendering failed for this diagram.
