# Diagram: common/notification_service/config/config.test.yml


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
classDiagram
    class crawl {
        +generate_mermaid(file_path)
        +main()
    }
    class crawlers {
        +Crawler
        +run()
    }
    class backend {
        +start_server()
    }
    class agents {
        +Agent
    }
    class sync {
        +sync_repos()
    }
    class tests {
        +test_suite()
    }
    class "copilot_models.py" {
        +guard_automation(name)
    }
    crawl --> crawlers : uses
    crawl --> "copilot_models.py" : calls
    crawlers --> agents : spawns
    backend --> agents : communicates
    sync --> backend : updates
    tests --> crawl : tests
    tests --> crawlers : tests
```

> SVG rendering failed for this diagram.
