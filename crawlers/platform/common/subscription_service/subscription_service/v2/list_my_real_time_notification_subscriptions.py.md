# Diagram: common/subscription_service/subscription_service/v2/list_my_real_time_notification_subscriptions.py


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
flowchart TD
    LH[lambda_handler(event, context, audit_refs)]
    Auth[auth_check: NOTIFICATION_MANAGEMENT_AUTH_CHECK]
    Req[ListMyRealTimeNotificationSubscriptionsRequest(event)]
    subgraph Connectors
      PERSONA_DB[FvDatabaseConnector\nPERSONA_DB (SecretNames.PERSONA_DATABASE)]
      SHIPMENT_DB[FvDatabaseConnector\nSHIPMENT_DB (SecretNames.DATABASE_TRACKING)]
    end
    UserDAO[UserDAO(SHIPMENT_DB)]
    OrgDAO[OrganizationDAO(SHIPMENT_DB)]
    RuleDAO[NotificationRuleDAO(PERSONA_DB)]
    Handler[ListMyRealTimeNotificationSubscriptionsHandler]
    HandleCall[.handle(request) → response]

    LH -->|wrapped by| Auth
    LH --> Req
    Req --> UserDAO
    Req --> OrgDAO
    Req --> RuleDAO
    UserDAO --> Handler
    OrgDAO --> Handler
    RuleDAO --> Handler
    Handler --> HandleCall
```

> SVG rendering failed for this diagram.
