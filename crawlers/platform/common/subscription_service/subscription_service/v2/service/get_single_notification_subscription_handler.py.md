# Diagram: common/subscription_service/subscription_service/v2/service/get_single_notification_subscription_handler.py


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
classDiagram
    class GetSingleNotificationSubscriptionRequest {
        +notification_rule_id
        +auth0_id
        +org_id
        +__init__(event)
    }
    class GetSingleNotificationSubscriptionResponse {
        +definition: dict
        +deliveryActions: list
        +eventType: str
        +id: str
        +product: str
        +name: str
        +from_notification_rule(record)
    }
    class NotificationRule {
        +id
        +definition
        +notification_delivery_actions
        +event_type
        +product
        +name
    }
    class NotificationSubscriptionsHandlerBase {
        +user_dao
        +organization_dao
        +__init__(user_dao, organization_dao)
        +retrieve_credentials(request)
    }
    class GetSingleNotificationSubscriptionHandler {
        +rule_dao
        +__init__(user_dao, organization_dao, rule_dao)
        +handle(request)
    }
    class RuleDAO {
        +fetch_by_id(rule_id, persona_id, org_fv_id)
    }
    class fv_aws_lambdas {
        +get_path_parameter(event, name, required)
        class auth {
            +get_user_id(event)
            +get_organization_id(event)
        }
    }
    class make_response {
        <<function>>
        +make_response(payload)
    }
    class NotFoundError {
        <<exception>>
    }

    GetSingleNotificationSubscriptionHandler --|> NotificationSubscriptionsHandlerBase
    GetSingleNotificationSubscriptionHandler o-- RuleDAO : uses
    GetSingleNotificationSubscriptionHandler --> make_response : returns
    GetSingleNotificationSubscriptionHandler --> NotFoundError : raises
    GetSingleNotificationSubscriptionHandler --> GetSingleNotificationSubscriptionRequest : consumes
    GetSingleNotificationSubscriptionRequest --> fv_aws_lambdas : calls
    fv_aws_lambdas.auth --> GetSingleNotificationSubscriptionRequest : provides
    GetSingleNotificationSubscriptionResponse ..> NotificationRule : created from
    RuleDAO ..> NotificationRule : returns
    GetSingleNotificationSubscriptionResponse --> make_response : serialized into
    NotificationSubscriptionsHandlerBase --> fv_aws_lambdas.auth : may use
```

> SVG rendering failed for this diagram.
