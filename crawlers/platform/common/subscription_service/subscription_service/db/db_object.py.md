# Diagram: common/subscription_service/subscription_service/db/db_object.py


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
flowchart TD
    A[does_object_exist(reference_id, system_id, owner_id?)] --> B[build data dict]
    B --> C[build where_clause]
    C --> D[compose SELECT query]
    D --> E[logging: cursor.mogrify(...)]
    E --> F[cursor.execute(query, data)]
    F --> G[cursor.fetchall() -> rows]
    G --> H{rows empty?}
    H -- yes --> I[return []]
    H -- no --> J[return rows]
    F -.exception.-> X[raise_generic_exception(e)]
    E -.exception.-> X

    subgraph Create
        K[create_object(reference_id, system_id, owner_id, reference_type)] --> L[build data dict]
        L --> M[compose INSERT ... RETURNING id]
        M --> N[logging: cursor.mogrify(...)]
        N --> O[cursor.execute(query, data)]
        O --> P[cursor.fetchone()[0] -> return id]
        O -.exception.-> X
    end

    subgraph Update
        Q[update_object_reference_type(id, reference_type)] --> R[build data dict]
        R --> S[compose UPDATE query]
        S --> T[logging: cursor.mogrify(...)]
        T --> U[cursor.execute(query, data)]
        U --> V[return id]
        U -.exception.-> X
    end

    subgraph Delete
        Y[delete_object(id)] --> Y1[build data dict]
        Y1 --> Y2[compose DELETE query]
        Y2 --> Y3[logging: cursor.mogrify(...)]
        Y3 --> Y4[cursor.execute(query, data)]
        Y4 -.exception.-> X
    end

    subgraph Get
        Z[get_object(id)] --> Z1[build data dict]
        Z1 --> Z2[compose SELECT query]
        Z2 --> Z3[logging: cursor.mogrify(...)]
        Z3 --> Z4[cursor.execute(query, data)]
        Z4 --> Z5[cursor.fetchone() -> row]
        Z5 --> Z6{row is None?}
        Z6 -- yes --> Z7[return None]
        Z6 -- no --> Z8[build column_names, zip -> dict -> return dict]
        Z4 -.exception.-> X
    end

    style X fill:#f66,stroke:#900,color:#fff
    classDef dbOps fill:#eef,stroke:#66a
    class D,M,S,Y2,Z2 dbOps
```

> SVG rendering failed for this diagram.
