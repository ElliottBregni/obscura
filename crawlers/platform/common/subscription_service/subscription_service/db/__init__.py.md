# Diagram: common/subscription_service/subscription_service/db/__init__.py


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
classDiagram
    class module_code {
        +raise_generic_exception(e)
    }
    class sys {
        +exc_info()
    }
    class fv_error_DatabaseError {
        +DatabaseError(message, client_message)
        +with_traceback(traceback)
    }
    class constants {
        +GENERIC_ERROR_MESSAGE
    }
    module_code --> sys : calls
    module_code --> constants : reads
    module_code --> fv_error_DatabaseError : raises
    fv_error_DatabaseError ..> "Exception" : <<extends>>
    module_code : uses traceback variable
    module_code : imports fv.error, subscription_service.common.constants
```

> SVG rendering failed for this diagram.
