# Diagram: common/subscription_service/subscription_service/db/db_product.py


> Auto-generated by Obscura crawlers

## Diagram 1

```mermaid
flowchart TD
    A[get_product(name)] --> B[build data dict {"name": name}]
    B --> C[prepare SQL query SELECT id FROM fv_subscription.product WHERE name = %(name)s]
    C --> D[logging.info(cursor.mogrify(query, data).decode())]
    D --> E[cursor.execute(query, data)]
    E --> F[row = cursor.fetchone()]
    F --> G{row exists?}
    G -- No --> H[return None]
    G -- Yes --> I[column_names = [desc[0] for desc in cursor.description]]
    I --> J[return dict(zip(column_names, row))]
    F --> K[Exception]
    K --> L[raise_generic_exception(e)]
```

> SVG rendering failed for this diagram.

## Diagram 2

```mermaid
flowchart TD
    A2[get_product_by_id(id)] --> B2[build data dict {"id": id}]
    B2 --> C2[prepare SQL query SELECT name FROM fv_subscription.product WHERE id = %(id)s]
    C2 --> D2[logging.info(cursor.mogrify(query, data).decode())]
    D2 --> E2[cursor.execute(query, data)]
    E2 --> F2[row = cursor.fetchone()]
    F2 --> G2{row exists?}
    G2 -- No --> H2[return None]
    G2 -- Yes --> I2[column_names = [desc[0] for desc in cursor.description]]
    I2 --> J2[return dict(zip(column_names, row))]
    F2 --> K2[Exception]
    K2 --> L2[raise_generic_exception(e)]
```

> SVG rendering failed for this diagram.
