# Diagram: common/subscription_service/subscription_service/db/db_subscription_notification_type.py


> Auto-generated by Obscura crawlers

## Diagram 1

```mermaid
flowchart TD
    subgraph GetNotificationTypes
        A[call get_notification_types(cursor, subscription_id)]
        B{prepare data dict}
        C[build SQL query string]
        D[logging.info(cursor.mogrify(query, data).decode())]
        E[cursor.execute(query, data)]
        F[rows = cursor.fetchall()]
        G[column_names = [desc[0] for desc in cursor.description]]
        H[notification_types = []]
        I[for each row: notification_types.append(dict(zip(column_names, row)))]
        J[return notification_types]
    end
    A --> B --> C --> D --> E --> F --> G --> H --> I --> J
```

> SVG rendering failed for this diagram.

## Diagram 2

```mermaid
flowchart TD
    subgraph CreateSubscriptionNotificationType
        K[call create_subscription_notification_type(cursor, subscription_id, notification_type_id)]
        L{prepare data dict}
        M[build INSERT SQL query string]
        N[logging.info(cursor.mogrify(query, data).decode())]
        O[cursor.execute(query, data)]
        P{Exception?}
        Q[raise_generic_exception(e)]
    end
    K --> L --> M --> N --> O --> P
    P -- yes --> Q
    P -- no --> R[end successfully]
```

> SVG rendering failed for this diagram.
