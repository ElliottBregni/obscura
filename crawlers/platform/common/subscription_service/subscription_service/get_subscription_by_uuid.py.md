# Diagram: common/subscription_service/subscription_service/get_subscription_by_uuid.py


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
flowchart LR
  Start([Start]) --> HandlerWrapper[@fv.aws.lambdas.mandatory_lambda_handling()]
  HandlerWrapper --> Lambda[lambda_handler(event, context, audit_refs)]
  Lambda --> Log[log info "Received get user subscription event"]
  Lambda --> DBConnect[DB_CONN.establish_connection()]
  DBConnect --> Cursor[cursor = DB_CONN.cursor]
  Cursor --> GetParam[get_query_parameter(event, "subscriptionId", required=True)]
  GetParam --> Fetch[db_subscription.get_user_subscription(cursor, subscription_id)]
  Fetch -->|not found| NotFound[make_not_found_response(event, context, error_msg)]
  NotFound --> EndNotFound([Return 404 Not Found])
  Fetch -->|found| BuildResp[Build response dict with subscription fields]
  BuildResp --> RefType[get_reference_type(subscription.get("reference_type"))]
  RefType --> RefDecide{reference_type empty or in VIN_TYPES?}
  RefDecide -->|yes| ReturnVIN["vin"]
  RefDecide -->|no| MapLookup[REFERENCE_TYPE_MAP lookup or return original]
  MapLookup --> ReturnMapped[returned reference type]
  ReturnVIN --> MergeResponse[set response.reference_type]
  ReturnMapped --> MergeResponse
  MergeResponse --> MakeResp[make_response(response, status_code=200)]
  MakeResp --> EndOK([Return 200 OK])
  style Start fill:#eef,stroke:#333,stroke-width:1px
  style EndOK fill:#dfd,stroke:#333,stroke-width:1px
  style EndNotFound fill:#fdd,stroke:#333,stroke-width:1px
```

> SVG rendering failed for this diagram.
