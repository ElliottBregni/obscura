# Diagram: common/subscription_service/config/config.prod-b.yml


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
flowchart TD
    CLI[User CLI] --> CRAWL[crawl.py]
    CRAWL --> CRAWLERS[crawlers.py / crawlers/]
    CRAWLERS --> FETCH[Fetch URLs / HTTP requests]
    FETCH --> PARSE[Parse & Extract Data]
    PARSE --> STORE[Store Results (repos/ backend/ files)]
    STORE --> SYNC[sync.py]
    SYNC --> BACKEND[backend/ service]
    BACKEND --> AGENTS[agents/ worker processes]
    AGENTS --> SDK[sdk/ integrations]
    CRAWLERS --> EXAMPLES[examples/ usage]
    CRAWL --> DOCS[docs/]
    classDef component fill:#f9f,stroke:#333,stroke-width:1px
    class CRAWL,CRAWLERS,FETCH,PARSE,STORE,SYNC,BACKEND,AGENTS,SDK,EXAMPLES,DOCS component
```

> SVG rendering failed for this diagram.
