# Diagram: common/subscription_service/serverless.subscription.yml


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
graph LR
  APIGW[API Gateway]
  subgraph Service: subscription
    subgraph Functions
      get_user_subscriptions["get_user_subscriptions\nGET /\nGET /reference/{reference_id}"]
      update_subscription["update_subscription\nPATCH /{id}"]
      get_subscription_by_uuid["get_subscription_by_uuid\nGET /user"]
      subscribe_to_event["subscribe_to_event\nPOST /subscribe"]
      activate_subscription["activate_subscription\nPOST /user/activate"]
      unsubscribe_from_event["unsubscribe_from_event\nPOST /unsubscribe"]
      unsubscribe_by_uuid["unsubscribe_by_uuid\nPOST /user/unsubscribe"]
      send_event_to_subscribers["send_event_to_subscribers\nPOST /send_event"]
      v2_subscribe["subscribe_to_real_time_notification\nPOST /v2\nPATCH /v2/{notification_rule_id}"]
      v2_list["list_my_real_time_notification_subscriptions\nGET /v2"]
      v2_delete["delete_a_real_time_notification_subscription\nDELETE /v2/{notification_rule_id}"]
      v2_get["get_a_real_time_notification_subscription\nGET /v2/{notification_rule_id}"]
      v2_unsubscribe["unsubscribe_from_real_time_notification\nPOST /v2/{notification_rule_id}/unsubscribe/{unsubscribe_id}"]
    end
  end

  APIGW --> get_user_subscriptions
  APIGW --> update_subscription
  APIGW --> get_subscription_by_uuid
  APIGW --> subscribe_to_event
  APIGW --> activate_subscription
  APIGW --> unsubscribe_from_event
  APIGW --> unsubscribe_by_uuid
  APIGW --> send_event_to_subscribers
  APIGW --> v2_subscribe
  APIGW --> v2_list
  APIGW --> v2_delete
  APIGW --> v2_get
  APIGW --> v2_unsubscribe

  subgraph Provider: AWS (python3.13, arm64)
    APIGW --> IAM["IAM Role\nmanagedPolicies + statements"]
    APIGW --> VPC["VPC (configured)"]
    APIGW --> Env["Environment Variables\nAWS_STAGE, ACCOUNT_ID, DD_*"]
  end
```

> SVG rendering failed for this diagram.
