# Diagram: common/subscription_service/docs/notification-v2.openapi.yaml


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
classDiagram
    class NotificationSubscriptionService {
        +PUT / : create NotificationSubscriptionRequest -> 201,400,401,403,500
        +GET / : list NotificationSubscriptions?pageNumber,pageSize -> 200,400,401,403,500
        +DELETE /{notificationRuleId} : delete by id -> 200,400,401,403,404,500
        +security: BasicAuth (http basic)
    }

    class NotificationSubscriptionRequest {
        +id: string (uuid)
        +name: string
        +product: "Shipments"
        +deliveryActions: NotificationDeliveryAction[]
        +eventType: string
        +condition: string
    }

    class NotificationDeliveryAction {
        +method: string {sms,email}
        +target: string
        +unsubscribeId: string (uuid)
    }

    class NotificationSubscriptions {
        +meta: Meta
        +data: NotificationSubscription[]
    }

    class Meta {
        +totalPages: number
        +currentPage: number
        +totalCount: number
    }

    class NotificationSubscription {
        +id: string (uuid)
        +name: string
        +product: "Shipments"
        +notificationDeliveryAction: NotificationDeliveryAction[]
        +state: string
        +modified: date-time
    }

    NotificationSubscriptionService --> NotificationSubscriptionRequest : "PUT body"
    NotificationSubscriptionService --> NotificationSubscriptions : "GET response"
    NotificationSubscriptionService --> NotificationSubscription : "DELETE target"
    NotificationSubscriptionRequest o-- NotificationDeliveryAction : "deliveryActions"
    NotificationSubscriptions o-- Meta : "meta"
    NotificationSubscriptions o-- NotificationSubscription : "data"
    NotificationSubscription o-- NotificationDeliveryAction : "notificationDeliveryAction"
```

> SVG rendering failed for this diagram.
