# Diagram: common/.gitlab-ci.yml


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
flowchart TD
  PY[".python"]
  COMMON[".common-layer-pip"]
  PY -->|extends| COMMON

  COMMON -->|before_script: 1| VENV["python -m venv venv"]
  VENV -->|2| ACT["source venv/bin/activate"]
  ACT -->|3| PIP_DEV["pip install -r dev-requirements.txt"]
  PIP_DEV -->|4| PIP_EDIT["pip install -e platform/common/fv/python"]
  PIP_EDIT -->|5| PIP_JWT["pip install -r platform/common/jwt_custom_authorizer/requirements.txt"]

  CT["common-tests"]
  CT -->|extends| COMMON
  CT -->|script| LOGIN_REF["!reference [.py-test:login, script]"]
  CT -->|script| PYTEST["pytest --cov-report xml:coverage.xml --cov-branch platform/common/fv/python"]
  CT -->|rules| CHANGES_COMMON["changes: platform/common/**/*"]

  SLS_BASE[".sls"]
  SLS_FV[".sls-fv-layer"]
  SLS_BASE -->|extends| SLS_FV
  SLS_FV -->|variables| SLS_VARS["ROLE_SESSION_NAME=fvLayer\nDEPLOY_PATH=platform/common/fv\nSERVERLESS_CONFIG_FILENAME=serverless.fv.yml"]

  SLS_MANUAL["sls-manual-deploy-fv-layer"]
  SLS_MANUAL -->|extends| SLS_FV
  SLS_MANUAL -->|extends| SLS_MANUAL_EXT[".sls-manual"]
  SLS_MANUAL -->|rules| MR_RULE["if $CI_PIPELINE_SOURCE == \"merge_request_event\" when: manual\nchanges: platform/common/fv/**/*"]

  SLS_PUSH["sls-deploy-fv-layer-on-push"]
  SLS_PUSH -->|extends| SLS_FV
  SLS_PUSH -->|rules| PUSH_RULE["if $CI_PIPELINE_SOURCE == \"push\" && $CI_COMMIT_BRANCH == \"develop\"\nwhen: on_success\nvariables: AWS_STAGE=dev\nchanges: platform/common/fv/**/*"]
```

> SVG rendering failed for this diagram.
