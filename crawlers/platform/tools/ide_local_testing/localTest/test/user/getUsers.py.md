# Diagram: tools/ide_local_testing/localTest/test/user/getUsers.py


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
flowchart LR
    A[Start] --> B[Call management_api.get_all_users(page=0)]
    B --> C[format_users_list(response)]
    C --> D[for loop range(0,10)]
    D --> E{response length > 0?}
    E -- Yes --> F[format_users_list(response)]
    F --> G[for user in userList]
    G --> H{email startswith "alligator"?}
    H -- Yes --> I[print user_id --> email]
    H -- No --> J{email startswith "fv_"?}
    J -- Yes --> I
    J -- No --> K[continue]
    E -- No --> L[break loop / End]
    I --> G
    K --> G
    G --> D
    L --> M[End]
```

> SVG rendering failed for this diagram.
