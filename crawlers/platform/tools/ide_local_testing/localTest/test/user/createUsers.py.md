# Diagram: tools/ide_local_testing/localTest/test/user/createUsers.py


> Auto-generated by Obscura crawlers

## Diagram 1

```mermaid
flowchart LR
  A[Load modules & headers] --> B[Init ShipmentDatabase connector]
  B --> C[get_cursor()]
  C --> D[get_organization_by_id]
  D --> E[get_org_solutions]
  D --> F[get_all_roles]
  F --> G[reverse_roles]
  H[Determine actor_id & allowed_roles] --> I[get_roles]
  I --> J[allowed_ids / allowed_names]
  K[Users list] --> L{For each user}
  L --> M[get_auth0_user_by_email]
  M -->|exists| N[remove_user]
  N --> O[continue]
  M -->|not found| O
  O --> P[create_users (dry_run=True)]
  P --> Q[create_users (dry_run=False)]
  style A fill:#f9f,stroke:#333,stroke-width:1px
  style Q fill:#bfe,stroke:#333,stroke-width:1px
```

> SVG rendering failed for this diagram.

## Diagram 2

```mermaid
classDiagram
  class ShipmentDatabase {
    -connectionString
    +ShipmentDatabase(connStr)
    +get_cursor()
  }
  class Role {
    +freightVerify_admin()
  }
  class IAM_DB {
    <<module>>
    +get_organization_by_id(cursor, organization_id)
    +get_org_solutions(cursor, org_id)
    +get_roles(cursor, actor_id, org_id)
    +get_all_roles(cursor, org_id)
  }
  class Users_Lambdas {
    <<module>>
    +get_auth0_user_by_email(email)
    +remove_user(connector, org, email)
    +create_users(connector, users, role_dict, allowed_ids, allowed_names, roles, headers, org, actor_id, dry_run, solutions, org_type)
    +reverse_roles(roles)
  }
  ShipmentDatabase --> IAM_DB : provides cursor to
  ShipmentDatabase --> Users_Lambdas : passed to remove_user/create_users
  IAM_DB --> Users_Lambdas : provides roles/role_dict/allowed_roles
  Role <-- Users_Lambdas : role IDs used by create_users
  Users_Lambdas ..> ShipmentDatabase : uses for DB ops
  note right of Users_Lambdas : create_users called twice (dry_run true/false)
```

> SVG rendering failed for this diagram.
