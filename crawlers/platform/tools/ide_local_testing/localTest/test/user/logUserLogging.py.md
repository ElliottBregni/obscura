# Diagram: tools/ide_local_testing/localTest/test/user/logUserLogging.py


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
flowchart LR
  Env[Set os.environ: SERVICE, LOG_LEVEL, SPECIFIED_ENV]
  Event[HTTP Event: POST /iam/login-audit]
  DictObj[ fv.utilities.DictionaryToObject({"function_name":"post_login_audit"}) ]
  PostAudit[iam_service.v1.lambdas.logins.post_login_audit.lambda_handler]
  Print[print(blah) → stdout]

  Event -->|event, context| PostAudit
  DictObj -->|context-like obj| PostAudit
  Env -->|process env| PostAudit
  PostAudit -->|returns| Print
```

> SVG rendering failed for this diagram.
