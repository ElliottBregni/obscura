# Diagram: tools/ide_local_testing/localTest/test/user/syncUser.py


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
flowchart LR
  Start([Start]) --> Imports[/"Import modules:\niam_service.v1.lambdas.users.sync_user\nfv.db.ShipmentDatabase\nlocalTest.core.get_user_id_by_email"/]
  Imports --> LoadEnv{Load env vars}
  LoadEnv -->|AUTH0_CONNECTION\nAUTH0_INITIAL_USER_PASSWORD| Setup[/"AUTH0_* variables set\nfrom os.environ"/]
  Setup --> DefineEmailList[/"Define emailList (tuple)\nmultiple reassigments\nfinal value: 3 emails"/]
  DefineEmailList --> InitCount[/"count = 0"/]
  InitCount --> ForEach[/For each email in emailList/]
  ForEach --> Increment[/"count = count + 1\nprint count and email"/]
  Increment --> GetUser[get_user_id_by_email(email)]
  GetUser --> CheckUser{user_id is not None?}
  CheckUser -->|yes| CreateDB[/"ShipmentDatabase(\"syncUser.test\")"/]
  CreateDB --> Sync[/"sync_user(ShipmentDatabase, user_id)\nprint result"/]
  CheckUser -->|no| NotFound[/print "Couldn't find user"/]
  Sync --> ForEach
  NotFound --> ForEach
  ForEach --> End([End])
```

> SVG rendering failed for this diagram.
