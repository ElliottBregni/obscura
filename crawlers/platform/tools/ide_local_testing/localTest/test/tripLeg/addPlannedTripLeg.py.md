# Diagram: tools/ide_local_testing/localTest/test/tripLeg/addPlannedTripLeg.py


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
flowchart LR
  A[Start] --> B[Set AWS_STAGE = "dev"]
  B --> C[Import modules]
  C --> C1[entity_service.trip_leg.trip_leg.post_put_planned_trip_leg]
  C --> C2[localTest.core]
  C --> D[Build body (shipmentId, vin, origin, destination, references)]
  D --> E[get_event(body, method=POST, resource=...planned-trip-leg, pathParameters)]
  E --> F[Define lambda_handler() (pass)]
  F --> G[print AWS_STAGE]
  G --> H[Call post_put_planned_trip_leg.lambda_handler(event, DictionaryToObject({"function_name":"addPlannedTripLeg"}))]
  H --> I[print retval]
  I --> Z[End]
```

> SVG rendering failed for this diagram.
