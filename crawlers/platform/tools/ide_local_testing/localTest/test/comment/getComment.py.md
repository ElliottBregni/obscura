# Diagram: tools/ide_local_testing/localTest/test/comment/getComment.py


> Auto-generated by Obscura crawlers

## Diagram 1

```mermaid
classDiagram
    class Authorizer {
        +setUsername(username)
        +setPassword(password)
        +syncFromAuth0()
        +setOrganizationId(orgId)
    }
    class CanonicalEvent {
        +setAuthorizer(authorizer)
        +addQueryStringParameter(key, value)
        +addPathParameter(key, value)
        +setBody(body)
        +toAwsEvent()
    }
    class DictionaryToObject {
        +__init__(dict)
    }
    class lambda_handler {
        +(event, context)
    }
    Authorizer <|-- CanonicalEvent : uses
    CanonicalEvent --> lambda_handler : produces event passed to
    DictionaryToObject --> lambda_handler : context passed to
    lambda_handler ..> "comment_service.comment_list" : defined in
```

> SVG rendering failed for this diagram.

## Diagram 2

```mermaid
flowchart TD
    Start([Start]) --> CreateAuth[Create Authorizer instance]
    CreateAuth --> ConfigureAuth[Configure: setUsername, setPassword]
    ConfigureAuth --> SyncAuth[syncFromAuth0() and setOrganizationId]
    SyncAuth --> CreateEvent[Create CanonicalEvent and setAuthorizer]
    CreateEvent --> AddQuery[addQueryStringParameter (reference_id, system_name, reference_type, pageNumber, pageSize)]
    AddQuery --> AddPath[addPathParameter(owner_id)]
    AddPath --> SetBody[setBody({})]
    SetBody --> ToAws[toAwsEvent() -> AWS event]
    ToAws --> Invoke[Invoke lambda_handler(event, DictionaryToObject(...))]
    Invoke --> Print[print(retval)]
    Print --> End([End])
```

> SVG rendering failed for this diagram.
