# Diagram: tools/ide_local_testing/localTest/test/partview/filterSearch/getOrderNumberList.py


> Auto-generated by Obscura crawlers

## Diagram 1

```mermaid
flowchart TD
    A[Start: Script] --> B[Authorizer().setUsername(...)]
    B --> C[Authorizer.syncFromAuth0()]
    C --> D[CanonicalEvent().setBody(None)]
    D --> E[.setAuthorizer(authorizer)]
    E --> F[.setHttpMethod("GET")]
    F --> G[.setPathParameters({"filterName":"order-number"})]
    G --> H[.prepare().toAwsEvent()]
    H --> I[DictionaryToObject({'function_name':"searchPartviewFilterList"})]
    H --> J[filterList.lambda_handler(event, context)]
    J --> K[print(result)]
    K --> L[End]
```

> SVG rendering failed for this diagram.

## Diagram 2

```mermaid
classDiagram
    class Authorizer {
        +setUsername(username)
        +syncFromAuth0()
    }
    class CanonicalEvent {
        +setBody(body)
        +setAuthorizer(authorizer)
        +setHttpMethod(method)
        +setPathParameters(params)
        +prepare()
        +toAwsEvent()
    }
    class DictionaryToObject {
        +__init__(dict)
    }
    class filterList {
        +lambda_handler(event, context)
    }
    Authorizer <|-- CanonicalEvent : uses
    DictionaryToObject <-- filterList : context
    CanonicalEvent --> filterList : event passed to
    filterList --> "Result" : returns
    filterList ..> print : output used by
```

> SVG rendering failed for this diagram.
