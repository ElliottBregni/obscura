# Diagram: tools/ide_local_testing/localTest/test/partview/container/searchContainer.py


> Auto-generated by Obscura crawlers

## Diagram 1

```mermaid
flowchart TD
  A[Authorizer()] --> B[setUsername("shipper-org-admin@yopmail.com")]
  B --> C[syncFromAuth0()]
  C --> D[authorizer variable]
  E[Query Parameters & Headers] --> F[CanonicalEvent()]
  D --> F
  F --> G[setAuthorizer(authorizer)]
  G --> H[setHttpMethod("GET")]
  H --> I[setQueryStringParameters(queryParameters)]
  I --> J[prepare()]
  J --> K[toAwsEvent()]
  K --> L[event dict with headers]
  L --> M[modify event.headers['Accept']]
  M --> N[searchContainer.lambda_handler(event, DictionaryToObject(...))]
  N --> O[print(result)]
```

> SVG rendering failed for this diagram.

## Diagram 2

```mermaid
sequenceDiagram
  participant U as Authorizer
  participant C as CanonicalEvent
  participant D as DictionaryToObject
  participant S as searchContainer
  participant P as print
  U->>U: setUsername("shipper-org-admin@yopmail.com")
  U->>U: syncFromAuth0()
  U->>C: setAuthorizer(authorizer)
  C->>C: setHttpMethod("GET")
  C->>C: setQueryStringParameters(queryParameters)
  C->>C: prepare()
  C->>C: toAwsEvent()
  C->>C: event created
  Note right of C: event['headers'] updated\nAccept -> 'application/json; version=DETAILS, text/plain, */*'
  C->>S: lambda_handler(event, D({'function_name': 'searchPackageContainer'}))
  S-->>P: result
  P-->>P: print(result)
```

> SVG rendering failed for this diagram.
