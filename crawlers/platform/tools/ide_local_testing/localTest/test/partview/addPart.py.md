# Diagram: tools/ide_local_testing/localTest/test/partview/addPart.py


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
flowchart TD
  A[Start] --> B[Imports: datetime, json, uuid, addPart, DictionaryToObject, Authorizer, CanonicalEvent]
  B --> C[creatorShipmentId = uuid.uuid4() -> str]
  C --> D[newTripleg dict\n(OWNER_ORG, CARRIER_ORG,\nCREATOR_SHIPMENT_ID, DEST, ORIG, STATUS)]
  D --> E[authorizer = Authorizer()\n.setUsername(...)\n.setOrganizationId(18)\n.setOrganizationProfiles(["SH"])]
  E --> F[event = CanonicalEvent()\n.setHttpMethod("POST")\n.setAuthorizer(authorizer)\n.setResourcePath("/partview/api/tripleg")\n.setBody(newTripleg)\n.setPathParameters({})\n.setQueryStringParameters({})\n.prepare()\n.toAwsEvent()]
  F --> G[jsonvent = json.dumps(event)]
  G --> H[print(current datetime) # before call]
  H --> I[retval = addTripleg(event, DictionaryToObject({"function_name":"getTriplegByLambda"}))]
  I --> J[print(current datetime) # after call]
  J --> K[print(retval)]
  K --> L[End]
```

> SVG rendering failed for this diagram.
