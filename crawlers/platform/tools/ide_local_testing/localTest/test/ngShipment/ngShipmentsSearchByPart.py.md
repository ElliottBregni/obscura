# Diagram: tools/ide_local_testing/localTest/test/ngShipment/ngShipmentsSearchByPart.py


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
flowchart TD
    A[Start] --> B[Create Authorizer]
    B --> C[Authorizer.setUsername(...).syncFromAuth0()]
    C --> D[Authorizer.setOrganizationId(18)]
    D --> E[Create CanonicalEvent]
    E --> F[CanonicalEvent.setAuthorizer(authorizer)]
    F --> G[CanonicalEvent.setHttpMethod("GET")]
    G --> H[CanonicalEvent.setBody(body)]
    H --> I[CanonicalEvent.parseUri(uri)]
    I --> J[CanonicalEvent.prepare()]
    J --> K[CanonicalEvent.toAwsEvent()]
    K --> L[ngGetShipments.lambda_handler(event, {"function_name":"ngGetShipments"})]
    L --> M{retval and retval.body?}
    M -->|Yes| N[body = json.loads(retval.body)]
    N --> O[prettyRetval = json.dumps(body, indent=2, sort_keys=True)]
    M -->|No| P[prettyRetval = ""]
    O --> Q[print(prettyRetval)]
    P --> Q
    Q --> R[End]
```

> SVG rendering failed for this diagram.
