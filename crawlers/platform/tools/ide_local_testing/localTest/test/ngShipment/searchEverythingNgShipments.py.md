# Diagram: tools/ide_local_testing/localTest/test/ngShipment/searchEverythingNgShipments.py


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
flowchart TD
  A[Start] --> B[Set URI string]
  B --> C[Create Authorizer]
  C --> C1[setUsername("shipper-org-admin@yopmail.com")]
  C1 --> C2[syncFromAuth0()]
  C2 --> D[Create CanonicalEvent]
  D --> D1[setAuthorizer(authorizer)]
  D1 --> D2[setHttpMethod("GET")]
  D2 --> D3[parseUri(uri)]
  D3 --> D4[prepare()]
  D4 --> D5[toAwsEvent()]
  D5 --> E[Create event object]
  E --> F[Call ngGetShipments.lambda_handler(event, DictionaryToObject(...))]
  F --> G{retval and retval.body?}
  G -->|yes| H[json.loads(retval.body) -> body]
  H --> I[prettyRetval = json.dumps(body, indent=2, sort_keys=True)]
  G -->|no| J[prettyRetval = ""]
  I --> K[print(prettyRetval)]
  J --> K
  K --> Z[End]
```

> SVG rendering failed for this diagram.
