# Diagram: tools/ide_local_testing/localTest/test/ngShipment/getNgShipments.py


> Auto-generated by Obscura crawlers

## Diagram 1

```mermaid
flowchart TD
    Start[Start script] --> CreateAuth[Create Authorizer]
    CreateAuth --> SyncAuth[.syncFromAuth0()]
    SyncAuth --> SetOrg[.setOrganizationId(1044)]
    SetAuthUsername[Set username] --> CreateAuth
    CreateAuth --> AuthReady[Authorizer ready]

    BuildEvent[Build CanonicalEvent] --> SetAuthorizer[.setAuthorizer(authorizer)]
    SetAuthorizer --> SetHttpMethod[.setHttpMethod("GET")]
    SetHttpMethod --> ParseUri[.parseUri(uri)]
    ParseUri --> Prepare[.prepare()]
    Prepare --> ToAws[.toAwsEvent()]
    ToAws --> EventReady[Event ready]

    AuthReady --> BuildEvent
    EventReady --> InvokeLambda[Call ngGetShipments.lambda_handler(event, context)]
    InvokeLambda --> CheckRetval{retval and retval.body?}
    CheckRetval -->|yes| ParseBody[json.loads(retval["body"])]
    ParseBody --> Pretty[json.dumps(body, indent=2, sort_keys=True)]
    CheckRetval -->|no| Empty[prettyRetval = ""]
    Pretty --> Print[print(prettyRetval)]
    Empty --> Print
```

> SVG rendering failed for this diagram.

## Diagram 2

```mermaid
classDiagram
    class Authorizer {
        +setUsername(username)
        +syncFromAuth0()
        +setOrganizationId(orgId)
    }
    class CanonicalEvent {
        +setAuthorizer(authorizer)
        +setHttpMethod(method)
        +parseUri(uri)
        +prepare()
        +toAwsEvent()
    }
    class DictionaryToObject {
        +__init__(dict)
    }
    class ngGetShipments {
        +lambda_handler(event, context)
    }
    class json {
        <<module>>
        +loads(s)
        +dumps(obj, indent, sort_keys)
    }
    Authorizer <|-- CanonicalEvent : uses
    CanonicalEvent --> ngGetShipments : provides event
    DictionaryToObject --> ngGetShipments : provides context
    ngGetShipments --> json : returns body (string)
    json --> Script : used by script
    Script[Script - main module] --> Authorizer
    Script --> CanonicalEvent
    Script --> ngGetShipments
```

> SVG rendering failed for this diagram.
