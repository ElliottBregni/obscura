# Diagram: tools/ide_local_testing/localTest/test/byUrl/searchEntityEvent.py


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
flowchart TD
  Start([Start]) --> Init{{"Initialize variables"}}
  Init --> EventObj["Build 'event' dict"]
  EventObj --> CallLambda["Call searchEvent(event, DictionaryToObject(...))"]
  CallLambda -->|returns retval| CheckRetval{"retval and retval.body?"}
  CheckRetval -->|yes| LoadBody["body = json.loads(retval.get('body'))"]
  LoadBody --> Pretty["prettyRetval = json.dumps(body, indent=2, sort_keys=True)"]
  CheckRetval -->|no| PrettyEmpty["prettyRetval = \"\""]
  Pretty --> PrintPretty["print(prettyRetval)"]
  PrettyEmpty --> PrintPretty
  CallLambda --> TimeEnd["end = time.time()"]
  Init --> TimeStart["start = time.time()"]
  TimeStart --> CallLambda
  TimeEnd --> CalcDuration["Lambda execution time = end - start"]
  CalcDuration --> PrintTime["print(f\"Lambda execution time: {end - start} seconds.\")"]
  PrintPretty --> PrintTime
  PrintTime --> End([End])
```

> SVG rendering failed for this diagram.
