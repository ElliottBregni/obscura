# Diagram: tools/ide_local_testing/localTest/test/byUrl/filterServiceGetFilterList.py


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
flowchart TD
    Start([Start]) --> A[Create Authorizer]
    A --> B[Authorizer.setUsername("dave.damon@...")]
    B --> C[Authorizer.syncFromAuth0()]
    C --> D{activeOrgId?}
    D -- yes --> E[Authorizer.setOrganizationId(activeOrgId)]
    D -- no --> F[skip setOrganizationId]
    E --> G[Create CanonicalEvent]
    F --> G
    G --> H[Event.setAuthorizer(Authorizer)]
    H --> I[Event.setHttpMethod("GET")]
    I --> J[Event.parseUri(uri)]
    J --> K[Event.setAcceptHeader("application/json")]
    K --> L[Event.setPathParameters({...})]
    L --> M[Event.prepare()]
    M --> N[Event.toAwsEvent()]
    N --> O[Invoke lambdaHandler(event, config)]
    O --> P{retval and retval.body?}
    P -- yes --> Q[body = json.loads(retval.body)]
    Q --> R[prettyRetval = json.dumps(body, indent=2, sort_keys=True)]
    P -- no --> S[prettyRetval = ""]
    R --> T[print(prettyRetval)]
    S --> T
    T --> End([End])
```

> SVG rendering failed for this diagram.
