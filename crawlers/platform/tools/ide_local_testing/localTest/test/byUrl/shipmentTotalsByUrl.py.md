# Diagram: tools/ide_local_testing/localTest/test/byUrl/shipmentTotalsByUrl.py


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
flowchart LR
    A[Start: script execution] --> B[Imports: json, time]
    B --> C[Imports from localTest.core]
    C --> C1[DictionaryToObject]
    C --> C2[get_event]
    C --> D[Imports from localTest.core.CanonicalEvent]
    D --> E[CanonicalEvent instance]
    C --> F[Imports from localTest.core.Authorizer]
    F --> G[Authorizer instance]
    B --> H[Import lambda_handler]
    H --> I[lambdaHandler (ngShipmentTotalsByURL)]
    A --> J[Configuration]
    J --> J1[acceptType = application/json]
    J --> J2[activeOrgId = 46771]
    J --> J3[organizationProfiles = ["SP"]]
    J --> K[uri = "https://.../shipments/totals"]
    G --> L[Authorizer.setUsername("dave.damon@freightverify.com")]
    L --> M[Authorizer.syncFromAuth0()]
    M --> N{activeOrgId present?}
    N -- yes --> O[Authorizer.setOrganizationId(46771)]
    O --> P[Authorizer.setOrganizationProfiles(["SP"])]
    E --> Q[CanonicalEvent.setAuthorizer(authorizer)]
    Q --> R[.setHttpMethod("GET")]
    R --> S[.setAcceptHeader(acceptType)]
    S --> T[.parseUri(uri)]
    T --> U[.prepare()]
    U --> V[.toAwsEvent() -> event]
    A --> W[start = time.time()]
    W --> X[retval = lambdaHandler(event, DictionaryToObject({"function_name":"ngShipmentTotalsByURL"}))]
    X --> Y[end = time.time()]
    Y --> Z{retval and retval.body?}
    Z -- yes --> ZA[body = json.loads(retval.body)]
    ZA --> ZB[prettyRetval = json.dumps(body, indent=2, sort_keys=True)]
    Z -- no --> ZC[prettyRetval = ""]
    ZB --> ZD[print(prettyRetval)]
    ZC --> ZD
    Y --> ZE[print("Lambda execution time: end - start seconds")]
    ZD --> ZF[End]
```

> SVG rendering failed for this diagram.
