# Diagram: tools/ide_local_testing/localTest/test/byUrl/filterResultConsumer.py


> Auto-generated by Obscura crawlers

## Diagram 1

```mermaid
classDiagram
class DictionaryToObject {
  +__init__(dict)
  +to_object()
}
class Authorizer {
  +authorize()
}
class CanonicalEvent {
  +normalize()
}
class lambda_handler_consumer {
  +__call__(event, context)
  +handler(event, context)
}
class event {
  +body: string (JSON)
  +headers: dict
}
DictionaryToObject <|-- lambda_handler_consumer : uses
lambda_handler_consumer ..> Authorizer : imports
lambda_handler_consumer ..> CanonicalEvent : imports
lambda_handler_consumer --> event : consumes
event o-- "complex JSON body" : contains
```

> SVG rendering failed for this diagram.

## Diagram 2

```mermaid
flowchart LR
  subgraph Input
    E[event JSON body]
    H[headers]
  end
  E -->|passed as event| Handler[lambda_handler_consumer.handler]
  H --> Handler
  Context[DictionaryToObject(context dict)] --> Handler
  Handler -->|calls| Authorizer[Authorizer]
  Handler -->|may create| CanonicalEvent[CanonicalEvent]
  Handler --> Solution[solution (print output)]
  Solution -->|stdout| Console(Console)
```

> SVG rendering failed for this diagram.
