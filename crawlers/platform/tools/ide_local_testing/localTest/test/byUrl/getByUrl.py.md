# Diagram: tools/ide_local_testing/localTest/test/byUrl/getByUrl.py


> Auto-generated by Obscura crawlers

## Diagram 1

```mermaid
flowchart LR
  A[Start script] --> B[Create Authorizer]
  B --> C{setUsername & syncFromAuth0}
  C --> D[setOrganizationId if activeOrgId]
  D --> E[Create CanonicalEvent]
  E --> E1[setAuthorizer]
  E1 --> E2[setHttpMethod("GET")]
  E2 --> E3[setAcceptHeader("application/json")]
  E3 --> E4[parseUri(uri)]
  E4 --> E5[prepare()]
  E5 --> F[toAwsEvent()]
  F --> G[Create event object]
  G --> H[Call lambda_handler(event, context)]
  H --> I{retval present?}
  I -->|yes| J[json.loads(retval["body"])]
  J --> K[prettyRetval = json.dumps(...)]
  I -->|no| L[prettyRetval = ""]
  K --> M[print(prettyRetval)]
  L --> M
```

> SVG rendering failed for this diagram.

## Diagram 2

```mermaid
classDiagram
  class Authorizer {
    +setUsername(username)
    +syncFromAuth0()
    +setOrganizationId(orgId)
  }
  class CanonicalEvent {
    +setAuthorizer(authorizer)
    +setHttpMethod(method)
    +setAcceptHeader(header)
    +parseUri(uri)
    +prepare()
    +toAwsEvent()
  }
  class DictionaryToObject {
    +__init__(dict)
  }
  class lambda_handler {
    +lambda_handler(event, context)
  }
  Authorizer --> CanonicalEvent : used by
  CanonicalEvent --> lambda_handler : produces event for
  DictionaryToObject --> lambda_handler : provides context
  lambda_handler --> "JSON body" : returns (body)
```

> SVG rendering failed for this diagram.
