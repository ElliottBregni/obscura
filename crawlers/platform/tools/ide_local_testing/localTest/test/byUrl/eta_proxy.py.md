# Diagram: tools/ide_local_testing/localTest/test/byUrl/eta_proxy.py


> Auto-generated by Obscura crawlers

## Diagram 1

```mermaid
classDiagram
class Script {
  +string acceptType
  +string shipmentId
  +dict body
  +float start
  +float end
  +dict retval
  +string prettyRetval
  +print()
}
class DictionaryToObject
class CanonicalEvent
class Authorizer
class eta_proxy::lambda_handler

Script ..> DictionaryToObject : imports
Script ..> CanonicalEvent : imports
Script ..> Authorizer : imports
Script ..> eta_proxy::lambda_handler : calls
DictionaryToObject <|-- Script : constructs
eta_proxy::lambda_handler ..> CanonicalEvent : may use
eta_proxy::lambda_handler ..> Authorizer : may use
```

> SVG rendering failed for this diagram.

## Diagram 2

```mermaid
flowchart LR
  A[Start script] --> B[Prepare body and requestContext]
  B --> C[Record start time]
  C --> D[Call lambda_handler(body, contextObj)]
  D --> E{retval and retval.body?}
  E -- Yes --> F[Parse JSON body into dict]
  F --> G[Pretty-print retval]
  E -- No --> H[Set prettyRetval empty]
  G --> I[Print prettyRetval]
  H --> I
  I --> J[Print Lambda execution time]
  J --> K[End]
```

> SVG rendering failed for this diagram.
