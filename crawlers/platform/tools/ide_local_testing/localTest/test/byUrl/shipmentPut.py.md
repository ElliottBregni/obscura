# Diagram: tools/ide_local_testing/localTest/test/byUrl/shipmentPut.py


> Auto-generated by Obscura crawlers

## Diagram 1

```mermaid
classDiagram
    class Authorizer {
        +setUsername(username)
        +syncFromAuth0()
        +setOrganizationId(id)
        +setOrganizationProfiles(profiles)
        +setFeatures(features)
    }
    class CanonicalEvent {
        +setAuthorizer(auth)
        +setHttpMethod(method)
        +setAcceptHeader(type)
        +parseUri(uri)
        +addPathParameter(key, val)
        +setBody(body)
        +prepare()
        +toAwsEvent()
    }
    class DictionaryToObject {
        +__init__(dict)
    }
    class handler {
        +(event, context)
    }
    Authorizer <|-- CanonicalEvent : uses
    DictionaryToObject --> handler : context
    CanonicalEvent --> handler : event
    handler --> "JSON body" : returns
```

> SVG rendering failed for this diagram.

## Diagram 2

```mermaid
flowchart TD
    A[Start script] --> B[Build body dict with stops & refs]
    B --> C[Authorizer created and configured]
    C --> D[CanonicalEvent constructed and prepared]
    D --> E[DictionaryToObject(context) created]
    E --> F[Invoke handler(event, context)]
    F --> G{retval}
    G -->|has body| H[parse JSON body -> prettyRetval]
    G -->|no body| I[prettyRetval = ""]
    H --> J[print prettyRetval]
    I --> J
    J --> K[print Lambda execution time]
    K --> L[End]
```

> SVG rendering failed for this diagram.
