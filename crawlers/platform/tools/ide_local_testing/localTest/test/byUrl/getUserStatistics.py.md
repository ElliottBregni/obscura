# Diagram: tools/ide_local_testing/localTest/test/byUrl/getUserStatistics.py


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
flowchart TD
  Start([Start]) --> ImportModules[/"Import modules: json, time, DictionaryToObject, Authorizer, CanonicalEvent, lambdaHandler"/]
  ImportModules --> CreateAuthorizer[/"Authorizer().setUsername(\"dave.damon@freightverify.com\").syncFromAuth0()"/]
  CreateAuthorizer --> ConfigureAuthorizer{activeOrgId set?}
  ConfigureAuthorizer -->|yes| SetOrg[/"setOrganizationId(1003), setOrganizationProfiles([...]), setPrivileges([...])"/]
  ConfigureAuthorizer -->|no| SkipOrg[/"skip org config"/]
  SetOrg --> Feature[/"featureName = \"PartView\" and uri constructed"/]
  SkipOrg --> Feature
  Feature --> BuildEvent[/"CanonicalEvent(): setAuthorizer -> setHttpMethod('POST') -> setAcceptHeader -> parseUri(uri) -> prepare() -> toAwsEvent()"/]
  BuildEvent --> CallLambda[/"lambdaHandler(event, DictionaryToObject({'function_name':'v2-post-put-shipment'}))"/]
  CallLambda --> CaptureTime1[/"record start and end times (time.time)"/]
  CaptureTime1 --> CheckRetval{retval and retval.get(\"body\")?}
  CheckRetval -->|true| ParseBody[/"body = json.loads(retval.get('body')) -> prettyRetval = json.dumps(body, indent=2, sort_keys=True)"/]
  CheckRetval -->|false| EmptyBody[/"prettyRetval = \"\""/]
  ParseBody --> PrintRetval[/"print(prettyRetval)"/]
  EmptyBody --> PrintRetval
  PrintRetval --> PrintTime[/"print Lambda execution time: end - start"/]
  PrintTime --> End([End])
```

> SVG rendering failed for this diagram.
