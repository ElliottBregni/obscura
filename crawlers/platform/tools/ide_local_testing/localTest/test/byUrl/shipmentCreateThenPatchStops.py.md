# Diagram: tools/ide_local_testing/localTest/test/byUrl/shipmentCreateThenPatchStops.py


> Auto-generated by Obscura crawlers

## Diagram 1

```mermaid
classDiagram
    class RandomString {
        +static randomAlpaNumericString(prefix, suffix, length)
    }
    class Authorizer {
        +setUsername(username)
        +syncFromAuth0()
        +setActorType(type)
        +setOrganizationId(id)
        +setOrganizationProfiles(profiles)
        +setPrivileges(privs)
    }
    class CanonicalEvent {
        +setAuthorizer(authorizer)
        +setHttpMethod(method)
        +setAcceptHeader(header)
        +parseUri(uri)
        +setBody(body)
        +prepare()
        +toAwsEvent()
    }
    class DictionaryToObject {
        +Constructor(dict)
    }
    class lambda_handler {
        +(event, context)
    }
    RandomString --> createBody : used to generate shipment id
    Authorizer <|-- CanonicalEvent : authorizer passed in
    DictionaryToObject <|-- lambda_handler : context object passed
    CanonicalEvent --> "AWS Event" : prepare().toAwsEvent()
    "AWS Event" --> lambda_handler : invoked
```

> SVG rendering failed for this diagram.

## Diagram 2

```mermaid
graph TD
    RS[RandomString.randomAlpaNumericString] --> CID[creatorShipmentId]
    CID --> CreateBody["createBody (tender, header, stops)"]
    AuthorizerObj[Authorizer instance] --> CanonicalCreate[CanonicalEvent (POST)]
    CanonicalCreate --> AwsEventCreate[AWS event]
    AwsEventCreate --> LambdaCreate[lambda_handler POST call]
    LambdaCreate --> RetValCreate[retval (body/json)]
    RetValCreate --> PrintCreate[print prettyRetval]
    CanonicalUpdate[CanonicalEvent (PUT)] <-- AuthorizerObj
    CanonicalUpdate --> AwsEventUpdate[AWS event]
    AwsEventUpdate --> LambdaUpdate[lambda_handler PUT call]
    LambdaUpdate --> RetValUpdate[retval (body/json)]
    RetValUpdate --> PrintUpdate[print prettyRetval]
    Timing[time.time start/end] --> LambdaCreate
    Timing --> LambdaUpdate
```

> SVG rendering failed for this diagram.
