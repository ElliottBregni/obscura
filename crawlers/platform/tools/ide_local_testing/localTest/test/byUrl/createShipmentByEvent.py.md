# Diagram: tools/ide_local_testing/localTest/test/byUrl/createShipmentByEvent.py


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
flowchart LR
  subgraph Input
    E[event (HTTP PUT payload)]
  end

  subgraph Prep
    DTO[DictionaryToObject({"function_name":"v2-post-put-shipment"})]
  end

  subgraph Services
    LH[lambda_handler (shipment_service.shipments.create_shipments)]
    Auth[Authorizer]
    CE[CanonicalEvent]
  end

  subgraph Execution
    StartTime["start = time.time()"]
    Invoke[Invoke lambda_handler(event, DTO)]
    EndTime["end = time.time()"]
    RetVal[retval]
    HasBody{retval and retval.body?}
    ParseBody["json.loads(retval.body)\nprettyRetval = json.dumps(... )"]
    Print["print(prettyRetval)\nprint(execution time)"]
  end

  E --> Invoke
  DTO --> Invoke
  Invoke --> LH
  LH --> Auth
  LH --> CE
  StartTime --> Invoke
  Invoke --> RetVal
  RetVal --> HasBody
  HasBody -->|yes| ParseBody --> Print
  HasBody -->|no| Print
  Invoke --> EndTime
  EndTime --> Print
```

> SVG rendering failed for this diagram.
