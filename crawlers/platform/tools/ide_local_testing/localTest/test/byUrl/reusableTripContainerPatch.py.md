# Diagram: tools/ide_local_testing/localTest/test/byUrl/reusableTripContainerPatch.py


> Auto-generated by Obscura crawlers

## Diagram 1

```mermaid
classDiagram
    class DictionaryToObject {
        +__init__(dict)
    }
    class CanonicalEvent {
        +setAuthorizer(a)
        +setHttpMethod(m)
        +setAcceptHeader(h)
        +parseUri(u)
        +addPathParameter(k,v)
        +setBody(b)
        +prepare()
        +toAwsEvent()
    }
    class Authorizer {
        +setUsername(u)
        +syncFromAuth0()
        +setOrganizationId(id)
        +setOrganizationProfiles(list)
        +setFeatures(list)
    }
    class lambda_handler {
        <<function>>
        +(event, context)
    }
    DictionaryToObject --> CanonicalEvent : used to build context
    Authorizer --> CanonicalEvent : injected via setAuthorizer()
    CanonicalEvent --> lambda_handler : toAwsEvent() -> event
    lambda_handler --> JSON : returns { "body": str }
    JSON <.. prettyRetval : parsed into
    lambda_handler ..> DictionaryToObject : function_name metadata
```

> SVG rendering failed for this diagram.

## Diagram 2

```mermaid
flowchart TD
    A[Start script] --> B[Create Authorizer]
    B --> C[Set username & syncFromAuth0]
    C --> D[Set organization, profiles, features]
    D --> E[Create CanonicalEvent]
    E --> F[Configure event (method, headers, uri, params, body)]
    F --> G[Prepare and convert to AWS event]
    G --> H[Call lambda_handler(event, context)]
    H --> I{retval has "body"?}
    I -->|yes| J[Parse JSON body]
    I -->|no| K[Set prettyRetval empty]
    J --> L[Pretty-print JSON]
    K --> L
    L --> M[Print prettyRetval]
    M --> N[End]
```

> SVG rendering failed for this diagram.
