# Diagram: tools/ide_local_testing/localTest/test/byUrl/shipmentStopLocationUpdate.py


> Auto-generated by Obscura crawlers

## Diagram 1

```mermaid
classDiagram
    class Authorizer {
        +setUsername(username)
        +syncFromAuth0()
        +setOrganizationId(orgId)
        +setOrganizationProfiles(profiles)
        +setFeatures(features)
    }
    class CanonicalEvent {
        +setAuthorizer(authorizer)
        +setHttpMethod(method)
        +setAcceptHeader(accept)
        +parseUri(uri)
        +setBody(body)
        +setPathParameters(params)
        +prepare()
        +toAwsEvent()
    }
    class DictionaryToObject {
        +__init__(dict)
    }
    class handler {
        +lambda_handler(event, context)
    }
    Authorizer --> CanonicalEvent : used by
    DictionaryToObject --> handler : context wrapper passed to
    CanonicalEvent --> handler : produces event for
    handler ..> CanonicalEvent : consumes event
    Authorizer : attributes: username, organizationId, profiles, features
```

> SVG rendering failed for this diagram.

## Diagram 2

```mermaid
flowchart TD
    A[Start script] --> B[Create Authorizer]
    B --> C[setUsername & syncFromAuth0]
    C --> D{activeOrgId?}
    D -- yes --> E[setOrganizationId & setOrganizationProfiles]
    D --> F[setFeatures]
    E --> F
    F --> G[Create CanonicalEvent]
    G --> H[Chain setters: setAuthorizer,setHttpMethod,setAcceptHeader,parseUri,setBody,setPathParameters,prepare,toAwsEvent]
    H --> I[Create DictionaryToObject(context)]
    I --> J[Call handler(lambda_handler) with event and context]
    J --> K[Receive retval]
    K --> L{retval contains body?}
    L -- yes --> M[json.loads(retval.body) -> prettyRetval -> print]
    L -- no --> N[prettyRetval = ""]
    M --> O[Print execution time]
    N --> O
    O --> P[End]
```

> SVG rendering failed for this diagram.
