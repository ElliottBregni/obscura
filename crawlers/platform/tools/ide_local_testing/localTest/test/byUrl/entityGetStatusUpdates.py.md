# Diagram: tools/ide_local_testing/localTest/test/byUrl/entityGetStatusUpdates.py


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
flowchart TD
  Start([Start]) --> Vars[/"Define config: acceptType, activeOrgId, solutionId, organizationProfiles"/]
  Vars --> BuildURI{"Build URI\nf\"https://data.../status-update?startTime=...\""}
  BuildURI --> AuthInit[Authorizer()\n.setUsername(...)\n.syncFromAuth0()]
  AuthInit --> ActiveOrgCheck{activeOrgId ?}
  ActiveOrgCheck -->|yes| SetOrg[.setOrganizationId(activeOrgId)\n.setOrganizationProfiles(...)\n.setSolutions([solutionId])]
  ActiveOrgCheck -->|no| SkipOrg[skip org setup]
  SetOrg --> EventBuild
  SkipOrg --> EventBuild
  EventBuild[CanonicalEvent()\n.setAuthorizer(authorizer)\n.setHttpMethod(\"GET\")\n.setAcceptHeader(acceptType)\n.parseUri(uri)\n.addPathParameter(\"solution_id\", solutionId)\n.prepare()\n.toAwsEvent()] --> InvokeLambda
  InvokeLambda[Invoke lambdaHandler(event, context)] --> LambdaExec[Lambda execution returns retval]
  LambdaExec --> BodyCheck{retval && retval.body ?}
  BodyCheck -->|yes| ParseBody[Parse JSON body\nprettyRetval = json.dumps(body, ...)]
  BodyCheck -->|no| EmptyBody[prettyRetval = ""]
  ParseBody --> PrintOut[print(prettyRetval)]
  EmptyBody --> PrintOut
  PrintOut --> PrintTime[print("Lambda execution time: ... seconds")]
  PrintTime --> End([End])
```

> SVG rendering failed for this diagram.
