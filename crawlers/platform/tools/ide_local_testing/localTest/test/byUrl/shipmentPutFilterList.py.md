# Diagram: tools/ide_local_testing/localTest/test/byUrl/shipmentPutFilterList.py


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
flowchart LR
  A[Start script] --> B[Set constants and variables]
  B --> C[Build label via urllib.parse.quote]
  B --> D[Construct uri string]
  D --> E[Create body dict]
  C --> F[Instantiate Authorizer]
  F --> G[Authorizer.setUsername & syncFromAuth0]
  G --> H[If activeOrgId then setOrganizationId]
  H --> I[Instantiate CanonicalEvent]
  I --> J[Set authorizer, http method, accept header]
  J --> K[parseUri(uri)]
  K --> L[setPathParameters & setQueryStringParameters]
  L --> M[setBody & prepare & toAwsEvent]
  M --> N[Call lambdaHandler(event, config)]
  N --> O{retval and retval.body?}
  O -->|yes| P[Load JSON, pretty print to prettyRetval]
  O -->|no| Q[Set prettyRetval = ""]
  P --> R[print(prettyRetval)]
  Q --> R
  R --> S[End]
```

> SVG rendering failed for this diagram.
