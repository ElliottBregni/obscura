# Diagram: tools/ide_local_testing/localTest/test/byUrl/partviewContainerGetReferenceSet.py


> Auto-generated by Obscura crawlers

## Diagram 1

```mermaid
flowchart TD
    A[Start Script] --> B[Create Authorizer]
    B --> C[setUsername() & syncFromAuth0()]
    C --> D{activeOrgId?}
    D -- yes --> E[setOrganizationId()\nsetOrganizationProfiles()]
    E --> F[setFeatures()]
    D -- no --> F
    F --> G[Create CanonicalEvent]
    G --> H[setAuthorizer()\nsetHttpMethod()\nsetAcceptHeader()\nparseUri()\naddPathParameter()\naddQueryStringParameter()\nsetBody()\nprepare()\ntoAwsEvent()]
    H --> I[Invoke handler(event, context)]
    I --> J[Receive retval]
    J --> K{retval.body?}
    K -- yes --> L[json.loads(retval.body)\nprint(prettyRetval)]
    K -- no --> M[print empty prettyRetval]
    L --> N[Print execution time]
    M --> N
```

> SVG rendering failed for this diagram.

## Diagram 2

```mermaid
classDiagram
    class Authorizer {
        +setUsername(username)
        +syncFromAuth0()
        +setOrganizationId(orgId)
        +setOrganizationProfiles(profiles)
        +setFeatures(features)
    }
    class CanonicalEvent {
        +setAuthorizer(authorizer)
        +setHttpMethod(method)
        +setAcceptHeader(header)
        +parseUri(uri)
        +addPathParameter(key, value)
        +addQueryStringParameter(key, value)
        +setBody(body)
        +prepare()
        +toAwsEvent()
    }
    class DictionaryToObject {
        +__init__(dict)
    }
    class handler {
        <<function>>
        +handler(event, context)
    }
    Authorizer <|-- CanonicalEvent : uses
    DictionaryToObject --> handler : passed as context
    CanonicalEvent --> handler : event passed to
    handler ..> "AWS Lambda" : returns retval
```

> SVG rendering failed for this diagram.
