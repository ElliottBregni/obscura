# Diagram: tools/ide_local_testing/localTest/test/byUrl/shipmentCreateByProxy.py


> Auto-generated by Obscura crawlers

## Diagram 1

```mermaid
classDiagram
    class Authorizer {
        +setUsername(username)
        +syncFromAuth0()
        +setOrganizationId(id)
        +setOrganizationProfiles(profiles)
        +setFeatures(features)
    }
    class CanonicalEvent {
        +setAuthorizer(authorizer)
        +setHttpMethod(method)
        +setAcceptHeader(type)
        +addHeader(name, value)
        +parseUri(uri)
        +addPathParameter(name, value)
        +setBody(body)
        +prepare()
        +toAwsEvent()
    }
    class DictionaryToObject {
        <<utility>>
        +__init__(dict)
    }
    class handler {
        <<function>>
        +(event, context)
    }
    Authorizer <|-- CanonicalEvent : uses
    DictionaryToObject --|> handler : context
    CanonicalEvent --> handler : produces event
    handler --> "retval (dict)" : returns
```

> SVG rendering failed for this diagram.

## Diagram 2

```mermaid
flowchart TD
    subgraph Setup
        A[body (shipment JSON)] --> B[Authorizer instance]
        B --> C[Authorizer configured (username, orgId, profiles)]
    end
    subgraph Event_Build
        C --> D[CanonicalEvent builder]
        A --> D
        D --> E[event (AWS-format)]
        E --> F[DictionaryToObject(context)]
    end
    subgraph Execution
        E --> G[handler(event, context)]
        G --> H[retval]
    end
    subgraph Post
        H --> I{retval.body?}
        I -->|yes| J[parse JSON body -> prettyRetval]
        I -->|no| K[prettyRetval = ""]
        J --> L[print(prettyRetval)]
        K --> L
        L --> M[print(Lambda execution time)]
    end
```

> SVG rendering failed for this diagram.
