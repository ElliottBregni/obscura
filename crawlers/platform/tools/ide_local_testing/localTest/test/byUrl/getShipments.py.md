# Diagram: tools/ide_local_testing/localTest/test/byUrl/getShipments.py


> Auto-generated by Obscura crawlers

## Diagram 1

```mermaid
classDiagram
    class DictionaryToObject {
        <<utility>>
        +__init__(data)
    }
    class Authorizer {
        +setUsername(username)
        +syncFromAuth0()
        +setOrganizationId(orgId)
    }
    class CanonicalEvent {
        +setAuthorizer(authorizer)
        +setHttpMethod(method)
        +setAcceptHeader(header)
        +parseUri(uri)
        +prepare()
        +toAwsEvent()
    }
    class handler {
        <<function>>
        +lambda_handler(event, context)
    }
    DictionaryToObject --> handler : context arg
    Authorizer --> CanonicalEvent : assigned to
    CanonicalEvent --> handler : event (toAwsEvent())
    Authorizer ..> DictionaryToObject : used to build context
    handler --> "Print" : returns solution
```

> SVG rendering failed for this diagram.

## Diagram 2

```mermaid
flowchart TD
    A[Create Authorizer] --> B[setUsername("dave.damon@...")]
    B --> C[syncFromAuth0()]
    C --> D{activeOrgId present?}
    D -- yes --> E[setOrganizationId(activeOrgId)]
    D -- no --> F[skip org set]
    E --> G[Create CanonicalEvent]
    F --> G
    G --> H[setHttpMethod("GET")]
    H --> I[setAcceptHeader("application/json")]
    I --> J[parseUri(uri)]
    J --> K[prepare() and toAwsEvent()]
    K --> L[Call handler(event, DictionaryToObject({...}))]
    L --> M[print(solution)]
```

> SVG rendering failed for this diagram.
