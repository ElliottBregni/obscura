# Diagram: tools/ide_local_testing/localTest/test/shipment/createShipment.py


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
flowchart LR
  A[Script Start] --> B[Import modules\n(localTest.core, shipment_service,\nfv.db, os)]
  B --> C[Create ShipmentDatabase\nconnector = ShipmentDatabase("createUsers.test")]
  C --> D[Generate shipmentId\nRandomString.randomAlpaNumericString]
  D --> E[Build body\n(tender, stops, header, reference)]
  E --> F[Create Authorizer\nAuthorizer().setUsername(...)]
  F --> G[Set OrganizationId\nif activeOrgId -> setOrganizationId(1028)]
  G --> H[Create CanonicalEvent\n.setAuthorizer(...)\n.setBody(body)\n.setHttpMethod("POST")\n.prepare().toAwsEvent()]
  H --> I[Call lambdaHandler\nshipment_service.shipments.create_shipments.lambda_handler(event, context)]
  I --> J[Print retval]
  J --> K[Script End]
```

> SVG rendering failed for this diagram.
