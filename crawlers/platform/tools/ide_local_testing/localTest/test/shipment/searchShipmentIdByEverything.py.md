# Diagram: tools/ide_local_testing/localTest/test/shipment/searchShipmentIdByEverything.py


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
graph TD
    Start[Start script / module executed] --> GetEvent[get_event(body, resource, method, queryStringParameters)]
    GetEvent --> Event[event object]
    Solution[solutionId = "FV_TEST"] 
    Gateway[gatewayUrl = "data-d.freightverify.com"]
    Dictionary[DictionaryToObject({"function_name":"searchShipmentByIdEverything"})]
    Event --> CallNg[ng_get_shipments.lambda_handler(event, DictionaryToObject(...))]
    Dictionary --> CallNg
    CallNg --> Print[print(result)]
    LambdaStub[lambda_handler() defined (pass)] -->|no-op| End[End]
    Solution -.-> Start
    Gateway -.-> Start
    classDef noop fill:#f2f2f2,stroke:#cccccc,stroke-width:1px
    class LambdaStub noop
```

> SVG rendering failed for this diagram.
