# Diagram: tools/ide_local_testing/localTest/test/shipment/statusUpdatePositionUpdate.py


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
flowchart LR
  A[Start: script execution] --> B[Define uri, activeOrgId, shipmentId, body]
  B --> C[Authorizer().setUsername("shipper-org-admin@yopmail.com")]
  C --> D[Authorizer.syncFromAuth0()]
  D --> E[CanonicalEvent().setAuthorizer(authorizer)]
  E --> F[.parseUri(uri)]
  F --> G[.setHeaders({...})]
  G --> H[.setBody(body)]
  H --> I[.setHttpMethod("GET")]
  I --> J[.prepare()]
  J --> K[.toAwsEvent()]
  K --> L[json.dumps(event) -> jsonvent]
  K --> M[postShipmentStatus(event, context)]
  M --> N[retval returned]
  N --> O[print(retval)]
  style A fill:#f9f,stroke:#333,stroke-width:1px
  style M fill:#ffd,stroke:#333,stroke-width:1px
```

> SVG rendering failed for this diagram.
