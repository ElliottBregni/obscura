# Diagram: tools/ide_local_testing/localTest/test/shipment/getNgShipmentEventsViaLambda.py


> Auto-generated by Obscura crawlers

## Diagram 1

```mermaid
classDiagram
    class Script {
        +lambda_handler()
        +event
        +output
    }
    class DictionaryToObject <<module>>
    class fv_shipment_service.events.get_events <<module>>
    Script "1" o-- "1" DictionaryToObject : uses
    Script "1" o-- "1" fv_shipment_service.events.get_events : imports
    Script : calls getEvents(event, DictionaryToObject({...}))
    fv_shipment_service.events.get_events : lambda_handler(event, context)
    DictionaryToObject : converts dict -> object
```

> SVG rendering failed for this diagram.

## Diagram 2

```mermaid
flowchart TD
    A[Start script] --> B[Define lambda_handler]
    B --> C[Create event dict]
    C --> D[Import getEvents from fv_shipment_service.events.get_events]
    D --> E[Call getEvents(event, DictionaryToObject({...}))]
    E --> F[Process events and return output]
    F --> G[print(output)]
    G --> H[End]
```

> SVG rendering failed for this diagram.
