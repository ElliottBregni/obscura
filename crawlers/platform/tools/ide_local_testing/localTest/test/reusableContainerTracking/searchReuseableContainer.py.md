# Diagram: tools/ide_local_testing/localTest/test/reusableContainerTracking/searchReuseableContainer.py


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
flowchart LR
  A[container_tracking_service.api.search.search_handler] -->|call| F[lambda_handler(event, DictionaryToObject({...}))]
  subgraph Setup
    I1[gatewayUrl = "data-t.freightverify.com"]
    I2[ladList = "supplier"]
    I3[body = None]
  end
  subgraph Auth
    B[Authorizer()] --> B2[.setUsername("shipper-org-admin@yopmail.com")]
    B2 --> B3[.syncFromAuth0()]
  end
  subgraph EventConstruction
    C[CanonicalEvent()] --> C1[.setAuthorizer(authorizer)]
    C1 --> C2[.setBody(body)]
    C2 --> C3[.setHttpMethod("POST")]
    C3 --> C4[.setPathParameters({"lad": ladList})]
    C4 --> C5[.prepare()]
    C5 --> C6[.toAwsEvent()]
  end
  subgraph Utils
    D[localTest.core.DictionaryToObject] 
    E[localTest.core.get_event]
  end
  B3 --> C1
  C6 --> F
  D --> F
  F --> G[print output]
```

> SVG rendering failed for this diagram.
