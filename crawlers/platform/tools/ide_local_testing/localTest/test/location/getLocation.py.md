# Diagram: tools/ide_local_testing/localTest/test/location/getLocation.py


> Auto-generated by Obscura crawlers

## Diagram 1

```mermaid
classDiagram
    class Authorizer {
        +setUsername(name)
        +setOrganizationId(id)
        +setOrganizationProfiles(list)
    }
    class CanonicalEvent {
        +setAuthorizer(auth)
        +setResourcePath(path)
        +setBody(body)
        +setHttpMethod(method)
        +setPathParameters(params)
        +setQueryStringParameters(params)
        +prepare()
        +toAwsEvent()
    }
    class DictionaryToObject {
        <<utility>>
        +__init__(dict)
    }
    class getLocation_module {
        +lambda_handler(event, context)
    }
    Authorizer --> CanonicalEvent : used by
    DictionaryToObject --> getLocation_module : context passed to
    CanonicalEvent --> getLocation_module : event passed to
    getLocation_module ..> "print" : returns retval
```

> SVG rendering failed for this diagram.

## Diagram 2

```mermaid
flowchart TD
    A[Start script] --> B[Create Authorizer]
    B --> C[Configure Authorizer (username, org id, profiles)]
    C --> D[Create CanonicalEvent]
    D --> E[Set event fields (resource, body, method, params)]
    E --> F[prepare() -> toAwsEvent()]
    F --> G[Call getLocation.lambda_handler(event, context)]
    G --> H[retval returned]
    H --> I[print(retval)]
    I --> Z[End]
```

> SVG rendering failed for this diagram.
