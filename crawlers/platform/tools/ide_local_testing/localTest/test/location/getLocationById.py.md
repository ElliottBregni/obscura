# Diagram: tools/ide_local_testing/localTest/test/location/getLocationById.py


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
flowchart LR
  Start([Start]) --> CreateAuthorizer[Authorizer()\n.setUsername(...)\n.setOrganizationId(...)\n.setOrganizationProfiles([...])]
  CreateAuthorizer --> CreateCanonicalEvent[CanonicalEvent()\n.setAuthorizer(authorizer)\n.setResourcePath(...)\n.setBody(None)\n.setHttpMethod("GET")\n.setPathParameters(...)\n.setQueryStringParameters(...)]
  CreateCanonicalEvent --> Prepare[prepare()]
  Prepare --> ToAws[toAwsEvent()]
  ToAws --> JsonDump[json.dumps(event)]
  JsonDump --> CallLambda[getLocation.lambda_handler(event,\nDictionaryToObject({...}))]
  CallLambda --> PrintResult[print(retval)]
  PrintResult --> End([End])
```

> SVG rendering failed for this diagram.
