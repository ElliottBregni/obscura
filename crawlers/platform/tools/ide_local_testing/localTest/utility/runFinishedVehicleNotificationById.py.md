# Diagram: tools/ide_local_testing/localTest/utility/runFinishedVehicleNotificationById.py


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
flowchart LR
  Run[run()] -->|executes query| DB[FvDatabaseConnector.cursor]
  DB -->|fetch results| Rows[results: rows]
  Rows --> ForEach{for each row}
  ForEach --> Extract[extract rowId, organizationId, payload, targetService]
  Extract --> GetSolution[getFinishedVehicleSolution(organizationId, "Finished Vehicle")]
  GetSolution --> Auth[Authorizer -> CanonicalEvent -> getSolution lambda]
  ForEach --> Process[process_message(solutionId, organizationId, payload, targetService, syncType, rowId, connector)]
  Process -->|returns httpCode| Decision{httpCode}
  Decision -->|200-299| ChangeComplete[change_state(rowId, State.COMPLETE)]
  Decision -->|409| ChangeNoVins[change_state(rowId, State.NO_VINS)]
  Decision -->|other| ChangeFailure[change_state(rowId, State.FAILURE)]
  ChangeComplete --> LoopEnd[continue]
  ChangeNoVins --> LoopEnd
  ChangeFailure --> LoopEnd
  LoopEnd --> ForEach
  style DB fill:#f9f,stroke:#333,stroke-width:1px
  classDef service fill:#efe,stroke:#2b8,stroke-width:1px
  class Process,GetSolution,Auth service
```

> SVG rendering failed for this diagram.
