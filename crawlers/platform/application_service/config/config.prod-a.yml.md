# Diagram: application_service/config/config.prod-a.yml


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
classDiagram
    class Crawl {
        +run()
        +load_config()
    }
    class Crawler {
        +start()
        +stop()
    }
    class Backend {
        +serve()
        +handle_request()
    }
    class Agent {
        +execute_task()
        +report_status()
    }
    class Skill {
        +generate_mermaid(file_path)
    }
    class Sync {
        +sync_repos()
    }
    Crawl "1" o-- "*" Crawler : manages
    Crawl --> Backend : uses
    Backend --> Agent : dispatches to
    Agent --> Skill : invokes
    Sync --> Backend : triggers
    Crawler <|-- Crawl : specializes
    Skill ..> "filesystem" : reads files
```

> SVG rendering failed for this diagram.
