# Diagram: application_service/container_tracking_app_service/utility/auth.py


> Auto-generated by Obscura crawlers

## Diagram 1

```mermaid
classDiagram
    class StringEnum <<abstract>>
    class ForbiddenError <<exception>>
    class Auth {
        - __user
        - __event
        - __auth_check
        - __cursor
        + user()
        + authorizer()
        + event()
        + auth_check()
        + cursor()
        + check_privileges(event, required_privileges, solution_id)
        + check_intersecting_lists(lst, user_list)
        + check_auth_wrapper()
    }
    class NoPrivException <<exception>>
    class NoSolutionException <<exception>>
    class NoGrantException <<exception>>
    class NonIntListException <<exception>>
    class Privilege <<enumeration>> {
        + CHANGE_ACTIVE_ORGANIZATIONS
        + SUPER_PRIVILEGE
        + MANAGE_ORGANIZATIONS
        + MANAGE_CARRIER_LOCATIONS
        + MANAGE_USERS
        + VIEW_LOCATIONS
        + MANAGE_LOCATIONS
        + VIEW_SHIPMENTS
        + MANAGE_SHIPMENTS
        + LINK_LOCATIONS
        + MANAGE_SHIPPER_LOCATIONS
        + CHANGE_USER_PASSWORDS
        + MANAGE_ENTITY
        + VIEW_ENTITY
        + VIEW_LOGGING
        + VIEW_AI
        + GLOBAL_ASSET_SEARCH
        + BUILD_REPORT
        + MANAGE_SHARED_REPORTS
        + VIEW_ALL_ENTITIES
        + MANAGE_GEOFENCE_NAME
        + MANAGE_REPORT_DIRECTORY
        + MANAGE_SCAC
    }
    class Roles <<enumeration>> {
        + THIRD_PARTY
        + CARRIER_SYSTEM
        + FV_ADMIN
        + ORG_ADMIN
        + ORG_USER
        + FINISHED_VEHICLE_USER
        + FINISHED_VEHICLE_SYSTEM
        + REPORT_BUILDER_USER
        + SCAC_ADMIN
    }
    class Auth0ClientID <<enumeration>> {
        + TEST
        + STAGING
        + PROD
    }
    class AuthType <<enumeration>> {
        + PRIVILEGE
        + SOLUTION
        + LOCATION_GRANT
        + ORG_TYPE
        + FEATURE
    }
    class Feature <<enumeration>> {
        + EVENT_API_READ_ACCESS
        + FINISHED_VEHICLE
        + PLANT_ASSET_TRACKING
        + CONNECTED_CAR
        + AUTO_DISTRIBUTION_CENTER
        + MILESTONE
        + REPORT_BUILDER
        + MILESTONE_ETA
        + VIN_VIEW
        + CONTAINER
        + PARTVIEW
        + CONTAINER_TRACKING
    }
    class FederatedRole <<enumeration>> {
        + FV
    }
    class OrgTypes <<enumeration>> {
        + SHIPPER
        + CARRIER
        + THIRD_PARTY
        + FREIGHTVERIFY
        + PARTNER
        + DEALER
        + LOGISTIC_SERVICE_PROVIDER
    }

    Privilege --|> StringEnum
    Roles --|> StringEnum
    Auth0ClientID --|> StringEnum
    AuthType --|> StringEnum
    Feature --|> StringEnum
    FederatedRole --|> StringEnum
    OrgTypes --|> StringEnum

    Auth o-- Privilege
    Auth o-- Roles
    Auth o-- AuthType
    Auth o-- Feature
    Auth ..> ForbiddenError : may raise
    NonIntListException --|> Exception
    NoPrivException --|> Exception
    NoSolutionException --|> Exception
    NoGrantException --|> Exception
```

> SVG rendering failed for this diagram.

## Diagram 2

```mermaid
flowchart TD
    Start([start]) --> Iterate[Iterate auth_check groups]
    Iterate --> SetUp[Get authorizer, user_org]
    SetUp --> CheckPrivilege{AuthType.PRIVILEGE in group?}
    CheckPrivilege -- Yes --> DoPrivilege[call check_privileges(event, privileges)]
    CheckPrivilege -- No --> SkipPriv[skip privilege check]
    DoPrivilege --> PrivResult{check_privileges succeeded?}
    PrivResult -- No --> AppendPrivError[append ForbiddenError to last_check_list]
    PrivResult -- Yes --> AfterPriv[continue]
    SkipPriv --> AfterPriv

    AfterPriv --> CheckOrg{AuthType.ORG_TYPE in group?}
    CheckOrg -- Yes --> DoOrg[call check_intersecting_lists(required_org_types, user_org_types)]
    CheckOrg -- No --> SkipOrg
    DoOrg --> OrgResult{org check succeeded?}
    OrgResult -- No --> AppendOrgError[append ForbiddenError to last_check_list]
    OrgResult -- Yes --> AfterOrg
    SkipOrg --> AfterOrg

    AfterOrg --> CheckFeature{AuthType.FEATURE in group?}
    CheckFeature -- Yes --> DoFeature[call check_intersecting_lists(required_features, user_features)]
    CheckFeature -- No --> SkipFeature
    DoFeature --> FeatureResult{feature check succeeded?}
    FeatureResult -- No --> AppendFeatureError[append ForbiddenError to last_check_list]
    FeatureResult -- Yes --> AfterFeature
    SkipFeature --> AfterFeature

    AfterFeature --> EvaluateGroup[Are all checks in last_check_list non-Exceptions or list empty?]
    EvaluateGroup -- Yes --> Success[authorization success] --> EndSuccess([end: authorized])
    EvaluateGroup -- No --> RecordFirst[record first Error if not set]
    RecordFirst --> NextGroup{more auth_groups?}
    NextGroup -- Yes --> Iterate
    NextGroup -- No --> RaiseError[raise first_error] --> EndFail([end: unauthorized])
```

> SVG rendering failed for this diagram.
