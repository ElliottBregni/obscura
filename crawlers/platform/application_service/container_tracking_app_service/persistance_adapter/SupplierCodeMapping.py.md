# Diagram: application_service/container_tracking_app_service/persistance_adapter/SupplierCodeMapping.py


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
classDiagram
    class SearchDataPostgresqlMapper {
        <<abstract>>
        +SearchDataPostgresqlMapper(tableName)
        +freeze()
        +add(key, value)
    }
    class SupplierCodeMapping {
        +SupplierCodeMapping()
        +build_map()
    }
    SearchDataPostgresqlMapper <|-- SupplierCodeMapping
    note right of SupplierCodeMapping : init -> super("reuse_trip_container"); super.freeze(); build_map -> add("supplier_code","supplier_code")
```

> SVG rendering failed for this diagram.
