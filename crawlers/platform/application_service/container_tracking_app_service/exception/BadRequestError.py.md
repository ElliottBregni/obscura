# Diagram: application_service/container_tracking_app_service/exception/BadRequestError.py


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
classDiagram
    class HandledException {
        <<exception>>
        +HandledException(message, client_message, http_status)
    }
    class BadRequestError {
        +BadRequestError(message, client_message, http_status = http.HTTPStatus.BAD_REQUEST.value)
    }
    HandledException <|-- BadRequestError
    note right of HandledException : defined in container_tracking_app_service.exception.HandledException
```

> SVG rendering failed for this diagram.
