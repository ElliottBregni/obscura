# Diagram: application_service/container_tracking_app_service/common/db/email.py


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
flowchart TD
Start([Start]) --> log_email["log_email(cursor, recipients_to, body, body_html, subject, recipients_cc, recipients_bcc, attachments)"]
log_email --> init["Initialize: replaces = {}, columns = \"\", values = \"\""]
init --> define_add["Define helper: add_field(field_name, field)"]
define_add --> call1["Call: add_field('recipients_to', recipients_to)"]
call1 --> cond1{field != None?}
cond1 -- yes --> upd1["replaces.update({field_name: field})\nappend columns, append values"]
cond1 -- no --> skip1([skip])
upd1 --> call2["Call: add_field('recipients_cc', recipients_cc)"]
skip1 --> call2
call2 --> cond2{field != None?}
cond2 -- yes --> upd2["replaces.update({field_name: field})\nappend columns, append values"]
cond2 -- no --> skip2([skip])
upd2 --> call3["Call: add_field('recipients_bcc', recipients_bcc)"]
skip2 --> call3
call3 --> cond3{field != None?}
cond3 -- yes --> upd3["replaces.update({field_name: field})\nappend columns, append values"]
cond3 -- no --> skip3([skip])
upd3 --> call4["Call: add_field('body', body)"]
skip3 --> call4
call4 --> cond4{field != None?}
cond4 -- yes --> upd4["replaces.update({field_name: field})\nappend columns, append values"]
cond4 -- no --> skip4([skip])
upd4 --> call5["Call: add_field('body_html', body_html)"]
skip4 --> call5
call5 --> cond5{field != None?}
cond5 -- yes --> upd5["replaces.update({field_name: field})\nappend columns, append values"]
cond5 -- no --> skip5([skip])
upd5 --> call6["Call: add_field('subject', subject)"]
skip5 --> call6
call6 --> cond6{field != None?}
cond6 -- yes --> upd6["replaces.update({field_name: field})\nappend columns, append values"]
cond6 -- no --> skip6([skip])
upd6 --> call7["Call: add_field('attachment_details', attachments)"]
skip6 --> call7
call7 --> cond7{field != None?}
cond7 -- yes --> upd7["replaces.update({field_name: field})\nappend columns, append values"]
cond7 -- no --> skip7([skip])
upd7 --> build_sql["Build SQL string with ({columns}) and ({values})"]
skip7 --> build_sql
build_sql --> mogrify["query = cursor.mogrify(sql, replaces)"]
mogrify --> execute["cursor.execute(query)"]
execute --> fetch["results = cursor.fetchone()"]
fetch --> has_res{results?}
has_res -- yes --> ret_id["return results.id"]
has_res -- no --> ret_none["return None"]
ret_id --> End([End])
ret_none --> End
```

> SVG rendering failed for this diagram.
