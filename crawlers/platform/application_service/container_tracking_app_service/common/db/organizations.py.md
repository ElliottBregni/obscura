# Diagram: application_service/container_tracking_app_service/common/db/organizations.py


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
flowchart TD
    A([Start]) --> B[Prepare SQL with cursor.mogrify(fv_id)]
    B --> C[Execute query (cursor.execute)]
    C --> D[Fetch one row (cursor.fetchone())]
    D --> E{org found?}
    E -- Yes --> F[Return org]
    E -- No --> G[Return None]
```

> SVG rendering failed for this diagram.
