# Diagram: application_service/container_tracking_app_service/common/aws/lambdas/invokinators/invokinator_organization.py


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
flowchart LR
  ExtractOrgID[extract_organization_id] -->|generates key| CacheKey1[TTLCache key]
  CacheKey1 -->|cached decorator| GetByFV[get_organization_by_fv_id]
  GetByFV -->|deepcopy event; set queryStringParameters| Invoke1[invoke_lambda "get_organizations"]
  GetByFV -->|missing fv id| BadReq1[BadRequestError]
  Invoke1 -->|status == 404| NotFound1[NotFoundError]
  Invoke1 -->|status >= 400| Handled1[HandledException]
  Invoke1 -->|status 200| OrgResp1[organization response]

  ExtractOrgID -->|generates key| CacheKey2[TTLCache key]
  CacheKey2 -->|cached decorator| GetByID[get_organization_by_id]
  GetByID -->|deepcopy event; set pathParameters| Invoke2[invoke_lambda "get_organizations"]
  GetByID -->|missing organization_id| BadReq2[BadRequestError]
  Invoke2 -->|status == 404| NotFound2[NotFoundError]
  Invoke2 -->|status >= 400| Handled2[HandledException]
  Invoke2 -->|status 200| OrgResp2[organization response]

  ExtractOrgData[extract_org_data] -->|generates key from params| CacheKey3[TTLCache key]
  CacheKey3 -->|cached decorator| GetParams[get_org_params]
  GetParams -->|deepcopy event; set queryStringParameters| Invoke3[invoke_lambda "get_organizations"]
  GetParams -->|everything param present| QueryFields[adds everything & fields]
  Invoke3 -->|status == 404| NotFound3[NotFoundError]
  Invoke3 -->|status >= 400| Handled3[HandledException]
  Invoke3 -->|status 200| OrgResp3[organizations response]
```

> SVG rendering failed for this diagram.
