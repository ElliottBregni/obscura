# Diagram: application_service/container_tracking_app_service/common/aws/lambdas/invokinators/invokinator_iam.py


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
flowchart LR
  GUP[get_user_org_prefs(event, organization_id, user_auth0_id=None, key_pref=None)]
  IGF[invoke_get_features(organization_id, lambda_level)]
  IGS[invoke_get_solution(solution_id)]
  SYNC[sync_user(auth0_user_id)]
  IGO[invoke_get_organizations(event, query_params)]
  IGSByOrg[invoke_get_solution_by_organization_id_and_feature(organization_id, feature_name)]
  EXS[extract_solution_id(solution_id)]
  EQP[extract_query_params(event, query_params)]
  EOINF[extract_organization_id_feature_name(organization_id, feature_name)]
  INV[invoke_lambda(name, full_payload)]
  AUTH[auth / requestContext.authorizer]
  CACHE["TTLCache\n@cached"]
  HAND[HandledException]
  JSON[json.loads / response parsing]

  GUP -->|sets authorizer, pathParameters| AUTH
  GUP -->|calls| INV
  GUP -->|parses body/status| JSON
  GUP -->|on error raises| HAND

  IGF -->|builds event with privileges| AUTH
  IGF -->|calls| INV
  IGF -->|parses body/status| JSON
  IGF -->|on error raises| HAND

  IGS -->|uses key from| EXS
  EXS -->|key provider| CACHE
  IGS -->|cached by| CACHE
  IGS -->|calls| INV
  IGS -->|parses body/status| JSON
  IGS -->|on error raises| HAND

  SYNC -->|builds admin event| INV
  SYNC -->|parses body/status| JSON
  SYNC -->|on error raises| HAND

  IGO -->|copies event & sets queryStringParameters| EQP
  EQP -->|reads org id via| AUTH
  EQP -->|produces cache key| CACHE
  IGO -->|cached by| CACHE
  IGO -->|calls| INV
  IGO -->|parses body/status and returns org response| JSON
  IGO -->|on non-404 error raises| HAND

  IGSByOrg -->|uses key from| EOINF
  EOINF -->|key provider| CACHE
  IGSByOrg -->|cached by| CACHE
  IGSByOrg -->|calls| INV
  IGSByOrg -->|parses body/status| JSON
  IGSByOrg -->|on error raises| HAND

  INV -->|returns response dict with body/statusCode| JSON

  style CACHE fill:#f9f,stroke:#333,stroke-width:1px
  style HAND fill:#faa,stroke:#900,stroke-width:1px
  style AUTH fill:#cff,stroke:#036,stroke-width:1px
```

> SVG rendering failed for this diagram.
