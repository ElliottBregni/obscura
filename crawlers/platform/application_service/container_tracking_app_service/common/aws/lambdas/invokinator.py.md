# Diagram: application_service/container_tracking_app_service/common/aws/lambdas/invokinator.py


> Auto-generated by Obscura crawlers

## Diagram 1

```mermaid
flowchart TD
    subgraph Incoming
        E[Incoming event]
    end
    E --> IG[invoke_get_location(event)]
    E --> GS[get_solution(event, solution_id)]
    E --> GSF[get_solution_by_feature_and_org_id(event, feature_id, organization_fv_id)]
    E --> GO[get_organization(event, organization_fv_id)]
    E --> GOS[get_organizations(key)]
    IG --> IL_locations[invoke_lambda("locations-get")]
    IL_locations --> PL_loc{statusCode >= 400?}
    PL_loc -- "yes" --> HE1[HandledException raised]
    PL_loc -- "no" --> RL_loc[return loc_body]
    GS --> IL_solution_get[invoke_lambda("get_solution")]
    IL_solution_get --> PS_sol{statusCode >= 400?}
    PS_sol -- "yes" --> HE2[HandledException raised with INTERNAL_SERVER_ERROR]
    PS_sol -- "no" --> RL_sol[return solution_body]
    GSF --> IL_solution_get2[invoke_lambda("get_solution") (by feature/org)]
    IL_solution_get2 --> PS_sol2{statusCode >= 400?}
    PS_sol2 -- "yes" --> HE3[HandledException raised with INTERNAL_SERVER_ERROR]
    PS_sol2 -- "no" --> RL_sol2[return solution_body]
    GOS --> IL_get_orgs[invoke_lambda("get_organizations")]
    IL_get_orgs --> PG_orgs{statusCode >= 400?}
    PG_orgs -- "yes" --> HE4[HandledException raised]
    PG_orgs -- "no" --> PARSE_ORGS[parse and map organizations by key]
    GO --> IL_get_org[invoke_lambda("get_organizations") with queryStringParameters]
    IL_get_org --> PG_org{statusCode == 404?}
    PG_org -- "yes" --> NF[NotFoundError raised]
    PG_org -- "no" --> PG_org2{statusCode >= 400?}
    PG_org2 -- "yes" --> HE5[HandledException raised]
    PG_org2 -- "no" --> RETURN_ORG[return organization.response]
    style HE1 fill:#f99,stroke:#900
    style HE2 fill:#f99,stroke:#900
    style HE3 fill:#f99,stroke:#900
    style HE4 fill:#f99,stroke:#900
    style HE5 fill:#f99,stroke:#900
```

> SVG rendering failed for this diagram.

## Diagram 2

```mermaid
classDiagram
    class Invokers {
        +invoke_get_location(event, query_params={}, headers={}, accept=None, path_params=None)
        +get_shipper_locations(organization_id: int)
        +get_solution(event, solution_id)
        +get_solution_by_feature_and_org_id(event, feature_id, organization_fv_id)
        +get_organization(event, organization_fv_id)
        +get_organizations(key=ORGANIZATION_KEY_FV_ID)
    }
    class Cache {
        +TTLCache(maxsize, ttl)
        +cached(func, key)
    }
    class Lambda {
        +invoke_lambda(name, full_payload)
    }
    class Errors {
        +HandledException
        +BadRequestError
        +NotFoundError
    }
    class Utils {
        +deepcopy(obj)
        +json.loads(s)
        +hashkey(*args)
    }
    Invokers --> Cache : "decorated with"
    Invokers --> Lambda : "calls"
    Invokers --> Errors : "raises"
    Invokers --> Utils : "uses"
    Lambda <..> Utils : "payload / parse"
    Cache <..> Utils : "key generator"
```

> SVG rendering failed for this diagram.
