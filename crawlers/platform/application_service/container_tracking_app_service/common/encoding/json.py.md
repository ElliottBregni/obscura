# Diagram: application_service/container_tracking_app_service/common/encoding/json.py


> Auto-generated by Obscura crawlers

## Diagram 1

```mermaid
flowchart TD
    Start([encoder(value)]) --> IsDate{Is instance of date/datetime/time?}
    IsDate -- Yes --> ISO[return value.isoformat()]
    IsDate -- No --> IsRange{Is instance of DateTimeRange?}
    IsRange -- Yes --> RangeBounds{value.lower and value.upper?}
    RangeBounds -- Yes --> RangeTS[return [value.lower.timestamp(), value.upper.timestamp()]]
    RangeBounds -- No --> RangeNone[return [None, None]]
    IsRange -- No --> IsMock{Is instance of unittest.mock.MagicMock?}
    IsMock -- Yes --> MockRet["return \"MOCK\""]
    IsMock -- No --> IsDecimal{Is instance of decimal.Decimal?}
    IsDecimal -- Yes --> DecRet["return float(value)"]
    IsDecimal -- No --> IsEnum{Is instance of enum.Enum?}
    IsEnum -- Yes --> EnumRet["return value.value"]
    IsEnum -- No --> IsDelta{Is instance of datetime.timedelta?}
    IsDelta -- Yes --> PrintCall["print(value.days, value.seconds)"]
    PrintCall --> CalcHours["hours = floor(value.seconds / 3600)"]
    CalcHours --> CalcMinutes["minutes = floor(value.seconds / 60 % 60)"]
    CalcMinutes --> FormatRet["return f\"DD:HH:MM\" (zfilled days/hours/minutes)"]
    IsDelta -- No --> Error["raise BadRequestError(message, message, http_status=500)"]
```

> SVG rendering failed for this diagram.

## Diagram 2

```mermaid
classDiagram
    class encoder {
        +encoder(value)
    }
    class datetime_types
    class DateTimeRange
    class MagicMock
    class Decimal
    class Enum
    class timedelta
    class BadRequestError

    encoder --> datetime_types : isoformat
    encoder --> DateTimeRange : timestamps
    encoder --> MagicMock : "MOCK"
    encoder --> Decimal : float(value)
    encoder --> Enum : value.value
    encoder --> timedelta : format to "DD:HH:MM"
    encoder --> BadRequestError : raises on unknown type
```

> SVG rendering failed for this diagram.
