# Diagram: application_service/serverless.container_tracking_app.yml


> Auto-generated by Obscura crawlers

## Diagram 1

```mermaid
flowchart LR
  subgraph AWS
    APIGW[API Gateway]
    IAM[IAM Role]
    S3[S3 Buckets\n(csv-export-*, fv-entity-media)]
    SM[Secrets Manager]
    SNS[SNS]
    Lambda1[update_reuse_trip_container_location_entry\n(lambda)]
    Lambda2[update_reuse_trip_container_location_exit\n(lambda)]
    Lambda3[add_reuse_trip_container_containertype\n(lambda)]
    Lambda4[delete_reuse_trip_container_containertype\n(lambda)]
  end

  APIGW -->|POST /containertracking/location?location_id| Lambda1
  APIGW -->|DELETE /containertracking/location?location_id| Lambda2
  APIGW -->|POST /containertracking/containertype?container_type| Lambda3
  APIGW -->|DELETE /containertracking/containertype?container_type| Lambda4

  IAM -->|grants| Lambda1
  IAM -->|grants| Lambda2
  IAM -->|grants| Lambda3
  IAM -->|grants| Lambda4

  IAM -->|allows s3:* on| S3
  IAM -->|allows secretsmanager:GetSecretValue on| SM
  IAM -->|allows sns:CreateTopic,Publish on| SNS
  IAM -->|denies logs:CreateLogGroup/CreateLogStream/PutLogEvents for specific log-groups| APIGW

  style AWS fill:#f9f,stroke:#333,stroke-width:1px
```

> SVG rendering failed for this diagram.

## Diagram 2

```mermaid
classDiagram
  class Function {
    +string name
    +string handler
    +string http_path
    +string http_method
    +string authorizer
    +string cors
  }

  class update_reuse_trip_container_location_entry {
    +name: ${opt:stage}-update_reuse_trip_container_location_entry
    +handler: container_tracking_app_service/api/update_reuse_trip_container_location_entry.lambda_handler
    +path: /containertracking/location
    +method: POST
    +query: location_id
  }

  class update_reuse_trip_container_location_exit {
    +name: ${opt:stage}-update_reuse_trip_container_location_exit
    +handler: container_tracking_app_service/api/update_reuse_trip_container_location_exit.lambda_handler
    +path: /containertracking/location
    +method: DELETE
    +query: location_id
  }

  class add_reuse_trip_container_containertype {
    +name: ${opt:stage}-add_reuse_trip_container_containertype
    +handler: container_tracking_app_service/api/add_reuse_trip_container_containertype.lambda_handler
    +path: /containertracking/containertype
    +method: POST
    +query: container_type
  }

  class delete_reuse_trip_container_containertype {
    +name: ${opt:stage}-delete_reuse_trip_container_containertype
    +handler: container_tracking_app_service/api/delete_reuse_trip_container_containertype.lambda_handler
    +path: /containertracking/containertype
    +method: DELETE
    +query: container_type
  }

  Function <|-- update_reuse_trip_container_location_entry
  Function <|-- update_reuse_trip_container_location_exit
  Function <|-- add_reuse_trip_container_containertype
  Function <|-- delete_reuse_trip_container_containertype
```

> SVG rendering failed for this diagram.
