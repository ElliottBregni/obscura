# Diagram: delete_log_groups.sh


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
flowchart LR
  A[Start] --> B[Set environment variables: STAGE=dev<br>FILE=partview_core/partview_service/serverless.partview_visibility.yml<br>REGION=us-east-1]
  B --> C[Run yq -r: extract function names<br>.functions | to_entries[] | (.value.name // ("${opt:stage}-" + .key))]
  C --> D[sed: replace \${opt:stage} with dev]
  D --> E[awk: prefix each name with /aws/lambda/]
  E --> F[xargs: for each line run<br>aws logs delete-log-group --region "us-east-1" --log-group-name "{}"]
  F --> G[Completed deletions]
  style B fill:#f9f,stroke:#333,stroke-width:1
  style F fill:#fdd,stroke:#900,stroke-width:1
```

> SVG rendering failed for this diagram.
