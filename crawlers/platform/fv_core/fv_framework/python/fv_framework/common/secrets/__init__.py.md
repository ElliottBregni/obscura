# Diagram: fv_core/fv_framework/python/fv_framework/common/secrets/__init__.py


> Auto-generated by Obscura crawlers

## Diagram 1

```mermaid
classDiagram
class SecretNames <<enumeration>> {
  +DATABASE
  +DATABASE_LOGGING
  +HERE
  +AUTH0
  +ADMIN
  +POWER_BI
  +DATABASE_TRACKING
  +DATABASE_EXILE
  +ENTITY_DATABASE
  +ENTITY_DATABASE_TRACKING
  +SMTP
  +L2_ETA
}
class Secrets {
  +client
  +secrets
  +Secrets(region_name="us-east-1")
  +get_secrets(secrets, required=True, dev_secrets=False)
  +get_secret(secret_name, required=True, dev_secrets=False, cached=True)
  -_get_secret(secret_with_env, required=True, cached=True)
}
class get_client <<function>>
class envkey <<function>>
class extract_cached_params <<function>>
class CACHE <<MyTTLCache>> {
  maxsize=128
  ttl=300
}
class boto3_session <<module>>
class SecretsManagerClient <<client>>
class ClientError <<exception>>
class BadRequestError <<exception>>

Secrets --> get_client : obtains client
Secrets o-- CACHE : uses cache
get_client --> boto3_session : creates Session()
get_client --> SecretsManagerClient : returns client
Secrets._get_secret ..> ClientError : handles
Secrets._get_secret --|> BadRequestError : may raise
SecretNames ..> Secrets : secret keys referenced
```

> SVG rendering failed for this diagram.

## Diagram 2

```mermaid
sequenceDiagram
participant Caller as Caller
participant S as Secrets
participant C as CACHE
participant SM as SecretsManager
participant BE as BadRequestError

Caller->>S: get_secret(secret_name, required=True, dev_secrets=False, cached=True)
S->>S: compute stage and secret_with_env
S->>C: check cached key (envkey)
alt cache hit
C-->>S: cached secret
S-->>Caller: return secret (dotdict)
else cache miss
S->>SM: get_secret_value(SecretId=secret_with_env)
alt secret string present
SM-->>S: {SecretString: "..."}
S->>S: parse JSON -> dotdict
S-->>C: store in cache
S-->>Caller: return secret (dotdict)
else ResourceNotFoundException
SM-->>S: ResourceNotFoundException
alt required == True
S-->>Caller: log error and raise exception
else required == False
S-->>Caller: log warning and return None
end
else ClientError
SM-->>S: ClientError
alt required == True
S-->>Caller: log error and raise
else required == False
S-->>Caller: log warning and return None
end
end
```

> SVG rendering failed for this diagram.
