# Diagram: fv_core/fv_framework/python/fv_framework/common/HERE/HERE.py


> Auto-generated by Obscura crawlers

## Diagram 1

```mermaid
classDiagram
    class HereModule {
        +get_lat_long(location, precise=True)
        +get_geocode(location)
        +get_normalized_address(location)
        +validate_response(r)
        +parse_geocode(r)
        +parse_reverse_geocode(r, need_full=True)
        +get_shipment_routing(waypoints, body_datetime=None)
        +get_routing(waypoints, body_datetime=None)
        +parse_routing(r, body_datetime=None)
        +get_stop_location(stop, payload)
    }
    class Secrets {
        +SECRETS
        +get_secret(name)
    }
    class Requests {
        +get(url, params, verify)
    }
    class Utilities {
        +is_blank(val)
        +is_present(val)
        +validate_location_update(lat, lon)
        +format_fv_stop_time(dt)
    }
    class Errors {
        +BadRequestError
    }

    HereModule --> Secrets : uses
    HereModule --> Requests : makes HTTP calls
    HereModule --> Utilities : uses
    HereModule --> Errors : raises
    HereModule ..> HereModule : calls get_geocode, parse_geocode, parse_routing, validate_response
    get_geocode ..> Requests : "requests.get(here_geocode_uri)"
    get_routing ..> Requests : "requests.get(here_routing_uri)"
    parse_geocode ..> validate_response : validates input
    get_normalized_address --> get_geocode : obtains (r, loc_str)
    get_shipment_routing --> get_routing : obtains routing response
    get_routing --> parse_routing : returns routing for parsing
    get_stop_location --> get_geocode : fallback geocode lookup
    get_stop_location --> parse_geocode : parse HERE response when available
```

> SVG rendering failed for this diagram.

## Diagram 2

```mermaid
flowchart TD
    Start([get_lat_long(location, precise)]) --> CheckAddr{Required address fields present?}
    CheckAddr -- No --> ErrBadReq[BadRequestError: missing address fields]
    CheckAddr -- Yes --> CheckName{One of name fields present OR not precise?}
    CheckName -- Yes --> CallGeo[Call get_geocode(location) -> logging.info(loc_str)]
    CallGeo --> ParseGeo[Call parse_geocode(r) -> (latitude, longitude)]
    ParseGeo --> ReturnLatLong([Return (latitude, longitude)])
    CheckName -- No --> ErrBadReq
    ErrBadReq --> End([Exception raised])
```

> SVG rendering failed for this diagram.
