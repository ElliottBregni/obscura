# Diagram: fv_core/fv_framework/python/fv_framework/common/db/email.py


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
flowchart TD
    A([Start]) --> B[Initialize<br/>replaces = {}<br/>columns = ""<br/>values = ""]
    B --> C[Define helper<br/>add_field(field_name, field)]
    C --> G[Call add_field for:<br/>recipients_to<br/>recipients_cc<br/>recipients_bcc<br/>body<br/>body_html<br/>subject<br/>attachment_details]
    G --> D{field is not None?}
    D -- Yes --> E[Update replaces[field_name] = field<br/>Append field_name to columns<br/>Append placeholder to values]
    D -- No --> F[No operation]
    E --> H[Build SQL string:<br/>INSERT INTO email_log (columns) VALUES (values) RETURNING id]
    F --> H
    H --> I[query = cursor.mogrify(sql, replaces)]
    I --> J[cursor.execute(query)]
    J --> K[results = cursor.fetchone()]
    K --> L{results?}
    L -- Yes --> M[return results.id]
    L -- No --> N[return None]
    M --> O([End])
    N --> O
```

> SVG rendering failed for this diagram.
