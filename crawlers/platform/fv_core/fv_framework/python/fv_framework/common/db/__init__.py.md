# Diagram: fv_core/fv_framework/python/fv_framework/common/db/__init__.py


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
classDiagram
class Exception
class DatabaseError {
  +http_status
  +__init__(message, http_status)
}
class UniqueConstraintViolation {
  +__init__(message, http_status)
}
DatabaseError --|> Exception
UniqueConstraintViolation --|> DatabaseError

class NamedTupleCursor
class Cursor {
  -db_conn
  +__init__(db_conn)
  +_establish_connection()
  +_execute(query, values=None)
  +_execute_with_reconnect(query, values=None)
  +execute(query, values=None, retry=False)
  +close()
  +commit()
  +rollback()
}
Cursor --|> NamedTupleCursor
Cursor ..> FvDatabaseConnector : uses db_conn

class FvDatabaseConnector {
  -database_name
  -application_name
  -autocommit
  -secret_config
  -cursor
  -connection
  -force_connect_main
  +__init__(application_name, database_name, autocommit=True, auto_connect=True, force_connect_main=False)
  +establish_connection(retry_new_secret=False)
  +get_cursor()
}
FvDatabaseConnector o-- Cursor : "holds"

class ShipmentDatabase {
  +__init__(application_name, autocommit=True, auto_connect=True)
}
class ShipmentReadOnlyDatabase {
  +__init__(application_name, autocommit=True, auto_connect=True)
}
ShipmentDatabase --|> FvDatabaseConnector
ShipmentReadOnlyDatabase --|> FvDatabaseConnector

class Secrets
class SecretNames
FvDatabaseConnector ..> Secrets : uses
FvDatabaseConnector ..> SecretNames : uses

class Utils <<module>> {
  +REPLICA_SECRETS
  +get_connection(configuration, is_logging=False, DB_APP_NAME="FV_Lambda", autocommit=False, force_connect_main=False)
  +get_cursor(connection, cursor_factory, arraysize=1000)
  +get_db_details(cursor)
  +log_query(q, logger=None, level=logging.DEBUG)
  +q(*args)
  +get_org_id(cursor, email)
  +is_shipper(cursor, id)
  +get_org_category(cursor, id)
  +sanitize_column(cursor, column)
}
Utils ..> psycopg2 : uses
Utils ..> "psycopg2.extras.NamedTupleCursor" : uses
Utils ..> Cursor : returns/get cursor
```

> SVG rendering failed for this diagram.
