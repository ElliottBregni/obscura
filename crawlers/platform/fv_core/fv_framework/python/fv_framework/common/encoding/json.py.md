# Diagram: fv_core/fv_framework/python/fv_framework/common/encoding/json.py


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
flowchart TD
    Start([encoder(value)]) --> IsDate{is datetime.date<br>/ datetime.datetime<br>/ datetime.time?}
    IsDate -- yes --> Iso[return value.isoformat()]
    IsDate -- no --> IsRange{is DateTimeRange?}
    IsRange -- yes --> RangeBounds{value.lower and value.upper?}
    RangeBounds -- yes --> RangeYes[return [value.lower.timestamp(),<br>value.upper.timestamp()]]
    RangeBounds -- no --> RangeNo[return [None, None]]
    IsRange -- no --> IsMock{is unittest.mock.MagicMock?}
    IsMock -- yes --> MockRet[return "MOCK"]
    IsMock -- no --> IsDecimal{is decimal.Decimal?}
    IsDecimal -- yes --> DecRet[return float(value)]
    IsDecimal -- no --> IsEnum{is enum.Enum?}
    IsEnum -- yes --> EnumRet[return value.value]
    IsEnum -- no --> IsTimedelta{is datetime.timedelta?}
    IsTimedelta -- yes --> TdCalc[hours = floor(value.seconds / 3600)<br>minutes = floor(value.seconds / 60 % 60)<br>return "DD:HH:MM" formatted]
    IsTimedelta -- no --> Error[raise BadRequestError(message,<br>http_status=HTTPStatus.INTERNAL_SERVER_ERROR.value)]
    Iso --> End([end])
    RangeYes --> End
    RangeNo --> End
    MockRet --> End
    DecRet --> End
    EnumRet --> End
    TdCalc --> End
    Error --> End
```

> SVG rendering failed for this diagram.
