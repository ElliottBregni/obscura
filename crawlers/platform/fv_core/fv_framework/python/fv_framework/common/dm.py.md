# Diagram: fv_core/fv_framework/python/fv_framework/common/dm.py


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
flowchart TD
  subgraph extract
    EStart([extract(d, path, required=True, default=None)])
    Glom{{glom.glom(d, path)}}
    EReturn([return value])
    BuildErr[/build missing-component error message/]
    ReqCheck1{required?}
    RaiseBR1[/raise BadRequestError with traceback/]
    Warn1([logging.warning(error)])
    EDefault([return default])

    EStart --> Glom
    Glom -->|success| EReturn
    Glom -->|PathAccessError| BuildErr
    BuildErr --> ReqCheck1
    ReqCheck1 -->|yes| RaiseBR1
    ReqCheck1 -->|no| Warn1 --> EDefault
  end

  subgraph extract_float
    EFStart([extract_float(d, path, required=True, default=None)])
    CallExtract1([value = extract(...)])
    TryFloat{can float(value)?}
    ReturnFloat([return float(value)])
    RaiseBR2[/raise BadRequestError: path must be a real number/]
    EFDefault([return default])

    EFStart --> CallExtract1 -->|value| TryFloat
    TryFloat -->|yes| ReturnFloat
    TryFloat -->|no (TypeError/ValueError)| ReqCheck2{required?}
    ReqCheck2 -->|yes| RaiseBR2
    ReqCheck2 -->|no| EFDefault
  end

  subgraph extract_int
    EIStart([extract_int(d, path, required=True, default=None)])
    CallExtract2([value = extract(...)])
    TryConvert{float(value) == int(value)?}
    ReturnInt([return int(value)])
    RaiseBR3[/raise BadRequestError: path must be an integer/]
    EIDefault([return default])

    EIStart --> CallExtract2 --> TryConvert
    TryConvert -->|yes| ReturnInt
    TryConvert -->|no or error| ReqCheck3{required?}
    ReqCheck3 -->|yes| RaiseBR3
    ReqCheck3 -->|no| EIDefault
  end

  subgraph extract_list
    ELStart([extract_list(d, path, required=True, default=None)])
    CallExtract3([value = extract(...)])
    CheckList{required and not isinstance(value, list)?}
    ReturnList([return value])
    RaiseBR4[/raise BadRequestError: path must be a list/]

    ELStart --> CallExtract3 --> CheckList
    CheckList -->|true| RaiseBR4
    CheckList -->|false| ReturnList
  end

  %% Call relationships
  EFStart --- EStart
  EIStart --- EStart
  ELStart --- EStart
```

> SVG rendering failed for this diagram.
