# Diagram: fv_core/fv_framework/python/fv_framework/sqs/middleware/middleware.py


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
flowchart LR
  RollBarConfig[RollBarConfig.configure()]
  Event[/event, context/]
  getAll[get_all_message_ids(event)]
  SQ[sqs_batch_failure_wrapper(handler_function)]
  Datadog[datadog_lambda_wrapper]
  Wrapper[wrapper(event, context, ...)]
  Handler[handler_function(event, context, ...)]
  Ex((Exception))
  MsgIDs[message_id list]
  Resp[response {"batchItemFailures": [...]}]
  Audit[RequestAudit(...)]
  Log[.log()]
  Rollbar[.rollbar(exc_info=e)]
  GetLambda[get_lambda_level(event)]
  GetOrg[get_organization_id(event)]

  SQ --> Wrapper
  Datadog --> Wrapper
  Wrapper --> Handler
  Handler -.throws.-> Ex
  Ex -.caught_by.-> Wrapper
  Wrapper -.calls.-> getAll
  Event --> getAll
  getAll --> MsgIDs
  MsgIDs --> Resp
  Wrapper -.on_exception_builds.-> Resp
  Wrapper -.calls_audit.-> Audit
  Audit --> Log --> Rollbar
  Event --> Wrapper
  Event --> GetLambda
  Event --> GetOrg
```

> SVG rendering failed for this diagram.
