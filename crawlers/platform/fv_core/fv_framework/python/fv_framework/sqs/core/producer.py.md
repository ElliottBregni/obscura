# Diagram: fv_core/fv_framework/python/fv_framework/sqs/core/producer.py


> Auto-generated by Obscura crawlers

## Diagram 1

```mermaid
classDiagram
class Sqs {
  +get_queue()
}
class SqsMessageProducer {
  +SqsMessageProducer(queue_name)
  +send_message(message_body, message_attributes)
  +normalize_group_id(message_group_id)
  +send_message_with_group_id(message_body, message_group_id, message_attributes)
  +send_fifo_message(message_body, message_attributes, message_group_id, deduplication_id, system_attributes)
}
class StandardQueueMessage {
  +StandardQueueMessage(body, attributes)
  +send(queue)
}
class FIFOQueueMessage {
  +FIFOQueueMessage(body, attributes, group_id, deduplication_id, system_attributes)
  +send(queue)
}
Sqs <|-- SqsMessageProducer
SqsMessageProducer --> StandardQueueMessage : creates/sends
SqsMessageProducer --> FIFOQueueMessage : creates/sends
```

> SVG rendering failed for this diagram.

## Diagram 2

```mermaid
sequenceDiagram
participant Producer as SqsMessageProducer
participant Sqs
participant Queue
participant StdMsg as StandardQueueMessage
participant FIFO as FIFOQueueMessage
participant Log as logging

Producer->>Sqs: get_queue()
Sqs-->>Producer: queue
Producer->>StdMsg: StandardQueueMessage(body, attributes)
StdMsg->>Queue: send(queue)
Queue-->>StdMsg: response
StdMsg-->>Producer: response

Producer->>Producer: normalize_group_id(message_group_id)
alt normalization fails
Producer->>Log: warning("Failed to normalize group ID")
end
Producer->>FIFO: FIFOQueueMessage(body, attributes, group_id, deduplication_id, system_attributes)
FIFO->>Queue: send(queue)
Queue-->>FIFO: response
FIFO-->>Producer: response
```

> SVG rendering failed for this diagram.
