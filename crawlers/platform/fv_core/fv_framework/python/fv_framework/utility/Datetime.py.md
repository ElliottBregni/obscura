# Diagram: fv_core/fv_framework/python/fv_framework/utility/Datetime.py


> Auto-generated by Obscura crawlers

## Diagram 1

```mermaid
classDiagram
class Datetime {
  - __datetime: Optional[datetime.datetime]
  + __init__(timestamp, now_if_none=True)
  + get_datetime(): datetime.datetime
  + set_datetime(timestamp: Optional[datetime.datetime]): Datetime
  + to_ISO_string(): Optional[str]
  + year(): int
  + is_before(other_datetime: Datetime, compare_at_eod: bool=False): bool
  + is_after(other_datetime: Datetime, compare_at_eod: bool=False): bool
  + to_simple_format(): str
  + change_time_to_midnight(): Datetime
  + get_time_at_eod(): Datetime
  + to_timezone_ISO_string(the_timezone: str)
  + timestamp_to_datetime(timestamp: str): Datetime
  + set_to_now(): Datetime
  + to_iso_date(value: datetime.datetime | date | None): str | None
}
class Freezeable
class BadRequestError
class _TIMEZONES <<constant>>
class _PATTERNS <<constant>>
class _IN_DAYLIGHT_SAVINGS <<constant>>
Datetime --|> Freezeable
Datetime ..> BadRequestError : raises
Datetime ..> _TIMEZONES : uses
Datetime ..> _PATTERNS : uses
Datetime ..> _IN_DAYLIGHT_SAVINGS : reads
```

> SVG rendering failed for this diagram.

## Diagram 2

```mermaid
flowchart TD
A[Input timestamp string] --> B[Replace trailing 'Z' placeholder and map TZ abbreviations]
B --> C{Matches \"+/-HH:MM\" timezone format?}
C -- yes --> D[Remove colon from timezone offset]
C -- no --> E[Skip colon removal]
D --> F[Apply regex to compact timezone digits]
E --> F
F --> G[Loop: try datetime.strptime with patterns from _PATTERNS]
G --> H{Parse succeeded?}
H -- yes --> I[If no tzinfo: set tzinfo=UTC; then astimezone(UTC)]
H -- no --> J[Raise BadRequestError("Invalid timestamp format")]
I --> K[call set_datetime(new_datetime) and return self]
J --> K
```

> SVG rendering failed for this diagram.
