# Diagram: fv_core/fv_framework/python/fv_framework/utility/Authorization.py


> Auto-generated by Obscura crawlers

## Diagram 1

```mermaid
classDiagram
class Auth {
  - __user
  - __event
  - __auth_check
  - __cursor
  + user()
  + authorizer()
  + event()
  + auth_check()
  + cursor()
  + check_privileges(event, required_privileges, solution_id=None)
  + check_intersecting_lists(lst, user_list)
  + check_auth_wrapper()
}
class NoPrivExcpetion <<exception>>
class NoSolutionException <<exception>>
class NoGrantException <<exception>>
class NonIntListException <<exception>>
class Privilege <<enumeration>> {
  CHANGE_ACTIVE_ORGANIZATIONS
  SUPER_PRIVILEGE
  MANAGE_ORGANIZATIONS
  MANAGE_CARRIER_LOCATIONS
  MANAGE_USERS
  VIEW_LOCATIONS
  MANAGE_LOCATIONS
  VIEW_SHIPMENTS
  MANAGE_SHIPMENTS
  LINK_LOCATIONS
  MANAGE_SHIPPER_LOCATIONS
  CHANGE_USER_PASSWORDS
  MANAGE_ENTITY
  VIEW_ENTITY
  VIEW_LOGGING
  VIEW_AI
  GLOBAL_ASSET_SEARCH
  BUILD_REPORT
  MANAGE_SHARED_REPORTS
  VIEW_ALL_ENTITIES
  MANAGE_GEOFENCE_NAME
  MANAGE_REPORT_DIRECTORY
  MANAGE_SCAC
  MANAGE_REUSE_CONTAINER
}
class Roles <<enumeration>> {
  THIRD_PARTY
  CARRIER_SYSTEM
  FV_ADMIN
  ORG_ADMIN
  ORG_USER
  FINISHED_VEHICLE_USER
  FINISHED_VEHICLE_SYSTEM
  REPORT_BUILDER_USER
  SCAC_ADMIN
}
class Auth0ClientID <<enumeration>> {
  TEST
  STAGING
  PROD
}
class AuthType <<enumeration>> {
  PRIVILEGE
  SOLUTION
  LOCATION_GRANT
  ORG_TYPE
  FEATURE
}
class Feature <<enumeration>> {
  EVENT_API_READ_ACCESS
  FINISHED_VEHICLE
  PLANT_ASSET_TRACKING
  CONNECTED_CAR
  AUTO_DISTRIBUTION_CENTER
  MILESTONE
  MILESTONE_DASHBOARD
  REPORT_BUILDER
  MILESTONE_ETA
  VIN_VIEW
  CONTAINER
  PARTVIEW
  PARTVIEW_INBOUND
  CONTAINER_TRACKING
  SURGICAL_TOTE_TRACKING
  OCEAN_TRACKING
}
class FederatedRole <<enumeration>> { FV }
class OrgTypes <<enumeration>> {
  SHIPPER
  CARRIER
  THIRD_PARTY
  FREIGHTVERIFY
  PARTNER
  DEALER
  LOGISTIC_SERVICE_PROVIDER
  HEALTHCARE_PROVIDER
}
class OrgTypeName <<enumeration>> { SHIPPER DEALER CARRIER }
class PartViewDefaults {
  + LOCATION_GRANTORS
  + DEALER_FILTER_SOLUTION
}

Auth --|> Privilege : uses
Auth --|> Roles : uses
Auth --|> Auth0ClientID : uses
Auth --|> AuthType : uses
Auth --|> Feature : uses
Auth --|> FederatedRole : uses
Auth --|> OrgTypes : uses
Auth --|> OrgTypeName : uses
Auth *-- PartViewDefaults : contains
Auth ..> NoPrivExcpetion : raises
Auth ..> NoSolutionException : raises
Auth ..> NoGrantException : raises
Auth ..> NonIntListException : raises
```

> SVG rendering failed for this diagram.

## Diagram 2

```mermaid
flowchart TD
  Start([Start]) --> ForEachGroup{For each auth_group in auth_check}
  ForEachGroup --> CheckPrivilege{AuthType.PRIVILEGE in auth_group?}
  CheckPrivilege -- Yes --> CallCheckPrivileges[call check_privileges(event, privileges)]
  CallCheckPrivileges --> PrivOK{NoPrivExcpetion raised?}
  PrivOK -- Yes --> AddForbidden1[append ForbiddenError to last_check_list]
  PrivOK -- No --> Continue1
  CheckPrivilege -- No --> Continue1
  Continue1 --> CheckOrgType{AuthType.ORG_TYPE in auth_group?}
  CheckOrgType -- Yes --> CallCheckIntersect1[call check_intersecting_lists(required_org_types, user_org_types)]
  CallCheckIntersect1 --> OrgOK{NonIntListException raised?}
  OrgOK -- Yes --> AddForbidden2[append ForbiddenError to last_check_list]
  OrgOK -- No --> Continue2
  CheckOrgType -- No --> Continue2
  Continue2 --> CheckFeature{AuthType.FEATURE in auth_group?}
  CheckFeature -- Yes --> CallCheckIntersect2[call check_intersecting_lists(required_features, user_features)]
  CallCheckIntersect2 --> FeatureOK{NonIntListException raised?}
  FeatureOK -- Yes --> AddForbidden3[append ForbiddenError to last_check_list]
  FeatureOK -- No --> Continue3
  CheckFeature -- No --> Continue3
  Continue3 --> EvaluateChecks{all checks passed or none?}
  EvaluateChecks -- Yes --> Success([Authorization Success])
  Success --> End([End])
  EvaluateChecks -- No --> CollectFirstError[set first_error if unset]
  CollectFirstError --> NextGroup{More auth_group?}
  NextGroup -- Yes --> ForEachGroup
  NextGroup -- No --> RaiseFirstError[raise first_error] --> End
```

> SVG rendering failed for this diagram.

## Diagram 3

```mermaid
sequenceDiagram
  participant Caller
  participant Auth
  participant Authorizer
  participant PrivChecker as check_privileges
  participant ListChecker as check_intersecting_lists
  participant ForbiddenError

  Caller->>Auth: check_auth_wrapper()
  Auth->>Authorizer: read authorizer (privileges, org_profiles, features)
  loop for each auth_group
    alt has PRIVILEGE
      Auth->>PrivChecker: check_privileges(event, privileges)
      PrivChecker-->>Auth: success or raise NoPrivExcpetion
      alt NoPrivExcpetion
        Auth->>ForbiddenError: create ForbiddenError and append to last_check_list
      end
    end
    alt has ORG_TYPE
      Auth->>ListChecker: check_intersecting_lists(required_org_types, user_org_types)
      ListChecker-->>Auth: success or raise NonIntListException
      alt NonIntListException
        Auth->>ForbiddenError: create ForbiddenError and append to last_check_list
      end
    end
    alt has FEATURE
      Auth->>ListChecker: check_intersecting_lists(required_features, user_features)
      ListChecker-->>Auth: success or raise NonIntListException
      alt NonIntListException
        Auth->>ForbiddenError: create ForbiddenError and append to last_check_list
      end
    end
    Auth-->>Caller: if any group success -> return success
  end
  alt no group succeeded
    Auth-->>Caller: raise first_error (ForbiddenError)
  end
```

> SVG rendering failed for this diagram.
