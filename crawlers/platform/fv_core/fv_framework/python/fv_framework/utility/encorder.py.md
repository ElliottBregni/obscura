# Diagram: fv_core/fv_framework/python/fv_framework/utility/encorder.py


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
flowchart TD
  Start([encoder(value)]) --> IsDate{Is value a datetime.date, datetime.datetime, or datetime.time?}
  IsDate -- Yes --> Iso[return value.isoformat()]
  IsDate -- No --> IsRange{Is value a DateTimeRange?}
  IsRange -- Yes --> RangeBounds{value.lower and value.upper?}
  RangeBounds -- Yes --> RangeTs[return [value.lower.timestamp(), value.upper.timestamp()]]
  RangeBounds -- No --> RangeNone[return [None, None]]
  IsRange -- No --> IsMock{Is value a unittest.mock.MagicMock?}
  IsMock -- Yes --> MockRet[return "MOCK"]
  IsMock -- No --> IsDecimal{Is value a decimal.Decimal?}
  IsDecimal -- Yes --> DecRet[return float(value)]
  IsDecimal -- No --> IsEnum{Is value an enum.Enum?}
  IsEnum -- Yes --> EnumRet[return value.value]
  IsEnum -- No --> IsDelta{Is value a datetime.timedelta?}
  IsDelta -- Yes --> Compute[hours = floor(value.seconds / 3600)\nminutes = floor(value.seconds / 60 % 60)]
  Compute --> Format[return formatted string "DD:HH:MM"]
  IsDelta -- No --> Error[raise BadRequestError(message, message, http_status=500)]
  Iso --> End([end])
  RangeTs --> End
  RangeNone --> End
  MockRet --> End
  DecRet --> End
  EnumRet --> End
  Format --> End
  Error --> End
```

> SVG rendering failed for this diagram.
