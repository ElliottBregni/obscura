# Diagram: fv_core/fv_framework/python/fv_framework/utility/Solution.py


> Auto-generated by Obscura crawlers

## Diagram 1

```mermaid
classDiagram
    class Solution {
        + stage : os.environ.get("AWS_STAGE")
        + config : botocore.config.Config(connect_timeout=300, read_timeout=300, retries={"max_attempts":4})
        + get_solution_by_id(event, solutionId)
    }
```

> SVG rendering failed for this diagram.

## Diagram 2

```mermaid
flowchart TD
    Start([get_solution_by_id(event, solutionId)]) --> Validate{Inputs valid?}
    Validate -- Yes --> Copy[Deepcopy event]
    Validate -- No --> Error[AssertionError / Invalid input]
    Copy --> SetParams[Set event.pathParameters = {"solution_id": solutionId}]
    SetParams --> CreateClient[Create lambda_client via boto3_client(region="us-east-1", config=Solution.config)]
    CreateClient --> Invoke[Invoke Lambda Function: Solution.stage-get_solution (RequestResponse)]
    Invoke --> ReadPayload[Read response["Payload"].read().decode("utf-8")]
    ReadPayload --> ParseJSON[parsed_response = json.loads(string_response)]
    ParseJSON --> CheckParsed{parsed_response present?}
    CheckParsed -- Yes --> ExtractStatus[status_code = int(parsed_response.get("statusCode", 500))]
    CheckParsed -- Yes --> ExtractBody[body = json.loads(parsed_response.get("body", "{}"))]
    CheckParsed -- No --> Nulls[status_code = None, body = None]
    ExtractStatus --> Return[Return (status_code, body)]
    ExtractBody --> Return
    Nulls --> Return
```

> SVG rendering failed for this diagram.
