# Diagram: fv_core/fv_framework/python/fv_framework/utility/InvokeEventHelper.py


> Auto-generated by Obscura crawlers

## Diagram 1

```mermaid
classDiagram
class InvokeEventHelper {
  <<dataclass,frozen>>
  +event: dict
  +request_id: str
  +body: dict
  +version: str
  +pathParameters: dict
  +queryStringParameters: dict
  +content_type: str
  +http_method: str
  +customer_org: int
  +actor_id: str
  +solutions: list
  +org_profiles: list
  +privs: list
  +sample_event_type: str
  +new_event: dict
  +features: list
  +use_sample: bool
  +get_full_payload(): dict
  +get_sample_admin_event(http_method, solutions, customer_id): dict
  +__repr__(): str
}
class EXAMPLE_ADMIN_EVENT {
  <<constant>>
}
class SAMPLE_EVENTS {
  <<constant dict>>
}
class Auth_Auth {
  <<external>>
  +Privilege
  +Feature
}
InvokeEventHelper ..> SAMPLE_EVENTS : reads
SAMPLE_EVENTS --> EXAMPLE_ADMIN_EVENT : contains
InvokeEventHelper ..> Auth_Auth : references Privilege/Feature
InvokeEventHelper o-- "new_event" : produces
InvokeEventHelper : modifies headers, body, pathParameters, queryStringParameters, requestContext
```

> SVG rendering failed for this diagram.

## Diagram 2

```mermaid
sequenceDiagram
participant Caller as Caller
participant Helper as InvokeEventHelper
participant Sample as get_sample_admin_event
participant Events as SAMPLE_EVENTS
participant Example as EXAMPLE_ADMIN_EVENT

Caller->>Helper: get_full_payload()
alt event provided
    Helper->>Helper: deepcopy(event)
    Helper->>Helper: set new_event["httpMethod"] = http_method
else no event
    Helper->>Sample: get_sample_admin_event(http_method, solutions=False, customer_id)
    Sample->>Events: SAMPLE_EVENTS.get(sample_event_type)
    Events->>Example: return EXAMPLE_ADMIN_EVENT JSON
    Sample->>Sample: set requestContext fields and authorizer (privileges, features, org_profiles, solutions, organization_id)
    Sample-->>Helper: return example_event
end
Helper->>Helper: ensure headers exist and update Accept with version
Helper->>Helper: set body, pathParameters, queryStringParameters, httpMethod, requestContext.httpMethod
Helper-->>Caller: return new_event
```

> SVG rendering failed for this diagram.
