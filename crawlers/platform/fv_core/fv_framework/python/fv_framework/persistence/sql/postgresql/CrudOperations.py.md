# Diagram: fv_core/fv_framework/python/fv_framework/persistence/sql/postgresql/CrudOperations.py


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
classDiagram
class CrudOperations {
  +update_persistable_from_result_row(map, persistable, result_row)
  +update_persistable_from_result_row_dirty(map, persistable, result_row)
  +modifyValueTypesForPersistence(values)
  +create_row_no_return(connector, table, map, persistable, query_builder_function)
  +create_row(connector, table, map, persistable, query_builder_function)
  +create_multiple_rows(connector, table, map, persistables, query_builder_function)
  +create_delete_row_ifnot_exists(connector, table, map, persistable, method)
  +update_multiple_rows(connector, table, map, persistables)
  +update_row(connector, table, map, persistable, return_all=True)
  +build_update_query(table, map, persistable, return_all=False)
  +build_read_query(table, map, persistable)
  +build_exists_query(table, map, persistable)
  +build_search_query(table, map, persistable, fuzzy_fields)
  +build_column_search_query(table, map, persistable, fuzzy_fields)
  +build_count_query(table, map, persistable, fuzzy_fields)
  +build_exception_query(table, map, persistable)
  +get_count(connector, table, map, persistable, fuzzy_fields)
  +validate_read_result(result)
  +read(connector, table, map, persistable)
  +delete(connector, table, map, persistable, soft_delete)
  +delete_multiple(connector, table, map, persistables, soft_delete=True)
  +execute_exists_query(connector, query, data)
  +exists(connector, table, map, persistable)
  +search_unique_fuzzy(connector, table, map, persistable, fuzzy_fields)
  +search_unique(connector, table, map, persistable)
  +fuzzy_paginated_search(connector, table, map, persistable, fuzzy_fields, order_by, page, page_size)
  +paginated_search(connector, table, map, persistable, order_by, page, page_size)
  +exception_paginated_search(connector, table, map, persistable, order_by, page, page_size)
  +fuzzy_paginated_count(connector, table, map, persistable, fuzzy_fields, page, page_size)
  +paginated_count(connector, table, map, persistable, page, page_size)
  +search(connector, table, map, persistable)
  +exception_search(connector, table, map, persistable)
  +search_for_data_ojects(connector, table, map, persistable, order_by)
  +get_sql_fields_from_map(map)
  +get_sql_columns_from_map(map)
  +get_where_column_condition(column_name, field_name, field_value, is_fuzzy)
  +get_where_clause(map, persistable, fuzzy_fields)
  +get_sql_exception_fields_from_map(map, table)
  +get_exception_where_clause(map, persistable, table)
}
class Freezeable
class Persistable {
  +is_valid()
  +get_dirty_fields()
  +set(field, value)
  +clear_fields()
  +get_key_field()
  +get(field_name)
  +id
  +STATE_DELETED
}
class ColumnMap {
  +get_column_names()
  +get_column_names_from_field_list(fields)
  +get_field(column_name)
}
class Connector {
  +establish_connection()
  +get_cursor()
}
class BadRequestError

CrudOperations --|> Freezeable
CrudOperations ..> Persistable : manipulates
CrudOperations ..> ColumnMap : reads
CrudOperations ..> Connector : executes queries
CrudOperations ..> BadRequestError : raises
Connector --> "1" Persistable : returns cursor/connection
ColumnMap ..> Persistable : maps fields
Persistable o-- "id" : has
BadRequestError <|-- ExceptionRenderer: <<exception>>
```

> SVG rendering failed for this diagram.
