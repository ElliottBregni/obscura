# Diagram: mocks/mock_apis/gov_decode_vin.py


> Auto-generated by Obscura crawlers

## Diagram 1

```mermaid
flowchart TD
    Event[Incoming Event] --> LH(lambda_handler)
    LH --> MakeResp[fv.aws.lambdas.make_response()]
    MakeResp --> Return[HTTP Response]
    subgraph Payload
        ResultsList[Results: List] --> CarObj[Car Object]
        CarObj --> Make[Make: "Chevrolet"]
        CarObj --> Model[Model: "Corvette"]
        CarObj --> ModelYear[ModelYear: str(datetime.now().year)]
    end
    LH --> Payload
```

> SVG rendering failed for this diagram.

## Diagram 2

```mermaid
classDiagram
    class LambdaHandler {
        +lambda_handler(event, context)
    }
    class ResponseFactory {
        +make_response(payload) 
    }
    class Car {
        +Make: str
        +Model: str
        +ModelYear: str
    }
    LambdaHandler --> ResponseFactory : uses
    LambdaHandler --> Car : returns
    ResponseFactory --> "List~Car~" : wraps
```

> SVG rendering failed for this diagram.
