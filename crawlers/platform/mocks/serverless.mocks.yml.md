# Diagram: mocks/serverless.mocks.yml


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
flowchart LR
  Client[Client] --> APIGW[API Gateway]
  APIGW --> |GET /vin/{vin}/decode| mock_vin_decoder["mock_vin_decoder\nhandler: mock_apis/decode_vin.lambda_handler\nname: ${opt:stage}-mock_vin_decoder"]
  APIGW --> |GET /gov-vin-decoder/{vin}/decode| mock_gov_vin_decoder["mock_gov_vin_decoder\nhandler: mock_apis/gov_decode_vin.lambda_handler\nname: ${opt:stage}-mock_gov_vin_decoder"]
  mock_vin_decoder --> Layers["Layers: ${self:custom.globalConfig.fv_layers}"]
  mock_gov_vin_decoder --> Layers
  mock_vin_decoder --> VPC["VPC: ${self:custom.globalConfig.vpc}"]
  mock_gov_vin_decoder --> VPC
  mock_vin_decoder -. Deny .-> IAM["IAM Role (statement: Deny logs:CreateLog/Stream/PutLogEvents for specific log-groups)"]
  mock_gov_vin_decoder -. Deny .-> IAM
  mock_vin_decoder --> Env["Environment vars\nAWS_STAGE, LOG_LEVEL, SERVICE, DD_*"]
  mock_gov_vin_decoder --> Env
  Env --> Datadog["Datadog config\nDD_SITE, DD_TRACE_ENABLED, DD_API_KEY_SECRET_ARN"]
  Datadog --> Secrets["SecretsManager ARN\narn:aws:secretsmanager:${aws:region}:${aws:accountId}:secret:${opt:stage}/datadog/credentials/lambda"]
  subgraph Packaging & Plugins
    PackagePatterns["package.patterns: excludes (.serverless, node_modules, __pycache__, venv, etc.)"]
    Plugins["plugins:\n- serverless-python-requirements\n- serverless-prune-plugin\n- serverless-domain-manager"]
  end
  APIGW --> Packaging & Plugins
  style IAM stroke:#d9534f,stroke-width:2px
  style Datadog stroke:#f0ad4e,stroke-width:1.5px
  style Layers stroke:#5bc0de,stroke-width:1.5px
```

> SVG rendering failed for this diagram.
