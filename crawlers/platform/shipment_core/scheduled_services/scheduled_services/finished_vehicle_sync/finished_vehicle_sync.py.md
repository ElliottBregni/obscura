# Diagram: shipment_core/scheduled_services/scheduled_services/finished_vehicle_sync/finished_vehicle_sync.py


> Auto-generated by Obscura crawlers

## Diagram 1

```mermaid
classDiagram
    class FinishedVehicleSync {
        - event: dict
        - context: dict
        - audit_refs: dict
        - aws_stage: str
        - request_id: str
        - process_limit: int
        - logger
        + __init__(event, context)
        + change_state(row_id, new_state)
        + invoke_lambda(name, payload, timeout=300, retries=4)
        + get_payload(notification, row_id, target_service, sync_type)
        + send_notification(solution, notification, row_id, target_service, sync_type=None)
        + get_claimed_events_query(identifier)
        + get_claiming_query(identifier)
        + change_state_non_finished_vehicles()
        + check_stage()
        + do_sync(identifier)
    end

    class Secrets {
        + __init__()
    }
    class FvDatabaseConnector {
        - connection
        - cursor
        + __init__(db_key, secret_name)
        + get_cursor()
    }
    class Audit {
        + __init__(duration, refs, function_name)
        + request_audit()
    }
    class fv_aws_lambdas {
        + get_request_id(event)
        + make_response(body, status, content_type="application/json")
    }
    class boto3_lambda_client {
        + invoke(FunctionName, InvocationType, Payload)
    }

    Secrets --|> FinishedVehicleSync : uses
    FvDatabaseConnector --|> FinishedVehicleSync : state_change_connector / connector
    Audit <-- FinishedVehicleSync : creates
    fv_aws_lambdas <-- FinishedVehicleSync : calls get_request_id, make_response
    boto3_lambda_client <-- FinishedVehicleSync : invokes lambda via invoke_lambda
```

> SVG rendering failed for this diagram.

## Diagram 2

```mermaid
flowchart LR
    A[lambda_handler(event, context)] --> B{extract identifier}
    B -->|requestContext.requestId| C[identifier from event]
    B -->|exception| D[uuid.uuid4() as identifier]
    C --> E[Instantiate FinishedVehicleSync(event, context)]
    D --> E
    E --> F[do_sync(identifier)]
    F --> G[check_stage()]
    G -->|invalid| H[raise BadRequestError]
    G -->|valid| I[start timer]
    I --> J[change_state_non_finished_vehicles()]
    J --> K[get_claiming_query(identifier) & execute]
    K --> L[get_claimed_events_query(identifier) & execute]
    L --> M[row = cursor.fetchone()]
    M --> N{row is not None?}
    N -->|no| O[if process_count>0 -> Audit.request_audit()]
    N -->|yes| P[notification = row.notification_payload]
    P --> Q{notification is not None?}
    Q -->|no| R[change_state(row.id, NO_NOTIFICATION) -> loop]
    Q -->|yes| S{notification contains shipment?}
    S -->|no| T[valid_shipment = False]
    S -->|yes| U[valid_shipment = True]
    U --> V{shipment.vins exists & len>0?}
    V -->|yes| W[sync_type = FINISHED_VEHICLE]
    V -->|no| X{shipment.equipment_numbers exists & len>0?}
    X -->|yes| Y[sync_type = EQUIPMENT]
    X -->|no| Z[sync_type = None]
    P --> AA{row.notification_type == "ShipmentCancel" OR (valid_shipment AND sync_type) OR row.target_service=="shipment"}
    AA -->|true| AB[change_state(row.id, PROCESSING) -> send_notification(...) -> loop]
    AA -->|false| AC{valid_shipment is False?}
    AC -->|true| AD[change_state(row.id, INVALID_SHIPMENT) -> loop]
    AC -->|false| AE{sync_type is None?}
    AE -->|true| AF[change_state(row.id, NO_VINS) -> loop]
    AE -->|false| AG[loop]
    AB --> AG
    AD --> AG
    AF --> AG
    AG --> M
    F -->|exception| AH[change_state(row.id, FAILURE) then raise ConflictError]
    O --> AI[return make_response({},200)]
```

> SVG rendering failed for this diagram.
