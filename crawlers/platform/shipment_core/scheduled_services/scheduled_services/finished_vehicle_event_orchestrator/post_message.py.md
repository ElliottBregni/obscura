# Diagram: shipment_core/scheduled_services/scheduled_services/finished_vehicle_event_orchestrator/post_message.py


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
flowchart TD
  A[lambda_handler(message, context, audit_refs)] --> B{establish_connection?}
  B -->|success| C[change_state(row_id, "ACTIVE PROCESSING")]
  C --> D[process_message(solution, organization, notification, target_service, sync_type, row_id, connector)]
  D --> E{http_code}
  E -->|200-299| F[change_state(row_id, State.COMPLETE)]
  E -->|409| G[change_state(row_id, State.NO_VINS)]
  E -->|other| H[change_state(row_id, State.FAILURE)]
  H --> I{http_code == 403}
  I -->|true| J[raise fv.error.ForbiddenError]
  I -->|false| K[raise fv.error.UnhandledException]
  B -->|failure / exception| L[record_event_log(row_id, ex, http_code)]
  L --> M[change_state(row_id, State.FAILURE)]
  M --> N[raise fv.error.ConflictError]
  D --> O{target_service in TargetServices?}
  O -->|entity| P[InboundTripLegEvent(...)._process(row_id, connector)]
  O -->|shipment| Q[InboundShipmentEvent(...)._process(row_id, connector)]
  O -->|other| R[raise fv.error.BadRequestError]
  subgraph DB
    S[connector: FvDatabaseConnector]
    T[get_cursor()] --> U[cursor.execute(query)]
  end
  C --> S
  L --> S
  F --> S
  G --> S
  H --> S
  A --> V[make_response({}, 200, application/json)]
```

> SVG rendering failed for this diagram.
