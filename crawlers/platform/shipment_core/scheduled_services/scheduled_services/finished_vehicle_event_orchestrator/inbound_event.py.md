# Diagram: shipment_core/scheduled_services/scheduled_services/finished_vehicle_event_orchestrator/inbound_event.py


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
classDiagram
class ABC
class InboundEvent <<abstract>> {
  +leg_event: dict
  +solution: str
  +organization: int
  +leg
  +events
  +trace_id
  +connector
  +logger
  +entity_url
  +tripleg_url
  +shipment_url
  +authorization
  +__init__(solution, organization, trip_leg_event, connector)
  +retry_session(retries, session=None, backoff_factor=0.3, status_forcelist=[], session_urls=[])
  +fail_on_error(retval)
  +process(retval=[])
  +build_endpoint_event(endpoint, action, timemark)
  #_sessions_to_mount_on_retry()
}
InboundEvent --|> ABC

class Secrets {
  +get_secret(name)
}
class requests {
  +Session()
}
class SuccessOrFail {
  <<enum>>
  +SUCCESS
  +FAILURE
}
class fv_error {
  +UnhandledException(msg, cause)
}
class fv_log {
  +get_log_level(str)
}

InboundEvent ..> Secrets : "configuration = Secrets().get_secret(...)"
InboundEvent ..> requests : "uses Session, HTTPAdapter"
InboundEvent ..> SuccessOrFail : "checks action.status"
InboundEvent ..> fv_error : "raises UnhandledException"
InboundEvent ..> fv_log : "get_log_level()"
```

> SVG rendering failed for this diagram.
