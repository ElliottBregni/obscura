# Diagram: shipment_core/scheduled_services/config/config.dev.yml


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
flowchart TD
    Start([Start]) --> CLI{Parse CLI args}
    CLI --> Config[Load configuration (mcp-config.json / env)]
    Config --> Init[Initialize crawlers & agents]
    Init --> Repos{Repositories list}
    Repos -->|for each| Fetch[Clone / fetch repository]
    Fetch --> Crawl[Run crawler modules]
    Crawl --> Parse[Parse files & extract data]
    Parse --> Store[Persist results (repos/, outputs/)]
    Store --> End([End])
    Fetch -->|errors| Error([Error])
    Error --> End
```

> SVG rendering failed for this diagram.
