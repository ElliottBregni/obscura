# Diagram: shipment_core/scheduled_services/serverless.scheduled.yml


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
flowchart TD
  Provider[AWS - python3.13 (arm64)\nservice: sched-services]:::provider
  IAM[ IAM Role\n- lambda:InvokeFunction\n- secretsmanager:GetSecretValue\n- sns:CreateTopic\n- sns:Publish\n- sqs:SendMessage\n(Deny logs:CreateLog* for some log groups)]:::iam
  VPC[VPC: custom.globalConfig.vpc]:::vpc
  Layers[Shared Layers\ncustom.globalConfig.fv_layers]:::layer
  Env[Environment Variables\nLOG_LEVEL, WATCH_RATE, PROCESS_LIMIT,\nDD_* etc.]:::env

  subgraph Functions
    F_finished_sync[finished_vehicle_sync\nreservedConcurrency:10\ntimeout:900]:::func
    F_finished_declaim[finished_vehicle_declaim\nreservedConcurrency:1\ntimeout:900]:::func
    F_post_event[post_event\nreservedConcurrency:10(default)]:::func
    F_add_unprocessed[add_unprocessed_event\nreservedConcurrency:5]:::func
    F_event_dispatcher[event_dispatcher\nreservedConcurrency:3\ntimeout:900]:::func
  end

  Provider --> IAM
  Provider --> VPC
  Provider --> Env
  Provider --> Layers

  IAM --> F_finished_sync
  IAM --> F_finished_declaim
  IAM --> F_post_event
  IAM --> F_add_unprocessed
  IAM --> F_event_dispatcher

  Layers --> F_finished_sync
  Layers --> F_finished_declaim
  Layers --> F_post_event
  Layers --> F_add_unprocessed
  Layers --> F_event_dispatcher

  Env --> F_finished_sync
  Env --> F_finished_declaim
  Env --> F_post_event
  Env --> F_add_unprocessed
  Env --> F_event_dispatcher

  %% Event sources / schedules
  Schedule1[Schedule: rate(1 minute) x4]:::event
  Schedule30[Schedule: rate(30 minutes)]:::event
  Schedule1b[Schedule: rate(1 minute)]:::event

  Schedule1 --> F_finished_sync
  Schedule30 --> F_finished_declaim
  Schedule1b --> F_event_dispatcher

  classDef provider fill:#f9f,stroke:#333,stroke-width:1px;
  classDef iam fill:#ffe4b5,stroke:#333;
  classDef vpc fill:#e6f7ff,stroke:#333;
  classDef layer fill:#f0f0f0,stroke:#333;
  classDef env fill:#e8ffe8,stroke:#333;
  classDef func fill:#fff,stroke:#333,stroke-width:1px;
  classDef event fill:#fff5e6,stroke:#333,stroke-dasharray: 5 5;
```

> SVG rendering failed for this diagram.
