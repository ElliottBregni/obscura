# Diagram: shipment_core/scheduled_services/slack.sh


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
flowchart TD
  Start([script start]) --> Call[post_to_slack($1, $2)]
  Call --> Format["format SLACK_MESSAGE = \"```\" + $2 + \"```\""]
  Format --> Payload["payload = {\"text\": SLACK_ICON + ' ' + SLACK_MESSAGE}"]
  Payload --> Curl["curl -X POST --data payload to $1"]
  Curl --> Exit["exit 0"]
  style Start fill:#f9f,stroke:#333,stroke-width:2px
  style Exit fill:#afa,stroke:#333,stroke-width:2px
```

> SVG rendering failed for this diagram.
