# Diagram: shipment_core/shipment_trip_plan_service/config/config.test.yml


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
classDiagram
    class crawl {
        +main()
        +generate()
    }
    class crawlers {
        +Crawler
        +run()
    }
    class "copilot_models.py" {
        +guard_automation()
    }
    class sync {
        +sync_all()
    }
    class backend {
        +start()
        +handle_request()
    }
    class agents {
        +Agent
        +dispatch()
    }
    class skills {
        +crawl-diagrams
    }
    class "package.json" {
        +scripts
    }
    crawl --> crawlers : uses
    crawl --> "copilot_models.py" : calls
    crawl --> skills : invokes
    sync --> backend : coordinates
    backend --> agents : manages
    skills --> "package.json" : configured_by
    crawlers --> backend : sends_data_to
```

> SVG rendering failed for this diagram.
