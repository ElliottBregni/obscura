# Diagram: shipment_core/shipment_trip_plan_service/config/config.qa.yml


> Auto-generated by Obscura crawlers

## Diagram 1

```mermaid
flowchart TD
    A[User Request] --> B[Crawl CLI (crawl.py)]
    B --> C[Crawler Manager (crawlers.py)]
    C --> D{Crawler Implementations}
    D -->|reads repo| E[Git Crawlers (crawlers/)]
    D -->|parses| F[HTML/HTTP Crawlers]
    C --> G[Agents (agents/)]
    G --> H[Background Tasks]
    B --> I[Sync (sync.py)]
    I --> J[Backend (backend/)]
    J --> K[Storage / Repos (repos/)]
    K --> L[Output / Examples (examples/)]
    style A fill:#f9f,stroke:#333,stroke-width:1px
    style B fill:#bbf,stroke:#333,stroke-width:1px
    style J fill:#bfb,stroke:#333,stroke-width:1px
```

> SVG rendering failed for this diagram.

## Diagram 2

```mermaid
classDiagram
    class CrawlCLI {
        +run()
        +parse_args()
    }
    class CrawlerManager {
        +discover_crawlers()
        +run_crawlers()
    }
    class Crawler {
        +crawl()
        +parse()
    }
    class Agent {
        +start()
        +stop()
        +handle_task()
    }
    class Sync {
        +synchronize()
        +resolve_conflicts()
    }
    class Backend {
        +store()
        +retrieve()
    }
    CrawlCLI --|> CrawlerManager : uses
    CrawlerManager <-- Crawler : manages >
    CrawlerManager --> Agent : delegates to
    CrawlCLI --> Sync : invokes
    Sync --> Backend : persists to
    Backend o-- "many" Repos : contains
    note right of Crawler : implementations in crawlers/ directory
```

> SVG rendering failed for this diagram.
