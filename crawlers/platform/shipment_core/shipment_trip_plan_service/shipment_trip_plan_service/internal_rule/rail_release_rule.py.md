# Diagram: shipment_core/shipment_trip_plan_service/shipment_trip_plan_service/internal_rule/rail_release_rule.py


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
flowchart TD
  A[Lambda Trigger<br/>(event, context, audit_refs)] --> B[Get trailerNumber<br/>(get_query_parameter)]
  B --> C[Build SQL query<br/>(cursor.mogrify)]
  C --> D[Execute query<br/>(cursor.execute)]
  D --> E[Fetch results<br/>(cursor.fetchall)]
  E --> F{response empty?}
  F -- No --> G[Iterate shipments]
  F -- Yes --> L[Return empty response]
  subgraph per_shipment [Process each shipment]
    G --> H[deepcopy(event) -> event_copy]
    H --> I[Extract header fields<br/>creator_id, carrier_id, shipment_id]
    I --> J[Select last location<br/>from locations]
    J --> K[Populate event_copy.headers<br/>X-WSS-FvShipmentId]
    K --> M[Populate event_copy.body<br/>status, header, location, references]
    M --> N[Invoke proxy_shipment_status lambda<br/>(invoke_lambda Event)]
    N --> O[Append shipment_id to ret]
  end
  G --> per_shipment
  O --> P[After loop]
  P --> Q[Return make_response(ret)]
  L --> Q
```

> SVG rendering failed for this diagram.
