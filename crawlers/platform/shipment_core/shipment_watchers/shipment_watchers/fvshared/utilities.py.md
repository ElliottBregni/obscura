# Diagram: shipment_core/shipment_watchers/shipment_watchers/fvshared/utilities.py


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
flowchart LR
    A[location_resolver] -->|calls| B[invoke_lambda("locations-get")]
    A -->|returns| C[location record or raises BadRequestError]
    D[resolve_location / resolve_locations] --> A
    E[get_stop_location] -->|calls| D
    F[get_intersections] -->|calls| invoke_lambda("locations-intersections")
    F -->|parses| G[intersections dict]
    H[get_waypoints_in_route] -->|calls per stop| E
    H -->|computes| I[waypoints, stop_waypoints]
    J[point_in_poly2D] -->|returns| K[inside (bool)]
    L[compute_distance] -->|returns| M[float distance]
    N[format_fv_stop_time] -->|formats| O[ISO Z time string]
    P[json_serial] -->|serializes| Q[datetime/DateTimeRange/MagicMock or raises]
    R[build_response] -->|wraps| S[statusCode, body, headers]
    T[invoke_lambda] -->|uses boto3 lambda.invoke| U[parsed_response]
    V[get_progress] -->|validates| W[returns 0-100 percent]
    X[valid_stops] -->|validates stops| Y[(True/False, error)]
    X -->|uses| Z[get_arrival_times]
    Z -->|parses| AA[convert_status_date_time -> datetime UTC]
    AB[make_v2_response] -->|calls| AC[make_v2_error_response] 
    AD[get_json_delta] -->|computes| AE[delta dict]
    style A fill:#f9f,stroke:#333,stroke-width:1px
    style H fill:#ff9,stroke:#333,stroke-width:1px
    style X fill:#9ff,stroke:#333,stroke-width:1px
```

> SVG rendering failed for this diagram.
