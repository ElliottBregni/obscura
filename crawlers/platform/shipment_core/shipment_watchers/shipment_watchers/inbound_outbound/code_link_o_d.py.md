# Diagram: shipment_core/shipment_watchers/shipment_watchers/inbound_outbound/code_link_o_d.py


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
flowchart LR
  subgraph AWS_Lambda
    LH[lambda_handler(event, context, audit_refs)]
  end

  subgraph DB["Databases"]
    DB1[(shipper_locations DB)]
    DB2[(shipper_locations TRACKING DB)]
  end

  subgraph Calls
    GIL[get_incorrectly_linked_locs(cursor_tracking, origin)]
    HIL[handle_incorrect_loc(cursor, example_event, context, loc, origin)]
    HLG[handle_location_get_create(example_event, context, tender_data, shipper_data, loc)]
    INV_GET[invoke_lambda("locations-get")]
    INV_POST[invoke_lambda("locations-post")]
    UPD[update_shipment_stop_location(cursor, shipment_id, loc_id, origin)]
  end

  LH --> DB1
  LH --> DB2
  LH --> GIL
  GIL -->|returns incorrect_dests & incorrect_origins| LH
  LH -->|iterates| HIL
  HIL --> HLG
  HLG --> INV_GET
  INV_GET -->|if found| HLG
  INV_GET -->|if not found| INV_POST
  INV_POST -->|201/409/4xx| HLG
  HLG --> UPD
  UPD --> DB1
  UPD -->|updates shipments & shipment_stops| DB2

  classDef db fill:#f9f,stroke:#333,stroke-width:1px;
  class DB1,DB2 db
```

> SVG rendering failed for this diagram.
