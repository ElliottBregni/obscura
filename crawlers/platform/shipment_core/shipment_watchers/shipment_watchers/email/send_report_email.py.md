# Diagram: shipment_core/shipment_watchers/shipment_watchers/email/send_report_email.py


> Auto-generated by Obscura crawlers

## Diagram 1

```mermaid
classDiagram
class lambda_handler {
  +event
  +context
  +audit_refs
  +schedule
  +success
  +error
  +tb
  +process_metadata
  +return make_response({})
}
class update_process_metadata {
  +process_metadata
  +new_process_state
  +returns process_metadata
}
class DB_CONN {
  +cursor
}
class pbi_utilities {
  +handle_export_lambda()
  +build_email_link()
  +create_email_body()
  +handle_schedule()
}
class email_sender {
  +send_email()
}
class email_db {
  +update_next_ts()
  +update_process_state()
  +update_process_metadata()
  +insert_into_audit_log_table()
}
class fvshared {
  <<module>>
  +constants
}
class fv_error {
  +HandledException
  +BadRequestError
}
lambda_handler --> pbi_utilities : calls
lambda_handler --> email_sender : sends email via
lambda_handler --> email_db : updates DB via
lambda_handler --> DB_CONN : uses cursor from
lambda_handler --> update_process_metadata : calls
lambda_handler --> fvshared : reads constants
lambda_handler --> fv_error : raises/errors
update_process_metadata --> fvshared : checks constants
email_db --> DB_CONN : uses cursor
pbi_utilities --> DB_CONN : (indirect in workflow)
fvshared <|-- fv_error : related module
```

> SVG rendering failed for this diagram.

## Diagram 2

```mermaid
sequenceDiagram
participant Client as Event
participant Lambda as lambda_handler
participant PBI as pbi_utilities
participant EmailSender as email_sender
participant EmailDB as email_db
participant DB as DB_CONN
participant Utils as update_process_metadata
participant FV as fvshared
participant Err as fv_error

Client->>Lambda: invoke(event, context, audit_refs)
Lambda->>DB: get cursor from DB_CONN
Lambda->>PBI: handle_export_lambda(event, report_number, filter_set_id)
alt export success (url)
  PBI-->>Lambda: return url
  Lambda->>PBI: build_email_link(report_number, filter_set_id, org_id)
  PBI-->>Lambda: website_link
  Lambda->>PBI: create_email_body(body, website_link, sender_email, download_url)
  PBI-->>Lambda: email_body
  Lambda->>EmailSender: send_email(recipient, email_body)
  EmailSender-->>Lambda: sent
  Lambda->>PBI: handle_schedule(pattern, details)
  PBI-->>Lambda: next_send_ts
  Lambda->>EmailDB: update_next_ts(cursor, next_send_ts, schedule_id)
  Lambda->>EmailDB: update_process_state(cursor, schedule_id, COMPLETED)
  Lambda->>Utils: update_process_metadata(process_metadata, COMPLETED)
else export failure
  PBI-->>Lambda: raise HandledException / error
  Lambda->>Lambda: check process_metadata failure_count
  alt second failure -> raise HandledException
    Lambda->>Err: raise HandledException
  else record failure
    Lambda->>EmailDB: update_process_state(cursor, schedule_id, FAILURE)
    Lambda->>Utils: update_process_metadata(process_metadata, FAILURE)
    opt max retries reached
      Lambda->>PBI: handle_schedule(pattern, schdule_details_copy)
      PBI-->>Lambda: next_send_ts
      Lambda->>EmailDB: update_next_ts(cursor, next_send_ts, schedule_id)
    end
end
Lambda->>EmailDB: update_process_metadata(cursor, schedule_id, process_metadata)
Lambda->>EmailDB: insert_into_audit_log_table(cursor, stage, schedule_id, audit_log_status, schedule, error)
Lambda-->>Client: return make_response({})
```

> SVG rendering failed for this diagram.
