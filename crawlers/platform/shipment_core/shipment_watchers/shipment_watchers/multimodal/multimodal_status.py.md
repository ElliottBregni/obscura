# Diagram: shipment_core/shipment_watchers/shipment_watchers/multimodal/multimodal_status.py


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
flowchart TD
  Start([Start]) --> EstablishDB[/"DB_CONN.establish_connection()"/]
  EstablishDB --> ExecuteQuery[/Execute SELECT query via cursor.mogrify/]
  ExecuteQuery --> CursorExecute["cursor.execute(query)"]
  CursorExecute --> FetchAll["all_data = cursor.fetchall()"]
  FetchAll --> BuildParentData["For each row: build parent_data"]
  BuildParentData --> SelectChild{"child with status in<br/>[delivered,in_transit,arrived,asset_pending]? "}
  SelectChild -->|yes| AddToUpdate[/"to_add = {'child': child, 'parent_data': parent_data}/"]
  SelectChild -->|no| AddFirstChild[/"to_add = {'child': data.children[0], 'parent_data': parent_data}/"]
  AddToUpdate --> AppendUpdate["update_data.append(to_add)"]
  AddFirstChild --> AppendUpdate
  AppendUpdate --> PrepareLoops["Initialize continued=0, updated=0, s_ids=[]"]
  PrepareLoops --> ForEachRow[/"for row_to_update in update_data"/]
  ForEachRow --> ExtractVars["Extract status, exception, child,<br/>parent_status, parent_exception,<br/>parent_active_child,..."]
  ExtractVars --> FinalLegOrStatus{"is_final_leg(row_to_update) OR<br/>status NOT IN [delivered,arrived]?"}
  FinalLegOrStatus -->|no| SkipUpdate["continued += 1\ncontinue"]
  FinalLegOrStatus -->|yes| CompareParent{"status==parent_status AND<br/>exception==parent_exception AND<br/>child==parent_active_child?"}
  CompareParent -->|yes| SkipUpdate
  CompareParent -->|no| DoUpdate["updated += 1\ns_ids.append(shipment_id)"]
  DoUpdate --> NormalizeNulls["Normalize 'None' strings -> None"]
  NormalizeNulls --> BuildUpdateQuery[/cursor.mogrify(UPDATE shipments SET ...)/]
  BuildUpdateQuery --> CursorExecuteUpdate["cursor.execute(update_query)"]
  CursorExecuteUpdate --> ContinueLoop[/"continue loop"/]
  ContinueLoop --> EndLoop([End loop])
  EndLoop --> ReturnResp[/return fv.aws.lambdas.make_response({"updateData": {}})/]
  SkipUpdate --> ContinueLoop
  Start --> EndLoop1([ ])
```

> SVG rendering failed for this diagram.
