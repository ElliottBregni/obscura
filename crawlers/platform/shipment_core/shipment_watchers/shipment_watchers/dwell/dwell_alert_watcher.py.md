# Diagram: shipment_core/shipment_watchers/shipment_watchers/dwell/dwell_alert_watcher.py


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
flowchart TD
  A[lambda_handler(event, context)] -->|calls| B[send_dwell_notification_alerts(event, context)]
  B --> C[DB_CONN.establish_connection()]
  C --> D[update_shipment_message_queue(cursor)]
  B --> E[get_pending_messages(cursor)]
  E --> F{for each message}
  F --> G[should_dwell_alert_be_sent(cursor, message.id)]
  G --> H{check expires_at}
  H -->|expired| I[update_message_state(EXPIRED)]
  H -->|not expired| J{destination_actual_arrival && no departure && within delivery window && asset_id && active && d1_id is None}
  J -->|true| K[get_shipment(cursor, shipment_id)]
  K --> L[get alert level from message.details]
  L --> M[send_notification(cursor, event, shipment, level, message)]
  M --> N[build event body with context]
  N --> O[invoke_lambda("create_notification")]
  M --> P[update_message_alert_level(cursor, message)]
  P --> Q{level < 3}
  Q -->|true| R[create_shipment_message with level+1, new_sent_at, expires_at]
  Q --> S[update_message_state(SENT)]
  J -->|false| T[update_message_state(CANCELED)]
  F -->|exception| U[update_message_state(EXCEPTION) with stack_trace]

  subgraph UsersFlow
    M --> V[get_users(cursor, message)]
    V --> W[get_dwell_alert_users(cursor, shipment_id)]
    W --> X[filter users by check_user_schedule & check_user_alert_level]
    X --> Y[format scheduled_users list]
    X -->|calls| Z[check_user_schedule(user)]
    X -->|calls| AA[check_user_alert_level(message, user)]
  end

  subgraph Utils
    O --> AB[get_shipment_url(shipment_id)]
    N --> AC[get_message_alert_minutes(level)]
    N --> AD[get_destination_window(window, timezone_name)]
    AD --> AE[timezone conversion, format window strings]
  end
```

> SVG rendering failed for this diagram.
