# Diagram: shipment_core/shipment_watchers/shipment_watchers/reprocess/reprocess_references.py


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
flowchart LR
    A[Start] --> B[Load SECRETS.get_secret(Database)]
    B --> C[Open DB connection (fv.db.get_connection, autocommit=True)]
    C --> D[Open cursor (fv.db.get_cursor)]
    D --> E[get_refs_to_reprocess(cursor)]
    E --> F[refs (list of rows with shipment_id, reference)]
    F --> G[unique_shipment_ids = {row.shipment_id for row in refs}]
    G --> H[get_all_refs_shipment_ids(cursor, unique_shipment_ids)]
    H --> I[db_refs: {shipment_id: ship_refs_list}]
    I --> J[build_db_ref_dict(db_refs)]
    J --> K[db_ref_dict: {shipment_id: [db_ref_dicts]}]
    K --> L{For each ref in refs}
    L --> M[shared_funcs.handle_specific_ref(cursor, ref.reference, ref.shipment_id, db_ref_dict[ref.shipment_id])]
    M --> N[Processed reference]
    N --> O[Loop until all refs processed]
    O --> P[Close cursor & connection]
    P --> Z[End]
```

> SVG rendering failed for this diagram.
