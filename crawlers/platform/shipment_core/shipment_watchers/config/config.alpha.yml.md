# Diagram: shipment_core/shipment_watchers/config/config.alpha.yml


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
classDiagram
    class Crawl {
        +run()
        +parse_config()
    }
    class CrawlersModule {
        +discover()
        +fetch()
    }
    class Backend {
        +start()
        +handle_request()
    }
    class Agents {
        +spawn()
        +schedule()
    }
    class Skills {
        +load()
        +execute()
    }
    class Sync {
        +sync()
    }
    class Repo {
        +files
    }
    Crawl --> CrawlersModule : uses
    Crawl --> Backend : sends data to
    Backend --> Agents : manages
    Backend --> Skills : loads
    Backend --> Sync : coordinates
    Repo ..> Crawl : contains
    Repo ..> CrawlersModule : contains
    Repo ..> Backend : contains
    Agents <.. Skills : may call
    CrawlersModule --> "crawlers (package)" : implements
    Skills --> "skills (dir)" : provides
    Agents --> "agents (dir)" : runs
```

> SVG rendering failed for this diagram.
