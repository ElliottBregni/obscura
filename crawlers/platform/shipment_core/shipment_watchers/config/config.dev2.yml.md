# Diagram: shipment_core/shipment_watchers/config/config.dev2.yml


> Auto-generated by Obscura crawlers

## Diagram 1

```mermaid
classDiagram
    class FileEntry {
        +Path repo_relative
        +Path absolute
        +str extension
        +int size
    }
    class CrawlResult {
        +str repo_name
        +Path repo_path
        +list[FileEntry] files
        +int skipped_dirs
        +int skipped_files
        +int skipped_size
        +int skipped_ext
        +total_discovered()
    }
    class CrawlersModule {
        +crawl_repo(repo_path, extensions=None, skip_dirs=None, max_size=...)
        +split_mermaid_diagrams(text)
        +generate_stub(entry)
        +write_output(result, output_dir, dry_run=False, workers=...)
    }
    FileEntry <|-- CrawlResult : contains
    CrawlersModule --> CrawlResult : returns
    CrawlersModule --> FileEntry : processes
    note right of CrawlersModule: helpers: _run_copilot_for_mermaid\n_strip_markdown_fences\n_trim_leading_prose\n_render_svg_with_mmdc\n_render_svg_with_kroki
```

> SVG rendering failed for this diagram.

## Diagram 2

```mermaid
flowchart TD
    A[Crawl start: crawl_repo(repo_path)] --> B[Walk filesystem]
    B --> C{Filter files}
    C -->|skip dirs/files/size/ext| D[Record skipped counts]
    C -->|valid source files| E[Create FileEntry list]
    E --> F[For each FileEntry: generate_stub]
    F --> G[_run_copilot_for_mermaid(code)]
    G --> H[Raw Mermaid text]
    H --> I[split_mermaid_diagrams(text)]
    I --> J{For each diagram}
    J --> K[_render_svg_with_mmdc]
    K -->|success| L[Embed SVG + fenced Mermaid into .md]
    K -->|fail| M[_render_svg_with_kroki]
    M -->|success| L
    M -->|fail| N[Record render failure; include Mermaid only]
    L --> O[Write markdown to output/<repo>/...]
    N --> O
    O --> P[Update INDEX.md]
    P --> Q[Done]
```

> SVG rendering failed for this diagram.
