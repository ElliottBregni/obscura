# Diagram: shipment_core/shipment_service/serverless.shipment_public.yml


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
classDiagram
    class Service {
        +string name = "public-shipment"
        +string frameworkVersion = "3"
        +package patterns...
    }
    class Provider {
        +name: aws
        +runtime: python3.13
        +architecture: arm64
        +timeout: 300
        +memorySize: ${self:custom.globalConfig.memorySize}
        +endpointType: ${self:custom.globalConfig.endpointType.${opt:stage},"REGIONAL"}
    }
    class Environment {
        +AWS_STAGE
        +ACCOUNT_ID
        +SPECIFIED_ENV
        +SERVICE
        +DD_* variables...
    }
    class VPC { +vpc: ${self:custom.globalConfig.vpc} }
    class IAM {
        +managedPolicies: ${self:custom.globalConfig.defaultManagedPolicies}
        +statements: [SecretsManager, LambdaInvoke, SSM GetParameter, KMS Decrypt, Deny Logs Create/Put]
    }
    class Plugins { +serverless-prune-plugin }
    class Custom { +datadog, prune, globalConfig }
    class Package { +patterns: [exclusions...] }

    class public_get_shipment     { +name, handler: shipment_service/public/handlers.handler_get_shipment_details, layers }
    class public_get_trip         { +name, handler: shipment_service/public/handlers.handler_get_trip_details, layers }
    class public_get_heat_map     { +name, handler: shipment_service/public/handlers.handler_get_heat_map, layers }
    class public_get_organization{ +name, handler: shipment_service/public/handlers.handler_get_organization, layers }
    class public_get_shipment_modes{+name, handler: shipment_service/public/handlers.handler_get_shipment_modes, layers }
    class public_get_countries   { +name, handler: shipment_service/public/handlers.handler_get_countries, layers }
    class public_get_lads        { +name, handler: shipment_service/public/handlers.handler_get_lads, layers }

    class Handlers { +module: shipment_service/public/handlers.py; +functions: handler_get_* }

    Service --> Provider
    Provider --> Environment
    Provider --> VPC
    Provider --> IAM
    Service --> Plugins
    Service --> Custom
    Service --> Package

    Service --> public_get_shipment
    Service --> public_get_trip
    Service --> public_get_heat_map
    Service --> public_get_organization
    Service --> public_get_shipment_modes
    Service --> public_get_countries
    Service --> public_get_lads

    public_get_shipment --> Handlers : handler_get_shipment_details
    public_get_trip --> Handlers : handler_get_trip_details
    public_get_heat_map --> Handlers : handler_get_heat_map
    public_get_organization --> Handlers : handler_get_organization
    public_get_shipment_modes --> Handlers : handler_get_shipment_modes
    public_get_countries --> Handlers : handler_get_countries
    public_get_lads --> Handlers : handler_get_lads

    IAM --> "SecretsManager, SSM, KMS, DenyLogs" 
    Environment --> "Datadog config and secrets ARNs"
    Custom --> "globalConfig file: ${env:FV_PATH}/config.global.yml"
```

> SVG rendering failed for this diagram.
