# Diagram: shipment_core/shipment_service/deploy.sh


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
flowchart LR
  Start([Start]) --> Loop{For each dir in ./shipment_service/*}
  Loop -->|is directory| CallDeploy[Call deploy(dir, stage, region)]
  Loop -->|not directory| LoopEndLoop((continue))
  CallDeploy --> DeploySub
  subgraph deploy(dir, stage, region)
    D1[cd $1]
    D2[rm .env]
    D3[append "STAGE=$2" to .env]
    D4[append "REGION=$3" to .env]
    D5[echo "Deploying $1 to stage $2 in region $3..."]
    D6[sls deploy --stage $2 --region $3 --verbose]
    D1 --> D2 --> D3 --> D4 --> D5 --> D6
  end
  D6 --> Loop
  Loop --> End([End])
  style DeploySub fill:none,stroke:none
  %% commented-out block
  NoteComment[/"Commented-out: loop over ./src/services/* and an extra deploy call"/]:::note
  Loop --- NoteComment
  classDef note fill:#fff3cd,stroke:#f0ad4e,color:#663c00,stroke-width:1px;
```

> SVG rendering failed for this diagram.
