# Diagram: shipment_core/shipment_service/ng_val/scripts/filters/ng_auto_val_GET_filters.py


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
flowchart TD
  Start([Start]) --> ParseArgs{Parse CLI args}
  ParseArgs -->|stage provided| SetStage[Set stage variable]
  SetStage --> CheckLagging[Is stage in ["prod"?]]
  CheckLagging -- yes --> Abort[Print "CANNOT TEST nextgen on stage {stage}" and Exit]
  CheckLagging -- no --> Configure[Configure lambda_url, ng_base_path, oauth_url]
  Configure --> SetLambdaFile[Set LAMBDA_FILE = "ng_auto_val_GET_filters.{stage}.json"]
  SetLambdaFile --> RemoveOld[If file exists: remove LAMBDA_FILE]
  RemoveOld --> BuildActions[Build actions dict with oauth keys]
  BuildActions --> GetEnvValues[auto_endpoints.get_env_value(...) for grant_type, client_id, username, password]
  GetEnvValues --> PostOAuth[Call auto_endpoints.validate_post_oauth_request(actions)]
  PostOAuth --> StoreToken[Store access_token in actions.oauth.access_token]
  StoreToken --> GET_Status[Call auto_endpoints.validate_get_data_oauth for /status_filters]
  GET_Status --> GET_Exception[Call auto_endpoints.validate_get_data_oauth for /exception_filters]
  GET_Exception --> GET_Type[Call auto_endpoints.validate_get_data_oauth for /type_filters]
  GET_Type --> End([End])
```

> SVG rendering failed for this diagram.
