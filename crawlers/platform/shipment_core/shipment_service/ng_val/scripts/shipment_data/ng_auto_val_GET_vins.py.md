# Diagram: shipment_core/shipment_service/ng_val/scripts/shipment_data/ng_auto_val_GET_vins.py


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
flowchart LR
  Start([Start]) --> ParseArgs[/"Parse CLI args\n--stage required"/]
  ParseArgs --> ToLower[/"stage = args.stage.lower()"/]
  ToLower --> StageDecision{stage value}
  StageDecision -->|prod-b| Prod[/"lambda_url=https://data-b.freightverify.com\ncg_base_path=shipping\nng_base_path=shipping-ng\noauth_url=https://login.freightverify.com/oauth/token"/]
  StageDecision -->|staging| Staging[/"lambda_url=https://data-s.freightverify.com\ncg_base_path=shipping\nng_base_path=shipping-ng\noauth_url=https://login-s.freightverify.com/oauth/token"/]
  StageDecision -->|test| Test[/"lambda_url=https://data-t.freightverify.com\ncg_base_path=shipping\nng_base_path=shipping-ng\noauth_url=https://login-t.freightverify.com/oauth/token"/]
  StageDecision -->|other| Other[/"lambda_url=https://data-s.freightverify.com\ncg_base_path=stage\nng_base_path=shipping-ng-<stage>\noauth_url=https://login-s.freightverify.com/oauth/token"/]
  Prod --> SetFile[LABEL:"LAMBDA_FILE = ng_auto_val_GET_vins.<stage>.json"]
  Staging --> SetFile
  Test --> SetFile
  Other --> SetFile
  SetFile --> RemoveIfExists{"if os.path.exists(LAMBDA_FILE)"}
  RemoveIfExists -->|yes| RemoveFile[/os.remove(LAMBDA_FILE)/]
  RemoveIfExists -->|no| SkipRemove([skip remove])
  RemoveFile --> BuildActions
  SkipRemove --> BuildActions
  BuildActions["Build actions dict\nactions['oauth'] = {}"] --> PopulateOAuth["Populate oauth fields\n- oauth_url = oauth_url\n- grant_type via auto_endpoints.get_env_value(AUTO_VALIDATE_OAUTH_GRANT_TYPE)\n- client_id via env\n- username via env\n- password via env"]
  PopulateOAuth --> SetGetVins["set actions['oauth']['get_vins'] = f\"{lambda_url}/{ng_base_path}/vins\""]
  SetGetVins --> PrintAuthStep["print: Posting to Auth0 endpoint for a token"]
  PrintAuthStep --> CallValidatePost["call auto_endpoints.validate_post_oauth_request(actions, heading, 2.0, verbose, LAMBDA_FILE)"]
  CallValidatePost --> StoreToken["store oauth_token in actions['oauth']['access_token']"]
  StoreToken --> PrintToken["if verbose: print token JSON"]
  PrintToken --> PrintGetVinsStep["print: Getting user associated VINs"]
  PrintGetVinsStep --> CallValidateGet["call auto_endpoints.validate_get_vins_oauth(actions, heading, 2.0, verbose, LAMBDA_FILE)"]
  CallValidateGet --> End([End])
```

> SVG rendering failed for this diagram.
