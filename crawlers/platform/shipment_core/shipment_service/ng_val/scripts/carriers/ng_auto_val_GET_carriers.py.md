# Diagram: shipment_core/shipment_service/ng_val/scripts/carriers/ng_auto_val_GET_carriers.py


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
flowchart TD
  Start([main(argv)]) --> ParseArgs{argv provided?}
  ParseArgs -->|no| UseSysArgs[argv = sys.argv[1:]]
  UseSysArgs --> ArgParse[Parse -s/--stage]
  ArgParse --> StageLower[stage = args.stage.lower()]
  StageLower --> StageDecision{stage == "prod-b" / "staging" / "test" / other}
  StageDecision --> |prod-b| ProdCfg[Set lambda_url=https://data-b.freightverify.com<br>cg_base_path=shipping<br>ng_base_path=shipping-ng<br>oauth_url=https://login.freightverify.com/oauth/token]
  StageDecision --> |staging| StagingCfg[Set lambda_url=https://data-s.freightverify.com<br>cg_base_path=shipping<br>ng_base_path=shipping-ng<br>oauth_url=https://login-s.freightverify.com/oauth/token]
  StageDecision --> |test| TestCfg[Set lambda_url=https://data-t.freightverify.com<br>cg_base_path=shipping<br>ng_base_path=shipping-ng<br>oauth_url=https://login-t.freightverify.com/oauth/token]
  StageDecision --> |other| OtherCfg[Set lambda_url=https://data-s.freightverify.com<br>cg_base_path=stage<br>ng_base_path=shipping-ng-<i>stage</i><br>oauth_url=https://login-s.freightverify.com/oauth/token]
  ProdCfg --> SetLambdaFile
  StagingCfg --> SetLambdaFile
  TestCfg --> SetLambdaFile
  OtherCfg --> SetLambdaFile
  SetLambdaFile[Set LAMBDA_FILE = "ng_auto_val_GET_carriers.{stage}.json"] --> RemoveIfExists{os.path.exists(LAMBDA_FILE)?}
  RemoveIfExists -->|yes| RemoveFile[os.remove(LAMBDA_FILE)]
  RemoveIfExists -->|no| SkipRemove
  RemoveFile --> BuildActions
  SkipRemove --> BuildActions
  BuildActions[Build actions dict and actions["oauth"] entries<br>- oauth_url<br>- grant_type/client_id/username/password from env] --> SetGetCarriers[Set actions["oauth"]["get_carriers"] = lambda_url/ng_base_path/carriers]
  SetGetCarriers --> LogTest1[Print "Posting to Auth0 endpoint for a token"]
  LogTest1 --> CallOAuth[oauth_token = validate_post_oauth_request(actions, heading, 2.0, verbose, LAMBDA_FILE)]
  CallOAuth -->|verbose True| PrintToken[Print oauth_token json]
  PrintToken --> SetAccessToken
  CallOAuth -->|verbose False| SetAccessToken[actions["oauth"]["access_token"] = oauth_token]
  SetAccessToken --> LogTest2[Print "Getting user associated carriers"]
  LogTest2 --> CallGetCarriers[validate_get_carriers_oauth(actions, test_heading, 2.0, verbose, LAMBDA_FILE)]
  CallGetCarriers --> End([return / exit])
```

> SVG rendering failed for this diagram.
