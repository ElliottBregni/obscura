# Diagram: shipment_core/shipment_service/ng_val/scripts/shipment_creation/auto_validate_lambdas_RACK_RETURN.py


> Auto-generated by Obscura crawlers

## Diagram 1

```mermaid
classDiagram
    class AutoValidateScript {
        +get_env_value(evname)
        +update_json_file(filename, data, state)
        +update_location(actions, token, email, shipment_info, latitude, longitude, min_offset, stop_sequence, include_trailer_number=False, send_as_code=None)
        +main(argv=None)
    }
    class Constants {
        +AUTO_VALIDATE_TOKEN_EVNAME
        +AUTO_VALIDATE_EMAIL_EVNAME
        +AUTO_VALIDATE_CREATOR_SENDER_FV_ID_EVNAME
        +AUTO_VALIDATE_CARRIER_FV_ID_EVNAME
        +AUTO_VALIDATE_OAUTH_GRANT_TYPE_EVNAME
        +AUTO_VALIDATE_OAUTH_CLIENT_ID_EVNAME
        +AUTO_VALIDATE_OAUTH_USERNAME_EVNAME
        +AUTO_VALIDATE_OAUTH_PASSWORD_EVNAME
        +LAMBDA_FILE
        +SHIPMENT_ID_PREFIX
        +ROUTE_ID_PREFIX
    }
    class ExternalModules {
        +auto_endpoints
        +uuid
        +os
        +json
        +argparse
        +sys
    }
    AutoValidateScript --> Constants
    AutoValidateScript --> ExternalModules
    ExternalModules ..> auto_endpoints : uses
    note right of AutoValidateScript : script entry point when run as __main__
```

> SVG rendering failed for this diagram.

## Diagram 2

```mermaid
flowchart TD
    Start([Start]) --> ParseArgs{Parse CLI args}
    ParseArgs --> DetermineStage[Set stage URLs and base paths]
    DetermineStage --> SetLambdafile[Set LAMBDA_FILE for stage]
    SetLambdafile --> Cleanup[Remove existing LAMBDA_FILE if exists]
    Cleanup --> GenerateUUIDs[Generate shipment_uuid, shipment_id, route_id]
    GenerateUUIDs --> BuildActions[Build actions dict with lambda & oauth endpoints]
    BuildActions --> ReadEnv[Read required ENV values via get_env_value]
    ReadEnv --> BuildShipmentInfo[Build shipment_info using auto_endpoints.build_shipment_info]
    BuildShipmentInfo --> OAuthRequest[Post to OAuth endpoint (validate_post_oauth_request)]
    OAuthRequest --> RemoveShipment1[Call auto_endpoints.remove_shipment]
    RemoveShipment1 --> CreateShipment[Call auto_endpoints.create_test_shipment]
    CreateShipment --> GetShipmentID[Extract lambda_id from response]
    GetShipmentID --> ConfigureGetShipment[Set actions.oauth.get_shipment URL]
    ConfigureGetShipment --> ValidateShipmentInfo1[validate_shipment_info_oauth (assert_completed=False)]
    ValidateShipmentInfo1 --> ValidateShipmentInfo2[validate_shipment_info_oauth (repeat)]
    ValidateShipmentInfo2 --> ValidateShipmentType[validate_shipment_type_oauth]
    ValidateShipmentType --> RemoveShipment2[Call auto_endpoints.remove_shipment (final)]
    RemoveShipment2 --> End([End])
```

> SVG rendering failed for this diagram.
