# Diagram: shipment_core/shipment_service/ng_val/scripts/heat_maps/ng_auto_val_GET_shipments_location-history.py


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
classDiagram
class Script {
  +AUTO_VALIDATE_OAUTH_GRANT_TYPE_EVNAME
  +AUTO_VALIDATE_OAUTH_CLIENT_ID_EVNAME
  +AUTO_VALIDATE_OAUTH_USERNAME_EVNAME
  +AUTO_VALIDATE_OAUTH_PASSWORD_EVNAME
  +LAMBDA_FILE
  +main(argv)
}
class Actions {
  +lambda : dict
  +oauth : dict
}
class OAuthData {
  +oauth_url
  +grant_type
  +client_id
  +username
  +password
  +access_token
  +get_active_shipments_list
  +get_location_histories_with_searching_template_ng
  +get_shipment
  +get_shipments_location-history
}
class AutoEndpoints <<module>> {
  +get_env_value(name)
  +validate_post_oauth_request(actions, heading, timeout, verbose, file)
  +validate_get_active_shipments_list_oauth(actions, heading, timeout, verbose, file)
  +validate_shipment_info_oauth(actions, heading, timeout, verbose, file, assert_completed)
  +validate_shipments_location_history_info_oauth(actions, heading, timeout, verbose, file)
  +validate_get_location_histories_with_searching_ng_oauth(actions, heading, timeout, verbose, query, file)
}
class CLIConfig {
  +stage
  +lambda_url
  +cg_base_path
  +ng_base_path
  +oauth_url
}

Script --> Actions : "creates & populates"
Actions *-- OAuthData : "oauth dictionary"
Script --> CLIConfig : "determines stage & URLs"
Script --> AutoEndpoints : "calls"
CLIConfig --> Script : "used to set LAMBDA_FILE and endpoints"
OAuthData <-- Actions : "stored under actions['oauth']"
Script : parses args -> stage selection -> sets endpoints
Script : sequences tests -> oauth token -> fetch shipments -> fetch shipment -> fetch location history -> batch query
```

> SVG rendering failed for this diagram.
