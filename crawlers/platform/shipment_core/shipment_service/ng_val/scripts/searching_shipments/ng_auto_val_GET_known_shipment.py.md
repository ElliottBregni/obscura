# Diagram: shipment_core/shipment_service/ng_val/scripts/searching_shipments/ng_auto_val_GET_known_shipment.py


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
flowchart TD
  Start([Start]) --> ParseArgs[/"Parse CLI args (stage)"/]
  ParseArgs --> LowerStage{stage.lower()}
  LowerStage --> |"prod-b"| ProdB[/"set lambda_url=data-b, cg_base_path=shipping, ng_base_path=shipping-ng, oauth_url=login.freightverify"/]
  LowerStage --> |"staging"| Staging[/"set lambda_url=data-s, cg_base_path=shipping, ng_base_path=shipping-ng, oauth_url=login-s"/]
  LowerStage --> |"test"| Test[/"set lambda_url=data-t, cg_base_path=shipping, ng_base_path=shipping-ng, oauth_url=login-t"/]
  LowerStage --> |other| Other[/"set lambda_url=data-s, cg_base_path=stage, ng_base_path=shipping-ng-stage, oauth_url=login-s"/]
  ProdB --> SetLambdaFile
  Staging --> SetLambdaFile
  Test --> SetLambdaFile
  Other --> SetLambdaFile
  SetLambdaFile["Set LAMBDA_FILE = ng_auto_val_GET_known_shipment.{stage}.json"] --> RemoveFile{"File exists?"}
  RemoveFile --> |yes| Remove[/"os.remove(LAMBDA_FILE)"/]
  RemoveFile --> |no| SkipRemove((skip remove))
  Remove --> BuildActions
  SkipRemove --> BuildActions
  BuildActions["Build actions.oauth with oauth_url, grant_type, client_id, username, password, get_active_shipments_list"] --> PrintAuthReq[/print 'Posting to Auth0 endpoint for a token'/]
  PrintAuthReq --> CallOAuth[/"oauth_token = validate_post_oauth_request(actions,...)/"]
  CallOAuth --> SetAccessToken["actions.oauth.access_token = oauth_token"]
  SetAccessToken --> PrepareGetShipment["known_shipment_id_to_get = 5402932\nset actions.oauth.get_shipment = {lambda_url}/{ng_base_path}/shipments/{id}"]
  PrepareGetShipment --> PrintGetShipment[/print 'Getting a particular Shipment (with known id 5402932)'/]
  PrintGetShipment --> ValidateShipment[/"validate_shipment_info_oauth(actions, test_heading, verbose=True, assert_completed=False)"/]
  ValidateShipment --> End([End])
```

> SVG rendering failed for this diagram.
