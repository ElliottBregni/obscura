# Diagram: shipment_core/shipment_service/ng_val/scripts/searching_shipments/ng_auto_val_GET_problem_searches.py


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
flowchart TD
  Start([Start]) --> ParseArgs[/"Parse CLI args (stage required)"/]
  ParseArgs --> ToLower{stage = args.stage.lower()}
  ToLower --> StageProdB[/stage == "prod-b"/]
  ToLower --> StageStaging[/stage == "staging"/]
  ToLower --> StageTest[/stage == "test"/]
  ToLower --> StageOther[/else/]
  StageProdB --> SetProdB[/"Set lambda_url, cg_base_path,\nng_base_path, oauth_url for prod-b"/]
  StageStaging --> SetStaging[/"Set lambda_url, cg_base_path,\nng_base_path, oauth_url for staging"/]
  StageTest --> SetTest[/"Set lambda_url, cg_base_path,\nng_base_path, oauth_url for test"/]
  StageOther --> SetOther[/"Set lambda_url=staging,\ncg_base_path=stage,\nng_base_path=shipping-ng-<stage>,\noauth_url=staging oauth"/]
  SetProdB --> SetLambdaFile[LAMBDA_FILE = "ng_auto_val_GET_problem_searches.<stage>.json"]
  SetStaging --> SetLambdaFile
  SetTest --> SetLambdaFile
  SetOther --> SetLambdaFile
  SetLambdaFile --> RemoveExisting[/"If LAMBDA_FILE exists: remove it"/]
  RemoveExisting --> BuildActions[/"Build actions dict:\nactions['oauth'] with oauth_url,\ngrant_type, client_id, username, password"/]
  BuildActions --> AddEndpoints[/"Add static NG endpoints:\nget_active_shipments_list,\nget_recent_shipment_exceptions,\nget_active_shipments_statuses_list,\nget_shipments_with_searching_template_ng"/]
  AddEndpoints --> PrintAuth["Print: Posting to Auth0 endpoint for a token"]
  PrintAuth --> CallValidateOauth[/"oauth_token = auto_endpoints.validate_post_oauth_request(actions, ...)" /]
  CallValidateOauth --> StoreToken[/"actions['oauth']['access_token'] = oauth_token"/]
  StoreToken --> SearchTestHeading[/"Prepare Test 5.24 heading and query args (q_a)"/]
  SearchTestHeading --> PrintSearch["Print: Searching for shipments..."]
  PrintSearch --> CallValidateGet[/"auto_endpoints.validate_get_all_shipments_with_searching_ng_oauth(actions, test_heading, ..., q_a, LAMBDA_FILE)"/]
  CallValidateGet --> End([End])
  style Start fill:#f9f,stroke:#333,stroke-width:1px
  style End fill:#bbf,stroke:#333,stroke-width:1px
```

> SVG rendering failed for this diagram.
