# Diagram: shipment_core/shipment_service/ng_val/scripts/oauth/ng_auto_val_POST_oauth.py


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
flowchart LR
    Start([Start]) --> ArgCheck{argv provided?}
    ArgCheck -- "No" --> UseSys[use sys.argv[1:]]
    ArgCheck -- "Yes" --> UseArg[use argv]
    UseSys --> ParseArgs[parse args (stage)]
    UseArg --> ParseArgs
    ParseArgs --> StageLower[stage = args.stage.lower()]
    StageLower --> Lagging{stage in ["prod"]?}
    Lagging -- "Yes" --> CannotTest1[print "CANNOT TEST oauth on stage {stage}"\nreturn]
    Lagging -- "No" --> StageCheck{stage == "staging" / "test" / other}
    StageCheck -- "staging" --> OAuthStg[oauth_url = "https://freightverify-s.auth0.com/oauth/token"]
    StageCheck -- "test" --> OAuthTest[oauth_url = "https://freightverify-t.auth0.com/oauth/token"]
    StageCheck -- "other" --> CannotTest2[print "CANNOT TEST oauth on stage {stage}"\nreturn]
    OAuthStg --> SetFile
    OAuthTest --> SetFile
    SetFile[set LAMBDA_FILE = "ng_auto_val_POST_oauth.{stage}.json"] --> RemoveFile{os.path.exists(LAMBDA_FILE)?}
    RemoveFile -- "Yes" --> Remove[os.remove(LAMBDA_FILE)]
    RemoveFile -- "No" --> SkipRemove[skip remove]
    Remove --> BuildActions
    SkipRemove --> BuildActions
    BuildActions[build actions dict\nactions["oauth"]["oauth_url"] = oauth_url] --> GetEnv1[get_env_value(AUTO_VALIDATE_OAUTH_GRANT_TYPE_EVNAME)]
    GetEnv1 --> SetGrant[set actions["oauth"]["grant_type"]]
    SetGrant --> GetEnv2[get_env_value(AUTO_VALIDATE_OAUTH_CLIENT_ID_EVNAME)]
    GetEnv2 --> SetClient[set actions["oauth"]["client_id"]]
    SetClient --> GetEnv3[get_env_value(AUTO_VALIDATE_OAUTH_USERNAME_EVNAME)]
    GetEnv3 --> SetUser[set actions["oauth"]["username"]]
    SetUser --> GetEnv4[get_env_value(AUTO_VALIDATE_OAUTH_PASSWORD_EVNAME)]
    GetEnv4 --> SetPass[set actions["oauth"]["password"]]
    SetPass --> LogTest[print "-- Posting to Auth0 endpoint for a token"]
    LogTest --> CallValidate[auto_endpoints.validate_post_oauth_request(actions, "Test 1.10 -- Posting to Auth0 endpoint for a token", verbose=True)]
    CallValidate --> End([return / exit])
```

> SVG rendering failed for this diagram.
