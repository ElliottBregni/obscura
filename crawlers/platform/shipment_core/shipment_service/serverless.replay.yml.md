# Diagram: shipment_core/shipment_service/serverless.replay.yml


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
graph TD
  subgraph Service["service: replay"]
    Provider["Provider: aws\nruntime: python3.13\narch: arm64\ntimeout: 900s\nmemorySize: ${self:custom.globalConfig.memorySize}\nendpointType: ${self:custom.globalConfig.endpointType.${opt:stage}, \"REGIONAL\"}"]
    IAM["IAM Role"]
    Plugins["Plugins:\n- serverless-python-requirements\n- serverless-prune-plugin"]
    Custom["Custom:\n- datadog\n- pythonRequirements\n- globalConfig\n- prune"]
    FunctionsSub["Functions"]
  end

  subgraph Functions["functions"]
    Replay["replay_events\nname: ${opt:stage}-replay_events\nhandler: shipment_service/replay/replay.lambda_handler\nlayers: ${self:custom.globalConfig.fv_layers}\nreservedConcurrency: 1\nmemorySize: 704\nenv: LIMIT=1000, THREADS=24\ntrigger: schedule(rate(60 minutes))"]
    Delete["delete_events\nname: ${opt:stage}-delete_events\nhandler: shipment_service/replay/delete.lambda_handler\nlayers: ${self:custom.globalConfig.fv_layers}\nenv: MAX_DAYS_IN_QUEUE=14\ntrigger: schedule(rate(60 minutes))"]
  end

  Provider --> IAM
  Provider --> Plugins
  Provider --> Custom
  Provider --> FunctionsSub
  FunctionsSub --> Replay
  FunctionsSub --> Delete

  IAM --> Allow["Allow:\n- lambda:InvokeFunction\n- sns:CreateTopic\n- sns:Publish\n- sqs:ReceiveMessage\n- sqs:GetQueueUrl\n- sqs:ListQueues\n- sqs:GetQueueAttributes\n- sqs:SendMessage\n- sqs:DeleteMessage\n- s3:PutObject\n- secretsmanager:GetSecretValue\nResource: *"]
  IAM --> Deny["Deny:\n- logs:CreateLogGroup\n- logs:CreateLogStream\n- logs:PutLogEvents\nResource:\n- arn:aws:logs:us-east-1:*:log-group:/aws/lambda/prod-b-delete_events:*\n- arn:aws:logs:us-east-1:*:log-group:/aws/lambda/staging1-delete_events:*\n- arn:aws:logs:us-east-1:*:log-group:/aws/lambda/test-delete_events:*"]
```

> SVG rendering failed for this diagram.
