# Diagram: shipment_core/shipment_service/shipment_service/window_time/update_window_timing.py


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
flowchart LR
  A[lambda_handler(event)] --> B[get_event_body & extract shipment_id]
  B --> C[convert status_date_time]
  C --> D[creator_fv_id -> get_organization_by_fv_id]
  D --> E[get_shipment(event, shipment_id, org_id)]
  E --> F[get_shipment_dates(shipment_info)]
  F --> G{shipment_status_code}
  G -->|AA| H[get_date_duration(new_origin)]
  H --> I[get_schedule_reset(destination_reset)]
  I --> J[post_shipment(...)]
  G -->|AB| K[compare status_time > origin_earliest]
  K -->|true| L[get_date_duration(destination)]
  L --> J
  K -->|false| M[raise ConflictError]
  G -->|other| N[raise ConflictError]
  J --> O[get_organization(event, id) / get_create_shipper_authorization]
  J --> P[modify_shipment(...)]
  P --> Q{for each shipment_stop}
  Q --> R[if stop_sequence==1 and new origin -> set origin stop times]
  Q --> S[elif stop==final_stop and new destination -> set destination times]
  Q --> T[else -> if shipment_status_code==AA call get_schedule_reset -> update intermediate stop times]
  R --> U[append updated stop to shipmentStops]
  S --> U
  T --> U
  U --> V[if update_shipment_bool True -> build tender payload]
  V --> W[invoke_lambda("create_shipments", event_copy)]
  W --> X{response.statusCode=="200"}
  X -->|true| Y[make_response(success,200)]
  X -->|false| Z[raise BadRequestError]
```

> SVG rendering failed for this diagram.
