# Diagram: shipment_core/shipment_service/shipment_service/asn/tests/test_asn.py


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
flowchart LR
  A[Start test_asn] --> B[Patch get_shipment_by_db_id]
  B --> C[Patch get_database_session_and_engine]
  C --> D[Create shipment_mock -> returned by get_shipment_by_db_id]
  D --> E[Create session_mock & engine_mock -> returned by get_database_session_and_engine]
  E --> F[Prepare event = status_event_boilerplate]
  F --> G[set_actor_type(event, "system")]
  G --> H[set_user_id(event, "test-user-id")]
  H --> I[set_actor_email(event, "test-user-id@example.com")]
  I --> J[set_organization_id(event, 2343)]
  J --> K[set_privileges(event, [MANAGE_SHIPMENTS])]
  K --> L[Modify event.body.status.reference: add {"qualifier":"A1","value":"steak sauce"}]
  L --> M[Add {"qualifier":"PARTS","value":"hammer||45"}]
  M --> N[Call MUT.lambda_handler(event, None)]
  N --> O[Receive resp]
  O --> P{resp["statusCode"] == "200"}
  P -- true --> Q[Test passes]
  P -- false --> R[Test fails]
```

> SVG rendering failed for this diagram.
