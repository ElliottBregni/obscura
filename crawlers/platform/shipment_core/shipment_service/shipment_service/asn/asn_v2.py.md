# Diagram: shipment_core/shipment_service/shipment_service/asn/asn_v2.py


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
flowchart TD
  Start([Start: lambda_handler]) --> Auth{Check privileges}
  Auth -->|Authorized| DBConnect[Establish DB connection]
  DBConnect --> ExtractEvent[Extract event body & path params]
  ExtractEvent --> GetMessageId[Extract message_id]
  ExtractEvent --> GetShipmentDbId[Extract shipment_db_id]
  GetShipmentDbId --> UpdateAudit[Update audit refs]
  ExtractEvent --> ParseStatusDate{Parse status_date_time}
  ParseStatusDate -->|ok| BodyDatetime[Convert to datetime]
  ParseStatusDate -->|error| BadRequest[Raise BadRequestError]
  BodyDatetime --> GetShipment[Get shipment by db_id]
  GetShipment -->|not found| DBError[Raise DatabaseError]
  GetShipment --> PrepareStatus[Create ShipmentStatus object]
  PrepareStatus --> InsertStatus[ss.insert()]
  InsertStatus --> CheckDuplicateEq[is_duplicate_equipment_num_allowed?]
  CheckDuplicateEq --> RefExtraction[Extract reference(s) from payload]
  RefExtraction --> ForEachRef{For each reference}
  ForEachRef --> ParseRefValue[Parse value and optional quantity]
  ParseRefValue --> IsEquipmentQual{qualifier_is_equipment_number?}
  IsEquipmentQual -->|yes & duplicates NOT allowed| CheckIfExists[check_if_equipment_number_exists]
  CheckIfExists -->|exists| UpdateEquip[update_shipment_equipment_number]
  CheckIfExists -->|not exists| InsertEquipRef[Insert ShipmentReference (equipment)]
  IsEquipmentQual -->|yes & duplicates allowed| InsertEquipRefAllowed[Insert ShipmentReference (equipment)]
  IsEquipmentQual -->|no| InsertRef[Insert ShipmentReference (other)]
  UpdateEquip --> ContinueLoop --> ForEachRef
  InsertEquipRef --> ContinueLoop
  InsertEquipRefAllowed --> ContinueLoop
  InsertRef --> ContinueLoop
  ContinueLoop --> AfterRefs[All references processed]
  AfterRefs --> ToJSON[ss.to_dict()]
  ToJSON --> ReturnResp[Return response with shipment_status]
  BadRequest --> EndError([Error])
  DBError --> EndError
```

> SVG rendering failed for this diagram.
