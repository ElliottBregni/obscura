# Diagram: shipment_core/shipment_service/shipment_service/asn/consume_route_book.py


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
flowchart TD
    A[lambda_handler(event, context, audit_refs=None)] --> B[logging.info("event:{event}")]
    B --> C[records = event.get("Records")]
    C --> D{records?}
    D -- yes --> E[for record in records]
    E --> F[body = json.loads(record.get("body"))]
    F --> G[routebook_consumer.process_child_shipments(body.get("event_copy"))]
    G --> H[result or []]
    D -- no --> I[skip processing]
    H --> J[return fv.aws.lambdas.make_response([], 200)]
    I --> J
```

> SVG rendering failed for this diagram.
