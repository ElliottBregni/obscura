# Diagram: shipment_core/shipment_service/shipment_service/asn/lambda_invokers.py


> Auto-generated by Obscura crawlers

## Diagram 1

```mermaid
flowchart LR
  subgraph delete_proxy["invoke_delete_proxy_endpoint(event)"]
    A[event] --> B[deepcopy(event) -> event_copy]
    B --> C[event_copy["httpMethod"] = "DELETE"]
    B --> D[event_copy["headers"]["X-WSS-fvShipmentId"] = build_fvShipmentId(event_copy.get("body"))]
    D --> E[fv.aws.lambdas.invoke_lambda("v2_delete_shipment", full_payload=event_copy, invoke_type="Event")]
    E --> F[return None]
  end
```

> SVG rendering failed for this diagram.

## Diagram 2

```mermaid
flowchart LR
  subgraph child_proxy["invoke_proxy_endpoint_for_child(event)"]
    A[event] --> B[deepcopy(event) -> event_copy]
    B --> C[event_copy["path"] = "/shipping/shipment/route_book"]
    B --> D[event_copy["headers"]["x-wss-fvshipmentid"] = build_fvShipmentId(event_copy.get("body"))]
    D --> E[child = fv.aws.lambdas.invoke_lambda("v2_post_put_shipment", full_payload=event_copy, invoke_type="RequestResponse")]
    E --> F{int(child.statusCode) >= 400?}
    F -- yes --> G[raise fv.error.BadRequestError(child.body.errors, child.body.errors)]
    F -- no --> H[return None]
  end
```

> SVG rendering failed for this diagram.

## Diagram 3

```mermaid
flowchart LR
  subgraph parent_proxy["invoke_proxy_endpoint_for_parent(event)"]
    A[event] --> B[deepcopy(event) -> event_copy]
    B --> C[event_copy["path"] = "/shipping/shipment/route_book"]
    C --> D[shipment = fv.aws.lambdas.invoke_lambda("v2_post_put_shipment", full_payload=event_copy, invoke_type="RequestResponse")]
    D --> E[ship_body = parse shipment.body (json.loads if str)]
    D --> F[status_code = int(shipment.statusCode or 500)]
    F --> G{status_code >= 400?}
    G -- yes --> H{status_code == 400?}
    H -- yes --> I[message = ship_body.errors]
    H -- no --> J[message = "Error with create shipment: " + ship_body.error.message if ship_body else None]
    I --> K[raise fv.error.BadRequestError(message, message)]
    J --> K
    G -- no --> L[return ship_body.shipment]
  end
```

> SVG rendering failed for this diagram.
