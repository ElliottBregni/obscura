# Diagram: shipment_core/shipment_service/shipment_service/deferred_processing/deferred_processing.py


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
flowchart TD
  A[lambda_handler(event, context)] --> B{aws_stage in MAIN_STAGES?}
  B -- No --> Z[return build_response(None)]
  B -- Yes --> C[SECRETS.get_secret(DB) & get_database_session_and_engine_ng]
  C --> D[DB_CONN.establish_connection(); cursor]
  C --> E[call process_scheduled_request(session, engine, cursor, context, json_body)]

  E --> F[Query ShipmentStatus rows filtered by actual_created_at, status_code, processed_at]
  F --> G{For each status in resp}
  G --> H[get_shipment_by_db_id(session, status.shipment_id)]
  H --> I{error?}
  I -- Yes --> J[set status.processed_at = now; session.add(status); continue]
  I -- No --> K[set status.processed_at = now; shipment.is_accepted = True; set asset ids if present; session.add(shipment)]

  K --> L{status.obc_asset_id?}
  L -- No --> M[skip SMS flow]
  L -- Yes --> N[get_fvmb_phone_number(status.obc_asset_id)]
  N --> O{phone_number?}
  O -- No --> M
  O -- Yes --> P[get_existing_shipments_by_db_id(cursor, status.shipment_id)]
  P --> Q[sorted_shipment_stops = map ShipmentStops(cursor, stop)]
  Q --> R{sorted_shipment_stops empty?}
  R -- Yes --> S[raise HandledException("Shipment has no stops.")]
  R -- No --> T[message = get_fvmb_message_v2(sorted_shipment_stops)]
  T --> U{phone_number and message and not shipment.parent_shipment_id?}
  U -- Yes --> V[send_sms(phone_number, message)]
  U -- No --> M

  G --> W[after loop session.commit()]
  W --> X[return build_response(None)]

  subgraph Exceptions
    E1[DeferredProcessingException] --> Ex1[utilities.unhandled_errors -> make_client_error_response]
    E2[SQLAlchemyError] --> Ex2[utilities.sql_alchemy_errors -> make_client_error_response]
  end

  style Exceptions fill:#f9f,stroke:#333,stroke-width:1px
  Z -.-> Clean[cleanup: session.close(), engine.dispose()]
  X -.-> Clean
  Ex1 -.-> Clean
  Ex2 -.-> Clean
```

> SVG rendering failed for this diagram.
