# Diagram: shipment_core/shipment_service/shipment_service/v2/conversion.py


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
flowchart TD
    A[Inputs: payload, payload_headers, context, httpMethod="PUT"] --> B[Create headers dict {"headers": payload_headers}]
    B --> C[Call httputil.get_header(headers, "X-WSS-fvShipmentId", True) -> header_data string]
    C --> D[Split header_data by "|"]
    D --> E[Assign carrier_id = part0]
    D --> F[Assign customer_id = part1]
    D --> G[Assign shipment_id = part2]
    A --> H[If "carrier" in payload]
    H -- yes --> I[header["sender_id"] = payload["carrier"]["fvCarrierId"]]
    B --> J[header["message_id"] = httputil.get_header(headers, "X-WSS-correlationId")]
    A --> K[If httpMethod == "PATCH"]
    K -- yes --> L[header["purpose_code"] = constants.PURPOSE_CODE_PATCH]
    E & F & G & I & J & L --> M[header dict assembled with keys: carrier_id, creator_id, shipment_id, sender_id?, message_id, purpose_code?]
    M --> N[Return header]
```

> SVG rendering failed for this diagram.
