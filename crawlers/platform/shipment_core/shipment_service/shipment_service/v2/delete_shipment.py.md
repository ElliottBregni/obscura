# Diagram: shipment_core/shipment_service/shipment_service/v2/delete_shipment.py


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
flowchart TD
  Start([Start: lambda_handler]) --> DB[Establish DB connection]
  DB --> GetBody[Get event body & headers]
  GetBody --> Validate[Validate headers with Validator]
  Validate -->|invalid| RaiseBad[Raise BadRequestError]
  Validate --> Convert[v2_convert_payload_delete_shipment]
  Convert --> BuildHeader[build_header (calls httputil.get_header)]
  BuildHeader --> PayloadReady[New payload ready]
  PayloadReady --> Log[logging.info(new_payload)]
  Log --> Extract[Extract correlation_id, shipment_id_header]
  Extract --> AsyncCheck{fv-async header?}
  AsyncCheck -->|true| InvokeTypeEvent[invoke_type = "Event"]
  AsyncCheck -->|false| InvokeTypeReq[invoke_type = "RequestResponse"]
  InvokeTypeEvent --> QueryParam[Check query param true_delete]
  InvokeTypeReq --> QueryParam
  QueryParam -->|true| TrueDelete[Call true_delete(cursor, header)]
  TrueDelete --> Response204[make_response('', 204)]
  QueryParam -->|false| InvokeProxy[invoke_lambda("proxy_shipments", full_payload, invoke_type)]
  InvokeProxy -->|is_async true| ReturnAccepted[make_response('', 202)]
  InvokeProxy -->|is_async false| ContinueResp[set response from invoked lambda]
  ContinueResp --> FinalResp[utilities.make_v2_response(response, correlation_id, shipment_id_header)]
  Response204 --> FinalResp
  ReturnAccepted --> FinalResp
  FinalResp --> End([Return success response])
  subgraph ErrorFlow [Exception handling]
    AnyError([Exception caught]) --> SetMsg[Prepare message and client_message]
    SetMsg --> CheckHTTP{e.http_status missing or >=500?}
    CheckHTTP -->|yes| CallMakeError[make_error_response(event, context, error=e)]
    CallMakeError --> RaiseHandled[Raise HandledException with 400]
    CheckHTTP -->|no| RaiseHandled
  end
  Start -.-> AnyError
```

> SVG rendering failed for this diagram.
