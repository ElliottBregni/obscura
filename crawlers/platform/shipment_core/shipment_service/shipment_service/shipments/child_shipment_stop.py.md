# Diagram: shipment_core/shipment_service/shipment_service/shipments/child_shipment_stop.py


> Auto-generated by Obscura crawlers

## Diagram 1

```mermaid
classDiagram
    class Module {
        +get_stops_snapshot(cursor, shipment)
        +create_shipment_meta(carrier_id, creator_id, sender_id, purpose_code, message_id)
        +get_vin_activity(stop_state)
        +get_wmi_to_org(cursor)
        +shipper_can_override_wmi(cursor, organization_fv_id)
        +get_vin_to_org(cursor, vins, shipper_org_fv_id)
        +generate_haulaway_shipments(cursor, shipment_meta, vins, _filter=True, removed_vins=[])
        +get_shipment_references(cursor, shipment_id)
        +cancel_old_child_shipment(cursor, parent_shipment, child)
        +generate_stop(sequence, the_stop, vins)
        +create_new_child_shipment(cursor, event, shipment_meta, parent_shipment, parent_references, child_shipment_id, child, purpose_code)
        +reconcile_all_stops(cursor, event, context, audit_refs, shipment_meta, parent_shipment, original_stop_state, new_stop_state, removed_vins=[])
    }
    class ShipmentStopCollection
    class fv.aws.lambdas
    class shipment_service.fvshared.database
    Module --> ShipmentStopCollection : returns
    Module --> "fv.aws.lambdas" : invokes lambdas (create_shipments, v2_delete_shipment)
    Module --> "shipment_service.fvshared.database" : reads system configuration
    Module : uses constants (PURPOSE_CODE_CREATE, PURPOSE_CODE_UPDATE, HAUL_AWAY_ID_MODIFIER)
    get_vin_activity ..> ShipmentStopCollection : reads stops
    generate_haulaway_shipments ..> get_vin_to_org : calls
    create_new_child_shipment ..> generate_stop : calls
    reconcile_all_stops ..> generate_haulaway_shipments : calls
    reconcile_all_stops ..> create_new_child_shipment : calls
    reconcile_all_stops ..> cancel_old_child_shipment : calls
    cancel_old_child_shipment ..> fv.aws.lambdas : invokes delete
    create_new_child_shipment ..> fv.aws.lambdas : invokes create_shipments
```

> SVG rendering failed for this diagram.

## Diagram 2

```mermaid
flowchart TD
    A[Start: reconcile_all_stops] --> B{original_stop_state exists?}
    B -- yes --> C[generate_haulaway_shipments(original_stop_state)]
    B -- no --> C2[original_children = {}]
    A --> D[generate_haulaway_shipments(new_stop_state)]
    C --> E[get_shipment_references(parent_shipment)]
    C2 --> E
    D --> F[Compare sorted keys]
    E --> F
    F --> G{key in original?}
    G -- yes --> H[create_new_child_shipment(..., purpose_code=UPDATE)]
    G -- no --> I[create_new_child_shipment(..., purpose_code=CREATE)]
    F --> J[For remaining original keys -> cancel_old_child_shipment]
    H --> K[invoke lambda create_shipments]
    I --> K
    K --> L{HTTP response 2xx?}
    L -- yes --> M[Update shipment_stops child_shipment_id]
    L -- no --> N[log error]
    J --> O[invoke v2_delete_shipment lambda]
    O --> P[if success -> clear child_shipment_id and call ufn_delete_shipment]
    P --> Q[Audit fallout_vins if any]
    M --> Q
    N --> Q
    Q --> Z[End]
```

> SVG rendering failed for this diagram.
