# Diagram: shipment_core/shipment_service/shipment_service/shipments/AsnOrder.py


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
classDiagram
    class PartviewPackageContainerFeed {
        - stage
        - queueName
        + __init__()
        + getOrganizationFvId(organizationId)
        + getOrganizationScac(organizationId)
        + getOrganizationDetail(organizationId)
        + getQueue()
        + sqsSendEvent(actor, organizationId, action, asnOrder)
    }

    class ContainerMilestoneEvent {
        <<deprecated>>
        + getActorTypeAndEventSubType(orgDetails, statusDictionary)
        + buildPayload(milestoneCode, organizationId, shipment, milestoneDictionary)
        + write(requestId, lambdaLevel, userId, userEmail, actorType, principalId, action, shipment, milestoneDictionary)
        + writeUsingEvent(event, action, shipment, milestoneDictionary)
    }

    class AsnOrder {
        - partNameRegex
        - partQualifiers
        - actualQualifiers
        - plannedQualifiers
        - referenceQualifierKey
        - referenceValueKey
        - referenceQuantityKey
        - purposeCodeTranslation
        - modeTranlation
        + __init__()
        + getQueue()
        + translatePurposeCode(purposeCode)
        + translateMode(modeNumber)
        + datetimeRangeParse(timerange)
        + parse_references(shipment, references)
        + sendAsnOrder(requestId, lambdaLevel, userId, userEmail, actorType, principalId, action, organizationId, asnOrder)
        + write(requestId, lambdaLevel, userId, userEmail, actorType, principalId, action, shipment, references)
    }

    PartviewPackageContainerFeed <|-- ContainerMilestoneEvent : uses
    PartviewPackageContainerFeed <|-- AsnOrder : uses
    ContainerMilestoneEvent ..> AsnOrder : sends ASN via sqsSendEvent
    AsnOrder ..> ContainerMilestoneEvent : may construct payloads
    PartviewPackageContainerFeed ..> "boto3 SQS" : interacts with
    ContainerMilestoneEvent ..> "get_organization_by_id" : calls
    AsnOrder ..> "Lob" : references enum
    note right of PartviewPackageContainerFeed : Responsible for queue naming and SQS send
    note right of ContainerMilestoneEvent : Builds milestone payloads and writes events to SQS
    note right of AsnOrder : Parses references and sends ASN orders to SQS
```

> SVG rendering failed for this diagram.
