# Diagram: shipment_core/shipment_service/shipment_service/replay/replay_distributor.py


> Auto-generated by Obscura crawlers

## Diagram 1

```mermaid
flowchart TD
  Start([Lambda invoked]) --> EstablishDB[DB_CONN.establish_connection()]
  EstablishDB --> Query[Execute complex SELECT query on shipment_request_queue & shipments]
  Query --> Fetch[events = [ev._asdict()[\"data\"] for ev in cursor.fetchall()]]
  Fetch --> Chunking[Create args list and build lst_of_lsts with <=100 items per sublist and cap 1000]
  Chunking --> ForEachLists[for lst in lst_of_lsts loop]
  ForEachLists --> PrintJson[print(json.dumps(lst)) then exit()]
  PrintJson --> EndExit([Process exits early with printed payload])
  EndExit -->|If continued| SplitArgs[args_split = fv.utilities.split_list_even(args, max_size=20)]
  SplitArgs --> DeepCopy[event_copy = copy.deepcopy(event)]
  DeepCopy --> ForEachSplit[for i, args in enumerate(args_split)]
  ForEachSplit --> BuildBody[body = {"args": args}; insert into event_copy["body"]]
  BuildBody --> CountInnerArgs[count inner args to compute ln]
  CountInnerArgs --> PickleAndSize[r = pickle.dumps(event_copy); size = sys.getsizeof(r)]
  PickleAndSize --> SizeCheck{size > max_size?}
  SizeCheck -->|yes| RatioCalc[ratio = ceil(size/max_size); split body args into ratio chunks]
  SizeCheck -->|no| Continue[proceed to next chunk or invoke lambda]
  Continue --> MakeResponse[fv.aws.lambdas.make_response({})]
  MakeResponse --> End([Return response])
```

> SVG rendering failed for this diagram.

## Diagram 2

```mermaid
classDiagram
  class Secrets {
    +Secrets()
  }
  class SecretNames {
  }
  class FvDatabaseConnector {
    +FvDatabaseConnector(app_name, secret_name)
    +establish_connection()
    +cursor
  }
  class AWS_Lambdas_Module {
    +mandatory_lambda_handling()
    +make_response(obj)
    +invoke_lambda(name, full_payload, invoke_type)
  }
  class Utilities {
    +split_list_even(list, max_size)
  }
  Secrets <|-- SECRETS
  SecretNames <-- SecretNames
  FvDatabaseConnector <|-- DB_CONN
  AWS_Lambdas_Module <-- fv.aws.lambdas
  Utilities <-- fv.utilities
  DB_CONN -->|uses| SecretNames
  lambda_handler -->|decorated by| AWS_Lambdas_Module
  lambda_handler -->|uses| DB_CONN
  lambda_handler -->|uses| Utilities
```

> SVG rendering failed for this diagram.
