# Diagram: shipment_core/shipment_service/shipment_service/fvshared/ShipmentEventStream.py


> Auto-generated by Obscura crawlers

## Diagram 1

```mermaid
classDiagram
    class ShipmentEventStream {
        +stage: str
        +queueName: str
        +get_queue()
        +send_sqs_event(message: str)
        +should_organization_stream_events(organization_id: int)
        +get_organization_detail(organization_id)
        +get_actor_type_and_event_type(org_details, status_dictionary)
        +build_payload(milestone_code: str, organization_id: int, shipment, event_dictionary: dict)
        +write(metadata: RequestMetaData, action: str, shipment: dict, event_dictionary: dict)
        +write_using_metadata(metadata: RequestMetaData, action: str, shipment: dict, event_dictionary: dict)
        +does_code_require_stop(status_code, translated_code)
        +stream_event(cursor, metadata: RequestMetaData, shipment, status_code, shipment_stop_id, sorted_shipment_stops, shipment_status, translated_code)
    }

    class StreamableShipmentStatus {
        -__shipment_status
        +__init__(shipment_status)
        +insert()
        +get_translated_code(status_code, mode_id)
        +skip_status_code(status_code)
        +stream_event(metadata: RequestMetaData, shipment, status_code, stop_id, sorted_stops)
        +simple_stream_event(metadata: RequestMetaData, shipment, status_code)
        +simple_stream_event_by_shipment_id(cursor, metadata: RequestMetaData, shipment_id, status_code)
    }

    ShipmentEventStream <|-- StreamableShipmentStatus : uses
    ShipmentEventStream ..> "fv.aws.lambdas.invokinators.invokinator_organization" : get_organization_by_id
    ShipmentEventStream ..> "fv.aws.lambdas.invokinators.invokinator_iam" : search_solution
    ShipmentEventStream ..> "boto3 SQS" : send_message
    StreamableShipmentStatus ..> "shipment_service.fvshared.db_no_orm" : db calls
    StreamableShipmentStatus ..> "shipment_service.fvshared.enums.MilestoneCodes" : uses
    StreamableShipmentStatus ..> "shipment_service.fvshared.constants" : uses
```

> SVG rendering failed for this diagram.

## Diagram 2

```mermaid
flowchart TD
    A[StreamableShipmentStatus.stream_event] --> B[get_translated_code(status_code, shipment.mode_id)]
    B --> C{translated_codes not empty}
    C -- yes --> D[For each translated_code]
    D --> E[resolve translated_code -> resolved_translated_code]
    E --> F{ShipmentEventStream.should_organization_stream_events(shipment.created_by_org_id)}
    F -- false --> G[stop processing]
    F -- true --> H[if sorted_stops is None -> fetch sorted_stops from db_no_orm]
    H --> I[determine requires_stop via does_code_require_stop]
    I -- requires_stop and no stop_id --> G
    I -- pass --> J[build status_dict with status and stop info]
    J --> K[attempt to enrich event_description via db_no_orm.get_status_name_from_code]
    K --> L[call ShipmentEventStream.write_using_metadata(metadata, "CREATE", shipment, status_dict)]
    L --> M[ShipmentEventStream.build_payload(...) -> json payload]
    M --> N[create message meta + payload]
    N --> O[ShipmentEventStream.send_sqs_event(message_string)]
    O --> P[Return / end]
```

> SVG rendering failed for this diagram.
