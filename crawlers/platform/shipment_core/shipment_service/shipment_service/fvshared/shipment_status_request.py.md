# Diagram: shipment_core/shipment_service/shipment_service/fvshared/shipment_status_request.py


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
classDiagram
    class Validation {
        - errors
        - failed
        + combine(other)
    }

    class ShipmentStatusRequest {
        - header
        - location
        - status
        - equip
        - reference
        - event_reference
        + to_dict()
        + to_json()
        + validate()
        + from_aws_event()
        + from_v2_aws_event()
    }

    ShipmentStatusRequest o-- Header : has
    ShipmentStatusRequest o-- Location : has
    ShipmentStatusRequest o-- Status : has
    ShipmentStatusRequest o-- Equip : has
    ShipmentStatusRequest o-- References : has
    ShipmentStatusRequest ..> Validation : uses

    class Header {
        - carrier_id
        - creator_id
        - shipment_id
        - sender_id
        - message_id
        + from_dict()
        + from_v2_aws_event()
        + to_dict()
        + validate()
    }

    class Location {
        - city
        - state
        - latitude
        - longitude
        - code
        - sequence
        + from_dict()
        + from_v2_aws_event()
        + to_dict()
        + validate()
    }

    class Status {
        - shipment_status_code
        - shipment_status_reason_code
        - remarks
        - shipment_ids
        - status_date_time
        - status_event_time
        - scac
        - fv_carrier_id
        + from_dict()
        + from_v2_aws_event()
        + to_dict()
        + validate()
    }

    class Equip {
        - obc_asset_identifier
        - trailer_number
        + from_dict()
        + from_v2_aws_event()
        + to_dict()
        + validate()
    }

    class References {
        - references
        - index
        + from_list()
        + to_list()
        + validate()
    }

    References *-- Reference : contains

    class Reference {
        - qualifier
        - value
        + from_dict()
        + to_dict()
        + validate()
    }

    class NullObject {
        + to_dict()
        + validate()
    }

    class NullList {
        + to_list()
        + validate()
    }

    ShipmentStatusRequest ..> NullObject
    ShipmentStatusRequest ..> NullList

    class AsyncShipmentStatusBody {
        - event
        - async_request_info
        + from_dict()
        + to_dict()
        + to_json()
    }

    AsyncShipmentStatusBody o-- AsyncRequestResponse : contains
    AsyncShipmentStatusBody ..> Validation : raises/uses

    %% external referenced classes
    class AsyncRequestResponse {
        - request_id
        - ts
        + to_dict()
    }

    %% utility / external modules (dependencies)
    class utilities
    class httputil
    class fv_dm << (M,#FFAAAA) >>
    class fv_error << (E,#FFE0E0) >>
    utilities <.. ShipmentStatusRequest : uses
    httputil <.. Header : uses
    fv_dm <.. Equip : uses
    fv_error <.. Status : raises
```

> SVG rendering failed for this diagram.
