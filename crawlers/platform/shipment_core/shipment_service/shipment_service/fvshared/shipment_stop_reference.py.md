# Diagram: shipment_core/shipment_service/shipment_service/fvshared/shipment_stop_reference.py


> Auto-generated by Obscura crawlers

## Diagram 1

```mermaid
classDiagram
class ShipmentStopReference {
  - dirty: bool
  - stop_id: int
  - id: int
  - qualifier: str
  - value: str
  - description: str
  - quantity: float|Decimal|int
  - created_at: date
  - updated_at: date
  + __init__(dictionary=dict)
  + _get_value_tuple() tuple
  + set_id(id:int) ShipmentStopReference
  + set_stop_id(stop_id:int) ShipmentStopReference
  + set_qualifier(qualifier:str) ShipmentStopReference
  + set_value(value:str) ShipmentStopReference
  + set_description(description:str) ShipmentStopReference
  + set_quantity(quantity) ShipmentStopReference
  + set_created_at(created_at:date) ShipmentStopReference
  + set_updated_at(updated_at:date) ShipmentStopReference
  + initialize(dict, dirty:bool=True) ShipmentStopReference
  + to_dictionary(dates_to_str:bool=True) dict
  + to_json() str
  + __str__() str
  + values_as_dictionary() dict
  + update(cursor) ShipmentStopReference
  + insert(cursor) ShipmentStopReference
  + upsert(cursor) ShipmentStopReference
  + delete(cursor) ShipmentStopReference
  + _select_single_row_by_query(cursor, query) ShipmentStopReference
  + get_by_id(cursor, id:int) ShipmentStopReference
}

class ShipmentStopReferenceCollection {
  - data: list
  + __init__(stop_references=None)
  + initialize(stop_references:list) ShipmentStopReferenceCollection
  + to_dictionary_array() list
  + update(cursor)
  + select_collection_by_query(cursor, query) ShipmentStopReferenceCollection
  + get_by_stop_id(cursor, stop_id:int) ShipmentStopReferenceCollection
  + get_by_stop_id_and_qualifier(cursor, stop_id:int, qualifier:str) ShipmentStopReferenceCollection
  + get_by_qualifier(cursor, qualifier:str) ShipmentStopReferenceCollection
}

ShipmentStopReferenceCollection "1" o-- "*" ShipmentStopReference : contains
ShipmentStopReference ..> "utility_classes.DecimalEncoder" : uses
ShipmentStopReference ..> "utility_classes.date_to_isoformat" : uses
ShipmentStopReference ..> cursor : DB operations
ShipmentStopReferenceCollection ..> cursor : DB operations
```

> SVG rendering failed for this diagram.

## Diagram 2

```mermaid
flowchart LR
  subgraph PythonObjects
    SSR[ShipmentStopReference]
    SSRCol[ShipmentStopReferenceCollection]
  end
  DB[(PostgreSQL DB)]
  CURS[cursor]
  SSR -->|insert(cursor)| CURS
  SSR -->|update(cursor)| CURS
  SSR -->|delete(cursor)| CURS
  SSR -->|get_by_id(cursor,id)| CURS
  SSRCol -->|select_collection_by_query(cursor,query)| CURS
  SSRCol -->|update(cursor) -> iterates SSR.update| SSR
  CURS -->|execute DDL/Queries| DB
  click SSR "javascript:void(0)" "ShipmentStopReference methods: initialize, to_dictionary, to_json, upsert"
```

> SVG rendering failed for this diagram.
