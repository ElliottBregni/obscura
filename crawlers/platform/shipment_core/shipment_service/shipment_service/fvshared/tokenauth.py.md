# Diagram: shipment_core/shipment_service/shipment_service/fvshared/tokenauth.py


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
flowchart LR
    Start([Start]) --> Check{ENV: FV_IS_TESTING == "1"?}
    Check -- Yes --> LogWarn[/"logging.warn(\"mocking for test\")"/]
    LogWarn --> AssignMocks[/"get_auth_fields = get_auth_fields_mock\nuser_token_is_valid = user_token_is_valid_mock"/]
    AssignMocks --> End([End])
    Check -- No --> End
    subgraph Constants
        SECRETS["SECRETS = Secrets()"]
        APP_NAME["APP_NAME = \"tokenauth\""]
        THIS_REQUEST["THIS_REQUEST = \"TokenAuthorization\""]
        FV_TESTING_EMAIL["_FV_TESTING_EMAIL = \"walter.heisenberg@example.com\""]
        FV_TESTING_TOKEN["_FV_TESTING_TOKEN = \"OhthuYai8eechoPh\""]
    end
    subgraph Functions
        AuthError(("class AuthError(Exception)"))
        is_unit_testing["_is_unit_testing()"]
        get_auth_fields_mock["get_auth_fields_mock(event)"]
        user_token_is_valid_mock["user_token_is_valid_mock(email, token)"]
    end
    Start --> is_unit_testing
    is_unit_testing --> Check
    get_auth_fields_mock -->|returns| AuthFields["(email, token)"]
    user_token_is_valid_mock -->|returns| Validity["(True/False, reason)"]
    Constants --> is_unit_testing
    Constants --> get_auth_fields_mock
    Constants --> user_token_is_valid_mock
```

> SVG rendering failed for this diagram.
