# Diagram: shipment_core/shipment_service/shipment_service/trip/post_put_trip.py


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
flowchart LR
  A[lambda_handler(event, context, audit_refs)] --> B{httpMethod POST/PUT}
  A --> C[validate_timezone_for_legs(body)]
  C --> D[sort_legs(body) => assign leg_sequence]
  D --> E[parse first/last stops -> DateTimeRange orig_window/dest_window]
  E --> F[db.trip_shipment_create(cursor, trip_id,... ) -> trip_internal_id]
  F --> G[get_equipment_numbers(body.legs)]
  G --> H{equipment_numbers present?}
  H -- yes --> I[set_equipment_numbers_to_all_legs(legs, equipment_numbers)]
  I --> J[existing_equipment_numbers = db.check_equip_nums(cursor, equipment_numbers)]
  J --> K[get_non_existing_equip_nums(trip_internal_id, existing_equipment_numbers, equipment_numbers)]
  K --> L{equipment_numbers_for_insertion?}
  L -- yes --> M[db.remove_old_equipment_numbers(...)] --> N[db.insert_equip_num_manage(cursor, trip_internal_id, equipment_numbers_for_insertion)]
  L -- no --> N
  H -- no --> N
  N --> O[create boto3 lambda_client]
  O --> P[prepare args per leg and call fv.utilities.send_multithreaded_requests(call_legs,...)]
  P --> Q[call_legs(event, [leg], customer_id, results, lambda_client)]
  Q --> R[call_v2_with_leg(event, leg, customer_id, lambda_client)]
  R --> S[fv.aws.lambdas.invoke_lambda("v2_post_put_shipment", leg_event)]
  S --> T{res.statusCode 200/201?}
  T -- yes --> U[parse shipment body -> add leg_sequence -> return shipment dict]
  T -- no --> V[raise BadRequestError or UnhandledException]
  U --> W[results.legs appended]
  P --> X[after threads complete -> shipments = results.legs; check results.errors]
  X --> Y[shipments.sort by leg_sequence]
  Y --> Z[for each shipment: db.add_shipment_to_trip(...) ; total_distance += db.get_shipment_total_distance(...)]
  Z --> AA[parent_shipment = db.get_existing_shipments_by_db_id_without_stops(...) ; update total_distance]
  AA --> AB[copy_shipment_stop for origin/dest -> db.update_newloc_ids(...)]
  AB --> AC{Exception during processing?}
  AC -- yes & httpMethod POST --> AD[db.delete_shipment_internal(cursor, trip_internal_id, leg=False) and cleanup legs]
  AC -- no --> AE{httpMethod == PUT?}
  AE -- yes --> AF[db.delete_shipments_not_used(cursor, shipment_internal_ids, trip_internal_id)]
  AE -- no --> AG
  AF --> AG[return fv.aws.lambdas.make_response(body, 201)]
```

> SVG rendering failed for this diagram.
