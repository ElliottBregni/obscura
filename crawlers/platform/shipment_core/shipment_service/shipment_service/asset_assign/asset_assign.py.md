# Diagram: shipment_core/shipment_service/shipment_service/asset_assign/asset_assign.py


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
classDiagram
class AssetAssignException
class AssetChangeCode {
  <<enumeration>>
  ASSIGN = "XB"
  RESIGN = "XX"
}
class Secrets {
  +SECRETS
}
class FvDatabaseConnector {
  +DB_CONN
  +establish_connection()
  +cursor
}
class lambda_handler {
  +lambda_handler(event, context, audit_refs)
  -parse event_body
  -validate shipment_status_code
  -prepare tracking_window_range
  -short_circuit_same_asset
}
class utilities {
  +convert_status_date_time(status_date_time)
  +get_field(ref, name)
}
class fv_dm {
  +extract(container, path)
}
class db_no_orm {
  +get_existing_shipments_by_db_id(cursor, shipment_id)
  +process_asset_assign(...)
}
class tables_Shipments {
  +Shipments(cursor, data)
  +to_dict()
  +update()
  +obc_asset_id
  +trailer_number
  +tracking_window
}
class fv_aws_lambdas {
  +get_event_body(event)
  +mandatory_lambda_handling(privs)
  +make_response(payload)
  class auth {
    +get_actor_type(event)
    +get_user_id(event)
    +get_actor_email(event)
    +get_organization_id(event)
    +Privilege
  }
}
Secrets --> FvDatabaseConnector : provides secrets
lambda_handler --> fv_aws_lambdas : get_event_body / make_response / decorator
lambda_handler --> fv_aws_lambdas.auth : get_actor_*
lambda_handler --> fv_dm : extract
lambda_handler --> utilities : convert_status_date_time / get_field
lambda_handler --> FvDatabaseConnector : DB_CONN.establish_connection() / cursor
lambda_handler --> db_no_orm : get_existing_shipments_by_db_id / process_asset_assign
lambda_handler --> tables_Shipments : instantiate Shipments
FvDatabaseConnector o-- "1" tables_Shipments : uses cursor
db_no_orm --> tables_Shipments : returns current_shipment
tables_Shipments --> lambda_handler : provides shipment data (obc_asset_id, trailer_number, tracking_window)
```

> SVG rendering failed for this diagram.
