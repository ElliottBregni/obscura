# Diagram: shipment_core/shipment_service/shipment_service/ng_preferences/ng_get_watchlist.py


> Auto-generated by Obscura crawlers

## Diagram 1

```mermaid
flowchart LR
    LH[lambda_handler(event, context, audit_refs)] --> PGP[process_get(event, context)]
    PGP --> DBE[DB_CONN.establish_connection()]
    DBE --> CUR[DB_CONN.cursor]
    PGP --> GUE[get_user_email(event)]
    GUE --> LOG[logging.warning(user_email)]
    PGP --> SQLCALL[ng_get_watchlist_sql(user_email, cursor)]
    SQLCALL --> DBQ[execute SQL on cursor]
    DBQ --> ITEMS[return cursor.fetchall() -> items]
    ITEMS --> MAP[map each item -> watchlist_row_to_json(item)]
    MAP --> RESULT[process_get_result (list)]
    RESULT -->|empty?| NOTFOUND{is empty?}
    NOTFOUND -->|yes| RAISE[fv.error.NotFoundError("no records found")]
    NOTFOUND -->|no| RESP[fv.aws.lambdas.make_response(process_get_result)]
    RESP --> RETURN[return response]
```

> SVG rendering failed for this diagram.

## Diagram 2

```mermaid
sequenceDiagram
    participant Event as Event (API Gateway)
    participant LH as lambda_handler
    participant P as process_get
    participant Auth as shipment_service.fvshared.oauth2.get_user_email
    participant DB as DB_CONN (FvDatabaseConnector)
    participant SQL as ng_get_watchlist_sql
    participant Cursor as cursor
    participant Conv as ng_preferences_common.watchlist_row_to_json
    participant AWS as fv.aws.lambdas.make_response
    Event->>LH: invoke(event, context, audit_refs)
    LH->>P: call process_get(event, context)
    P->>DB: establish_connection()
    DB-->>Cursor: expose cursor
    P->>Auth: get_user_email(event)
    Auth-->>P: user_email
    P->>SQL: ng_get_watchlist_sql(user_email, cursor)
    SQL->>Cursor: execute query
    Cursor-->>SQL: fetchall() -> items
    SQL-->>P: return items
    loop for each item
        P->>Conv: watchlist_row_to_json(item)
        Conv-->>P: json_item
    end
    alt no items
        P->>P: raise NotFoundError
    else items present
        P->>AWS: make_response(process_get_result)
        AWS-->>P: response
        P-->>LH: return response
        LH-->>Event: return response
```

> SVG rendering failed for this diagram.
