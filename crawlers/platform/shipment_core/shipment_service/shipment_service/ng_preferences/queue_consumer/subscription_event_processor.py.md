# Diagram: shipment_core/shipment_service/shipment_service/ng_preferences/queue_consumer/subscription_event_processor.py


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
flowchart LR
  A[Start: lambda_handler(event)] --> B{Records present?}
  B -- No --> Z[Return 200 OK]
  B -- Yes --> C[DB_CONN.establish_connection()]
  C --> D[For each record]
  D --> E[Parse record body (json.loads or raw)]
  E --> F[Extract payload, meta, shipment ids, fv ids, owner_id]
  F --> G[Create event via fv.aws.lambdas.get_sample_admin_event("post")]
  G --> H[Deepcopy event -> event_copy]
  H --> I[Set pathParameters, authorizer user/org, X-WSS-fvShipmentId = format_fv_shipment_id(carrier,creator,creator_shipment)]
  I --> J{request_type}
  J -- subscribe --> K[Prepare subscribe body]
  K --> K1[Invoke subscribe_to_shipment_event]
  K1 --> L[Log response]
  J -- unsubscribe --> M[Prepare unsubscribe body]
  M --> M1[Invoke unsubscribe_from_shipment_event]
  M1 --> L
  J -- update --> N[Prepare update body]
  N --> O[subscription_id = db_no_orm.get_subscribtion_id(cursor, shipment_id)]
  O --> P{subscription_id found?}
  P -- No --> Q[Raise NotFoundError]
  P -- Yes --> R[Set pathParameters with subscription_id]
  R --> S[Invoke update_shipment_subscription]
  S --> L
  L --> T[Collect responses]
  T --> Z

  subgraph Helpers
    HF1[format_fv_shipment_id(carrier_id,creator_id,shipment_id)]
    HF2[DB_CONN: ShipmentDatabase("subscription_event_processor")]
  end
  HF2 --> C
  HF1 --> I
```

> SVG rendering failed for this diagram.
