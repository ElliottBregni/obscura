# Diagram: shipment_core/shipment_service/shipment_service/ng_preferences/subscription/sync_saved_seach_subscription.py


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
flowchart TD
    Event["Lambda Event"] --> AuthCheck["auth_check (AuthType.PRIVILEGE)"]
    AuthCheck --> Handler[lambda_handler(event, context, audit_refs)]
    Handler --> LogRecv["logging.info(received event)"]
    Handler --> DBConnEstablish["DB_CONN.establish_connection()"]
    DBConnEstablish --> Cursor["cursor = DB_CONN.cursor"]
    Handler --> GetBody["get_event_body(event)"]
    GetBody --> ParseJSON["json.loads(unsubscribe_callback_data)"]
    ParseJSON --> ConditionShipment{"shipment_data present?"}
    ConditionShipment -- Yes --> ExtractIDs["extract creator_shipment_id, creator_fv_id, carrier_fv_id"]
    ExtractIDs --> GetShipment["db_no_orm.get_shipment(cursor, creator_shipment_id, creator_fv_id, carrier_fv_id)"]
    GetShipment --> ShipmentDBID["shipment_db_id = shipment.get('shipment_db_id')"]
    ConditionShipment -- No --> ShipmentDBID["shipment_db_id = None"]
    Handler --> SubscriptionID["subscription_id = json_data.get('subscription_id')"]
    ShipmentDBID --> CheckUpdate{"shipment_db_id and subscription_id exist?"}
    SubscriptionID --> CheckUpdate
    CheckUpdate -- Yes --> UpdateDB["db_no_orm.update_subscribed_shipment(cursor, shipment_db_id, subscription_id)"]
    CheckUpdate -- No --> SkipUpdate["no DB update"]
    UpdateDB --> Response["make_response({}, status_code=200)"]
    SkipUpdate --> Response
    Response --> Return["Return HTTP 200"]
```

> SVG rendering failed for this diagram.
