# Diagram: shipment_core/shipment_service/shipment_service/ng_preferences/subscription/unsubscribe.py


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
flowchart LR
  Event[Incoming event] --> Lambda[lambda_handler(event, context, audit_refs)]
  Lambda -->|establish_connection| DB[DB_CONN (FvDatabaseConnector)]
  Lambda -->|get header|getHeader[httputil.get_header]
  Lambda -->|get user| Auth[auth.get_user_id]
  Lambda -->|get body|getBody[get_event_body]
  Lambda -->|get path param|getPath[get_path_parameter]
  Lambda -->|get shipment| GetShipment[db_no_orm.get_shipment]
  GetShipment -->|returns| Shipment[(shipment)]
  Lambda -->|unsubscribe| Unsub1[db_no_orm.unsubscribe_shipment_alert_me_subscription]
  Lambda -->|delete saved search| DeleteSS[db_no_orm.delete_subscribed_saved_search_delta_by_id]
  Lambda -->|prepare body| Prepare[prepare json body]
  Lambda -->|invoke| Invoke[invoke_unsubscribe_from_shipment(event, body)]
  Invoke -->|deepcopy + POST| InvokeCall[fv.aws.lambdas.invoke_lambda("unsubscribe_from_event")]
  InvokeCall -->|success| ResponseOK[res_json with statusCode < 400]
  ResponseOK -->|unsubscribe again| Unsub2[db_no_orm.unsubscribe_shipment_alert_me_subscription]
  Invoke -->|exception| NotFound[raise fv.error.NotFoundError]
  Lambda -->|return| Return[return res_json]
```

> SVG rendering failed for this diagram.
