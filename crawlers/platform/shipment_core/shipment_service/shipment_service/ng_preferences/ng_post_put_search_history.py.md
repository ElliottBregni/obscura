# Diagram: shipment_core/shipment_service/shipment_service/ng_preferences/ng_post_put_search_history.py


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
flowchart TD
    LH[lambda_handler(event, context, audit_refs)] --> PP[process_post_put(event, context)]
    PP --> DBConn[DB_CONN.establish_connection()/cursor]
    PP --> GetBody[get_event_body(event) -> json_body]
    GetBody --> LogBody[logging.warning(search_item)]
    PP --> GetEmail[shipment_service.fvshared.oauth2.get_user_email(event) -> user_email]
    GetEmail --> LogEmail[logging.warning(user_email)]
    PP --> Decide{pathParameters contains item_id?}
    Decide -->|yes| Update[update_search_history(cursor, row_id, search_name, search_body, user_email)]
    Decide -->|no| Post[post_search_history(cursor, name, search, search_type, user_email)]
    Update --> SQLU[cursor.mogrify(UPDATE ...)]
    Post --> SQLP[cursor.mogrify(INSERT ...)]
    SQLU --> ExecU[cursor.execute(sql) -> cursor.fetchone() or None]
    SQLP --> ExecP[cursor.execute(sql) -> cursor.fetchone() or None]
    ExecU --> RowU[search_row]
    ExecP --> RowP[search_row]
    RowU --> Convert[ng_preferences_common.search_row_to_json(search_row) -> search_row_json]
    RowP --> Convert
    Convert --> Check{process_post_put_result exists?}
    Check -->|no| Error[fv.error.NotFoundError("no records effected")]
    Check -->|yes| Response[fv.aws.lambdas.make_response(process_post_put_result)]
    Response --> Return[return response]
```

> SVG rendering failed for this diagram.
