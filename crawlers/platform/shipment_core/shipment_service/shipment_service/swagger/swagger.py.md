# Diagram: shipment_core/shipment_service/shipment_service/swagger/swagger.py


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
flowchart LR
  Start([Start]) --> Decorator[/"fv.aws.lambdas.mandatory_lambda_handling()"/]
  Decorator --> LambdaHandler["lambda_handler(event, context, audit_refs)"]
  LambdaHandler --> GetReq["fv.aws.lambdas.get_request_id(event)"]
  GetReq --> Configure["fv.log.configure_logging(request_id)"]
  Configure --> ParentDir["parent_dir = pathlib.Path(__file__).resolve().parent"]
  ParentDir --> YamlFile["yaml_file = os.path.join(parent_dir, \"swagger.yaml\")"]
  YamlFile --> Exists{"os.path.exists(yaml_file)?"}
  Exists -- No --> Warn["logging.warning(...)"]
  Warn --> BuildNone["utilities.build_response(None)"]
  BuildNone --> End([End])
  Exists -- Yes --> OpenFile["with open(yaml_file) as f"]
  OpenFile --> YLoad["y = yaml.load(f)"]
  YLoad --> BuildY["utilities.build_response(y)"]
  BuildY --> End
  subgraph Imports
    A[fv.aws.lambdas]
    B[fv.log]
    C[pathlib]
    D[os]
    E[yaml]
    F[logging]
    G[shipment_service.fvshared.utilities]
  end
  Decorator --- A
  GetReq --- A
  Configure --- B
  ParentDir --- C
  YamlFile --- D
  Exists --- D
  Warn --- F
  YLoad --- E
  BuildNone --- G
  BuildY --- G
```

> SVG rendering failed for this diagram.
