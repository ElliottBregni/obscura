# Diagram: shipment_core/shipment_service/shipment_service/ng_shipments/ng_get_origin_country.py


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
flowchart LR
  A[Event] --> B[lambda_handler(event, context, audit_refs)]
  B --> C[DB_CONN.establish_connection()]
  B --> D[call process_origin_country_get(DB_CONN.cursor, event, count=False)]
  subgraph Globals
    G1[DB_CONN: FvDatabaseConnector("ng_get_origin_country", SecretNames.DATABASE_EXILE, auto_connect=False)]
    G2[SECRETS: Secrets()]
  end
  D --> E[get_user_org_id(event)]
  D --> F[fv.aws.lambdas.auth.get_user_org_types(event)]
  D --> G[fv.aws.lambdas.get_query_parameter(event, "query", default="")]
  F --> H{is_dealer?\nOrgTypes.DEALER in org_profiles}
  G --> I{fuzzy != ""?}
  H --> J[apply org filter in SQL if not dealer]
  I --> K[append fuzzy ILIKE clauses to SQL if true]
  J --> L[sql_origin_country (constructed SQL with pagination)]
  K --> L
  L --> M[cursor.mogrify(sql_origin_country, params)]
  M --> N[logging.info(query.decode())]
  L --> O[cursor.execute(sql_origin_country, params)]
  O --> P[cursor.fetchall() -> sql_result]
  P --> Q[origin_country_results = [{"name": country.name, "code": country.code2} ...]]
  Q --> R[full_count = len(origin_country_results)]
  R --> S[return (origin_country_results, full_count)]
  S --> T[make_response(process_origin_country_get(...))]
  T --> U[Lambda Response]
  classDef globals fill:#f9f,stroke:#333,stroke-width:1px;
  class G1,G2 globals
```

> SVG rendering failed for this diagram.
