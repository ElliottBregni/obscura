# Diagram: shipment_core/shipment_service/shipment_service/ng_shipments/ng_get_shipments.py


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
flowchart TD
  LambdaHandler["lambda_handler(event, context, audit_refs)"]
  ParseAccept["parse_accept_header -> (requested_format, version)"]
  DB["DB_CONN.establish_connection()/cursor"]
  GetNG["get_ng_shipments(cursor, event, organization_id, shipment_id, requested_format, version)"]
  GetShip["get_shipments(cursor, organization_id, event, is_csv, request_type)"]
  GetSingle["get_shipment(cursor, organization_id, shipment_id, event)"]
  MakeResp["make_response(...) / async_export(...)"]
  GetDetails["get_shipment_details(event, shipment, cursor, with_status_names, ...)"]
  Translate["get_shipment_details_translated(shipment)"]
  BasicDetails["get_shipment_details_basic(shipment)"]
  LocationData["get_location_data(shipment)"]
  LocationBasic["get_location_data_basic(shipment)"]
  BuildEventRefs["build_event_references(event_references)"]
  ETAHandling["handle_eta_display_values / set_eta_categories / update_shipment_eta_tbd"]
  CSV["create_csv(shipments, timezone, event)"]
  Helpers["fast_convert / make_last_reported_at / get_optional_shipment_field / append_geofence_data"]
  Linked["get_linked_shipments / get_close_linked_shipments / get_common_vin_shipments"]
  Parts["get_shipment_parts_from_partview / get_shipment_parts / create_shipment_part_numbers / filter_parts"]
  Watched["get_watched_status / get_alert_me_status"]
  Equipment["get_equipment_type_ng"]
  StatusTranslations["get_active_status_ng / get_active_exceptions_ng / get_current_status_ng"]

  LambdaHandler --> ParseAccept
  LambdaHandler --> DB
  LambdaHandler --> GetNG
  GetNG -->|shipment_id present| GetSingle
  GetNG -->|no shipment_id| GetShip
  GetNG --> MakeResp
  GetShip --> BuildEventRefs
  GetShip --> Translate
  GetShip --> CSV
  GetShip --> ETAHandling
  GetSingle --> GetDetails
  GetDetails --> LocationData
  GetDetails --> Equipment
  GetDetails --> Linked
  GetDetails --> Parts
  GetDetails --> Watched
  GetDetails --> StatusTranslations
  Translate --> LocationBasic
  Translate --> BuildEventRefs
  LocationData --> BuildEventRefs
  LocationData --> Helpers
  LocationData --> LocationBasic
  ETAHandling --> Helpers
  CSV --> MakeResp
  Linked -->|uses| get_close_linked_shipments
  Parts --> get_shipment_parts
  BuildEventRefs -->|returns| "event_references dict"
  Helpers --> fast_convert
  StatusTranslations -->|depends on| SHIPMENT_STATUS_CODE_MAP["ShipmentStatus"]
  Equipment --> DB
  Watched --> DB
```

> SVG rendering failed for this diagram.
