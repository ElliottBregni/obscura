# Diagram: shipment_core/shipment_service/shipment_service/ng_shipments/ng_get_destination_city.py


> Auto-generated by Obscura crawlers

## Diagram 1

```mermaid
classDiagram
    direction TB
    class Constants {
        +string SHIPMENTS_APPLICATION_ID
        +string SHIPMENTS_APPLICATION_NAME
        +string SHIPMENTS_CITY_FILTER_NAME
    }
    class CityListRelay {
        -dict __event
        -string __application_id
        -string __application_name
        -string __filter_name
        -dict __authorizer
        -string __tenant
        -string __filter
        -int __page_number
        -int __page_size
        +__init__(event)
        +get_list() dict
    }
    class get_paginated_destination_city_list {
        +cursor
        +event
        +count: bool
        +returns dict response
    }
    Constants <|-- CityListRelay
    CityListRelay --> "fv.aws.lambdas.get_query_parameter" : uses
    CityListRelay --> "fv.aws.lambdas.get_int_query_parameter" : uses
    CityListRelay --> "fv.aws.lambdas.invoke_lambda" : invokes
    get_paginated_destination_city_list --> CityListRelay : instantiates
    get_paginated_destination_city_list ..> json : parses
    get_paginated_destination_city_list ..> logging : logs errors
```

> SVG rendering failed for this diagram.

## Diagram 2

```mermaid
flowchart TD
    A[get_paginated_destination_city_list(cursor, event, count)] --> B[Instantiate CityListRelay(event)]
    B --> C[CityListRelay.get_list()]
    C --> D[invoke_lambda("get-filter-list", full_payload=event)]
    D --> E[response]
    E --> F[Parse body = json.loads(response.body)]
    F --> G[meta = body.meta]
    F --> H[cities = body.destination_city]
    H --> I[Loop over cities]
    I --> J{json.loads(city) succeeds?}
    J -- yes --> K[append parsed city to json_city]
    J -- JSONDecodeError --> L[logging.error(JSONDecodeError)]
    J -- other Exception --> M[logging.error(Unexpected error)]
    K --> N[continue loop]
    L --> N
    M --> N
    N --> O[new_body = {meta, destination_city: json_city}]
    O --> P[response.body = new_body]
    P --> Q[return response]
```

> SVG rendering failed for this diagram.
