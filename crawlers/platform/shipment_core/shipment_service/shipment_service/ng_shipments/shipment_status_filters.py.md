# Diagram: shipment_core/shipment_service/shipment_service/ng_shipments/shipment_status_filters.py


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
classDiagram
    class fv_aws_lambdas {
        <<module>>
        +mandatory_lambda_handling()
        +make_response(obj)
    }
    class fv_log {
        <<module>>
    }
    class shipment_service_fvshared_ng_searching {
        <<module>>
        <<enumeration>>
        +ShipmentStatus
        +SHIPMENT_STATUS_DESCRIPTIONS
    }
    class current_module {
        <<module>>
        +process_statuses(*args)
        +lambda_handler(event, context, audit_refs)
    }
    fv_aws_lambdas --|> current_module : uses
    shipment_service_fvshared_ng_searching --|> current_module : imports
    fv_log --|> current_module : imports
    current_module : process_statuses() "builds list of status_filters"
    shipment_service_fvshared_ng_searching : ShipmentStatus "enum values: ACTIVE, ASSET_PENDING, SCHEDULED, ARRIVED, CANCELED, AVAILABLE, RELEASED, ARRIVED_DESTINATION, ARRIVED_ORIGIN, ARRIVED_PICKUP, ARRIVED_DROPOFF, DEPARTED_DESTINATION, DEPARTED_ORIGIN, DEPARTED_PICKUP, DEPARTED_DROPOFF, AVAILABLE_FOR_DELIVERY"
    shipment_service_fvshared_ng_searching : SHIPMENT_STATUS_DESCRIPTIONS "mapping from ShipmentStatus to human names"
```

> SVG rendering failed for this diagram.
