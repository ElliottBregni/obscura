# Diagram: shipment_core/shipment_service/shipment_service/ng_shipments/comment/db.py


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
classDiagram
    class shipments_module {
        +execute_query(cursor, sql, replaces)
        +get_dealer_ids(cursor, event, shipper_id, creator_shipment_id)
        +get_shipment_related_ids(cursor, shipment_id)
        +update_comments_count_number(cursor, owner_id, organization_id, creator_shipment_id, factor)
        +get_shipment(cursor, shipment_id)
        +get_shipment_id(cursor, creator_shipment_id, organization_id)
    }
    class execute_query {
        +mogrify -> query
        +execute(query)
        +fetchall()/fetchone()
    }
    class get_dealer_ids {
        +get_destination_id()
        +get_grantee_dealer_ids_from_location_id()
        +invokinator_location.get_location_grants(event, location_ids, pov)
    }
    class get_shipment_related_ids {
        +SELECT carrier_organization_id, created_by_org_id, creator_shipment_id
        +raises BadRequestError
    }
    class update_comments_count_number {
        +get_current_comment_count()
        +change_comment_count()
        +UPDATE shipments...jsonb_set
    }
    class get_shipment {
        +SELECT creator_shipment_id, created_by_org_id
        +raises NotFoundError
    }
    class get_shipment_id {
        +SELECT id
        +raises NotFoundError
    }
    class fv_error {
        +BadRequestError
        +NotFoundError
    }
    class invokinator_location {
        +get_location_grants(event, location_ids, pov)
    }
    class QSP {
        +LocationGrant.POV_GRANTER
    }

    shipments_module --> execute_query : uses
    shipments_module --> get_dealer_ids : defines
    shipments_module --> get_shipment_related_ids : defines
    shipments_module --> update_comments_count_number : defines
    shipments_module --> get_shipment : defines
    shipments_module --> get_shipment_id : defines

    get_dealer_ids --> invokinator_location : calls
    get_dealer_ids --> QSP : references
    get_shipment_related_ids --> execute_query : calls
    update_comments_count_number --> execute_query : calls
    get_shipment --> fv_error : raises
    get_shipment_id --> fv_error : raises
    get_shipment_related_ids --> fv_error : raises

    execute_query ..> "database cursor" : interacts with
    invokinator_location ..> "external AWS lambda" : invokes
    fv_error ..> "exceptions" : defines
```

> SVG rendering failed for this diagram.
