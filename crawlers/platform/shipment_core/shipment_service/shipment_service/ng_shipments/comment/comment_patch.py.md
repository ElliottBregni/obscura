# Diagram: shipment_core/shipment_service/shipment_service/ng_shipments/comment/comment_patch.py


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
flowchart LR
  Start([lambda_handler(event, context, audit_refs)])
  Start --> Establish[DB_CONN.establish_connection()]
  Establish --> GetBody[get_event_body(event) --> data]
  GetBody --> ExtractText[text = data.get("text")]
  GetBody --> ExtractShared[shared_with = data.get("shared_with", [])]
  ExtractText --> GetOwner
  ExtractShared --> GetOwner
  GetOwner[get_owner_id(DB_CONN.cursor, event)] --> GetCommentId[get_path_parameter(event, "comment_id")]
  GetOwner --> GetShipmentId[get_path_parameter(event, "shipment_id")]
  GetCommentId --> CheckPermission
  GetShipmentId --> CheckPermission
  CheckPermission[is_user_allowed_to_share_with_these_organizations(DB_CONN.cursor, event, shared_with, owner_id, int(shipment_id))] -->|false| ErrorResponse
  CheckPermission -->|true| BuildPayload
  ErrorResponse[make_response(MESSAGE_ERROR_USER_NOT_ALLOWED_TO_SHARE, HTTPStatus.BAD_REQUEST.value)] --> ReturnError([return error response])
  BuildPayload --> Payload{{payload dict with requestContext, pathParameters, body}}
  Payload --> Invoke[invoke_lambda("comment_patch", full_payload=payload) --> response]
  Invoke --> Update[update_number_of_comments_count(DB_CONN.cursor, event, response, owner_id, int(shipment_id))]
  Update --> ReturnSuccess([return response])
  ReturnError --> End([end])
  ReturnSuccess --> End
```

> SVG rendering failed for this diagram.
