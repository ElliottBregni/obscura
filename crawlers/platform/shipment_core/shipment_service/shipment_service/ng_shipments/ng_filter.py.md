# Diagram: shipment_core/shipment_service/shipment_service/ng_shipments/ng_filter.py


> Auto-generated by Obscura crawlers

## Diagram 1

```mermaid
classDiagram
    class Secrets {
        +SECRETS
    }
    class FvDatabaseConnector {
        +establish_connection()
        +cursor
    }
    class DB_CONN {
        +autocommit
    }
    class filter_functions_dict {
        +mapping of Filter -> function
    }
    class handle_filters {
        +handle_filters(query_params, event)
    }
    class lambda_handler {
        +lambda_handler(event, context, audit_refs)
    }
    Secrets --|> Secrets : instance SECRETS
    DB_CONN --> FvDatabaseConnector : instance of
    handle_filters --> filter_functions_dict : uses
    lambda_handler --> DB_CONN : calls establish_connection()
    lambda_handler --> handle_filters : calls
    filter_functions_dict o-- "many" : filter functions
```

> SVG rendering failed for this diagram.

## Diagram 2

```mermaid
flowchart TD
    Start([Start lambda_handler])
    Start --> EstablishDB[Establish DB_CONN.establish_connection()]
    EstablishDB --> GetQuery[Get query params from event]
    GetQuery --> ForEachFilter{Iterate queryParams}
    ForEachFilter --> IsEnabled[cast_to_bool(include_filter)?]
    IsEnabled --> InDict{filter_name in filter_functions_dict?}
    InDict --> CheckCountList{filter_name in check_count?}
    CheckCountList --> TrueCount[Set count=True; get page_size/page_number]
    TrueCount --> NormalizePages[Normalize page_size/page_number]
    NormalizePages --> ParseAccept[Parse Accept header -> app_header?]
    ParseAccept --> CallFilterCount[Call filter function with count=True]
    CallFilterCount --> SpecialStops{Is stop/city/state filter?}
    SpecialStops --> IfSpecial[Yes]
    IfSpecial --> ExtractBody[res = result.body if result]
    SpecialStops --> IfNotSpecial[No]
    IfNotSpecial --> ComputePages[Compute total_pages using ceil(result[1]/page_size)]
    ComputePages --> AddMeta[If app_header add meta to res]
    AddMeta --> StoreResult[res[filter_name] = result[0]]
    InDict --> NotCount[Call filter function without count]
    NotCount --> StoreSimple[res[filter_name] = result]
    IsEnabled --> SkipFilter[No]
    SkipFilter --> ForEachFilter
    StoreResult --> ForEachFilter
    StoreSimple --> ForEachFilter
    ForEachFilter --> ReturnRes[Return make_response(res)]
    ReturnRes --> End([End])
```

> SVG rendering failed for this diagram.
