# Diagram: shipment_core/shipment_service/shipment_service/ng_shipments/ng_get_shipments_location-history.py


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
classDiagram
    class DB_CONN {
        +establish_connection()
        +cursor
    }
    class SECRETS
    class FilterOptions {
        <<enumeration>>
        SHIPMENT_IDS
    }
    class get_filter_options {
        +event
        +returns dict
    }
    class get_all_options {
        +event
        +returns dict|None
    }
    class make_shipment_location_history {
        +event, cursor, organization_id, shipment_id
        +returns HTTP response
    }
    class make_location_histories_from_shipments {
        +cursor, organization_id, event
        +returns HTTP response
    }
    class lambda_handler {
        +event, context, audit_refs
        +returns HTTP response
    }
    class fv_db_FvDatabaseConnector
    class fv_db_shipments {
        +get_shipments(cursor, organization_id, shipment_ids)
    }
    class fv_aws_lambdas {
        +get_int_list_query_parameter(event, name)
        +get_int_path_parameter(event, name)
        +make_response(body, status_code?)
        +mandatory_lambda_handling(privs)
        class auth { +Privilege }
    }
    class fv_secrets { +Secrets, SecretNames }
    class shipment_service { +get_shipment_details(event, shipment, cursor, with_status_names=True) }
    DB_CONN --> fv_db_FvDatabaseConnector : instance of
    DB_CONN --> fv_db_shipments : uses
    make_shipment_location_history --> fv_db_shipments : calls get_shipments
    make_shipment_location_history --> shipment_service : calls get_shipment_details
    make_shipment_location_history --> fv_aws_lambdas : uses make_response
    make_location_histories_from_shipments --> fv_aws_lambdas : uses make_response
    make_location_histories_from_shipments --> fv_db_shipments : calls get_shipments
    make_location_histories_from_shipments --> shipment_service : calls get_shipment_details
    get_filter_options --> fv_aws_lambdas : uses get_int_list_query_parameter
    get_all_options --> FilterOptions : iterates
    lambda_handler --> fv_aws_lambdas : uses get_int_path_parameter, decorator mandatory_lambda_handling
    lambda_handler --> DB_CONN : calls establish_connection and cursor
    lambda_handler --> make_shipment_location_history : delegates when shipment_id present
    lambda_handler --> make_location_histories_from_shipments : delegates when no shipment_id
    SECRETS --> fv_secrets : instance of
```

> SVG rendering failed for this diagram.
