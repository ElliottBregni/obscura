# Diagram: shipment_core/shipment_service/shipment_service/ng_shipments/ng_get_countries.py


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
flowchart LR
  A[lambda_handler(event, context, audit_refs)] --> B{decorated by\nmandatory_lambda_handling}
  B --> C[get_int_query_parameter(event, "pageNumber", default=0)]
  B --> D[get_int_query_parameter(event, "pageSize", default=0)]
  B --> E[get_query_parameter(event, "query", default="")]
  C --> F[page_number]
  D --> G[page_size]
  E --> H[fuzzy_value]
  B --> I[process_get(page_number, page_size, fuzzy_value)]
  I --> J{fuzzy_value\npresent?}
  J -- yes --> K[Filter countries where\nfuzzy_value in name or alpha2]
  J -- no --> L[Take all countries]
  K --> M[Map each country: {"name": COUNTRY_CONV.get(c.name, c.name), "code": c.alpha2}]
  L --> M
  M --> N{page_size > 0?}
  N -- yes --> O[get_bounds_from_pagination_options(page_number, page_size)]
  O --> P[lower_bound, upper_bound]
  P --> Q[Slice country_list[lower:upper]]
  Q --> R[meta = {TOTAL_COUNT, TOTAL_PAGES, CURRENT_PAGE}]
  N -- no --> S[meta = {}]
  R --> T[country_list, meta]
  S --> T
  T --> U[make_response({"meta": meta, "data": country_list})]
  U --> V[HTTP Response]
  style B stroke:#333,stroke-width:2px
  style I stroke:#333,stroke-dasharray: 5 5
  classDef util fill:#f9f,stroke:#333,stroke-width:1px
  class O,P,Q util
```

> SVG rendering failed for this diagram.
