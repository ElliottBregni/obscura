# Diagram: shipment_core/shipment_service/shipment_service/ng_shipments/ng_get_parts.py


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
graph LR
  subgraph Imports
    A[fv.aws.lambdas]
    B[fv.aws.lambdas.shipments.constants]
    C[shipment_visibility_conditions]
    D[fv.db]
    E[fv.log]
    F[Secrets / SecretNames]
    G[get_user_org_id]
  end

  SECRETS[SECRETS = Secrets()] -->|uses| F
  DB_CONN[DB_CONN: FvDatabaseConnector("ng_get_parts", SecretNames.DATABASE_TRACKING, autocommit=True, auto_connect=False)] -->|connector| D

  lambda_handler["lambda_handler(event, context, audit_refs)"] -->|establishes| DB_CONN_establish[DB_CONN.establish_connection()]
  lambda_handler -->|calls| make_resp[fv.aws.lambdas.make_response(...)]

  lambda_handler -->|calls| process_get_parts["process_get_parts(cursor, event)"]
  process_get_parts -->|calls| get_user_org_id["get_user_org_id(event)"]
  process_get_parts -->|calls| cursor_execute["cursor.execute(get_parts_sql(...))"]
  process_get_parts -->|iterates| dedupe["collect part_values set -> return list"]

  get_parts_sql["get_parts_sql(cursor, organization_id)"] -->|builds SQL using| Table[TableVersions.SHIPMENT_TRACKING]
  get_parts_sql -->|calls| shipment_visibility_conditions["shipment_visibility_conditions(leg_filtering=False)"]
  cursor_execute -->|uses SQL from| get_parts_sql

  make_resp -->|wraps| process_get_parts

  style SECRETS fill:#f9f,stroke:#333,stroke-width:1px
  style DB_CONN fill:#ff9,stroke:#333,stroke-width:1px
  style lambda_handler fill:#9f9,stroke:#333,stroke-width:1px
  style process_get_parts fill:#9cf,stroke:#333,stroke-width:1px
  style get_parts_sql fill:#fc9,stroke:#333,stroke-width:1px
  style shipment_visibility_conditions fill:#ffc,stroke:#333,stroke-width:1px
  style get_user_org_id fill:#cfc,stroke:#333,stroke-width:1px
```

> SVG rendering failed for this diagram.
