# Diagram: shipment_core/shipment_service/shipment_service/ng_shipments/ng_get_locations.py


> Auto-generated by Obscura crawlers

## Diagram 1

```mermaid
flowchart TD
    A[Event] -->|query param| B[get_query_parameter(event)]
    A --> C[get_user_org_id(event)]
    B --> D[find_origin_locations / find_destination_locations]
    D --> E[find_locations(cursor,event,count,origin_or_destination)]
    E --> F[construct SQL]
    F --> G[shipment_visibility_conditions(leg_filtering=False)]
    F --> H[db.basic_filter_pagination(event)]
    E --> I[cursor.execute(sql, {"organization_id": user_org_id, "fuzzy_search": fuzzy_search})]
    I --> J[cursor.fetchall() -> sql_result]
    J --> K{len(sql_result) > 0}
    K -->|yes| L[return (list of {id,code,name}, sql_result[0].full_count)]
    K -->|no| M[return ([], 0)]
    subgraph DB
      DB_CONN[FvDatabaseConnector "DB_CONN"]
      cursor[(cursor)]
      DB_CONN --> cursor
    end
    E --> DB_CONN
```

> SVG rendering failed for this diagram.

## Diagram 2

```mermaid
classDiagram
    class Modules {
        +fv.aws.lambdas
        +fv.db
        +fv.log
        +shipment_service.fvshared.constants
        +shipment_service.fvshared.db_no_orm
        +shipment_service.fvshared.oauth2
        +fv.secrets
        +fv.aws.lambdas.shipments.constants
        +fv.aws.lambdas.shipments.shipments
    }
    class Globals {
        +DB_CONN : FvDatabaseConnector
        +SECRETS : Secrets
    }
    class Functions {
        +find_origin_locations(cursor,event,count)
        +find_destination_locations(cursor,event,count)
        +find_locations(cursor,event,count,origin_or_destination)
    }
    class Helpers {
        +get_query_parameter(event, "query")
        +get_user_org_id(event)
        +shipment_visibility_conditions(leg_filtering=False)
        +basic_filter_pagination(event)
    }
    Modules --> Globals
    Modules --> Functions
    Functions --> Helpers
    Globals --> Helpers
    Globals -- uses --> Modules
    Helpers -- calls --> Modules
```

> SVG rendering failed for this diagram.
