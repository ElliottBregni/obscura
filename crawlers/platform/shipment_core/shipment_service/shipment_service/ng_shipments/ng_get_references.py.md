# Diagram: shipment_core/shipment_service/shipment_service/ng_shipments/ng_get_references.py


> Auto-generated by Obscura crawlers

## Diagram 1

```mermaid
classDiagram
class fv.db.FvDatabaseConnector {
  +establish_connection()
  +cursor
}
class Secrets {
  +__init__()
}
class module_globals {
  +DB_CONN : FvDatabaseConnector
  +SECRETS : Secrets
}
class lambda_handler {
  +lambda_handler(event, context, audit_refs)
}
class process_reference {
  +process_reference(cursor, event, reference=None)
}
class db_module {
  +get_reference_values(cursor, event, qualifiers, non_ref_sql=None, count=False)
  +get_filter_from_view(cursor, event, view, field, table_version, is_dealer)
  +process_country_filters(cursor, event, count)
}
class shipments_module {
  +shipment_visibility_conditions(leg_filtering=False)
}
class fv.aws.lambdas {
  +get_query_parameter(event, name, default=None)
  +make_response(response)
  +mandatory_lambda_handling()
  auth
  constants
}
class auth {
  +get_user_org_types(event)
}
class constants_mod {
  +OrgTypes
  +DEALER
}
module_globals --> fv.db.FvDatabaseConnector : "instance of"
module_globals --> Secrets : "instance of"
lambda_handler --> module_globals : "uses DB_CONN, SECRETS"
lambda_handler --> process_reference : "calls"
process_reference --> fv.aws.lambdas : "calls get_query_parameter, auth"
process_reference --> db_module : "calls get_reference_values / get_filter_from_view"
process_reference --> shipments_module : "uses FilterReference"
process_reference --> constants_mod : "reads FilterReference"
db_module ..> "database" : "operates on"
fv.aws.lambdas --> auth : "contains"
fv.aws.lambdas --> constants_mod : "contains"
```

> SVG rendering failed for this diagram.

## Diagram 2

```mermaid
flowchart LR
Event[Incoming HTTP Event] --> Handler[lambda_handler(event, context, audit_refs)]
Handler --> EstablishDB[DB_CONN.establish_connection()]
EstablishDB --> Cursor[cursor = DB_CONN.cursor]
Cursor --> ProcessRef[process_reference(cursor, event)]
ProcessRef --> CheckRef{reference provided?}
CheckRef -- no --> GetQuery[fv.aws.lambdas.get_query_parameter(event, "reference")]
GetQuery --> CheckRef
ProcessRef --> Auth[auth.get_user_org_types(event)]
Auth --> IsDealer{OrgTypes.DEALER in org_profiles}
IsDealer --> DecisionFilter{reference == SERVICE_CODE?}
DecisionFilter -- yes --> DBFilter[db.get_filter_from_view(...)]
DecisionFilter -- no --> DBRefs[db.get_reference_values(cursor, event, reference_list)]
DBFilter --> ResponseData[response]
DBRefs --> ResponseData
ResponseData --> MakeResp[fv.aws.lambdas.make_response(response)]
MakeResp --> Return[Return HTTP response]
```

> SVG rendering failed for this diagram.
