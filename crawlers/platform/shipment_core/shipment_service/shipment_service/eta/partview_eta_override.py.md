# Diagram: shipment_core/shipment_service/shipment_service/eta/partview_eta_override.py


> Auto-generated by Obscura crawlers

## Diagram 1

```mermaid
classDiagram
    class ManualEtaOverrideEventProcessor {
        - __sqs
        - __stage
        - __queueName
        - __queue
        - __event_code
        + __init__()
        + send(event, csv_rows)
    }
    class functions {
        + parse_txt_csv(csv_text)
        + lambda_handler(event, context, audit_refs)
    }
    class Constants {
        <<enumeration>>
        _ETA_KEY
        _PKG_CONTAINER_KEY
        _LOCATION_CODE
        _REQUIRED_HEADERS
        _MAX_PKG_COUNT
        _INVALID_DATE
        _MISSING_REQUIRED_COLUMN
        AUTH_CHECK
    }
    class ExternalModules {
        <<interface>>
        boto3
        fv.aws.lambdas
        eta_override_utils
        fv.error
        datetime
        logger
    }
    ManualEtaOverrideEventProcessor --> ExternalModules : uses boto3, datetime
    functions --> ExternalModules : uses fv.aws.lambdas, eta_override_utils, fv.error, logger
    functions --> ManualEtaOverrideEventProcessor : instantiates
    class Constants <|-- functions
    class Constants <|-- ManualEtaOverrideEventProcessor
```

> SVG rendering failed for this diagram.

## Diagram 2

```mermaid
flowchart TD
    A([lambda_handler invoked]) --> B{Get timezone \n and solution_id}
    B --> C[provided_csv = event.body]
    C --> D{provided_csv is None?}
    D -- Yes --> E[raise BadRequestError: "Missing request body"]
    D -- No --> F[validate headers via eta_override_utils.has_valid_headers]
    F --> G{headers valid?}
    G -- No --> H[raise BadRequestError: "Invalid columns..."]
    G -- Yes --> I[parse_txt_csv(provided_csv) -> csv_rows]
    I --> J{len(csv_rows) == 0?}
    J -- Yes --> K[raise BadRequestError: "No records provided..."]
    J -- No --> L{len(csv_rows) > _MAX_PKG_COUNT?}
    L -- Yes --> M[raise BadRequestError: "Must have less than ... rows"]
    L -- No --> N[skipped_pkgcontainers = {invalid_date:[], missing_required_column:[]}]
    N --> O[(validate_has_required_columns(csv_rows))]
    O --> P[valid_column_rows, invalid_column_rows]
    P --> Q[append invalid_column_rows -> skipped_pkgcontainers[missing_required_column]]
    Q --> R[(adjust_csv_to_utc(valid_column_rows, timezone))]
    R --> S[valid_csv_rows_utc, invalid_date_rows]
    S --> T[append invalid_date_rows -> skipped_pkgcontainers[invalid_date]]
    T --> U{len(valid_csv_rows_utc) == 0?}
    U -- Yes --> V[raise BadRequestError: "No valid records to process..."]
    U -- No --> W[logger.info counts]
    W --> X[eta_processor = ManualEtaOverrideEventProcessor()]
    X --> Y[eta_processor.send(event, csv_rows=valid_csv_rows_utc)]
    Y --> Z[return make_response({override_count, invalid_rows}, status_code)]
    Z --> END([done])
```

> SVG rendering failed for this diagram.
