# Diagram: shipment_core/shipment_service/shipment_service/eta/e2e/test_entity_eta_expiration.py


> Auto-generated by Obscura crawlers

## Diagram 1

```mermaid
flowchart TD
  A[Test: test_entity_eta_expiration_triggers_intermediate_eta_recalculation] --> B[EntityApiClient.post_entity_status_update]
  B --> C[planned_trip_leg: intermediate ETA created on PTL]
  B --> D[entity_eta_expired: row inserted]
  D --> E[Active entity sync check]
  E --> F[DB update: set expired_time = now (artificial)]
  F --> G[Milestone watcher runs (periodic)]
  G --> H[Expiration consumer processes entity_eta_expired]
  H --> I[Recalculate ETA for VIN]
  I --> J[Update planned_trip_leg ETA]
  J --> K[assert_truthy_eventually verifies ETA changed]
  style A fill:#f9f,stroke:#333,stroke-width:1px
  style G fill:#ffd,stroke:#333,stroke-width:1px
  style H fill:#ffd,stroke:#333,stroke-width:1px
```

> SVG rendering failed for this diagram.

## Diagram 2

```mermaid
flowchart TD
  A2[Test: test_new_milestone_takes_precedence_over_recalculation] --> B2[EntityApiClient.post_entity_status_update (REL)]
  B2 --> C2[entity_progress_update -> VIN ETA recorded (eta1)]
  B2 --> D2[entity_eta_expired: row inserted]
  D2 --> E2[Active entity sync check]
  E2 --> F2[DB update: set expired_time = artificial_expired_time]
  F2 --> G2[Milestone watcher runs]
  G2 --> H2[Expiration consumer triggers recalculation -> VIN ETA changes to eta2]
  H2 --> I2[EntityApiClient.post_entity_status_update (CST) new milestone]
  I2 --> J2[New milestone creates a new ETA (eta3)]
  J2 --> K2[assert_truthy_eventually verifies eta3 != eta2]
  style A2 fill:#f9f,stroke:#333,stroke-width:1px
  style G2 fill:#ffd,stroke:#333,stroke-width:1px
  style I2 fill:#cfc,stroke:#333,stroke-width:1px
```

> SVG rendering failed for this diagram.
