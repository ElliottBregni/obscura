# Diagram: shipment_core/shipment_service/shipment_service/eta/.gitlab-ci.yml


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
flowchart LR
  subgraph Templates
    ETA_PIP[".eta-within-shipment-pip"]
    PY_TEST[".py-test-template"]
    RUFF[".ruff-strict"]
    PYRIGHT[".pyright"]
    SLS[".sls"]
  end

  subgraph Jobs
    tests["eta-within-shipment-tests"]
    ruff["eta-within-shipment-ruff"]
    pyright_job["eta-within-shipment-pyright"]
    sls_template[".sls-eta-lambdas"]
    sls_manual["sls-manual-deploy-eta-stack"]
    sls_push["sls-deploy-eta-stack-on-push"]
    docker_login["docker_gitlab_runner_login"]
  end

  ETA_PIP -.extends.-> tests
  PY_TEST -.extends.-> tests
  ETA_PIP -.extends.-> ruff
  RUFF -.extends.-> ruff
  ETA_PIP -.extends.-> pyright_job
  PYRIGHT -.extends.-> pyright_job

  SLS -.extends.-> sls_template
  sls_template -.extends.-> sls_manual
  sls_template -.extends.-> sls_push

  docker_login -->|needs| sls_push
  tests -->|optional needs| sls_push

  deploy_paths["deploy-eta-lambdas-change-paths (anchor)"]
  deploy_paths -->|used by rules/changes| sls_manual
  deploy_paths -->|used by rules/changes| sls_push

  tests -->|script: pytest (ignores e2e), cov| (end1[end])
  ruff -->|linting| (end2[end])
  pyright_job -->|linting (LINT_DIRECTORIES: .)| (end3[end])

  sls_manual -->|rule: merge_request_event (manual)| (sls_manual_end[end])
  sls_push -->|rules on push with branch conditions| (sls_push_end[end])

  classDef tpl fill:#f9f,stroke:#333,stroke-width:1px;
  class ETA_PIP,PY_TEST,RUFF,PYRIGHT,SLS tpl
  class tests,ruff,pyright_job,sls_template,sls_manual,sls_push job
```

> SVG rendering failed for this diagram.
