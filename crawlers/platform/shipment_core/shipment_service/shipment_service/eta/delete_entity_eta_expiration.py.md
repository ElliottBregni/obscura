# Diagram: shipment_core/shipment_service/shipment_service/eta/delete_entity_eta_expiration.py


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
classDiagram
    class LambdaModule {
        +tracer
        +logger
        +DB_CONN_ENTITY : FvDatabaseConnector
        +sns_topics : SnsTopics
        +SEND_TO_EVENTS_SNS : bool
        +_validate_required_params(internal_entity_id, event_ts) : list[string]
        +lambda_handler(event, context) : dict
    }
    class ExpirationDAO {
        +delete_expiration_record_for_internal_entity_id(internal_entity_id)
    }
    class SnsTopics {
        +send_to_eta_event(payload, deduplication_id)
    }
    class FvDatabaseConnector {
        +__init__(name, secret)
    }
    class ClearEtaExpiration {
        +objectType
        +objectId
        +eventTs
        +solutionId
    }
    class AwsLambdas {
        +get_request_id(event)
    }
    class Decorators {
        +fv_datadog_wrap_lambda_handler
        +rollbar_lambda_function
    }

    LambdaModule --> ExpirationDAO : creates
    LambdaModule --> SnsTopics : uses
    LambdaModule --> FvDatabaseConnector : DB_CONN_ENTITY instance of
    LambdaModule --> ClearEtaExpiration : constructs
    LambdaModule --> AwsLambdas : calls get_request_id
    LambdaModule ..> Decorators : decorated by
    ExpirationDAO <-- FvDatabaseConnector : uses connector
    SnsTopics <-- ClearEtaExpiration : accepts payload
    note right of LambdaModule : flow: validate params -> request_id -> optional SNS -> delete expiration -> return 200
```

> SVG rendering failed for this diagram.
