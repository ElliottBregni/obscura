# Diagram: shipment_core/shipment_service/shipment_service/eta/scripts/rollback_eta_data.py


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
flowchart LR
    A[main()] --> B[get_shipments_to_revert(cursor, ts)]
    B --> DB[(FvDatabaseConnector.cursor)]
    DB -->|execute SQL SELECT| C[fetchall() -> list of shipments]
    A --> D[Progress(len(shipments_to_revert))]
    D --> E[for i, shipment in enumerate(shipments_to_revert)]
    E --> F[output_progress(i)]
    E --> G[rollback_shipment(cursor, shipment)]
    G --> H[prepare data: {eta, shipment_id}]
    H --> I[UPDATE shipment_stops SET eta=%(eta)s WHERE shipment_id=%(shipment_id)s]
    I --> J[print(cursor.query.decode())]
    G --> K[UPDATE shipments SET eta=%(eta)s WHERE id=%(shipment_id)s]
    K --> L[print(cursor.query.decode())]
    style DB fill:#f9f,stroke:#333,stroke-width:1px
    style C fill:#fffbcc
    style D fill:#ccf2ff
    style G fill:#e6ffe6
```

> SVG rendering failed for this diagram.
