# Diagram: shipment_core/shipment_service/shipment_service/eta/watchers/shipment_rail_eta_daily_extension_watcher.py


> Auto-generated by Obscura crawlers

## Diagram 1

```mermaid
classDiagram
    class ShipmentRailEtaDailyExtensionWatcher {
        +str service_name
        +cursor
        +function
        +__init__()
        +__call__()
        +batch_up_extension_requests()
    }
    class send_batch_to_extender_lambda {
        +batch: list
    }
    class lambda_handler {
        +event: dict
        +context: dict
    }
    class fv.db.FvDatabaseConnector <<external>>
    class shipment_rail_db_extension_queries <<external>>
    class eta_utils <<external>>
    class fv.aws.lambdas <<external>>
    class eta_constants <<external>>
    ShipmentRailEtaDailyExtensionWatcher --> fv.db.FvDatabaseConnector : uses
    ShipmentRailEtaDailyExtensionWatcher --> shipment_rail_db_extension_queries : calls get_rail_shipments_to_extend_eta
    ShipmentRailEtaDailyExtensionWatcher --> eta_utils : calls chunk_list
    send_batch_to_extender_lambda --> fv.aws.lambdas : invokes invoke_lambda
    ShipmentRailEtaDailyExtensionWatcher ..> send_batch_to_extender_lambda : calls
    lambda_handler --> ShipmentRailEtaDailyExtensionWatcher : instantiates
    lambda_handler --> fv.aws.lambdas : returns make_response
    lambda_handler ..> send_batch_to_extender_lambda : triggers via watcher
```

> SVG rendering failed for this diagram.

## Diagram 2

```mermaid
flowchart TD
    A[lambda_handler(event, context)] --> B[Create ShipmentRailEtaDailyExtensionWatcher]
    B --> C[Establish DB connection via FvDatabaseConnector]
    C --> D[batch_up_extension_requests()]
    D --> E[get_rail_shipments_to_extend_eta(cursor, limit=QUERY_LIMIT)]
    E --> F{records returned}
    F -->|0..N| G[Extract record ids]
    G --> H[chunk_list(all_record_ids, 50)]
    H --> I[For each batch]
    I --> J[send_batch_to_extender_lambda(batch)]
    J --> K[fv.aws.lambdas.invoke_lambda(name="extend_rail_eta", body=... , invoke_type="Event")]
    K --> L[Invoke complete]
    L --> M[fv.aws.lambdas.make_response({}, 200)]
    M --> N[Return response from lambda_handler]
```

> SVG rendering failed for this diagram.
