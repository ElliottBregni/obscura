# Diagram: shipment_core/shipment_service/shipment_service/eta/watchers/entity_eta_expiration_milestone_watcher.py


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
flowchart LR
  subgraph Init
    A[configure_structlog("info", AWS_STAGE)] --> B[configure_rollbar()]
    B --> C[tracer = trace.get_tracer(__name__)]
    C --> D[DB_CONN_ENTITY = FvDatabaseConnector(SERVICE_NAME, SecretNames.ENTITY_DATABASE)]
  end

  subgraph Lambda
    L[lambda_handler(event, context)]
    L -. datadog wrapper .-> L2[rollbar.lambda_function wrapper]
    L2 --> Q[queue_expired_etas()]
    Q --> E[ExpirationDAO(DB_CONN_ENTITY)]
    E --> G[get_expired_expected_interval_entities(limit, expiration_source=MILESTONE)]
    G --> H[logger.info(...)]
    H --> P[prep_sns_messages(expired_records)]
    P --> S[construct_sns_message(message, group_id)]
    S --> M[queue_expired_milestone(sns_messages)]
    M --> C1[eta_utils.chunk_list(sns_messages, 10)]
    C1 --> F[send_batch(sns_topic, batch)]
    L2 --> R[make_response("Watcher run successfully", 200)]
  end

  style Init fill:#f9f,stroke:#333,stroke-width:1px
  style Lambda fill:#bbf,stroke:#333,stroke-width:1px
  A --- B
  L --> Q
  F --> R
  R --> end((Done))
```

> SVG rendering failed for this diagram.
