# Diagram: shipment_core/shipment_service/shipment_service/eta/watchers/entity_eta_expiration_frozen_watcher.py


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
flowchart LR
  LH[lambda_handler<br/>(wrapped by datadog & rollbar)]
  LH --> QE[queue_expired_etas()]
  subgraph Init["Initialization"]
    TR[tracer = trace.get_tracer(__name__)]
    SL[configure_structlog("info", AWS_STAGE)]
    RB[configure_rollbar()]
    DB[DB_CONN_ENTITY = FvDatabaseConnector(SERVICE_NAME, SecretNames.ENTITY_DATABASE)]
  end
  Init --> LH
  QE --> DAO[ExpirationDAO(DB_CONN_ENTITY)]
  DAO --> GET[get_expired_expected_interval_entities(limit, expiration_source=FROZEN)]
  GET --> LOG[logger.info("N entities found with expired frozen ETAs")]
  GET --> PREP[prep_sns_messages(expired_records)]
  PREP --> BATCH[queue_expired_frozen(sns_messages)]
  BATCH --> CHUNK[eta_utils.chunk_list(sns_messages, 10)]
  CHUNK --> SEND[fv.aws.lambdas.send_batch(sns_topic, batch)]
  LH --> RESP[fv.aws.lambdas.make_response("Watcher run successfully", 200)]
  PREP -->|calls| SNSCON[fv.aws.sns.construct_sns_message(message, group_id)]
  style Init fill:#f8f9fa,stroke:#333,stroke-width:1px
  style LH fill:#e6f7ff,stroke:#1f78b4
  style QE fill:#fff4e6,stroke:#ff8c00
  style DAO fill:#fff,stroke:#333
  style SEND fill:#e6ffe6,stroke:#33a02c
```

> SVG rendering failed for this diagram.
