# Diagram: shipment_core/shipment_service/shipment_service/eta/eta_milestone_update/transform_status_update_to_segment/transform_status_update_to_segment.py


> Auto-generated by Obscura crawlers

## Diagram 1

```mermaid
classDiagram
    class ShipmentInfoForSegmentTransform {
    }
    class ReplayContextForSegmentTransform {
        +str creator_shipment_id
        +str carrier_id
    }
    ShipmentInfoForSegmentTransform <|-- ReplayContextForSegmentTransform

    class SegmentParameters {
        +organization_id
        +current_leg_eta
        +creator_shipment_id
        +mode_id
        +points_interest_date
        +current_leg_destination
        +solution_id
        +entity_list
        +replay_segment
        +in_transit_to_dealer
    }

    class StatusUpdateParameters {
        +status_update_id
        +solution_id
        +external_entity_id
        +in_transit_to_dealer
    }

    class TRANSFORM_TO_SEGMENT_ETA_ENABLED_MILESTONE_CODES {
    }

    class get_latest_actual_trip_leg_info {
        +cursor_entity, external_entity_id, solution_id
        -> ShipmentInfoForSegmentTransform | None
    }
    class get_shipment_info_for_segment_transform {
        +cursor_shipment, creator_shipment_id, solution_id, carrier_id
        -> ShipmentInfoForSegmentTransform | None
    }
    class get_status_update_details {
        +cursor_entity, status_update_id
        -> {code}
    }
    class get_context_for_segment_eta_replay {
        +cursor_entity, cursor_shipment, external_entity_id, solution_id
        -> ReplayContextForSegmentTransform | None
    }
    class transform_params_to_segment_if_enabled {
        +cursor_entity, cursor_shipments, params
        -> SegmentParameters | StatusUpdateParameters
    }

    transform_params_to_segment_if_enabled ..> get_status_update_details : calls
    transform_params_to_segment_if_enabled ..> TRANSFORM_TO_SEGMENT_ETA_ENABLED_MILESTONE_CODES : checks
    transform_params_to_segment_if_enabled ..> get_context_for_segment_eta_replay : calls
    get_context_for_segment_eta_replay ..> get_latest_actual_trip_leg_info : calls
    get_context_for_segment_eta_replay ..> get_shipment_info_for_segment_transform : calls
    transform_params_to_segment_if_enabled ..> SegmentParameters : may return
    transform_params_to_segment_if_enabled ..> StatusUpdateParameters : may return
```

> SVG rendering failed for this diagram.

## Diagram 2

```mermaid
flowchart TD
    A[Start: transform_params_to_segment_if_enabled] --> B{get_status_update_details(params.status_update_id)}
    B -->|no result or missing code| R[Return params (StatusUpdateParameters)]
    B -->|has code| C[Lookup enabled_codes = TRANSFORM_TO_SEGMENT_ETA_ENABLED_MILESTONE_CODES[params.solution_id]]
    C -->|enabled_codes empty or code not in enabled_codes| R
    C -->|code in enabled_codes| D[get_context_for_segment_eta_replay(...)]
    D -->|no context| R
    D -->|context found| E[Build SegmentParameters from context + params]
    E --> F[logger.info(...)]
    F --> S[Return SegmentParameters]
    R --> End[End] 
    S --> End
```

> SVG rendering failed for this diagram.
