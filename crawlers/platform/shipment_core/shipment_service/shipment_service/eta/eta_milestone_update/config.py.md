# Diagram: shipment_core/shipment_service/shipment_service/eta/eta_milestone_update/config.py


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
flowchart LR
  subgraph Constants
    ENV_INITIAL["INITIAL_ETA_MILESTONES\n(os.getenv -> TypeAdapter(dict[str, list[str]]).validate_json)"]
    CONST_TRANSFORM["TRANSFORM_TO_SEGMENT_ETA_ENABLED_MILESTONE_CODES\n(dict)"]
    CONST_UPFITTER["ETA_MILESTONES_FOR_UPFITTER_PARTNER_TYPE\n(dict)"]
  end

  subgraph Functions
    GIM["get_initial_eta_milestone_codes(solution_id)"]
    ETA_TRIG["eta_triggering_milestone_codes(solution_id)\n@cached TTLCache"]
    ETA_EST["eta_estimated_milestone_codes(solution_id)\n@cached TTLCache"]
  end

  subgraph AWS
    LAMBDA["fv.aws.lambdas.invoke_lambda('get_system_configuration')"]
  end

  ENV_INITIAL -->|lookup by solution_id| GIM
  GIM -->|returns list or None| Caller[Caller]

  ETA_TRIG -->|calls| LAMBDA
  LAMBDA -->|response| ETA_TRIG
  ETA_TRIG -->|if 200 and body != []| PARSE_TRIG[parse JSON -> response[0] -> get qualifier -> json.loads(milestone_code_list)]
  PARSE_TRIG -->|returns list| Caller

  ETA_EST -->|calls| LAMBDA
  LAMBDA -->|response| ETA_EST
  ETA_EST -->|if 200 and body != []| PARSE_EST[parse JSON -> response[0] -> return metadata]
  PARSE_EST -->|returns list| Caller

  CONST_TRANSFORM -.->|static mapping| Caller
  CONST_UPFITTER -.->|static mapping| Caller

  style ENV_INITIAL fill:#f9f,stroke:#333,stroke-width:1px
  style LAMBDA fill:#ffeb99,stroke:#333,stroke-width:1px
  style ETA_TRIG fill:#e6f7ff,stroke:#333,stroke-width:1px
  style ETA_EST fill:#e6f7ff,stroke:#333,stroke-width:1px
  style GIM fill:#e6ffe6,stroke:#333,stroke-width:1px
```

> SVG rendering failed for this diagram.
