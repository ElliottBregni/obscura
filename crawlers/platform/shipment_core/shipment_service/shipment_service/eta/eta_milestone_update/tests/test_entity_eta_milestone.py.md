# Diagram: shipment_core/shipment_service/shipment_service/eta/eta_milestone_update/tests/test_entity_eta_milestone.py


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
classDiagram
    class TestCase {
        +str entity_external_id
        +int entity_internal_id
        +str solution_id
        +str entity_state
        +datetime entity_state_change_ts
        +str? ultimate_destination
        +str? last_entity_progress_update
        +insert_record(cursor) int
        +cleanup_record(cursor)
    }
    class _set_up_test_case {
        +(test_case, db_conn_entity) generator
    }
    class Fixtures {
        +tc_basic_vin
        +tc_inactive_vin
        +tc_vin_with_ult_dest_and_last_entity_progress_update
    }
    class EntityEtaMilestoneData {
        +solution_id
        +entity_external_id
        +entity_state
        +entity_state_change_ts
        +ultimate_destination_location_id
        +ultimate_destination_location_code
        +destination_eta
        +eta_event_ts
        +next_milestone_eta_date
    }
    class get_entity_eta_milestone_data {
        +(cursor_entity, solution_id, entity_id) -> EntityEtaMilestoneData|None
    }
    class ensure_tz_present {
        +(dt) -> datetime
    }
    class TestEntityEtaMilestoneData {
        +test_no_entity_found()
        +test_found_basic_entity()
        +test_find_by_entity_internal_id()
        +test_inactive_entity()
        +test_vin_with_ult_dest_and_last_entity_progress_update()
    }

    TestCase --> _set_up_test_case : used by
    Fixtures --> _set_up_test_case : yields from
    TestEntityEtaMilestoneData --> Fixtures : depends on
    TestEntityEtaMilestoneData --> get_entity_eta_milestone_data : calls
    get_entity_eta_milestone_data --> EntityEtaMilestoneData : returns
    get_entity_eta_milestone_data --> ensure_tz_present : uses
    TestCase --> EntityEtaMilestoneData : verifies fields against
    TestEntityEtaMilestoneData --> TestCase : creates instances of
    ensure_tz_present ..> "dateutil.parse.parse" : interoperates with
    note right of Fixtures : fixtures insert and cleanup records in DB via Cursor and FvDatabaseConnector
```

> SVG rendering failed for this diagram.
