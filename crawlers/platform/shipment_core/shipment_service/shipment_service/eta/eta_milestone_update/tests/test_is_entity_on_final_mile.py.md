# Diagram: shipment_core/shipment_service/shipment_service/eta/eta_milestone_update/tests/test_is_entity_on_final_mile.py


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
classDiagram
    class ShipmentStop <<dataclass>> {
        +int stop_sequence
        +datetime eta
    }
    class _Location <<dataclass>> {
        +int location_id
        +int resolved_location_id
    }
    class Locations {
        +NEWK_F: _Location
        +LNDN_F: _Location
    }
    class _Organization <<dataclass>> {
        +int id
        +str fv_id
    }
    class Organizations {
        +FV_DPU_F: _Organization
        +TEST_Carrier: _Organization
    }
    class Shipment <<dataclass>> {
        +str creator_shipment_id
        +int? destination_location_id
        +int created_by_org_id
        +int carrier_organization_id
        +int mode_id
    }
    class TestCase <<dataclass>> {
        +str description
        +Shipment shipment
        +list~ShipmentStop~ shipment_stops
    }
    class InstantiatedTestCase <<dataclass>> {
        +TestCase test_case
        +int internal_shipment_id
    }
    class _insert_records() {
        +Cursor cursor
        +TestCase test_case
        +returns int internal_shipment_id
    }
    class _set_up_test_case() {
        +TestCase test_case
        +FvDatabaseConnector db_conn_shipments
        +yields InstantiatedTestCase
    }
    class happy_path_case <<fixture>> {
        +yields InstantiatedTestCase
    }
    class missing_destination_on_shipment <<fixture>> {
        +yields InstantiatedTestCase
    }
    class TestIsEntityOnFinalMile <<integration_test>> {
        +test_happy_path_match_locations()
        +test_cannot_find_last_shipment()
        +test_no_location_for_shipment_destination_newloc_id()
        +test_locations_do_not_match()
        +test_entity_has_no_ult_destination()
    }

    %% Associations
    ShipmentStop --> Shipment : "belongs to (via shipment_stops list)"
    TestCase o-- Shipment : contains
    TestCase "1" --> "0..*" ShipmentStop : shipment_stops
    InstantiatedTestCase o-- TestCase : wraps
    Locations "1" o-- "_Location" : contains
    Organizations "1" o-- "_Organization" : contains

    %% Function interactions
    _set_up_test_case ..> _insert_records : calls
    happy_path_case ..> _set_up_test_case : uses
    missing_destination_on_shipment ..> _set_up_test_case : uses
    TestIsEntityOnFinalMile ..> happy_path_case : depends on
    TestIsEntityOnFinalMile ..> missing_destination_on_shipment : depends on
    TestIsEntityOnFinalMile ..> is_entity_on_final_mile : invokes

    %% External
    class is_entity_on_final_mile {
        +cursor_shipment
        +last_shipment_id
        +resolved_entity_ult_dest_loc_id
        +organization_id
        +returns bool
    }
```

> SVG rendering failed for this diagram.
