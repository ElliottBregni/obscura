# Diagram: shipment_core/shipment_service/shipment_service/eta/tests/test_entity_eta_expiration.py


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
classDiagram
    class TestExpirationDAO {
        +test_insert_eta_expired()
        +db_instance (fixture)
        +cleanup_expired_record (fixture)
        +db_conn_entity (fixture)
    }
    class ExpirationDAO {
        +_insert_eta_expired(entity_id, expired_time, status_update_id, location_org_id, source)
        +persist_eta_expired(entity_id, expired_time, status_update_id, location_org_id, source) (-> new_record_id, is_update)
    }
    class DBConnEntity {
        +connection
        +cursor()
    }
    class EtaExpirationSource <<enumeration>> {
        FROZEN
        // other sources
    }

    TestExpirationDAO --> ExpirationDAO : uses db_instance
    TestExpirationDAO --> DBConnEntity : uses db_conn_entity
    TestExpirationDAO --> "cleanup_expired_record" : manages list
    TestExpirationDAO ..> EtaExpirationSource : references
    ExpirationDAO --> DBConnEntity : executes SQL via connection.cursor()
    ExpirationDAO ..> EtaExpirationSource : accepts source
    DBConnEntity --> "entity_eta_expired table" : performs INSERT/SELECT/DELETE
    note right of TestExpirationDAO : Asserts inserted record matches inputs and persist updates existing record
```

> SVG rendering failed for this diagram.
