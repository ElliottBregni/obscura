# Diagram: shipment_core/shipment_service/shipment_service/eta/tests/test_entity_eta_frozen_expiration_handler.py


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
classDiagram
    class FakeLambdaClient {
        +invoke(FunctionName, InvocationType, Payload, ClientContext)
    }

    class EntityEtaExpirationFrozenHandler {
        +EntityEtaExpirationFrozenHandler(request_id, expiration_dao, entity_dao, db_cursor_shipments, db_cursor_entity, lambda_client)
        +handle(sqs_record) RecordHandledResponse
        +expired_eta_override_extension : timedelta
    }

    class RecordHandledResponse {
        <<enumeration>>
        EXPIRATION_NOT_FOUND
        INACTIVE_ENTITY
        SUCCESS
    }

    class TestEntityEtaMilestoneExpirationConsumer {
        +_initialize()
        +get_test_handler(db_cursor_shipments=None, db_cursor_entity=None)
        +test_process_record_empty_return_from_get_expiration_record()
        +test_process_record_inactive_entity()
        +test_process_record_success(db_conn_shipments, db_conn_entity)
        +test_process_record_matches_new_frozen_logic(db_conn_shipments, db_conn_entity)
        +test_process_record_has_default_if_negative(db_conn_shipments, db_conn_entity)
    }

    class FakeExpirationDAO {
        +set_db_for_record(record)
        +db : dict
        +new_expiration_record_id
    }

    class FakeEntityDAO {
        +set_response_get_entity_data(data)
        +get_entity_data()
    }

    class TestFixtures {
        +example_valid_sqs_record : dict
        +get_sqs_record(record_id) : dict
    }

    TestEntityEtaMilestoneExpirationConsumer --> EntityEtaExpirationFrozenHandler : creates/uses
    EntityEtaExpirationFrozenHandler --> FakeExpirationDAO : uses
    EntityEtaExpirationFrozenHandler --> FakeEntityDAO : uses
    EntityEtaExpirationFrozenHandler --> FakeLambdaClient : uses
    TestEntityEtaMilestoneExpirationConsumer --> FakeExpirationDAO : instantiates
    TestEntityEtaMilestoneExpirationConsumer --> FakeEntityDAO : instantiates
    TestEntityEtaMilestoneExpirationConsumer --> TestFixtures : uses
    TestFixtures --> TestEntityEtaMilestoneExpirationConsumer : provides
    FakeExpirationDAO o-- "db : dict" : contains
    RecordHandledResponse <|-- EntityEtaExpirationFrozenHandler : returns
    FakeEntityDAO <.. EntityEtaExpirationFrozenHandler : provides entity data
```

> SVG rendering failed for this diagram.
