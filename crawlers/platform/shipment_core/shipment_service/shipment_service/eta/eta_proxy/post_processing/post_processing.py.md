# Diagram: shipment_core/shipment_service/shipment_service/eta/eta_proxy/post_processing/post_processing.py


> Auto-generated by Obscura crawlers

## Diagram 1

```mermaid
classDiagram
    class EtaProxyParameters
    class RailShipmentParameters
    class ShipmentEtaParameters
    class EtaRouteConfigurationDAO {
        +lookup_route_configuration_spot_time(organization_fv_id, origin_location_id, destination_location_id)
    }
    class SpotTimeHelper {
        +get_spot_time_adjusted_eta(eta)
    }
    class EtaPushbackHelper {
        +_extend_eta_for_holidays_during_transit(eta)
        +_capped_eta_date(eta)
        +_adjust_eta_forward_holiday_sunday(eta)
    }
    class PostProcessorAggregate
    class PostProcessor

    EtaProxyParameters <|-- RailShipmentParameters
    EtaProxyParameters <|-- ShipmentEtaParameters

    create_spot_time_processor "function" as CSP
    create_sunday_holiday_and_eta_cap_processors "function" as CSHP
    apply_post_processor "function" as APP

    CSP ..> EtaRouteConfigurationDAO : instantiates
    CSP ..> SpotTimeHelper : instantiates when route_config
    CSP --> PostProcessor : returns PostProcessor (spot_time)
    CSHP ..> EtaPushbackHelper : instantiates
    CSHP --> PostProcessor : returns list[PostProcessor]
    APP --> PostProcessor : accepts PostProcessor
    APP --> PostProcessorAggregate : reads/writes
    APP ..> trace : records span events
    APP ..> logger : logs applied processors
```

> SVG rendering failed for this diagram.

## Diagram 2

```mermaid
flowchart TD
    A[Incoming request/event + params_obj] --> B{params_obj type check}
    B -- RailShipmentParameters and spot_time applicable --> C[create_spot_time_processor]
    B -- ShipmentEtaParameters --> D[create_sunday_holiday_and_eta_cap_processors returns []]
    C --> E{route_config lookup}
    E -- org_fv_id present --> F[EtaRouteConfigurationDAO.lookup_route_configuration_spot_time]
    F --> G[SpotTimeHelper.get_spot_time_adjusted_eta]
    G --> H[spot_time PostProcessor returned]
    B -- not rail or not applicable --> I[no spot_time processor]
    D --> J{destination country code from config}
    J -- present --> K[EtaPushbackHelper instantiated]
    K --> L[returns processors: extend, cap, adjust_forward]
    L --> M[PostProcessor list returned]
    H & M --> N[PostProcessorAggregate sequence]
    N --> O[apply_post_processor called per processor]
    O --> P{processor result}
    P -- datetime --> Q[update eta_date, record days_adjustment]
    P -- tuple(eta, attrs) --> Q
    Q --> R[trace.add_event & logger.info]
    Q --> S[final ETA and accumulated attributes]
```

> SVG rendering failed for this diagram.
