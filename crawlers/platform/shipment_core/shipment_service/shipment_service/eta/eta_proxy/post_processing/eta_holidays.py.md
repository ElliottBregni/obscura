# Diagram: shipment_core/shipment_service/shipment_service/eta/eta_proxy/post_processing/eta_holidays.py


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
flowchart LR
  GH[get_holidays(country_code, years, holiday_config)] --> EM[ETAHolidayManager(country_code, years, holiday_config)]
  EM --> P[populate_holidays()]
  P --> D{country_code}
  D -->|US| U[Instantiate USHolidays(years)]
  D -->|CA| C[Instantiate CAHolidays(years)]
  D -->|other| N[No holidays instantiated]
  U --> UP[USHolidays._populate(year) -> add_custom_holidays(year)]
  C --> CP[CAHolidays._populate(year) -> add_custom_holidays(year)]
  UP --> R[remove_unused_holidays()]
  CP --> R
  N --> R
  R --> RET[return holiday_manager.holidays]
  subgraph ETAHolidayManager
    EM
    P
    R
  end
  subgraph USHolidays_class
    U
    UP
  end
  subgraph CAHolidays_class
    C
    CP
  end
```

> SVG rendering failed for this diagram.
