# Diagram: shipment_core/shipment_service/shipment_service/eta/eta_proxy/post_processing/helpers.py


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
flowchart LR
  A[get_max_eta_date_config(solution_id)] -->|creates| B[EtaSystemConfigurationHandler]
  B --> C[get_eta_metadata(qualifier="ETA_DAYS_MAX", solution_id)]
  C --> D[returns eta metadata dict]

  E[get_destination_country_code(params_obj, org_id)] --> F{params_obj is PartviewEtaParameters?}
  F -- yes --> G[eta_helper.get_locations_by_codes(location_codes=[dest_loc_code], organization_id=org_id)]
  G --> H{loc_info is list with items?}
  H -- yes --> I[return loc_info[0].country]
  H -- no --> J[logger.warning(...) and return None]
  F -- no --> K[get_location_by_id(location_id=dest_loc_id, organization_id=org_id, dereference=True, bypass_visibility=True)]
  K --> L{loc_info truthy?}
  L -- yes --> M[return loc_info.country]
  L -- no --> N[logger.warning(...) and return None]

  O[get_max_eta_days_from_config(solution_id, eta_source, destination_country_code)] -->|calls| A
  A -->|if config| P[return config.get(eta_source, {}).get(destination_country_code)]
  A -->|else| Q[return None]

  R[get_org_fv_id(event, org_id)] --> S[invokinator_organization.get_organization_by_id(event, org_id)]
  S --> T{raises fv.error.HandledException?}
  T -- yes --> U[logger.warning(...) ; res = None]
  T -- no --> V[res object]
  V --> W{res is not None?}
  W -- yes --> X[return res.fv_id]
  W -- no --> Y[return None]

  Z[get_eta_holiday_config(event, org_fv_id)] --> AA[qualifier = ETA_HOLIDAYS]
  AA --> AB[fvshared_utils.invoke_get_shipment_system_config(event, qualifier, organization_fv_id)]
  AB --> AC{res_body is None or "[]"?}
  AC -- yes --> AD[logger.info(...) ; return None]
  AC -- no --> AE[config_str = json.loads(res_body)[0].get(qualifier)]
  AE --> AF{json.loads(config_str) succeeds?}
  AF -- yes --> AG[return parsed holiday config]
  AF -- no --> AH[logger.warning("Error parsing ETA holiday config") ; return None]
```

> SVG rendering failed for this diagram.
