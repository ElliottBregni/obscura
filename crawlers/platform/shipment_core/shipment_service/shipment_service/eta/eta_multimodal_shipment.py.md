# Diagram: shipment_core/shipment_service/shipment_service/eta/eta_multimodal_shipment.py


> Auto-generated by Obscura crawlers

## Diagram 1

```mermaid
classDiagram
    class Module {
        +DB_CONN: FvDatabaseConnector
        +SECRETS: Secrets
        +tracer
        +logger
        +get_max_eta_from_child_shipment_stops(child_shipment_stops)
        +get_arrival_time(child_shipment)
        +get_eta_for_multimodal_parent(event)
        +lambda_handler(event, context, audit_refs)
    }
    class FvDatabaseConnector {
        +establish_connection()
    }
    class Secrets
    class Utilities {
        +parse_as_dt(value, naive=True)
    }
    Module --> FvDatabaseConnector : DB_CONN
    Module --> Secrets : SECRETS
    Module ..> Utilities : uses
    Module ..> "fv.aws.lambdas" : uses
    Module ..> "fv.secrets" : uses
    Module ..> "shipment_service.fvshared.utilities" : uses
```

> SVG rendering failed for this diagram.

## Diagram 2

```mermaid
flowchart TD
    Start([start]) --> CheckChildShipments{child_shipments present?}
    CheckChildShipments -->|no or empty| LogNoChild[/"Log: No child shipments provided"\] --> ReturnNone1[/return (None, None)/]
    CheckChildShipments -->|yes| GetLast[/"last_shipment = child_shipments[-1]"\]
    GetLast --> CheckLastStatus{last_shipment.active_status not in (expired,cancelled)?}
    CheckLastStatus -->|yes| LastEta["last_shipment_eta = get_max_eta_from_child_shipment_stops(last_shipment)"]
    LastEta -->|not None| LogLastHasEta[/"Log: Last shipment in trip has an ETA"\] --> ReturnLast[/return (last_shipment_eta, meta)/]
    LastEta -->|None| Continue1([continue])
    CheckLastStatus -->|no| Continue1
    Continue1 --> BuildLists[/"eta_or_arrived_child_shipments = filter(...)"\n"active_child_shipments = filter(...)"\]
    BuildLists --> CheckEtaOrArrived{len(eta_or_arrived_child_shipments) > 0?}
    CheckEtaOrArrived -->|no| CheckActiveDestinations
    CheckEtaOrArrived -->|yes| LoopActive[/for each child_shipment in active_child_shipments:\n compute child_eta, arrival_time, update greatest_active_child_eta & delta/]
    LoopActive --> DeltaComputed{delta is not None?}
    DeltaComputed -->|no| CheckActiveDestinations
    DeltaComputed -->|yes| CalcWithDelta["last_scheduled_delivery_date = parse_as_dt(active_child_shipments[-1].destination_delivery_window[1])\nwith_delta = last_scheduled_delivery_date + delta"]
    CalcWithDelta --> ValidLastSched{last_scheduled_delivery_date is not None?}
    ValidLastSched -->|no| CheckActiveDestinations
    ValidLastSched -->|yes| ReturnWithDelta[/return (with_delta, meta)/]
    CheckActiveDestinations --> ActiveChildrenWithDest{any active child has destination_delivery_window[1]?}
    ActiveChildrenWithDest -->|yes| SelectGreatest["greatest_delivery_window_ceil = max(...)[1]"] --> ReturnGreatest[/return (greatest_delivery_window_ceil, None)/]
    ActiveChildrenWithDest -->|no| LogNone[/"Log: No conditions met, ETA set to None"\] --> ReturnNone2[/return (None, None)/]
    ReturnLast --> End([end])
    ReturnWithDelta --> End
    ReturnGreatest --> End
    ReturnNone1 --> End
    ReturnNone2 --> End
```

> SVG rendering failed for this diagram.
