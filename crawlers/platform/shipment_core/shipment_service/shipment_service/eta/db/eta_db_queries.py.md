# Diagram: shipment_core/shipment_service/shipment_service/eta/db/eta_db_queries.py


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
classDiagram
class ShipmentsModule {
  +cache_key_from(...)
  +raise_generic_exception(e)
  +insert_eta_update(cursor, eta_update)
  +get_trip_leg_shipment(cursor, org_id, shipment_id)
  +get_last_stop_eta(cursor, org_id, creator_shipment_id)
  +get_entity_eta_history(cursor, entity_id, offset, limit)
  +get_entity_eta_history_count(cursor, entity_id)
  +is_pending_dispatch(cursor, entity_id)
  +has_gm_hold_exceptions(cursor, entity_id)
  +get_shipment_if_entity_on_final_leg(cursor, entity_external_id, solution_id)
  +get_delivery_window_for_shipment(cursor_shipment, creator_shipment_id, org_id)
  +check_entity_for_frozen_eta(cursor_entity, solution_id, external_entity_id)
  +get_next_planned_location(cursor_entity, external_entity_id, solution_id, current_location_id)
  +get_city_state_country(cursor_shipment, location_id)
  +get_existing_eta_dict(cursor_entity, entity_ids)
  +get_final_destination_details(cursor_entity, entity_ids)
  +get_entity_reference(cursor, entity_external_ids, solution_id, qualifier, default)
}
class DatabaseCursor {
  +mogrify(sql, params)
  +execute(query)
  +fetchone()
  +fetchall()
}
class ETAConstants {
  +GENERIC_ERROR_MESSAGE
  +TRIGGER_TYPES
}
class Cache {
  +TTLCache(maxsize, ttl)
  +cached(decorator)
}
class Logger {
  +debug(msg)
  +info(msg)
}
class Psycopg2Errors {
  +UniqueViolation
}
class FVError {
  +DatabaseError(message, client_message)
}
ShipmentsModule --> DatabaseCursor : executes SQL via
ShipmentsModule --> ETAConstants : reads constants
ShipmentsModule --> Cache : applies caching to functions
ShipmentsModule --> Logger : logs queries and info
ShipmentsModule --> Psycopg2Errors : inspects exceptions
ShipmentsModule --> FVError : raises DatabaseError
DatabaseCursor <--|--fv.db.Cursor
insert_eta_update ..> DatabaseCursor : uses
get_trip_leg_shipment ..> DatabaseCursor : uses
get_last_stop_eta ..> DatabaseCursor : uses
get_entity_eta_history ..> DatabaseCursor : uses
get_entity_eta_history_count ..> DatabaseCursor : uses
is_pending_dispatch ..> DatabaseCursor : uses
has_gm_hold_exceptions ..> DatabaseCursor : uses
get_shipment_if_entity_on_final_leg ..> DatabaseCursor : uses
get_delivery_window_for_shipment ..> DatabaseCursor : uses
check_entity_for_frozen_eta ..> DatabaseCursor : uses
get_next_planned_location ..> DatabaseCursor : uses
get_city_state_country ..> DatabaseCursor : uses
get_existing_eta_dict ..> DatabaseCursor : uses
get_final_destination_details ..> DatabaseCursor : uses
get_entity_reference ..> DatabaseCursor : uses
```

> SVG rendering failed for this diagram.
