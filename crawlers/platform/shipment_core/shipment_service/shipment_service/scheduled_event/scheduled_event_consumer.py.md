# Diagram: shipment_core/shipment_service/shipment_service/scheduled_event/scheduled_event_consumer.py


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
flowchart TD
    subgraph Decorators
        DD1[fv.datadog.wrap_lambda_handler]
        DD2[ValidateHandler]
    end

    DD1 --> L[lambda_handler(event: ShipmentScheduledEventConsumerEvent, context: ContextSchema)]
    DD2 --> L

    subgraph Globals
        DB_CONN[FvDatabaseConnector("shipment_schedule_event_consumer", SecretNames.DATABASE)]
    end

    L --> |"calls"| DB_CONN_EST[DB_CONN.establish_connection()]
    L --> |"calls"| CURS[DB_CONN.get_cursor()]
    CURS --> REPO[ShipmentScheduledEventRepository(cursor)]
    L --> |"instantiates"| SRV[HandleShipmentScheduledEventService(repository)]
    L --> |"instantiates"| PROC[SqsBatchProcessorWithPartialItemFailure(handler=service)]

    PROC --> |"process_batch(event.model_dump(by_alias=True))"| PROC_RES[result]
    PROC_RES --> RESP[EventResponseSchema(**result)]
    RESP --> |"returns"| L_END[lambda_handler returns EventResponseSchema]
```

> SVG rendering failed for this diagram.
