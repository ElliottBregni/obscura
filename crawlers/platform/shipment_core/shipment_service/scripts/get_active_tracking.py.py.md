# Diagram: shipment_core/shipment_service/scripts/get_active_tracking.py.py


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
flowchart TD
  Start([Start]) --> ParseArgs[/"Parse CLI args (stage)"/]
  ParseArgs --> GetSession["Get DB session & engine\nutilities.get_session_and_engine_by_stage(stage)"]
  GetSession --> Expected["expected_shipments = utilities.get_shipments_by_time_window(session)"]
  Expected --> QueryShipments["Query shipments by IDs\nsession.query(tables.Shipments).filter(...).all()"]
  QueryShipments --> AssertLen{"assert len(shipments) == len(expected_shipments)"}
  AssertLen --> InitSets["Init sets: completed_ids, no_asset_ids"]
  InitSets --> ActiveTelem["active_telematics = get_active_telematics(session, expected_shipments)"]
  ActiveTelem --> ForEachS{"for each shipment s in shipments"}
  ForEachS --> CheckActive{"if s in active_telematics\n  -> skip"}
  CheckActive --> CheckAsset{"elif s.obc_asset_id is None\n  -> add to no_asset_ids"}
  CheckAsset --> CheckCompleted{"elif s.is_completed is True\n  -> add to completed_ids"}
  CheckCompleted --> LoopEnd[/"end for loop"/]
  LoopEnd --> ComputeErrors["Compute error_ids: expected - (active ∪ no_asset ∪ completed)"]
  ComputeErrors --> PrintSummary["Print summary counts"]
  PrintSummary --> PrintHeader["Print table header"]
  PrintHeader --> ForEachError{"for sid in error_ids"}
  ForEachError --> LastUpdate["last_update = get_last_telematic_time(session, sid)"]
  ForEachError --> Carrier["carrier = utilities.get_carrier(session, sid)"]
  ForEachError --> TimeRange["start, stop = utilities.get_shipment_time_range(session, sid)"]
  LastUpdate --> PrintRow["Print formatted row for sid"]
  Carrier --> PrintRow
  TimeRange --> PrintRow
  PrintRow --> NextError{"next sid / end loop"}
  NextError --> Dispose["engine.dispose()"]
  Dispose --> End([End])

  subgraph Functions
    direction TB
    GLT[get_last_telematic_time(session, shipment_id)]
    GLT -->|query ShipmentStatus with lon/lat| GLTQuery[".order_by(desc(updated_at)).first()"]
    GLTQuery --> GLTReturn["return timestampZ or None"]

    GAT[get_active_telematics(session, shipment_list)]
    GAT -->|now, past = now - 30min| GATQuery["query ShipmentStatus\nfilter lon/lat, updated_at>past, id in list\n.distinct(...).all()"]
    GATQuery --> GATReturn["return set(shipment_ids)"]
  end

  GetSession -.-> GLT
  GetSession -.-> GAT
```

> SVG rendering failed for this diagram.
