# Diagram: shipment_core/shipment_service/scripts/get_stale_obc.py


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
flowchart TD
  A[main(argv)] --> B[parse args]
  B --> C[utilities.get_session_and_engine_by_stage(stage) --> (session, engine)]
  C --> D[get_active_telematics(session)]
  D --> D1[now = utcnow()]
  D --> D2[past = now - 30 minutes]
  D --> D3[session.query(tables.ShipmentStatus)]
  D3 --> D4[filter longitude != None]
  D3 --> D5[filter latitude != None]
  D3 --> D6[filter updated_at > past]
  D3 --> D7[distinct by shipment_id]
  D7 --> D8[.all() -> updates]
  D8 --> D9[shipment_ids = [s.shipment_id for s in updates]]
  D9 --> D10[return set(shipment_ids)]
  C --> E[utilities.get_shipments_by_time_window(session) -> expected_list]
  D10 --> F[active_obc_list]
  E --> G[expected_list]
  F --> H[stale_list = active_obc_list - expected_list]
  G --> H
  H --> I[print(len(stale_list))]
  C --> J[engine.dispose()]
  I --> K[exit main]
```

> SVG rendering failed for this diagram.
