# Diagram: shipment_core/shipment_service/scripts/manully_record_geofence.py


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
flowchart TD
  Start([Start]) --> GetSession[Get database session & engine]
  GetSession --> OpenConn[Open DB connection via fv.db.get_connection]
  OpenConn --> OpenCursor[Get cursor]
  OpenCursor --> ExecuteQuery[Execute SELECT on shipment_statuses]
  ExecuteQuery --> FetchStatuses[Fetch statuses]
  FetchStatuses --> ForEachStatus{For each status}
  ForEachStatus --> GetShipment[get_shipment_by_db_id(session, status.shipment_id)]
  ForEachStatus --> GetStops[get_shipment_stops_by_shipment_db_id(session, status.shipment_id)]
  GetShipment --> FindCurStop[Find cur_stop where stop.id == status.shipment_stop_id]
  GetStops --> FindCurStop
  FindCurStop --> NoCurStop{cur_stop exists?}
  NoCurStop -- No --> SkipStatus[continue to next status]
  NoCurStop -- Yes --> CheckProcessed{cur_stop.id in stops_processed?}
  CheckProcessed -- Yes --> MarkProcessedAlready[Mark processed (no-op) and continue]
  CheckProcessed -- No --> RecordArrival[call record_arrival(session, shipment, last_stop, location, \"\", cur_stop, actual_created_at, stops)]
  RecordArrival --> RecordDeparture[call record_departure(session, shipment, last_stop, location, \"\", cur_stop, actual_created_at, stops)]
  RecordDeparture --> MarkProcessed[stops_processed[cur_stop.id] = True]
  MarkProcessed --> NextStatus[Proceed to next status]
  MarkProcessedAlready --> NextStatus
  SkipStatus --> NextStatus
  NextStatus --> ForEachStatus
  ForEachStatus -->|no more statuses| CloseCursor[Close cursor & connection]
  CloseCursor --> End([End])
```

> SVG rendering failed for this diagram.
