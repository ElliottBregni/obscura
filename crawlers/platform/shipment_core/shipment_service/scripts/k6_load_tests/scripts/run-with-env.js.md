# Diagram: shipment_core/shipment_service/scripts/k6_load_tests/scripts/run-with-env.js


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
flowchart LR
  Start([Start]) --> CheckStage{AWS_STAGE present?}
  CheckStage -- No --> Err[Error: "AWS_STAGE environment variable must be set."] 
  CheckStage -- Yes --> LogStage[Log Found AWS_STAGE value]
  LogStage --> CheckProfile{AWS_PROFILE present?}
  CheckProfile -- Yes --> LogProfile[Log Found AWS_PROFILE and request creds]
  LogProfile --> RunAwsCli[Run: aws configure export-credentials --profile=$AWS_PROFILE --format process]
  RunAwsCli --> ParseCreds[Parse JSON credentials]
  ParseCreds --> SetAccessKey[Set AWS_ACCESS_KEY_ID env var]
  SetAccessKey --> SetSecret[Set AWS_SECRET_ACCESS_KEY env var]
  ParseCreds -->|has SessionToken| SetSession[Set AWS_SESSION_TOKEN env var]
  CheckProfile -- No --> SkipAwsCli[Skip aws-cli credential request]
  SetSecret --> Continue
  SetSession --> Continue
  SkipAwsCli --> Continue
  Continue --> CheckRegion{AWS_REGION present?}
  CheckRegion -- No --> SetDefaultRegion[Set AWS_REGION="us-east-1" and log]
  CheckRegion -- Yes --> KeepRegion[Keep existing AWS_REGION]
  SetDefaultRegion --> AfterRegion
  KeepRegion --> AfterRegion
  AfterRegion --> BlankLine[Print blank line]
  BlankLine --> ReadArg[Read testFile from process.argv[2]]
  ReadArg --> BuildCmd[Build command: k6 run <testFile>]
  BuildCmd --> LogCmd[Log command string]
  LogCmd --> ExecK6[Execute k6 run with env]
  ExecK6 --> AttachStdout[Pipe stdout to process.stdout]
  ExecK6 --> AttachStderr[Pipe stderr to process.stderr]
  AttachStdout --> End([End])
  AttachStderr --> End
```

> SVG rendering failed for this diagram.
