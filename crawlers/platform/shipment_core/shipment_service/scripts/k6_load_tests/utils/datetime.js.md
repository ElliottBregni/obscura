# Diagram: shipment_core/shipment_service/scripts/k6_load_tests/utils/datetime.js


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
flowchart TD
A[Call offset(base, hours=0, minutes=0, seconds=0, milliseconds=0)]
B[Create next = new Date(base)]
H[Compute hoursArg = next.getHours() + hours]
M[Compute minutesArg = next.getMinutes() + minutes]
S[Compute secondsArg = next.getSeconds() + seconds]
MS[Compute msArg = next.getMilliseconds() + milliseconds]
C[next.setHours(hoursArg, minutesArg, secondsArg, msArg)]
D[return next]
A --> B
B --> H
B --> M
B --> S
B --> MS
H --> C
M --> C
S --> C
MS --> C
C --> D
```

> SVG rendering failed for this diagram.
