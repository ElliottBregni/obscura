# Diagram: shipment_core/shipment_service/scripts/k6_load_tests/utils/env.js


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
flowchart LR
  Start([Start]) --> GetValue[Get __ENV[key]]
  GetValue --> ValidatorCall[Call validator(value)]
  ValidatorCall -->|true| ReturnValue[/Return value/]
  ValidatorCall -->|false| ThrowError((Throw Error: "key is not valid"))
  ReturnValue --> End([End])

  subgraph env_object [env object]
    direction TB
    AWS_STAGE_node[AWS_STAGE] -->|validate("AWS_STAGE", stage in [...])| validate_stage
    AWS_REGION_node[AWS_REGION] -->|validate("AWS_REGION", region in ["us-east-1"])| validate_region
    AWS_ACCESS_KEY_ID_node[AWS_ACCESS_KEY_ID] -->|direct| AWS_ACCESS_KEY_ID_value["__ENV.AWS_ACCESS_KEY_ID"]
    AWS_SECRET_ACCESS_KEY_node[AWS_SECRET_ACCESS_KEY] -->|direct| AWS_SECRET_ACCESS_KEY_value["__ENV.AWS_SECRET_ACCESS_KEY"]
    AWS_SESSION_TOKEN_node[AWS_SESSION_TOKEN] -->|direct| AWS_SESSION_TOKEN_value["__ENV.AWS_SESSION_TOKEN"]
    SHIPMENTS_API_USERNAME_node[SHIPMENTS_API_USERNAME] -->|direct| SHIPMENTS_API_USERNAME_value["__ENV.SHIPMENTS_API_USERNAME"]
    SHIPMENTS_API_PASSWORD_node[SHIPMENTS_API_PASSWORD] -->|direct| SHIPMENTS_API_PASSWORD_value["__ENV.SHIPMENTS_API_PASSWORD"]
  end

  validate_stage{{validate("AWS_STAGE")}}
  validate_region{{validate("AWS_REGION")}}

  validate_stage -->|uses| GetValue
  validate_region -->|uses| GetValue
  validate_stage --> ValidatorCall
  validate_region --> ValidatorCall

  style env_object fill:#f9f,stroke:#333,stroke-width:1px
  style validate_stage fill:#fee,stroke:#333,stroke-width:1px
  style validate_region fill:#fee,stroke:#333,stroke-width:1px
```

> SVG rendering failed for this diagram.
