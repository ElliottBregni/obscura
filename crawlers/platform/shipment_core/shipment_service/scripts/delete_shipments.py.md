# Diagram: shipment_core/shipment_service/scripts/delete_shipments.py


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
flowchart LR
  A[Start: main(argv)] --> B[Parse CLI args (-f, -s)]
  B --> C[utilities.get_session_and_engine_by_stage(stage)]
  C --> D[Open CSV file and sniff dialect]
  D --> E[Read rows -> collect shipment_db_ids]
  E --> F{For each shipment_db_id}
  F --> G[Query tables.Shipments by id]
  G --> H{For each shipment}
  H --> I{asset_tracking_start_stop == ATSS_STARTED_TRACKING\nand obc_asset_id present?}
  I -- yes --> J[Log: tracked shipment info]
  I -- no --> K[Log: unknown tracking state]
  H --> L[Query ShipmentStatus for shipment_id]
  L --> M[Log each status row]
  H --> N[Query ShipmentOrganizations for shipment_id]
  N --> O[Log each organization row]
  H --> P[Query ShipmentReferences for shipment_id]
  P --> Q[Log each reference row]
  H --> R[Query ShipmentStops for shipment_id]
  R --> S[Log each stop row]
  H --> T[Load delayed = session.query(DelayedTelematicsPosts).all()]
  T --> U{For each delayed record: json.loads(payload)}
  U --> V{data.Telematics.shipment_id == shipment.creator_shipment_id?}
  V -- yes --> W[Mark deleted_delayed_rows and log delayed row]
  V -- no --> X[Continue checking delayed records]
  U --> Y{deleted_delayed_rows is None?}
  Y -- yes --> Z[Log: ZERO delayed telematics rows]
  Y -- no --> AA[Deleted delayed rows logged]
  H --> AB[Next shipment]
  F --> AC[Next shipment_db_id]
  AC --> AD[After all IDs, engine.dispose()]
  AD --> AE[Exit]
```

> SVG rendering failed for this diagram.
