# Diagram: shipment_core/shipment_service/scripts/backfill_no_activity_scheduled_event/backfill_no_activity_scheduled_event.py


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
flowchart LR
    A[Start: __main__] --> B[main()]
    B --> C{Env vars present?}
    C -->|No AWS_PROFILE| E[Raise Exception: Missing AWS_PROFILE]
    C -->|No AWS_STAGE| F[Raise Exception: Missing AWS_STAGE]
    C -->|Yes| G[DB_CONN.establish_connection()]
    G --> H[cursor = DB_CONN.get_cursor()]
    H --> I[get_shipments_that_should_have_no_activity_flag(cursor)]
    I --> J[shipments list]
    J --> K[process_requests_in_parallel(cursor, shipments)]
    K --> L[split_list(results, 50) -> result_chunks]
    L --> M[Start threads for each chunk]
    M --> N[worker(chunk, success_count, cursor)] 
    N --> O{For each shipment}
    O --> P[Wrap row as tables.Shipments]
    P --> Q[Determine no_activity_flag_set_at]
    Q --> R{Mode & origin_close_time logic}
    R -->|LTL or PARCEL| S[delta = 5min or 72h -> no_activity_flag_set_at]
    R -->|TRUCK or INTERMODAL + origin_close_time| T[base_flag_set_time = max(now, origin_close_time)]
    T --> U[delta depending on org_id and mode -> no_activity_flag_set_at]
    Q --> V{no_activity_flag_set_at set?}
    V -->|Yes| W[utilities.update_no_activity_flag(cursor, shipment, no_activity_flag_set_at)]
    W --> X[shipment.update()]
    X --> Y[utilities.set_and_send_schedule_event(... ScheduledEventProcess.ACTIVITY_MONITORING)]
    V -->|No| Z[Skip shipment]
    N --> AA[Thread finishes]
    M --> AB[All threads joined]
    AB --> AC[Return success count]
    AC --> AD[End]
```

> SVG rendering failed for this diagram.
