# Diagram: shipment_core/shipment_service/scripts/backfill_line_of_business_ids.py


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
flowchart TD
    A[SECRETS = Secrets()] --> B[parse_args()]
    B --> C[args = parse_args()\nthread_count = int(args.threads)]
    D[config = SECRETS.get_secret(SecretNames.DATABASE)] --> E[fv.db.get_connection(config,\nDB_APP_NAME="test_line_of_business_ids",\nautocommit=True)]
    C --> E
    E --> F[get_cursor(conn) as cursor]
    F --> G[update_shipments_not_owned_by_customer(cursor,\nLob.INBOUND.value, "FCSD")]
    F --> H[update_shipments_owned_by_customer(cursor,\nLob.AFTERMARKET.value, "FCSD")]
    subgraph Update_Not_Owned
        G --> G1[SELECT id FROM shipment_tracking\nWHERE line_of_business_id IS NULL\nAND created_by_org_id <> (SELECT id FROM organizations o WHERE o.fv_id = 'FCSD')\nLIMIT 100]
        G1 --> G2[UPDATE shipments SET line_of_business_id = Lob.INBOUND]
    end
    subgraph Update_Owned
        H --> H1[SELECT id FROM shipment_tracking\nWHERE line_of_business_id IS NULL\nAND created_by_org_id = (SELECT id FROM organizations o WHERE o.fv_id = 'FCSD')\nLIMIT 100]
        H1 --> H2[UPDATE shipments SET line_of_business_id = Lob.AFTERMARKET]
    end
    style Update_Not_Owned fill:#f9f,stroke:#333,stroke-width:1px
    style Update_Owned fill:#9ff,stroke:#333,stroke-width:1px
```

> SVG rendering failed for this diagram.
