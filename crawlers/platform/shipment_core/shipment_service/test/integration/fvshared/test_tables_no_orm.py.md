# Diagram: shipment_core/shipment_service/test/integration/fvshared/test_tables_no_orm.py


> Auto-generated by Obscura crawlers

## Diagram 1

```mermaid
classDiagram
class Shipments
class insert_new_shipment <<function>>
class get_shipment <<function>>
class delete_shipment <<function>>
class shipment_id <<fixture>>
class shipment <<fixture>>
class TestTables
class TestTablesWithJsonbDeltaUpdate

insert_new_shipment --> Shipments : creates/returns id
insert_new_shipment --> get_shipment : uses for verification
get_shipment --> Shipments : returns record
delete_shipment --> Shipments : removes record

shipment_id --> insert_new_shipment : yields id
shipment --> get_shipment : builds Shipments from record
TestTables --> shipment_id : uses fixture
TestTables --> shipment : uses fixture
TestTables --> Shipments : tests insert/update methods
TestTablesWithJsonbDeltaUpdate --> TestTables : reuses tests with config change
TestTablesWithJsonbDeltaUpdate --> shipment_id : uses fixture
TestTablesWithJsonbDeltaUpdate --> shipment : uses fixture
```

> SVG rendering failed for this diagram.

## Diagram 2

```mermaid
graph TD
A[insert_new_shipment] --> B[get_shipment]
B --> C[Build Shipments instance]
C --> D{Test action}
D -->|insert| E[DB INSERT via Shipments.insert()]
D -->|update| F[DB UPDATE via Shipments.update()]
E --> G[Verify fields/assertions]
F --> G
G --> H[Cleanup: delete_shipment]
H --> I[End]
```

> SVG rendering failed for this diagram.
