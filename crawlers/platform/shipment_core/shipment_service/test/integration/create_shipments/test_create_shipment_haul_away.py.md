# Diagram: shipment_core/shipment_service/test/integration/create_shipments/test_create_shipment_haul_away.py


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
classDiagram
    class Module {
        <<module>>
    }
    class put_shipment {
        +body: dict
        +returns CreateShipmentResponse
    }
    class create_shipments_lambda_handler {
        +event, context
    }
    class get_default_lambda_event {
        +method, path
    }
    class get_lambda_context {
        +function_name
    }
    class CreateShipmentResponse {
        +model_validate(result)
        +status_code
        +body
    }
    class HTTPStatus {
        <<enum>>
    }
    class TestPutCreateShipmentHaulAway {
        <<integration_test class>>
        +test_sync_type_is_expected_value_after_updating_stops(shipments_db_cursor)
    }
    class get_random_creator_shipment_id {
        +prefix, length
    }
    class get_existing_shipments_by_db_id_without_stops {
        +cursor, shipment_id
    }

    Module --> put_shipment
    Module --> TestPutCreateShipmentHaulAway
    put_shipment --> get_default_lambda_event : uses
    put_shipment --> get_lambda_context : uses
    put_shipment --> create_shipments_lambda_handler : invokes
    put_shipment --> CreateShipmentResponse : parses
    put_shipment --> HTTPStatus : asserts

    TestPutCreateShipmentHaulAway --> get_random_creator_shipment_id : generates id
    TestPutCreateShipmentHaulAway --> put_shipment : calls
    TestPutCreateShipmentHaulAway --> get_existing_shipments_by_db_id_without_stops : queries
    get_existing_shipments_by_db_id_without_stops --> Module : from shipment_service.fvshared.db_no_orm
    create_shipments_lambda_handler --> Module : from shipment_service.shipments.create_shipments
    get_default_lambda_event --> Module : from test.test_utilities.lambda_helpers
    get_lambda_context --> Module : from test.test_utilities.lambda_helpers
    CreateShipmentResponse --> Module : from test.models.responses
```

> SVG rendering failed for this diagram.
