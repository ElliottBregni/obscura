# Diagram: shipment_core/mobile_tracking_api/mobile_tracking_api/get_mobile_asset.py


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
flowchart TD
  LambdaHandler[lambda_handler(event, context, audit_refs)]
  DB_CONN[DB_CONN (FvDatabaseConnector)]
  Process[process_event(event, cursor)]
  GetHeader[fv.aws.lambdas.get_header(token)]
  GetQuery[fv.aws.lambdas.get_query_parameter(mobile_number)]
  InvokeShip[invoke_get_shipments(asset_id)]
  GetParentIDs[get_parent_ids(shipments.data)]
  AddGeofences[add_geofences(shipment)]
  GetStopInterval[get_stop_tracking_interval(cursor)]
  ParseVIN[parse_vin_data(shipment)]
  GetActualSeq[get_actual_stop_seq(shipment, cursor)]
  SortedKey[sorted_child_shipment_key(child_shipment)]
  GetParentCreator[get_parent_creator_id(cursor, parent_id)]
  MakeResp[fv.aws.lambdas.make_response(shipments, status)]
  SECRET[SECRET (Secrets.get_secret)]
  Error[BadRequestError handler -> make_response 204]

  LambdaHandler -->|establish_connection| DB_CONN
  LambdaHandler -->|calls| Process
  DB_CONN --> Process
  Process --> GetHeader
  Process --> GetQuery
  Process --> InvokeShip
  InvokeShip --> GetParentIDs
  GetParentIDs -->|iterates parents| Process
  Process --> AddGeofences
  AddGeofences --> GetStopInterval
  Process --> ParseVIN
  ParseVIN --> GetActualSeq
  GetActualSeq --> Process
  Process --> SortedKey
  Process --> GetParentCreator
  Process --> MakeResp
  GetHeader -->|compares| SECRET
  SECRET --> Process
  Process -->|on BadRequestError| Error
  Error --> MakeResp
```

> SVG rendering failed for this diagram.
