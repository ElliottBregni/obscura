# Diagram: shipment_core/mobile_tracking_api/mobile_tracking_api/send_mobile_push_notification.py


> Auto-generated by Obscura crawlers

## Diagram 1

```mermaid
classDiagram
    class Client {
        +connection
        +ios_arn
        +android_arn
        +connect()
        +get_account_number()
        +create_ios_platform_endpoint(token)
        +create_android_platform_endpoint(token)
        +delete_platform_endpoint(arn)
        +publish_to_ios(arn,title_loc_key,loc_key,loc_args,content_available)
        +publish_to_android(arn,title_loc_key,loc_key,loc_args,content_available)
    }
    class Device {
        +device_token
        +device_os
        +client
        +arn
        +register()
        +deregister()
        +send(title_loc_key,loc_key,loc_args,content_available)
    }
    class DB_CONN {
        +establish_connection()
        +cursor
    }
    class Functions {
        +get_push_notification_record(cursor)
        +send_sns_mobile_push_notification_to_device(device,title_loc_key,loc_key,loc_args,content_available)
        +lambda_handler(event,context,audit_refs)
    }
    Client <.. Device : "instantiated as client"
    Device --> Client : "uses"
    Functions --> DB_CONN : "uses"
    Functions --> Device : "creates"
    Functions --> Client : "calls publish"
    DB_CONN --> "Postgres" : "connects to"
```

> SVG rendering failed for this diagram.

## Diagram 2

```mermaid
sequenceDiagram
    participant Lambda as lambda_handler
    participant DB as DB_CONN
    participant Cursor as cursor
    participant Query as get_push_notification_record
    participant Loop as NotificationLoop
    participant DeviceObj as Device
    participant ClientObj as Client
    participant SNS as AWS_SNS
    participant Processor as process_hard_stop
    Lambda->>DB: establish_connection()
    DB->>Cursor: provide cursor
    Lambda->>Query: get_push_notification_record(cursor)
    Query-->>Cursor: execute query & fetchall
    Query-->>Lambda: return records
    Lambda->>Loop: iterate notifications
    Loop->>DeviceObj: instantiate(device_token, device_type)
    DeviceObj->>ClientObj: create Client()
    DeviceObj->>ClientObj: create_platform_endpoint(token) (register)
    ClientObj->>SNS: create_platform_endpoint(...)
    SNS-->>ClientObj: EndpointArn
    ClientObj-->>DeviceObj: return response
    DeviceObj->>ClientObj: publish_to_ios/publish_to_android(...)
    ClientObj->>SNS: publish(MessageStructure='json')
    SNS-->>ClientObj: publish response
    ClientObj-->>DeviceObj: return message,response
    DeviceObj->>ClientObj: delete_endpoint(arn) (deregister)
    ClientObj->>SNS: delete_endpoint(...)
    SNS-->>ClientObj: delete response
    Loop->>Processor: process_hard_stop(cursor, notification)
    Processor-->>Loop: success / Exception
    Loop->>Lambda: continue
    Lambda-->>Lambda: make_response([],200)
```

> SVG rendering failed for this diagram.
