# Diagram: shipment_core/mobile_tracking_api/serverless.mobile_tracking.yml


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
flowchart TD
  API[API Gateway (REGIONAL)] -->|GET /get_mobile_asset| LambdaGet[get_mobile_asset<br/>handler: mobile_tracking_api/get_mobile_asset.lambda_handler]
  API -->|GET /get_asset_id| LambdaGet
  API -->|POST /update_mobile_asset| LambdaUpdate[update_mobile_asset<br/>handler: mobile_tracking_api/update_mobile_asset.lambda_handler]
  API -->|POST /update_mobile_asset/{mobile_number}| LambdaUpdate
  Scheduler[schedule: rate(3 minutes)] --> SendPush[send_mobile_push_notification<br/>reservedConcurrency:3]
  LambdaGet -->|reads secrets| Secrets[Secrets Manager<br/>DD_API_KEY_SECRET_ARN]
  LambdaGet -->|reads params| SSM[SSM Parameters]
  LambdaUpdate --> Secrets
  LambdaUpdate --> SSM
  SendPush --> Secrets
  SendPush --> SSM
  IAM[IAM Role (managedPolicies + statements)] --> LambdaGet
  IAM --> LambdaUpdate
  IAM --> SendPush
  LambdaGet -->|kms:Decrypt| KMS[KMS Key]
  LambdaUpdate --> KMS
  SendPush --> KMS
  LambdaGet -->|s3:*| S3[S3 fv-doc bucket]
  LambdaUpdate --> S3
  SendPush -->|publishes| SNS[SNS Topics / Endpoints]
  VPC[VPC] -->|attached| LambdaGet
  VPC --> LambdaUpdate
  VPC --> SendPush
  Env[Environment Variables<br/>TRACKING_DISTANCE, TRACKING_INTERVAL, TRACKING_STOP, DD_*] --> LambdaGet
  Env --> LambdaUpdate
  Env --> SendPush
  subgraph Service
    API
    Scheduler
    LambdaGet
    LambdaUpdate
    SendPush
  end
```

> SVG rendering failed for this diagram.
