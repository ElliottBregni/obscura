# Diagram: shipment_core/chromium_export/fv/scripts/seed_here_locator_par.py


> Auto-generated by Obscura crawlers

## Diagram 1

```mermaid
classDiagram
class Shipment_Processor {
  -queue
  +Shipment_Processor(queue)
  +run()
  +process_row(city, state, country, postal_code, long, lat, source)
  +write_by_long_lat_db(long, lat, city, state, country, postal_code, source)
  +get_shipment_statuses(cursor, shipment_id)
  +check_key(key, dict)
  +check_value(key, dict)
}
class DB_CONN {
  +establish_connection()
  +cursor
}
class FV_DB {
  +FvDatabaseConnector(name, secret)
}
class Cache {
  +TTLCache(maxsize, ttl)
  +cached(cache, key)
}
class Functions {
  +get_shipment(cursor, offset, PAGE_SIZE)
  +extract_org_cache_key(cursor, long, lat)
  +get_seed_locator(cursor, long, lat)
  +process_shipments()
  +main()
}
Shipment_Processor --> DB_CONN : uses
Functions --> DB_CONN : uses
Functions ..> Cache : uses
Functions ..> FV_DB : creates
Shipment_Processor --> "psycopg2" : handles exceptions with
Shipment_Processor o-- Queue : consumes
Shipment_Processor ..> fv.utilities : calls is_valid_lat_long
```

> SVG rendering failed for this diagram.

## Diagram 2

```mermaid
flowchart LR
    Start([Start process_shipments])
    A[Establish DB connection] --> B[Create Queue and Workers]
    B --> C[Fetch shipments (get_shipment)]
    C --> D{More shipments?}
    D -- Yes --> E[Collect shipment IDs batch]
    E --> F[Enqueue batch to Queue]
    F --> G[Workers (Shipment_Processor) dequeue ID]
    G --> H[Worker get_shipment_statuses(cursor, id)]
    H --> I{Valid lat/long?}
    I -- No --> J[Log error / raise BadRequestError]
    I -- Yes --> K[process_row -> write_by_long_lat_db -> insert geo_location]
    K --> L[cursor.execute RETURNING id]
    L --> M[Continue processing next status]
    M --> D
    D -- No --> N[Print total records read]
    N --> End([End])
```

> SVG rendering failed for this diagram.
