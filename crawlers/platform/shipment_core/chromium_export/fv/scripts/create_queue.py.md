# Diagram: shipment_core/chromium_export/fv/scripts/create_queue.py


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
flowchart LR
    Start((start)) --> ParseArgs[/"parse_args()"/]
    ParseArgs --> Args[("args: stage, f")]
    Args --> Decide{args.f ?}
    Decide -- false --> ReceivedQueueName["queue_name = 'received-requests'"]
    Decide -- true --> FailedQueueName["queue_name = 'failed-requests'"]
    ReceivedQueueName --> QNStage["qn_stage = f\"{args.stage}-<queue_name>\""]
    FailedQueueName --> QNStage
    Start --> SQSClient["sqs_client = boto3.client('sqs')"]
    Start --> SNSClient["sns_client = boto3.client('sns')"]
    QNStage --> SQSArn["sqs_arn = f\"arn:aws:sqs:...:{qn_stage}\""]
    QNStage --> SNSArn["sns_arn = f\"arn:aws:sns:...:{qn_stage}\""]
    SQSArn --> CreateQueue["sqs_client.create_queue(QueueName=qn_stage, Attributes={...})"]
    SNSArn --> CreateQueue
    CreateQueue --> Attributes["Attributes:\n- MessageRetentionPeriod: 1209600\n- VisibilityTimeout: 300\n- Policy: {...}"]
    Attributes --> Policy["Policy: Statement -> Action: SQS:SendMessage\nResource: sqs_arn\nCondition: ArnEquals aws:SourceArn = sns_arn"]
    SNSClient -.-> Subscribe["# sns_client.subscribe(TopicArn=sns_arn, Protocol='sqs', Endpoint=sqs_arn)"]
    Policy --> End((end))
```

> SVG rendering failed for this diagram.
