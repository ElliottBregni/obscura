# Diagram: shipment_core/chromium_export/fv/scripts/seed_here_locator.py


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
classDiagram
    class Module {
        +SERVICE_NAME
        +locations_inserted
        +locations_already
    }
    class DB_CONN_ENTITY {
        +FvDatabaseConnector(...)
    }
    class DB_CONN {
        +establish_connection()
        +cursor
    }
    class Logger {
        +debug/info/error()
    }
    class main {
        +main()
    }
    class process_shipments {
        +process_shipments()
    }
    class get_shipment {
        +get_shipment(cursor, offset, PAGE_SIZE)
    }
    class get_shipment_statuses {
        +get_shipment_statuses(cursor, shipment_id)
    }
    class get_postion_updates {
        +get_postion_updates(cursor, entity_id)
    }
    class extract_org_cache_key {
        +extract_org_cache_key(cursor, long, lat)
    }
    class get_seed_locator {
        +get_seed_locator(cursor, long, lat)
        <<cached TTLCache>>
    }
    class write_by_long_lat_db {
        +write_by_long_lat_db(long, lat, city, state, country, postal_code, source)
    }
    class process_row {
        +process_row(city, state, country, postal_code, long, lat, source)
    }
    class check_key {
        +check_key(key, dict)
    }
    class check_value {
        +check_value(key, dict)
    }
    Module --> DB_CONN_ENTITY : defines
    Module --> DB_CONN : defines
    Module --> Logger : defines
    main --> process_shipments : calls
    process_shipments --> DB_CONN : uses
    process_shipments --> get_shipment : calls
    process_shipments --> get_shipment_statuses : calls
    process_shipments --> process_row : calls
    process_shipments --> check_key : uses
    process_shipments --> check_value : uses
    get_shipment --> DB_CONN : uses cursor
    get_shipment_statuses --> DB_CONN : uses cursor
    process_row --> DB_CONN : uses cursor
    process_row --> get_seed_locator : queries
    process_row --> write_by_long_lat_db : inserts
    get_seed_locator --> extract_org_cache_key : key function
    get_seed_locator ..> "TTLCache (maxsize=128, ttl=300)" : cached_with
    write_by_long_lat_db --> DB_CONN : uses cursor
    get_postion_updates --> DB_CONN : uses cursor
    process_row --> check_value : uses
    process_row --> check_key : uses
```

> SVG rendering failed for this diagram.
