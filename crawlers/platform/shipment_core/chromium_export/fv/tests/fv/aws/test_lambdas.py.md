# Diagram: shipment_core/chromium_export/fv/tests/fv/aws/test_lambdas.py


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
flowchart LR
  TestSuite[Test: fv.aws.lambdas tests] --> TB1[Test: test_get_event_body]
  TestSuite --> TB2[Test: test_sanitize_event]

  subgraph LambdaFunctions
    A[get_event_body()]:::fn
    B[sanitize()]:::fn
    C[get_header()]:::fn
    D[get_multivalue_header()]:::fn
  end

  TB1 -->|calls| A
  TB1 -->|raises| E1[ParameterError on invalid JSON]
  TB1 -->|asserts equality| F1[parsed_body == unparsed_body]
  A -->|on missing body| E1

  TB2 -->|calls| B
  TB2 -->|uses sanitized output with| C
  TB2 -->|uses sanitized output with| D

  B -->|redacts| G[Authorization -> "Basic REDACTED"]
  B -->|redacts| H[X-User-Token -> "REDACTED"]
  B -->|preserves| I[X-User-Email -> "foo@example.com"]

  C -->|reads single header| J[Header value]
  D -->|reads multivalue header| K[Header values array]

  classDef fn fill:#f9f,stroke:#333,stroke-width:1px;
```

> SVG rendering failed for this diagram.
