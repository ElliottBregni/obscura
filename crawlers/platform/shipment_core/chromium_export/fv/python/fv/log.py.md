# Diagram: shipment_core/chromium_export/fv/python/fv/log.py


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
classDiagram
    class ISO8601LogFormatter {
        +converter
        +default_time_format
        +default_msec_format
        +formatTime(record)
    }
    class StringLogFormatter {
        +formatMessage(record)
    }
    class StringLogFilter {
        +filter(record)
    }
    class StructuredLogFilter {
        +filter(record)
    }
    class StructuredLogFormatter {
        +format(record)
    }
    class LoggerConfig {
        +config_logging(func)
        +configure_logging(log_level, log_format, request_id)
    }
    class Helpers {
        +checkKey(dict, key)
        +is_logging_enabled(context)
        +get_log_level(level)
        +smashlines(s)
    }
    class LogUtils {
        +make_log_record(message,function,event,client_error,http_status,audit_refs,error_type,trace,duration,result)
        +log_with_stack(message,logger,level)
        +log_elapsed_time(f)
        +call_log_lambda(lambda_name,function_name,groups,runtime,extra_data)
    }
    ISO8601LogFormatter --|> logging.Formatter
    StringLogFormatter --|> ISO8601LogFormatter
    StringLogFilter --|> logging.Filter
    StructuredLogFilter --|> logging.Filter
    StructuredLogFormatter --|> ISO8601LogFormatter
    StructuredLogFormatter ..> "fv.encoding.json" : uses
    LoggerConfig --> StructuredLogFormatter : configures
    LoggerConfig --> StringLogFormatter : configures
    LoggerConfig --> StructuredLogFilter : addsFilter
    LoggerConfig --> StringLogFilter : addsFilter
    LoggerConfig --> fv.aws.lambdas : request_id via fv.aws.lambdas.get_request_id
    Helpers --> os : reads env
    Helpers --> checkKey : (contains)
    LogUtils --> Helpers : uses
    LogUtils --> traceback : formats trace
    LogUtils --> inspect : builds stack
    LogUtils --> fv.aws.lambdas : invokes in call_log_lambda
    LogUtils --> fv.log : uses get_log_level
    LoggerConfig --> LogUtils : uses make_log_record/logging helpers
```

> SVG rendering failed for this diagram.
