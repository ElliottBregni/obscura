# Diagram: shipment_core/chromium_export/fv/python/fv/aws/lambdas/shipments/multimodal.py


> Auto-generated by Obscura crawlers

## Diagram 1

```mermaid
classDiagram
    class module_fv_aws_lambdas_shipments_constants {
        <<module>>
    }
    class module_fv_aws_lambdas_shipments_shipments {
        <<module>>
        +shipment_visibility_conditions()
    }
    class get_multimodal_sql {
        +get_multimodal_sql(stops=True,statuses=True,trip_id=True,user_id=True)
        -stops_sql
        -statuses_sql
        -trip_sql
        -view
    }
    module_fv_aws_lambdas_shipments_constants <|-- get_multimodal_sql : uses
    module_fv_aws_lambdas_shipments_shipments <|-- get_multimodal_sql : calls
    get_multimodal_sql -- "returns SQL string"
```

> SVG rendering failed for this diagram.

## Diagram 2

```mermaid
flowchart TD
    Start([get_multimodal_sql invoked])
    Start --> Init{"initialize\nstops_sql,statuses_sql,trip_sql,view"}
    Init --> Checks
    subgraph Checks
        Checks --> StopsCond{stops?}
        StopsCond -- yes --> AddStops[append stops_sql fragment]
        StopsCond -- no --> NoStops[skip stops]
        Checks --> StatusesCond{statuses?}
        StatusesCond -- yes --> AddStatuses[append statuses_sql fragment]
        StatusesCond -- no --> NoStatuses[skip statuses]
        Checks --> TripCond{trip_id?}
        TripCond -- yes --> AddTrip[append trip_sql, set view=SHIPMENT_DETAILS, include visibility conditions]
        TripCond -- no --> NoTrip[keep view=SHIPMENT_TRACKING]
    end
    AddStops --> BuildSQL
    NoStops --> BuildSQL
    AddStatuses --> BuildSQL
    NoStatuses --> BuildSQL
    AddTrip --> BuildSQL
    NoTrip --> BuildSQL
    BuildSQL[Construct SELECT with child_shipments subquery\ninclude {stops_sql} and {statuses_sql} and {trip_sql}] --> ReturnSQL[/return SQL string/]
    ReturnSQL --> End([Done])
```

> SVG rendering failed for this diagram.
