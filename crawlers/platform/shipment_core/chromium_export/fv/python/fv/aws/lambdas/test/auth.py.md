# Diagram: shipment_core/chromium_export/fv/python/fv/aws/lambdas/test/auth.py


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
flowchart TD
  A[Start: call get_headers(user, token, role, org_id, org_fv_id, test, bearer)] --> B[Init headers {"Content-Type":"application/json"}]
  B --> C{bearer provided?}
  C -- no --> D[Set Authorization = "Bearer " + get_access_token(user, token)]
  C -- yes --> E[Set Authorization = bearer]
  D --> F
  E --> F
  F --> G{org_id present?}
  G -- yes --> G1[Set "x-active-org" = str(org_id)]
  G -- no --> H
  G1 --> H
  H{org_fv_id present?} -->|yes| H1[Set "x-active-fv-org" = org_fv_id]
  H --> I{role present?}
  H1 --> I
  I -- yes --> I1[Set "x-active-role" = role]
  I -- no --> J
  I1 --> J
  J{test == True?} -- yes --> J1[Set "api_testing" = "t"]
  J -- no --> K
  J1 --> K
  K[Return headers] --> Z[End]
```

> SVG rendering failed for this diagram.
