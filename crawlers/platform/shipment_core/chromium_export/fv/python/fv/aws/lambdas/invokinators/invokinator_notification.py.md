# Diagram: shipment_core/chromium_export/fv/python/fv/aws/lambdas/invokinators/invokinator_notification.py


> Auto-generated by Obscura crawlers

## Diagram 1

```mermaid
graph TD
  A[Start: create_notification(event, body, ...)] --> B[deepcopy(event) -> event_copy]
  B --> C[Set event_copy.body = body]
  C --> D[Set event_copy.pathParameters = path_params]
  D --> E[Set event_copy.queryStringParameters = query_params]
  E --> F[Update event_copy.headers with headers]
  F --> G[Invoke fv.aws.lambdas.invoke_lambda("create_notification", full_payload=event_copy)]
  G --> H[results received]
  H --> I[Parse results_body = json.loads(results.get("body", "{}"))]
  I --> J[status_code = int(results.get("statusCode", 500))]
  J --> K{status_code >= 400?}
  K -- Yes --> L[Compose message: "Create notification call failed: ..."]
  L --> M[Raise fv.error.HandledException(message, ..., http_status=500)]
  K -- No --> N[Return results_body]
  M --> O[End with exception]
  N --> O[End success]
```

> SVG rendering failed for this diagram.

## Diagram 2

```mermaid
classDiagram
  class create_notification {
    +create_notification(event, body, query_params={}, headers={}, path_params={}) return dict
  }
  class fv.aws.lambdas {
    +invoke_lambda(name, full_payload) return dict
  }
  class fv.error {
    +HandledException(message, public_message, http_status)
  }
  class json {
    +loads(s) return dict
  }
  class http {
    <<module>>
    +HTTPStatus
  }
  create_notification --> fv.aws.lambdas : uses
  create_notification --> fv.error : raises
  create_notification --> json : parses
  create_notification --> http : reads HTTPStatus
  fv.error <|-- HandledException
  fv.aws.lambdas : external dependency
  fv.error : external dependency
```

> SVG rendering failed for this diagram.
