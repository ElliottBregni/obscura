# Diagram: shipment_core/chromium_export/fv/python/fv/aws/lambdas/invokinators/invokinator_iam.py


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
flowchart LR
    subgraph ClientFunctions
        GUP[get_user_org_prefs(event, org_id, user_auth0_id?, key_pref?)]
        IGF[invoke_get_features(organization_id, lambda_level)]
        IGS[invoke_get_solution(solution_id, get_shipper_name?)]
        SYNC[sync_user(auth0_user_id)]
        IGO[invoke_get_organizations(event, query_params)]
        IGOM[invoke_get_organizations_members(event, query_params)]
        IGSByOrg[invoke_get_solution_by_organization_id_and_feature(organization_id, feature_name)]
        ORG_HAS[organization_has_feature(organization_id, feature)]
    end

    subgraph AWS_Lambdas
        L_CREATE["invoke_lambda('create_user_pref')"]
        L_FEATURES["invoke_lambda('get_features')"]
        L_SOLUTION["invoke_lambda('get_solution')"]
        L_UPDATE_USERS["invoke_lambda('update_users')"]
        L_ORGS["invoke_lambda('get_organizations')"]
        L_ORG_MEMBERS["invoke_lambda('get_organizations_members')"]
    end

    subgraph Caching
        TTL1["TTLCache(maxsize=128, ttl=300)"]
        TTL2["TTLCache(maxsize=128, ttl=900)"]
    end

    GUP -->|full_payload invoke_event| L_CREATE
    IGF -->|full_payload event| L_FEATURES
    IGS -->|cached key=extract_solution_id| TTL1
    TTL1 -->|on miss full_payload event| L_SOLUTION
    SYNC -->|full_payload event| L_UPDATE_USERS
    IGO -->|cached key=extract_query_params| TTL2
    IGOM -->|cached key=extract_query_params| TTL2
    TTL2 -->|on miss full_payload event| L_ORGS & L_ORG_MEMBERS
    IGSByOrg -->|cached key=extract_organization_id_feature_name| TTL2
    TTL2 --> L_SOLUTION
    ORG_HAS -->|calls| IGSByOrg
    L_CREATE -->|returns body/statusCode| GUP
    L_FEATURES -->|returns body/statusCode| IGF
    L_SOLUTION -->|returns body/statusCode| IGS & IGSByOrg
    L_UPDATE_USERS -->|returns body/statusCode| SYNC
    L_ORGS -->|returns body/statusCode| IGO
    L_ORG_MEMBERS -->|returns body/statusCode| IGOM

    style Caching fill:#f9f,stroke:#333,stroke-width:1px
    style AWS_Lambdas fill:#efe,stroke:#333,stroke-width:1px
    style ClientFunctions fill:#eef,stroke:#333,stroke-width:1px
```

> SVG rendering failed for this diagram.
