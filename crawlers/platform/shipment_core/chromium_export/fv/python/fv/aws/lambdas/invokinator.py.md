# Diagram: shipment_core/chromium_export/fv/python/fv/aws/lambdas/invokinator.py


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
classDiagram
class Helpers {
  +invoke_get_location(event, query_params, headers, accept, path_params)
  +extract_solution_id(event, solution_id)
  +organization_cache_name(organization_id, qualifier)
  +get_shipper_locations(organization_id)
  +get_solution(event, solution_id)
  +extract_organization_id(event, organization_fv_id)
  +get_organization(event, organization_fv_id)
  +organizations_cache_name(key)
  +get_organizations(key)
}
class "fv.aws.lambdas" as Lambdas
class "fv.log" as Log
class "fv.error" as Error
class "cachetools.TTLCache" as TTLCache
class "cachetools.cached" as Cached

Helpers --> Lambdas : invoke_lambda(full_payload)
Helpers ..> Log : @log_elapsed_time
Helpers ..> Error : raises HandledException/BadRequestError/NotFoundError
Helpers --> TTLCache : uses TTLCache(maxsize, ttl)
Helpers --> Cached : decorated_by @cached(TTLCache..., key=...)
Lambdas <..> TTLCache : returns cached responses
```

> SVG rendering failed for this diagram.
