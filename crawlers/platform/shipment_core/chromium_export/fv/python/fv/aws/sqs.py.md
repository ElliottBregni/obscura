# Diagram: shipment_core/chromium_export/fv/python/fv/aws/sqs.py


> Auto-generated by Obscura crawlers

## Diagram 1

```mermaid
flowchart TD
  Start([Start])
  A[create_item_failure_object(record_message_id, error_message)]
  A -->|returns| Aret{{"itemIdentifier": record_message_id, "errorMessage": error_message}}
  B[create_sqs_consumer_response(failed_records=None)]
  Start --> B
  B --> C{failed_records and len(failed_records) > 0}
  C -->|yes| D[response_obj["batchItemFailures"] = failed_records]
  C -->|no| E[no batchItemFailures added]
  D --> F[return response_obj {"body": {}, "batchItemFailures": failed_records}]
  E --> F2[return response_obj {"body": {}}]
  subgraph Usage
    A -->|can produce items for| B
  end
```

> SVG rendering failed for this diagram.

## Diagram 2

```mermaid
classDiagram
  class create_item_failure_object {
    +str record_message_id
    +str error_message
    +dict returns(itemIdentifier, errorMessage)
  }
  class create_sqs_consumer_response {
    +list[dict] failed_records=None
    +dict returns({"body": {}, "batchItemFailures": ...})
  }
  create_sqs_consumer_response ..> create_item_failure_object : may use items from
```

> SVG rendering failed for this diagram.
