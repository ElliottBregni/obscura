# Diagram: shipment_core/chromium_export/fv/python/fv/aws/lambdas/shipments/ng_shipments.py


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
flowchart LR
  subgraph Enum
    SS[ShipmentStatus\n(ACTIVE, ASSET_PENDING, SCHEDULED, ARRIVED, RELEASED, AVAILABLE, CANCELED, ARRIVED_DESTINATION, ARRIVED_ORIGIN, ARRIVED_PICKUP, ARRIVED_DROPOFF, DEPARTED_DESTINATION, DEPARTED_ORIGIN, DEPARTED_PICKUP, DEPARTED_DROPOFF, MANUAL_DELIVERY, AUTOMATICALLY_COMPLETED, AVAILABLE_FOR_DELIVERY, EXPIRED)]
  end

  subgraph Maps
    CONST[SHIPMENT_STATUS_CONSTANTS\n(enum -> code)]
    DESC[SHIPMENT_STATUS_DESCRIPTIONS\n(enum -> description)]
  end

  subgraph Functions
    MD[map_active_status_description()\n(description -> code or passthrough)]
    MC[map_active_status_code()\n(code -> description or passthrough)]
    GN[get_active_status_ng()\n(shipment -> description)]
  end

  SS -->|defines keys for| CONST
  SS -->|defines keys for| DESC

  DESC -->|input| MD
  CONST -->|input| MD
  MD -->|returns code string (or original)| OUT1((code/original))

  CONST -->|input| MC
  DESC -->|input| MC
  MC -->|returns description (or original)| OUT2((description/original))

  CONST -->|reads active_status| GN
  GN -->|returns description| OUT3((description))

  style SS fill:#f9f,stroke:#333,stroke-width:1px
  style CONST fill:#ffefba,stroke:#333
  style DESC fill:#bae1ff,stroke:#333
  style MD fill:#d4ffd4,stroke:#333
  style MC fill:#d4ffd4,stroke:#333
  style GN fill:#fff0f0,stroke:#333
```

> SVG rendering failed for this diagram.
