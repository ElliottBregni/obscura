# Diagram: shipment_core/chromium_export/fv/python/fv/aws/lambdas/shipments/crossborder.py


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
classDiagram
    class Module_fv_aws_lambdas_shipments_constants {
        <<module>>
    }
    class Module_fv_aws_lambdas_shipments_shipments {
        <<module>>
        +shipment_visibility_conditions()
    }
    class get_crossborder_sql {
        +stops: bool
        +statuses: bool
        +trip_id: bool
        +user_id: bool
        +returns: str (SQL)
        -stops_sql: str
        -statuses_sql: str
        -trip_sql: str
        -view: TableVersions
    }
    class SQL_Fragments {
        <<fragments>>
        +stops_fragment
        +statuses_fragment
        +trip_fragment
        +child_shipments_subquery
    }
    Module_fv_aws_lambdas_shipments_constants --|> get_crossborder_sql : uses
    Module_fv_aws_lambdas_shipments_shipments --|> get_crossborder_sql : calls
    get_crossborder_sql --> SQL_Fragments : composes
    get_crossborder_sql --> Module_fv_aws_lambdas_shipments_constants : selects view
    SQL_Fragments : stops_fragment --> "SELECT ... FROM shipment_stops"
    SQL_Fragments : statuses_fragment --> "SELECT ... FROM shipment_statuses"
    SQL_Fragments : trip_fragment --> "AND s.id=%(trip_id)s\nAND shipment_visibility_conditions()"
    SQL_Fragments : child_shipments_subquery --> "jsonb_array_elements(...) JOIN public.shipments c_s"
    get_crossborder_sql ..> "Returned SQL" : builds final query
```

> SVG rendering failed for this diagram.
