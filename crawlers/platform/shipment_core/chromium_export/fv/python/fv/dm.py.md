# Diagram: shipment_core/chromium_export/fv/python/fv/dm.py


> Auto-generated by Obscura crawlers

## Diagram 1

```mermaid
classDiagram
    class "fv.dm (module)" {
        +extract(d, path, required=True, default=None)
        +extract_float(d, path, required=True, default=None)
        +extract_int(d, path, required=True, default=None)
        +extract_list(d, path, required=True, default=None)
    }
    class glom {
        +glom(d, path)
        +PathAccessError
    }
    class "fv.error.BadRequestError" {
        +with_traceback(tb)
    }
    "fv.dm (module)" --> glom : calls
    "fv.dm (module)" --> "fv.error.BadRequestError" : raises
    glom <-- "fv.dm (module)" : uses PathAccessError handling
```

> SVG rendering failed for this diagram.

## Diagram 2

```mermaid
flowchart TD
    Start[Start: extract call] --> CallGlom[Call glom.glom(d, path)]
    CallGlom -->|success| GotValue[Return value]
    CallGlom -->|glom.core.PathAccessError| HandleMissing[Handle PathAccessError]
    HandleMissing --> ExtractDetails[Determine missing_component and matched_path]
    ExtractDetails --> IsRequired{required?}
    IsRequired -->|yes| RaiseErr[Raise fv.error.BadRequestError with traceback]
    IsRequired -->|no| LogWarn[logging.warning(error) and continue]
    LogWarn --> ReturnDefault[return default]
    GotValue --> TypeChecks[Type-specific conversion/checks]
    TypeChecks --> CheckFloat{called by extract_float?}
    CheckFloat -->|yes| TryFloat[try: float(value)]
    TryFloat -->|ok| ReturnFloat[return float value]
    TryFloat -->|fail & required| RaiseFloatErr[raise BadRequestError]
    TryFloat -->|fail & not required| ReturnDefault
    TypeChecks --> CheckInt{called by extract_int?}
    CheckInt -->|yes| TryInt[try: float->int equality check]
    TryInt -->|ok| ReturnInt[return int value]
    TryInt -->|fail & required| RaiseIntErr[raise BadRequestError]
    TryInt -->|fail & not required| ReturnDefault
    TypeChecks --> CheckList{called by extract_list?}
    CheckList -->|yes| IsList{isinstance(value, list)?}
    IsList -->|yes| ReturnList[return list]
    IsList -->|no & required| RaiseListErr[raise BadRequestError]
    IsList -->|no & not required| ReturnDefault
    ReturnFloat --> End[End]
    ReturnInt --> End
    ReturnList --> End
    ReturnDefault --> End
    RaiseErr --> End
    RaiseFloatErr --> End
    RaiseIntErr --> End
    RaiseListErr --> End
```

> SVG rendering failed for this diagram.
