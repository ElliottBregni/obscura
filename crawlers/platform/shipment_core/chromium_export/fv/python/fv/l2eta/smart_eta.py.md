# Diagram: shipment_core/chromium_export/fv/python/fv/l2eta/smart_eta.py


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
flowchart TD
  CachedTTL[TTLCache cached(get_eta_configurations)] -->|calls| GetConfigs[get_eta_configurations()]
  GetConfigs -->|uses secret| Secrets[Secrets.get_secret(SecretNames.L2_ETA)]
  GetConfigs -->|returns| Configs[(url, username, password, api_key, test_harness, test_harness_status)]

  Str2Bool[str2bool(v)] 
  GetQS[get_qs(mode_id, origin, destination, shipment_id, lng, lat, lo_datetime, pre_processor)]
  Predict[predict_eta(..., version=1|2)]
  Utilities[utilities.timestamp_to_datetime]

  GetQS -->|calls| GetConfigs
  GetQS -->|if test_harness_status true -> parse| Str2Bool
  Str2Bool -->|used by| GetQS
  GetQS -->|parses/constructs| QS[qs JSON payload]
  GetQS -->|returns| QS & lo_datetime

  Predict -->|validates lat/lng| Validate[lat/lng check]
  Validate -->|if missing| PredictNull[returns None]
  Predict -->|calls| GetConfigs
  Predict -->|may override url when version==1| ConfigOverride[url v1]
  Predict -->|calls| GetQS
  Predict -->|builds headers/auth| Auth[headers, auth]
  Predict -->|HTTP GET| Requests[requests.get(..., verify=False)]
  Requests -->|response handling| ParseResp[parse JSON response]
  ParseResp -->|version 1: extract median eta -> timedelta hours| ETA1[ai_eta_date]
  ParseResp -->|version 2: extract eta list -> timedelta hours| ETA2[ai_eta_date]
  ETA1 -->|return| Predict
  ETA2 -->|return| Predict
  ParseResp -->|on 500 or error| PredictNull
  PredictNull -->|return None| Predict

  style CachedTTL fill:#f0f8ff,stroke:#333,stroke-width:1px
  style GetConfigs fill:#fff5ba
  style Secrets fill:#ffd1dc
  style GetQS fill:#e6f7ff
  style Predict fill:#e6ffe6
  style Requests fill:#ffe6e6
  style Utilities fill:#f0f0f0
```

> SVG rendering failed for this diagram.
