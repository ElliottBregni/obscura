# Diagram: shipment_core/chromium_export/fv/python/fv/db/email.py


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
flowchart LR
    A[log_email(cursor, recipients_to, body, body_html, subject, recipients_cc, recipients_bcc, attachments)] --> B{Initialize state}
    B --> B1[replaces = {}]
    B --> B2[columns = ""]
    B --> B3[values = ""]
    A --> C[define add_field(field_name, field)]
    C --> C1["if field is not None:\n- replaces.update({field_name: field})\n- columns += formatted field_name\n- values += formatted placeholder"]
    A --> D[call add_field for recipients_to]
    A --> E[call add_field for recipients_cc]
    A --> F[call add_field for recipients_bcc]
    A --> G[call add_field for body]
    A --> H[call add_field for body_html]
    A --> I[call add_field for subject]
    A --> J[call add_field for attachment_details]
    D --> K[columns/values/replaces updated]
    E --> K
    F --> K
    G --> K
    H --> K
    I --> K
    J --> K
    K --> L[Build SQL string:\nINSERT INTO email_log ({columns}) VALUES ({values}) RETURNING id]
    L --> M[query = cursor.mogrify(sql, replaces)]
    M --> N[cursor.execute(query)]
    N --> O[results = cursor.fetchone()]
    O --> P{results exists?}
    P -->|yes| Q[return results.id]
    P -->|no| R[return None]
```

> SVG rendering failed for this diagram.
