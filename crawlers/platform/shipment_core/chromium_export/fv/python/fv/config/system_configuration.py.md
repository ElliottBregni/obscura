# Diagram: shipment_core/chromium_export/fv/python/fv/config/system_configuration.py


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
flowchart LR
  A[get_system_configuration_setting] -->|quick_cache True| B[get_sys_config_quick_cache]
  A -->|quick_cache False| C[get_sys_config_slow_cache]
  B --> D[get_sys_config]
  C --> D
  D --> E[prepare clauses: solution_clause, feature_clause, org_type_clause]
  E --> F[build query via cursor.mogrify]
  F --> G[cursor.execute(query)]
  G --> H[cursor.fetchall() -> rows]
  H -->|rows truthy| I[return [row.config for row in rows]]
  H -->|rows falsy| J[return []]
  K[sys_config_cache_key] -.->|used as key| B
  K -.->|used as key| C
  subgraph CACHING
    B
    C
    K
  end
```

> SVG rendering failed for this diagram.
