# Diagram: shipment_core/chromium_export/fv/python/fv/HERE/__init__.py


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
flowchart LR
  Start((Start)) --> ReadEnv[AWS_STAGE = os.environ.get("AWS_STAGE")]
  ReadEnv --> ProdStaging{aws_stage != "prod" and\\naws_stage != "staging"}
  ProdStaging -- true --> SetTest[aws_stage = "test"]
  ProdStaging -- false --> KeepStage[keep aws_stage]
  SetTest --> CheckEmpty{if not aws_stage}
  KeepStage --> CheckEmpty
  CheckEmpty -- true --> Raise[(raise fv.error.HandledException\\nhttp_status=HTTPStatus.INTERNAL_SERVER_ERROR)]
  CheckEmpty -- false --> BuildConfig[fv.config.Configuration\\nsources=[fv.config.ssm.SSMConfigurationSource(ssm_paths=[\"/fv/here/config\"])]] 
  BuildConfig --> ReturnConfig[return Configuration]
  ReturnConfig --> ConfigVar[config = get_configuration()]
```

> SVG rendering failed for this diagram.
