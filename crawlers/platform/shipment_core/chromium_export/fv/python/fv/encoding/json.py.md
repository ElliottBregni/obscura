# Diagram: shipment_core/chromium_export/fv/python/fv/encoding/json.py


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
flowchart LR
    A[encoder(value)] --> B{Type of value}
    B -->|datetime.date / datetime.datetime / datetime.time| C[return value.isoformat()]
    B -->|DateTimeRange| D{value.lower and value.upper}
    D -->|True| E[return [value.lower.timestamp(), value.upper.timestamp()]]
    D -->|False| F[return [None, None]]
    B -->|unittest.mock.MagicMock| G[return "MOCK"]
    B -->|decimal.Decimal| H[return float(value)]
    B -->|enum.Enum| I[return value.value]
    B -->|datetime.timedelta| J[Compute days, seconds -> format "DD:HH:MM"]
    J --> K[print(value.days, value.seconds)]
    J --> L[hours = floor(value.seconds / 3600)]
    J --> M[minutes = floor(value.seconds / 60 % 60)]
    J --> N[return f"{str(value.days).zfill(2)}:{str(hours).zfill(2)}:{str(minutes).zfill(2)}"]
    B -->|other| O[message = f"Could not encode value: {value}"]
    O --> P[raise fv.error.BadRequestError(message, message, http_status=500)]
```

> SVG rendering failed for this diagram.
