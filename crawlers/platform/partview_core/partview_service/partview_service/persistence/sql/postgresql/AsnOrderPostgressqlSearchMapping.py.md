# Diagram: partview_core/partview_service/partview_service/persistence/sql/postgresql/AsnOrderPostgressqlSearchMapping.py


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
classDiagram
    class SearchPostgresqlMapping {
        <<abstract>>
        +__init__(application_name)
        +freeze()
        +add_from_dict(mapping: dict)
        +read(sql: str, replaces: dict)
    }

    class AsnOrderPostgresqlSearchMapping {
        +__init__(application_name: str | None = None)
        +build_map()
        +get_asn_orders_by_external_ids(asn_orders)
        -mapping: dict
    }

    class GenerateApplicationName {
        <<utility>>
        +generate_application_name(application_name: str | None, class_name: str) string
    }

    SearchPostgresqlMapping <|-- AsnOrderPostgresqlSearchMapping
    AsnOrderPostgresqlSearchMapping ..> GenerateApplicationName : uses
    AsnOrderPostgresqlSearchMapping : build_map() adds fields:
    AsnOrderPostgresqlSearchMapping : id, ts, modified, status, solution_id
    AsnOrderPostgresqlSearchMapping : external_id, purpose_code, priority, packing_slip
    AsnOrderPostgresqlSearchMapping : part_per_asn, packaging_quantity, order_ts, order_written_ts
    AsnOrderPostgresqlSearchMapping : point_of_loading_code, ownership_code, asn_match_type
    AsnOrderPostgresqlSearchMapping : customer, record_count, total_line_items, total_quantity_line_items
    AsnOrderPostgresqlSearchMapping : serial_number, ship_to, ship_from, intermediate_consignee
    AsnOrderPostgresqlSearchMapping : promised_delivery_date, needed_delivery_date, ship_date, delivery_date
    AsnOrderPostgresqlSearchMapping : get_asn_orders_by_external_ids() -> performs SELECT with replacements (uuids, solution_id) and calls read()
```

> SVG rendering failed for this diagram.
