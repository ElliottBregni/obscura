# Diagram: partview_core/partview_service/partview_service/persistence/sql/postgresql/PackageContainerSearchCountFilters.py


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
classDiagram
class PackageContainerSearchCountFilters {
  <<abstract>>
  +static assemble_filter(query_list)
  +static get_part_name_filter(options: dict)
  +static get_carrier_filter(options: dict)
  +static get_part_number_filter(options: dict)
  +static get_lifecycle_state_filter(options: dict)
  +static get_exception_name_filter(options: dict)
  +static get_order_priority_filter(options: dict)
  +static get_shipment_id_filter(options: dict)
  +static get_order_number_filter(options: dict)
  +static get_tracking_number_filter(options: dict)
  +static get_status_filter(options: dict)
  +static get_everything_filter(options: dict)
}
class SearchQueryParameters {
  <<enum-like>>
  PART_NAME
  PART_NAME_CONTAINS
  PART_NAME_EMPTY
  PART_NAME_IS_NOT_EMPTY
  CARRIER
  CARRIER_CONTAINS
  CARRIER_EMPTY
  CARRIER_IS_NOT_EMPTY
  PART_NUMBER
  PART_NUMBER_CONTAINS
  PART_NUMBER_EMPTY
  PART_NUMBER_IS_NOT_EMPTY
  LIFECYCLE_STATE
  LIFECYCLE_STATE_CONTAINS
  LIFECYCLE_STATE_EMPTY
  LIFECYCLE_STATE_IS_NOT_EMPTY
  EXCEPTION
  EXCEPTION_CONTAINS
  EXCEPTION_EMPTY
  EXCEPTION_IS_NOT_EMPTY
  ORDER_PRIORITY
  ORDER_PRIORITY_CONTAINS
  ORDER_PRIORITY_EMPTY
  ORDER_PRIORITY_IS_NOT_EMPTY
  SHIPMENT_ID_CONTAINS
  ORDER_NUMBER_CONTAINS
  TRACKING_NUMBER_CONTAINS
  STATUS
  EVERYTHING
}
PackageContainerSearchCountFilters ..> SearchQueryParameters : references
PackageContainerSearchCountFilters : "assemble_filter" <|-- "get_*_filter" : called by
PackageContainerSearchCountFilters o-- "get_part_name_filter" : builds "(p.name ...)"
PackageContainerSearchCountFilters o-- "get_carrier_filter" : builds "tl.carrier_org_fv_id ..."
PackageContainerSearchCountFilters o-- "get_part_number_filter" : builds "p.canonical_external_id ..."
PackageContainerSearchCountFilters o-- "get_lifecycle_state_filter" : builds "p.status ..."
PackageContainerSearchCountFilters o-- "get_exception_name_filter" : builds "pcet.name ..."
PackageContainerSearchCountFilters o-- "get_order_priority_filter" : builds "ao.priority ..."
PackageContainerSearchCountFilters o-- "get_shipment_id_filter" : builds "tl.creator_shipment_id ..."
PackageContainerSearchCountFilters o-- "get_order_number_filter" : builds "ao.external_id ..."
PackageContainerSearchCountFilters o-- "get_tracking_number_filter" : builds "pc.external_id ..."
PackageContainerSearchCountFilters o-- "get_status_filter" : builds "pc.status ..."
PackageContainerSearchCountFilters o-- "get_everything_filter" : aggregates multiple field checks
SearchQueryParameters <|-- PackageContainerSearchCountFilters : uses constants
```

> SVG rendering failed for this diagram.
