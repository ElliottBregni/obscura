# Diagram: partview_core/partview_service/partview_service/persistence/sql/postgresql/ProcessRouteUploadPostgresqlMapping.py


> Auto-generated by Obscura crawlers

## Diagram 1

```mermaid
classDiagram
    class BasePostgresqlMapping
    class ProcessRouteUploadPostgresqlMapping
    BasePostgresqlMapping <|-- ProcessRouteUploadPostgresqlMapping
    ProcessRouteUploadPostgresqlMapping : +__init__(application_name)
    ProcessRouteUploadPostgresqlMapping : +build_map()
    ProcessRouteUploadPostgresqlMapping : +update_process_route_upload(id, state, comment)
    ProcessRouteUploadPostgresqlMapping : -fields: id, ts, modified, row_id, file_name, group_id, action, state, comment, solution_id, identifier, identifier_key
```

> SVG rendering failed for this diagram.

## Diagram 2

```mermaid
flowchart TD
    A[update_process_route_upload(id, state, comment)] --> B[Create params dict]
    B --> C[Compose SQL UPDATE query string]
    C --> D[Connect to primary database connector]
    D --> E[Get cursor from connection]
    E --> F[Execute cursor.mogrify(query, params)]
    F --> G[cursor.execute(mogrified_query)]
    G --> H[cursor.fetchall() : RETURNING id]
```

> SVG rendering failed for this diagram.
