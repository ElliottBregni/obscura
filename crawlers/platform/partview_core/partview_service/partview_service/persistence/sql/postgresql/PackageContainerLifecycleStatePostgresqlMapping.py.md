# Diagram: partview_core/partview_service/partview_service/persistence/sql/postgresql/PackageContainerLifecycleStatePostgresqlMapping.py


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
classDiagram
    class BasePostgresqlMapping {
        +BasePostgresqlMapping(name, application_name)
        +freeze()
        +get_primary_database_connector()
    }
    class PackageContainerLifecycleStatePostgresqlMapping {
        +CTE_QUERY: String
        +__init__(application_name)
        +build_map()
        +update_lifecycle_to_delayed()
        +insert_lifecycle_history()
        +update_and_insert_lifecycle()
    }
    BasePostgresqlMapping <|-- PackageContainerLifecycleStatePostgresqlMapping
    PackageContainerLifecycleStatePostgresqlMapping --> "1" BasePostgresqlMapping : super() calls
    PackageContainerLifecycleStatePostgresqlMapping o-- "uses" "DB Cursor" : get_primary_database_connector().establish_connection().get_cursor()
    note for PackageContainerLifecycleStatePostgresqlMapping "CTE_QUERY contains SQL logic to detect lifecycle state changes\nand reasons ('Delayed', 'In Route') and provides columns:\nid, lifecycle_state, old_lifecycle_state, reason, solution_id, effective_eta"
```

> SVG rendering failed for this diagram.
