# Diagram: partview_core/partview_service/partview_service/persistence/sql/postgresql/UnprocessedShipmentEventPostgresqlMapping.py


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
classDiagram
    class BasePostgresqlMapping {
        <<abstract>>
        +freeze()
        +add(field, mapping)
        +get_tracking_database_connector()
        +get_column_names()
        +get_field(column)
    }
    class UnprocessedShipmentEvent {
        +set(field, value)
    }
    class UnprocessedShipmentEventPostgresqlMapping {
        -application_name: str | None
        -table_name: "unprocessed_shipment_event"
        +__init__(application_name: str | None = None)
        +build_map()
        +get_unprocessed_shipment_events(creator_shipment_id) : list~UnprocessedShipmentEvent~
        +write_query_function(table, columns, values, dirty_fields) : str
    }
    class GenerateApplicationName {
        +generate_application_name(application_name, class_name) : str
    }

    BasePostgresqlMapping <|-- UnprocessedShipmentEventPostgresqlMapping
    UnprocessedShipmentEventPostgresqlMapping --> UnprocessedShipmentEvent : creates/returns
    UnprocessedShipmentEventPostgresqlMapping ..> GenerateApplicationName : uses
    UnprocessedShipmentEventPostgresqlMapping ..> "unprocessed_shipment_event" : maps to table
    UnprocessedShipmentEventPostgresqlMapping ..> BasePostgresqlMapping : super() calls
```

> SVG rendering failed for this diagram.
