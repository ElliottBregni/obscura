# Diagram: partview_core/partview_service/partview_service/aws/SecretsManagerProxy.py


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
classDiagram
    class SecretsManagerProxy {
        - __stage
        - __region
        - __client
        - __secrets
        + SecretsManagerProxy(aws_settings: AwsSettings)
        + get_client(region_name)
        + _get_secret(secret_with_env)
        + get_secret(secret_name)
        + get_secrets(secrets)
    }
    class Freezeable
    class AwsSettings {
        + get_stage()
        + get_region()
    }
    class PartviewTTLCache
    class DotAccessibleDictionary
    class NotFoundError
    class ClientError
    class Boto3Client
    class CACHE

    SecretsManagerProxy --|> Freezeable
    SecretsManagerProxy --> AwsSettings : depends on
    SecretsManagerProxy o-- Boto3Client : creates/holds
    SecretsManagerProxy --> DotAccessibleDictionary : constructs
    SecretsManagerProxy ..> NotFoundError : raises
    SecretsManagerProxy ..> ClientError : catches
    CACHE --> PartviewTTLCache : instance of
    SecretsManagerProxy .. uses ..> CACHE : @cached(CACHE)
```

> SVG rendering failed for this diagram.
