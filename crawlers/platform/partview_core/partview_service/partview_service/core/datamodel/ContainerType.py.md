# Diagram: partview_core/partview_service/partview_service/core/datamodel/ContainerType.py


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
classDiagram
    class Persistable

    class ContainerTypeProperties <<enumeration>> {
        +name = "name"
    }

    class ContainerType {
        -__name: str
        -__solution_id: str
        +table_name: str = "package_container_type"
        +name(): str
        +solution_id(): str
        +name(name: str): ContainerType
        +solution_id(solution_id: str): ContainerType
        +is_valid(): bool
    }

    Persistable <|-- ContainerType
    ContainerTypeProperties ..> ContainerType : uses
```

> SVG rendering failed for this diagram.
