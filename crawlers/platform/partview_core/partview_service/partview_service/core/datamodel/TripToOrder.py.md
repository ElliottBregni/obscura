# Diagram: partview_core/partview_service/partview_service/core/datamodel/TripToOrder.py


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
classDiagram
    class Persistable {
    }

    class TripToOrder {
        +table_name: "order_to_trip_leg"
        -__trip_leg_id: str
        -__order_id: str
        -__solution_id: str
        +solution_id(): str
        +trip_leg_id(): str
        +order_id(): str
        +solution_id=(solution_id: str): TripToOrder
        +trip_leg_id=(trip_leg_id: str): TripToOrder
        +trip_leg_id:deleter()
        +order_id=(order_id: str): TripToOrder
        +order_id:deleter()
        +is_valid(): bool
        +to_dict(): dict
    }

    class TripOrderProperties <<enumeration>> {
        SOLUTION_ID = "solution_id"
        ORDER_ID = "order_id"
        TRIP_LEG_ID = "trip_leg_id"
    }

    TripToOrder --|> Persistable
```

> SVG rendering failed for this diagram.
