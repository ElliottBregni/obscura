# Diagram: partview_core/partview_service/partview_service/core/datamodel/FilterModels.py


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
classDiagram
    class AutocompleteSpec {
        <<dataclass>>
        +AutocompleteKind kind
        +AutocompleteValueSource value_source = ORIGINAL_TERMS
        +str lc_field = ""
        +str original_field = ""
        +str? nested_path = None
        +str suggest_field = ""
    }
    class DropdownSpec {
        <<dataclass>>
        +str agg_name
        +list<tuple<str,str>> key_fields_lc
        +list<tuple<str,str>> display_fields
        +list<str> required_exists
        +str value_key
        +str label_template
        +list<str> search_fields_contains
        +str? nested_path = None
    }
    class FilterItem {
        <<dataclass>>
        +str id
        +str label
        +str value
    }
    class FilterMeta {
        <<dataclass>>
        +int totalCount = 0
        +int currentPage = 0
        +int totalPages = 0
    }
    class DropdownMeta {
        <<dataclass>>
        +dict<string, any>? searchAfter = None
    }
    class FilterResponse {
        <<dataclass>>
        +list<FilterItem> data
        +FilterMeta|DropdownMeta? meta = None
    }

    AutocompleteSpec ..> AutocompleteKind : uses
    AutocompleteSpec ..> AutocompleteValueSource : uses
    FilterResponse "1" --> "*" FilterItem : data
    FilterResponse "1" --> "0..1" FilterMeta : meta
    FilterResponse "1" --> "0..1" DropdownMeta : meta
    DropdownSpec ..> "tuple<str,str>" : contains
    DropdownMeta ..> "dict<string, any>" : contains
```

> SVG rendering failed for this diagram.
