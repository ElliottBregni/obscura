# Diagram: partview_core/partview_service/partview_service/core/business/ocean_tracking/OceanConstants.py


> Auto-generated by Obscura crawlers

## Diagram 1

```mermaid
classDiagram
    class TransshipReferenceKey {
        <<enumeration>>
        +KEY = "code"
        +VALUE = "T"
    }
    class Constants {
        +TRANSSHIP_MIN_PORT_TO_PORT_TRIPS: int = 2
        +PORT_LAD_CODE: str = "P"
    }
    class is_ocean_feature(feature, org_id) {
        +is_ocean_feature(feature: str|None, org_id: str|None = None) : bool
    }
    TransshipReferenceKey <|-- Constants
    is_ocean_feature --> Constants
```

> SVG rendering failed for this diagram.

## Diagram 2

```mermaid
flowchart TD
    A[Start: is_ocean_feature(feature, org_id)] --> B{feature provided?}
    B -- Yes --> C[Compare str(feature).lower() == Auth.Feature.OCEAN_TRACKING.lower()]
    C --> D[Return comparison result (bool)]
    B -- No --> E{org_id is None?}
    E -- Yes --> F[/Raise ValueError: "org_id is required..."/]
    E -- No --> G[Call InvokeGetSolution.invoke_get_solution_by_org_and_feature(org_id, Auth.Feature.OCEAN_TRACKING)]
    G --> H{solution is dict and has solution_id truthy?}
    H -- Yes --> I[Return True]
    H -- No --> J[Return False]
    D --> K[End]
    F --> K
    I --> K
    J --> K
```

> SVG rendering failed for this diagram.
