# Diagram: partview_core/partview_service/partview_service/api/trip_leg/handlers/mapping/TripLegApiMapping.py


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
classDiagram
    class ApiMapping {
        <<abstract>>
        +set_api_name(name)
        +set_default_fields(dict)
        +set_internal_fields(list)
        +set_time_range_fields(list)
        +set_str_lower_fields(list)
        +set_str_upper_fields(list)
        +set_int_fields(list)
        +set_json_fields(list)
        +set_bidirectional_map(map)
    }

    class TripLegApiMapping {
        +TripLegApiMapping()
        +set_api_mapping()
        -default_fields: {"status":"ACTIVE"}
        -internal_fields: ["solution_id","actor_id"]
        -time_range_fields: ["scheduled_origin_pickup_window","scheduled_destination_delivery_window"]
        -str_lower_fields: ["mode","delivery_type"]
        -str_upper_fields: ["type","status"]
        -int_fields: ["sequence_number"]
        -json_fields: ["destination_location_details","origin_location_details"]
        -bidirectional_map: BidirectionalMap
    }

    class BidirectionalMap {
        +map_from_dict(dict)
    }

    class TripLegPayload {
        <<constants>>
        +TYPE
        +CREATOR_SHIPMENT_ID
        +SHIPMENT_NUMBER
        +SEQUENCE_NUMBER
        +OWNER_ORGANIZATION_FV_ID
        +CARRIER_ORGANIZATION_FV_ID
        +SCAC
        +MODE
        +STATUS
        +DESTINATION_LOCATION_CODE
        +ORIGIN_LOCATION_CODE
        +SCHEDULED_ORIGIN_DEPARTURE_WINDOW
        +SCHEDULED_DESTINATION_DELIVERY_TIME_WINDOW
        +DESTINATION_LOCATION_DETAILS
        +ORIGIN_LOCATION_DETAILS
        +BILL_OF_LADING_NUMBER
        +LLP_NUMBER
        +CONSOLIDATOR_NUMBER
        +TRAILER_EQUIPMENT_NUMBER
        +DELIVERY_TYPE
        +ROUTE_ID
        +PARENT_CREATOR_ID
        +LOADCODE
        +SOLUTION_ID
    }

    TripLegApiMapping --|> ApiMapping
    TripLegApiMapping ..> BidirectionalMap : uses
    TripLegApiMapping ..> TripLegPayload : references constants
    BidirectionalMap --> "0..*" TripLegPayload : maps keys from
```

> SVG rendering failed for this diagram.
