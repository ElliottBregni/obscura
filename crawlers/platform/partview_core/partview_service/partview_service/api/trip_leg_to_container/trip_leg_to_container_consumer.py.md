# Diagram: partview_core/partview_service/partview_service/api/trip_leg_to_container/trip_leg_to_container_consumer.py


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
flowchart LR
    subgraph Decorator
        DB[<b>sqs_batch_failure_wrapper</b>]
    end

    subgraph Lambda
        LH[lambda_handler(event, context)]
    end

    subgraph HandlerFactory
        TF[TripLegToContainerRequestHandlerFactory]
    end

    subgraph Topic
        SQ[SQStopic.TRIP_LEG_TO_CONTAINER]
    end

    subgraph Consumer
        DFC[DataFeedConsumerHandler]
    end

    DB --> LH
    LH --> DFC
    DFC -->|topic, event, factory| TF
    DFC -->|uses| SQ
    DFC -->|method| P[process_with_single_batch_failure()]
    classDef bold fill:#f9f,stroke:#333,stroke-width:1px;
    class DB,DFC,TF,SQ bold
```

> SVG rendering failed for this diagram.
