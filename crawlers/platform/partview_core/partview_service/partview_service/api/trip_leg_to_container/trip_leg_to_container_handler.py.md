# Diagram: partview_core/partview_service/partview_service/api/trip_leg_to_container/trip_leg_to_container_handler.py


> Auto-generated by Obscura crawlers

## Diagram 1

```mermaid
classDiagram
class FVRequestFactory {
  +handler
  +http_method
  +__init__(event)
}
class TripLegToContainerRequestHandlerFactory {
  -handlers
  +__init__(event)
}
TripLegToContainerRequestHandlerFactory --|> FVRequestFactory
TripLegToContainerRequestHandlerFactory o-- "handlers: dict" : contains
class GetTripLegToContainerRequestHandler {
  +handle_request_with_new_mandatory_handler()
}
class PostTripLegToContainerRequestHandler {
  +handle_request_producer(topic, identifier, default, is_fifo)
}
class DeleteTripLegToContainerRequestHandler {
  +handle_request_producer(topic, identifier, default, is_fifo)
}
TripLegToContainerRequestHandlerFactory ..> GetTripLegToContainerRequestHandler
TripLegToContainerRequestHandlerFactory ..> PostTripLegToContainerRequestHandler
TripLegToContainerRequestHandlerFactory ..> DeleteTripLegToContainerRequestHandler
class LambdaHandlerMiddleware {
  +mandatory_lambda_wrapper(func)
}
LambdaHandlerMiddleware ..> TripLegToContainerRequestHandlerFactory
```

> SVG rendering failed for this diagram.

## Diagram 2

```mermaid
flowchart TD
A[lambda_handler(event, context)] --> B[Create TripLegToContainerRequestHandlerFactory(event)]
B --> C[request_handler = handler_factory.handler]
B --> D{handler_factory.http_method == "GET"}
D -- Yes --> E[call request_handler.handle_request_with_new_mandatory_handler()]
D -- No --> F[call request_handler.handle_request_producer(SQStopic.TRIP_LEG_TO_CONTAINER, SQSMessageIdentifier.TRIP_LEG_TO_CONTAINER, default=AcceptVersion.FIFO.value, is_fifo=true)]
E --> G[return response]
F --> G
```

> SVG rendering failed for this diagram.
