# Diagram: partview_core/partview_service/partview_service/api/part_to_container/part_to_container.py


> Auto-generated by Obscura crawlers

## Diagram 1

```mermaid
classDiagram
    class FVRequestFactory {
        <<abstract>>
        +__init__(event)
        +handler
        +http_method
    }
    class PartToContainerRequestHandlerFactory {
        +__init__(event)
        -handlers : dict
    }
    class GetPartToContainerRequestHandler {
        +handle_request_with_new_mandatory_handler()
        +handle_request_producer(topic, identifier, default, is_fifo)
    }
    class PostPartToContainerRequestHandler {
        +handle_request_producer(topic, identifier, default, is_fifo)
    }
    class DeletePartToContainerRequestHandler {
        +handle_request_producer(topic, identifier, default, is_fifo)
    }
    class PatchPartToContainerRequestHandler {
        +handle_request_producer(topic, identifier, default, is_fifo)
    }
    class LambdaHandlerMiddleware {
        <<utility>>
        +mandatory_lambda_wrapper(func)
    }
    class AcceptVersion {
        <<enum>>
        +FIFO
    }
    class SQSMessageIdentifier {
        <<enum>>
        +PART_TO_CONTAINER
    }
    class SQStopic {
        <<enum>>
        +PART_TO_CONTAINER
    }

    PartToContainerRequestHandlerFactory --|> FVRequestFactory : extends
    PartToContainerRequestHandlerFactory o-- GetPartToContainerRequestHandler : maps to "GET"
    PartToContainerRequestHandlerFactory o-- PostPartToContainerRequestHandler : maps to "POST"
    PartToContainerRequestHandlerFactory o-- DeletePartToContainerRequestHandler : maps to "DELETE"
    PartToContainerRequestHandlerFactory o-- PatchPartToContainerRequestHandler : maps to "PATCH"
    LambdaHandlerMiddleware ..> "lambda_handler" : decorates
    GetPartToContainerRequestHandler ..> AcceptVersion : uses
    *-- SQSMessageIdentifier
    *-- SQStopic
```

> SVG rendering failed for this diagram.

## Diagram 2

```mermaid
flowchart LR
    subgraph AWS_Lambda
        direction TB
        Event([event, context]) --> LambdaWrapper{mandatory_lambda_wrapper}
        LambdaWrapper --> LambdaFunc[lambda_handler]
    end

    LambdaFunc --> FactoryInit[PartToContainerRequestHandlerFactory(event)]
    FactoryInit --> GetHandlerRef[handler = factory.handler]
    GetHandlerRef --> AuthCheck[shipper_auth_check()]
    AuthCheck --> AuthenticatedHandler[request_handler]

    subgraph Decision
        direction TB
        IsGET{handler_factory.http_method == "GET?"}
    end

    AuthenticatedHandler --> IsGET
    IsGET -- Yes --> HandleGET[handle_request_with_new_mandatory_handler()]
    IsGET -- No --> HandleProduce[handle_request_producer(SQStopic.PART_TO_CONTAINER, SQSMessageIdentifier.PART_TO_CONTAINER, default=AcceptVersion.FIFO, is_fifo=true)]

    HandleGET --> ResponseGET([Response])
    HandleProduce --> SQSProduce([Enqueue SQS Message (FIFO)])
    SQSProduce --> ResponseProduce([Response])
```

> SVG rendering failed for this diagram.
