# Diagram: partview_core/partview_service/partview_service/api/dashboard/dynamic_widget/mock_data.py


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
classDiagram
    class MockData {
        +get_exception_response(request) PieTableWidgetData
        +get_lifecycle_response(request) PieTableWidgetData
        +get_tracking_history_response(request) TableWidgetData
        +get_mock_data_by_type(request) TableWidgetData|PieTableWidgetData
    }

    class PieTableWidgetData {
        +widgetTitle
        +widgetType
        +dataType
        +tableData
        +pieChart
        +lowerLeft
    }

    class TableWidgetData {
        +widgetTitle
        +widgetType
        +dataType
        +noDataConfig
        +isCollapsed
        +tableConfig
        +tableData
    }

    class DynamicWidgetTableData {
        +rows
        +summaryData
        +data
    }

    class DynamicWidgetRowData {
        +data
    }

    class DynamicWidgetSummaryData {
        +countType
        +data
    }

    class WidgetType <<enumeration>> {
        +PIE_TABLE_WIDGET
        +TABLE_WIDGET
        +...
    }

    class DynamicWidgetDataType <<enumeration>> {
        +EXCEPTION
        +LIFECYCLE_STATE
        +TRACKING_HISTORY
        +...
    }

    class DynamicWidgetCountType <<enumeration>> {
        +TABLE_PACKAGE_EXCEPTION_COUNT
        +EXCEPTION_SUM
        +WIDGET_PACKAGE_EXCEPTION_COUNT
        +PACKAGE_COUNT
        +PACKAGE_COUNT_24HR
        +...
    }

    MockData --> PieTableWidgetData : returns
    MockData --> TableWidgetData : returns
    PieTableWidgetData *-- DynamicWidgetTableData : contains
    PieTableWidgetData o-- DynamicWidgetSummaryData : pieChart/lowerLeft
    TableWidgetData o-- DynamicWidgetTableData : tableData (map)
    DynamicWidgetTableData *-- DynamicWidgetRowData : rows
    DynamicWidgetTableData o-- DynamicWidgetSummaryData : summaryData
    DynamicWidgetSummaryData --> DynamicWidgetCountType : countType uses
    MockData ..> WidgetType : validates
    MockData ..> DynamicWidgetDataType : validates
```

> SVG rendering failed for this diagram.
