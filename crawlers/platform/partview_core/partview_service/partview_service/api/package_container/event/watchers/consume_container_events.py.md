# Diagram: partview_core/partview_service/partview_service/api/package_container/event/watchers/consume_container_events.py


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
flowchart TD
    H[mandatory_lambda_handling()] --> A[lambda_handler(event, context, audit_refs)]
    subgraph Imports
        M1[BackorderContainerEventsSqsHelper]
        M2[make_response(status, body)]
        M3[logging.info]
        M4[json.loads]
    end
    A --> L1[logging.info("Batch Messages: {event}")]
    A --> B{for each message in event.Records}
    B --> C[attempt parse: record = json.loads(message.body)]
    C -->|success| E[instantiate helper = BackorderContainerEventsSqsHelper()]
    C -->|failure| D[record = message.body]
    D --> E
    E --> F[helper.read(record)]
    F --> B
    B --> G[return make_response({}, 200)]
    G --> M2
    L1 --> M3
    C --> M4
```

> SVG rendering failed for this diagram.
