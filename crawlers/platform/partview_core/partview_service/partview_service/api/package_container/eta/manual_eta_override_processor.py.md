# Diagram: partview_core/partview_service/partview_service/api/package_container/eta/manual_eta_override_processor.py


> Auto-generated by Obscura crawlers

## Diagram 1

```mermaid
classDiagram
    class ManualEtaOverrideHandler {
        - __stage: str
        - __queueName: str
        - __queue
        - __event
        + __init__(event)
        + read()
    }
    class InvokeLambda <<utility>>
    class InvokeEventHelper <<utility>>
    class boto3_resource <<aws>>
    class mandatory_lambda_handling <<decorator>>
    class make_response <<function>>
    class lambda_handler <<function>>

    boto3_resource --> ManualEtaOverrideHandler : provides SQS resource
    ManualEtaOverrideHandler --> InvokeEventHelper : constructs
    ManualEtaOverrideHandler --> InvokeLambda : constructs
    lambda_handler ..> mandatory_lambda_handling : decorated by
    lambda_handler --> ManualEtaOverrideHandler : instantiates
    lambda_handler --> make_response : returns result
    InvokeLambda --> "invoke_function()" : calls
```

> SVG rendering failed for this diagram.

## Diagram 2

```mermaid
flowchart LR
    A[Event received] --> B{Records in event?}
    B -- Yes --> C[For each message in Records]
    C --> D[Try parse message.body as JSON]
    D --> E{JSON parsed?}
    E -- Yes --> F[record = parsed JSON]
    E -- No --> G[record = raw body string]
    F --> H[Instantiate ManualEtaOverrideHandler(record)]
    G --> H
    H --> I[Call read() on handler]
    I --> J[InvokeEventHelper created with event, POST, body, pathParameters]
    J --> K[InvokeLambda created with organization_id, function_name, full_payload, invoke_type=Event]
    K --> L[invoke.invoke_function() executed]
    L --> M[Continue next message]
    M --> N[All messages processed]
    N --> O[make_response({}, 200) returned]
    B -- No --> O
```

> SVG rendering failed for this diagram.
