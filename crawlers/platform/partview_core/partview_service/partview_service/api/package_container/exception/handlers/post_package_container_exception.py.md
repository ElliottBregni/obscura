# Diagram: partview_core/partview_service/partview_service/api/package_container/exception/handlers/post_package_container_exception.py


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
classDiagram
    class PostContainerExceptionHandler {
        -RO_EVENT_CODES: list
        -__container_exception: PackageContainerException
        -__package_container
        -__package_container_id
        -__event_ts: datetime
        -__hold_days: int
        -__reason_code: str
        -__category: str
        -__data_store: PackageContainerExceptionPostgresqlMapping
        -__helper: PackageContainerExceptionHelper
        -__container_data_store_helper: PackageContainerHelper
        -__container_exception_status: FVPackageContainerExceptionStatus
        -__container_exception_api_mapping: PackageContainerExceptionApiMapping
        -__package_container_event_bl: PackageContainerEventBusinessLogic
        +__init__(event)
        +parse_request()
        +get_group_id()
        +validate_parameters()
        +process()
        +format_result()
        +container_exception_from_body()
    }

    class PartViewRequestHandler {
        +get_request_id()
        +get_path_parameter(name)
        +get_body()
        +get_solution_id()
        +get_application_name()
        +get_user_email()
        +get_request_type_path_param()
        +get_http_method()
        +set_message_group_id(id)
        +set_audit_keys(...)
    }

    class PackageContainerException {
        +set(field, value)
        +solution_id
        +exception_type_id
        +exception_category_id
        +container_id
    }

    class PackageContainerHelper {
        +get_full_container_by_external_id_event(id, handler, create_if_not_found=False)
        +get_full_container_by_primary_id(id)
    }

    class PackageContainerExceptionHelper {
        +get_exception_type_id_by_reason_code(solution_id, reason_code)
        +get_exception_category_id(solution_id, category, exception_type_id)
        +get_package_container_exception(container_id, exception_type_id, app_name, status)
        +package_container_exception_logic(...)
    }

    class PackageContainerExceptionPostgresqlMapping {
        +__init__(application_name)
        +persist(...)
    }

    class PackageContainerEventBusinessLogic {
        +__init__(application_name, config, request_id)
    }

    class FVPackageContainerExceptionStatus {
        +__init__(application_name)
    }

    class PackageContainerExceptionApiMapping {
        +set_api_mapping()
    }

    class PackageContainerArchiveHelper {
        +insert_into_container_orchestrator(containers)
    }

    class MapAction {
        +map_persistable_to_payload(mapping, persistable, return_no_nulls=True)
        +map_request_to_persistable(mapping, body, persistable, method, ignore_keys=[])
    }

    class GeneralDataUtils {
        +remove_url_encoded_characters(s)
    }

    class FvUuid {
        +is_valid_uuid(id, raise_error=False, error_message=None)
    }

    class Datetime {
        +__init__(value)
        +get_datetime()
    }

    class BadRequestError {
        +__init__(message, detail)
    }

    class PersistableStatus {
        <<enumeration>>
        ACTIVE
    }

    class http {
        <<namespace>>
        +HTTPStatus
    }

    PartViewRequestHandler <|-- PostContainerExceptionHandler
    PostContainerExceptionHandler --> PackageContainerException : uses
    PostContainerExceptionHandler --> PackageContainerHelper : uses
    PostContainerExceptionHandler --> PackageContainerExceptionHelper : uses
    PostContainerExceptionHandler --> PackageContainerExceptionPostgresqlMapping : instantiates
    PostContainerExceptionHandler --> PackageContainerEventBusinessLogic : instantiates
    PostContainerExceptionHandler --> FVPackageContainerExceptionStatus : instantiates
    PostContainerExceptionHandler --> PackageContainerExceptionApiMapping : instantiates
    PostContainerExceptionHandler --> PackageContainerArchiveHelper : calls
    PostContainerExceptionHandler --> MapAction : calls
    PostContainerExceptionHandler --> GeneralDataUtils : calls
    PostContainerExceptionHandler --> FvUuid : calls
    PostContainerExceptionHandler --> Datetime : uses
    PostContainerExceptionHandler --> BadRequestError : raises
    PackageContainerExceptionHelper --> PackageContainerExceptionPostgresqlMapping : may use
    PackageContainerHelper --> PackageContainerArchiveHelper : may call
    MapAction ..> PackageContainerException : maps
    PersistableStatus <|-- PackageContainerExceptionPostgresqlMapping
    http .-> PostContainerExceptionHandler : returns status codes
```

> SVG rendering failed for this diagram.
