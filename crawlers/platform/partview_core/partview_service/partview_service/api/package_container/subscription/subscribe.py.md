# Diagram: partview_core/partview_service/partview_service/api/package_container/subscription/subscribe.py


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
flowchart LR
    A[Event + Context] --> B[mandatory_lambda_handling wrapper]
    B --> C[SubscriptionHandlerFactory.getSubscriptionHandler(event, "add", lambda_application_name)]
    C --> D[request_handler.parse_request()]
    D --> E[request_handler.validate_parameters()]
    E --> F[request_handler.process()]
    F --> G[request_handler.format_result()\n(status, data)]
    G --> H[make_response(data, status)\n(return)]
    D -- AssertionError --> I[catch AssertionError]
    I --> J[raise BadRequestError(message, message)]
```

> SVG rendering failed for this diagram.
