# Diagram: partview_core/partview_service/partview_service/api/package_container/subscription/classes/SubscribeByExternalIdAndOrganization.py


> Auto-generated by Obscura crawlers

## Diagram 1

```mermaid
classDiagram
    class SubscribeBase
    class SubscribeByExternalIdAndOrganization
    class PackageContainer
    class NotFoundError
    class InvokeOrganization
    SubscribeByExternalIdAndOrganization --|> SubscribeBase
    SubscribeByExternalIdAndOrganization : +__init__(event, application_name, action)
    SubscribeByExternalIdAndOrganization : +parse_request() SubscribeByExternalIdAndOrganization : -setContainerId(id)
    SubscribeByExternalIdAndOrganization : -setContainerOwnerOrganizationId(org_id)
    SubscribeByExternalIdAndOrganization ..> PackageContainer : constructs/sets tracking_number, solution_id
    SubscribeByExternalIdAndOrganization ..> NotFoundError : raises when not found
    SubscribeByExternalIdAndOrganization ..> InvokeOrganization : invoke_get_organization_from_fv_id()
    PackageContainer : +set(key, value)
    PackageContainer : +get(key, default=None)
    NotFoundError : +message
    InvokeOrganization : +invoke_get_organization_from_fv_id(fv_id)
```

> SVG rendering failed for this diagram.

## Diagram 2

```mermaid
flowchart TD
    Start((Start)) --> A[Get path parameter "containerId"]
    A --> B{Validate containerId}
    B -- invalid --> RaiseNotFound["Raise NotFoundError(\"Invalid primary ID\")/assert"]
    B -- valid --> C[Read owner_solution_id from body]
    C --> D{solution present?}
    D -- yes --> E[use owner_solution_id]
    D -- no --> F[get solution via get_solution_id()]
    E --> G[Build PackageContainer with tracking_number=containerId and solution_id]
    F --> G
    G --> H[Search packageContainer mapping]
    H --> I{packageContainerSearch empty?}
    I -- yes --> J[Raise NotFoundError("Container not found")]
    I -- no --> K[packageContainer = first search result]
    K --> L[setContainerId(containerId)]
    L --> M[organization_fv_id = packageContainer.get("owner_org_fv_id")]
    M --> N[organization = InvokeOrganization.invoke_get_organization_from_fv_id(organization_fv_id)]
    N --> O[organization_id = organization.get("organization_id")]
    O --> P{organization_id present?}
    P -- no --> Q[Assert fail "Can't find organization"]
    P -- yes --> R[setContainerOwnerOrganizationId(organization_id)]
    R --> End((Return self))
```

> SVG rendering failed for this diagram.
