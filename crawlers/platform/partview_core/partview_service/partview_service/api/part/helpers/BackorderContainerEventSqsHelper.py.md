# Diagram: partview_core/partview_service/partview_service/api/part/helpers/BackorderContainerEventSqsHelper.py


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
classDiagram
class BackorderContainerEventsSqsHelper {
  +int MAX_NUMBER_OF_MESSAGES
  +int MAX_NUMBER_OF_RETRY
  +int MESSAGE_DUMP_SIZE
  +String INVOCATION_TYPE
  -resource __sqs
  -String __stage
  -String __queueName
  -Queue __queue
  +__init__()
  +send(message_body)
  +invoke_service_orchestrator(message_dump, authorizer, headers, request_id, bo_type)
  +send_messages(messages)
  +read(message)
  +process_message(message)
  +create_backorder_milestone(body)
  +create_lambda_event(request_id, actor_id, organization_id)
}
class InvokeEventHelper {
  +InvokeEventHelper(event, request_id, actor_id, customer_org)
  +get_sample_admin_event(http_method, customer_id)
}
class InvokeBackorder {
  +send_backorder_events(authorizer, message, request_id, version, method)
}
class InvokeContainerEvent {
  +post_container_event(event, organizationFvId, containerId, eventCode, invocationType)
}
class VersionOptions {
  class VersionTypes {
    +PARTVIEW_BO
    +SHIPMENT_BO
    +PV_VERSION
    +SH_VERSION
  }
}
class boto3 {
  +resource(service_name)
}
class ClientError
class logging {
  +info(message)
}

BackorderContainerEventsSqsHelper --> boto3 : uses
BackorderContainerEventsSqsHelper --> InvokeBackorder : calls
BackorderContainerEventsSqsHelper --> InvokeContainerEvent : instantiates/calls
BackorderContainerEventsSqsHelper --> InvokeEventHelper : instantiates
BackorderContainerEventsSqsHelper --> VersionOptions.VersionTypes : reads
BackorderContainerEventsSqsHelper --> ClientError : catches
BackorderContainerEventsSqsHelper --> logging : logs with
```

> SVG rendering failed for this diagram.
