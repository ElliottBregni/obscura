# Diagram: partview_core/partview_service/partview_service/api/part/backorder/backorder_events_builder/backorder_events_builder_handler.py


> Auto-generated by Obscura crawlers

## Diagram 1

```mermaid
classDiagram
    class FVRequestFactory {
        <<framework>>
        +handler
        +__init__(event)
    }
    class BackorderEventsBuilderHandlerFactory {
        +handlers
        +__init__(event)
    }
    class PostBackorderEventsBuilder {
        +handle_request()
    }
    class mandatory_lambda_handling {
        <<decorator>>
    }
    class make_response {
        <<function>>
        +make_response(response, http_code)
    }
    FVRequestFactory <|-- BackorderEventsBuilderHandlerFactory
    BackorderEventsBuilderHandlerFactory --> PostBackorderEventsBuilder : handlers["POST"]
    BackorderEventsBuilderHandlerFactory : +handlers = {"POST": PostBackorderEventsBuilder}
    BackorderEventsBuilderHandlerFactory : +handler (property from FVRequestFactory)
```

> SVG rendering failed for this diagram.

## Diagram 2

```mermaid
flowchart TD
    Event["AWS Lambda Event"] -->|passed to| LambdaHandler[lambda_handler]
    LambdaHandler -.->|decorated by| Decorator[mandatory_lambda_handling]
    LambdaHandler --> FactoryCtor[BackorderEventsBuilderHandlerFactory(event)]
    FactoryCtor --> RequestHandler[request_handler (from factory.handler)]
    RequestHandler -->|calls| PostBuilder[PostBackorderEventsBuilder.handle_request()]
    PostBuilder --> ResponseObj[response, http_code]
    ResponseObj -->|wrapped by| MakeResponse[make_response(response, http_code)]
    MakeResponse --> AWSResp["HTTP Response"]
```

> SVG rendering failed for this diagram.
