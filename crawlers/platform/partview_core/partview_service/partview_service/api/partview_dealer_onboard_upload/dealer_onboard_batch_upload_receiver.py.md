# Diagram: partview_core/partview_service/partview_service/api/partview_dealer_onboard_upload/dealer_onboard_batch_upload_receiver.py


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
classDiagram
    class PartViewRequestHandler {
        <<abstract>>
        +handle_request()
    }

    class DealerBatchUploadReceiverHandler {
        -__stage
        -__queueName
        -__queue
        -__location_processor
        -__user_organization_id
        -__request_id
        -__group_id
        -__data_store
        -__user_solution_id
        +DealerBatchUploadReceiverHandler(event)
        +parse_request()
        +validate_parameters()
        +get_user_solutions()
        +get_user_organization_id()
        +set_user_organization_id(organization_id)
        +set_user_solution_id(user_solution_id)
        +get_request_id()
        +set_request_id(request_id)
        +get_group_id()
        +set_group_id(group_id)
        +change_status(current_status, future_status, row_id, record, comment=None)
        +format_batch_messages(results)
        +send_notification(presigned_url, batch_message, status, user_email)
        +send_batch_completed_notification(results, failed_results, meta, sqs_event)
        +invoke_notification_service(max_count, group_id, meta, sqs_event)
        +get_dealer_organization_by_name(bac)
        +get_organization_for_dealer_org(org_id, user_org_id=None)
        +get_organization_by_fv_id_no_cache(fv_id, user_org_id=None)
        +get_dealer_organization_by_name_using_everything(dealer_name)
        +get_dealer_grants_by_grantee_id(dealer_id)
        +create_dealer_organization(dealer_org_body, row_id, record)
        +update_dealer_organization(dealer_org)
        +assemble_dealer_payload(name, fv_id, bac, email_address, location, organization_profile_id, map_data=None, existing_grants=None, location_codes=None, is_update=False)
        +get_external_org_codes(dealer_org_id)
        +update_dealer_organization_visibility(dealer_org, payload, location_details, payload_meta)
        +get_solution_by_fv_id_and_feature(org_id, feature=Auth.Feature.PARTVIEW)
        +create_solution(solution_body, row_id, record)
        +get_location_details(location_code)
        +create_location(map_data)
        +handle_partview_solution(record, org_id, fv_id, dealer_name, row_id)
        +get_dealer_meta(dealer_org_body, bac)
        +handle_org_get_or_create(location_name, map_data, meta, location_details, row_id, record)
        +get_or_create_location(location_code, map_data)
        +process_partview_dealer_onboard(payload, payload_meta)
        +process_message(event)
        +process()
        +format_result()
    }

    class ProcessRouteUploadPostgresqlMapping {
        +search(obj)
        +update(obj)
    }

    class ProcessRouteUpload {
        +row_id
        +identifier
        +group_id
        +state
        +id
        +identifier_key
        +action
        +comment
    }

    class VisibilityGrantPostgresqlMapping {
        +update_package_container_data_for_visibility_grant(location_codes, solution, type, name)
    }

    class LocationProcessor {
        +process_location(org_id, location_details_dict, user_solutions, group_id, ...)
    }

    class InvokeLambda {
        +__init__(organization_id, function_name, full_payload, invoke_type=None)
        +invoke_function()
    }

    class InvokeLocation {
        +invoke_get_location_by_code_return_single(org_id, code)
        +get_location_codes_by_location_id(org_id, location_ids)
    }

    class InvokeGetSolution {
        +invoke_get_solution_by_org_and_feature(org_id, feature)
    }

    class Auth {
        class Feature {
            PARTVIEW
        }
    }

    class State {
        PENDING
        CLAIMED
        SUCCESS
        FAILURE
    }

    class lambda_handler {
        +lambda_handler(event, context, audit_refs)
    }

    PartViewRequestHandler <|-- DealerBatchUploadReceiverHandler
    DealerBatchUploadReceiverHandler --> ProcessRouteUploadPostgresqlMapping : uses
    DealerBatchUploadReceiverHandler --> ProcessRouteUpload : constructs
    DealerBatchUploadReceiverHandler --> VisibilityGrantPostgresqlMapping : uses
    DealerBatchUploadReceiverHandler --> LocationProcessor : has
    DealerBatchUploadReceiverHandler --> InvokeLambda : invokes
    DealerBatchUploadReceiverHandler --> InvokeLocation : uses
    DealerBatchUploadReceiverHandler --> InvokeGetSolution : queries
    DealerBatchUploadReceiverHandler ..> Auth : references
    DealerBatchUploadReceiverHandler ..> State : references
    lambda_handler --> DealerBatchUploadReceiverHandler : creates/dispatches
    InvokeLambda --> "AWS Lambda" : external service
    VisibilityGrantPostgresqlMapping --> "PostgreSQL DB" : persistence
    ProcessRouteUploadPostgresqlMapping --> "PostgreSQL DB" : persistence
```

> SVG rendering failed for this diagram.
