# Diagram: partview_core/partview_service/partview_service/api/public/PostFullOrder.py


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
classDiagram
    class PostAsnOrder {
        - __response
        - __parsed_body_list
        - __package_container_parser
        + invoke_order_and_link_to_trip(orders, shipment, trip_id)
        + calculate_packages_per_message_group(container_list_depth)
        + invoke_container_handler(container_list, trip_leg)
        + container_response(return_list, containers, trip_leg)
        + invoke_trip_leg_to_order(trip_id, orders)
        + invoke_trip_leg(trip)
        + process_full_asn_order(trip, containers, orders)
        + process_trip(trip)
        + process_orders(orders, trip_id, index)
        + process_containers(containers, trip_leg, index)
        + parse_request()
        + process()
        + validate_parameters()
        + validate_trip_leg(parsed_body, index)
        + validate_orders(orders)
        + validate_containers(containers)
        + validate_parts(parts)
        + format_result()
        + get_group_id()
        + shipper_auth_check()
    }

    class PartViewRequestHandler
    class InvokeLambda
    class InvokeEventHelper
    class GeneralDataUtils
    class ThreadPoolExecutor
    class OrderParser
    class ASNOrderValidator
    class PackageContainerParser
    class PackageContainerValidator
    class PartParser
    class PartValidator
    class TripLegParser
    class TripLegValidator
    class TripLeg
    class LambdaHandlerMiddleware
    class SQStopic
    class SQSMessageIdentifier
    class AcceptVersion

    PostAsnOrder --|> PartViewRequestHandler
    PostAsnOrder --> InvokeLambda : "creates/uses"
    PostAsnOrder --> InvokeEventHelper : "builds payload"
    PostAsnOrder --> GeneralDataUtils : "split_list, utils"
    PostAsnOrder --> ThreadPoolExecutor : "parallel processing"
    PostAsnOrder --> OrderParser : "parses orders"
    PostAsnOrder --> ASNOrderValidator : "validates orders"
    PostAsnOrder --> PackageContainerParser : "parses containers"
    PostAsnOrder --> PackageContainerValidator : "validates containers"
    PostAsnOrder --> PartParser : "parses parts"
    PostAsnOrder --> PartValidator : "validates parts"
    PostAsnOrder --> TripLegParser : "parses trip"
    PostAsnOrder --> TripLegValidator : "validates trip"
    PostAsnOrder --> TripLeg : "produces TripLeg instances"
    PostAsnOrder --> LambdaHandlerMiddleware : "wrapped by"
    PostAsnOrder ..> SQStopic : "uses constants"
    PostAsnOrder ..> SQSMessageIdentifier : "uses constants"
    PostAsnOrder ..> AcceptVersion : "uses constants"

    %% Constants and environment
    class ENV {
        + INVOKE_RELATIONSHIP_SYNC
        + ORDERS_PER_TRIP
        + PACKAGES_PER_GROUP_DIVISOR
        + FORCE_BATCH_SIZE
    }
    PostAsnOrder ..> ENV : "reads"
    InvokeLambda --> "AWS Lambda / external handlers" : "invokes"
    InvokeEventHelper --> "Event Payload" : "constructs"
    GeneralDataUtils --> "List utilities" : "split_list"
    ThreadPoolExecutor --> "concurrent.futures" : "manages threads"
```

> SVG rendering failed for this diagram.
