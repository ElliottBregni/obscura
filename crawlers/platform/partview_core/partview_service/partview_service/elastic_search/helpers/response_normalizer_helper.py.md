# Diagram: partview_core/partview_service/partview_service/elastic_search/helpers/response_normalizer_helper.py


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
flowchart LR
  Results[Results (Any)] -->|is list?| IsList{Is instance of list?}
  IsList -- No --> ReturnResults[Return original results]
  IsList -- Yes --> Iterate[Iterate over results]
  Iterate --> IsDict{Is item a dict?}
  IsDict -- No --> AppendRaw[Append item unchanged]
  IsDict -- Yes --> CallNorm[Call normalize_v1_fields(item)]
  CallNorm --> NormFunc[normalize_v1_fields]
  NormFunc --> CopyDoc[Make shallow copy of document]
  CopyDoc --> ForEach[For each (internal_field, public_field) in _V1_TO_PUBLIC_FIELD_MAP]
  ForEach --> HasInternal{internal_field in doc?}
  HasInternal -- Yes and public missing --> MoveField[Set public_field = pop(internal_field)]
  HasInternal -- No --> Skip
  MoveField --> ContinueLoop[continue]
  Skip --> ContinueLoop
  ContinueLoop --> AfterLoop[After loop return normalized_document]
  AfterLoop --> AppendNorm[Append normalized dict to normalized_results]
  AppendRaw --> ContinueIter[Continue iteration]
  AppendNorm --> ContinueIter
  ContinueIter --> EndLoop[After all items return normalized_results]
  ReturnResults --> EndLoop
  EndLoop --> End[Result returned]
```

> SVG rendering failed for this diagram.
