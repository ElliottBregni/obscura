# Diagram: partview_core/partview_service/partview_service/elastic_search/counts/partview_counts.py


> Auto-generated by Obscura crawlers

## Diagram 1

```mermaid
classDiagram
    class PartRequestHandler {
        <<abstract>>
        +get_version()
        +get_user_id()
        +get_solution_id()
        +get_org_profiles()
        +get_query_parameter(name)
    }

    class PartviewCountsMapping {
        +search(query)
    }

    class OpenSearchCountsQueryParamBuilder {
        +__init__(params)
        +build_counts_query()
    }

    class CountsQueryParameters
    class LifecycleStates
    class ExceptionHelper
    class DefaultSolution

    class CountsHandler {
        -__results
        -__solution_id
        -__status
        -__user_id
        -__count_params
        -__application_name
        -__version
        -__data_store : PartviewCountsMapping
        +CountsHandler(event)
        +parse_request()
        +validate_parameters()
        +process()
        +format_result()
        +set_count_parameters(option_name, query_parameter_name)
    }

    PartRequestHandler <|-- CountsHandler
    CountsHandler o-- PartviewCountsMapping : uses
    CountsHandler ..> OpenSearchCountsQueryParamBuilder : builds query with
    CountsHandler ..> CountsQueryParameters : reads constants from
    CountsHandler ..> LifecycleStates : iterates over
    CountsHandler ..> ExceptionHelper : retrieves exception codes
    CountsHandler ..> DefaultSolution : uses default when isDealer
    note right of CountsHandler : application name PV-CountsHandler
```

> SVG rendering failed for this diagram.

## Diagram 2

```mermaid
flowchart TD
    A[Lambda event] --> B[lambda_handler]
    B --> C[Instantiate CountsHandler(event)]
    C --> D[parse_request()]
    D --> E[validate_parameters()]
    E --> F[OpenSearchCountsQueryParamBuilder.build_counts_query()]
    F --> G[PartviewCountsMapping.search(query)]
    G --> H[CountsHandler.process() sets __results]
    H --> I[format_result() — select by __version]
    I --> J{__version mapping}
    J -->|TOTAL_COUNT| K[format_total_count()]
    J -->|EXCEPTION| L[format_exception()]
    J -->|LIFECYCLE_STATE| M[format_lifecycle_state()]
    J -->|DELIVERED| N[format_delivered()]
    K --> O[aggregate exception, lifecycle, delivered]
    L --> O
    M --> O
    N --> O
    O --> P[make_response(parts_data, HTTP 200)]
    P --> Q[Return HTTP 200 response]
```

> SVG rendering failed for this diagram.
