# Diagram: partview_core/partview_service/partview_service/utility/InvokeSubscription.py


> Auto-generated by Obscura crawlers

## Diagram 1

```mermaid
classDiagram
    class InvokeSubscription {
        +invoke_subscribe_to_event(event, organization_fv_id, body)
        +invoke_unsubscribe_from_event(event, organization_fv_id, body)
        +invoke_update_event_subscription(event, organization_fv_id, subscription_id, body)
        +invoke_get_event_subscriptions(event, organization_fv_id, container_id, query_parameters)
        +invoke_send_event_to_subscribers(organization_fv_id, container, body)
    }
    class InvokeLambda {
        +__init__(organization_id, function_name, full_payload)
        +invoke_function() 
    }
    class InvokeEventHelper {
        +__init__(event, version, body, http_method, pathParameters)
        +get_full_payload()
    }
    class logging {
        +info(msg)
    }
    InvokeSubscription --|> InvokeLambda : creates/uses
    InvokeSubscription --|> InvokeEventHelper : builds payload with
    InvokeSubscription --> logging : logs status/data
    InvokeLambda --> "AWS Lambda Function" : invokes
```

> SVG rendering failed for this diagram.

## Diagram 2

```mermaid
flowchart TD
    A[Caller / Client] --> B[InvokeSubscription]
    B --> |subscribe| C[InvokeLambda: subscribe_to_event]
    B --> |unsubscribe| D[InvokeLambda: unsubscribe_from_event]
    B --> |update| E[InvokeLambda: update_subscription]
    B --> |get_subscriptions| F[InvokeLambda: get_user_subscriptions]
    B --> |send_event| G[InvokeEventHelper]
    G --> H[InvokeLambda: send_event_to_subscribers]
    C --> I{invoke_function()}
    D --> I
    E --> I
    F --> I
    H --> I
    I --> J[AWS Lambda / Service]
    I --> K[status_code, data]
    K --> B
    F --> L[logging.info(status, data)]
```

> SVG rendering failed for this diagram.
