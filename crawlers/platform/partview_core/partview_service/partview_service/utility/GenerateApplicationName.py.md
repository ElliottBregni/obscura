# Diagram: partview_core/partview_service/partview_service/utility/GenerateApplicationName.py


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
flowchart LR
  Start([start]) --> CheckParent{parent_application_name\nis falsy?}
  CheckParent -- Yes --> ReturnMissing["return \"PV-MissingName.{class_name}\""]
  CheckParent -- No --> Concat["application_name = parent_application_name + '.' + class_name"]
  Concat --> LengthCheck{len(application_name) > 63?}
  LengthCheck -- No --> ReturnApp["return application_name"]
  LengthCheck -- Yes --> ForEachLoop["for word,replacement in OVER_USED_WORDS.items():\n application_name = application_name.replace(word,replacement)"]
  ForEachLoop --> ReturnAppAfter["return application_name"]
  subgraph Constants
    direction TB
    OVER_USED_WORDS[/"OVER_USED_WORDS: dict[string->string]"/]
    OW1["\"Handler\" → \"H\""]
    OW2["\"Request\" → \"R\""]
    OW3["\"PostgresqlMapping\" → \"SQL\""]
    OVER_USED_WORDS --> OW1
    OVER_USED_WORDS --> OW2
    OVER_USED_WORDS --> OW3
  end
  Start -.-> OVER_USED_WORDS
```

> SVG rendering failed for this diagram.
