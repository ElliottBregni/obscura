# Diagram: partview_core/partview_service/partview_service/persistence_adapter/postgresql/package_container/PackageContainerSearchFilters.py


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
classDiagram
class PackageContainerSearchFilters {
  <<abstract>>
  +orderFromDate_filter(options: dict)
  +orderToDate_filter(options: dict)
  +assemble_filter(query_list)
  +assemble_filter_with_operation(query_list, operation)
  +get_tracking_number_filter(options: dict)
  +get_order_number_filter(options: dict)
  +get_order_priority_filter(options: dict)
  +get_origin_location_code_filter(options: dict)
  +get_destination_location_code_filter(options: dict)
  +get_final_mile_origin_filter(options: dict)
  +get_trailer_equipment_number_filter(options: dict)
  +get_shipment_id_filter(options: dict)
  +get_part_number_filter(options: dict)
  +get_part_name_filter(options: dict)
  +get_carrier_filter(options: dict)
  +get_exception_name_filter(options: dict)
  +get_lifecycle_state_filter(options: dict)
  +lifecycle_state_ts(options)
  +get_order_from_date_filter(options: dict)
  +get_order_to_date_filter(options: dict)
  +get_delivery_from_date_filter(options: dict)
  +get_delivery_to_date_filter(options: dict)
  +get_status_filter(options: dict)
  +get_bill_of_lading_filter(options: dict)
  +get_batch_search_filter(options: dict)
  +get_everything_filter(options: dict)
}
class SearchQueryParameters {
  <<enum-like>>
}
class Version {
  <<enum-like>>
}
class DefaultQueries {
  +search_exception_not_in
}

PackageContainerSearchFilters ..> SearchQueryParameters : uses
PackageContainerSearchFilters ..> Version : checks
PackageContainerSearchFilters ..> DefaultQueries : uses search_exception_not_in
PackageContainerSearchFilters o-- "assemble_filter" : helper
PackageContainerSearchFilters o-- "assemble_filter_with_operation" : helper
SearchQueryParameters <|-- Version
DefaultQueries : search_exception_not_in = "search_exception_not_in"
PackageContainerSearchFilters : "many SQL EXISTS / ILIKE / IS NULL checks"
```

> SVG rendering failed for this diagram.
