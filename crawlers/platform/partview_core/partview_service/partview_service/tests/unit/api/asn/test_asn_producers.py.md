# Diagram: partview_core/partview_service/partview_service/tests/unit/api/asn/test_asn_producers.py


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
flowchart LR
  subgraph EnvCheck["Environment"]
    A[AWS_STAGE != 'dev'] -->|true| RunSkippedTests
    A -->|false| SkipTests
  end

  subgraph Tests["PyTest Smoke & Producer Tests"]
    RunSkippedTests --> T1[test_asn_creation_success]
    RunSkippedTests --> T2[test_package_creation_success]
    RunSkippedTests --> T3[test_package_to_container_success]
    RunSkippedTests --> T4[test_package_container_event_success]
    SkipTests --> Skipped[skipped (dev)]
    T5[test_trip_leg_producer_success]
  end

  subgraph LambdaHandlers["Lambda Handlers"]
    ASN[asn_order_lambda_handler]
    PKG_CON[package_container_lambda_handler]
    P2C[part_to_container_lambda_handler]
    PKG_EVT[package_container_event_lambda_handler]
    TRIP[trip_leg_lambda_handler_producer]
    CTX[MockLambdaContext(timeout=30)]
  end

  T1 -->|calls| ASN
  T2 -->|calls| PKG_CON
  T3 -->|calls| P2C
  T4 -->|calls| PKG_EVT
  T5 -->|calls| TRIP

  ASN --> CTX
  PKG_CON --> CTX
  P2C --> CTX
  PKG_EVT --> CTX
  TRIP --> CTX

  CTX -->|returns| R1[response]
  R1 -->|assert statusCode == "200"| PASS[pass]

  style EnvCheck fill:#f9f,stroke:#333,stroke-width:1px
  style Tests fill:#bbf,stroke:#333,stroke-width:1px
  style LambdaHandlers fill:#bfb,stroke:#333,stroke-width:1px
  style PASS fill:#9f9,stroke:#333,stroke-width:1px
```

> SVG rendering failed for this diagram.
