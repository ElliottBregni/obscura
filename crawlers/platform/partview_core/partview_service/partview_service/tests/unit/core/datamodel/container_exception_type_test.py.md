# Diagram: partview_core/partview_service/partview_service/tests/unit/core/datamodel/container_exception_type_test.py


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
classDiagram
    class ContainerExceptionTypeTest {
        - id: str
        - solution_id: str
        - exception_description: str
        - update_exception: str
        - connector
        + test_exception_description_as_int_value()
        + test_container_exception_type_write()
        + test_container_exception_type_read()
        + test_container_exception_type_update()
        + test_container_exception_type_delete()
    }

    class CrudOperations {
        + create_row(connector, table_name, column_map, obj)
        + read(connector, table_name, column_map, obj)
        + update_row(connector, table_name, column_map, obj)
        + delete(connector, table_name, column_map, obj, soft)
    }

    class ContainerExceptionType {
        - id
        - solution_id
        - exception_description
        + __init__(id=None, solution_id=None, exception_description=None)
        + from_dict(d)
        + to_dict()
        + get_table_name()
        + get_column_map()
        + set(field, value)
    }

    class TestSetup {
        + get_database_connector(stage, region, secrets_file)
    }

    class unittest.TestCase

    ContainerExceptionTypeTest --|> unittest.TestCase : extends
    ContainerExceptionTypeTest --> TestSetup : uses
    ContainerExceptionTypeTest --> CrudOperations : uses
    ContainerExceptionTypeTest --> ContainerExceptionType : uses
    CrudOperations ..> ContainerExceptionType : operates on
    ContainerExceptionTypeTest : "connector = TestSetup().get_database_connector(...)"
    TestSetup --> "DatabaseConnector" : returns
```

> SVG rendering failed for this diagram.
