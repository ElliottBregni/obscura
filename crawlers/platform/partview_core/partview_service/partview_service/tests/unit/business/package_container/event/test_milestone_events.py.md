# Diagram: partview_core/partview_service/partview_service/tests/unit/business/package_container/event/test_milestone_events.py


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
flowchart LR
  subgraph Tests
    T1[Test: backorder_milestone_create_response]
    T2[Test: carrier_event_no_eta_resolved_location]
    T3[Test: asn_creation_success]
    T4[Test: search_package_container]
  end

  subgraph Handlers
    EH[event_handler.lambda_handler]
    ASN[asn_order_lambda_handler]
    SEARCH[search_package_container_lambda_handler]
    ETA[eta_history_lambda_handler]
  end

  subgraph Utilities
    CTX[MockLambdaContext (Context)]
    BASE[BaseCollection]
    LOG[logging.logger]
    JSON[json (json.loads)]
  end

  T1 --> EH
  T1 --> CTX
  T1 --> JSON

  T2 --> EH
  T2 --> CTX

  T3 --> EH
  T3 --> ASN
  T3 --> SEARCH
  T3 --> ETA
  T3 --> BASE
  T3 --> CTX
  T3 --> JSON
  T3 --> LOG

  T4 --> EH
  T4 --> ASN
  T4 --> SEARCH
  T4 --> BASE
  T4 --> CTX
  T4 --> JSON
  T4 --> LOG

  EH -->|produces events for| ASN
  ASN -->|creates/searches containers via| SEARCH
  SEARCH -->|provides tracking results to| ETA
  ETA --> LOG
```

> SVG rendering failed for this diagram.
