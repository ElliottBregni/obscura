# Diagram: partview_core/partview_service/partview_service/tests/unit/business/package_container/event/package_container_event_business_logic_test.py


> Auto-generated by Obscura crawlers

## Diagram 1

```mermaid
classDiagram
    class FakeTripLeg {
        +int sequence_number
        +str origin_location_code
        +str destination_location_code
        +dict origin_location_details
        +dict destination_location_details
        +__init__(seq, origin, dest, origin_lad, dest_lad)
    }
    class FakePackageContainer {
        +str id
        +__init__(id="container-123")
    }
    class FakePackageContainerEvent {
        +str location_code
        +get(key)
        +__init__(location_code)
    }
    class FakeTripLegLoader {
        +list trip_legs
        +get_trips() list
        +get_container() FakePackageContainer
        +__init__(trip_legs)
    }
    class PackageContainerEventBusinessLogic {
        -__container_loader
        +__get_eta_trip_info(container, event) Optional<PackageContainerEtaTripInfo>
        +__init__(app_name, testing_parameters={})
    }
    class PackageContainerEtaTripInfo {
        +int stop_number
        +int total_stops
        +str lad_name
        +list next_locations
    }
    FakeTripLeg "1" --o "many" FakeTripLegLoader : contains
    FakeTripLegLoader --> FakePackageContainer : provides
    PackageContainerEventBusinessLogic o-- FakeTripLegLoader : uses
    PackageContainerEventBusinessLogic --> PackageContainerEtaTripInfo : returns
    FakePackageContainerEvent --> PackageContainerEventBusinessLogic : input
    FakePackageContainer --> PackageContainerEventBusinessLogic : input
```

> SVG rendering failed for this diagram.

## Diagram 2

```mermaid
flowchart TD
    A[Test setup: trip legs] --> B[Inject FakeTripLegLoader into BusinessLogic]
    B --> C{Event location found in trip legs?}
    C -- Yes --> D[Build PackageContainerEtaTripInfo]
    D --> E[Set lad_name and stop_number]
    D --> F[Compute total_stops and next_locations]
    C -- No --> G[Return PackageContainerEtaTripInfo with stop_number=0 and lad_name="Location not on trip plan"]
    C -- Final stop --> H[Skip final stop and return None]
    E --> I[Tests assert values]
    F --> I
    G --> I
    H --> I
```

> SVG rendering failed for this diagram.
