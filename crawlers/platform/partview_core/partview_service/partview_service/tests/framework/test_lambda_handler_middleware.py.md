# Diagram: partview_core/partview_service/partview_service/tests/framework/test_lambda_handler_middleware.py


> Auto-generated by Obscura crawlers

## Diagram 1

```mermaid
classDiagram
class LambdaHandlerMiddleware {
  <<utility>>
  +get_client_error(resp)
  +sanitize_event(event)
  +ensure_status_code(func)
}
class Tests {
  +test_get_client_error_none_returns_default()
  +test_get_client_error_dict()
  +test_get_client_error_string_body_json()
  +test_get_client_error_string_invalid_json()
  +test_sanitize_event_redacts_headers()
  +test_sanitize_event_no_headers()
  +test_ensure_status_code_wraps_string()
  +test_ensure_status_code_wraps_dict_and_tuple()
}
Tests --> LambdaHandlerMiddleware : calls
note right of LambdaHandlerMiddleware : get_client_error normalizes\nvarious response shapes into a client error object
note left of LambdaHandlerMiddleware : sanitize_event redacts headers\nensure_status_code normalizes return values
```

> SVG rendering failed for this diagram.

## Diagram 2

```mermaid
flowchart TD
    A[get_client_error(resp)] -->|resp is None| B[Return default error: "Unexpected error"]
    A -->|resp.body is dict with error.message| C[Return error.message from body]
    A -->|resp.body is JSON string| D[Parse JSON -> return error.message]
    A -->|resp.body invalid JSON or other| B
    E[sanitize_event(event)] -->|headers present| F[Redact sensitive headers to "REDACTED"]
    E -->|no headers| G[Return event unchanged]
    H[ensure_status_code(func)] --> I{func result type}
    I -->|string| J[Wrap to {"message": value} and use instance.get_status_code()]
    I -->|dict| K[Return dict and use instance.get_status_code()]
    I -->|(dict, status)| L[Return dict and provided status]
    J --> M[Return (body_dict, status)]
    K --> M
    L --> M
```

> SVG rendering failed for this diagram.
