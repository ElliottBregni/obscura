# Diagram: partview_core/partview_service/partview_service/tests/common/utility/base_collection.py


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
classDiagram
    class BaseCollection {
        <<dataclass>>
        - shipment_number
        - shipment_id
        - container_id
        - od_pairs
        + base_post_full_order(base_collection_planned_trip_leg_event_data_array) : list
        + set_container_id_on_event_payload(event)
        + set_container_uuid_on_event_payload(event)
        + set_event_at_origin(event)
        + set_event_at_destination(event)
        + set_event_at_origin_destination(event)
        + set_event_at_destination_origin(event)
        + set_event_timestamp_to_now(event)
        + set_event_timestamp_to_specific_time(event, time)
        + set_event_timestampe_to_three_days_ago(event)
        + set_event_timestampe_to_four_days_ago(event)
        + set_random_values()
        + build_events(event_ar_at_location, test_carrier_load_close_events)
    }

    class Datetime {
        + __init__(time)
        + get_datetime() : datetime
    }

    class Common {
        + get_random_string(length) : str
    }

    BaseCollection --> Datetime : uses
    BaseCollection --> Common : uses
    Datetime ..> "datetime.timedelta" : returns/uses
    note for BaseCollection "manipulates event payloads, timestamps, and od_pairs"
```

> SVG rendering failed for this diagram.
