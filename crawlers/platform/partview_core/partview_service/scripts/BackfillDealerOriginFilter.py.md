# Diagram: partview_core/partview_service/scripts/BackfillDealerOriginFilter.py


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
flowchart LR
  Start([start]) --> ParseArgs[/read organization_id and env/]
  ParseArgs --> CheckEnv{env in allowed list?}
  CheckEnv -- no --> InvalidEnv["Log: The entered environment is not valid"]
  InvalidEnv --> End([end])
  CheckEnv -- yes --> GetConnector[DatabaseConnectorFactory.get_connector(...).get_primary()]
  GetConnector --> CheckAuthorizer{event.requestContext.authorizer.organization_id present?}
  CheckAuthorizer -- no --> LogMissing["Log: Add organization_id to the authorizer"]
  LogMissing --> End
  CheckAuthorizer -- yes --> SetAuthorizer["Set event.requestContext.authorizer.organization_id = organization_id"]
  SetAuthorizer --> InvokeGetOrgs[invoke_get_orgs(event, organization_id) -> fv_org_id]
  InvokeGetOrgs --> InvokeGetSolution[invoke_get_solution_by_organization_id_and_feature(event, organization_id, PartView) -> solution_id]
  InvokeGetSolution --> GetOrigins[get_origins(connector, solution_id) -> results]
  GetOrigins --> GetGrants[get_granted_solution_ids(fv_org_id, organization_id, results) -> formatted_result]
  GetGrants --> Chunk[chunk_list_of_tuples(formatted_result, chunk_size=100) -> chunked_list]
  Chunk --> BuildAndExecute[build_and_execute_query(chunked_list, connector, total_counts)]
  BuildAndExecute --> LogComplete["Log: Backfill is complete"]
  LogComplete --> End

  classDef io fill:#f9f,stroke:#333,stroke-width:1px
  class InvokeGetOrgs,InvokeGetSolution,GetOrigins,GetGrants,BuildAndExecute io
```

> SVG rendering failed for this diagram.
