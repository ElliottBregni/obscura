# Diagram: partview_core/partview_service/scripts/SyncVisWithLocationGrants.py


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
flowchart TD
  Main["__main__\n(parse_arguments, get_grants, loop grants)"] -->|calls| Parse[parse_arguments()]
  Main -->|calls| GetGrants[get_grants(args)]
  GetGrants -->|calls for each dealer| GetDealers[get_dealers(feature, profile)]
  GetDealers -->|uses| ShipmentConn[shipment_connector]
  ShipmentConn -->|establish_connection|getDealersDB["DB Cursor -> SQL query\n(organizations/solution/feature)"]
  GetGrants -->|for each dealer calls| GetDealerGrants[get_dealer_grants_by_grantee_id(dealer_id, fv_id, org_id)]
  GetDealerGrants -->|invokes| InvokeGetOrg[InvokeLambda(grants-get)]
  InvokeGetOrg -->|payload helper| InvokeEvent[InvokeEventHelper.get_full_payload()]
  GetDealerGrants -->|fetch location| InvokeLocation[InvokeLocation.invoke_get_location_by_id]
  GetDealerGrants -->|returns| Codes["list of location codes"]
  Main -->|when action contains 'delete'| DelGrants[del_grants(grants_codes_and_solution_id)]
  DelGrants -->|uses| PartviewConn[partview_connector]
  PartviewConn -->|establish_connection| DelDB["DB Cursor -> DELETE visibility_grant\n(reference_id filter against package_container)"]
  DelDB -->|returns| DeletedRows["deleted rows"]
  Main -->|when action contains 'add'| NotImpl["NotImplementedError: Add not implemented"]
  subgraph Connectors
    ShipmentConn
    PartviewConn
  end
```

> SVG rendering failed for this diagram.
