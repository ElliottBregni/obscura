# Diagram: partview_core/partview_service/scripts/AddRemoveAssociations.py


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
flowchart TD
  A[Start: __main__] --> B[parse_arguments()]
  B --> C[main()]
  C --> D{Create DB Connector}
  D --> D1[DatabaseConnectorFactory.get_connector(AWS_STAGE, us-east-1, shipments, remove_bac_mapping)]
  D1 --> E[connector.get_primary().establish_connection().get_cursor() --> cursor]
  C --> F[read_csv(args.file_path)]
  F --> G[Validate headers: fv_id & Correct BAC]
  G --> H[Return csv_data (list of dicts)]
  C --> I{args.action contains "add" or "remove"}
  I -->|contains "add"| J[add_bac(csv_data, cursor)]
  I -->|contains "remove"| K[remove_bac(csv_data, cursor)]
  J --> L[For each row in csv_data]
  L --> M{bac_code in ("Not Found in files","Discrepancy")}
  M -->|yes| N[continue to next row]
  M -->|no| O[Build INSERT query using fv_id and bac_code]
  O --> P[cursor.mogrify(query)]
  P --> Q[cursor.execute(query)]
  Q --> R[result = cursor.fetchall()]
  R --> S[logging.info Inserted rows]
  K --> T[For each row in csv_data]
  T --> U{bac_code in ("Not Found in files","Discrepancy")}
  U -->|yes| V[continue to next row]
  U -->|no| W[Build DELETE query using fv_id and bac_code]
  W --> X[cursor.mogrify(query)]
  X --> Y[cursor.execute(query)]
  Y --> Z[result = cursor.fetchall()]
  Z --> AA[logging.info Deleted rows]
  S --> AB[End]
  AA --> AB[End]
```

> SVG rendering failed for this diagram.
