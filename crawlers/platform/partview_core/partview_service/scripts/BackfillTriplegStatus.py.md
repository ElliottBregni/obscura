# Diagram: partview_core/partview_service/scripts/BackfillTriplegStatus.py


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
flowchart LR
  A[Script Start (__main__)] --> B[parse_arguments()]
  B -->|returns args| C[DatabaseConnectorFactory.get_connector(stage, region, db, job)]
  C --> D[connector.get_primary()]
  D --> E[establish_connection()]
  E --> F[get_cursor()]
  F --> G[cursor.mogrify(query, {solution_id: tuple(SOLUTION_ID)})]
  G --> H[cursor.execute(sql)]
  subgraph Constants & Imports
    I[SOLUTION_ID = ["FV_PARTVIEW","GM_PART"]]
    J[import argparse]
    K[DatabaseConnectorFactory import]
  end
  A --> I
  A --> J
  A --> K
  style Constants & Imports fill:#f9f,stroke:#333,stroke-width:1px
```

> SVG rendering failed for this diagram.
