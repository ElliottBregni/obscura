# Diagram: partview_core/partview_service/serverless.partview_visibility.yml


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
flowchart TD
  Service[partview-visibility]
  Provider[AWS Provider<br/>runtime: python3.13<br/>arch: arm64<br/>timeout: 300<br/>memory: ${self:custom.globalConfig.memorySize}]
  Service --> Provider

  Provider --> IAM[IAM Role<br/>managedPolicies + statements]
  Provider --> VPC[VPC: ${self:custom.globalConfig.vpc}]
  Provider --> Env[Environment Variables]
  Provider --> Layers[Layers: ${self:custom.globalConfig.fv_core_layers}]
  Provider --> Resources[Resources: ${self:custom.globalConfig.resources}]
  Provider --> S3["S3: arn:aws:s3:::${opt:stage}-fv-csv-export/*"]
  Provider --> SNS["SNS: CreateTopic / Publish"]
  Provider --> SQS_Perms["SQS: Send/Receive/Delete/GetQueueUrl/GetQueueAttributes"]
  Provider --> LogsDeny["Logs: Deny Create/Put on /aws/lambda/*"]

  subgraph Functions [Lambda Functions]
    T1["trigger-post-visibility-grant-handler<br/>POST /api/visibility-grants/data"]
    V1["visibility-grant-handler<br/>GET /api/visibility-grant<br/>GET/PUT/PATCH/DELETE /api/visibility-grant/{id}<br/>POST /api/visibility-grant"]
    P1["partview-dealer-onboarding-handler<br/>POST /api/partview-dealer-onboarding-upload"]
    D1["dealer_onboard_batch_upload_receiver<br/>SQS: ${opt:stage}-PV-dealer-onboard.fifo"]
  end

  Service --> Functions
  T1 -->|HTTP (authorizer: custom)| Provider
  V1 -->|HTTP (authorizer: custom)| Provider
  P1 -->|HTTP (authorizer: custom)| Provider
  D1 -->|SQS event| Provider

  D1 --> SQS["SQS Queue: ${opt:stage}-PV-dealer-onboard.fifo"]
  SQS -->|maxConcurrency:10 batchSize:10| D1

  classDef infra fill:#f9f,stroke:#333,stroke-width:1px;
  class Provider,IAM,VPC,Env,Layers,Resources,S3,SNS,SQS_Perms,LogsDeny infra;
```

> SVG rendering failed for this diagram.
