# Diagram: sso/auth0/actions/test.mercedesBenzOidc.js


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
flowchart LR
  A[onExecutePostLogin(event, api)] --> B{connection == 'mercedesBenzOidcTest' && strategy == 'oidc'}
  B -- no --> X[exit (no action)]
  B -- yes --> C[extract user, connection, strategy, userMeta]
  C --> D[build tokenOptions (client_credentials POST)]
  D --> E[request token via axios]
  E --> F[get access_token]
  F --> G[derive userId from user.user_id]
  G --> H[build usersOptions (GET userId with Bearer token)]
  H --> I[request user data via axios]
  I --> J[extract applicationUser, userInformation, userAuthorization, dealerCode]
  J --> K[determine netstarEntitlements & isNetstarLink]
  K --> L{userInformation.userType in enterpriseTypes/fieldUserTypes/dealerTypes}
  L -- enterprise & isNetstarLink --> M[privileges = enterprise admin set; userType='enterprise']
  L -- enterprise/fieldUser --> N[privileges = enterprise limited set; userType='enterprise']
  L -- dealer --> O[set userType='dealer', federatedOrgId, orgType='DL', privileges=['VIEW_ENTITY']]
  L -- none --> P[api.access.deny("Incorrect Entitlements configuration provided in federation.")]
  M --> Q[set idToken and accessToken custom claim with user_authorization payload]
  N --> Q
  O --> Q
  P --> R[stop further claim setting]
  Q --> S[complete post-login successfully]
  subgraph ErrorHandling
    T[exception caught] --> U[log failure and exception]
  end
  style ErrorHandling fill:#fdd,stroke:#900,stroke-width:1px
```

> SVG rendering failed for this diagram.
