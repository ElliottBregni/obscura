# Diagram: sso/auth0/actions/prod.mercedesBenzOidc.js


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
flowchart LR
  Start([Start]) --> A[onExecutePostLogin(event, api)]
  A --> B{connection == "mercedesBenzOidc" && strategy == "oidc"}
  B -- no --> End([End])
  B -- yes --> C[Extract user, email, country, phoneNumber]
  C --> D[Request token\nPOST https://sso.mercedes-benz.com/as/token.oauth2]
  D --> E[Receive access_token]
  E --> F[Derive userId from user.user_id]
  F --> G[Request user data\nGET /users/{userId}]
  G --> H[Receive userData → applicationUser]
  H --> I[Extract userInformation, userAuthorization, dealerCode]
  I --> J[Compute netstarEntitlements and isNetstarLink]
  J --> K{userInformation.userType type checks}
  K -->|enterpriseTypes ∧ isNetstarLink| L1[Set privileges: MANAGE_SHIPPER_LOCATIONS,...\nuserType = enterprise]
  K -->|fieldUserTypes ∨ enterpriseTypes| L2[Set privileges: VIEW_LOCATIONS, VIEW_SHIPMENTS,...\nuserType = enterprise]
  K -->|dealerTypes| L3[Set privileges: VIEW_ENTITY\nuserType = dealer\nfederatedOrgId = MERCEDESBENZ|dealerCode\norgType = DL]
  K -->|none match| M[api.access.deny("Incorrect Entitlements configuration provided in federation.")]
  L1 --> N[Set idToken custom claim user_authorization]
  L2 --> N
  L3 --> N
  N --> O[Set accessToken custom claim user_authorization]
  M --> End
  O --> End
  End --> Z([Finish])
```

> SVG rendering failed for this diagram.
