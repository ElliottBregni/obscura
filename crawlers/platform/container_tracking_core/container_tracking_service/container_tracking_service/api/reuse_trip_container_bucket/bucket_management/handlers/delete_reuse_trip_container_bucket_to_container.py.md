# Diagram: container_tracking_core/container_tracking_service/container_tracking_service/api/reuse_trip_container_bucket/bucket_management/handlers/delete_reuse_trip_container_bucket_to_container.py


> Auto-generated by Obscura crawlers

## Diagram 1

```mermaid
classDiagram
class DeleteReuseTripContainerBucket {
  -__bucket_name
  -__container_id
  -__solution_id
  -__type
  -__container_identifier
  -__bucket_location_code
  -__results
  -__application_name
  -__partview_cursor
  +DeleteReuseTripContainerBucket(event)
  +parse_request()
  +validate_parameters()
  +process()
  +format_result()
  +get_matching_bucket(bucket_name)
  +get_containers_by_type_or_id()
  +remove_locations_for_bucket()
}
class ContainerTrackingRequestHandler
class DatabaseConnectorFactory
class RemoteMethodInvoke
class get_event_body
class BadRequestError
DeleteReuseTripContainerBucket --|> ContainerTrackingRequestHandler
DeleteReuseTripContainerBucket --> DatabaseConnectorFactory : uses
DeleteReuseTripContainerBucket ..> RemoteMethodInvoke : invokes
DeleteReuseTripContainerBucket ..> get_event_body : reads request body
DeleteReuseTripContainerBucket ..> BadRequestError : raises on invalid params
```

> SVG rendering failed for this diagram.

## Diagram 2

```mermaid
sequenceDiagram
participant Client
participant Handler as DeleteReuseTripContainerBucket
participant Remote as RemoteService
participant Partview as PartviewCursor
Client->>Handler: create(event)
Handler->>Handler: parse_request()
Handler->>Handler: validate_parameters()
alt bucket_location_code present
    Handler->>Remote: DELETE /location-bucket (reuse-trip-container-bucket-location-handler)
    Remote-->>Handler: status, response
end
alt container_id/type/container_identifier present
    alt no container_id provided
        Handler->>Remote: GET /reuse-trip-container-search/container (reuse-trip-container-search)
        Remote-->>Handler: status, response (list of ids)
    end
    Handler->>Partview: DELETE FROM container_to_buckets WHERE container_id IN (...) AND solution_id = ...
    Partview-->>Handler: deleted rows
end
Handler->>Handler: format_result()
Handler-->>Client: formatted response (solution_id, bucket_name?, container_id?)
```

> SVG rendering failed for this diagram.
