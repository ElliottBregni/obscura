# Diagram: container_tracking_core/container_tracking_service/container_tracking_service/api/advanced_search_filters_dynamic/supplier_code/supplier_code_handler.py


> Auto-generated by Obscura crawlers

## Diagram 1

```mermaid
classDiagram
    class ContainerTrackingRequestHandler
    class GetSupplierCodeRequestHandler {
        - __path_parameter: str
        - __supplier_code_lst: SupplierCodeList[]
        - __search_query: str|None
        - __solution_id: str
        - __page_size: int
        - __page_number: int
        - __application_name: str
        - __data_store: SupplierCodeListMapping
        + __init__(event)
        + parse_request()
        + validate_parameters()
        + process()
        + format_result()
    }
    class SupplierCodeListMapping {
        + search(query, params, model) SupplierCodeList[]
    }
    class SupplierCodeList {
        + supplier_code
        + full_count
    }
    class mandatory_lambda_handling
    class get_query_parameter
    class make_response
    ContainerTrackingRequestHandler <|-- GetSupplierCodeRequestHandler
    GetSupplierCodeRequestHandler --> SupplierCodeListMapping : instantiates/uses
    GetSupplierCodeRequestHandler --> SupplierCodeList : populates/returns
    GetSupplierCodeRequestHandler ..> get_query_parameter : calls
    GetSupplierCodeRequestHandler ..> make_response : returns final data
    lambda_handler ..> GetSupplierCodeRequestHandler : creates
    lambda_handler ..> mandatory_lambda_handling : decorated_by
```

> SVG rendering failed for this diagram.

## Diagram 2

```mermaid
flowchart TD
    Event["Lambda Event (event, context, audit_refs)"] --> Init[Create GetSupplierCodeRequestHandler(event)]
    Init --> Parse["parse_request() -> read query params\nfilter_name, query, pageSize, pageNumber"]
    Parse --> Validate["validate_parameters() -> type checks\nstring/int validations"]
    Validate --> Process["process() -> build SQL query\ncalls SupplierCodeListMapping.search()"]
    Process --> Format["format_result() -> compute meta\nbuild results list"]
    Format --> MakeResponse["make_response(data, HTTPStatus.OK)"]
    MakeResponse --> Return["Return HTTP response"]
    Event -.-> Decorator["mandatory_lambda_handling(auth_check) (decorator)"] --> Init
```

> SVG rendering failed for this diagram.
