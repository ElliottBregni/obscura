# Diagram: container_tracking_core/container_tracking_service/container_tracking_service/api/position_update/position_update_handler.py


> Auto-generated by Obscura crawlers

## Diagram 1

```mermaid
classDiagram
class ContainerTrackingRequestHandler {
  +event
  +AUTH_CHECK
  +__init__(event, name)
}
class ReuseTripContainerPositionUpdateRequestHandler {
  -METHODS
  -__container_identifier
  -__position_update
  -__position_updates
  -__http_code
  -__http_method
  -__body
  -__application_name
  -__data_store
  -__search_data_store
  -__business_logic
  -__table_name
  +parse_request()
  +validate_parameters()
  +validate_required_fields(part)
  +process()
  +format_result()
  +position_update_from_body()
}
class ReuseTripContainerPositionUpdatePostgresqlMapping {
  +get_table()
  +write()
  +read()
}
class ReuseTripContainerPositionUpdateSearchPostgresqlMapping {
  +read()
}
class ReuseTripContainerPositionUpdate {
  +set(field,value)
  +to_dict()
  +id
  +latitude
  +longitude
  +position_update_ts
}
class ReuseTripContainerPositionUpdateBL {
  +get_city_state_country_by_lat_long(lat,long)
}
ContainerTrackingRequestHandler <|-- ReuseTripContainerPositionUpdateRequestHandler
ReuseTripContainerPositionUpdateRequestHandler --> ReuseTripContainerPositionUpdatePostgresqlMapping : uses
ReuseTripContainerPositionUpdateRequestHandler --> ReuseTripContainerPositionUpdateSearchPostgresqlMapping : uses
ReuseTripContainerPositionUpdateRequestHandler --> ReuseTripContainerPositionUpdate : constructs
ReuseTripContainerPositionUpdateRequestHandler --> ReuseTripContainerPositionUpdateBL : uses
```

> SVG rendering failed for this diagram.

## Diagram 2

```mermaid
sequenceDiagram
participant Client
participant Lambda as lambda_handler
participant Auth as auth
participant Handler as ReuseTripContainerPositionUpdateRequestHandler
participant SearchStore as ReuseTripContainerPositionUpdateSearchPostgresqlMapping
participant DataStore as ReuseTripContainerPositionUpdatePostgresqlMapping
participant BL as ReuseTripContainerPositionUpdateBL
participant HTTP as make_response

Client->>Lambda: invoke(event, context)
Lambda->>Auth: get_organization_id(event)
Auth-->>Lambda: org_id
Lambda->>Handler: instantiate(event)
Handler-->>Handler: parse_request()
Handler-->>Handler: validate_parameters()
Handler-->>Handler: process()
alt GET
  Handler->>SearchStore: read(query by container id)
  SearchStore-->>Handler: list of ids
  loop for each id
    Handler->>DataStore: read(ReuseTripContainerPositionUpdate(id))
    DataStore-->>Handler: position_update
  end
else POST
  Handler->>Handler: position_update_from_body()
  Handler->>BL: get_city_state_country_by_lat_long(lat, long)
  BL-->>Handler: city,state,country
  Handler->>DataStore: write(position_update)
  DataStore-->>Handler: created_position_update
end
Handler-->>Handler: format_result()
Handler-->>Lambda: position_updates_data, http_code
Lambda->>HTTP: make_response(position_updates_data, http_code)
HTTP-->>Lambda: response
Lambda-->>Client: response
```

> SVG rendering failed for this diagram.
