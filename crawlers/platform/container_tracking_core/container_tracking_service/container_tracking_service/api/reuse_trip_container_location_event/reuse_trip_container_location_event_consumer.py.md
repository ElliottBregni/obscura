# Diagram: container_tracking_core/container_tracking_service/container_tracking_service/api/reuse_trip_container_location_event/reuse_trip_container_location_event_consumer.py


> Auto-generated by Obscura crawlers

## Diagram 1

```mermaid
classDiagram
class AppContainer {
  +config
  +reuse_trip_container_helper()
  +reuse_trip_container_location_event_mapping()
}
class ReuseTripContainerLocationEventConsumer {
  -reuse_trip_container_helper: ReuseTripContainerHelper
  -reuse_trip_container_location_event_mapping: ReuseTripContainerLocationEventApiMapping
  -reuse_trip_container_location_event_logician: ReuseTripContainerLocationEventLogician
  +read(messages)
  -__process_message(message)
  -__process_container_location_event_message(body)
}
class ReuseTripContainerHelper
class ReuseTripContainerLocationEventApiMapping
class ReuseTripContainerLocationEventLogician {
  +create_reuse_trip_container_location_event(target_feature, event)
}
class ReuseTripContainer
class ReuseTripContainerEvent
class ReuseTripContainerLocationEventMsg
class MapAction
class BadRequestError
class ProcessingError

AppContainer --> ReuseTripContainerHelper : provides
AppContainer --> ReuseTripContainerLocationEventApiMapping : provides
ReuseTripContainerLocationEventConsumer --> ReuseTripContainerHelper : uses
ReuseTripContainerLocationEventConsumer --> ReuseTripContainerLocationEventApiMapping : uses
ReuseTripContainerLocationEventConsumer --> ReuseTripContainerLocationEventLogician : creates
ReuseTripContainerHelper --> ReuseTripContainer : searches
MapAction --> ReuseTripContainerEvent : maps_to
ReuseTripContainerLocationEventMsg --> ReuseTripContainerLocationEventConsumer : deserializes
```

> SVG rendering failed for this diagram.

## Diagram 2

```mermaid
sequenceDiagram
participant Lambda as lambda_handler
participant Container as AppContainer
participant Consumer as ReuseTripContainerLocationEventConsumer
participant Helper as ReuseTripContainerHelper
participant Mapping as ReuseTripContainerLocationEventApiMapping
participant Map as MapAction
participant Logician as ReuseTripContainerLocationEventLogician
participant MakeResp as make_response

Lambda->>Container: instantiate AppContainer()
Container-->>Lambda: provide helper() and mapping()
Lambda->>Consumer: instantiate ReuseTripContainerLocationEventConsumer(helper, mapping)
Lambda->>Consumer: read(records)
alt records exist
  loop for each message
    Consumer->>Consumer: __process_message(message)
    Consumer->>Consumer: parse message body (json)
    alt body present
      Consumer->>Helper: search_reuse_trip_containers(tag_id, solution_id)
      Helper-->>Consumer: reuse_trip_container (or null)
      alt not found
        Consumer-->>Lambda: raise BadRequestError
      else found
        Consumer->>Map: map_request_to_persistable(mapping, data, ReuseTripContainerEvent, "POST")
        Map-->>Consumer: reuse_trip_container_event
        Consumer->>Logician: instantiate Logician(solution_id, org_id, org_fv_id, APPLICATION_NAME)
        Consumer->>Logician: create_reuse_trip_container_location_event(target_feature, reuse_trip_container_event)
        Logician-->>Consumer: (status_code, response_data)
        alt status_code != 200
          Consumer-->>Lambda: raise ProcessingError
        end
      end
    else no body
      Consumer-->>Lambda: raise BadRequestError
    end
  end
end
Lambda->>MakeResp: make_response({}, 200)
MakeResp-->>Lambda: HTTP 200 response
```

> SVG rendering failed for this diagram.
