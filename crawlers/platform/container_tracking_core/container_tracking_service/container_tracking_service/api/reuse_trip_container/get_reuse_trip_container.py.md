# Diagram: container_tracking_core/container_tracking_service/container_tracking_service/api/reuse_trip_container/get_reuse_trip_container.py


> Auto-generated by Obscura crawlers

## Diagram 1

```mermaid
classDiagram
    class ContainerTrackingRequestHandler {
        +AUTH_CHECK
        +__init__(event, service_name)
        +get_path_parameter(param)
    }
    class GetReuseTripContainerRequestHandler {
        -__reuse_trip_container_identifier
        -__get_reuse_trip_container
        -__application_name
        -__data_store
        +__init__(event)
        +parse_request()
        +validate_parameters()
        +process()
        +format_None_result()
        +format_result()
        +set_reuse_trip_container(reusable_trip_container)
    }
    class ReuseTripContainerPostgresqlMapping {
        +__init__(application_name)
        +read(reuseTripContainer)
    }
    class ReuseTripContainer {
        +id
        +ts
        +lifecycle_state
        +tag_identifier
        +container_identifier
        +solution_id
        +get(key)
    }
    ContainerTrackingRequestHandler <|-- GetReuseTripContainerRequestHandler
    GetReuseTripContainerRequestHandler o-- ReuseTripContainerPostgresqlMapping : uses
    GetReuseTripContainerRequestHandler --> ReuseTripContainer : reads/constructs
```

> SVG rendering failed for this diagram.

## Diagram 2

```mermaid
flowchart TD
    L[lambda_handler(event, context, audit_refs)]
    A[auth.get_organization_id(event)]
    R[GetReuseTripContainerRequestHandler(event)]
    P[parse_request()]
    V[validate_parameters()]
    X[process()]
    C{reuse_trip_container_data != None}
    S_ok[make_response(format_result(), 200)]
    S_notfound[make_response(format_None_result(), 404)]

    L --> A
    A --> R
    R --> P
    P --> V
    V --> X
    X --> C
    C -- "yes" --> S_ok
    C -- "no" --> S_notfound
```

> SVG rendering failed for this diagram.
