# Diagram: container_tracking_core/container_tracking_service/container_tracking_service/api/lifecycle/lifecycle_state_handler.py


> Auto-generated by Obscura crawlers

## Diagram 1

```mermaid
classDiagram
class ContainerTrackingRequestHandler
class ReuseTripContainerLifeCycleStateRequestHandler {
  - __reuse_trip_container_identifier: str
  - __reuse_trip_container_lifecycle
  - __reuse_trip_container
  - __old_state
  - __application_name: str
  - __data_store
  - __rtc_data_store
  + parse_request()
  + validate_parameters()
  + process()
  + format_result()
}
ContainerTrackingRequestHandler <|-- ReuseTripContainerLifeCycleStateRequestHandler
class ReuseTripContainerLifecycleStatePostgresqlMapping
class ReuseTripContainerPostgresqlMapping
class ReuseTripContainer
class ReuseTripContainerLifeCycleState
ReuseTripContainerPostgresqlMapping --> ReuseTripContainer : reads/writes
ReuseTripContainerLifecycleStatePostgresqlMapping --> ReuseTripContainerLifeCycleState : reads/writes
ReuseTripContainerLifeCycleStateRequestHandler --> ReuseTripContainerLifecycleStatePostgresqlMapping : uses
ReuseTripContainerLifeCycleStateRequestHandler --> ReuseTripContainerPostgresqlMapping : uses
ReuseTripContainerLifeCycleStateRequestHandler ..> invoke_lambda : calls
ReuseTripContainerLifeCycleStateRequestHandler ..> get_path_parameter : uses
ReuseTripContainerLifeCycleStateRequestHandler ..> make_response : uses
ReuseTripContainerLifeCycleStateRequestHandler ..> mandatory_lambda_handling : decorated_by
ReuseTripContainerLifeCycleStateRequestHandler ..> BadRequestError : may_raise
ReuseTripContainerLifeCycleStateRequestHandler ..> auth : reads_organization
class invoke_lambda
class get_path_parameter
class make_response
class mandatory_lambda_handling
class auth
class BadRequestError
```

> SVG rendering failed for this diagram.

## Diagram 2

```mermaid
sequenceDiagram
participant Lambda as "lambda_handler"
participant Auth as "auth"
participant Handler as "ReuseTripHandler"
participant RTC as "ReuseTripContainerStore"
participant LC as "LifecycleStateStore"
participant Invoke as "invoke_lambda"
participant Response as "make_response"

Lambda->>Auth: get_organization_id(event)
Lambda->>Handler: instantiate with event
Handler->>RTC: read(ReuseTripContainer(id))
Handler->>Handler: parse_request()
Handler->>Handler: validate_parameters()
alt HTTP GET
  Handler->>LC: read(ReuseTripContainerLifeCycleState(id))
  Handler-->>Lambda: format_result() [GET payload]
else HTTP POST
  Handler->>Handler: process() read body -> newState
  alt newState == old_state
    Handler-->>Lambda: no-op (returns)
  else
    Handler->>LC: write(ReuseTripContainerLifeCycleState(...))
    Handler->>Invoke: invoke_lambda("update-reuse-trip-container", full_payload)
    Handler-->>Lambda: format_result() [POST payload]
  end
end
Lambda->>Response: make_response(result, 200)
Lambda-->>Auth: return organization id
```

> SVG rendering failed for this diagram.
