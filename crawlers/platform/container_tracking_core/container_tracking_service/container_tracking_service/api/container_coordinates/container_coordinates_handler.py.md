# Diagram: container_tracking_core/container_tracking_service/container_tracking_service/api/container_coordinates/container_coordinates_handler.py


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
flowchart LR
  Decorator([mandatory_lambda_handling decorator]) --> Lambda([lambda_handler(event, context, audit_refs)])
  Lambda --> HandlerInst[Instantiate ReuseTripContainerPositionUpdateRequestHandler(event)]
  HandlerInst --> Parse[parse_request()\n-> get_path_parameter(event, "containerId", True)]
  Parse --> Validate{validate_parameters()\ncontainer id is str?}
  Validate -- "yes" --> Process[process()\n- compute today, date_bound\n- build SQL query]
  Validate -- "no" --> BadReq[BadRequestError thrown]
  Process --> Query[SQL query:\nselect ... from reuse_trip_container_event\nwhere container_id = %(container_identifier)s\nAND event_ts > %(date_bound)s\norder by event_ts DESC]
  Process --> DataStore[PositionUpdateMapping.search(query, params, PositionUpdate())]
  DataStore --> Updates[List<PositionUpdate>]
  Updates --> Format[format_result()\n-> [update.to_dict() for update in list]]
  Format --> Response[make_response(reuse_trip_container_data, HTTPStatus.OK)]
  Response --> Return([return HTTP 200 response])
```

> SVG rendering failed for this diagram.
