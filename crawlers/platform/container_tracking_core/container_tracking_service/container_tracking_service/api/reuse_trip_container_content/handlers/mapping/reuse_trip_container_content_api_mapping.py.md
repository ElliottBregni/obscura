# Diagram: container_tracking_core/container_tracking_service/container_tracking_service/api/reuse_trip_container_content/handlers/mapping/reuse_trip_container_content_api_mapping.py


> Auto-generated by Obscura crawlers

## Diagram 1

```mermaid
classDiagram
    class ApiMapping {
        <<abstract>>
        +set_api_name(name)
        +set_internal_fields(fields)
        +set_timestamp_fields(fields)
        +set_bidirectional_map(map)
    }
    class BidirectionalMap {
        +map_from_dict(dict)
    }
    class ReuseTripContainerContentApiMapping {
        +__init__()
        +set_api_mapping()
        -api_name: "reuse trip container content"
        -internal_fields: ["solution_id"]
        -timestamp_fields: ["ts","modified"]
        -bidirectional_map: {"value":"value","status":"status"}
    }
    ApiMapping <|-- ReuseTripContainerContentApiMapping
    ReuseTripContainerContentApiMapping ..> BidirectionalMap : uses/creates
```

> SVG rendering failed for this diagram.

## Diagram 2

```mermaid
flowchart TD
    A[Instantiate ReuseTripContainerContentApiMapping] --> B[call set_api_mapping()]
    B --> C[set_api_name("reuse trip container content")]
    C --> D[set_internal_fields(["solution_id"])]
    D --> E[set_timestamp_fields(["ts","modified"])]
    E --> F[create BidirectionalMap and call map_from_dict({"value":"value","status":"status"})]
    F --> G[set_bidirectional_map(map)]
    G --> H[return self]
```

> SVG rendering failed for this diagram.
