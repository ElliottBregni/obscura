# Diagram: container_tracking_core/container_tracking_service/container_tracking_service/api/SurgicalToteTrackingRequestHandler.py


> Auto-generated by Obscura crawlers

## Diagram 1

```mermaid
classDiagram
    class SurgicalToteTrackingRequestHandler {
        +AUTH_CHECK
        +__init__(event, application_name)
        +_post_init() SurgicalToteTrackingRequestHandler
    }
    class FvRequestHandler {
        +parse_and_validate_feature_header(event, allowed_features, default_feature)
    }
    class Auth {
        <<module>>
        +AuthType
        +Feature
    }
    class BadRequestError

    SurgicalToteTrackingRequestHandler --|> FvRequestHandler
    SurgicalToteTrackingRequestHandler ..> Auth : references
    SurgicalToteTrackingRequestHandler ..> BadRequestError : raises
```

> SVG rendering failed for this diagram.

## Diagram 2

```mermaid
sequenceDiagram
    participant Event as event
    participant Handler as SurgicalToteTrackingRequestHandler
    participant Fv as FvRequestHandler
    participant AuthUtil as Auth
    participant Error as BadRequestError

    Event->>Handler: call __init__(event, application_name)
    Handler->>Fv: parse_and_validate_feature_header(event, [CONTAINER_TRACKING, SURGICAL_TOTE_TRACKING], SURGICAL_TOTE_TRACKING)
    alt feature_name not found
        Handler->>Error: raise BadRequestError("Invalid Container Tracking request, feature name not found")
    else feature_name found
        Handler->>Fv: super().__init__(event, feature_name, application_name)
    end
    Handler->>Handler: returns instance
    Handler->>Handler: _post_init() calls fetch_solution() and returns self
```

> SVG rendering failed for this diagram.
