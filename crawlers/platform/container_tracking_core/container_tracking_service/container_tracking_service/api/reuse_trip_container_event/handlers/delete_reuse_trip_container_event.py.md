# Diagram: container_tracking_core/container_tracking_service/container_tracking_service/api/reuse_trip_container_event/handlers/delete_reuse_trip_container_event.py


> Auto-generated by Obscura crawlers

## Diagram 1

```mermaid
classDiagram
class DeleteReuseTripContainerEvent {
  - __container_id
  - __event_id
  - __body
  - __http_method
  - __reuse_trip_container_event
  - __application_name
  - __data_store
  + parse_request()
  + validate_parameters()
  + process()
  + format_result()
  + reuse_trip_container_event_from_body()
  - __populate_event_from_request()
  - __send_location_filter_update()
  + get_container_location()
  + get_container_event()
}
class ContainerTrackingRequestHandler
class ReuseTripContainerEventPostgresqlMapping {
  + hard_delete(event)
}
class ReuseTripContainerEvent {
  + set(field, value)
  + get(field)
}
class ReuseTripContainerType
class RemoteMethodInvoke {
  + get_location_by_code(event, org_id, code)
  + update_location_filter_mapping(event)
}
class Datetime {
  + get_datetime()
}
class MapAction {
  + map_from_dictionary(mapping, target, source, timestamp_fields, timerange_fields)
}
class AWSLambdaHelpers {
  + get_event_body(event)
  + get_http_method(event)
  + get_path_parameter(event, name, required)
  + make_response(body, status)
}
class BadRequestError
class ConflictError
class UnhandledException
class psycopg2_Error

DeleteReuseTripContainerEvent --|> ContainerTrackingRequestHandler
DeleteReuseTripContainerEvent --> ReuseTripContainerEventPostgresqlMapping : uses
DeleteReuseTripContainerEvent --> RemoteMethodInvoke : calls
DeleteReuseTripContainerEvent --> ReuseTripContainerEvent : constructs/sets
DeleteReuseTripContainerEvent --> Datetime : converts timestamps
DeleteReuseTripContainerEvent --> MapAction : maps fields
DeleteReuseTripContainerEvent --> AWSLambdaHelpers : reads request/makes response
ReuseTripContainerEventPostgresqlMapping ..> psycopg2_Error : may raise
psycopg2_Error ..> ConflictError
psycopg2_Error ..> UnhandledException
```

> SVG rendering failed for this diagram.

## Diagram 2

```mermaid
sequenceDiagram
participant Client
participant Handler as DeleteReuseTripContainerEvent
participant AWS as AWSLambdaHelpers
participant Mapper as MapAction
participant Remote as RemoteMethodInvoke
participant DB as ReuseTripContainerEventPostgresqlMapping
participant Event as ReuseTripContainerEvent
participant Time as Datetime
Client->>Handler: HTTP invoke with event
Handler->>AWS: get_path_parameter(event, "id", true) / get_path_parameter(event, "eventId", false)
AWS-->>Handler: container_id, event_id
Handler->>AWS: get_event_body(event)
AWS-->>Handler: body
Handler->>Handler: parse_request() / validate_parameters()
Handler->>Mapper: map_from_dictionary(reusableContainerEventMapping,...)
Mapper-->>Handler: mapped ReuseTripContainerType
Handler->>Remote: get_location_by_code(event, org_id, location_code)
Remote-->>Handler: location
Handler->>Event: ReuseTripContainerEvent().set("container_id", id).set("id", event_id)
Event-->>Handler: reuse_trip_container_event
Handler->>DB: hard_delete(reuse_trip_container_event)
alt delete successful
  DB-->>Handler: deleted_event
  Handler->>AWS: make_response("Event deleted successfully", 200)
  AWS-->>Client: HTTP 200
else database error (psycopg2.Error)
  DB-->>Handler: raises psycopg2.Error
  Handler->>Handler: inspect pgcode -> raise ConflictError / UnhandledException
  Handler-->>Client: Error response (Conflict/Unhandled)
end
```

> SVG rendering failed for this diagram.
