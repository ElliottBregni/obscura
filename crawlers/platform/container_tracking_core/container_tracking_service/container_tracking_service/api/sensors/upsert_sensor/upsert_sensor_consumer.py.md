# Diagram: container_tracking_core/container_tracking_service/container_tracking_service/api/sensors/upsert_sensor/upsert_sensor_consumer.py


> Auto-generated by Obscura crawlers

## Diagram 1

```mermaid
classDiagram
class UpsertSensorConsumerHandler {
  +__application_name
  +__payload
  +__sensor_data_store
  +__sensor_update_data_store
  +__solution_id
  +__organization_id
  +__data
  +device_id
  +device_name
  +__sensor_id
  +parse_request()
  +validate_parameters()
  +process()
  +format_result()
}
class ContainerTrackingRequestHandler
class Sensor
class SensorUpdate
class SensorPostgresqlMapping
class SensorUpdatePostgresqlMapping
class Datetime
class BadRequestError
class ConflictError
class psycopg2

UpsertSensorConsumerHandler --|> ContainerTrackingRequestHandler
UpsertSensorConsumerHandler ..> Sensor : composes/uses
UpsertSensorConsumerHandler ..> SensorUpdate : composes/uses
UpsertSensorConsumerHandler ..> SensorPostgresqlMapping : uses
UpsertSensorConsumerHandler ..> SensorUpdatePostgresqlMapping : uses
UpsertSensorConsumerHandler ..> Datetime : uses
UpsertSensorConsumerHandler ..> BadRequestError : raises
UpsertSensorConsumerHandler ..> ConflictError : raises
UpsertSensorConsumerHandler ..> psycopg2 : handles exceptions
```

> SVG rendering failed for this diagram.

## Diagram 2

```mermaid
sequenceDiagram
participant Lambda as lambda_handler
participant RecordsParser as "Records JSON"
participant Handler as UpsertSensorConsumerHandler
participant Datetime as Datetime
participant SensorStore as SensorPostgresqlMapping
participant SensorUpdateStore as SensorUpdatePostgresqlMapping
participant MakeResponse as make_response
participant DB as psycopg2

Lambda->>RecordsParser: parse event.Records
alt records present
loop each record
  Lambda->>Handler: instantiate(record)
  Handler->>Handler: handle_request()
  Handler->>Handler: parse_request()
  Handler->>Datetime: timestamp_to_datetime(LastUpdateTime/LastReadTime)
  Datetime-->>Handler: datetime objects
  Handler->>Handler: validate_parameters()
  Handler->>SensorStore: search(Sensor(sensor_id))
  alt sensor found
    SensorStore-->>Handler: sensor with id
    Handler->>Handler: set sensor_update.sensor_id and sensor.id
  else sensor not found
    SensorStore-->>Handler: none
    Handler->>SensorStore: write(self.__sensor)
    SensorStore-->>Handler: sensor_details (id)
    Handler->>Handler: set sensor_update.sensor_id
  end
  Handler->>SensorUpdateStore: exists(sensor_update_criteria)
  alt not duplicate
    SensorUpdateStore-->>Handler: false
    Handler->>SensorUpdateStore: write(sensor_update)
    SensorUpdateStore-->>Handler: sensor_update_details (id)
    Handler->>SensorStore: update(sensor.latest_sensor_update)
  else duplicate
    SensorUpdateStore-->>Handler: true
  end
  par database error handling
    DB-->>Handler: psycopg2.Error
    alt UNIQUE_VIOLATION
      Handler->>ConflictError: raise ConflictError(...)
    else other DB error
      Handler->>Handler: propagate error
    end
  end
end
end
Lambda->>MakeResponse: make_response({}, 200)
MakeResponse-->>Lambda: HTTP 200 OK
```

> SVG rendering failed for this diagram.
