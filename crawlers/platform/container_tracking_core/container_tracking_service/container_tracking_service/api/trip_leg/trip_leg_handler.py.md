# Diagram: container_tracking_core/container_tracking_service/container_tracking_service/api/trip_leg/trip_leg_handler.py


> Auto-generated by Obscura crawlers

## Diagram 1

```mermaid
classDiagram
    class TripLegRequestHandlerFactory {
        +handlers
        +TripLegRequestHandlerFactory(event)
    }
    class FVRequestFactory
    TripLegRequestHandlerFactory --|> FVRequestFactory

    class GetTripLegRequestHandler
    class PostTripLegRequestHandler
    class PutTripLegRequestHandler
    class PatchTripLegRequestHandler
    class DeleteTripLegRequestHandler

    TripLegRequestHandlerFactory --> GetTripLegRequestHandler : "GET"
    TripLegRequestHandlerFactory --> PostTripLegRequestHandler : "POST"
    TripLegRequestHandlerFactory --> PutTripLegRequestHandler : "PUT"
    TripLegRequestHandlerFactory --> PatchTripLegRequestHandler : "PATCH"
    TripLegRequestHandlerFactory --> DeleteTripLegRequestHandler : "DELETE"

    class make_response <<function>>
    class mandatory_lambda_handling <<decorator>>
    class lambda_handler <<function>>
    lambda_handler --> mandatory_lambda_handling : decorated by
    lambda_handler --> TripLegRequestHandlerFactory : uses
    lambda_handler --> make_response : returns via
```

> SVG rendering failed for this diagram.

## Diagram 2

```mermaid
sequenceDiagram
    participant Lambda as lambda_handler
    participant Decorator as mandatory_lambda_handling
    participant Factory as TripLegRequestHandlerFactory
    participant Handler as RequestHandler
    participant MakeResp as make_response

    Note over Lambda,Decorator: lambda_handler is wrapped by mandatory_lambda_handling
    Lambda->>Factory: TripLegRequestHandlerFactory(event)
    Factory-->>Lambda: .handler
    Lambda->>Handler: handler.handle_request()
    Handler-->>Lambda: trip_leg_data, http_code
    alt lambda_level conversion if needed
        Lambda->>Lambda: normalize lambda_level (str->int fallback 0)
    end
    Lambda->>MakeResp: make_response(trip_leg_data, http_code)
    MakeResp-->>Lambda: HTTP response
```

> SVG rendering failed for this diagram.
