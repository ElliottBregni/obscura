# Diagram: container_tracking_core/container_tracking_service/container_tracking_service/utility/InvokeLocationGrant.py


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
flowchart TD
    DECACHE[CACHED(TTLCache(maxsize=128, ttl=300), key=hashkey(organization_id))]
    A[InvokeLocationGrant.invoke_get_location_grant(organization_id, organization_fv_id, grantors, request_id)] --> DECACHE
    DECACHE --> B[Build InvokeEventHelper payload: queryStringParameters(granteeId=organization_fv_id, granterIds=grantors), customer_org=organization_id, request_id]
    B --> C[Create InvokeLambda(function_name="get-granted-locations", organization_id, full_payload)]
    C --> D[invoke.invoke_function() -> (status_code, location_list)]
    D -->|exception| E[logging.error(e) & logging.info("Error in get-granted-locations. May not be deployed"); return ()]
    D -->|success| F{location_list.body exists AND status_code in [HTTPStatus.OK]?}
    F -->|no| G[Return None / fallthrough]
    F -->|yes| H[location_list.body = json.loads(location_list.body)]
    H --> I[For each location in location_list.body: grants.append(location[0].code)]
    I --> J{grants non-empty?}
    J -->|yes| K[return tuple(grants)]
    J -->|no| G
```

> SVG rendering failed for this diagram.
