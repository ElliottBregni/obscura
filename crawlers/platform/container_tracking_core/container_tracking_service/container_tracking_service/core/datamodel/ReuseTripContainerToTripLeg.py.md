# Diagram: container_tracking_core/container_tracking_service/container_tracking_service/core/datamodel/ReuseTripContainerToTripLeg.py


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
classDiagram
    class StringEnum
    StringEnum <|-- ReuseTripContainerToTripLegProperties
    class ReuseTripContainerToTripLegProperties {
        <<enumeration>>
        + SOLUTION_ID = "solution_id"
        + CONTAINER_ID = "container_id"
        + TRIP_LEG_ID = "trip_leg_id"
    }
    class Persistable {
        <<abstract>>
        + id
        + ts
        + modified
        + add_dirty_field(field, value)
    }
    Persistable <|-- ReuseTripContainerToTripLeg
    class ReuseTripContainerToTripLeg {
        + table_name = "reuse_trip_container_to_trip_leg"
        - __trip_leg_id: str
        - __container_id: str
        - __solution_id: str
        - __is_valid__: bool
        + solution_id: str
        + trip_leg_id: str
        + container_id: str
        + solution_id(solution_id: str) ReuseTripContainerToTripLegType
        + trip_leg_id(trip_leg_id: str) ReuseTripContainerToTripLegType
        + container_id(container_id: str) ReuseTripContainerToTripLegType
        + is_valid(): bool
        + to_dict(): dict
    }
```

> SVG rendering failed for this diagram.
