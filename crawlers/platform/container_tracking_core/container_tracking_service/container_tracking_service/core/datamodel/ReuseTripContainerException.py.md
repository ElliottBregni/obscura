# Diagram: container_tracking_core/container_tracking_service/container_tracking_service/core/datamodel/ReuseTripContainerException.py


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
classDiagram
    class Persistable {
        <<abstract>>
        +get(id)
        +add_dirty_field(field, value)
    }
    class ReuseTripContainerException {
        -__container_id: str
        -__exception_type_id: str
        -__solution_id: str
        -__location_code: str
        -__latitude: float
        -__longitude: float
        -__comments: str
        -__active_ts: datetime
        -__resolved_ts: datetime
        -__ts: datetime
        -__modified: datetime
        -__reason_code: str
        -__exception_name: str
        -__description: str
        -__fv_id: str
        +ReuseTripContainerException(id, ts, modified)
        +container_id(): str
        +container_id(container_id): ReuseTripContainerException
        +exception_type_id(): str
        +exception_type_id(exception_type_id)
        +solution_id(): str
        +solution_id(solution_id): ReuseTripContainerException
        +location_code(): str
        +location_code(location_code)
        +latitude(): float
        +latitude(latitude)
        +longitude(): float
        +longitude(longitude)
        +comments(): str
        +comments(comments)
        +active_ts(): datetime
        +active_ts(active_ts)
        +resolved_ts(): datetime
        +resolved_ts(resolved_ts)
        +reason_code(): str
        +reason_code(reason_code)
        +exception_name(): str
        +exception_name(exception_name)
        +description(): str
        +description(description)
        +fv_id(): str
        +fv_id(fv_id)
        +is_valid(): bool
        +to_dict(): dict
    }
    Persistable <|-- ReuseTripContainerException
```

> SVG rendering failed for this diagram.
