# Diagram: container_tracking_core/container_tracking_service/container_tracking_service/core/__init__.py


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
classDiagram
    class BackendProtocol <<interface>> {
        +start()
        +stop()
        +send(prompt)
        +stream(prompt)
        +create_session()
        +resume_session()
        +list_sessions()
        +delete_session()
        +register_tool(spec)
        +register_hook(hook, cb)
    }
    class ObscuraClient {
        -_backend_type
        -_backend
        -_tool_registry
        +start()
        +stop()
        +send(prompt)
        +stream(prompt)
        +register_tool(spec)
        +on(hook, cb)
    }
    class CopilotBackend {
        -_auth
        -_client
        -_session
        -_tools
        +start()
        +stop()
        +send(prompt)
        +stream(prompt)
        +create_session()
        +register_tool(spec)
    }
    class ClaudeBackend {
        -_auth
        -_client
        -_last_session_id
        -_tools
        +start()
        +stop()
        +send(prompt)
        +stream(prompt)
        +create_session()
        +register_tool(spec)
    }
    class ToolRegistry {
        -_tools
        +register(spec)
        +get(name)
        +all()
        +names()
    }
    class ToolSpec {
        name
        description
        parameters
        handler()
    }
    class SessionStore {
        +add(ref)
        +remove(id)
        +list_all(backend)
    }
    class ModelConfig {
        alias
        model_id
        category
        max_requests_per_run
    }

    BackendProtocol <|.. CopilotBackend
    BackendProtocol <|.. ClaudeBackend
    ObscuraClient --> BackendProtocol : delegates to
    ObscuraClient --> CopilotBackend : instantiates when COPILOT
    ObscuraClient --> ClaudeBackend : instantiates when CLAUDE
    ObscuraClient --> ToolRegistry : builds/uses
    ToolRegistry o-- ToolSpec
    ToolRegistry --> BackendProtocol : registers tools with
    CopilotBackend --> SessionStore : _session_store
    ClaudeBackend --> SessionStore : _session_store
    ObscuraClient ..> ModelConfig : resolves model alias via copilot_models
```

> SVG rendering failed for this diagram.
