# Diagram: container_tracking_core/container_tracking_service/container_tracking_service/core/helpers/csv_export_helper.py


> Auto-generated by Obscura crawlers

## Diagram 1

```mermaid
classDiagram
    class CsvExportHelper {
        +event
        +target_lambda
        +__init__(event, target_lambda)
        +get_export_data(results, is_async_export)
        +create_csv(result)
        +format_sensor_results(result) <<static>>
    }
    class CSV_LAMBDAS
    class VersionOptions
    class make_response
    class parse_accept_header
    class async_export
    class csv_export
    class BadRequestError

    CsvExportHelper --> CSV_LAMBDAS : checks target
    CsvExportHelper --> VersionOptions : uses
    CsvExportHelper --> parse_accept_header : calls
    CsvExportHelper --> async_export : may call
    CsvExportHelper --> make_response : may call
    CsvExportHelper --> csv_export : calls (create_csv)
    CsvExportHelper --> BadRequestError : raises on invalid format
```

> SVG rendering failed for this diagram.

## Diagram 2

```mermaid
sequenceDiagram
    participant Event
    participant Helper as CsvExportHelper
    participant Parser as parse_accept_header
    participant Async as async_export
    participant Make as make_response
    participant CSV as csv_export
    participant Error as BadRequestError

    Event->>Helper: get_export_data(results, is_async_export)
    Helper->>Parser: parse_accept_header(event, VersionOptions.ContainerTracking)
    Parser-->>Helper: requested_format, version
    alt requested_format contains "text/csv"
        alt is_async_export
            Helper->>Async: async_export(event, target_lambda)
            Async-->>Helper: async_result
            Helper-->>Event: return async_result
        else synchronous export
            Helper->>Helper: create_csv(results)
            Helper->>CSV: (create_csv) csv_export(data, event)
            CSV-->>Helper: csv_response
            Helper->>Make: make_response(csv_response, content_type="text/csv")
            Make-->>Helper: response
            Helper-->>Event: return response
        end
    else invalid or null format
        Helper->>Error: raise BadRequestError(message, message)
    end
```

> SVG rendering failed for this diagram.

## Diagram 3

```mermaid
flowchart TD
    A[create_csv(result)] --> B{target_lambda == CSV_LAMBDAS.SENSOR_SEARCH?}
    B -- yes --> C[format_sensor_results(result)]
    C --> D[csv_export(data, event)]
    B -- no --> E[keep data = None]
    E --> D
    D --> F[return csv_export(...) ]
```

> SVG rendering failed for this diagram.
