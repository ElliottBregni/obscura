# Diagram: container_tracking_core/container_tracking_service/container_tracking_service/persistence_adapter/postgresql/ReuseTripContainerSearchResultPostgresqlMapping.py


> Auto-generated by Obscura crawlers

## Diagram 1

```mermaid
classDiagram
    class SearchPagePostgresqlMapping {
        +add(field, mapping)
        +freeze()
    }
    class ReuseTripContainerSearchResultPostgresqlMapping {
        +__init__(application_name)
        +build_map()
        +get_query(version)
        +__get_basic_query()
        +__get_saved_search_query()
        +__get_minify_query()
    }
    class VersionOptions {
        <<enum>>
        +ContainerTracking.BASIC
        +ContainerTracking.SAVED_SEARCH
        +ContainerTracking.MINIFY
    }
    SearchPagePostgresqlMapping <|-- ReuseTripContainerSearchResultPostgresqlMapping
    ReuseTripContainerSearchResultPostgresqlMapping ..> VersionOptions : references
```

> SVG rendering failed for this diagram.

## Diagram 2

```mermaid
flowchart TD
    Start(("get_query(version)"))
    Start --> Check{version == VersionOptions.ContainerTracking.BASIC?}
    Check -- Yes --> Basic["return __get_basic_query()"]
    Check -- No --> Check2{version == VersionOptions.ContainerTracking.SAVED_SEARCH?}
    Check2 -- Yes --> Saved["return __get_saved_search_query()"]
    Check2 -- No --> Check3{version == VersionOptions.ContainerTracking.MINIFY?}
    Check3 -- Yes --> Minify["return __get_minify_query()"]
    Check3 -- No --> None["return None"]
    Basic --> End((End))
    Saved --> End
    Minify --> End
    None --> End
```

> SVG rendering failed for this diagram.
