# Diagram: container_tracking_core/container_tracking_service/container_tracking_service/persistence_adapter/postgresql/SearchDataPostgresqlMapper.py


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
classDiagram
class SearchDataPostgresqlMapper {
  - Connector __primary_database_connector
  - CrudOperations __crudOperations
  - SearchOperations _searchOperations
  + SearchDataPostgresqlMapper(table, application_name)
  + select_count(query)
  + write(object)
  + list_distinct_data(sqlQuery, values)
  + read_by_column(object, column_Name, solution_id)
  + read(connector: Connector, table: str, map: ColumnMap, persistable: Persistable, column_Name: str, solution_id: str)
  + build_read_query_by_column(table: str, map: ColumnMap, persistable: Persistable, column_Name: str, solution_id: str) string
  + update_persistable_from_result_row(map, persistable, result_row)
  + validate_read_result(result)
  + hard_delete_by_column(object, location_code, solution_id)
  + delete_by_column(connector: Connector, table: str, map: ColumnMap, persistable: Persistable, location_code: str, solution_id: str, type: str)
  + search(query, values, object)
  + write_delete_if_not_exists(object, method)
  <<abstract>> build_map()
}
class ColumnMap {
  + get_column_names_from_field_list(keys)
  + get_column_names()
  + get_field(column)
  + get_table()
  + freeze()
}
class Connector
class DatabaseConnectorFactory {
  + get_connector(stage, region, service, application_name)
}
class CrudOperations {
  + create_row(connector, table, map, object)
  + create_delete_row_ifnot_exists(connector, table, map, object, method)
}
class SearchOperations {
  + search(connector, query, values, map, object)
}
class Persistable {
  + id
  + get_dirty_fields()
  + set(field, value)
  + clear_fields()
  + get_key_field(location_code)
}
class BadRequestError
class NotFoundError

SearchDataPostgresqlMapper --|> ColumnMap
SearchDataPostgresqlMapper o-- Connector : uses/holds
SearchDataPostgresqlMapper ..> DatabaseConnectorFactory : obtains
SearchDataPostgresqlMapper o-- CrudOperations
SearchDataPostgresqlMapper o-- SearchOperations
SearchDataPostgresqlMapper ..> Persistable : method params
SearchDataPostgresqlMapper ..> BadRequestError
SearchDataPostgresqlMapper ..> NotFoundError
```

> SVG rendering failed for this diagram.
