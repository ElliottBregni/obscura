# Diagram: container_tracking_core/container_tracking_service/container_tracking_service/persistence_adapter/postgresql/SearchPostgresqlMapping.py


> Auto-generated by Obscura crawlers

## Diagram 1

```mermaid
classDiagram
class BasePostgresqlMapping {
  +get_tracking_database_connector()
}
class SearchPostgresqlMapping {
  +__init__(table, application_name)
  +read(query, replaces={})
  +search(query)
  <<abstract>> +process(options)
}
BasePostgresqlMapping <|-- SearchPostgresqlMapping
```

> SVG rendering failed for this diagram.

## Diagram 2

```mermaid
sequenceDiagram
participant Caller
participant SearchPostgresqlMapping as SPM
participant TrackingConnectorProvider as Connector
participant Connection as Conn
participant Cursor
participant Database
participant Logger
Caller->>SPM: read(query, replaces)
SPM->>Connector: get_tracking_database_connector()
Connector->>Conn: establish_connection()
Conn->>Cursor: get_cursor()
Cursor->>Cursor: mogrify(query, replaces)
Cursor-->>SPM: mogrified_query (bytes)
SPM->>Logger: info("Mogrified_Query_decoded: ...")
SPM->>Cursor: execute(mogrified_query)
Cursor->>Database: execute query
Database-->>Cursor: execution result
Cursor-->>SPM: fetchall() -> rows
```

> SVG rendering failed for this diagram.
