# Diagram: container_tracking_core/container_tracking_service/container_tracking_service/persistence_adapter/postgresql/SupplierCodeListMapping.py


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
classDiagram
    class SearchDataPostgresqlMapper {
        <<abstract>>
        +add(key, column)
        +freeze()
        -table
        -application_name
    }
    class SupplierCodeListMapping {
        +__init__(application_name)
        +build_map()
        +init_calls: super(public.vw_reuse_trip_container_last_event_005, application_name)
        +init_calls: super().freeze()
        +mapping: supplier_code -> supplier_code
        +mapping: full_count -> full_count
    }
    SearchDataPostgresqlMapper <|-- SupplierCodeListMapping
```

> SVG rendering failed for this diagram.
