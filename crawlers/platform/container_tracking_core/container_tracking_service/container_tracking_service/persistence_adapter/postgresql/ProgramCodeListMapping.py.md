# Diagram: container_tracking_core/container_tracking_service/container_tracking_service/persistence_adapter/postgresql/ProgramCodeListMapping.py


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
classDiagram
    class SearchDataPostgresqlMapper {
        +SearchDataPostgresqlMapper(tableName, application_name)
        +freeze()
        +add(key, value)
    }
    class ProgramCodeListMapping {
        +ProgramCodeListMapping(application_name)
        +build_map()
        program_code : program_code
        full_count : full_count
    }
    SearchDataPostgresqlMapper <|-- ProgramCodeListMapping
    ProgramCodeListMapping ..> SearchDataPostgresqlMapper : "super().__init__(table, application_name)"
    ProgramCodeListMapping ..> SearchDataPostgresqlMapper : "freeze()"
```

> SVG rendering failed for this diagram.
