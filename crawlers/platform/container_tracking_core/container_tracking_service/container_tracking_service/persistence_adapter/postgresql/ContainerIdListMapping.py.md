# Diagram: container_tracking_core/container_tracking_service/container_tracking_service/persistence_adapter/postgresql/ContainerIdListMapping.py


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
classDiagram
    class BasePostgresqlMapping {
        <<abstract>>
        +__init__(table_name, application_name)
        +freeze()
        +add(column, mapping)
    }
    class ContainerIdListMapping {
        +__init__(application_name)
        +build_map()
        +mapping: container_id -> container_id
    }
    ContainerIdListMapping --|> BasePostgresqlMapping
    ContainerIdListMapping : calls super().__init__(trip_leg, application_name)
    ContainerIdListMapping : calls super().freeze()
    ContainerIdListMapping : build_map() -> add(container_id, container_id)
```

> SVG rendering failed for this diagram.
