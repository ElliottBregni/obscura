# Diagram: container_tracking_core/container_tracking_service/container_tracking_service/common/utilities/__init__.py


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
classDiagram
class utils {
  +is_blank(test_str)
  +is_present(test_str)
  +unescape(s)
  +add_escape_to_backslash(string, remove=False)
  +get_distance(first_coords, sec_coords)
  +straight_line_distance(p0, p1)
  +get_distance_waypoints(waypoints)
  +dev_to_staging(stage, spec_env="")
  +split_to_list(comma_sep_string)
  +is_valid_lat_long(lat, lng)
  +snake_to_camel(string)
  +snake_to_camel_recur(obj)
  +get_rand_hex(length)
  +send_multithreaded_requests(thread_function, thread_nums=4, args=[], kwargs=[])
  +fix_wildcards(query)
  +format_fv_stop_time(stop_time)
  +validate_location_update(latitude, longitude)
  +validate_lat_long(latitude, longitude)
  +strip_str(string)
  +strip_recur(obj)
  +send_email(email, body)
  +datetime_from_iso_format(date_str, end=True)
  +merge_date_and_time(date, time)
  +split_list_even(lst, max_size=1000)
  +timestamp_to_datetime(timestamp, to_utc)
  +check_email_format(email)
  +is_timestamp(ts_str)
}

class dict
class dotdict {
  <<dict>>
  +__getattr__()
  +__setattr__()
  +__delattr__()
}

class DictionaryToObject {
  +__init__(d)
}

class Cache
class TTLCache
class MyTTLCache {
  +__setitem__(key, value, cache_setitem=Cache.__setitem__)
}

dict <|-- dotdict
TTLCache <|-- MyTTLCache
MyTTLCache ..> Cache : uses
DictionaryToObject o-- dict : assigns __dict__
utils ..> html : uses
utils ..> urllib : uses
utils ..> binascii : uses
utils ..> os : uses
utils ..> math : uses
utils ..> re : uses
utils ..> threading : uses
utils ..> datetime : uses
utils ..> cachetools : uses
utils ..> dateutil_parser : uses
```

> SVG rendering failed for this diagram.
