# Diagram: container_tracking_core/container_tracking_service/container_tracking_service/common/aws/sns.py


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
flowchart TD
    A[send_sms(phone_number, message)] --> B{sns_client provided?}
    B -- yes --> C[Use provided sns_client]
    B -- no --> D[Create sns_client = boto3.client(sns)]
    C --> E[Call sns_client.publish(PhoneNumber, Message)]
    D --> E
    E --> F[Publish succeeded]
    E -.-> G[botocore.exceptions.ClientError raised]
    G --> H[logging.error("Could not publish SMS")]
    G --> I[set sns_client = None]
```

> SVG rendering failed for this diagram.
