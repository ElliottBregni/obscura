# Diagram: container_tracking_core/container_tracking_service/container_tracking_service/common/aws/lambdas/invokinators/invokinator_iam.py


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
flowchart LR
  GUP[get_user_org_prefs(event, organization_id, user_auth0_id=None, key_pref=None)]
  IGF[invoke_get_features(organization_id, lambda_level)]
  EGS[extract_solution_id(solution_id)]
  IGS[invoke_get_solution(solution_id)]
  CACHE1[TTLCache(maxsize=128, ttl=300)]
  SYNC[sync_user(auth0_user_id)]
  GETS[get_sample_admin_event("PATCH")]
  EQP[extract_query_params(event, query_params)]
  IGO[invoke_get_organizations(event, query_params)]
  CACHE2[TTLCache(maxsize=128, ttl=900)]
  EOFN[extract_organization_id_feature_name(organization_id, feature_name)]
  IGS2[invoke_get_solution_by_organization_id_and_feature(organization_id, feature_name)]
  CACHE3[TTLCache(maxsize=128, ttl=900)]
  INV[invoke_lambda(name, full_payload)]
  AUTH[auth.get_organization_id(event)]
  HAND[HandledException]

  GUP -->|calls create_user_pref| INV
  IGF -->|calls get_features| INV
  IGS -->|calls get_solution| INV
  IGS2 -->|calls get_solution| INV
  SYNC -->|calls update_users| INV
  IGO -->|calls get_organizations| INV

  EGS -->|cache key| CACHE1
  CACHE1 -->|cached result| IGS

  EQP -->|cache key| CACHE2
  CACHE2 -->|cached result| IGO
  AUTH -->|used by| EQP

  EOFN -->|cache key| CACHE3
  CACHE3 -->|cached result| IGS2

  GETS -->|sample admin event| SYNC

  GUP -->|on status>=400| HAND
  IGF -->|on status>=400| HAND
  IGS -->|on status>=400| HAND
  IGS2 -->|on status>=400| HAND
  SYNC -->|on status>=400| HAND
  IGO -->|on status>=400| HAND
```

> SVG rendering failed for this diagram.
