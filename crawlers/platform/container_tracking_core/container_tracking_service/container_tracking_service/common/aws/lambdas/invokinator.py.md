# Diagram: container_tracking_core/container_tracking_service/container_tracking_service/common/aws/lambdas/invokinator.py


> Auto-generated by Obscura crawlers

## Diagram 1

```mermaid
flowchart TD
    subgraph Invokers
        IG[invoke_get_location(event, ...)]
        GSL[get_shipper_locations(organization_id)]
        GS[get_solution(event, solution_id)]
        GSF[get_solution_by_feature_and_org_id(event, feature_id, organization_fv_id)]
        GO[get_organization(event, organization_fv_id)]
        GOS[get_organizations(key)]
    end

    subgraph Lambdas
        L_LOC[invoke_lambda("locations-get")]
        L_SHIP[invoke_lambda("shipper-locations")]
        L_SOLUTION[invoke_lambda("get_solution")]
        L_ORGS[invoke_lambda("get_organizations")]
    end

    IG -->|full_payload event_copy| L_LOC
    L_LOC --> LLOC_PARSE[parse body -> loc_body]
    LLOC_PARSE --> SC1{status_code >= 400?}
    SC1 -- Yes --> HE1[HandledException thrown]
    SC1 -- No --> RET_LOC[return loc_body]

    GSL -->|full_payload aws_event| L_SHIP
    L_SHIP --> SLOC_PARSE[parse body -> loc_body]
    SLOC_PARSE --> SC2{status_code >= 400?}
    SC2 -- Yes --> HE2[HandledException thrown]
    SC2 -- No --> RET_SHIP[return loc_body]

    GS -->|full_payload invoke_event| L_SOLUTION
    GSF -->|full_payload invoke_event| L_SOLUTION
    L_SOLUTION --> S_PARSE[parse body -> solution_body]
    S_PARSE --> SC3{status_code >= 400?}
    SC3 -- Yes --> HE3[HandledException thrown]
    SC3 -- No --> RET_SOLUTION[return solution_body]

    GO -->|full_payload invoke_event| L_ORGS
    L_ORGS --> O_PARSE[parse body -> organization_body]
    O_PARSE --> SC4{status_code == 404?}
    SC4 -- Yes --> NF[NotFoundError thrown]
    SC4 -- No --> SC5{status_code >= 400?}
    SC5 -- Yes --> HE4[HandledException thrown]
    SC5 -- No --> RET_ORG[return organization]

    GOS -->|full_payload aws_event| L_ORGS
    L_ORGS --> ORGS_PARSE[parse body -> organizations]
    ORGS_PARSE --> SC6{status_code >= 400?}
    SC6 -- Yes --> HE5[HandledException thrown]
    SC6 -- No --> RET_ORGS[map organizations by key and return]
```

> SVG rendering failed for this diagram.

## Diagram 2

```mermaid
classDiagram
    class invoke_lambda {
      <<external>>
      +invoke_lambda(name, full_payload)
    }
    class HandledException {
      <<error>>
    }
    class NotFoundError {
      <<error>>
    }
    class BadRequestError {
      <<error>>
    }

    class invoke_get_location {
      +invoke_get_location(event, query_params={}, headers={}, accept=None, path_params=None)
    }
    class get_shipper_locations {
      <<cached>>
      +get_shipper_locations(organization_id)
    }
    class get_solution {
      <<cached>>
      +get_solution(event, solution_id)
    }
    class get_solution_by_feature_and_org_id {
      <<cached>>
      +get_solution_by_feature_and_org_id(event, feature_id, organization_fv_id)
    }
    class get_organization {
      <<cached>>
      +get_organization(event, organization_fv_id)
    }
    class get_organizations {
      <<cached>>
      +get_organizations(key=ORGANIZATION_KEY_FV_ID)
    }

    invoke_get_location --|calls|> invoke_lambda
    get_shipper_locations --|calls|> invoke_lambda
    get_solution --|calls|> invoke_lambda
    get_solution_by_feature_and_org_id --|calls|> invoke_lambda
    get_organization --|calls|> invoke_lambda
    get_organizations --|calls|> invoke_lambda

    get_organization --|raises on missing id|> BadRequestError
    get_organization --|raises on 404|> NotFoundError
    get_shipper_locations --|raises on error|> HandledException
    get_solution --|raises on error|> HandledException
    get_solution_by_feature_and_org_id --|raises on error|> HandledException
    get_organization --|raises on error|> HandledException
    get_organizations --|raises on error|> HandledException
```

> SVG rendering failed for this diagram.
