# Diagram: container_tracking_core/container_tracking_service/container_tracking_service/common/aws/lambdas/auth.py


> Auto-generated by Obscura crawlers

## Diagram 1

```mermaid
classDiagram
class Privilege <<enumeration>> {
  +CHANGE_ACTIVE_ORGANIZATIONS
  +SUPER_PRIVILEGE
  +MANAGE_ORGANIZATIONS
  +MANAGE_CARRIER_LOCATIONS
  +MANAGE_USERS
  +VIEW_LOCATIONS
  +MANAGE_LOCATIONS
  +VIEW_SHIPMENTS
  +MANAGE_SHIPMENTS
  +LINK_LOCATIONS
  +MANAGE_SHIPPER_LOCATIONS
  +CHANGE_USER_PASSWORDS
  +MANAGE_ENTITY
  +VIEW_ENTITY
  +VIEW_LOGGING
  +VIEW_AI
  +GLOBAL_ASSET_SEARCH
  +BUILD_REPORT
  +MANAGE_SHARED_REPORTS
  +VIEW_ALL_ENTITIES
  +MANAGE_GEOFENCE_NAME
  +MANAGE_REPORT_DIRECTORY
  +MANAGE_SCAC
  +MANAGE_REUSE_CONTAINER
}
class Roles <<enumeration>> {
  +THIRD_PARTY
  +CARRIER_SYSTEM
  +FV_ADMIN
  +ORG_ADMIN
  +ORG_USER
  +FINISHED_VEHICLE_USER
  +FINISHED_VEHICLE_SYSTEM
  +REPORT_BUILDER_USER
  +SCAC_ADMIN
}
class AuthType <<enumeration>> {
  +PRIVILEGE
  +SOLUTION
  +LOCATION_GRANT
  +ORG_TYPE
  +FEATURE
}
class Feature <<enumeration>> {
  +EVENT_API_READ_ACCESS
  +FINISHED_VEHICLE
  +PLANT_ASSET_TRACKING
  +CONNECTED_CAR
  +AUTO_DISTRIBUTION_CENTER
  +MILESTONE
  +REPORT_BUILDER
  +MILESTONE_ETA
  +VIN_VIEW
  +CONTAINER
  +CONTAINER_TRACKING
}
class FederatedRole <<enumeration>> {
  +FV
}
class AuthModule {
  +get_auth_fields(event)
  +user_token_is_valid(config,email,token)
  +requires_tokenauth(config,resource)
  +convert_basic_auth(func)
  +convert_basic_auth_direct(event,context)
  +get_access_token_add_com(email,secret)
  +get_access_token(username,password,retry=False)
  +get_actor_email(event)
  +get_actor_type(event)
  +get_privileges(event)
  +get_user_id(event)
  +get_organization_id(event)
  +get_user_org_types(event)
  +get_user_phone_num(event,full=False)
  +get_role_from_privs(cursor,privileges)
  +approve_solution(user_solutions,solution_id)
  +check_privileges(event,required_privileges,solution_id=None)
  +check_intersecting_lists(lst,user_list)
  +check_location_grant(cursor,user_org,solution_id,location_code)
  +get_first_error(check_group)
  +check_check_list(check_list)
  +check_auth_wrapper(cursor,event,auth_check)
}
class AWSLambdaService {
  +get_header(event,key,required=False)
  +get_url_extension(stage)
  +make_client_error(event,message)
  +make_error_response(event,context,message,resource,client_error_type,client_message,http_status)
}
class DB {
  +get_connection(config)
  +get_cursor(conn)
}
class Secrets {
  +get_secret(name)
}
class HTTP {
  +post(url,data)
}
AuthModule ..> Privilege : uses
AuthModule ..> Roles : uses
AuthModule ..> AuthType : uses
AuthModule ..> Feature : uses
AuthModule ..> FederatedRole : uses
AuthModule ..> AWSLambdaService : calls
AuthModule ..> DB : calls
AuthModule ..> Secrets : uses
AuthModule ..> HTTP : uses
AWSLambdaService ..> AuthModule : provides headers
DB ..> AuthModule : returns query results
```

> SVG rendering failed for this diagram.

## Diagram 2

```mermaid
sequenceDiagram
participant Client
participant APIGateway
participant Decorator as requires_tokenauth
participant AuthModule
participant AWSLambda as AWSLambdaService
participant DB
participant Handler as OriginalFunction

Client->>APIGateway: HTTP request
APIGateway->>Decorator: invoke decorated_handler(event,context)
Decorator->>AuthModule: get_auth_fields(event)
AuthModule->>AWSLambda: get_header(event,"x-user-email")
AWSLambda-->>AuthModule: email or KeyError
AuthModule->>AWSLambda: get_header(event,"x-user-token")
AWSLambda-->>AuthModule: token or KeyError
Decorator->>AuthModule: user_token_is_valid(config,email,token)
AuthModule->>DB: get_connection(config) / get_cursor
DB->>DB: execute SELECT users WHERE email,token,is_active
DB-->>AuthModule: user row or None
AuthModule-->>Decorator: True or False
alt valid token
Decorator->>Handler: call original function(event,context)
Handler-->>Decorator: function response
Decorator-->>APIGateway: success response
else invalid token or errors
Decorator->>AWSLambda: make_client_error(event,str(e))
AWSLambda-->>Decorator: client_error
Decorator->>AWSLambda: make_error_response(...,http_status=401)
AWSLambda-->>APIGateway: error response
```

> SVG rendering failed for this diagram.
