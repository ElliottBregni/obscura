# Diagram: container_tracking_core/container_tracking_service/container_tracking_service/common/HERE/HERE_locator.py


> Auto-generated by Obscura crawlers

## Diagram 1

```mermaid
classDiagram
    class mock_response {
        - row
        + get_long_lat()
    }
    class GeoDB {
        + read_by_long_lat_db(cursor, long, lat, include_api_response)
        + delete_by_long_lat_db(long, lat, cursor)
        + write_by_long_lat_db(cursor, long, lat, city, state, country, postal_code, api_response, source_ref)
        + read_by_city_state_db(cursor, city, state, postal_code, country)
    }
    class Cache {
        + read_by_long_lat_db_by_cache(cursor, long, lat, include_api_response)
        + read_by_city_state_db_by_cache(cursor, city, state, postal_code, country)
    }
    class Utilities {
        + get_select_fields(include_api_response)
        + check_key(key, dict)
        + extract_key(cursor, long, lat, include_api_response)
        + extract_city_state_zip_country(cursor, city, state, postal_code, country)
    }
    class Decorators {
        + HERE_locator_by_lat_long(f)
        + HERE_locator_Address_by_lat_long(f)
    }

    GeoDB --> mock_response : returns mock_response(row)
    Cache --> GeoDB : delegates lookup/calls
    Decorators --> Cache : reads via cached layer
    Decorators --> GeoDB : writes via write_by_long_lat_db
    Utilities --> GeoDB : provides get_select_fields / keys
    Cache ..> Utilities : uses key functions
```

> SVG rendering failed for this diagram.

## Diagram 2

```mermaid
flowchart TD
    A[Incoming call to decorated function] --> B{is_valid_lat_long(lng,lat)?}
    B -- No --> C[Call original function f(*args,**kwargs) and return result]
    B -- Yes --> D[Format lat/long to 2 decimal places]
    D --> E{cursor is None?}
    E -- Yes --> F[Call original function f and return its result]
    E -- No --> G[Call read_by_long_lat_db_by_cache(long,lat,cursor)]
    G --> H{DB cache returned result?}
    H -- Yes --> I[Log "in db" and return cached result]
    H -- No --> J[Log "Not in db"; call original function f to get API result]
    J --> K[Call write_by_long_lat_db(long,lat,city,state,country,postal,api_response,source_ref,cursor)]
    K --> L[Return API result to caller]
```

> SVG rendering failed for this diagram.
