# Diagram: container_tracking_core/container_tracking_service/container_tracking_service/common/constants.py


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
classDiagram
    class Stage <<enumeration>> {
        STAGING = "staging"
        TEST = "test"
        PROD = "prod-b"
        QA = "qa"
        QA2 = "qa2"
        DEV = "dev"
        DEV2 = "dev2"
        PROD_A = "prod-a"
        STAGING1 = "staging1"
        ALPHA = "alpha"
    }

    class ENVIRONMENTS <<set>> {
        values = set(Stage.value for Stage)
    }

    class PROD_ACCOUNT_ENVIRONMENTS <<set>> {
        members = {Stage.PROD, Stage.TEST}
    }

    class STAGING_ACCOUNT_ENVIRONMENTS <<set>> {
        members = {Stage.STAGING1}
    }

    class WEBSITE_BASE_URL <<map>> {
        dev = "portal-d.freightverify.com"
        dev2 = "portal-d2.freightverify.com"
        qa = "portal-q.freightverify.com"
        qa2 = "portal-q2.freightverify.com"
        test = "portal-t.freightverify.com"
        staging1 = "portal-s1.freightverify.com"
        prod-a = "portal-a.freightverify.com"
        prod-b = "portal.freightverify.com"
    }

    class EventCodeTypes <<enumeration>> {
        ARRIVED = "ARRIVED"
        DEPARTED = "DEPARTED"
        LO = "LO"
    }

    class LifeCycleState <<enumeration>> {
        AT_LOCATION = "At Location"
        IN_TRANSIT = "In Transit"
    }

    class Exceptions <<enumeration>> {
        UNACCOUNTED = "UNACCOUNTED"
        EXCESSIVE_DWELL = "EXCESSIVE_DWELL"
        NO_UPDATE_THIRTY_DAYS = "NO_UPDATE_30+_DAYS"
        OFF_ROUTE = "OFF_ROUTE"
        BATTERY_LEVEL = "BATTERY_LEVEL"
    }

    class UpdateTypes <<enumeration>> {
        LOCATION_UPDATE = "Location Update"
        SHIPMENT_UPDATE = "Shipment Update"
    }

    class UpdateDescription <<enumeration>> {
        AT_LOCATION = "At Location"
        DEPARTED = "Departed"
    }

    class ContainerAttributes <<enumeration>> {
        BATTERY_CHARGE = "battery_charge"
        BATTERY_LEVEL_EXCEPTION = "BATTERY_LEVEL"
    }

    class SensorSearchQueryParameters <<enumeration>> {
        SOLUTION_ID = "solution_id"
        EVERYTHING = "everything"
        PAGE_NUM = "pageNumber"
        PAGE_SIZE = "pageSize"
        LOCATION = "location"
        LOCATION_CONTAINS = "location:contains"
        LOCATION_IS_NULL = "location:isNull"
        LOCATION_IS_NOT_NULL = "location:isNotNull"
        SENSOR_ID = "sensorId"
        SENSOR_ID_CONTAINS = "sensorId:contains"
        STATUS = "status"
        STATUS_CONTAINS = "status:contains"
        STATUS_IS_NULL = "status:isNull"
        STATUS_IS_NOT_NULL = "status:isNotNull"
        SORT_COLUMN = "sortColumn"
        REVERSE_SORT = "reverseSort"
        BATCH_TYPE = "batchType"
    }

    class CsvExport <<enumeration>> {
        ASYNC_EXPORT = "asyncExport"
    }

    ENVIRONMENTS o-- Stage : derived_from
    PROD_ACCOUNT_ENVIRONMENTS o-- Stage : contains
    STAGING_ACCOUNT_ENVIRONMENTS o-- Stage : contains
    WEBSITE_BASE_URL o-- Stage : keyed_by
    EventCodeTypes <-- LifeCycleState
    UpdateTypes <-- UpdateDescription
    ContainerAttributes <-- SensorSearchQueryParameters
    CsvExport ..> WEBSITE_BASE_URL : unrelated_constant
```

> SVG rendering failed for this diagram.
