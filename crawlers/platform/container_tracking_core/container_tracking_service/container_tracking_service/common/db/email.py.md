# Diagram: container_tracking_core/container_tracking_service/container_tracking_service/common/db/email.py


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
flowchart TD
  Start((log_email))
  Params[/Params: cursor, recipients_to, recipients_cc, recipients_bcc, body, body_html, subject, attachments/]
  Init[/Initialize: replaces = {}, columns = '', values = ''/]
  Start --> Params --> Init

  subgraph AddField [add_field(field_name, field)]
    AF_cond{field is not None?}
    AF_update[replaces.update({field_name: field})]
    AF_append[append to columns and values strings]
    AF_cond -->|yes| AF_update --> AF_append
  end

  Init --> AddField
  Call1[call add_field("recipients_to", recipients_to)]
  Call2[call add_field("recipients_cc", recipients_cc)]
  Call3[call add_field("recipients_bcc", recipients_bcc)]
  Call4[call add_field("body", body)]
  Call5[call add_field("body_html", body_html)]
  Call6[call add_field("subject", subject)]
  Call7[call add_field("attachment_details", attachments)]
  AddField --> Call1 --> Call2 --> Call3 --> Call4 --> Call5 --> Call6 --> Call7

  BuildSQL[/Build SQL: INSERT INTO email_log ({columns}) VALUES ({values}) RETURNING id/]
  Call7 --> BuildSQL
  Mogrify[query = cursor.mogrify(sql, replaces)]
  BuildSQL --> Mogrify
  Execute[cursor.execute(query)]
  Mogrify --> Execute
  Fetch[results = cursor.fetchone()]
  Execute --> Fetch
  Decision{results?}
  Fetch --> Decision
  Decision -- yes --> ReturnID[return results.id]
  Decision -- no --> ReturnNone[return None]
  ReturnID --> End((end))
  ReturnNone --> End((end))
```

> SVG rendering failed for this diagram.
