# Diagram: container_tracking_core/container_tracking_service/container_tracking_service/common/db/organizations.py


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
flowchart TD
  Start([Start]) --> Mogrify[Mogrify SQL with cursor.mogrify(fv_id)]
  Mogrify --> Execute[Execute query with cursor.execute(query)]
  Execute --> Fetch[Fetch one row with cursor.fetchone()]
  Fetch --> Check{org found?}
  Check -- yes --> ReturnOrg[Return org]
  Check -- no --> ReturnNone[Return None]
  ReturnOrg --> End([End])
  ReturnNone --> End
```

> SVG rendering failed for this diagram.
