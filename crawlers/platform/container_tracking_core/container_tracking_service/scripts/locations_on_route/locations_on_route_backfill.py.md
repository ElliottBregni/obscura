# Diagram: container_tracking_core/container_tracking_service/scripts/locations_on_route/locations_on_route_backfill.py


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
flowchart TD
  Start([Start: main]) --> Validate{env in ["dev","dev2","qa","qa2","test","prod-a","prod-b"]?}
  Validate -- Yes --> GetConnector[DatabaseConnectorFactory.get_connector(env, "us-east-1", "partview", "backfill_locations_on_route").get_primary() ]
  GetConnector --> Connector[connector]
  GetConnector --> BuildEvent[build_event(org_id, env)]
  BuildEvent --> InvokeOrgs[invoke_get_orgs(event, organization_id)]
  BuildEvent --> InvokeSolution[invoke_get_solution_by_organization_id_and_feature(event, organization_id, feature_name)]
  InvokeOrgs --> FVId[fv_id]
  InvokeSolution --> SolutionId[solution_id]
  Connector --> GetLocations[get_locations(connector, solution_id)]
  GetLocations --> Results[location_codes (rows)]
  Results --> GetNames[get_location_names(location_codes, loc_event)]
  GetNames --> RemoteCall1[RemoteMethodInvoke(env).aws_status_remote_invocation(..., qsp={"code": record.location_code})]
  RemoteCall1 --> StatusCheck1{status == 200 and locations present?}
  StatusCheck1 -- Yes --> Append1[append [location.id, record.location_code, location.name] to location_details]
  StatusCheck1 -- No --> RemoteCall2[RemoteMethodInvoke(env).aws_status_remote_invocation(..., qsp={"code": record.location_code, "includeChildLocations":"true"})]
  RemoteCall2 --> StatusCheck2{status == 200 and locations present?}
  StatusCheck2 -- Yes --> Append2[append [location.id, record.location_code, location.name] to location_details]
  StatusCheck2 -- No --> SkipRecord[skip record]
  Append1 --> LocationDetails[location_details]
  Append2 --> LocationDetails
  LocationDetails --> Backfill[backfill_locations_to_bucket_table(location_details, connector)]
  Backfill --> UpdateDB[UPDATE locations_to_buckets SET location_id=%s, location_name=%s WHERE location_code=%s]
  UpdateDB --> LogDone[logging.info("Back fill is complete")]
  Validate -- No --> LogInvalid[logging.info("The entered environment is not valid, Please try again !!")]
```

> SVG rendering failed for this diagram.
