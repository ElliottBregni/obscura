# Diagram: container_tracking_core/container_tracking_service/config.global.yml


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
graph TD
  EndpointType[endpointType\nstaging: REGIONAL\ntest: EDGE\nprod-b: EDGE]
  IAM[iamRoleStatements\nAction: s3:*\nResource: arn:aws:s3:::${opt:stage}-fv-csv-export/*]
  SpecEnv[spec_env\n${env:SPECIFIED_ENV, "nothing"}]
  NoDev[no_dev\n(stage -> name map)]
  NoDevProdA[no_dev_prod_a\n(stage -> name map)]
  NoDevWithoutDev[no_dev_without_dev\n(stage -> name map)]
  PruneNumber[prune_number\n(per-stage counts)]
  Prune[prune\nautomatic: true\nincludeLayers: true\nnumber: ${self:custom.globalConfig.prune_number.${opt:stage}, 1}]
  CustomDomain[customDomain\nbasePath / createRoute53Record / domainName / stage]
  Domain[domain\n(dev,dev2,qa,qa2,user,prod,prod-b,prod-a,staging,test,staging1)]
  Options[options\n${opt:}]
  Authorizer[authorizer\narn: lambda function using no_dev mappings\nidentitySource/resultTtlInSeconds/type]
  PythonReq[pythonRequirements\ndockerizePip/dockerSsh/useDownloadCache/useStaticCache]
  Config[config -> file(config/config.${self:custom.globalConfig.no_dev.${...}}.yml)]
  FVLayers[fv_layers\nlayer ARNs from CloudFormation]
  Base[base\nper-stage base paths]
  CORS[cors_headers\n(allowed headers list)]
  Memory[memorySize: 512]
  DefaultPolicies[defaultManagedPolicies\nAWSLambdaMSKExecutionRole]
  VPCConfigs[vpcConfigs from SSM]
  LambdaSG[lambdaSecurityGroupId\nfrom globalConfig]
  VpcSubnetIds[vpcSubnetIds\n3 AZ subnet ids]
  VPC[vpc\nsecurityGroupIds / subnetIds]
  MSK[MSK configs\nmskClusterArn / bootstrapServers / SCHEMA_REGISTRY_URL / BOOTSTRAP_SERVERS]
  Resources[resources\nAPI Gateway Responses:\nAuthFailure, DenyFailure, Timeout]
  
  %% relationships
  EndpointType -->|affects| CustomDomain
  Domain -->|used by| CustomDomain
  Options -->|supplies| CustomDomain
  SpecEnv -->|selects mapping| NoDev
  SpecEnv -->|selects mapping| NoDevProdA
  SpecEnv -->|selects mapping| NoDevWithoutDev
  NoDev -->|feeds| Authorizer
  NoDevProdA -->|feeds| Authorizer
  NoDevWithoutDev -->|feeds| Authorizer
  Authorizer -->|attached to| Resources
  IAM -->|policies for| Resources
  PruneNumber -->|parameter for| Prune
  Prune -->|applies to| Config
  Config -->|includes| FVLayers
  FVLayers -->|dependent on| PythonReq
  Base -->|basePath used by| CustomDomain
  CORS -->|applies to| Resources
  Memory -->|lambda setting| Resources
  DefaultPolicies -->|applied to| IAM
  VPCConfigs -->|source for| VpcSubnetIds
  VPCConfigs -->|source for| LambdaSG
  LambdaSG -->|used in| VPC
  VpcSubnetIds -->|used in| VPC
  VPC -->|used by| MSK
  MSK -->|bootstrap info| Config
  Config -->|deploys| Resources
```

> SVG rendering failed for this diagram.
