# Diagram: container_tracking_core/container_tracking_service/hooks/pre-commit.py


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
flowchart TD
  Start((Start)) --> DiffCached[/"git diff --cached --name-only --diff-filter=ACM"/]
  DiffCached --> ReadCached[Read stdout -> split by "\n" -> file_names]
  ReadCached --> IsValidCheck[is_valid_file(x): x != None && strip() != "" && ".py" in lower(x)]
  IsValidCheck --> Filtered[Filter file_names by is_valid_file]
  Filtered --> ForLoop{file_names non-empty?}
  ForLoop -->|yes| RunBlack[/"black -l 100 -q <file_name>"/]
  RunBlack --> BlackWait[black_process.wait()]
  BlackWait --> ForLoop
  ForLoop -->|no| DiffPost[/"git diff --name-only --diff-filter=ACM"/]
  DiffPost --> ReadPost[Read stdout -> split by "\n"]
  ReadPost --> FilterPost[formatted_files = [x for x in lines if is_valid_file(x) && x in file_names]]
  FilterPost --> PostLoop{formatted_files non-empty?}
  PostLoop -->|yes| GitAdd[/"git add <file_name>"/]
  GitAdd --> PostLoop
  PostLoop -->|no| End((End))
```

> SVG rendering failed for this diagram.
