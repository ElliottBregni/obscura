# Diagram: container_tracking_core/container_tracking_service/tests/unit/api/reuse_trip_container_location_event/reuse_trip_container_location_event_consumer_test.py


> Auto-generated by Obscura crawlers

## Diagram 1

```mermaid
classDiagram
class AppContainer
class ReuseTripContainerLocationEventConsumer {
  +read(message_body)
}
class reuse_trip_container_helper <<Mock>>
class reuse_trip_container_location_event_mapping <<Mock>>
class BadRequestError
class providers.Object

AppContainer --> reuse_trip_container_helper : provides
AppContainer --> reuse_trip_container_location_event_mapping : provides
ReuseTripContainerLocationEventConsumer o-- reuse_trip_container_helper : dependency
ReuseTripContainerLocationEventConsumer o-- reuse_trip_container_location_event_mapping : dependency
ReuseTripContainerLocationEventConsumer ..> BadRequestError : raises
AppContainer ..> providers.Object
```

> SVG rendering failed for this diagram.

## Diagram 2

```mermaid
sequenceDiagram
participant Tests as pytest tests
participant Container as AppContainer (fixture/mock)
participant Consumer as ReuseTripContainerLocationEventConsumer
participant Helper as reuse_trip_container_helper (Mock)
participant Mapping as reuse_trip_container_location_event_mapping (Mock)
participant Error as BadRequestError

Tests->>Container: create AppContainer() with overrides
Tests->>Consumer: instantiate with Container.provided mocks
Note over Consumer: Consumer initialized with helper and mapping
Tests->>Consumer: read([]) 
Consumer-->>Tests: return (no exception)

Tests->>Consumer: read(invalid_message_body)
alt invalid structure or missing body/data
Consumer-->>Tests: throw Error: "Unable to process message, invalid body detected in message"
end

Tests->>Consumer: read(message_body with data)
Consumer->>Helper: search_reuse_trip_containers("123456789", "TEST_HEALTHCARE_TRACKING_FV")
alt helper finds container
Helper-->>Consumer: container
Consumer->>Mapping: map event data
Consumer-->>Tests: complete successfully
else helper returns None
Helper-->>Consumer: null
Consumer-->>Tests: throw Error: "reuse trip container with tag id 123456789 does not exist"
end
```

> SVG rendering failed for this diagram.
