# Diagram: container_tracking_core/container_tracking_service/tests/unit/api/sensors/search/sensor_search_request_handler_test.py


> Auto-generated by Obscura crawlers

## Diagram 1

```mermaid
classDiagram
class SensorSearchRequestHandler {
  - event
  + application_name : str
  + parse_request()
  + validate_parameters()
}
class MockLambdaEvent {
  + http_method : str
  + query_string_parameters : dict
  + features : list
  + get_aws_event()
}
class ParentSolutionLookup {
  + lookup_parent_solution_id(query) : Optional[str]
}
class BadRequestError
class NotFoundError {
  + client_message : str
}
BadRequestError <|-- NotFoundError
SensorSearchRequestHandler ..> MockLambdaEvent : consumes
SensorSearchRequestHandler ..> ParentSolutionLookup : queries
MockLambdaEvent ..> SensorSearchRequestHandler : provides_event
```

> SVG rendering failed for this diagram.

## Diagram 2

```mermaid
sequenceDiagram
participant Test
participant Setup as setup_module
participant Mock as MockLambdaEvent
participant Handler as SensorSearchRequestHandler
participant Lookup as ParentSolutionLookup
participant Pytest as pytest.raises

Test->>Setup: call setup_module()
Setup-->>Test: return qsp ({"sensorId":"HSAP","pageSize":"10","pageNumber":"0"})
Test->>Mock: MockLambdaEvent(http_method="GET", query_string_parameters=qsp, features=DEFAULT_FEATURES)
Mock->>Mock: get_aws_event()
Mock-->>Test: return event
Test->>Handler: SensorSearchRequestHandler(event)
Handler-->>Test: instance created (application_name="container-tracking-sensor-search")
Test->>Handler: parse_request()
Handler->>Handler: parse_request() completes
Test->>Pytest: with pytest.raises(NotFoundError)
Handler->>Handler: validate_parameters()
Handler->>Lookup: lookup_parent_solution_id(...)
Lookup-->>Handler: not found
Handler-->>Pytest: raise NotFoundError(client_message="Parent solution ID not found")
Pytest-->>Test: exception caught
```

> SVG rendering failed for this diagram.
