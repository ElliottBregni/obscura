# Diagram: container_tracking_core/container_tracking_service/tests/unit/api/sensors/upsert_sensor/upsert_sensor_consumer_handler_test.py


> Auto-generated by Obscura crawlers

## Diagram 1

```mermaid
classDiagram
class UpsertSensorConsumerHandler {
  - application_name
  - device_id
  - device_name
  - __solution_id
  - __organization_id
  - __sensor_data_store
  - __sensor_update_data_store
  - __sensor_id
  + UpsertSensorConsumerHandler(event)
  + parse_request()
  + validate_parameters()
  + process()
}
class SensorDataStore {
  + search()
  + write()
  + update()
}
class SensorUpdateDataStore {
  + exists()
  + write()
}
class BadRequestError {
  + client_message
}
UpsertSensorConsumerHandler --> SensorDataStore : uses
UpsertSensorConsumerHandler --> SensorUpdateDataStore : uses
UpsertSensorConsumerHandler ..> BadRequestError : raises
```

> SVG rendering failed for this diagram.

## Diagram 2

```mermaid
sequenceDiagram
participant TestSuite as Test
participant Handler as UpsertSensorConsumerHandler
participant SensorStore as SensorDataStore
participant UpdateStore as SensorUpdateDataStore
participant Error as BadRequestError

Note over Test,Handler: validate_parameters without DeviceID/DeviceName
Test->>Handler: instantiate(event without DeviceID/DeviceName)
Test->>Handler: validate_parameters()
Handler-->>Error: raise BadRequestError("Either device Name or device Id is not getting sent")
Error-->>Test: exception caught

Note over Test,Handler: process new sensor flow
Test->>Handler: instantiate(event)
Test->>Handler: parse_request()
Handler->>Handler: validate_parameters()
Handler->>SensorStore: search(sensor_key)
SensorStore-->>Handler: [] 
Handler->>SensorStore: write(sensor)
SensorStore-->>Handler: new_sensor
Handler->>UpdateStore: exists(new_sensor.latest_sensor_update)
UpdateStore-->>Handler: false
Handler->>UpdateStore: write(sensor_update)
UpdateStore-->>Handler: new_sensor_update
Handler->>SensorStore: update(sensor with latest_sensor_update)
SensorStore-->>Handler: updated
Handler-->>Test: process complete
```

> SVG rendering failed for this diagram.
