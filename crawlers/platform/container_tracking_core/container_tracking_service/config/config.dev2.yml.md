# Diagram: container_tracking_core/container_tracking_service/config/config.dev2.yml


> Auto-generated by Obscura crawlers

## Diagram 1

```mermaid
classDiagram
    class Crawlers {
        +crawl_repo(repo_path)
        +generate_stub(entry)
        +write_output(result, output_dir, dry_run=False, workers=4)
        +generate_index(result)
        +build_parser()
        +main(argv=None)
    }
    class Helpers {
        +_run_copilot_for_mermaid(code)
        +_render_svg_with_mmdc(mermaid)
        +_render_svg_with_kroki(mermaid)
        +split_mermaid_diagrams(text)
        +_strip_markdown_fences(text)
        +_trim_leading_prose(text)
    }
    class Data {
        +FileEntry(repo_relative, absolute, extension, size)
        +CrawlResult(repo_name, repo_path, files, skipped_dirs, skipped_files, skipped_size, skipped_ext)
        +CrawlResult.total_discovered()
    }
    Crawlers --> Helpers : uses
    Crawlers --> Data : instantiates
    Helpers ..> "copilot subprocess" : invokes
    Helpers ..> "mmdc / Kroki" : renders
    Data <|-- FileEntry
    Data <|-- CrawlResult
```

> SVG rendering failed for this diagram.

## Diagram 2

```mermaid
flowchart TD
    Main["main() / CLI"] --> Parse[parse args]
    Parse --> CheckRepo{repo exists?}
    CheckRepo -- yes --> Crawl[crawl_repo(repo_path)]
    CheckRepo -- no --> Exit[exit 1]
    Crawl --> Stats{files found}
    Stats -- 0 --> WriteIndex[write INDEX.md]
    Stats -- >0 --> Write[write_output(result)]
    Write --> ProcessFiles{for each file}
    ProcessFiles --> GenerateStub[generate_stub(entry)]
    GenerateStub --> Copilot["copilot -p (guard_automation)"]
    Copilot --> MermaidText["Mermaid text (stdout)"]
    MermaidText --> Split[split_mermaid_diagrams()]
    Split --> RenderChoice{render available?}
    RenderChoice -- mmdc OK --> MMDC["mmdc -> SVG"]
    RenderChoice -- mmdc fail --> Kroki["POST to https://kroki.io/mermaid/svg -> SVG"]
    MMDC --> EmbedSVG[embed SVG in markdown]
    Kroki --> EmbedSVG
    EmbedSVG --> WriteFile[write {repo_relative}.md]
    WriteFile --> Counter[update counter & progress]
    Counter --> FinalIndex[write INDEX.md]
    FinalIndex --> Done["Done / output summary"]
```

> SVG rendering failed for this diagram.
