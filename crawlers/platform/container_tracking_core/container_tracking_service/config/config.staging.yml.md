# Diagram: container_tracking_core/container_tracking_service/config/config.staging.yml


> Auto-generated by Obscura crawlers

## Diagram 1

```mermaid
classDiagram
    class Crawler {
        +start()
        +stop()
        +crawl(url)
        -_running: bool
    }
    class Fetcher {
        +fetch(url): Response
        +set_headers(h)
    }
    class Parser {
        +parse(response): Document
        +extract_links(doc): List
    }
    class Scheduler {
        +schedule(url)
        +next(): url
    }
    class Storage {
        +save(item)
        +exists(id): bool
    }
    Crawler --> Fetcher
    Crawler --> Parser
    Crawler --> Scheduler
    Crawler --> Storage
```

> SVG rendering failed for this diagram.

## Diagram 2

```mermaid
flowchart TD
    Start([Start]) --> Sched[Scheduler]
    Sched -->|provides URL| CrawlerProc[Crawler.process]
    CrawlerProc --> FetcherProc[Fetcher.fetch]
    FetcherProc --> ParserProc[Parser.parse]
    ParserProc --> Store[Storage.save]
    ParserProc -->|found links| Sched
    Store --> End([End])
```

> SVG rendering failed for this diagram.
