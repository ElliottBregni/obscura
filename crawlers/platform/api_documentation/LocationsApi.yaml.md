# Diagram: api_documentation/LocationsApi.yaml


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
classDiagram
    class Location {
        number child_count
        number id
        number organization_id
        number customer_id
        string name
        string code
        bool is_active
        string category
        string address
        number address2
        string city
        string state
        string country
        string postal_code
        string[] region
        string time_zone
        bool verified
        number referent_id
        number resolved_referent_id
        string location_references
        number count
    }

    class LAD {
        number id
        string lad_name
        number default_id
        string default_name
        string code
        string description
        string lob_name
    }

    class PaginationMetaData {
        integer totalCount
        integer currentPage
        integer totalPages
    }

    class Coordinate {
        number[2] coords
    }

    class Geofence {
        string type
        GeofenceProperties properties
        GeofenceGeometry geometry
        string rawGeometry
        string lastUpdater
        string lastUpdateTime
    }

    class GeofenceProperties {
        number buffer
        Center center
        Center firstCenter
    }

    class Center {
        number latitude
        number longitude
    }

    class GeofencePointGeometry {
        string type
        Coordinate coordinates
    }

    class GeofencePolygonGeometry {
        string type
        Coordinate[][] coordinates
    }

    class GeofenceMultiPolygonGeometry {
        string name
        GeofencePolygonGeometry geometry
        number sequence
        number lastSequence
    }

    class GrantLocation {
        string granteeId
        string granterId
        string locationCode
    }

    class GrantLocationResponse {
        integer id
        integer location_id
    }

    class LocationAssetDescriptor {
        number id
        string lad_name
        number default_id
        string default_name
        string code
        string description
        string lob_name
    }

    Location "1" o-- "1" Geofence : geofence
    Geofence o-- GeofenceProperties : properties
    GeofenceProperties o-- Center : center
    GeofenceProperties o-- Center : firstCenter
    Geofence "1" --o "1" GeofencePointGeometry : pointOption
    Geofence "1" --o "1" GeofencePolygonGeometry : polygonOption
    Geofence "1" --o "1" GeofenceMultiPolygonGeometry : multiPolygonOption
    GeofencePointGeometry --|> Coordinate
    GeofencePolygonGeometry --|> Coordinate
    GeofenceMultiPolygonGeometry --|> GeofencePolygonGeometry
    LocationsList <-- Location : items
    LocationAssetDescriptorList <-- LocationAssetDescriptor : items
    /location/locations/ --> PaginationMetaData : returns meta
    GrantLocationResponse <-- GrantLocation : createdBy
    Location "1" o-- "1" LAD : lad
```

> SVG rendering failed for this diagram.
