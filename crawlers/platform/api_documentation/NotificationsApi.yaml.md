# Diagram: api_documentation/NotificationsApi.yaml


> Auto-generated by Obscura crawlers

## Diagram 1

```mermaid
flowchart LR
  Client[Client] -->|GET /notification/| API[Notification API]
  API -->|200 OK| Response[Response JSON]
  Response --> NotificationsListNode[NotificationsList (array)]
  Response --> MetaNode[NotificationPaginationMetaData]
  API -->|400| BadRequest[400 Bad Request]
  API -->|403| Forbidden[403 Forbidden]
  API -->|404| NotFound[404 Not Found]
  API -->|500| ServerError[500 Internal Server Error]
  subgraph NotificationsList_Item
    NotificationItem[NotificationItem]
    NotificationItem -->|has property| id[id: number]
    NotificationItem --> type[type: string]
    NotificationItem --> eventTs[eventTs: string]
    NotificationItem --> sourceService[sourceService: string]
    NotificationItem --> context[context: object]
    context --> exportId[exportId: string]
    NotificationItem --> read[read: boolean]
    NotificationItem --> referenceId[referenceId: string]
  end
  NotificationsListNode -->|items of| NotificationItem
  MetaNode --> totalCount[totalCount: integer]
  MetaNode --> currentPage[currentPage: integer]
  MetaNode --> totalPages[totalPages: integer]
  MetaNode --> unreadCount[unreadCount: integer]
```

> SVG rendering failed for this diagram.

## Diagram 2

```mermaid
classDiagram
  class NotificationItem {
    +number id
    +string type
    +string eventTs
    +string sourceService
    +object context
    +boolean read
    +string referenceId
  }
  class NotificationsList {
    <<array>>
  }
  class NotificationContext {
    +string exportId
  }
  class NotificationPaginationMetaData {
    +integer totalCount
    +integer currentPage
    +integer totalPages
    +integer unreadCount
  }
  NotificationsList "1" o-- "*" NotificationItem : contains
  NotificationItem "1" o-- "1" NotificationContext : has
  NotificationPaginationMetaData : <<required: totalPages,currentPage,totalCount,unreadCount>>
```

> SVG rendering failed for this diagram.
