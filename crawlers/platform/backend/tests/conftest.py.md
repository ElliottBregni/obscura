# Diagram: backend/tests/conftest.py


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
flowchart TD
    Script[Python Script] --> ImportLogging[import logging]
    ImportLogging --> Configure[logging.basicConfig(level=logging.DEBUG)]
    Configure --> LevelDEBUG[Level: DEBUG]
    Configure --> RootLogger[Root Logger]
    RootLogger --> DebugEmit[Emits debug messages]
```

> SVG rendering failed for this diagram.
