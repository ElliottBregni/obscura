# Diagram: backend/.gitlab-ci.yml


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
flowchart TD
  subgraph Templates
    UV[.uv-venv]
    RUFF_STRICT[.ruff-strict]
    PYRIGHT_STRICT[.pyright-strict]
    PY_TEST_UV[.py-test-uv]
    DOCKER_BUILD_PIPELINE[.docker-build-pipeline-image]
  end

  subgraph Jobs
    BV[backend-venv]
    RUF[backend-ruff]
    PRG[backend-pyright]
    PT[backend-pytest]
    BLD[backend-build-image (disabled)]
  end

  UV --> BV
  BV --> RUF
  BV --> PRG
  BV --> PT

  RUFF_STRICT --> RUF
  PYRIGHT_STRICT --> PRG
  PY_TEST_UV --> PT

  BV ---|cache key: backend-venv-py313| Cache[cache: backend-venv-py313]
  BV ---|variables: SRC_PATH=platform/backend, UV_ARGS=--locked| Vars[(variables)]
  BV ---|rules: changes: platform/backend/**| Rules[(rules)]

  BLD -.-> DOCKER_BUILD_PIPELINE
  BLD -.-> DockerDeps[docker_gitlab_runner_login]
  DockerDeps -.-> BLD
  RUF --> BLD
  PRG --> BLD
  PT --> BLD

  classDef disabled fill:#f2f2f2,stroke:#999,stroke-dasharray:5 5;
  class BLD disabled
```

> SVG rendering failed for this diagram.
