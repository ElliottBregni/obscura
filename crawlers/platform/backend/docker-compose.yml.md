# Diagram: backend/docker-compose.yml


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
flowchart LR
  App["app service"]
  Build["build"]
  Context["context: ."]
  Dockerfile["dockerfile: Dockerfile"]
  Args["args:<br/>SRC_PATH: ."]
  Ports["ports:<br/>8500:8500"]
  Develop["develop.watch"]
  W1["action: sync+restart<br/>path: ./src<br/>target: /app/src"]
  W2["action: rebuild<br/>path: ./pyproject.toml"]
  Health["healthcheck"]
  Test["test:<br/>python -c \"import sys,urllib.request; resp=urllib.request.urlopen('http://localhost:8500/health/liveness'); sys.exit(0 if resp.status==200 else 1)\""]
  Interval["interval: 10s"]
  Timeout["timeout: 3s"]
  Retries["retries: 6"]
  Start["start_period: 1s"]

  App --> Build
  Build --> Context
  Build --> Dockerfile
  Build --> Args
  App --> Ports
  App --> Develop
  Develop --> W1
  Develop --> W2
  App --> Health
  Health --> Test
  Health --> Interval
  Health --> Timeout
  Health --> Retries
  Health --> Start
```

> SVG rendering failed for this diagram.
