# Diagram: backend/src/main.py


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
classDiagram
    class Application {
        +FastAPI app
        +register_all_routers(app)
        +add_exception_handler(Exception, unhandled_exception_handler)
        +add_exception_handler(StarletteHTTPException, http_exception_handler)
        +add_middleware(CaptureRequestIdMiddleware)
        +add_middleware(CORSMiddleware, config)
    }
    class Logging {
        +basicConfig(level)
        +getLogger(name)
    }
    class HandlerFilters {
        +AddRequestIdToLoggingContextFilter
        +CaptureRequestIdMiddleware
    }
    class JsonFormatter {
        +format(record)
    }
    class HealthProbeEndpointFilter {
        +setup_log_filter()
    }
    class RoutesLoader {
        +register_all_routers(app)
    }
    class ExceptionHandlers {
        +unhandled_exception_handler
        +http_exception_handler
    }
    class Starlette {
        +StarletteHTTPException
        +CORSMiddleware
    }
    class Uvicorn {
        +run(app)
    }

    Logging --> HandlerFilters : adds filter to handlers
    HandlerFilters --> JsonFormatter : setFormatter on handlers
    Application --> RoutesLoader : uses
    Application --> ExceptionHandlers : registers
    Application --> HandlerFilters : attaches middleware
    Application --> Starlette : uses CORSMiddleware & StarletteHTTPException
    HealthProbeEndpointFilter ..> Logging : configures log filter
    RoutesLoader --> Application : registers routers
    ExceptionHandlers --> Starlette : handles StarletteHTTPException
    Uvicorn --> Application : runs app

    %% simple grouping
    class Application,RoutesLoader,HealthProbeEndpointFilter,ExceptionHandlers,HandlerFilters,JsonFormatter,Logging,Uvicorn,Starlette {
        <<module>>
    }
```

> SVG rendering failed for this diagram.
