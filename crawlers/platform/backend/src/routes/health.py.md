# Diagram: backend/src/routes/health.py


> Auto-generated by Obscura crawlers

## Diagram 1

```mermaid
graph TD
  A[API Router: prefix "/health"] --> B["GET /liveness\nsummary: Liveness probe\nreturns: {status: 'alive'}"]
  A --> C["GET /readiness\nsummary: Readiness probe\nreturns: {status: 'ready'}"]
  D[uvicorn.access logger] --> E[HealthProbeEndpointFilter instance]
  E -->|added by| F[setup_log_filter()]
  E -->|filters out requests to| A
  style A fill:#f9f,stroke:#333,stroke-width:2
  style D fill:#ff9,stroke:#333,stroke-width:1
  style E fill:#9f9,stroke:#333,stroke-width:1
```

> SVG rendering failed for this diagram.

## Diagram 2

```mermaid
classDiagram
  class HealthProbeEndpointFilter {
    +bool filter(record: logging.LogRecord)
    +static setup_log_filter()
  }
  class Router {
    +prefix: str = "/health"
    +tags: list[str] = ["health"]
  }
  class Endpoint {
    +liveness() : dict[str,str]
    +readiness() : dict[str,str]
  }
  Router "1" -- "2" Endpoint : exposes
  HealthProbeEndpointFilter --> Router : ignores health endpoints in logs
  HealthProbeEndpointFilter ..> "uvicorn.access logger" : adds filter in setup_log_filter()
```

> SVG rendering failed for this diagram.
