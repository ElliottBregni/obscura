# Diagram: backend/src/handlers/starlette_exception_handler.py


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
flowchart LR
    A[http_exception_handler(Request, exc)] --> B[logger.exception("Handling starlette exception")]
    B --> C{isinstance(exc, StarletteHTTPException)?}
    C -- Yes --> D[Return JSONResponse]
    D --> D1[status_code = exc.status_code]
    D --> D2[content = {"detail": exc.detail, "requestId": request_id_ctx_var.get()}]
    D --> D3[headers = exc.headers]
    C -- No --> E[Call unhandled_exception_handler(Request, exc)]
    E --> F[Return result of unhandled_exception_handler]
```

> SVG rendering failed for this diagram.
