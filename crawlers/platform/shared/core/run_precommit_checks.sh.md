# Diagram: shared/core/run_precommit_checks.sh


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
flowchart LR
  Start([Start]) --> SetShell([set -euo pipefail])
  SetShell --> RepoRoot[REPO_ROOT = git rev-parse --show-toplevel || pwd]
  RepoRoot --> ConfigPath[CONFIG_PATH = "platform/shared/core/.pre-commit-config.yaml"]
  ConfigPath --> CDRepo[cd "$REPO_ROOT"]
  CDRepo --> CheckVenv1{file venv/bin/activate exists?}
  CheckVenv1 -- yes --> ActivateVenv1[. venv/bin/activate]
  CheckVenv1 -- no --> CheckVenv13{file venv13/bin/activate exists?}
  CheckVenv13 -- yes --> ActivateVenv13[. venv13/bin/activate]
  CheckVenv13 -- no --> NoVenv([No virtualenv activated])
  ActivateVenv1 --> ExportPY[export PYTHONPATH="platform/shared/core/src${PYTHONPATH:+:$PYTHONPATH}"]
  ActivateVenv13 --> ExportPY
  NoVenv --> ExportPY
  ExportPY --> EchoPython[echo "Using Python: $(python -V 2>/dev/null || true)"]
  EchoPython --> RunPreCommit[pre-commit run --all-files --hook-stage pre-commit --config "${CONFIG_PATH}"]
  RunPreCommit --> End([End])
```

> SVG rendering failed for this diagram.
