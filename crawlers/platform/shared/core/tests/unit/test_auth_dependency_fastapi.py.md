# Diagram: shared/core/tests/unit/test_auth_dependency_fastapi.py


> Auto-generated by Obscura crawlers

## Diagram 1

```mermaid
classDiagram
    class TestAuthDependencyInFastAPI {
        +_app() : FastAPI
        +test_dependency_parses_headers_and_exposes_context() : None
        +test_missing_required_headers_are_reported_by_fastapi() : None
    }
    class FastAPI
    class TestClient
    class Depends
    class get_auth_context
    class whoami
    TestAuthDependencyInFastAPI --> FastAPI : creates
    TestAuthDependencyInFastAPI --> TestClient : uses
    TestAuthDependencyInFastAPI o-- Depends : auth_dep
    Depends --> get_auth_context : wraps
    FastAPI --> whoami : defines endpoint /whoami
    whoami --> Depends : parameter ctx=auth_dep
    whoami --> "dict[str, str | list[str]]" : returns userId, privileges
```

> SVG rendering failed for this diagram.

## Diagram 2

```mermaid
flowchart TD
    A[Test Client] -->|GET /whoami with headers| B[TestClient wrapper]
    B --> C[FastAPI app]
    C --> D[/whoami endpoint (whoami)]
    D --> E[get_auth_context dependency]
    E --> F[AuthContext {user_id, privileges, email, org ids}]
    D -->|returns 200 JSON| G[Response body: {userId, privileges}]
    B --> H[GET /whoami without headers]
    H --> C
    H --> D
    D -->|validation error| I[422 Unprocessable Entity]
```

> SVG rendering failed for this diagram.
