# Diagram: shared/core/tests/unit/test_exception_handlers_integration.py


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
flowchart LR
  subgraph App["FastAPI app (add_exception_mapping)"]
    BR[/bad-request] -->|raises| BadReq[BadRequestError]
    FB[/forbidden] -->|raises| Forbidden[ForbiddenError]
    MS[/missing] -->|raises| NotFound[NotFoundError]
    UN[/unavailable] -->|raises| Unavailable[UnavailableError]
    IN[/integrity] -->|raises| Integrity[sqlalchemy.IntegrityError]
    DB[/db-down] -->|raises| Operational[sqlalchemy.OperationalError]
    VAL[/validate?age=...] -->|invalid param| ValidationError[RequestValidationError]
  end

  subgraph ExceptionMapping["exception handlers"]
    BadReq -->|mapped to| S400_1[HTTP 400]
    Forbidden -->|mapped to| S403[HTTP 403]
    NotFound -->|mapped to| S404[HTTP 404]
    Unavailable -->|mapped to| S503_1[HTTP 503]
    Integrity -->|mapped to| S409[HTTP 409\n"Integrity constraint violation"]
    Operational -->|mapped to| S503_2[HTTP 503\n"Database unavailable"]
    ValidationError -->|mapped to| S400_2[HTTP 400]
  end

  subgraph Tests["TestExceptionMappingBehavior"]
    TC[TestClient(self._app())]
    TC -->|GET /bad-request ->| S400_1
    TC -->|GET /forbidden ->| S403
    TC -->|GET /missing ->| S404
    TC -->|GET /unavailable ->| S503_1
    TC -->|GET /integrity ->| S409
    TC -->|GET /db-down ->| S503_2
    TC -->|GET /validate?age=not-an-int ->| S400_2
  end
```

> SVG rendering failed for this diagram.
