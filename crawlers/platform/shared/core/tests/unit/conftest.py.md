# Diagram: shared/core/tests/unit/conftest.py


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
flowchart TD
A[import pytest] --> C[_clear_storage_factory_cache fixture (autouse)]
B[import get_storage from core.storage.factory] --> C
C --> D{try}
D -->|no exception| E[get_storage.cache_clear()]
D -->|exception| F[except Exception]
E --> H[end]
F --> G[pass] --> H
```

> SVG rendering failed for this diagram.
