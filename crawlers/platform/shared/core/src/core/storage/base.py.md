# Diagram: shared/core/src/core/storage/base.py


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
classDiagram
    class PresignResultBase {
        +string url
        +datetime expiresAt
    }
    class PresignResult {
        +string method
        +dict~string,Any~ headers
        +dict~string,Any~ fields
    }
    interface Storage {
        +string name
        +can_handle(url: str) bool
        +presign(url: str, expires_in: int) PresignResult
        +upload_file(file_path: str, url: str) bool
        +upload_bytes(data: bytes, url: str) bool
        +generate_object_url(filename: str) string
        +build_target_url(key: str, provided_url: str|None) string
        +head_object(url: str) dict~string,Any~
        +get_presigned_upload_url(url: str, expires_in: int, method: str, content_type: str|None) PresignResult
        +delete_object(url: str) None
        +delete_objects(urls: list~string~) None
    }
    PresignResult --|> PresignResultBase
    Storage <|.. PresignResultBase : uses
```

> SVG rendering failed for this diagram.
