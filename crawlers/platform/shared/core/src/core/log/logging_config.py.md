# Diagram: shared/core/src/core/log/logging_config.py


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
flowchart LR
    A[Start: configure_logging(default_level="INFO")] --> B{Read LOG_LEVEL env var}
    B -->|set| C[env_level = LOG_LEVEL.upper()]
    B -->|unset| C2[env_level = "INFO"]
    C --> D[Lookup level in _LEVELS dict]
    C2 --> D
    D --> E{Found?}
    E -->|yes| F[level = mapped logging constant]
    E -->|no| G[level = logging.INFO]
    F --> H[Build dictConfig config]
    G --> H
    H --> I[formatters: plain -> "%(asctime)s %(levelname)s [%(name)s] %(message)s"]
    I --> J[handlers: stdout -> StreamHandler (sys.stdout), formatter plain]
    J --> K[root logger -> level, handlers: stdout]
    K --> L[loggers: uvicorn, uvicorn.error, uvicorn.access, sqlalchemy, core.exception -> level, stdout, propagate=False]
    L --> M[Call dictConfig(config)]
    M --> N[Logging configured]
    N --> O[End]
```

> SVG rendering failed for this diagram.
