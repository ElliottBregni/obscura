# Diagram: shared/core/src/core/utils/duration.py


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
flowchart LR
  Start([start]) --> CheckNone{duration is None?}
  CheckNone -- yes --> ReturnNone1([return None])
  CheckNone -- no --> IsNumericType{is instance of int or float?}
  IsNumericType -- yes --> ToInt[/value = int(duration)/]
  ToInt --> CheckPositiveNum{value <= 0?}
  CheckPositiveNum -- yes --> RaiseErrNum([raise ValueError: "Duration must be a positive value"])
  CheckPositiveNum -- no --> ReturnNum([return value])
  IsNumericType -- no --> ToString[/s = str(duration).strip().upper()/]
  ToString --> CheckEmpty{s == ""?}
  CheckEmpty -- yes --> ReturnNone2([return None])
  CheckEmpty -- no --> IsDigits{s.isdigit()?}
  IsDigits -- yes --> ParseDigits[/value = int(s)/]
  ParseDigits --> CheckPositiveDigits{value <= 0?}
  CheckPositiveDigits -- yes --> RaiseErrDigits([raise ValueError: "Duration must be a positive value"])
  CheckPositiveDigits -- no --> ReturnDigits([return value])
  IsDigits -- no --> RegexMatch{match ISO‑8601 regex?}
  RegexMatch -- no --> RaiseErrFormat([raise ValueError: "Invalid ISO-8601 duration format"])
  RegexMatch -- yes --> Extract[/extract years,months,weeks,days,hours,minutes,seconds (0 if missing)/]
  Extract --> ComputeTotal[/total = years*365*24*3600 + months*30*24*3600 + weeks*7*24*3600 + days*24*3600 + hours*3600 + minutes*60 + seconds/]
  ComputeTotal --> CheckTotal{total <= 0?}
  CheckTotal -- yes --> RaiseErrZero([raise ValueError: "Duration must be a positive value"])
  CheckTotal -- no --> ReturnTotal([return total])
  ReturnNone1 --> End([end])
  ReturnNum --> End
  ReturnNone2 --> End
  ReturnDigits --> End
  ReturnTotal --> End
  RaiseErrNum --> End
  RaiseErrDigits --> End
  RaiseErrFormat --> End
  RaiseErrZero --> End
```

> SVG rendering failed for this diagram.
