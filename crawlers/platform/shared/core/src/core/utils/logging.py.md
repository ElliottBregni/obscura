# Diagram: shared/core/src/core/utils/logging.py


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
classDiagram
    class log_duration {
        +operation: str
        +__call__(func: Callable) Callable
    }
    class decorator {
        +func: Callable
        +__call__(...P) -> T
    }
    class wrapper {
        +*args, **kwargs
        +returns T
    }
    class logger {
        +info(msg, *args)
    }
    class time {
        +perf_counter() -> float
    }
    class Callable {
    }
    class ParamSpec_P {}
    class TypeVar_T {}

    log_duration --> decorator : returns
    decorator --> wrapper : returns
    wrapper --> logger : uses
    wrapper --> time : measures
    decorator --> Callable : accepts
    wrapper --> Callable : calls
    wrapper ..> ParamSpec_P : typed by
    wrapper ..> TypeVar_T : returns type
    logger <-- module : __logger__
    note right of wrapper : logs start and duration of operation
```

> SVG rendering failed for this diagram.
