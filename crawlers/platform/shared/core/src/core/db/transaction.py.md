# Diagram: shared/core/src/core/db/transaction.py


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
flowchart LR
  A[call transactional(method)] --> B[wrapper(self, *args, **kwargs)]
  B --> C[session = cast(Session, self._session)]
  C --> D[transactional_scope(session)]
  D --> E{session.in_transaction()?}
  E -- yes --> F[yield (no new transaction)]
  E -- no --> G[with session.begin() (start transaction)]
  G --> H[yield (inside transaction)]
  G --> I{Exception during body?}
  I -- yes --> J[raise Exception (propagate)]
  I -- no --> K[commit and exit context]
  F --> L[call original method(self, *args, **kwargs)]
  H --> L
  L --> M[return method result]
  M --> N[wrapper returns result]
```

> SVG rendering failed for this diagram.
