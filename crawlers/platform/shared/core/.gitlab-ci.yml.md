# Diagram: shared/core/.gitlab-ci.yml


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
flowchart LR
  DG[Docker GitLab Runner Login]
  CB[.core:base\n(before_script: cd platform/shared/core)]
  CBD[.core:base:dependencies\n(before_script: references .core:base + venv + pip installs)]
  PYEXT[.python]
  RS[.ruff-strict]
  CR[core:ruff\n(sets LINT_DIRECTORIES=platform/shared/core/src/core)]
  CPY[core:pyright\n(stage: lint)]
  CTEST[core:pytest\n(stage: test)]
  PYRIGHT_CMD[pyright -p pyrightconfig.json --warnings]
  PYTEST_CMD[PYTHONPATH=src python -m pytest -q tests/unit]

  DG --> CB
  CB -.-> CBD
  PYEXT -.-> CBD
  CB -.-> CR
  RS -.-> CR
  CR -->|uses LINT_DIRECTORIES| LINT[platform/shared/core/src/core]
  CBD -.-> CPY
  CBD -.-> CTEST
  CPY --> PYRIGHT_CMD
  CTEST --> PYTEST_CMD
```

> SVG rendering failed for this diagram.
