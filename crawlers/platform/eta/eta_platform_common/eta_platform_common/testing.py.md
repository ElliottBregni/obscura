# Diagram: eta/eta_platform_common/eta_platform_common/testing.py


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
flowchart TD
  subgraph Functions
    TSQ[truck_shipment_qsp()] -->|returns| QSP_TS[Truck QSP]
    RSQ[rail_shipment_qsp()] -->|returns| QSP_RS[Rail QSP]
    OSQ[ocean_shipment_qsp()] -->|returns| QSP_OS[Ocean QSP]
    EQ[entity_qsp()] -->|returns| QSP_E[Entity QSP]
    IQ[intermediate_eta_qsp()] -->|returns| QSP_I[Intermediate ETA QSP]
    PVQ[partview_qsp()] -->|returns| QSP_PV[Partview QSP]
    EEQ[entity_qsp_org_ptsi()] -->|wraps/calls| EQ
    EPVQ[partview_qsp_org_ptsi()] -->|wraps/calls| PVQ
    ORG[ORG_PTSI (constant)]
    ORG -.-> EEQ
    ORG -.-> EPVQ
  end

  subgraph IntermediateETA_Structure
    QSP_I --> PlannedArrivals[plannedArrivals]
    QSP_I --> PlannedDepartures[plannedDepartures]
    QSP_I --> NextLocations[nextLocations]
    QSP_I --> TripLegs[trip_leg_ids]
    QSP_I --> CarrierScac[carrierScac]
    QSP_I --> TransportMode[transportMode]
    QSP_I --> ETA_Tracking[eta_tracking]
    QSP_I --> LastStatus[lastStatusUpdate]
    PlannedArrivals --> PA1["[\"2024-12-12 14:30:00\", ...]"]
    PlannedDepartures --> PD1["[ [..], [..], None ]"]
  end

  classDef qspNode fill:#f9f,stroke:#333,stroke-width:1px;
  class QSP_TS,QSP_RS,QSP_OS,QSP_E,QSP_I,QSP_PV qspNode;
```

> SVG rendering failed for this diagram.
