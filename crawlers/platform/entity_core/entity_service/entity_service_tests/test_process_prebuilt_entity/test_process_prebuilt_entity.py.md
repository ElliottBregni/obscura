# Diagram: entity_core/entity_service/entity_service_tests/test_process_prebuilt_entity/test_process_prebuilt_entity.py


> Auto-generated by Obscura crawlers

## Diagram 1

```mermaid
flowchart LR
  Event[Incoming event (event.body.code, order info)]
  Event --> GetID[get_entity_id(cursor, event) -> real_entity_id]
  GetID --> SysConf[get_system_configuration_setting(solution)]
  SysConf --> GetLinked[get_entity_linked_to_order(cursor, event) -> linked_entity]
  GetLinked --> CodeCheck{event.body.code == "ASGN" or "UNASGN" or other}
  CodeCheck -->|ASGN| ASGN_PATH
  CodeCheck -->|UNASGN| UNASGN_PATH
  CodeCheck -->|other| INVALID_CODE
  subgraph ASSIGNMENT_FLOW[Assignment flow]
    ASGN_PATH --> StateCheckA{linked_entity.state == "Prebuilt" or "Linked" or None}
    StateCheckA -->|Prebuilt| Call_Link_Prebuilt[link_prebuilt_entity(cursor, event, solution, real_entity, proxy_entity)]
    StateCheckA -->|Linked| Call_Link_Then_Unlink[link_prebuilt_entity(...) then unlink_proxy_or_real_entity(cursor, event, real_entity, proxy_entity)]
    Call_Link_Prebuilt --> Result_Merged["Return: The order is merged to <real.external_id>"]
    Call_Link_Then_Unlink --> Result_Merged
    StateCheckA -->|None/Unexpected| INVALID_STATE_ASGN["raise Exception (invalid state for ASGN)"]
  end
  subgraph UNASSIGNMENT_FLOW[Unassignment flow]
    UNASGN_PATH --> StateCheckU{linked_entity.state == "Linked" or "Prebuilt" or None}
    StateCheckU -->|Linked| Call_Unlink_Prebuilt[unlink_prebuilt_entity(cursor, event, real_entity, proxy_entity, linked_data)]
    Call_Unlink_Prebuilt --> Result_Unassigned["Return: The order <order_id> is unassigned"]
    StateCheckU -->|Prebuilt or None| INVALID_STATE_UNASGN["raise Exception (invalid state for UNASGN)"]
  end
  INVALID_CODE -->|invalid| RAISE_INVALID["raise Exception (unsupported status code)"]
  INVALID_STATE_ASGN --> RAISE_INVALID
  INVALID_STATE_UNASGN --> RAISE_INVALID
```

> SVG rendering failed for this diagram.

## Diagram 2

```mermaid
classDiagram
  class Entity {
    <<dataclass>>
    +id: str
    +external_id: str
    +organization_id: int
    +solution_id: str
  }
  class MockDataContainer {
    +id
    +external_id
    +state
    +linked_data
  }
  class process_prebuilt_entity {
    +process_prebuilt_entity(cursor, event) : str
  }
  class get_prebuilt_status_codes {
    +get_prebuilt_status_codes(config_client, solution) : (list, list)
  }
  class ProcessPrebuiltEntityTests {
    +setUp()
    +test_assignment_code_triggers_assignment_for_prebuilt_entity()
    +test_unassignment_code_triggers_unassignment_for_linked_prebuilt_entity()
    +test_assignment_code_triggers_unassignment_and_assignment_for_linked_prebuilt_entity()
    +test_raises_bad_request_exception_for_invalid_status_codes()
  }
  class GetPrebuiltStatusCodeTest {
    +test_get_prebuilt_status_code_returns_status_codes()
    +test_get_prebuilt_status_code_raises_exception()
  }
  ProcessPrebuiltEntityTests --> Entity : creates/uses
  ProcessPrebuiltEntityTests --> MockDataContainer : uses (mocked linked entity)
  ProcessPrebuiltEntityTests ..> process_prebuilt_entity : tests
  ProcessPrebuiltEntityTests ..> get_prebuilt_status_codes : uses (via system config)
  GetPrebuiltStatusCodeTest ..> get_prebuilt_status_codes : tests
  process_prebuilt_entity o-- Entity : accepts
  process_prebuilt_entity ..> MockDataContainer : inspects.state
  get_prebuilt_status_codes ..> "system_configuration.get_system_configuration_setting" : reads configuration
```

> SVG rendering failed for this diagram.
