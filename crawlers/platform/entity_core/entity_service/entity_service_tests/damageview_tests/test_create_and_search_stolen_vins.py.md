# Diagram: entity_core/entity_service/entity_service_tests/damageview_tests/test_create_and_search_stolen_vins.py


> Auto-generated by Obscura crawlers

## Diagram 1

```mermaid
classDiagram
    class TestCreateAndSearchStolenVins {
        +setUp()
        +test_stolen_missing_filter_returns_submissions_having_stolen_missing_vins()
        +test_stolen_recovered_filter_returns_submissions_having_stolen_recovered_vins()
        +test_not_stolen_filter_returns_submissions_having_not_stolen_vins()
        +test_translated_search_with_matching_substring()
        +tearDown()
    }
    class SubmissionHandler {
        +set_vars_from_body()
        +created_submission_ids
    }
    class FvDatabaseConnector {
        +establish_connection()
        +cursor
        +start_transaction()
        +commit()
        +get_cursor()
    }
    class create_submissions_and_vins_data {
        +function(db_conn, data, solution_id, ...)
    }
    class get_submissions {
        +function(cursor, params)
    }
    class translated_search {
        +function(args, params)
    }
    class MagicMock {
        +log_event()
    }

    TestCreateAndSearchStolenVins --> FvDatabaseConnector : uses db_conn
    TestCreateAndSearchStolenVins --> SubmissionHandler : instantiates
    TestCreateAndSearchStolenVins --> create_submissions_and_vins_data : calls
    TestCreateAndSearchStolenVins --> get_submissions : calls
    TestCreateAndSearchStolenVins --> translated_search : calls
    TestCreateAndSearchStolenVins --> MagicMock : uses history_logger
    SubmissionHandler --> FvDatabaseConnector : depends_on
    create_submissions_and_vins_data --> SubmissionHandler : updates/creates submissions
    get_submissions --> FvDatabaseConnector : reads via cursor
    translated_search --> (external) : pure_function
```

> SVG rendering failed for this diagram.

## Diagram 2

```mermaid
flowchart TD
    A[setUp] --> B[Instantiate FvDatabaseConnector]
    B --> C[establish_connection]
    C --> D[cursor.start_transaction]
    D --> E[Instantiate SubmissionHandler]
    E --> F[SubmissionHandler.set_vars_from_body]
    F --> G[create_submissions_and_vins_data]
    G --> H[cursor.commit]
    H --> I[get_cursor -> run tests]
    I --> J[test_stolen_missing_filter]
    I --> K[test_stolen_recovered_filter]
    I --> L[test_not_stolen_filter]
    I --> M[test_translated_search_with_matching_substring]
    J --> N[assert stolenVin == "Stolen - Missing"]
    K --> O[assert stolenVin == "Stolen - Recovered"]
    L --> P[assert stolenVin == "Not Stolen"]
    M --> Q[assert translated_search returns expected keys/values]
    Q --> R[tearDown]
    R --> S[UPDATE damage.submission SET archived_ts = NOW_UTC() WHERE id IN submission_ids]
```

> SVG rendering failed for this diagram.
