# Diagram: entity_core/entity_service/entity_service_tests/get_event_tests/test_get_legs_to_complete.py


> Auto-generated by Obscura crawlers

## Diagram 1

```mermaid
flowchart TD
    Start([Start]) --> Call[get_legs_to_complete(known_legs_to_complete, actual_trip_leg)]
    Call --> IsKnown{actual_trip_leg in known_legs_to_complete?}
    IsKnown -- No --> ProcessProvided[legs_to_complete_now = [actual_trip_leg]\nstill_to_complete = known_legs_to_complete]
    IsKnown -- Yes --> IsLast{is actual_trip_leg the last known leg?}
    IsLast -- Yes --> CompletePrev[legs_to_complete_now = previous leg ids (all legs before actual_trip_leg)\nstill_to_complete = [(index_of_actual, actual_trip_leg)]]
    IsLast -- No --> KeepCurrent[legs_to_complete_now = []\nstill_to_complete = known_legs_to_complete]
    CompletePrev --> End([End])
    KeepCurrent --> End
    ProcessProvided --> End
```

> SVG rendering failed for this diagram.

## Diagram 2

```mermaid
sequenceDiagram
    participant Test
    participant Service as get_legs_to_complete
    Test->>Service: call(known_legs_to_complete, actual_trip_leg)
    alt actual_trip_leg is known and is last
        Service-->>Test: legs_to_complete_now = previous leg ids; still_to_complete = [(last_index, actual_trip_leg)]
    else actual_trip_leg is known but not last
        Service-->>Test: legs_to_complete_now = []; still_to_complete = known_legs_to_complete
    else actual_trip_leg is not known
        Service-->>Test: legs_to_complete_now = [actual_trip_leg]; still_to_complete = known_legs_to_complete
    end
```

> SVG rendering failed for this diagram.
