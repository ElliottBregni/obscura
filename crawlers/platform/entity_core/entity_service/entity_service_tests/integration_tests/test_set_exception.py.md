# Diagram: entity_core/entity_service/entity_service_tests/integration_tests/test_set_exception.py


> Auto-generated by Obscura crawlers

## Diagram 1

```mermaid
classDiagram
class TestSetException {
+test_sets_exception_successfully()
+test_sets_exception_returns_bad_request_for_invalid_expected_clear_date()
+setUp()
+setup_test_data()
+setup_test_event()
+tearDown()
+assertOk(response)
+assertDictContains(expected,response)
+assertBadRequest(response)
}
class DB_CONN <<variable>> {
+database: "test_set_exception"
+secret_name: "ENTITY_DATABASE"
+auto_connect: false
}
class FvDatabaseConnector {
+establish_connection()
+get_cursor()
}
class FakeLambdaEvent
class LambdaContext
class Privilege
class lambda_handler <<function>>
TestSetException ..> DB_CONN : uses
DB_CONN --> FvDatabaseConnector : instance of
TestSetException ..> FakeLambdaEvent : constructs
TestSetException ..> LambdaContext : constructs
TestSetException ..> Privilege : references
TestSetException ..> lambda_handler : invokes
```

> SVG rendering failed for this diagram.

## Diagram 2

```mermaid
flowchart TD
setUp[setUp] --> setup_test_data[setup_test_data]
setup_test_data --> insert_entity[INSERT entity INTO DB\nRETURNING id -> entity_internal_id]
insert_entity --> setup_test_event[setup_test_event]
setup_test_event --> call_lambda[Call lambda_handler(event, LambdaContext("set_exception"))]
call_lambda --> check_date{expectedClearDate valid?}
check_date -- Yes --> success[assertOk(response)\nassertDictContains(expected,response)]
check_date -- No --> bad[assertBadRequest(response)\nassert body contains "expectedClearDate in invalid format"]
success --> tearDown[tearDown\nDELETE FROM exception; DELETE FROM entity]
bad --> tearDown
```

> SVG rendering failed for this diagram.
