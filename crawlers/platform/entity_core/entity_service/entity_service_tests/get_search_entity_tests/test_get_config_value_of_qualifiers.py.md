# Diagram: entity_core/entity_service/entity_service_tests/get_search_entity_tests/test_get_config_value_of_qualifiers.py


> Auto-generated by Obscura crawlers

## Diagram 1

```mermaid
flowchart TD
  A[Start] --> B[Receive configs list]
  B --> C{Configs empty?}
  C -->|Yes| D[Return {}]
  C -->|No| E[Iterate over each config item]
  E --> F{Qualifier == "ON_HOLD_EXCEPTON_LABEL"?}
  F -->|Yes| G[Set "onHoldExceptonLabel" = boolean(value)]
  F -->|No| H[Convert qualifier to camelCase and set key = value]
  G --> I[Continue loop]
  H --> I
  I --> J{More items?}
  J -->|Yes| E
  J -->|No| K[Return configurations dict]
  K --> L[End]
```

> SVG rendering failed for this diagram.

## Diagram 2

```mermaid
flowchart TD
  A[Start] --> B[Inputs: configs, solution_id, configurations]
  B --> C{Is ON_HOLD_EXCEPTON_LABEL present in configs?}
  C -->|No| D[Do nothing — leave configurations unchanged]
  C -->|Yes| E[Extract ON_HOLD_EXCEPTON_LABEL metadata and value]
  E --> F{Is value truthy/enabled?}
  F -->|No| G[Do nothing — remove or keep as appropriate]
  F -->|Yes| H{Does metadata contain solution_id?}
  H -->|Yes| I[Set displayHoldEventsForDealersAndCarriers = metadata[solution_id].display_hold_events_for_dealers_and_carriers]
  H -->|No| J[Set displayHoldEventsForDealersAndCarriers = metadata.DEFAULT.display_hold_events_for_dealers_and_carriers]
  I --> K[Remove onHoldExceptonLabel key from configurations]
  J --> K
  K --> L[End]
```

> SVG rendering failed for this diagram.
