# Diagram: entity_core/entity_service/entity_service_tests/get_search_entity_tests/integration_tests/test_get_search_entity_support.py


> Auto-generated by Obscura crawlers

## Diagram 1

```mermaid
classDiagram
    class GetSearchEntitySupportDPUFilters {
        +test_count_version_for_dpu_filter_returns_ok_for_test_shipper()
        +test_dpu_filter_with_all_filters_applied_returns_ok_and_full_page_for_test_dealer()
        +setUp()
        +setup_test_data()
        +getTestEvent(version, org_type, extra_qsp=None)
        -solution_id
        -org_id_shipper
        -org_id_dealer
        -cursor
        -external_id
    }
    class FvDatabaseConnector {
        +establish_connection()
        +get_cursor()
        -connection
    }
    class _search_entity {
        +_search_entity(cursor, event, solution_id, ..., org_type)
    }
    class generate_test_vin {
        +generate_test_vin()
    }
    GetSearchEntitySupportDPUFilters --> FvDatabaseConnector : uses DB_CONN
    GetSearchEntitySupportDPUFilters --> _search_entity : calls
    GetSearchEntitySupportDPUFilters --> generate_test_vin : invokes
    FvDatabaseConnector o-- "_DB Cursor" : provides
    GetSearchEntitySupportDPUFilters ..> "SQL/DB (entity, active_entity, entity_dpu, dda.dda_lifecycle_workflow)" : writes
```

> SVG rendering failed for this diagram.

## Diagram 2

```mermaid
flowchart TD
    A[setUp()] --> B[setup_test_data()]
    B --> C[DB_CONN.establish_connection()]
    C --> D[DB_CONN.get_cursor() -> cursor]
    D --> E[external_id = generate_test_vin()]
    E --> F[Execute SQL inserts into entity, active_entity, entity_dpu, dda_lifecycle_workflow]
    F --> G[test_count_version_for_dpu_filter_returns_ok_for_test_shipper()]
    G --> H[Create event via getTestEvent(version="count", OrgTypes.SHIPPER)]
    H --> I[_search_entity(cursor, event, solution_id, ..., OrgTypes.SHIPPER)]
    I --> J[returns (_, count)]
    J --> K[assertGreater(count, 0)]
    F --> L[test_dpu_filter_with_all_filters_applied_returns_ok_and_full_page_for_test_dealer() - skipped]
    style L stroke:#999,stroke-dasharray: 5 5,fill:#f9f9f9
```

> SVG rendering failed for this diagram.
