# Diagram: entity_core/entity_service/platform_applications/damage_submission_history_event/helm/templates/deployment.yaml


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
flowchart TD
  Deployment["Deployment: {{ .Chart.Name }}<br/>namespace: {{ .Values.namespace }}"]
  Deployment --> Spec["spec"]
  Spec --> Revision["revisionHistoryLimit: {{ .Values.revisionHistoryLimit | default 2 }}"]
  Spec --> Strategy["strategy: RollingUpdate<br/>maxSurge: 1<br/>maxUnavailable: 0"]
  Spec --> Selector["selector: matchLabels<br/>app: {{ .Chart.Name }}"]
  Spec --> Template["template"]
  Template --> TMetadata["metadata: labels/annotations"]
  TMetadata --> Labels["labels: app: {{ .Chart.Name }}"]
  TMetadata --> Annotations["annotations: rollme: {{ randAlphaNum 5 | quote }}"]
  Template --> PodSpec["spec"]
  PodSpec --> Termination["terminationGracePeriodSeconds: 30"]
  PodSpec --> ServiceAccount["serviceAccountName: \"{{ .Values.env }}-platform-controller\""]
  PodSpec --> Volumes["volumes"]
  Volumes --> SecretsStore["secrets-store-inline (CSI)<br/>driver: secrets-store.csi.k8s.io<br/>secretProviderClass: {{ .Chart.Name }}-aws-secret-provider"]
  PodSpec --> Containers["containers"]
  Containers --> Container["container: {{ .Chart.Name }}"]
  Container --> Image["image: \"{{ .Values.imageName }}\"<br/>imagePullPolicy: Always"]
  Container --> Port["containerPort: {{ .Values.port }}"]
  Container --> Resources["resources<br/>requests: cpu={{ .Values.resources.requests.cpu }} mem={{ .Values.resources.requests.memory }}<br/>limits: cpu={{ .Values.resources.limits.cpu }} mem={{ .Values.resources.limits.memory }}"]
  Container --> Probes["probes"]
  Probes --> Liveness["livenessProbe<br/>GET /health/liveness<br/>initialDelaySeconds:10<br/>periodSeconds:15"]
  Probes --> Readiness["readinessProbe<br/>GET /health/readiness<br/>initialDelaySeconds:45<br/>periodSeconds:10"]
  Probes --> Startup["startupProbe<br/>GET /health/liveness<br/>initialDelaySeconds:10<br/>periodSeconds:10"]
  Container --> VolumeMounts["volumeMounts"]
  VolumeMounts --> Mount["secrets-store-inline -> /mnt/secrets-store-inline (readOnly)"]
  Container --> Env["env variables"]
  Env --> SimpleEnv["SERVER_PORT, LOG_LEVEL, STAGE, APPLICATION_NAME, DAMAGE_HISTORY_ENV, ENTITY_DATABASE_ECHO, ENTITY_DATABASE_ENGINE, ENTITY_DATABASE_TIMEOUT"]
  Env --> SecretRefs["ENTITY_DATABASE_HOST/PORT/NAME/USERNAME/PASSWORD -> secretKeyRef: {{ .Chart.Name }}-entity-database"]
  Spec --> SelectorNote["pod template labels match selector: app: {{ .Chart.Name }}"]
```

> SVG rendering failed for this diagram.
