# Diagram: entity_core/entity_service/platform_applications/damage_submission_history_event/helm/templates/service.yaml


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
classDiagram
class Service {
  +string apiVersion = v1
  +string kind = Service
}
class Metadata {
  +string name = {{ include "damage-view-submission-history-service.fullname" . }}
  +Labels labels
}
class Labels {
  +string app = {{ include "damage-view-submission-history-service.name" . }}
}
class Spec {
  +string type = {{ .Values.serviceType | default "ClusterIP" }}
  +Port[] ports
  +Selector selector
}
class Port {
  +int port = {{ .Values.port }}
  +int targetPort = {{ .Values.port | default 8080 }}
}
class Selector {
  +string app = {{ include "damage-view-submission-history-service.name" . }}
}
Service --> Metadata
Service --> Spec
Metadata --> Labels
Spec --> Port
Spec --> Selector
```

> SVG rendering failed for this diagram.
