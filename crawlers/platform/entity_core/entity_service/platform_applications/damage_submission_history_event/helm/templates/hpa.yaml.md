# Diagram: entity_core/entity_service/platform_applications/damage_submission_history_event/helm/templates/hpa.yaml


> Auto-generated by Obscura crawlers

## Diagram 1

```mermaid
classDiagram
    class HorizontalPodAutoscaler {
        +apiVersion: autoscaling/v2
        +kind: HorizontalPodAutoscaler
        +metadata: Metadata
        +spec: Spec
    }
    class Metadata {
        +name: {{ .Chart.Name }}
        +namespace: {{ .Values.namespace }}
    }
    class Spec {
        +scaleTargetRef: ScaleTargetRef
        +minReplicas: {{ .Values.autoscaling.minReplicas }}
        +maxReplicas: {{ .Values.autoscaling.maxReplicas }}
        +metrics: Metric[]
    }
    class ScaleTargetRef {
        +apiVersion: apps/v1
        +kind: Deployment
        +name: {{ .Chart.Name }}
    }
    class Metric {
        +resource: Resource
        +type: Resource
    }
    class Resource {
        +name: cpu | memory
        +target: Target
    }
    class Target {
        +averageUtilization: {{ .Values.autoscaling.targetCPUUtilizationPercentage }} / {{ .Values.autoscaling.targetMemoryUtilizationPercentage }}
        +type: Utilization
    }
    HorizontalPodAutoscaler --> Metadata
    HorizontalPodAutoscaler --> Spec
    Spec --> ScaleTargetRef
    Spec --> Metric
    Metric --> Resource
    Resource --> Target
```

> SVG rendering failed for this diagram.

## Diagram 2

```mermaid
flowchart TD
    HPA[HorizontalPodAutoscaler<br/>name: {{ .Chart.Name }}<br/>namespace: {{ .Values.namespace }}] -->|scales| DEPLOY[Deployment<br/>name: {{ .Chart.Name }}]
    HPA --> REPL[Replicas<br/>min: {{ .Values.autoscaling.minReplicas }}<br/>max: {{ .Values.autoscaling.maxReplicas }}]
    HPA --> METR[Metrics]
    METR --> CPU[cpu<br/>averageUtilization: {{ .Values.autoscaling.targetCPUUtilizationPercentage }}%]
    METR --> MEM[memory<br/>averageUtilization: {{ .Values.autoscaling.targetMemoryUtilizationPercentage }}%]
    CPU -->|type| UTIL1[Utilization]
    MEM -->|type| UTIL2[Utilization]
```

> SVG rendering failed for this diagram.
