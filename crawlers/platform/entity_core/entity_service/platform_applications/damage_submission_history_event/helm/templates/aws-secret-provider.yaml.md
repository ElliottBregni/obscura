# Diagram: entity_core/entity_service/platform_applications/damage_submission_history_event/helm/templates/aws-secret-provider.yaml


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
flowchart LR
SPC[SecretProviderClass: {{ .Chart.Name }}-aws-secret-provider]
SPC --> Provider[AWS provider]
Provider --> Params[parameters.objects]
Params --> O1[objectName: {{ .Values.legacyEnv | default .Values.env }}/entity/database_api_service<br>objectType: secretsmanager]
O1 --> AE[entity-database-engine<br>(jmesPath: engine)]
O1 --> AH[entity-database-host<br>(jmesPath: host)]
O1 --> AP[entity-database-port<br>(jmesPath: to_string(port))]
O1 --> AN[entity-database-name<br>(jmesPath: dbname)]
O1 --> AU[entity-database-username<br>(jmesPath: username)]
O1 --> APass[entity-database-password<br>(jmesPath: password)]
SPC --> SecretObj[secretObjects → Secret: {{ .Chart.Name }}-entity-database<br>(type: Opaque)]
SecretObj --> KEngine[engine ← entity-database-engine]
SecretObj --> KHost[host ← entity-database-host]
SecretObj --> KPort[port ← entity-database-port]
SecretObj --> KName[name ← entity-database-name]
SecretObj --> KUser[username ← entity-database-username]
SecretObj --> KPass[password ← entity-database-password]
AE --> KEngine
AH --> KHost
AP --> KPort
AN --> KName
AU --> KUser
APass --> KPass
```

> SVG rendering failed for this diagram.
