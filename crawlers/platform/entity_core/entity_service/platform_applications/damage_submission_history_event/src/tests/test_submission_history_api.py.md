# Diagram: entity_core/entity_service/platform_applications/damage_submission_history_event/src/tests/test_submission_history_api.py


> Auto-generated by Obscura crawlers

## Diagram 1

```mermaid
flowchart TD
    SeedFuncs[seed_basic / _seed_three] -->|calls| Insert[insert_history_row]
    Insert -->|persists rows| DB[(Database)]
    TestFlow[tests] -->|prepare data| SeedFuncs
    TestFlow -->|HTTP GET| API[/GET /submission-history/submissions/{id}?pageNumber=&pageSize=/]
    API -->|returns| Resp[response.json() body]
    Resp --> Items[get_items(body)]
    Resp --> Meta[get_meta(body)]
    Items --> Changed[get_changed_ats(items)]
    Changed --> Assertions[assertions on items & fields]
    Meta --> Assertions
    Assertions -->|verifies| TestFlow
```

> SVG rendering failed for this diagram.

## Diagram 2

```mermaid
classDiagram
    class Utils {
        +_ts(n:int) : str
        +get_items(body:dict) : list[dict]
        +get_meta(body:dict) : dict
        +get_changed_ats(items:list[dict]) : list[str]
    }
    class Seeder {
        +seed_basic(db:Session, submission_id:int) : None
        +_seed_three(db:Session, submission_id:int) : None
    }
    class Tests {
        +test_get_history_basic(client, db)
        +test_get_history_pagination(client, db)
        +test_pagination_meta_and_ordering(client, db)
        +test_pagination_second_page(client, db)
        +test_wrong_param_names_are_ignored_and_defaults_apply(client, db)
    }
    class Conftest {
        +insert_history_row(...): None
    }
    Tests --> Utils : uses
    Tests --> Seeder : seeds data
    Seeder --> Conftest : calls
    Conftest --> DB[(Database)]
    Tests --> API[/submission-history API/] : queries
    API --> Conftest : reads history rows
```

> SVG rendering failed for this diagram.
