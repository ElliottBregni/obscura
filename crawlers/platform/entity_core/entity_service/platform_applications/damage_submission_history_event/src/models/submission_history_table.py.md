# Diagram: entity_core/entity_service/platform_applications/damage_submission_history_event/src/models/submission_history_table.py


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
flowchart TD
  SCHEMA_DEF[SCHEMA_DEFAULT = "damage"]
  TABLE_CONST["TABLE_NAME = submission_history_event"]
  Session[Session]
  Session --> GetBind[get_bind()]
  GetBind --> Bind[bind]
  Bind --> CheckBind{bind is not None?}
  CheckBind -- yes --> Dialect[dialect = bind.dialect.name]
  CheckBind -- no --> AssertFail[AssertionError: SQLAlchemy session is not bound to an engine.]
  Dialect --> IsSQLite{dialect == "sqlite"?}
  IsSQLite -- yes --> SchemaNone[schema = None]
  IsSQLite -- no --> SchemaDefault[schema = SCHEMA_DEFAULT]
  SchemaNone --> Metadata[metadata = MetaData(schema=schema)]
  SchemaDefault --> Metadata
  Metadata --> TableDef[Table(TABLE_NAME, metadata, autoload_with=bind, schema=schema)]
  TableDef --> Return[return Table]
```

> SVG rendering failed for this diagram.
