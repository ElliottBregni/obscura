# Diagram: entity_core/entity_service/entity_listener/entity_listener_service/lambdas/entity_intermediate_eta_processor_consumer.py


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
classDiagram
    class FvDatabaseConnector {
        +FvDatabaseConnector(name, secret, auto_connect=False)
    }
    class SecretNames
    class DB_CONN <<singleton>> {
        +name: "entity_intermediate_eta_processor_consumer"
    }
    DB_CONN --> FvDatabaseConnector : instanceOf
    DB_CONN ..> SecretNames : uses

    class IntermediateEtaConfigDAO
    class TripLegDAO
    class EntityDAO

    class IntermediateEtaInvoker
    class IntermediateEtaProcessor {
        +process()
    }
    IntermediateEtaProcessor --> IntermediateEtaConfigDAO : uses
    IntermediateEtaProcessor --> EntityDAO : uses
    IntermediateEtaProcessor --> TripLegDAO : uses
    IntermediateEtaProcessor --> IntermediateEtaInvoker : uses

    class SqsBatchProcessorWithPartialItemFailure {
        +process_batch(event)
    }
    SqsBatchProcessorWithPartialItemFailure --> IntermediateEtaProcessor : handler

    class MandatoryLambdaHandling <<decorator>>
    class lambda_handler {
        +handle(event, context, audit_refs)
    }
    MandatoryLambdaHandling ..> lambda_handler : decorates
    lambda_handler --> IntermediateEtaConfigDAO : creates
    lambda_handler --> EntityDAO : creates
    lambda_handler --> TripLegDAO : creates
    lambda_handler --> IntermediateEtaInvoker : creates
    lambda_handler --> IntermediateEtaProcessor : creates
    lambda_handler --> SqsBatchProcessorWithPartialItemFailure : instantiates
    lambda_handler ..> SecretNames : references
```

> SVG rendering failed for this diagram.
