# Diagram: entity_core/entity_service/entity_listener/tests/unit/test_initial_eta_processor.py


> Auto-generated by Obscura crawlers

## Diagram 1

```mermaid
classDiagram
class TestInitialEtaProcessor {
  +setUp()
  +create_record(entity_id, solution_id, milestone, destination_eta)
  +assert_handle_result(result, expected_result)
  +test_sets_initial_eta_if_valid_milestone_and_no_previous_initial_eta()
  +test_no_eta_reference_does_not_set_initial_eta()
  +test_invalid_milestone_does_not_set_eta()
  +test_skips_if_initial_eta_already_set()
}
class InitialEtaProcessor {
  +handle(record)
}
class FakeInitialEtaConfigDAO {
  -valid_milestones
  +get_initial_eta_valid_milestone_codes(solution_id)
}
class FakeEntityReferenceDAO {
  -initial_etas
  +has_initial_eta(entity_id, solution_id)
  +set_initial_eta(entity_id, solution_id, eta)
}
class FakeUpdateEntityInvoker {
  -set_initial_eta_called
  -initial_eta
  +set_initial_eta(entity_id, solution_id, initial_eta)
}
TestInitialEtaProcessor --> InitialEtaProcessor : uses
TestInitialEtaProcessor --> FakeInitialEtaConfigDAO : constructs
TestInitialEtaProcessor --> FakeEntityReferenceDAO : constructs
TestInitialEtaProcessor --> FakeUpdateEntityInvoker : constructs
InitialEtaProcessor --> FakeInitialEtaConfigDAO : reads valid milestones
InitialEtaProcessor --> FakeEntityReferenceDAO : queries/sets initial eta
InitialEtaProcessor --> FakeUpdateEntityInvoker : invokes set_initial_eta
FakeEntityReferenceDAO "1" o-- "n" : initial_etas
```

> SVG rendering failed for this diagram.

## Diagram 2

```mermaid
flowchart TD
Start((Start)) --> ParseBody[Parse record body JSON]
ParseBody --> ExtractMessage[Extract "Message" object]
ExtractMessage --> HasRef{progressUpdateReference contains etaRuleLookup?}
HasRef -- No --> EndNoRef[Return False]
HasRef -- Yes --> ParseRef[Parse etaRuleLookup.value JSON]
ParseRef --> ExtractMilestone[Extract "points-of-interest" => milestone]
ExtractMilestone --> GetValid[Get valid milestones via get_initial_eta_valid_milestone_codes(solution_id)]
GetValid --> MilestoneValid{milestone in valid_milestones?}
MilestoneValid -- No --> EndInvalid[Return False]
MilestoneValid -- Yes --> HasInitial{entity_reference_dao.has_initial_eta(entity_id, solution_id)?}
HasInitial -- Yes --> EndAlready[Return False (initial ETA already set)]
HasInitial -- No --> SetInitial[Call update_entity_invoker.set_initial_eta(entity_id, solution_id, destinationEta)\nThen call entity_reference_dao.set_initial_eta(...)]
SetInitial --> EndSet[Return True]
```

> SVG rendering failed for this diagram.
