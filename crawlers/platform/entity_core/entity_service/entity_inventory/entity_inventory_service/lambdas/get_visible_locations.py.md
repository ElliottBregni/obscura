# Diagram: entity_core/entity_service/entity_inventory/entity_inventory_service/lambdas/get_visible_locations.py


> Auto-generated by Obscura crawlers

## Diagram 1

```mermaid
classDiagram
class lambda_handler {
  +lambda_handler(event, context, audit_ref)
}
class LocationVisibility {
  +get_visible_locations_for_an_org(event)
}
class LocationService
class LocationDAO {
  +__init__(db_conn)
}
class FvDatabaseConnector {
  +__init__(name, secret, auto_connect=False)
}
class SecretNames <<Enum>> {
  ENTITY_DATABASE
}
class mandatory_lambda_handling {
  +__call__(func)
}
class AuthType
class Privilege
class Feature
class OrgTypes
lambda_handler --> LocationVisibility : calls/instantiates
LocationVisibility --> LocationService : uses
LocationVisibility --> LocationDAO : uses
LocationDAO --> FvDatabaseConnector : uses DB_CONN
FvDatabaseConnector --> SecretNames : reads ENTITY_DATABASE
lambda_handler ..> mandatory_lambda_handling : decorated by
AUTH_CHECK ..> AuthType
AUTH_CHECK ..> Privilege
AUTH_CHECK ..> Feature
AUTH_CHECK ..> OrgTypes
```

> SVG rendering failed for this diagram.

## Diagram 2

```mermaid
flowchart TD
evt[Incoming event] --> dec{mandatory_lambda_handling(auth_check=AUTH_CHECK)}
dec --> lh[lambda_handler(event, context, audit_ref)]
lh --> instVis[Create LocationVisibility(LocationService(), LocationDAO(DB_CONN))]
instVis --> LS[LocationService]
instVis --> LD[LocationDAO]
LD --> DB[FvDatabaseConnector("get_visible_locations_for_organization", SecretNames.ENTITY_DATABASE)]
DB --> Secret[SecretNames.ENTITY_DATABASE]
instVis --> callGet[get_visible_locations_for_an_org(event)]
callGet --> resp[Return visible locations]
```

> SVG rendering failed for this diagram.
