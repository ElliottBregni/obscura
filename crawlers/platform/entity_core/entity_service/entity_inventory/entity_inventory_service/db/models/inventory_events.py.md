# Diagram: entity_core/entity_service/entity_inventory/entity_inventory_service/db/models/inventory_events.py


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
classDiagram
class InventoryMilestoneTypes <<enumeration>> {
  IN
  OUT
}
class BaseModel <<pydantic>> {
}
class MilestoneReference {
  +str code
  +List~dict~ references
  +Optional~Dict[str, Dict[str, Any]]~ valid_codes
  +set_valid_codes(valid_codes)
  +is_valid_milestone(milestone_type, valid_codes)
  +is_valid_in_milestone(valid_codes)
  +is_valid_out_milestone(valid_codes)
  +has_valid_reference()
  +get_partner_type_reference()
  +get_location_code_reference()
}
class InventoryMilestone {
  +Optional[str] solution_id
  +Optional[str] entity_external_id
  +Optional[str] milestone_code
  +Optional[int] milestone_id
  +Optional[int] status_location_id
  +Optional[List~dict~] references
  +Optional[str] location_code
  +Optional[int] location_org_id
  +Optional[int] entity_internal_id
  +Optional[int] location_id
  +Optional[dict] valid_codes
  +Optional[MilestoneReference] milestone_reference
  +set_valid_codes(valid_codes)
  +is_eligible_to_process()
  +get_location_code_reference()
  +get_valid_codes(milestone_type)
  +get_valid_in_codes()
  +get_valid_out_codes()
  +parse(data)
  +__str__()
}
class InventoryTripleg {
  +List~int~ entity_ids
  +Optional[int] trip_leg_id
  +Optional[int] origin_location_id
  +Optional[int] destination_location_id
  +Optional[str] solution_id
  +parse(data)
}
MilestoneReference ..> InventoryMilestoneTypes : uses
InventoryMilestone o-- MilestoneReference : milestone_reference
InventoryMilestone --|> BaseModel
InventoryTripleg --|> BaseModel
```

> SVG rendering failed for this diagram.
