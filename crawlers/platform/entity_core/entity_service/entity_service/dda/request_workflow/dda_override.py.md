# Diagram: entity_core/entity_service/entity_service/dda/request_workflow/dda_override.py


> Auto-generated by Obscura crawlers

## Diagram 1

```mermaid
flowchart TD
    A[Invoke lambda_handler] --> B[DB_CONN.establish_connection / get_cursor]
    B --> C[parse_parameters(event)]
    C --> D{internal_id present?}
    D -- No --> E[raise BadRequestError: VIN does not exist]
    D -- Yes --> F[check_exists_in_dpu(internal_id)]
    F --> G{is_delivered_vin?}
    G -- Yes --> H[raise BadRequestError: VIN already delivered]
    G -- No --> I{exists_in_dpu?}
    I -- True --> J[handle_override(disregard_current_status, internal_id)]
    J --> K[complete_status if disregardCurrentStatus]
    I -- False --> L[determine carrier_fv_id via parameters or get_carrier()]
    L --> M[get_carrier_id(event, carrier_fv_id) -> carrier_org_id]
    M --> N[update_dda_vin_data(parameters, dao_container)]
    N --> O[grant_carrier_dpu_visibility(internal_id, carrier_org_id)]
    O --> P[add_override_vin(cursor, parameters)]
    P --> Q[DPUForwarder.invoke_dpu_dealership_pickup_distance(external_id)]
    Q --> R[send_to_dpu_status_topic({}, solution_id, external_id, {"trigger":"Override"})]
    R --> S[return make_response({})]
```

> SVG rendering failed for this diagram.

## Diagram 2

```mermaid
sequenceDiagram
    participant Event
    participant Lambda as lambda_handler
    participant DB as Database
    participant DAO as DAOContainer
    participant Forwarder as DPUForwarder
    participant OrgLambda as get_organizations
    participant Topic as DPUStatusTopic

    Event->>Lambda: invoke(event, context, audit_refs)
    Lambda->>DB: DB_CONN.establish_connection(); get_cursor()
    Lambda->>Lambda: parse_parameters(event) -> parameters
    Lambda->>DB: get_entity(cursor, external_id) -> (internal_id, solution_id)
    alt internal_id missing
        Lambda-->>Event: raise BadRequestError("VIN does not exist")
    else
        Lambda->>DB: check_exists_in_dpu(internal_id)
        Lambda->>DAO: entity_dao.is_delivered_vin(internal_id)
        alt is_delivered
            Lambda-->>Event: raise BadRequestError("VIN has already been delivered")
        else
            alt exists_in_dpu true
                Lambda->>DB: handle_override(cursor, disregard_current_status, internal_id)
            end
            Lambda->>DB: maybe get_carrier(cursor, parameters) -> carrier_fv_id
            Lambda->>OrgLambda: invoke_lambda("get_organizations", event_copy) -> response
            OrgLambda-->>Lambda: organization_id
            Lambda->>DAO: vin_data_dao.set_preauthorization(internal_id, solution_id, ts)
            Lambda->>DAO: vin_data_dao.set_current_location_code_and_source_id(internal_id, location_code, scac)
            Lambda->>DAO: visibility_grant_dao.grant_carrier_dpu_visibility(internal_id, carrier_org_id, reason)
            Lambda->>DB: add_override_vin(cursor, parameters)
            Lambda->>Forwarder: forwarder.invoke_dpu_dealership_pickup_distance(external_id)
            Lambda->>Topic: send_to_dpu_status_topic({}, solution_id, external_id, {"trigger":"Override"})
            Lambda-->>Event: make_response({})
        end
    end
```

> SVG rendering failed for this diagram.
