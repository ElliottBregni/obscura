# Diagram: entity_core/entity_service/entity_service/dda/comments/comment_post.py


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
flowchart LR
  Event[HTTP Event] --> Decorator{mandatory_lambda_handling(auth_check=AUTH_CHECK)}
  Decorator --> Lambda[lambda_handler(event, context, audit_refs)]
  Lambda --> DB[DB_CONN.establish_connection()]
  Lambda --> DPU[get_path_parameter(event, "dpuId")]
  Lambda --> Owner[auth.get_organization_id(event) ─ owner_id]
  DPU --> GetBodyFunc[get_body_data(dpu_id)]
  GetBodyFunc --> GetEventBody[get_event_body(event)]
  GetEventBody --> BuildBody[build body: {reference_id, system_name, reference_type, text, shared_with, batch_flag}]
  BuildBody --> BodyVar[body]
  BodyVar --> Payload[create payload: {requestContext, queryStringParameters, pathParameters:{owner_id}, body: json.dumps(body)}]
  Payload --> Print[print(payload)]
  Payload --> Copy[event_copy = deepcopy(event) → event_copy.update(payload)]
  Copy --> Invoke[invoke_lambda("comment_post", full_payload=event_copy) → response]
  Invoke --> UpdateDB[update_comments_count_number(DB_CONN.cursor, dpu_id, factor=1)]
  UpdateDB --> Return[return response]
  Invoke --> Return
```

> SVG rendering failed for this diagram.
