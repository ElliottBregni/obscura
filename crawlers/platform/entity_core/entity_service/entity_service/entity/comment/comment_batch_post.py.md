# Diagram: entity_core/entity_service/entity_service/entity/comment/comment_batch_post.py


> Auto-generated by Obscura crawlers

## Diagram 1

```mermaid
flowchart TD
LH[lambda_handler(event, context, audit_refs)] --> H1[get header: content-type]
H1 --> C1{content_type in [JSON, CSV]?}
C1 -->|No| ER1[raise BadRequestError -> make_error_response]
C1 -->|Yes| VBP[validate_batch_payload(event, content_type)]
VBP -->|Invalid| ER1
VBP -->|Valid| DEC{content_type == JSON?}
DEC -->|Yes| PJ[parse_comments_from_json(ids, event_body)]
DEC -->|No| PC[parse_comments_from_csv(event)]
PJ --> GB[get_batch_comment_payloads(event, comments)]
PC --> GB
GB --> INV[invoke_post_batch_message_lambda(event, payloads)]
INV --> INVL[invoke_lambda("post_batch_message") for each payload]
INVL --> GID[group_id returned]
GID --> RESP[make_response(body, status=202 Accepted)]
LH -->|exception| ER2[make_error_response Internal Server Error]
```

> SVG rendering failed for this diagram.

## Diagram 2

```mermaid
classDiagram
class ContentTypes {
  <<StringEnum>>
  +JSON = "application/json"
  +CSV = "text/csv"
}
class Constants {
  +DB_CONN = fv.db.FvDatabaseConnector(...)
  +MAX_PAYLOAD_SIZE = 250000
  +MAX_BATCH_LIMIT = 50000
  +MAX_COMMENT_LENGTH = 2000
}
class Utils {
  +validate_json_content(event)
  +validate_csv_content(event)
  +validate_batch_payload(event, content_type)
  +parse_comments_from_json(external_ids, event_data)
  +parse_comments_from_csv(event)
  +get_batch_comment_payloads(event, comments)
  +invoke_post_batch_message_lambda(event, payloads)
  +get_body_data(external_id, text, shared_with)
  +parse_csv_data(data)
  +get_csv_data_from_event(event)
  +lambda_handler(event, context, audit_refs)
}
ContentTypes <-- Utils : uses
Constants <-- Utils : reads
Utils --> fv.aws.lambdas : calls
Utils --> fv.error : raises
Utils --> auth : reads organization/auth data
Utils --> csv, uuid, sys, logging : util modules
```

> SVG rendering failed for this diagram.
