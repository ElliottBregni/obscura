# Diagram: entity_core/entity_service/entity_service/entity/forecasted_capacity/check_forecasted_query_status.py


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
flowchart TD
    Start([Start]) --> ReadEvent[Read event and extract query_ids]
    ReadEvent --> Init[Set all_queries_completed = true<br/>Set all_queries_successful = true]
    Init --> ForLoop{More query_ids?}
    ForLoop -->|yes| GetExec[Call Athena get_query_execution(query_id)]
    GetExec --> GetStatus[status = response.QueryExecution.Status.State]
    GetStatus --> IsFinished{status in FINISHED_STATES?}
    IsFinished -->|no| SetPending[Set event.queryStatus = "PENDING"]
    SetPending --> ReturnPending([Return event])
    IsFinished -->|yes| IsSucceeded{status == "SUCCEEDED"?}
    IsSucceeded -->|no| MarkNotAllSuccess[Set all_queries_successful = false]
    MarkNotAllSuccess --> NextQuery[Continue to next query]
    IsSucceeded -->|yes| NextQuery
    NextQuery --> ForLoop
    ForLoop -->|no| AfterLoop{all_queries_completed?}
    AfterLoop -->|yes| SetCompleted[Set event.queryStatus = "COMPLETED"<br/>Set event.succeeded = all_queries_successful]
    SetCompleted --> ReturnCompleted([Return event])
    AfterLoop -->|no| SetPending2[Set event.queryStatus = "PENDING"]
    SetPending2 --> ReturnPending2([Return event])
```

> SVG rendering failed for this diagram.
