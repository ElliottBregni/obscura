# Diagram: entity_core/entity_service/entity_service/entity/entity_location/get_entity_location.py


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
flowchart TD
  LH(lambda_handler):::func --> PP[get_path_parameter(solution_id, location_id)]
  PP --> AUD[audit_refs.update({Searchable_Ids.SOLUTION_ID: solution_id})]
  AUD --> DB[DB_CONN.establish_connection()]
  DB --> CURS[cursor = DB_CONN.cursor]
  CURS --> DEC{location_id is None?}
  DEC -->|yes| SEARCH[search_funcs.search_entity_location(event, solution_id)]
  DEC -->|no| GETDET[get_ent_loc_details(event, location_id, solution_id)]
  GETDET --> INV[invoke_get_location(event, path_params={id: location_id}, ...)]
  INV --> LOCFOUND{location returned?}
  LOCFOUND -->|no| NF[raise fv.error.NotFoundError(message)]
  LOCFOUND -->|yes| CHILD[child_ids = location.get("children_locations", [])]
  GETDET --> ORG[auth.get_user_org_types(event)]
  GETDET --> CARRIER[search_funcs.get_non_shipper_org_id(event)]
  LOCFOUND --> RECD[record_details = search_funcs.get_details(solution_id, [location_id], carrier_org_id, org_types)]
  RECD --> EXTRACT[extract dwelling/aging/capacity/non_shippable/on_site/is_manager]
  EXTRACT --> CALC[percent_fill = round((on_site_count / capacity)*100,2) if capacity != 0 else 0]
  CALC --> POP[populate location.capacity, location.dwell, location.nonShippable, location.is_manager]
  POP --> IDTO[id_to_locationId(location)]
  IDTO --> RETLOC[return location]
  SEARCH --> RESPCHK
  RETLOC --> RESPCHK
  RESPCHK{response is None?}
  RESPCHK -->|yes| BR[raise fv.error.BadRequestError("Bad request")]
  RESPCHK -->|no| MAKE[fv.aws.lambdas.make_response(response)]
  MAKE --> END((return response))
  subgraph Helpers
    IDFUNC[id_to_locationId: replace "id" with "locationId" preserving insertion order]
  end
  IDTO --> IDFUNC

  classDef func fill:#f9f,stroke:#333,stroke-width:1px;
```

> SVG rendering failed for this diagram.
