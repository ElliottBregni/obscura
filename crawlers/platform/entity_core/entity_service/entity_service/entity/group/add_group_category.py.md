# Diagram: entity_core/entity_service/entity_service/entity/group/add_group_category.py


> Auto-generated by Obscura crawlers

## Diagram 1

```mermaid
flowchart LR
    LH[lambda_handler] --> GQ[get_group(group_type)]
    GQ -->|None| BR[Raise BadRequest]
    GQ -->|Exists| DEC{path contains "/batch"?}
    DEC -->|Yes| BATCH[add_batch_group_category]
    DEC -->|No| SINGLE[add_group_category]
    BATCH --> UID[get_user_id(event)]
    BATCH --> OID[get_context_val(organization_id)]
    BATCH --> EXC_TYPE[get_exception_type_id("ITSS")]
    BATCH --> CHECK[check_key(categories) & check_key(entities)]
    CHECK -->|missing| BR
    CHECK --> VALIDATE[validate categories -> get_group_catelog_entity]
    VALIDATE --> ENT_LOOP[for each entity: get_entity_id]
    ENT_LOOP -->|NotFound| ADD_ENTITY[add_entity(...)]
    ENT_LOOP --> EX_SET[set_exception -> maybe get_exception]
    EX_SET --> CAT_ENTITY[get_category_entity(...)]
    CAT_ENTITY --> PERSIST_ENTITY[db_group_entity.persist(...)]
    PERSIST_ENTITY --> RESP_APPEND[append "Category ... Entity ... added"]
    RESP_APPEND --> MAKE_RESP[make_response(response_body,200)]
    SINGLE --> SERVICE_CFG[get serviceConfig]
    SINGLE --> METHOD_DEC{httpMethod == POST?}
    METHOD_DEC -->|POST| REQ_CAT[check_key(body,"category") or raise BadRequest]
    METHOD_DEC -->|PUT| PATH_CAT[get_path_parameter(group_category) -> get_category_entity or raise BadRequest]
    SINGLE --> AUDIT[audit_refs update]
    SINGLE --> UID2[get_user_id(event)]
    SINGLE --> REFS[get_references(body)]
    SINGLE --> FROZEN_INACTIVE[get_fozen_inactive(body, service_config, group_category_entity)]
    SINGLE --> PERSIST_CAT[db_group_category.persist_catelog(...)]
    PERSIST_CAT --> MAKE_RESP
```

> SVG rendering failed for this diagram.

## Diagram 2

```mermaid
sequenceDiagram
    participant Client
    participant Lambda as lambda_handler
    participant DBAccess as db_access
    participant DBGroupCat as db_group_category
    participant DBGroupEnt as db_group_entity
    participant DB as database

    Client->>Lambda: HTTP POST/PUT /solution/{solution_id}/group-type/{group_type}...
    Lambda->>DBAccess: get_group(group_type)
    DBAccess-->>Lambda: group_entity / None
    alt group_entity missing
        Lambda->>Client: raise BadRequestError
    else group_entity exists
        Lambda->>Lambda: determine batch vs single (path contains /batch)
        alt batch path
            Lambda->>DBAccess: get_exception_type_id("ITSS")
            Lambda->>DBAccess: check_key(body,"categories") & check_key(body,"entities")
            DBAccess-->>Lambda: categories, entities
            loop validate categories
                Lambda->>DBAccess: get_group_catelog_entity(cursor, group_type, group_category, solution_id...)
                DBAccess-->>Lambda: group_category_entity
            end
            loop for each entity
                Lambda->>DBAccess: get_entity_id(solution_id, vin)
                alt NotFound
                    DBAccess-->>Lambda: NotFoundError
                    Lambda->>DBAccess: add_entity(event, entity_external_id, solution_id, org_id)
                end
                Lambda->>DBAccess: set_exception(event, entity_external_id, solution_id, org_id, category, ...)
                DBAccess-->>Lambda: exception_id, message
                Lambda->>DBAccess: get_category_entity(...)
                DBAccess-->>Lambda: group_category_entity
                Lambda->>DBGroupEnt: persist(group_category_id, solution_id, entity_id, actor_id, ...)
                DBGroupEnt-->>Lambda: persisted
            end
            Lambda->>Client: make_response(response_body, 200)
        else single (non-batch)
            opt POST
                Lambda->>Lambda: validate body contains "category" or raise BadRequest
            end
            opt PUT
                Lambda->>DBAccess: get_category_entity(cursor, group_type, group_category, solution_id)
                DBAccess-->>Lambda: group_category_entity or None
                alt None
                    Lambda->>Client: raise BadRequestError
                end
            end
            Lambda->>Lambda: audit_refs update; get_references; get_fozen_inactive
            Lambda->>DBGroupCat: persist_catelog(cursor, solution_id, group_type, category, ...)
            DBGroupCat-->>Lambda: response
            Lambda->>Client: make_response(response, 200)
        end
    end
```

> SVG rendering failed for this diagram.
