# Diagram: entity_core/entity_service/entity_service/entity/group/update_group_entity.py


> Auto-generated by Obscura crawlers

## Diagram 1

```mermaid
flowchart TD
  Event[HTTP Event (POST/DELETE)] --> Lambda[lambda_handler]
  Lambda --> DBConn[DB_CONN.establish_connection()]
  Lambda --> Params[Get path params: vin, solution_id, group_type, group_category]
  Params --> Audit[audit_refs.update(...)]
  Audit --> CheckEntity{entity_id = db_access.get_entity_id(...)}
  CheckEntity -- null --> BadReq[BadRequestError: "Invalid VIN"]
  CheckEntity -- exists --> CheckCategory{group_category provided?}
  CheckCategory -- yes --> RemoveDirect[remove_entity_id(...)] --> Resp1[make_response(response, 200)]
  CheckCategory -- no --> GetGroup[db_group_entity.get_group_entity(...)] --> ForEach[for each entity in entity_list]
  ForEach --> RemoveLoop[remove_entity_id(...)]
  RemoveLoop --> Collected[collect category into list]
  Collected --> Resp2[make_response({"Removed": category_list}, 200)]
```

> SVG rendering failed for this diagram.

## Diagram 2

```mermaid
sequenceDiagram
  participant Client
  participant Lambda as LambdaHandler
  participant Auth as AuthModule
  participant DBConn as DB_CONN
  participant DBGroup as DBGroupEntity
  participant DBAccess as DBAccess
  participant AwsUtil as AWSLambda
  participant Log as Logger

  Client->>Lambda: HTTP request / event
  Lambda->>DBConn: establish_connection()
  Lambda->>AwsUtil: get_path_parameter(vin, solution_id, group_type, group_category)
  Lambda->>Log: info("add group entity event")
  Lambda->>DBAccess: get_entity_id(solution_id, vin)
  alt entity not found
    DBAccess-->>Lambda: None
    Lambda-->>Client: raise BadRequestError("Invalid VIN")
  else entity found
    DBAccess-->>Lambda: entity_id
    alt group_category provided
      Lambda->>DBAccess: get_category_entity(group_type, group_category, solution_id)
      DBAccess-->>Lambda: group_category_entity
      Lambda->>DBGroup: get_exception_id(solution_id, vin, group_type, group_category)
      DBGroup-->>Lambda: exception_id
      alt HTTP DELETE
        Lambda->>DBGroup: persist(..., event_time, event_comments, ...)
        DBGroup-->>Lambda: persist_response
      else not DELETE
        DBGroup-->>Lambda: {}
      end
      alt exception_id exists
        Lambda->>DBAccess: clear_exception(event, vin, solution_id, organization_id, group_type, exception_id, event_comments, event_time, group_category)
      end
      Lambda-->>Client: make_response(response, 200)
    else no group_category
      Lambda->>DBGroup: get_group_entity(solution_id, vin, group_type, category=None, ...)
      DBGroup-->>Lambda: entity_list
      loop for each entity in entity_list
        Lambda->>DBAccess: get_category_entity(group_type, entity.category, solution_id)
        Lambda->>DBGroup: get_id(solution_id, vin, group_type, entity.category)
        alt id not found and frozen
          DBGroup-->>Lambda: None
          Lambda-->>Client: raise BadRequestError("Entity ID not found in category")
        else
          Lambda->>DBGroup: persist(... DELETE ...)
        end
      end
      Lambda-->>Client: make_response({"Removed": [...]}, 200)
  end
```

> SVG rendering failed for this diagram.
