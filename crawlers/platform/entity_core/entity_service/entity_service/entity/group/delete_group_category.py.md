# Diagram: entity_core/entity_service/entity_service/entity/group/delete_group_category.py


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
flowchart TD
  Start([HTTP DELETE /solution/{solution_id}/group-type/{group_type}/group-category/{group_category}])
  AuthCheck[/mandatory_lambda_handling(auth_check=AUTH_CHECK)/]
  EstablishDB[/DB_CONN.establish_connection()/]
  Log[/logging.info("add group entity event")/]
  GetPathParams[/get_path_parameter(solution_id, group_type, group_category)/]
  UpdateAudit[/audit_refs.update(Searchable_Ids.GROUP_CATEGORY, GROUP_TYPE, SOLUTION_ID)/]
  GetGroupEntity[/db_access.get_group(cursor, group_type)/]
  ValidateGroup{group_entity == None?}
  BadRequestGroup[/raise BadRequestError("Invalid group")/]
  DecodeCategory[/group_category = urllib.parse.unquote(group_category)/]
  GetCategoryEntity[/db_access.get_category_entity(cursor, group_type, group_category, solution_id)/]
  ValidateCategory{group_category_entity == None?}
  BadRequestCategory[/raise BadRequestError("Invalid group category")/]
  TryDelete[/db_group_category.delete_catelog(cursor, solution_id, group_type, category)/]
  ReturnResp[/fv.aws.lambdas.make_response(response, 200)/]
  ExceptFlow{Exception during delete?}
  TracePrint[/traceback.print_exc(); get sys.exc_info()/]
  RaiseDBErr[/raise fv.error.DatabaseError(...).with_traceback(trace_back)/]

  Start --> AuthCheck --> EstablishDB --> Log --> GetPathParams --> UpdateAudit --> GetGroupEntity --> ValidateGroup
  ValidateGroup -- "no (found)" --> DecodeCategory
  ValidateGroup -- "yes (missing)" --> BadRequestGroup
  DecodeCategory --> GetCategoryEntity --> ValidateCategory
  ValidateCategory -- "no (found)" --> TryDelete
  ValidateCategory -- "yes (missing)" --> BadRequestCategory
  TryDelete -- "success" --> ReturnResp
  TryDelete -- "exception" --> ExceptFlow
  ExceptFlow --> TracePrint --> RaiseDBErr
```

> SVG rendering failed for this diagram.
