# Diagram: entity_core/entity_service/entity_service/entity/entity/remove_entity.py


> Auto-generated by Obscura crawlers

## Diagram 1

```mermaid
flowchart TD
  Event[HTTP Event] --> LH[lambda_handler(event, context, audit_refs)]
  LH --> GP1[get_path_parameter(event, "entity_id")]
  LH --> GP2[get_path_parameter(event, "solution_id")]
  LH --> UpdateAudit[audit_refs.update({ENTITY_ID, SOLUTION_ID})]
  LH --> DBConn[DB_CONN.establish_connection()]
  DBConn --> Cursor[DB_CONN.cursor]
  Cursor --> Delete[entity_service.db.entity.delete_entity(cursor, solution_id, entity_id, event)]
  Delete --> Check{retval == None?}
  Check -- Yes --> NotFoundErr[raise fv.error.NotFoundError(...)]
  Check -- No --> Produce[ProduceEntityToSQS().send(payload)]
  Produce --> Resp[fv.aws.lambdas.make_response(retval, 200)]
  Resp --> Return[return response]
  LH --> CatchNameError[except NameError -> raise NotFoundError with traceback]
  LH --> CatchDBError[except psycopg2.Error -> raise fv.error.DatabaseError with traceback]
```

> SVG rendering failed for this diagram.

## Diagram 2

```mermaid
sequenceDiagram
  participant Client
  participant Lambda as lambda_handler
  participant DB as DB_CONN
  participant EntityDB as entity_service.db.entity
  participant SQS as ProduceEntityToSQS
  participant FV as fv.aws.lambdas
  Client->>Lambda: HTTP DELETE /solutions/{id}/entities/{id} (event, context, audit_refs)
  Lambda->>Lambda: logging.info(event)
  Lambda->>FV: get_path_parameter(event, "entity_id")
  Lambda->>FV: get_path_parameter(event, "solution_id")
  Lambda->>Lambda: audit_refs.update(...)
  Lambda->>DB: establish_connection()
  Lambda->>EntityDB: delete_entity(cursor, solution_id, entity_id, event)
  alt retval == None
    EntityDB-->>Lambda: None
    Lambda-->>Client: raise NotFoundError
  else retval exists
    EntityDB-->>Lambda: retval
    Lambda->>SQS: send({"solutionId", "entityId", "httpMethod"})
    Lambda->>FV: make_response(retval, 200)
    FV-->>Client: 200 OK with retval
```

> SVG rendering failed for this diagram.
