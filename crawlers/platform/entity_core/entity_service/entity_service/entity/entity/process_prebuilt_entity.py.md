# Diagram: entity_core/entity_service/entity_service/entity/entity/process_prebuilt_entity.py


> Auto-generated by Obscura crawlers

## Diagram 1

```mermaid
flowchart TD
  LH[lambda_handler(event, context, audit_refs)] --> DB[DB_CONN.establish_connection()]
  DB --> PROC[process_prebuilt_entity(cursor, event)]
  PROC --> EB[get_event_body(event)]
  PROC --> SP[get_path_parameter(solution_id, entity_id)]
  PROC --> OR[get_order_number_from_event(event_body)]
  PROC --> SC[get_prebuilt_status_codes(cursor, solution_id)]
  PROC --> PE[get_entity_linked_to_order(cursor, order_number, solution_id)]
  PE -->|missing| E1[BadRequestError: "entity linked to order doesn't exist"]
  PE -->|found| REID[get_entity_id(cursor, solution_id, real_vin_external_id)]
  REID -->|missing| E2[BadRequestError: "entity doesn't exist"]
  REID -->|found| CE[construct real_entity & proxy_entity Entities]
  CE --> CH{status_update_code in assignment_codes?}
  CH -- yes --> U1[unlink_proxy_or_real_entity(cursor,event,real,proxy)]
  U1 --> LNK[link_prebuilt_entity(cursor,event,solution_id,real,proxy)]
  LNK --> R1[return "The order is merged to <real_vin_external_id>"]
  CH -- no --> CH2{status_update_code in unassignment_codes?}
  CH2 -- yes --> CSTATE{proxy_entity_data.state == VinState.LINKED?}
  CSTATE -- no --> E3[BadRequestError: "The order is not assigned yet"]
  CSTATE -- yes --> UNL[unlink_prebuilt_entity(cursor,event,real,proxy,linked_data)]
  UNL --> R2[return "The order <order_number> is unassigned"]
  CH2 -- no --> E4[BadRequestError: "Invalid status update code"]
```

> SVG rendering failed for this diagram.

## Diagram 2

```mermaid
flowchart TD
  LSTART[link_prebuilt_entity(cursor,event,solution_id,real_entity,proxy_entity)] --> RREFS[get_refs_for_entity(cursor, proxy_entity.id, solution_id)]
  RREFS --> COPY[copy_entity_references(cursor, real_entity.id, proxy_refs) --> entity_reference_ids]
  LSTART --> PPL[has_planned_trip_legs(cursor, proxy_entity.id, solution_id) --> proxy_planned_trip_legs?]
  LSTART --> RPL[has_planned_trip_legs(cursor, real_entity.id, solution_id) --> real_planned_trip_legs?]
  PPL -->|true & not real_planned_trip_legs| MOVE_LEGS[move_entity_planned_trip_legs(cursor, proxy_entity, real_entity) --> entity_planned_trip_leg_ids]
  LSTART --> MOVE_STATUS[move_status_updates(cursor, proxy_entity.id, real_entity.id) --> status_update_ids]
  LSTART --> MOVE_EVENTS[move_entity_events(cursor, proxy_entity.id, real_entity.id, BATCH_SIZE) --> event_ids]
  LSTART --> HVG[has_visibility_grant(cursor, real_entity.id) --> has_visibility?]
  HVG -- false --> MOVE_VIS[move_visibility_grants(cursor, proxy_entity.id, real_entity.id) --> visibility_grant_ids]
  MOVE_VIS --> CREATE_LOOKUP[create_real_entity_lookup(cursor, proxy_entity.id, real_entity.id, linked_data)]
  MOVE_LEGS --> CREATE_LOOKUP
  MOVE_STATUS --> CREATE_LOOKUP
  MOVE_EVENTS --> CREATE_LOOKUP
  CREATE_LOOKUP --> MARK_LINKED[mark_entity_as_linked(cursor, proxy_entity.id)]
  MARK_LINKED --> PROC_DESC[process_description_and_source_for_linking(...)]
  PROC_DESC --> RECALC[handle_entity_data_to_recalculate_eta(event, EntityReplayETARepository(cursor), real_entity.external_id, real_entity.solution_id, ReplayETAQueuePublisher())]
  RECALC --> LEND[linked -> done]
```

> SVG rendering failed for this diagram.

## Diagram 3

```mermaid
flowchart TD
  USTART[unlink_prebuilt_entity(cursor,event,real_entity,proxy_entity,linked_data)] --> NORM[ensure linked_data is dict]
  NORM --> REAL_REFS[get_refs_for_entity(cursor, real_entity.id, real_entity.solution_id) --> real_entity_refs]
  NORM --> ER_IDS[entity_reference_ids = linked_data.entity_reference_ids]
  ER_IDS -->|present| DEL_REFS[delete_entity_references(cursor, real_entity.id, entity_reference_ids)]
  NORM --> ET_LEGS[entity_planned_trip_leg_ids = linked_data.entity_planned_trip_leg_ids]
  ET_LEGS -->|present| MOVE_LEGS_BACK[move_entity_planned_trip_legs(cursor, real_entity, proxy_entity, entity_planned_trip_leg_ids)]
  NORM --> SUIDS[status_update_ids = linked_data.status_update_ids]
  SUIDS -->|present| MOVE_STATUS_BACK[move_status_updates(cursor, real_entity.id, proxy_entity.id, status_update_ids)]
  NORM --> EIDS[event_ids = linked_data.event_ids]
  EIDS -->|present| MOVE_EVENTS_BACK[move_entity_events(cursor, real_entity.id, proxy_entity.id, BATCH_SIZE, event_ids)]
  NORM --> VIDS[visibility_grant_ids = linked_data.visibility_grant_ids]
  VIDS -->|present| MOVE_VIS_BACK[move_visibility_grants(cursor, real_entity.id, proxy_entity.id, visibility_grant_ids)]
  MOVE_EVENTS_BACK --> DEL_LOOKUP[delete_real_entity_lookup(cursor, proxy_entity.id, real_entity.id)]
  MOVE_VIS_BACK --> DEL_LOOKUP
  DEL_LOOKUP --> MARK_PREBUILT[mark_entity_as_prebuilt(cursor, proxy_entity.id)]
  MARK_PREBUILT --> PROC_DESC_UNLINK[process_description_and_source_for_unlinking(cursor, real_entity, real_entity_refs)]
  PROC_DESC_UNLINK --> RECALC_UNLINK[handle_entity_data_to_recalculate_eta(event, EntityReplayETARepository(cursor), real_entity.external_id, real_entity.solution_id, ReplayETAQueuePublisher())]
  RECALC_UNLINK --> UEND[unlinked -> done]
```

> SVG rendering failed for this diagram.
