# Diagram: entity_core/entity_service/entity_service/entity/admin_tool/manual_eta_range/manual_eta_range_request.py


> Auto-generated by Obscura crawlers

## Diagram 1

```mermaid
classDiagram
class ManualEtaRequestEvent {
  +solution_id
  +valid_eta_ranges: list~ManualETARange~
  +validation_errors: dict
  +__init__(solution_id=None, valid_eta_ranges=None, validation_errors=None)
  +_get_deduplicated_data(csv_data)
  +_validate_and_parse_csv_data(csv_data, validation_errors)
  +parse_event(event)
  +_parse_csv_data(event)
}
class ManualETARange
class ManualETARangeCSVRowValidation {
  +is_valid()
  +error_msg
}
class get_ordered_dict_array <<function>>
class get_path_parameter <<function>>
class BadRequestError <<exception>>
ManualEtaRequestEvent --> ManualETARange : instantiates
ManualEtaRequestEvent --> ManualETARangeCSVRowValidation : validates with
ManualEtaRequestEvent ..> get_ordered_dict_array : calls
ManualEtaRequestEvent ..> get_path_parameter : calls
ManualEtaRequestEvent ..> BadRequestError : may raise
```

> SVG rendering failed for this diagram.

## Diagram 2

```mermaid
flowchart TD
PE[parse_event(event)] --> GP[get_path_parameter(event, "solution_id")]
PE --> PC[_parse_csv_data(event)]
PC --> BOD{body present?}
BOD -- No --> ERR[BadRequestError: "The CSV file does not contain any data!"]
BOD -- Yes --> ORD[get_ordered_dict_array(data)]
ORD --> DED[_get_deduplicated_data(csv_data)]
DED --> VAL[_validate_and_parse_csv_data(cleaned_data, validation_errors)]
VAL --> RES[valid_eta_ranges + validation_errors]
RES --> CRT[ManualEtaRequestEvent(solution_id, valid_eta_ranges, validation_errors)]
```

> SVG rendering failed for this diagram.
