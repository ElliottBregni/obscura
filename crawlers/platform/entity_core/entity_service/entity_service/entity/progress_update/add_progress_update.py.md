# Diagram: entity_core/entity_service/entity_service/entity/progress_update/add_progress_update.py


> Auto-generated by Obscura crawlers

## Diagram 1

```mermaid
sequenceDiagram
participant API as API_Gateway
participant Lambda as Lambda
participant DB as Database
participant ES as EntityService
participant FV as FV_Lambdas
participant INV as Invokinator
participant SNS as SNS_Topic

API->>Lambda: POST /solutions/{solution_id}/entity/{entity_id}/progress-update (event)
Lambda->>Lambda: extract path params (solution_id, entity_id, actual_trip_leg_id) and auth
Lambda->>DB: get_entities_by_external_ids(solution_id, [vin])
DB-->>Lambda: entity record or empty
alt entity not found
Lambda-->>API: raise BadRequestError("Entity Id ... doesn't exist")
else entity found
Lambda->>ES: get_trip_leg(cursor, solution_id, ext_actual_trip_leg_id) [if provided]
ES-->>Lambda: trip_leg or null
alt trip leg not found
Lambda-->>API: raise BadRequestError("Trip leg doesn't exist")
end
Lambda->>Lambda: get_event_body(event) and validate schema (Schema_Validator.PROGRESS_UPDATE_ADD)
alt invalid body / schema error
Lambda-->>API: raise BadRequestError("Invalid request parameters" / missing body)
else valid body
Lambda->>Lambda: format_date for datetime, destinationEta*, create_progress_dict(body)
opt position present
Lambda->>FV: invoke_lambda("add_entity_position_update", full_payload=event_copy)
FV-->>Lambda: position_update_results (statusCode)
Lambda->>Lambda: log error if statusCode not 2xx
end
Lambda->>DB: add_progress_update(trip_leg_id, progress_payload, position_payload, cursor, entity_id)
DB-->>Lambda: progress_id
Lambda->>ES: get_progress_update_by_entity_id(entity_id, progress_id, cursor)
ES-->>Lambda: progress_update
Lambda->>Lambda: build_fv_event_json(source=PROGRESS_UPDATE, id=progress_id, event_ts, event_code)
Lambda->>INV: get_solution(event, solution_id)
INV-->>Lambda: solution (feature_name)
Lambda->>FV: invoke_add_event(event, solution_id, fv_event, entity_id=vin)
alt update_on_entity conditions true
Lambda->>ES: update_entity_last_progress(cursor, [vin], solution_id, progress_update, ...)
end
Lambda->>ES: send_to_entity_progress_updated_sns_topic(progress_update, vin, solution_id)
Lambda->>Lambda: cull_dictionary_nulls(progress_update)
Lambda-->>API: make_response(json_dumps(progress_update), status_code=201)
end
```

> SVG rendering failed for this diagram.

## Diagram 2

```mermaid
flowchart TD
Start([API POST /progress-update]) --> GetParams{Extract path params}
GetParams --> CheckEntity[DB: get_entities_by_external_ids(solution_id, [vin])]
CheckEntity -->|not found| ErrEntity[[BadRequest: Entity Id doesn't exist]]
CheckEntity -->|found| MaybeTripLeg{actual_trip_leg_id provided?}
MaybeTripLeg -->|yes| GetTripLeg[DB: get_trip_leg(solution_id, ext_actual_trip_leg_id)]
GetTripLeg -->|not found| ErrTripLeg[[BadRequest: Trip leg doesn't exist]]
GetTripLeg -->|found| GetBody[Get event body]
MaybeTripLeg -->|no| GetBody
GetBody -->|empty| ErrBody[[BadRequest: must have a request body]]
GetBody -->|present| Validate[validate schema: PROGRESS_UPDATE_ADD]
Validate -->|invalid| ErrSchema[[BadRequest: Invalid request parameters]]
Validate -->|valid| FormatDates[format_date: datetime, destinationEta, destinationEtaStart, destinationEtaEnd]
FormatDates --> CreateProgress[create_progress_dict(body)]
CreateProgress --> PositionDec{position present?}
PositionDec -->|yes| InvokePosition[fv.aws.lambdas.invoke_lambda add_entity_position_update]
InvokePosition --> PositionResult[log error if statusCode not 2xx]
PositionDec -->|no| SkipPosition
PositionResult --> AddProgress[DB: add_progress_update(...)]
SkipPosition --> AddProgress
AddProgress --> GetProgress[DB: get_progress_update_by_entity_id(entity_id, progress_id)]
GetProgress --> BuildEvent[build_fv_event_json(PROGRESS_UPDATE)]
BuildEvent --> GetSolution[invokinator.get_solution(event, solution_id)]
GetSolution --> InvokeAddEvent[invoke_add_event(event, solution_id, fv_event)]
InvokeAddEvent --> UpdateCondition{UPDATE_ON_ENTITY env & not ext_actual_trip_leg_id & not override ETA}
UpdateCondition -->|true| PrepareProgress[set destinationEtaStart/End on progress_update]
PrepareProgress --> UpdateEntity[entity.update_entity_last_progress(...)]
UpdateCondition -->|false| SkipUpdate
UpdateEntity --> SNS[send_to_entity_progress_updated_sns_topic(progress_update)]
SkipUpdate --> SNS
SNS --> CullNulls[cull_dictionary_nulls(progress_update)]
CullNulls --> Return[make_response(progress_update, 201)]
```

> SVG rendering failed for this diagram.
