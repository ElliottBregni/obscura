# Diagram: entity_core/entity_service/entity_service/dpu/dpu_service/lambdas/dpu_vin_history.py


> Auto-generated by Obscura crawlers

## Diagram 1

```mermaid
flowchart TD
  Event([Incoming event]) --> Decorator[mandatory_lambda_handling<br/>(auth_check=AUTH_CHECK)]
  Decorator --> Lambda[lambda_handler(event, context, audit_refs)]
  Lambda --> Parse[DPUVinHistoryEventParser.parse_event(event)]
  Lambda --> Instantiate[create DPUVinHistoryHandler(daos=DAOContainer(DB_CONN))]
  Parse --> CallHandle[handler.handle_dpu_vin_history(request)]
  Instantiate --> Handler[DPUVinHistoryHandler]
  Handler --> CallHandle
  CallHandle --> MakeResp[make_response(response)]
  MakeResp --> Response([HTTP response])
  AUTH_CHECK[AUTH_CHECK (auth.AuthType.PRIVILEGE -> Privilege.FV_ADMIN_TOOL)] -.-> Decorator
  DB_CONN[FvDatabaseConnector("dpu_vin_history", SecretNames.ENTITY_DATABASE)] --> DAOContainer[DAOContainer]
  DAOContainer --> Handler
```

> SVG rendering failed for this diagram.

## Diagram 2

```mermaid
classDiagram
  class FvDatabaseConnector {
    +__init__(db_name, secret_name, auto_connect=False)
    +connect()
  }
  class DAOContainer {
    +__init__(db_conn)
    +get_dao(name)
  }
  class DPUVinHistoryHandler {
    +__init__(daos)
    +handle_dpu_vin_history(request)
  }
  class DPUVinHistoryEventParser {
    +parse_event(event)
  }
  class lambda_handler <<function>> {
    +lambda_handler(event, context, audit_refs)
  }
  class mandatory_lambda_handling <<decorator>>
  class make_response <<function>>
  class auth <<module>>
  class SecretNames <<enum>>
  class DAOContainerUsage <<utility>>

  DPUVinHistoryHandler --> DAOContainer : uses
  DAOContainer --> FvDatabaseConnector : holds connection to
  lambda_handler --> DPUVinHistoryEventParser : calls
  lambda_handler --> DPUVinHistoryHandler : instantiates
  lambda_handler --> make_response : calls
  mandatory_lambda_handling <|-- lambda_handler : wraps
  mandatory_lambda_handling --> auth : enforces auth_check
  lambda_handler --> SecretNames : references
  DAOContainerUsage ..> DAOContainer : helper for DAOs
```

> SVG rendering failed for this diagram.
