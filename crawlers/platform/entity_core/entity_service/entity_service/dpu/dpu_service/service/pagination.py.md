# Diagram: entity_core/entity_service/entity_service/dpu/dpu_service/service/pagination.py


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
flowchart LR
  subgraph ResponsePagination
    A1[get_response_pagination(total_count, page_size, page_number)]
    A2[total_pages = ceil(total_count / page_size)]
    A3[return { MetaFields.CURRENT_PAGE: page_number, MetaFields.TOTAL_COUNT: total_count, MetaFields.TOTAL_PAGES: total_pages }]
    A1 --> A2 --> A3
  end
  subgraph QueryPagination
    B1[get_query_pagination(page_size, page_number)]
    B2[offset = page_number * page_size]
    B3[limit = page_size]
    B4[return (offset, limit)]
    B1 --> B2 --> B3 --> B4
  end
```

> SVG rendering failed for this diagram.
