# Diagram: entity_core/entity_service/entity_service/dpu/scripts/backfill_dpu_deny_reason_codes.py


> Auto-generated by Obscura crawlers

## Diagram 1

```mermaid
classDiagram
    class DPUDenyReasonCodesProcessor {
        +cursor
        +csv_file
        +solution_id
        +process_csv_file()
        +insert_deny_reason_codes(reason, reason_code, solution_id, explanation)
    }
    class FvDatabaseConnector {
        +cursor
    }
    class DB_CONN_ENTITY <<instance>> {
    }
    class parse_arguments {
        +()
    }
    class main {
        +()
    }
    DPUDenyReasonCodesProcessor --> DB_CONN_ENTITY : uses
    DB_CONN_ENTITY --|> FvDatabaseConnector : instanceOf
    main --> DPUDenyReasonCodesProcessor : creates
    main --> parse_arguments : calls
```

> SVG rendering failed for this diagram.

## Diagram 2

```mermaid
flowchart TD
    Start([Start]) --> ParseArgs[parse_arguments()]
    ParseArgs --> Main[main()]
    Main --> Instantiate[Create DPUDenyReasonCodesProcessor]
    Instantiate --> OpenCSV[Open CSV file (csv.DictReader)]
    OpenCSV --> ForEach{More rows?}
    ForEach --> Row[Read row]
    Row --> Check{reason and reason_code?}
    Check -- Yes --> Insert[insert_deny_reason_codes(reason, reason_code, solution_id, explanation)]
    Insert --> ExecuteSQL[Cursor.execute(SQL, params)]
    ExecuteSQL --> ForEach
    Check -- No --> ForEach
    ForEach -- No --> End([End])
```

> SVG rendering failed for this diagram.
