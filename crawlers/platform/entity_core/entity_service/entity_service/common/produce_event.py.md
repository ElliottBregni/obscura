# Diagram: entity_core/entity_service/entity_service/common/produce_event.py


> Auto-generated by Obscura crawlers

## Diagram 1

```mermaid
flowchart TD
    Start([Start]) --> Check{StreamEnabledDAO.is_entity_event_stream_enabled_for_org(solution_id)}
    Check -- No --> End([Exit — no streaming])
    Check -- Yes --> CreatePub[Create EntityEventPublisher instance]
    CreatePub --> Switch{http_method}
    Switch -- "POST" --> POSTCall[request_event_api.post(path=ENTITY_EVENT_CREATE_PATH.replace("solutionId", solution_id), body=entity)]
    Switch -- "PUT" --> PUTCall[request_event_api.put(path=ENTITY_EVENT_UPDATE_OR_DELETE_PATH.replace("solutionId", solution_id).replace("entityId", entity_id), body=entity)]
    Switch -- "DELETE" --> DELETECall[request_event_api.delete(path=ENTITY_EVENT_UPDATE_OR_DELETE_PATH.replace("solutionId", solution_id).replace("entityId", entity_id))]
    Switch -- "PATCH" --> PATCHCall[request_event_api.patch(path=ENTITY_EVENT_UPDATE_OR_DELETE_PATH.replace("solutionId", solution_id).replace("entityId", entity_id), body=entity)]
    POSTCall --> Log[logging.info("Entity Event Process Response", event_return_value)]
    PUTCall --> Log
    DELETECall --> Log
    PATCHCall --> Log
    Log --> End
```

> SVG rendering failed for this diagram.

## Diagram 2

```mermaid
classDiagram
    class StreamEnabledDAO {
        +is_entity_event_stream_enabled_for_org(solution_id)
    }
    class EntityEventPublisher {
        +post(path, body)
        +put(path, body)
        +delete(path)
        +patch(path, body)
    }
    class produce_event_to_queue {
        +produce_event_to_queue(cursor, entity, solution_id, http_method, entity_id=None)
        -event_return_value
        -stream_enabled_dao
        -request_event_api
    }
    produce_event_to_queue --> StreamEnabledDAO : uses
    produce_event_to_queue --> EntityEventPublisher : creates/uses
    produce_event_to_queue ..
```

> SVG rendering failed for this diagram.
