# Diagram: entity_core/entity_service/entity_service/common/dictionary_tools.py


> Auto-generated by Obscura crawlers

## Diagram 1

```mermaid
flowchart LR
start1([Start cull_nulls(json)]) --> loop1{For each entry in list(json.keys())}
loop1 --> cond1{json[entry] is None?}
cond1 -- Yes --> del1[Delete entry: del json[entry]] --> loop1
cond1 -- No --> loop1
loop1 -- done --> end1([End — implicit return None])
```

> SVG rendering failed for this diagram.

## Diagram 2

```mermaid
flowchart LR
start2([Start cull_dictionary_nulls(dictionary)]) --> init2[retval = {}]
init2 --> loop2{For each (key, value) in dictionary.items()}
loop2 --> cond2{value is not None?}
cond2 -- Yes --> add2[retval[key] = value] --> loop2
cond2 -- No --> loop2
loop2 -- done --> check2{len(retval) != 0?}
check2 -- Yes --> ret2[return retval]
check2 -- No --> retNone2[return None]
```

> SVG rendering failed for this diagram.
