# Diagram: entity_core/entity_service/entity_service/common/__init__.py


> Auto-generated by Obscura crawlers

## Diagram 1

```mermaid
flowchart TD
    InvokeAddEvent[invoke_add_event(event, solution_id, fv_event, entity_id, actual_trip_leg_id, planned_trip_leg_id, asynchronous)]
    CheckIDs{Any of entity_id<br/>actual_trip_leg_id<br/>planned_trip_leg_id?}
    BadRequest[BadRequestError: "One of entity_id, actual_trip_leg_id, or planned_trip_leg_id is required"]
    SetFullEvent[deepcopy(event) -> full_event; full_event.body = fv_event; full_event.pathParameters.solution_id = solution_id]
    ChooseLambda[determine lambda_name and set pathParameters]
    EntityID[entity_id set -> lambda_name = "add_entity_event"; set entity_id]
    ActualLeg[actual_trip_leg_id set -> lambda_name = "add_event_trip_leg"; set actual_trip_leg_id]
    PlannedLeg[planned_trip_leg_id set -> lambda_name = "add_event_trip_leg"; set planned_trip_leg_id]
    AsyncCheck{asynchronous?}
    InvokeEvent[invoke_lambda(lambda_name, full_payload=full_event, invoke_type="Event")]
    InvokeRequest[invoke_lambda(lambda_name, full_payload=full_event, invoke_type="RequestResponse")]
    InvokeAddEventBatch[invoke_add_event_batch(event, solution_id, fv_event) -> prepare full_event.body, pathParameters.solution_id]
    InvokeAddEventBatch --> InvokeBatchCall[invoke_lambda("add_entity_event", full_payload=full_event, invoke_type="Event")]
    InvokeClearException[invoke_clear_exception(event, org_id, dwell_records)]
    PrepareClear[deepcopy(event) -> full_event; set headers Accept version; build body with entityExceptionIdMapping; set authorizer privileges and solutions; set pathParameters.solution_id]
    InvokeClearCall[invoke_lambda(name="update_exception", headers={"x-active-org": org_id}, full_payload=full_event, invoke_type="Event")]

    InvokeAddEvent --> CheckIDs
    CheckIDs -->|none| BadRequest
    CheckIDs -->|some| SetFullEvent
    SetFullEvent --> ChooseLambda
    ChooseLambda --> EntityID
    ChooseLambda --> ActualLeg
    ChooseLambda --> PlannedLeg
    EntityID --> AsyncCheck
    ActualLeg --> AsyncCheck
    PlannedLeg --> AsyncCheck
    AsyncCheck -->|true| InvokeEvent
    AsyncCheck -->|false| InvokeRequest
    InvokeClearException --> PrepareClear --> InvokeClearCall
```

> SVG rendering failed for this diagram.

## Diagram 2

```mermaid
classDiagram
    class Schema_Validator {
        <<enumeration>>
        +ENTITY_ADD
        +ENTITY_UPDATE
        +ENTITY_ADD_RESPONSE
        +ENTITY_SEARCH_RESPONSE
        +ENTITY_RESPONSE
        +EVENT_ADD
        +EVENT_GET
        +EXCEPTION_ADD
        +EXCEPTION_CLEAR
        +HOLD_SET
        +HOLD_CLEAR
        +HOLD_TYPE_STATUS
        +ACTUAL_TRIP_LEG_ADD
        +ACTUAL_TRIP_LEG_UPDATE
        +PLANNED_TRIP_LEG_ADD
        +PLANNED_TRIP_LEG_UPDATE
        +PROGRESS_UPDATE_ADD
        +STATUS_UPDATE_ADD
        +POSITION_UPDATE_ADD
        +POSITION_UPDATE_UPDATE
        +DWELL_START
        +DWELL_END
        +ADD_GROUP_TYPE
        +ADD_GROUP_CATEGORY
        +ADD_GROUP_ENTITY
    }

    class DateTimeEncoder {
        +default(o)
    }

    class JSONUtils {
        +json_dumps(o, sort_keys=False)
    }

    class Translators {
        +snake_to_camel(string)
        +camel_to_snake(string)
        +field_name_translator(obj, translator)
        +snake_to_camel_translator(obj)
        +camel_to_snake_translator(obj)
        +resp_converter(resp)
    }

    class Utilities {
        +seconds_to_ppdate(seconds)
        +is_string_literal(x)
        +snake_to_camel_translator(obj)
        +camel_to_snake_translator(obj)
        +dict_keys_to_camelcase(d)
        +fix_entity_return_casing(entity_dict)
    }

    class Authorization {
        +entity_carrier_authorization(event, cursor, internal_entity_id)
        +entity_partner_authorization(cursor, internal_entity_id)
        +entity_dealer_authorization(event, cursor, internal_entity_ids, dealer_org_id)
        +trip_leg_dealer_authorization(event, cursor, ...)
        +_entity_authorization_visibility_table(event, cursor, internal_entity_ids, given_org_id)
    }

    class DB {
        +get_carrier_name(cursor, carrier_id, carrier_scac)
        +get_shipper_location_name(cursor, location_code, organization_id)
        +get_location_ids_from_entities(entities)
        +get_fv_id(cursor, organization_id)
        +get_ult_dest_by_trip_leg_db_id(cursor, internal_id, trip_type)
    }

    class Invokers {
        +invoke_add_event(...)
        +invoke_add_event_batch(...)
        +invoke_clear_exception(...)
        +get_location_lad(...)
        +get_locations_and_dest_lad(...)
    }

    class Helpers {
        +sanitize_ids(unsanitized_ids, is_list=False)
        +form_window(lower, upper)
        +convert_datetime_naive(dt)
        +get_utc_datetime(date)
        +extract_location_id_cache_key(event, organization_id, location_id)
    }

    Schema_Validator ..> "fastjsonschema.compile" : uses
    JSONUtils --> DateTimeEncoder : uses
    Translators --> Utilities : provides transformations
    Utilities --> Helpers : calls
    Authorization --> DB : queries
    Invokers --> Authorization : checks privileges
    Invokers --> DB : fetches locations
    Helpers --> DB : helper DB lookups
    HelperMethods Utilities ..> Translators : uses
```

> SVG rendering failed for this diagram.
