# Diagram: entity_core/entity_service/entity_service/common/status_update.py


> Auto-generated by Obscura crawlers

## Diagram 1

```mermaid
flowchart TD
  A[Start] --> B[transform_references(references)]
  B --> C{For each reference in references}
  C -->|next| D[field = reference["qualifier"]]
  D --> E[field = field.replace(" ", "_")]
  E --> F[formatted = {"value": reference.get("value")}]
  F --> G{reference.get("reference_location_id") != None}
  G -->|yes| H[formatted["reference_location_id"] = reference.get("reference_location_id")]
  G -->|no| I[skip adding reference_location_id]
  H --> J[new_reference[field] = formatted]
  I --> J
  J --> C
  C -->|done| K[return new_reference]
```

> SVG rendering failed for this diagram.

## Diagram 2

```mermaid
flowchart TD
  A[Start] --> B[remove_evt_received_time(references)]
  B --> C{references.get("evt_received_time")?}
  C -->|yes| D[del references["evt_received_time"]]
  C -->|no| E[skip deletion]
  D --> F[return references]
  E --> F
```

> SVG rendering failed for this diagram.

## Diagram 3

```mermaid
flowchart TD
  A[Start] --> B[get_partner_type(payload)]
  B --> C[refs = payload.get("references", [])]
  C --> D{Iterate refs: ref.get("qualifier") == "userRole"?}
  D -->|match| E[return ref.get("value")]
  D -->|no match| C
  C -->|exhausted| F[return None]
```

> SVG rendering failed for this diagram.
