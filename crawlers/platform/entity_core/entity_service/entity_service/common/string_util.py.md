# Diagram: entity_core/entity_service/entity_service/common/string_util.py


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
flowchart TD
    A([Start]) --> B[Receive group_id (str)]
    B --> C[Define allowed_pattern regex]
    C --> D[Compute valid_group_id = re.sub("[^allowed_chars]", "_", group_id)]
    D --> E[Truncate valid_group_id to first 128 characters]
    E --> F([Return valid_group_id])
```

> SVG rendering failed for this diagram.
