# Diagram: entity_core/entity_service/entity_service/damageview/submission/get_assigned_locations.py


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
flowchart LR
    DBInit[DB_CONN = FvDatabaseConnector("get_assigned_locations", SecretNames.ENTITY_DATABASE)]
    Start((Invoke lambda_handler))
    Start --> GetQuery["assigned_user_email = fv.aws.lambdas.get_query_parameter(event, QSP.DamageView.EMAIL, required=True)"]
    GetQuery --> Establish["DB_CONN.establish_connection()"]
    Establish --> Cursor["cursor = DB_CONN.cursor"]
    Cursor --> GetAssigned["assigned_locations = entity_service.db.damageview.get_locations_assigned_to_user(cursor, assigned_user_email)"]
    GetAssigned --> Decision{assigned_locations<br/>is empty?}
    Decision -- Yes --> ReturnEmpty["return fv.aws.lambdas.make_response([])"]
    Decision -- No --> GetLocations["locations_data, _ = entity_service.db.damageview.get_locations(event, cursor, {\"location_ids\": assigned_locations}, 0, 100)"]
    GetLocations --> Filter["return fv.aws.lambdas.make_response([{k: loc[k] for k in (\"id\",\"name\",\"code\")} for loc in locations_data])"]
    DBInit -.-> Establish
    classDef db fill:#f3f4f6,stroke:#333,stroke-width:1px;
    class DBInit db
```

> SVG rendering failed for this diagram.
