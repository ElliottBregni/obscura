# Diagram: entity_core/entity_service/entity_service/damageview/submission/update_submission/models/event.py


> Auto-generated by Obscura crawlers

## Diagram 1

```mermaid
classDiagram
class UpdatePayload {
  <<BaseModel>>
  assignee: str|None
  status: str|None
  inProgressStatus: str|None
  rejectedComments: str|None
  repairLocation: str|None
  submitterEmail: str|None
  submitterPhoneNumber: str|None
  customFields: list[KVField]|None
  vinDetails: list[VinEditPayload]|None
  +_email(v)
  +_phone(v)
  +from_event_all(event)
}
class UpdateSubmissionRequest {
  submission_id: int
  solution_id: str
  external_submission_id: str|None
  org_type: str
  org_id: int|str
  actor_email: str
  user_id: int|str|None
  payload: UpdatePayload
  +to_body_dict()
  +from_event(event, submission_id, solution_id, external_submission_id)
}
class _parse_body <<function>> {
  +_parse_body(event)
}
class _actor_from_event <<function>> {
  +_actor_from_event(event)
}
class _solution_id_from_event <<function>> {
  +_solution_id_from_event(event)
}
class auth <<module>> {
  +get_user_org_types(event)
  +get_organization_id(event)
  +get_actor_email(event)
  +get_user_id(event)
}
class KVField
class VinEditPayload

UpdateSubmissionRequest --> UpdatePayload : "has payload"
UpdatePayload --> KVField : "uses"
UpdatePayload --> VinEditPayload : "uses"
_update_parse_body ..> UpdatePayload : "creates"
_parse_body ..> UpdatePayload : "provides body to"
_actor_from_event ..> auth : "calls"
_actor_from_event ..> UpdateSubmissionRequest : "returns actor tuple"
_solution_id_from_event ..> UpdateSubmissionRequest : "returns solution id"
auth <.. _actor_from_event
```

> SVG rendering failed for this diagram.

## Diagram 2

```mermaid
sequenceDiagram
participant Client as Event
participant Parser as _parse_body
participant Payload as UpdatePayload
participant Actor as _actor_from_event
participant Sol as _solution_id_from_event
participant Request as UpdateSubmissionRequest
participant Auth as auth

Client->>Parser: send event with body/headers/pathParameters
alt body is string
  Parser->>Parser: attempt JSON loads
  alt JSON valid
    Parser-->>Payload: return parsed dict
  else JSON invalid
    Parser-->>Client: raise ValueError("Invalid request body JSON")
  end
else body is dict
  Parser-->>Payload: return dict
end
Payload->>Payload: field validators (_email, _phone) run
Client->>Actor: request actor info
Actor->>Auth: get_user_org_types / get_organization_id / get_actor_email / get_user_id
Auth-->>Actor: return org_type, org_id, actor_email, user_id
Actor-->>Request: provide actor tuple
Client->>Sol: check pathParameters.headers
Sol-->>Request: return solution_id or None
Request->>Payload: call UpdatePayload.from_event_all(event)
Request-->>Client: return UpdateSubmissionRequest instance
```

> SVG rendering failed for this diagram.
