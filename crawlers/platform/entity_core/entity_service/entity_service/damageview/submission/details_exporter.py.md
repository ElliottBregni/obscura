# Diagram: entity_core/entity_service/entity_service/damageview/submission/details_exporter.py


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
flowchart TD
  Create[create_submission_detail_form_pdf()] -->|calls| Damage[damage_details_section(pdf, submission_details, template, timezone)]
  Create -->|iterates vins and calls| Vin[vin_details_section(pdf, solution_id, vin, vin_photos, template, cursor)]
  Create -->|returns| PdfExport[fv.aws.lambdas.pdf_export(bytearray(pdf.output()), event)]
  Damage -->|normalizes reportingLocation| Reporting[reporting_location_text(location_dict)]
  Reporting -->|uses| Country[get_country_name(alpha_2) → pycountry.countries.get]
  Damage -->|flattens| Flatten[flatten_dict(d)]
  Flatten -->|generator| FlattenGen[_flatten_dict_gen(d, parent_key, sep)]
  Damage -->|formats submitter| FormatSubmitter[format_submitter_email_and_phone(flatten_submission_details, first, second)]
  FormatSubmitter -->|updates| Flatten
  Damage -->|transliterates headers/values| Transliterate[transliterate_unicode(obj)]
  Damage -->|formats dates| TryDate[try_format_date(date_string, timezone)]
  TryDate -->|converts tz| UTC[fv.aws.lambdas.utc_to_tz(datetime, timezone)]
  Vin -->|builds left/right items| Transliterate
  Vin -->|queries stolen label| GetLabel[get_vin_field_label(cursor, vin_stolen, solution_id, default)]
  Vin -->|maps complex items| MapItem[map_item_value(item, title, template)]
  MapItem -->|reads template keys| Template[template lookups]
  Vin -->|if photos exist| Photos[photos_provided_section(pdf, photos_dict, template)]
  Photos -->|for each photo| Compress[compress_image(image_url, max_width, quality)]
  Compress -->|http fetch| Requests[requests.get(image_url, stream=True)]
  Compress -->|image ops| PIL[PIL.Image.open(io.BytesIO(response.content))]
  PIL -->|convert/resize/save| Buffer[io.BytesIO JPEG buffer]
  Compress -->|logs on error| LogCompress[logging.error]
  Transliterate -->|uses| Unidecode[unidecode(str)]
  Titlelize[titlelize_string(text)] -->|helper used in templates| Titlelize
  TryDate -->|on parse error returns| RawString[original date string]
```

> SVG rendering failed for this diagram.
