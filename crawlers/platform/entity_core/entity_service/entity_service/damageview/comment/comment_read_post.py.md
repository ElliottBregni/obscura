# Diagram: entity_core/entity_service/entity_service/damageview/comment/comment_read_post.py


> Auto-generated by Obscura crawlers

## Diagram 1

```mermaid
flowchart TD
    Start([POST /damageview/submission/{submission_id}/comment/read]) --> AuthCheck{Auth Check: FEATURE DAMAGE_VIEW}
    AuthCheck -->|pass| GetSubmission[Get submission_id (get_path_parameter)]
    GetSubmission --> GetOwner[Get owner_id (auth.get_organization_id)]
    GetOwner --> PrepareBody[Read body (get_event_body) and build get_body_data]
    PrepareBody --> BuildEvent[deepcopy event; set body=json.dumps(get_body_data); set pathParameters.owner_id]
    BuildEvent --> Invoke[invoke_lambda("comment_read_post", full_payload=event_comment)]
    Invoke --> Return([Return invoke result])
```

> SVG rendering failed for this diagram.

## Diagram 2

```mermaid
sequenceDiagram
    participant API as API Gateway
    participant Lambda as damageview.lambda_handler
    participant Auth as auth
    participant Comment as comment-service
    API->>Lambda: POST /damageview/submission/{submission_id}/comment/read
    Lambda->>Auth: mandatory auth check (AUTH_CHECK)
    Auth-->>Lambda: auth passes / features present
    Lambda->>Lambda: get_path_parameter(event, "submission_id")
    Lambda->>Auth: auth.get_organization_id(event)
    Auth-->>Lambda: owner_id
    Lambda->>Lambda: get_event_body(event) -> get_body_data(submission_id)
    Lambda->>Lambda: deepcopy event; set body and pathParameters
    Lambda->>Comment: invoke_lambda("comment_read_post", full_payload=event_comment)
    Comment-->>Lambda: response
    Lambda-->>API: return response
```

> SVG rendering failed for this diagram.
