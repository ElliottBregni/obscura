# Diagram: entity_core/entity_service/entity_service/damageview/comment/comment_list.py


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
flowchart TD
    AuthCheck[Authorization check (AUTH_CHECK)] --> Lambda(lambda_handler(event, context, audit_refs))
    Lambda --> GetSubmission[Get submission_id via get_path_parameter(event, "submission_id")]
    GetSubmission --> GetOwner[owner_id = auth.get_organization_id(event)]
    GetOwner --> BuildQuery[Build query params via get_query_string(event, submission_id)]
    BuildQuery --> CloneEvent[event_comment = deepcopy(event)]
    CloneEvent --> ClearBody[Set event_comment.body = None]
    ClearBody --> SetPath[Set event_comment.pathParameters = {owner_id}]
    SetPath --> SetQuery[Set event_comment.queryStringParameters = query params]
    SetQuery --> Invoke[invoke_lambda("comment_list", full_payload=event_comment)]
    Invoke --> Return[Return response from comment_list]
```

> SVG rendering failed for this diagram.
