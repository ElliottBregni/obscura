# Diagram: entity_core/entity_service/entity_service/damageview/fields/pipeline/utils.py


> Auto-generated by Obscura crawlers

## Diagram 1

```mermaid
flowchart TD
A([coerce_json(v)]) --> B{isinstance(v, str)?}
B -- yes --> C[/s = v.strip()/]
C --> D{not s?}
D -- yes --> E([return None])
D -- no --> F[/attempt json.loads(s)/]
F --> G{json.loads(s) raises error?}
G -- no --> H([return parsed JSON])
G -- yes --> I([return v])
B -- no --> I
```

> SVG rendering failed for this diagram.

## Diagram 2

```mermaid
flowchart TD
U([clean_up(vw)]) --> V{isinstance(vw, dict)?}
V -- no --> W([return None])
V -- yes --> X[/cleaned = {k: v for k, v in vw.items() if v is not None}/]
X --> Y{cleaned?}
Y -- no --> W
Y -- yes --> Z([return cleaned])
```

> SVG rendering failed for this diagram.
