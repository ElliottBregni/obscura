# Diagram: entity_core/entity_service/entity_service/damageview/common/notification.py


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
flowchart TD
    Start([Start]) --> Construct[Construct message]
    Construct --> MessageContents[Message contents: event=deepcopy(event)\nsubmission_id\nnotification_type\nnotification_context]
    Construct --> ReadStage[Read AWS_STAGE env (default "dev")]
    ReadStage --> CreateSQS[Create SQS resource via boto3.resource("sqs")]
    CreateSQS --> GetQueue[Get queue by name: "<stage>-DV-add-notification"]
    GetQueue --> SendMsg[Send message: queue.send_message(MessageBody=json.dumps(message, default=str), MessageAttributes={})]
    SendMsg --> Log[logging.info(response)]
    Log --> End([End])
```

> SVG rendering failed for this diagram.
