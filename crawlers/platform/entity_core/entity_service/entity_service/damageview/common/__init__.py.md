# Diagram: entity_core/entity_service/entity_service/damageview/common/__init__.py


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
flowchart LR
  dmg[get_damage_view_s3_bucket()]
  env[os.environ: AWS_STAGE]
  dv[DamageviewS3.BUCKET.value]
  bucketStr[bucket string (AWS_STAGE-BUCKET)]
  env -->|reads| dmg
  dv -->|reads| dmg
  dmg -->|returns| bucketStr

  keyFunc[extract_org_cache_key(location_id)]
  hashKey[cachetools.keys.hashkey]
  keyFunc -->|calls| hashKey

  ttlcache[TTLCache(maxsize=128, ttl=300)]
  cachedWrapper[cached(TTLCache(maxsize=128, ttl=300), key=extract_org_cache_key)]
  ttlcache --> cachedWrapper
  keyFunc --> cachedWrapper

  cachedWrapper -->|wraps| addrFunc[get_address_as_admin(location_id)]
  addrFunc -->|calls| entityCall[entity_service.common.entity.get_location_details_as_admin([location_id])]
  entityCall --> locations[locations list]
  locations --> decision{location present?}
  decision -->|yes| buildAddr[address dict constructed: name, code, address, city, state, country, postalCode]
  buildAddr --> returnAddr([return address])
  decision -->|no| returnNone([return None])
```

> SVG rendering failed for this diagram.
