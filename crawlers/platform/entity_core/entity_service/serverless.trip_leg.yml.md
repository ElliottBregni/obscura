# Diagram: entity_core/entity_service/serverless.trip_leg.yml


> Auto-generated by Obscura crawlers

## Diagram 1

```mermaid
flowchart TD
  API_GW[API Gateway\n(HTTP endpoints)] --> create_actual[create_actual_trip_leg\nPOST/PUT /solution/{solution_id}/actual-trip-leg]
  API_GW --> create_planned[create_planned_trip_leg\nPOST/PUT /solution/{solution_id}/planned-trip-leg]
  API_GW --> get_actual[get_actual_trip_leg\nGET /solution/{solution_id}/actual-trip-leg]
  API_GW --> get_planned[get_planned_trip_leg\nGET /solution/{solution_id}/planned-trip-leg]
  API_GW --> get_actual_id[get_actual_trip_leg_id\nGET /solution/{solution_id}/actual-trip-leg/{id}]
  API_GW --> get_planned_id[get_planned_trip_leg_id\nGET /solution/{solution_id}/planned-trip-leg/{id}]
  API_GW --> delete_actual[delete_actual_trip_leg\nDELETE /solution/{solution_id}/actual-trip-leg/{id}]
  API_GW --> delete_planned[delete_planned_trip_leg\nDELETE /solution/{solution_id}/planned-trip-leg/{id}]
  API_GW --> add_progress[add_trip_leg_progress_update\nPOST /solution/{solution_id}/actual-trip-leg/{id}/progress-update]
  API_GW --> get_progress[get_trip_leg_progress_update\nGET /solution/{solution_id}/actual-trip-leg/{id}/progress-update/{id}]
  API_GW --> get_all_progress[get_all_trip_leg_progress_updates\nGET /solution/{solution_id}/actual-trip-leg/{id}/progress-update]
  API_GW --> add_position[add_trip_leg_position_update\nPOST /solution/{solution_id}/actual-trip-leg/{id}/position-update]
  API_GW --> associate_docs[trip_leg_associate_documents\nPOST /solution/{solution_id}/document]
  API_GW --> override_eta[override_planned_trip_leg_eta\nPUT /solution/{solution_id}/planned-trip-leg/{id}/eta]
  API_GW --> override_current[override_current_planned_trip_leg\nPOST /solution/{solution_id}/override-current-planned-trip-leg]

  SQS_FV[SVS SQS Queues\n(FIFO)] --> augment[augment_fv_trip_legs\nSQS: fv-trip-leg-augmentation.fifo]
  SQS_UpdateEntities[SQS: TripLegCreatedUpdatedDeleted-UpdateEntities] --> update_consumer[update_entity_carrier_queue_consumer\nSQS consumer]
  SQS_CurrentPlanned[SQS: FIN-current-planned-trip-leg.fifo] --> update_current[update_current_planned_trip_leg\nSQS consumer]

  IAM[(IAM Role\nmanaged policies + statements)] --- create_actual
  IAM --- create_planned
  IAM --- get_actual
  IAM --- get_planned
  IAM --- get_actual_id
  IAM --- get_planned_id
  IAM --- delete_actual
  IAM --- delete_planned
  IAM --- add_progress
  IAM --- get_progress
  IAM --- get_all_progress
  IAM --- add_position
  IAM --- associate_docs
  IAM --- override_eta
  IAM --- override_current
  IAM --- augment
  IAM --- update_consumer
  IAM --- update_current

  LAYERS[Shared Layers\n(fv_layers)] --- create_actual
  LAYERS --- create_planned
  LAYERS --- get_actual
  LAYERS --- get_planned
  LAYERS --- augment
  LAYERS --- update_consumer
  LAYERS --- update_current
  LAYERS --- associate_docs

  VPC[VPC Configuration] --- create_actual
  VPC --- create_planned
  VPC --- augment
  VPC --- update_consumer
  VPC --- update_current

  Datadog[Datadog Env & Config] --- create_actual
  Datadog --- create_planned
  Datadog --- augment
  Datadog --- associate_docs

  subgraph Lambdas
    create_actual
    create_planned
    get_actual
    get_planned
    get_actual_id
    get_planned_id
    delete_actual
    delete_planned
    add_progress
    get_progress
    get_all_progress
    add_position
    associate_docs
    override_eta
    override_current
    augment
    update_consumer
    update_current
  end
```

> SVG rendering failed for this diagram.

## Diagram 2

```mermaid
classDiagram
  class create_actual_trip_leg {
    +handler: entity_service/trip_leg/trip_leg/post_put_actual_trip_leg.lambda_handler
    +events: HTTP POST /solution/{solution_id}/actual-trip-leg
    +events: HTTP PUT /solution/{solution_id}/actual-trip-leg/{actual_trip_leg_id}
    +layers: fv_layers
  }
  class create_planned_trip_leg {
    +handler: entity_service/trip_leg/trip_leg/post_put_planned_trip_leg.lambda_handler
    +events: HTTP POST /solution/{solution_id}/planned-trip-leg
    +events: HTTP PUT /solution/{solution_id}/planned-trip-leg/{planned_trip_leg_id}
    +layers: fv_layers
  }
  class update_actual_trip_leg {
    +handler: entity_service/trip_leg/trip_leg/patch_actual_trip_leg.lambda_handler
    +events: HTTP PATCH /solution/{solution_id}/actual-trip-leg/{actual_trip_leg_id}
  }
  class update_planned_trip_leg {
    +handler: entity_service/trip_leg/trip_leg/patch_planned_trip_leg.lambda_handler
    +events: HTTP PATCH /solution/{solution_id}/planned-trip-leg/{planned_trip_leg_id}
  }
  class augment_fv_trip_legs {
    +handler: entity_service/trip_leg/trip_leg/augment_fv_trip_leg/handler.lambda_handler
    +events: SQS fv-trip-leg-augmentation.fifo
    +concurrency: 15
  }
  class update_entity_carrier_queue_consumer {
    +handler: entity_service/trip_leg/trip_leg/entity/update_entity_carrier_queue_consumer.lambda_handler
    +events: SQS TripLegCreatedUpdatedDeleted-UpdateEntities
  }
  class get_actual_trip_leg_id {
    +handler: entity_service/trip_leg/trip_leg/get_actual_trip_leg.lambda_handler
    +events: HTTP GET /solution/{solution_id}/actual-trip-leg/{actual_trip_leg_id}
  }
  class get_planned_trip_leg_id {
    +handler: entity_service/trip_leg/trip_leg/get_planned_trip_leg.lambda_handler
    +events: HTTP GET /solution/{solution_id}/planned-trip-leg/{planned_trip_leg_id}
  }
  class get_actual_trip_leg {
    +handler: entity_service/trip_leg/trip_leg/get_actual_trip_leg.lambda_handler
    +events: HTTP GET /solution/{solution_id}/actual-trip-leg
    +events: HTTP GET internal/actual-trip-leg
  }
  class get_planned_trip_leg {
    +handler: entity_service/trip_leg/trip_leg/get_planned_trip_leg.lambda_handler
    +events: HTTP GET /solution/{solution_id}/planned-trip-leg
    +events: HTTP GET internal/planned-trip-leg
  }
  class delete_actual_trip_leg {
    +handler: entity_service/trip_leg/trip_leg/delete_actual_trip_leg.lambda_handler
    +events: HTTP DELETE /solution/{solution_id}/actual-trip-leg/{actual_trip_leg_id}
  }
  class delete_planned_trip_leg {
    +handler: entity_service/trip_leg/trip_leg/delete_planned_trip_leg.lambda_handler
    +events: HTTP DELETE /solution/{solution_id}/planned-trip-leg/{planned_trip_leg_id}
  }
  class add_trip_leg_progress_update {
    +handler: entity_service/trip_leg/progress_update/add_progress_update.lambda_handler
    +events: HTTP POST /solution/{solution_id}/actual-trip-leg/{actual_trip_leg_id}/progress-update
  }
  class get_trip_leg_progress_update {
    +handler: entity_service/trip_leg/progress_update/get_progress_update.lambda_handler
    +events: HTTP GET /solution/{solution_id}/actual-trip-leg/{actual_trip_leg_id}/progress-update/{progress_update_id}
  }
  class get_all_trip_leg_progress_updates {
    +handler: entity_service/trip_leg/progress_update/get_progress_update.lambda_handler
    +events: HTTP GET /solution/{solution_id}/actual-trip-leg/{actual_trip_leg_id}/progress-update
  }
  class add_trip_leg_position_update {
    +handler: entity_service/trip_leg/position_update/add_position_update.lambda_handler
    +events: HTTP POST /solution/{solution_id}/actual-trip-leg/{actual_trip_leg_id}/position-update
  }
  class add_event_trip_leg {
    +handler: entity_service/trip_leg/event/add_event.lambda_handler
  }
  class override_current_planned_trip_leg {
    +handler: entity_service/entity/admin_tool/override_current_planned_trip_leg/override_current_planned_trip_leg_lambda.lambda_handler
    +events: HTTP POST /solution/{solution_id}/override-current-planned-trip-leg
  }
  class update_current_planned_trip_leg {
    +handler: entity_service/entity/entity/update_current_planned_trip_leg/update_current_planned_trip_leg_lambda.lambda_handler
    +events: SQS FIN-current-planned-trip-leg.fifo
  }
  class trip_leg_associate_documents {
    +handler: entity_service/trip_leg/associate_documents/handler.lambda_handler
    +events: HTTP POST /solution/{solution_id}/document
  }
  class override_planned_trip_leg_eta {
    +handler: entity_service/trip_leg/trip_leg/override_planned_trip_leg_eta.lambda_handler
    +events: HTTP PUT /solution/{solution_id}/planned-trip-leg/{planned_trip_leg_id}/eta
  }

  %% Relationships
  augment_fv_trip_legs -->|uses| update_entity_carrier_queue_consumer
  update_current_planned_trip_leg -->|reads| SQS_CurrentPlanned
  trip_leg_associate_documents -->|uses| create_actual_trip_leg
  override_current_planned_trip_leg -->|triggers| update_current_planned_trip_leg
```

> SVG rendering failed for this diagram.
