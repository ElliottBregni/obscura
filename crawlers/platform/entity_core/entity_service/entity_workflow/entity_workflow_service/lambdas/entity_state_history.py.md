# Diagram: entity_core/entity_service/entity_workflow/entity_workflow_service/lambdas/entity_state_history.py


> Auto-generated by Obscura crawlers

## Diagram 1

```mermaid
classDiagram
class EntityStateHistoryFactory {
  +PostgresEntityStateHistoryDAO dao
  +EntityDAO entity_dao
  +parse_request(event)
  +create_handler()
}
class PostgresEntityStateHistoryDAO {
  +FvDatabaseConnector db_conn
  +query(...)
}
class EntityDAO {
  +FvDatabaseConnector db_conn
  +fetch(...)
}
class FvDatabaseConnector {
  +connect()
  +close()
}
class lambda_handler <<function>>
class make_response <<function>>
class mandatory_lambda_handling <<decorator>>
class AuthType
class Privilege

EntityStateHistoryFactory --> PostgresEntityStateHistoryDAO : uses
EntityStateHistoryFactory --> EntityDAO : uses
PostgresEntityStateHistoryDAO --> FvDatabaseConnector : uses
EntityDAO --> FvDatabaseConnector : uses
lambda_handler ..> mandatory_lambda_handling : decorated_by
lambda_handler --> EntityStateHistoryFactory : instantiates
lambda_handler --> make_response : returns_with
```

> SVG rendering failed for this diagram.

## Diagram 2

```mermaid
flowchart TD
Evt[Incoming event]
Evt --> LH[lambda_handler(event, context, audit_ref)]
subgraph Auth
  AUTH_CHECK[AUTH_CHECK: AuthType.PRIVILEGE -> Privilege.VIEW_ENTITY]
end
subgraph Init
  DB_CONN[DB_CONN = FvDatabaseConnector("entity_state_history", SECRET, auto_connect=False)]
end
AUTH_CHECK -.-> LH
DB_CONN --> FactoryInit[EntityStateHistoryFactory(PostgresEntityStateHistoryDAO(DB_CONN), EntityDAO(DB_CONN))]
LH --> FactoryInit
FactoryInit --> Parse[factory.parse_request(event)]
Parse --> Request[request]
FactoryInit --> CreateHandler[factory.create_handler()]
CreateHandler --> Handler[handler]
Handler --> Handle[handler.handle(request)]
Handle --> Result[result]
Result --> Response[make_response(result)]
Response --> Return[HTTP response]
LH .. decorator .. mandatory_lambda_handling
```

> SVG rendering failed for this diagram.
