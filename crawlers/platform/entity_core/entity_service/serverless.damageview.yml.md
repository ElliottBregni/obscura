# Diagram: entity_core/entity_service/serverless.damageview.yml


> Auto-generated by Obscura crawlers

## Diagram 1

```mermaid
flowchart LR
  subgraph "API Gateway (HTTP)"
    APIGW[API Gateway]
  end

  subgraph "Lambda Functions"
    batch[(batch)]
    getFilters[(get_submission_filters)]
    getSubmission[(get_submission)]
    getSubmissionDetails[(get_submission_details)]
    updateSubmissionDetails[(update_submission_details)]
    watch[(watch)]
    commentPost[(add_submission_comment_post)]
    commentList[(get_submission_comment_list)]
    commentPatch[(patch_submission_comment_patch)]
    commentRead[(add_submission_comment_read_post)]
    impactedVins[(get_impacted_vins)]
    addDamageSubmission[(add_damage_submission)]
    presigned[(generate_presigned_url_submission)]
    vinPhotos[(get_impacted_vin_photos)]
    vinCounts[(get_vin_counts)]
    getFields[(get_fields)]
    setLocationsAssignee[(set_locations_assignee)]
    getAssignedLocations[(get_assigned_locations)]
    reportingGrant[(reporting_location_grant)]
    notifHandler[(damageview_notification_handler)]
    postProcessingConsumer[(post_processing_submission_consumer)]
    statusChangeHandler[(damage_status_change_integration_handler)]
  end

  subgraph "SQS Queues"
    sqs_notifications[("${opt:stage}-DV-add-notification (SQS)")]
    sqs_post_processing[("${opt:stage}-DV-PostProcessingDamageSubmissionCreation (SQS)")]
    sqs_status_change[("${opt:stage}-DV-DamageSubmissionStatusChangeEventNotification (SQS)")]
  end

  subgraph "Shared AWS Resources"
    S3[/S3: ${opt:stage}-damage-view/*\n${opt:stage}-fv-csv-export/*/]
    Secrets[SecretsManager]
    SNS[SNS]
    SQSResource[SQS (other queues)]
    Datadog[Datadog (env)]
  end

  APIGW --> batch
  APIGW --> getFilters
  APIGW --> getSubmission
  APIGW --> getSubmissionDetails
  APIGW --> updateSubmissionDetails
  APIGW --> watch
  APIGW --> commentPost
  APIGW --> commentList
  APIGW --> commentPatch
  APIGW --> commentRead
  APIGW --> impactedVins
  APIGW --> addDamageSubmission
  APIGW --> presigned
  APIGW --> vinPhotos
  APIGW --> vinCounts
  APIGW --> getFields
  APIGW --> setLocationsAssignee
  APIGW --> getAssignedLocations
  APIGW --> reportingGrant

  sqs_notifications --> notifHandler
  sqs_post_processing --> postProcessingConsumer
  sqs_status_change --> statusChangeHandler

  batch --> S3
  addDamageSubmission --> S3
  presigned --> S3
  notifHandler --> SQSResource
  postProcessingConsumer --> SQSResource
  statusChangeHandler --> SQSResource

  batch --> Secrets
  addDamageSubmission --> Secrets
  notifHandler --> SNS
  postProcessingConsumer --> SNS

  batch --> Datadog
  getSubmissionDetails --> Datadog
  notifHandler --> Datadog

  classDef lambda fill:#f9f,stroke:#333,stroke-width:1px;
  class batch,getFilters,getSubmission,getSubmissionDetails,updateSubmissionDetails,watch,commentPost,commentList,commentPatch,commentRead,impactedVins,addDamageSubmission,presigned,vinPhotos,vinCounts,getFields,setLocationsAssignee,getAssignedLocations,reportingGrant,notifHandler,postProcessingConsumer,statusChangeHandler lambda
```

> SVG rendering failed for this diagram.

## Diagram 2

```mermaid
classDiagram
  class Batch {
    +handler: entity_service/damageview/submission/batch.lambda_handler
    +endpoint: POST /submission/batch
  }
  class GetSubmissionFilters {
    +handler: entity_service/damageview/filter/get_filters.lambda_handler
    +endpoint: GET /submission/filters
  }
  class GetSubmission {
    +handler: entity_service/damageview/submission/get_submission.lambda_handler
    +endpoint: GET /submission
  }
  class GetSubmissionDetails {
    +handler: entity_service/damageview/submission/get_submission_details.lambda_handler
    +endpoints: GET /submission/{submission_id}\nGET /solution_id/{solution_id}/submission/{submission_id}
  }
  class UpdateSubmissionDetails {
    +handler: entity_service/damageview/submission/update_submission_details.lambda_handler
    +endpoint: PATCH /submission/{submission_id}/details
  }
  class Watch {
    +handler: entity_service/damageview/submission/watch.lambda_handler
    +endpoints: POST /submission/{submission_id}/watch\nDELETE /submission/{submission_id}/watch
  }
  class CommentPost {
    +handler: entity_service/damageview/comment/comment_post.lambda_handler
    +endpoint: POST /submission/{submission_id}/comment
  }
  class CommentList {
    +handler: entity_service/damageview/comment/comment_list.lambda_handler
    +endpoint: GET /submission/{submission_id}/comment
  }
  class CommentPatch {
    +handler: entity_service/damageview/comment/comment_patch.lambda_handler
    +endpoint: PATCH /submission/{submission_id}/comment/{comment_id}
  }
  class CommentReadPost {
    +handler: entity_service/damageview/comment/comment_read_post.lambda_handler
    +endpoint: POST /submission/{submission_id}/comment/read
  }
  class ImpactedVins {
    +handler: entity_service/damageview/vins/get_impacted_vins.lambda_handler
    +endpoint: GET /submission/{submission_id}/vins
  }
  class AddDamageSubmission {
    +handler: entity_service/damageview/submission/add_damage_submission.lambda_handler
    +endpoints: POST /submission/solution/{solution_id}\nPATCH /submission/{submission_id}
  }
  class PresignedURL {
    +handler: entity_service/damageview/submission/generate_presigned_url.lambda_handler
    +endpoint: PUT /submission/{submission_id}/presigned_url
  }
  class VinPhotos {
    +handler: entity_service/damageview/vins/get_impacted_vin_photos.lambda_handler
    +endpoint: GET /submission/{submission_id}/vin/{vin}/photos
  }
  class VinCounts {
    +handler: entity_service/damageview/counts/get_vin_counts.lambda_handler
    +endpoint: GET /{solution_id}/counts/vin
  }
  class GetFields {
    +handler: entity_service/damageview/submission/get_fields.lambda_handler
    +endpoints: GET /solution_id/{solution_id}/fields\nGET /submission/{submission_id}/fields
  }
  class LocationsAssignee {
    +handler: entity_service/damageview/submission/set_locations_assignee.lambda_handler
    +endpoint: PATCH /locations/assignee
  }
  class GetAssignedLocations {
    +handler: entity_service/damageview/submission/get_assigned_locations.lambda_handler
    +endpoint: GET /locations/assignee
  }
  class ReportingLocationGrant {
    +handler: entity_service/damageview/submission/location_visibility.lambda_handler
    +endpoints: GET /location/{location_id}/grant\nPOST /location/{location_id}/grant\nPUT /location/{location_id}/grant
  }
  class DamageViewNotificationHandler {
    +handler: entity_service/damageview/notification_handler/notification_handler.lambda_handler
    +trigger: SQS ${opt:stage}-DV-add-notification
  }
  class PostProcessingSubmissionConsumer {
    +handler: entity_service/damageview/submission/post_processing_submission_consumer.lambda_handler
    +trigger: SQS ${opt:stage}-DV-PostProcessingDamageSubmissionCreation
  }
  class DamageStatusChangeIntegrationHandler {
    +handler: entity_service/damageview/damage_integration/handler.lambda_handler
    +trigger: SQS ${opt:stage}-DV-DamageSubmissionStatusChangeEventNotification
  }
```

> SVG rendering failed for this diagram.
