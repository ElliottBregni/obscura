# Diagram: entity_core/entity_service/entity_service_scripts/backfill_FIN_6526_trip_plan_complete.py


> Auto-generated by Obscura crawlers

## Diagram 1

```mermaid
flowchart TD
    Start([Start]) --> Main[main()]
    Main --> ParseArgs[parse_args()]
    ParseArgs --> SetArgs{args.limit\nargs.entity_id}
    Main --> DBConn[DB_CONN.establish_connection()]
    DBConn --> Cursor[DB_CONN.cursor]
    Main --> CallGet[get_last_status_update(cursor, entity_id, limit)]
    CallGet --> BuildQuery[mogrify(query)]
    BuildQuery --> ExecuteQuery[cursor.execute(query)]
    ExecuteQuery --> Fetch[output = cursor.fetchall()]
    Fetch --> Check{output empty?}
    Check -- Yes --> End([End])
    Check -- No --> ForEach[for each row in output]
    ForEach --> Extract[extract ts, external_id]
    Extract --> UpdateSQL[prepare UPDATE entity SET ...]
    UpdateSQL --> MogrifyUpdate[mogrify(sql)]
    MogrifyUpdate --> ExecuteUpdate[cursor.execute(sql)]
    ExecuteUpdate --> Increment[count += 1]
    Increment --> ForEach
    ForEach --> ReQuery{more rows from new fetch?}
    ReQuery -- Yes --> BuildQuery
    ReQuery -- No --> End([End])
```

> SVG rendering failed for this diagram.

## Diagram 2

```mermaid
sequenceDiagram
    participant CLI
    participant Main
    participant DB_CONN
    participant Cursor
    participant DB
    CLI->>Main: invoke script
    Main->>Main: parse_args()
    Main->>DB_CONN: DB_CONN.establish_connection()
    DB_CONN->>DB_CONN: open connection
    DB_CONN->>Cursor: create cursor
    Main->>Cursor: get_last_status_update(cursor, entity_id, limit)
    loop while True
        Cursor->>DB: mogrify(query) and execute(query)
        DB-->>Cursor: return rows
        alt rows returned
            loop for each row in rows
                Cursor->>DB: mogrify(update sql) and execute(update)
                DB-->>Cursor: update result
            end
            Cursor->>DB: execute(query) to fetch next batch
        else no rows
            DB-->>Cursor: empty result
            break
        end
    end
    Main-->>CLI: exit (script complete)
```

> SVG rendering failed for this diagram.
