# Diagram: entity_core/entity_service/entity_service_scripts/fix_status_location_id.py


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
flowchart LR
main[main()] --> condOffset{offset != -1}
condOffset -->|true| fetchAll[get_all_status_updates(cursor, offset, PAGE_SIZE)]
fetchAll --> forLoop[for status_update in status_updates]
forLoop --> increment[records_read = records_read + 1]
forLoop --> getLoc[get_shipper_locs(core_cursor, organization_id, code)]
getLoc --> cache[TTLCache cached(key = extract_org_cache_key)]
cache --> coreExec[core_cursor.execute(...) -> fetchone()]
coreExec --> checkUnknown{location_info == None or len(location_info) == 0}
checkUnknown -->|yes| printUnknown[print "Unknown location"]
checkUnknown -->|no| checkDifferent{status_update[1] == None or int(location_info[0]) != int(status_update[1])}
checkDifferent -->|yes| buildQuery[locations_different += 1\nquery = update_status_id(location_org_id, location_id, shipper_location_code)]
buildQuery --> uniqueCheck{query not in update_query}
uniqueCheck -->|true| appendQ[update_query.append(query)]
uniqueCheck -->|false| skipAppend[skip append]
checkDifferent -->|no| incOk[locations_ok += 1]
forLoop --> endFor[after iterating status_updates]
endFor --> checkRead{records_read >= total_count}
checkRead -->|yes| setOffsetEnd[offset = -1]
checkRead -->|no| more[print "records read..."; offset = offset + PAGE_SIZE]
setOffsetEnd --> exitPrint[print totals and print update_query]
more --> condOffset
exitPrint --> end[End]
```

> SVG rendering failed for this diagram.
