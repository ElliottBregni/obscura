# Diagram: entity_core/entity_service/entity_service_scripts/backfill_last_entity_position_source.py


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
flowchart TD
    Start((start)) --> Establish[Establish DB connections]
    Establish --> DB_CONN[DB_CONN.establish_connection()]
    Establish --> DB_CONN2[DB_CONN2.establish_connection()]
    DB_CONN2 --> Query["cursor2.execute(\"select * from solution s where feature_id in (6)\")"]
    Query --> ForEach[/for solution in cursor2/]
    ForEach --> SetSolutionId[solution_id = solution.external_id]
    SetSolutionId --> InitRes[res = True]
    InitRes --> WhileLoop{res?}
    WhileLoop -->|Yes| Mogrify1[update_last_position = cursor.mogrify(query1, {s_id: solution_id})]
    Mogrify1 --> Exec1[cursor.execute(update_last_position)]
    Exec1 --> Fetch1[cursor.fetchone() -> update_last_position_query]
    Fetch1 --> Print[len(update_last_position_query)]
    Print --> Increment[updated += 500]
    Fetch1 --> Mogrify2[last_entity_position_update = cursor.mogrify(query2, [tuple(...), {...}])]
    Mogrify2 --> Exec2[cursor.execute(last_entity_position_update)]
    Exec2 --> SetResEval{update_last_position_query ?}
    SetResEval -->|True| WhileLoop
    SetResEval -->|False| EndSolution[end solution loop]
    EndSolution --> ForEach
    ForEach -->|no more| End((end))
```

> SVG rendering failed for this diagram.
