# Diagram: entity_core/entity_service/entity_service_scripts/add_milestone_grouping.py


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
flowchart TD
    Start([Script Load]) --> CheckMain{"__name__ == '__main__'?"}
    CheckMain -- yes --> InstantiateDB[Create FvDatabaseConnector add_milestone_grouping, SecretNames.ENTITY_DATABASE]
    InstantiateDB --> MainFunc[/main()/]
    MainFunc --> Cursor[Get cursor from DB_CONN]
    MainFunc --> Vars[Set solution_id = HYUNDAI_FV<br/>milestone_codes = [PA]<br/>ticket = ISS-17029]
    Cursor --> PrepareSQL[Prepare SQL update query using ufn_add_values_to_non_array_text_array(metadata::text, %(milestone_codes)s::text[])]
    PrepareSQL --> Execute[Execute cursor.execute(sql, params)]
    Execute --> DBUpdate[(system_configuration rows updated; actor_id set to ticket)]
    DBUpdate --> Print[Print "Updated {cursor.rowcount} rows"]
    Print --> End([Exit])
    CheckMain -- no --> End
```

> SVG rendering failed for this diagram.
