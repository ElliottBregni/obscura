# Diagram: entity_core/entity_service/entity_service_scripts/backfill_dealer_allowed_origin.py


> Auto-generated by Obscura crawlers

## Diagram 1

```mermaid
flowchart TD
    Main[main()] --> GetSolutions[get_fv_solutions(cursor2)]
    GetSolutions --> Solutions[solutions list]
    Main --> Loop[while True]
    Loop --> ForLoop[for solution in solutions]
    ForLoop --> PrintSol[print(solution)]
    ForLoop --> Blacklist[get_blacklisted_locations(cursor2)]
    ForLoop --> Entities[get_entities(cursor, solution)]
    Entities --> EntitiesList[entities list]
    ForLoop --> UpdateOrigins[update_dealer_origins(cursor, entities, blacklist)]
    UpdateOrigins --> ForEachEntity[for entity_id in entities]
    ForEachEntity --> UpdateOne[update_dealer_origin(cursor, entity_id, blacklist)]
    UpdateOne --> QueryStops[SQL: SELECT planned_trip_stop JOIN planned_trip_leg JOIN entity_planned_trip_leg JOIN entity WHERE e.id=entity_id ORDER BY UPPER(scheduled_arrival_window)]
    QueryStops --> Stops[stops = cursor.fetchall()]
    Stops --> FindNext[reverse stops; find first not in blacklist => next_location]
    FindNext --> InsertSQL[INSERT INTO entity_dealer_origin(entity_id, dealer_ultimate_origin) VALUES(...) ON CONFLICT DO UPDATE]
    InsertSQL --> ReturnLoc[return next_location]
    UpdateOne --> PrintResult[print(entity_id, returnval.code)]
    UpdateOrigins --> PrintElapsed[print elapsed time]
```

> SVG rendering failed for this diagram.

## Diagram 2

```mermaid
sequenceDiagram
    participant M as main()
    participant C2 as DB_CONN2.cursor
    participant C1 as DB_CONN.cursor
    participant GFV as get_fv_solutions()
    participant GBL as get_blacklisted_locations()
    participant GENT as get_entities()
    participant UDO as update_dealer_origins()
    participant UDOE as update_dealer_origin()
    participant DB as Database

    M->>C2: call get_fv_solutions(cursor2) (SELECT external_id FROM solution ...)
    C2-->>M: list of solution external_ids
    M->>GBL: call get_blacklisted_locations(cursor2)
    GBL->>DB: execute blacklist query
    DB-->>GBL: list of blacklisted location ids
    GBL-->>M: blacklist
    M->>GENT: call get_entities(cursor, solution)
    GENT->>DB: execute entity query (jsonb stops filter, state='Active', no entity_dealer_origin)
    DB-->>GENT: rows of entity ids
    GENT-->>M: entities
    M->>UDO: call update_dealer_origins(cursor, entities, blacklist)
    UDO->>UDOE: for each entity_id call update_dealer_origin(cursor, entity_id, blacklist)
    UDOE->>DB: SELECT JSON_BUILD_OBJECT(...) FROM planned_trip_stop JOIN planned_trip_leg JOIN entity_planned_trip_leg JOIN entity WHERE e.id=entity_id ORDER BY UPPER(...)
    DB-->>UDOE: stops list
    UDOE->>UDOE: reverse stops; find next non-blacklisted stop
    UDOE->>DB: INSERT INTO entity_dealer_origin ... ON CONFLICT UPDATE
    DB-->>UDOE: insert/update result
    UDOE-->>UDO: return next_location
    UDO-->>M: finished updating origins (prints and elapsed time)
```

> SVG rendering failed for this diagram.
