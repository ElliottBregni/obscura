# Diagram: entity_core/entity_service/entity_service_scripts/backfill_DPU-1323.py


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
flowchart LR
  Start((Start)) --> InitDB[DB_CONN_ENTITY = fv.db.FvDatabaseConnector(...)]
  InitDB --> Cursor[cursor = DB_CONN_ENTITY.cursor]
  Cursor --> LogExec1[print("executing ENTITY_ID_SQL")]
  LogExec1 --> ExecuteEntitySQL[Execute ENTITY_ID_SQL]
  ExecuteEntitySQL --> LogExec2[print("executed ENTITY_ID_SQL")]
  LogExec2 --> FetchAll[res = cursor.fetchall()]
  FetchAll --> SplitList[res_list_of_lists = fv.aws.lambdas.split_list(res, 500)]
  SplitList --> ProgInit[prog = Progress.Progress(len(res_list_of_lists), skip=5)]
  ProgInit --> ForLoop[for i, res_list in enumerate(res_list_of_lists)]
  ForLoop --> OutputProgress[prog.output_progress(i)]
  OutputProgress --> ExtractIDs[entity_internal_ids = [row.entity_id for row in res_list]]
  ExtractIDs --> Mogrify[query = cursor.mogrify(UPDATE_VIN_DATA_SQL, {"entity_ids": tuple(entity_internal_ids)})]
  Mogrify --> ExecuteUpdate[cursor.execute(query)]
  ExecuteUpdate --> PrintProgress[print("{i} / {len(res_list_of_lists)}")]
  PrintProgress --> End((End))
  subgraph constants [Constants & Globals]
    ENTITY_ID_SQL["ENTITY_ID_SQL (SELECT entity_id ...)"]
    UPDATE_VIN_DATA_SQL["UPDATE_VIN_DATA_SQL (WITH max_ts_entity ... UPDATE ...)"]
    CUSTOMER_MEMORIZE["CUSTOMER_MEMORIZE = {}"]
    DB_CONN_ENTITY_REF["DB_CONN_ENTITY <- used to open cursor"]
  end
  ExecuteEntitySQL ---|uses| ENTITY_ID_SQL
  ExecuteUpdate ---|uses| UPDATE_VIN_DATA_SQL
  InitDB --- DB_CONN_ENTITY_REF
  Start --- constants
```

> SVG rendering failed for this diagram.
