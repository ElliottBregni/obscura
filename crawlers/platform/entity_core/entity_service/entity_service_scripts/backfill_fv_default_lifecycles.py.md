# Diagram: entity_core/entity_service/entity_service_scripts/backfill_fv_default_lifecycles.py


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
flowchart TD
  Start([Start]) --> A[SECRETS = Secrets() / main()]
  A --> B[config = SECRETS.get_secret(SecretNames.ENTITY_DATABASE)]
  B --> C{fv.db.get_connection(config,\nDB_APP_NAME="backfill_entity_location_ids",\nautocommit=True)}
  C --> D[with conn as conn]
  D --> E{fv.db.get_cursor(conn)}
  E --> F[with cursor as cursor]
  F --> G[res = True<br/>updated = 0]
  G --> H{res is truthy?}
  H -- yes --> I{updated % 1000 == 0?}
  I -- yes --> J[print(updated)]
  I -- no --> K[skip print]
  J --> L[sql = multi-line UPDATE query]
  K --> L
  L --> M[cursor.execute(sql)]
  M --> N[res = cursor.fetchone()]
  N --> O[updated += 100]
  O --> H
  H -- no --> End([End])
```

> SVG rendering failed for this diagram.
