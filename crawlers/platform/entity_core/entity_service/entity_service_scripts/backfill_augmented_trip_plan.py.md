# Diagram: entity_core/entity_service/entity_service_scripts/backfill_augmented_trip_plan.py


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
flowchart TD
  subgraph Configuration
    DB_CONN_LOGGING["fv.db.FvDatabaseConnector\n(DB_CONN_LOGGING)"]
    DB_CONN_ENTITY["fv.db.FvDatabaseConnector\n(DB_CONN_ENTITY)"]
  end
  subgraph Execution
    A[main()]
    B[parse_args()]
    C{args.vins present?}
    D["entity_ids = args.vins.strip().split(',')"]
    E[get_entity_ids(organization_id)]
    F[print \"No. of VINs to be reprocessed: len(entity_ids)\"]
    G["publish_to_fv_trip_leg_augmentation_queue(\nSystemConfigurationDAO(DB_CONN_ENTITY),\nargs.solution_id, entity_ids)"]
  end
  A --> B
  B --> C
  C -- yes --> D
  C -- no --> E
  D --> F
  E --> DB_CONN_LOGGING
  DB_CONN_LOGGING --> H[establish_connection()]
  H --> I[get_cursor()]
  I --> J[execute(query, {organization_id})]
  J --> K[fetchall() -> list of entity_external_id]
  K --> F
  F --> G
```

> SVG rendering failed for this diagram.
