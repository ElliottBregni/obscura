# Diagram: entity_core/entity_service/entity_service_scripts/backfill_dwell_location_ids.py


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
flowchart TD
    Start([Start]) --> Main[main()]
    Main --> Secrets[SECRETS.get_secret(ENTITY_DATABASE) / SECRETS.get_secret(DATABASE)]
    Secrets --> ConnApp[fv.db.get_connection(config, DB_APP_NAME=backfill_dwell_location_ids, autocommit=true)]
    ConnApp --> CursorApp[fv.db.get_cursor(conn)]
    Secrets --> ConnCore[fv.db.get_connection(core_config, DB_APP_NAME=backfill_dwell_location_ids, autocommit=true)]
    ConnCore --> CursorCore[fv.db.get_cursor(core_conn)]
    CursorApp --> GetAll[get_all_edw(cursor)]
    GetAll --> UpdateOrgs[update_corresponding_organization_ids(core_cursor, edws)]
    UpdateOrgs --> Group[group_edws(edws)]
    Group --> ForEach{for organization_id, codes in edw_dict}
    ForEach --> GetShipper[get_shipper_locs(core_cursor, organization_id, codes)]
    GetShipper --> UpdateLoc[update_location_id(cursor, shipper_locs, edws, organization_id)]
    UpdateLoc --> ForEach
    ForEach --> CloseConns[close conn & core_conn]
    CloseConns --> End([End])
```

> SVG rendering failed for this diagram.
