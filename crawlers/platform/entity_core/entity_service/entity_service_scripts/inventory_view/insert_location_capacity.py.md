# Diagram: entity_core/entity_service/entity_service_scripts/inventory_view/insert_location_capacity.py


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
flowchart TD
  Main[main()] --> Parse[parse_arguments()]
  Parse --> Args["args: csv_path, capacity, org_fv_id"]
  Main --> ConnE[DB_CONN_ENTITY]
  Main --> ConnS[DB_CONN_SHIPMENTS]
  Main --> ReadCSV["pd.read_csv(csv_path) -> location_df"]
  Main --> GetDB["get_db_id(shipments_cursor, org_fv_id)"]
  ConnE -->|provides| EntityCursor(entity_cursor)
  ConnS -->|provides| ShipmentsCursor(shipments_cursor)
  GetDB --> DBOrg["SELECT id FROM organizations WHERE fv_id=%(fv_id)s"]
  DBOrg -->|fetchone()| DBID[db_id]
  ReadCSV --> InsertLoc["insert_locations(entity_cursor, db_id, location_df, capacity)"]
  InsertLoc --> LoopRows["for each row in location_df"]
  LoopRows --> DetermineCap["capacity = row['Capacity'] or default_capacity"]
  DetermineCap --> Upsert["UPSERT INTO entity_location_information(location_id, capacity)"]
  ReadCSV --> InsertFeat["insert_location_feature(shipments_cursor, db_id, location_df)"]
  InsertFeat --> CollectCodes["location_codes = unique(Location Code)"]
  CollectCodes --> FeatureInsert["INSERT INTO location.location_feature (location_id, feature_code='INV') ON CONFLICT DO NOTHING"]
  Args --> GetDB
  Args --> InsertLoc
  Args --> InsertFeat
```

> SVG rendering failed for this diagram.
