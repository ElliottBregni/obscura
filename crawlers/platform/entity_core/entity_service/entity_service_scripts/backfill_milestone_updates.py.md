# Diagram: entity_core/entity_service/entity_service_scripts/backfill_milestone_updates.py


> Auto-generated by Obscura crawlers

## Diagram 1

```mermaid
flowchart TD
    Start((Start)) --> Main[main()]
    Main --> Parse[parse_arguments()]
    Main --> ReadCSVs[Read CSVs\nstatus_updates, references]
    ReadCSVs --> BuildCombined[build_combined_data(status_updates, references)]
    BuildCombined --> BuildAndSend[build_and_send_status_updates(combined_data)]
    BuildAndSend --> Loop{for each status in combined_data.values()}
    Loop -->|i == 0| SkipFirst[skip first iteration]
    Loop -->|otherwise| BuildStatus[build_status_update(status_dict)]
    BuildStatus --> GetEntity[get_entity_ext_id(status_dict["entity_id"])]
    GetEntity --> DB[(DB_CONN.cursor -> entity table)]
    GetEntity -->|not found| Print[print "Entity_id ... does not exist"] --> Loop
    GetEntity -->|found| PrepareEvent[set event["pathParameters"] and event["body"]]
    PrepareEvent --> Invoke[fv.aws.lambdas.invoke_lambda("add_entity_status_update", full_payload)]
    Invoke --> Loop
    Loop --> End((End))
```

> SVG rendering failed for this diagram.

## Diagram 2

```mermaid
classDiagram
    class BackfillScript {
        +main()
        +parse_arguments()
        +build_combined_data(status_updates, references)
        +build_status_update(status_dict)
        +build_and_send_status_updates(combined_data)
    }
    class FvDatabaseConnector {
        +cursor
        +execute(query, params)
        +fetchone()
    }
    class fv_aws_lambdas {
        +get_sample_admin_event(method, solutions)
        +invoke_lambda(name, full_payload)
    }
    class SecretNames
    class csv_DictReader
    BackfillScript --> FvDatabaseConnector : uses DB_CONN
    BackfillScript --> fv_aws_lambdas : uses
    BackfillScript --> csv_DictReader : reads files
    BackfillScript ..> SecretNames : references
    FvDatabaseConnector ..> "entity table" : queries
```

> SVG rendering failed for this diagram.
