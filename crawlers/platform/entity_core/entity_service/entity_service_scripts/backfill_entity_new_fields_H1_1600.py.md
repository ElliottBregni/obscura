# Diagram: entity_core/entity_service/entity_service_scripts/backfill_entity_new_fields_H1_1600.py


> Auto-generated by Obscura crawlers

## Diagram 1

```mermaid
flowchart LR
    subgraph config["Configuration"]
        SECRETS[Secrets()] -->|get_secret(SecretNames.ENTITY_DATABASE)| entity_write[entity_write]
        SOLUTION_ID["SOLUTION_ID = \"FV_TEST\""]
    end
    __main__["__main__"] -->|uses| fvdb[fv.db]
    fvdb -->|get_connection(entity_write, DB_APP_NAME, autocommit)| connection[(connection)]
    connection -->|get_cursor(connection)| cursor[(cursor)]
    cursor -->|execute(update_sql / count_sql)| database[(Postgres DB)]
    __main__ -->|calls| backfill[backfill_new_fields(cursor, get_sql_status)]
    backfill -->|invokes| get_sql_status[get_sql_status]
    backfill -->|may invoke| get_sql_progress[get_sql_progress]
    backfill -->|may invoke| get_sql_position[get_sql_position]
    backfill -->|may invoke| get_sql_ult[get_sql_ult]
    get_sql_status -->|returns| count_sql_status["count_sql"]
    get_sql_status -->|returns| update_sql_status["update_sql"]
    get_sql_progress -->|returns| count_sql_progress["count_sql"]
    get_sql_progress -->|returns| update_sql_progress["update_sql"]
    get_sql_position -->|returns| count_sql_position["count_sql"]
    get_sql_position -->|returns| update_sql_position["update_sql"]
    get_sql_ult -->|returns| count_sql_ult["count_sql"]
    get_sql_ult -->|returns| update_sql_ult["update_sql"]
    backfill -->|loops (offset, batch_size)| cursor
    database -->|rows| cursor
```

> SVG rendering failed for this diagram.

## Diagram 2

```mermaid
sequenceDiagram
    participant Main as __main__
    participant DB as fv.db
    participant Conn as connection
    participant Cur as cursor
    participant Back as backfill_new_fields
    participant SQL as get_sql_status

    Main->>DB: get_connection(entity_write, DB_APP_NAME, autocommit=True)
    DB-->>Main: connection
    Main->>DB: get_cursor(connection)
    DB-->>Main: cursor
    Main->>Back: backfill_new_fields(cursor, get_sql_status)
    Back->>SQL: get_sql_status(SOLUTION_ID, batch_size, offset)
    SQL-->>Back: (count_sql, update_sql)
    Back->>Cur: execute(count_sql)  -- obtain total
    Cur-->>Back: total
    loop while offset < total
        Back->>SQL: get_sql_status(SOLUTION_ID, batch_size, offset)
        SQL-->>Back: (count_sql, update_sql)
        Back->>Cur: execute(update_sql)
        Cur-->>Back: update_result
        Back->>Cur: execute(count_sql)
        Cur-->>Back: remaining_count
        Back->>Back: offset += batch_size; updated = total - remaining_count
    end
    Back-->>Main: finished (time elapsed)
```

> SVG rendering failed for this diagram.
