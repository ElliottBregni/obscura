# Diagram: entity_core/entity_service/entity_service_scripts/backfill_FIN-7972.py


> Auto-generated by Obscura crawlers

## Diagram 1

```mermaid
flowchart TD
Start([Start]) --> MainFun[main()]
MainFun --> ParseArgs[parse_args()]
ParseArgs --> Limit[limit = args.limit]
MainFun --> Establish[DB_CONN.establish_connection()]
Establish --> CursorAssign[cursor = DB_CONN.cursor]
CursorAssign --> UpdateCall[update_last_entity_position_update(cursor, limit)]
UpdateCall --> Loop{last_count != 0}
Loop -->|true| Exec1[execute SQL for last_position_update]
Exec1 --> SetLast1[set last_count = cursor.rowcount]
SetLast1 --> Exec2[execute SQL for last_entity_position_update]
Exec2 --> AddLast[last_count += cursor.rowcount]
AddLast --> Total[total_count += last_count]
Total --> Print[print Total processed]
Print --> Loop
Loop -->|false| End([End])
```

> SVG rendering failed for this diagram.

## Diagram 2

```mermaid
sequenceDiagram
participant Main as main()
participant Parse as parse_args()
participant DB as DB_CONN
participant Update as update_last_entity_position_update()
participant Cursor as cursor
Main->>Parse: call parse_args()
Parse-->>Main: return args
Main->>DB: establish_connection()
DB-->>Main: connection ready
Main->>DB: get cursor
DB-->>Main: cursor
Main->>Update: call update_last_entity_position_update(cursor, limit)
loop while last_count != 0
  Update->>Cursor: execute SQL (last_position_update)
  Cursor-->>Update: rowcount
  Update->>Cursor: execute SQL (last_entity_position_update)
  Cursor-->>Update: rowcount
  Update->>Update: last_count += cursor.rowcount; total_count += last_count
  Update->>Main: print Total processed
end
Update-->>Main: return
```

> SVG rendering failed for this diagram.
