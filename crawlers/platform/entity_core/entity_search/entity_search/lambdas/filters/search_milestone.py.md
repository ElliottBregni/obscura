# Diagram: entity_core/entity_search/entity_search/lambdas/filters/search_milestone.py


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
flowchart TD
    LH[lambda_handler] -->|routes to| GS[get_status_updates]
    LH -->|single id| GSU[get_status_update]
    LH -->|establish| DB[DB_CONN_ENTITY]
    DB -->|provides cursor| GS
    GS -->|builds query via| MS[msSupport.MilestoneBaseFilteredQuery]
    GS -->|gets query results| GMR[get_milestone_result]
    GMR -->|calls| LDB1[list_db.get_milestone_dashboard_by_filter_criteria]
    GSU -->|fetches by id| LDB2[list_db.get_milestone_dashboard_by_status_id]
    GS -->|may call| GET_MAP[get_entity_to_group_categories_mapping]
    GET_MAP -->|invokes| INVOKE_GC[fv.aws.lambdas.invoke_lambda("get_group_category")]
    INVOKE_GC -->|returns| GET_MAP
    GET_MAP -->|returns mapping| GS
    GS -->|may call| CACHE_LOC[get_cache_locations_codes]
    CACHE_LOC -.->|cached TTL| CACHE_SYS[TTLCache (cached)]
    CACHE_LOC -->|invokes| INVOKE_LOC[fv.aws.lambdas.invoke_lambda("locations-get")]
    INVOKE_LOC -->|returns| CACHE_LOC
    GS -->|may call| CACHE_CUST[get_cache_customer_code]
    CACHE_CUST -.->|cached TTL| CACHE_SYS
    CACHE_CUST -->|calls| GET_ORG[get_organization]
    GET_ORG -->|invokes| INVOKE_ORG[fv.aws.lambdas.invoke_lambda("get_organizations")]
    INVOKE_ORG -->|returns| GET_ORG
    GS -->|may call| CACHE_SRC[get_cache_source_id]
    CACHE_SRC -->|uses| GET_ORG_FV[get_organization_by_fv_id]
    GET_ORG_FV -->|proxy to| INVOKE_ORG_FV[fv.aws.lambdas.invokinator.get_organization]
    GS -->|on CSV| CREATE_CSV[create_csv]
    CREATE_CSV -->|uses| LDB3[list_db.get_status_references_for_status_updates]
    CREATE_CSV -->|exports via| CSV_EXP[fv.aws.lambdas.csv_export]
    GS -->|enhances rows via| ENH[enhance_milestone_dashboard_data]
    ENH -->|may call| CACHE_LOC
    ENH -->|may call| CACHE_CUST
    ENH -->|may call| CACHE_SRC
    GS -->|search helper| GET_SEARCH[get_search_locations]
    GET_SEARCH -->|calls| GET_LOCATIONS[get_locations_search_everything]
    GET_LOCATIONS -->|invokes| INVOKE_LOC
    MS -->|provides Sort/Filter enums| GS
    subgraph ExternalLibraries
        FV[fv.aws.lambdas]
        LISTDB[list_db]
        MSL[msSupport module]
    end
    GS --> FV
    GMR --> LISTDB
    MS --> MSL
```

> SVG rendering failed for this diagram.
