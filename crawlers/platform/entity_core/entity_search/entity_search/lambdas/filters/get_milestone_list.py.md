# Diagram: entity_core/entity_search/entity_search/lambdas/filters/get_milestone_list.py


> Auto-generated by Obscura crawlers

## Diagram 1

```mermaid
flowchart LR
    Start([Start]) --> ValidateCall[/"validate(event)"/]
    ValidateCall -->|valid| CheckOrg{organization_id present?}
    ValidateCall -->|invalid| BadRequest[/"BadRequestError raised"/]
    CheckOrg -- No --> Forbidden[/"ForbiddenError: missing organization_id"/]
    CheckOrg -- Yes --> EstablishEntity[/"DB_CONN_ENTITY.establish_connection()"/]
    EstablishEntity --> GetCursor[/"cursor = DB_CONN_ENTITY.cursor"/]
    GetCursor --> GetLists[/"get_lists(event, cursor, query_params, organization_id)"/]
    subgraph get_lists_flow ["get_lists"]
        GetLists --> DetermineParams{query_params contains...}
        DetermineParams -->|customer_shipper| CustBranch[get_cache_customer_shipper (cached)]
        CustBranch --> GetOrgLoop["loop: get_organization(org_id)"]
        GetOrgLoop --> AppendShippers["append {id,name} to list_dict.shippers"]
        DetermineParams -->|milestone_code| CodeBranch[get_cache_milestone_codes (cached)]
        CodeBranch --> AppendCodes["list_dict.milestone_code"]
        DetermineParams -->|milestone_description| DescBranch[get_cache_milestone_descrip (cached)]
        DescBranch --> AppendDesc["list_dict.milestone_description"]
        DetermineParams -->|location| LocBranch[get_cache_locations_id (cached)]
        LocBranch --> FormatLoc["format_location_names(cache_locations)"]
        FormatLoc --> AppendLoc["list_dict.locations = formatted locations"]
        LocBranch --> ComputeMeta["meta = {total_count, total_pages, current_page}"]
        ComputeMeta --> AppendMeta["list_dict.meta = meta"]
        DetermineParams --> Continue["other flows / return list_dict"]
    end
    GetLists --> MakeResponse[/"fv.aws.lambdas.make_response(list_dict)"/]
    MakeResponse --> End([End])
    %% exception path
    GetLists -->|exception| Traceback["traceback.print_exc() and re-raise"]
```

> SVG rendering failed for this diagram.

## Diagram 2

```mermaid
classDiagram
    class FvDatabaseConnector {
        +establish_connection()
        +cursor
    }
    class CacheFunctions {
        +get_cache_customer_shipper(event,cursor,organization_id,query_type)
        +get_cache_milestone_codes(event,cursor,organization_id,query_type)
        +get_cache_milestone_descrip(event,cursor,organization_id,query_type)
        +get_cache_locations_id(event,organization_id,cursor,fuzzy_text,page_number,page_size)
        +get_cache_milestone_location_code_entity(event,cursor,location_code,organization_id)
    }
    class Organization {
        +get_organization(event,id)
        +get_organization_authorization(id)
    }
    class Utils {
        +extract_key(event,cursor,organization_id,query_type)
        +extract_key_location(event,cursor,location_id)
        +extract_key_location_code(event,cursor,location_code,organization_id)
        +format_location_names(locations)
        +validate(event)
    }
    class LambdaHandler {
        +lambda_handler(event,context,audit_refs)
        -AUTH_CHECK
    }
    class EntitySearchDB {
        +get_milestone_organization_entity(cursor,organization_id)
        +get_milestone_code_cache(cursor,organization_id)
        +get_milestone_description_cache(cursor,organization_id)
        +get_milestone_shipper_location_cache(cursor,organization_id,fuzzy_text,page_number,page_size)
        +get_milestone_location_entity_by_code(cursor,organization_id,location_code)
    }
    class DB_CONN_TRACKING <<instance>> {}
    class DB_CONN_ENTITY <<instance>> {}

    DB_CONN_TRACKING --|> FvDatabaseConnector : "instance_of"
    DB_CONN_ENTITY --|> FvDatabaseConnector : "instance_of"
    LambdaHandler --> Utils : "calls/uses"
    LambdaHandler --> CacheFunctions : "calls (cached funcs)"
    CacheFunctions --> EntitySearchDB : "queries"
    CacheFunctions --> Organization : "invokes"
    LambdaHandler --> FvDatabaseConnector : "establish_connection"
    EntitySearchDB ..> DB_CONN_ENTITY : "uses cursor from"
    LambdaHandler ..> DB_CONN_TRACKING : "establish_connection for tracking"
    LambdaHandler ..> AUTH_CHECK : "decorated with"
```

> SVG rendering failed for this diagram.
