# Diagram: entity_core/entity_search/entity_search/db/entity_extract_support.py


> Auto-generated by Obscura crawlers

## Diagram 1

```mermaid
classDiagram
class ExtractSupport {
  +references_requested
  +include_leg
  +version
  +sql
  +add_sql_parts(event_types)
  +build_sql()
  +__get_milestone_sql()
  +__get_exception_sql()
  +__get_hold_sql()
  +__get_diagnostic_sql()
  +__get_references_sql()
  +__get_progress_sql()
  +__get_dealer_progress_sql()
  +__get_dwell_updates_sql()
  +__current_location_updates_sql()
  +__make_references_subquery()
  +__get_trip_leg_subquery()
  +__get_progress_columns(column_type="prog")
}
class EntityEventType <<enumeration>> {
  MILESTONE
  EXCEPTION
  HOLDS
  DIAGNOSTICS
  REFERENCES
  PROGRESS
  DEALER_PROGRESS
  DWELL_UPDATE
  CURRENT_LOCATION_UPDATE
}
class VersionOptions <<enumeration>> {
  EntityExtract.PROGRESS_V2
}
class find_location_update_references <<function>> {
  +db_cursor
  +position_update_id
}
class __pur_subquery <<function>> {
  +qualifier_name
  +pos_update_id
}
class find_position_update_references <<function>> {
  +cursor
  +pur_update_id
}
ExtractSupport ..> EntityEventType : uses mapping
ExtractSupport ..> VersionOptions : checks version
find_location_update_references ..> __pur_subquery : calls (multiple)
find_location_update_references --> find_position_update_references : related domain
find_position_update_references ..> VersionOptions : may reference same constants
```

> SVG rendering failed for this diagram.

## Diagram 2

```mermaid
flowchart LR
  subgraph AddSQLPartsFlow
    A[Start: add_sql_parts(event_types)] --> B[Build type_to_func_mapping]
    B --> M[MAPPING: EntityEventType.MILESTONE --> __get_milestone_sql()]
    B --> X[... other mappings ...]
    M --> C{Iterate event_types}
    X --> C
    C -->|for each event_type| D[Append mapping[event_type] to sql list]
    D --> C
    C --> E[Done: sql list populated]
    E --> F[build_sql() joins with "UNION ALL"]
  end
```

> SVG rendering failed for this diagram.

## Diagram 3

```mermaid
flowchart LR
  subgraph LocationRefsFlow
    L1[find_location_update_references(db_cursor, position_update_id)] --> L2[Compose SQL using __pur_subquery for each qualifier]
    L2 --> L3[db_cursor.mogrify(sql)]
    L3 --> L4[db_cursor.execute(query)]
    L4 --> L5[fetchone()[0] -> result JSON]
    L5 --> L6[return JSON]
  end
```

> SVG rendering failed for this diagram.
