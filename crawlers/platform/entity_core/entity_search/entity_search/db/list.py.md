# Diagram: entity_core/entity_search/entity_search/db/list.py


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
flowchart LR
  Caller[Event / API Caller]
  subgraph Core["Core functions"]
    GF[get_* functions\n(e.g., get_stop_sql, get_location_ids,\nget_reference_vals, get_product_types,\nget_carriers, get_pickup_locations)]
    MS[get_milestone_* functions\n(e.g., get_milestone_dashboard_by_status_id,\nget_milestone_events_by_time,\nget_milestone_shipper_location_cache)]
    CACHE[(TTLCache cached\n(@cached wrappers))]
  end
  subgraph DB["Postgres (cursor)"]
    DB[(Database / SQL)]
  end
  subgraph Libs["External libs & services"]
    ES[entity_search.common.list]
    SC[system_config_invoker]
    FV[fv.aws.lambdas\n(invoke_lambda, location)]
    MD[md.MilestoneDashboardEntity]
    LOG[fv.log / logging]
  end

  Caller --> GF
  Caller --> MS
  GF -->|queries via cursor| DB
  MS -->|queries via cursor| DB
  GF --> ES
  GF --> SC
  GF --> LOG
  MS --> FV
  MS --> MD
  MS --> LOG
  MS --> CACHE
  GF --> CACHE
  CACHE --> GF
  DB -->|rows/results| GF
  DB -->|rows/results| MS
  FV -->|location data| MS
  ES --> GF
  SC --> GF
  LOG --> GF
  LOG --> MS
```

> SVG rendering failed for this diagram.
