# Diagram: entity_core/entity_search/entity_search/common/emailer.py


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
classDiagram
    class Emailer <<abstract>> {
        +send_email(to: list[string], subject: string, body: string)
    }
    class SmtpEmailer {
        +send_email(to: list[string], subject: string, body: string)
    }
    class email_sender <<module>> {
        +send_email(recipients_to: list[string], subject: string, body: string)
    }
    SmtpEmailer --|> Emailer
    SmtpEmailer ..> email_sender : calls
```

> SVG rendering failed for this diagram.
