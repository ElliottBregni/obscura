# Diagram: entity_core/entity_search/tests/unit_tests/db/test_customize_last_milestone_options_for_hold.py


> Auto-generated by Obscura crawlers

## Diagram 1

```mermaid
flowchart TD
  A[Inputs: last_milestone_query_results<br/>hold_sys_config<br/>solution_ids] --> B{Iterate solution_ids}
  B -->|config exists| C{apply_last_milestone_filter true?}
  C -->|Yes| D[Add configured last_milestone option (last_milestone_code / last_milestone_desc)]
  C -->|No| E[Check query results for solution_id entries]
  B -->|config missing| F{DEFAULT present?}
  F -->|Yes| D
  F -->|No| E
  E --> G{Any query results for solution_id?}
  G -->|Yes| H[Add each query result's code / code_description]
  G -->|No| I[No option added for this solution_id]
  E --> J{Any query result is_hold == true?}
  J -->|Yes and corresponding config apply_last_milestone_filter is false| K[Include existing hold code(s) from query_results]
  J -->|Yes and corresponding config apply_last_milestone_filter is true| L[Skip adding individual hold codes; configured hold already added]
  J -->|No| M[Nothing additional to add]
  H --> N[Accumulate options in order of processing]
  D --> N
  K --> N
  L --> N
  M --> N
  I --> N
  N --> O[Return ordered milestone options list]
```

> SVG rendering failed for this diagram.

## Diagram 2

```mermaid
flowchart TD
  A[Fuzzy value<br/>milestone_options] --> B{fuzzy_value is empty?}
  B -->|Yes| C[Return all milestone_options]
  B -->|No| D[Filter milestone_options where code or codeDescription contains fuzzy_value (case-insensitive)]
  D --> E[Return filtered list of milestone_options]
```

> SVG rendering failed for this diagram.
