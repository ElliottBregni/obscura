# Diagram: entity_core/watcher_service/watcher_service_tests/ocean_watcher_tests/test_ocean_vessel.py


> Auto-generated by Obscura crawlers

## Diagram 1

```mermaid
classDiagram
    class TestModule {
        +TEST_DIR: str
        +FIXTURE_DIR: str
        +ALL_FILES: pytest.mark
        +test_ocean_configuration()
        +test_ocean_access()
        +get_dock_data(datafiles)
        +test_new_ports(datafiles)
        +test_updated_shipments(datafiles)
    }
    class VesselConfigurations {
        +access_key: str
        +ocean_access_url: str
    }
    class VesselDataAccess {
        +get_vessel_calls(vessel_id)
    }
    class ShipmentHelper {
        +ports_visited
        +shipment_data
        +shipper_full_organization
        +new_ports
        +get_updated_shipment()
    }
    TestModule ..> VesselConfigurations : uses/instantiates
    TestModule ..> VesselDataAccess : uses/instantiates
    TestModule ..> ShipmentHelper : constructs/uses
    ShipmentHelper --> "dock_data.dat, shipmentdata.dat" : reads_from
```

> SVG rendering failed for this diagram.

## Diagram 2

```mermaid
flowchart TD
    A[Start: tests module] --> B[test_ocean_configuration]
    B --> C[Instantiate VesselConfigurations]
    C --> D{Assert configuration values}
    A --> E[test_ocean_access (skipped)]
    A --> F[get_dock_data(datafiles)]
    F --> G[Open dock_data.dat]
    G --> H[ast.literal_eval -> dock_data]
    F --> I[Open shipmentdata.dat]
    I --> J[ast.literal_eval -> shipment_data]
    H --> K[Prepare full_shipper_organization]
    J --> L[Instantiate ShipmentHelper(ports_visited, shipment_data, shipper_full_organization)]
    L --> M[return helper]
    M --> N[test_new_ports (skipped)]
    N --> O[Open new_port.dat]
    O --> P[Compare helper.new_ports to expected]
    M --> Q[test_updated_shipments (skipped)]
    Q --> R[Open updated_shipments.dat]
    R --> S[Compare helper.get_updated_shipment() to expected]
```

> SVG rendering failed for this diagram.
