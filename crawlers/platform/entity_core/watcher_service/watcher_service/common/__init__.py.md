# Diagram: entity_core/watcher_service/watcher_service/common/__init__.py


> Auto-generated by Obscura crawlers

## Diagram 1

```mermaid
classDiagram
class WatcherModule {
  +secrets: Secrets
  +app_name: str
  +entity_read
  +entity_write
  +db_read
  +BEHIND_SCHEDULE_EXCEPTION_TYPE_ID: int
  +IN_HOLD_EXCEPTION_TYPE_ID: int
  +EXCESSIVE_DWELL_EXCEPTION_TYPE_ID: int
  +EXCEPTION_COMMENTS: str
  +json_dumps(o)
  +invoke_set_exception(event, entity_external_id, solution_id, org_id, fv_event={}, exception_to_clear=None, invoke_type="Event")
  +batch_set_columns_single_value(table_name, row_ids, columns={}, cursor=None)
  +__life_cycle_watcher_read(sql, replaces={}, cursor=None, many=True)
  +__life_cycle_watcher_write(sql, replaces={}, cursor=None)
  +get_entites_state(state)
  +get_entites_rules(solution_id, state)
  +get_entites_rules_to(solution_id, state)
  +get_distinct_solution_id()
  +unwatch_entites(cursor, Entity)
  +set_batch(batch_list, replaces={})
  +__get_entity_state(entity_id, cursor)
  +get_solution_type_id(solution_id)
  +is_life_cycle_valid(state, cursor=None, solution_id=None)
  +invoke_update_entity(event, body, path_parameters)
}
class DateTimeEncoder {
  +default(o)
}
class Decorators {
  +provide_entity_db_read_cursor(f)
  +provide_entity_db_write_cursor(f)
  +provide_db_read_cursor(f)
}
class fv_secrets_Secrets {
  +get_secret(name)
}
class fv_db {
  +get_connection(...)
  +get_cursor(...)
}
WatcherModule --> DateTimeEncoder : uses
WatcherModule --> Decorators : provides
WatcherModule --> fv_secrets_Secrets : instantiates
WatcherModule --> fv_db : uses
```

> SVG rendering failed for this diagram.

## Diagram 2

```mermaid
sequenceDiagram
participant Caller as "Caller"
participant Watcher as "watcher module"
participant AWS as "fv.aws.lambdas"
Caller->>Watcher: invoke_set_exception(event, entity_external_id, solution_id, org_id, fv_event, exception_to_clear, invoke_type)
Watcher->>Watcher: deepcopy event; set body and requestContext
alt exception_to_clear present
  Watcher->>AWS: invoke_lambda(name="update_exception", headers, full_payload, invoke_type)
else no exception_to_clear
  Watcher->>AWS: invoke_lambda(name="set_exception", headers, full_payload, invoke_type)
end
AWS-->>Watcher: res
opt invoke_type == "RequestResponse"
  Watcher-->>Caller: return res
end
```

> SVG rendering failed for this diagram.
