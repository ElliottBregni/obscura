# Diagram: entity_core/watcher_service/watcher_service/entity_set_dwell_watcher.py


> Auto-generated by Obscura crawlers

## Diagram 1

```mermaid
flowchart LR
    subgraph Module
        CR[configure_rollbar()] --> DB[DB_CONN: FvDatabaseConnector("entity_set_dwell_watcher", SecretNames.ENTITY_DATABASE)]
        DB --> G[get_entities_need_dwell(cursor)]
        G -->|executes SELECT JOINs on entity/solution/feature/location and checks entity_dwell_watch| DB
        G --> Res[returns cursor.fetchall()]
        S[set_dwell_on_entity(entity)] --> GA[get_sample_admin_event("POST", [entity.solution_id], entity.organizations_id)]
        GA --> Prep[prepare admin_event.body and admin_event.pathParameters]
        Prep --> PathSet[set solution_id, entity_id, dwellStartTs, locationCode]
        PathSet --> Invoke[invoke_lambda("start_entity_dwell", full_payload=admin_event)]
        L[lambda_handler(event, context)] -->|decorated by| DD[fv.datadog.wrap_lambda_handler]
        L -->|decorated by| RB[rollbar.lambda_function]
        L -.-> G
        L -.-> S
        CR -.-> L
    end
```

> SVG rendering failed for this diagram.

## Diagram 2

```mermaid
classDiagram
    class Module {
      +DB_CONN : FvDatabaseConnector
      +get_entities_need_dwell(cursor)
      +set_dwell_on_entity(entity)
      +lambda_handler(event, context)
    }
    class "fv.aws.lambdas" {
      +get_sample_admin_event(method, solutions, org_id)
      +invoke_lambda(name, full_payload)
    }
    class "fv.db" {
      +FvDatabaseConnector(db_name, secret_name)
      +cursor()
    }
    class rollbar {
      +lambda_function(decorated_fn)
    }
    class "fv.datadog" {
      +wrap_lambda_handler(fn)
    }
    Module --> "fv.aws.lambdas" : uses
    Module --> "fv.db" : creates/uses
    Module --> rollbar : decorated_by
    Module --> "fv.datadog" : decorated_by
```

> SVG rendering failed for this diagram.
