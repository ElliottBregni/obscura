# Diagram: entity_core/watcher_service/watcher_service/db/system_configuration.py


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
flowchart TD
  A[get_system_configuration_setting(cursor, qualifier, solution_id, quick_cache, join_feature_with_solution, org_type)]
  A -->|quick_cache = true| B[get_sys_config_quick_cache(...)<br/>@cached TTLCache(maxsize=128, ttl=60)]
  A -->|quick_cache = false| C[get_sys_config_slow_cache(...)<br/>@cached TTLCache(maxsize=128, ttl=3600)]
  E[sys_config_cache_key(cursor, solution_id, qualifier, join_feature_with_solution, org_type)] -.->|key=| B
  E -.->|key=| C
  B --> D[get_sys_config(cursor, solution_id, qualifier, join_feature_with_solution, org_type)]
  C --> D
  subgraph SQL_Construct
    direction TB
    F1[feature_clause = "" or EXISTS JOIN feature→solution]
    F2[org_type_filter = "AND org_type='...'" if org_type else ""]
    F3[query = cursor.mogrify(SELECT json_build_object(...), data)]
  end
  D --> SQL_Construct
  F3 --> G[cursor.execute(query)]
  G --> H[row = cursor.fetchone()]
  H -->|row present| I[return row.config]
  H -->|no row| J[return {}]
```

> SVG rendering failed for this diagram.
