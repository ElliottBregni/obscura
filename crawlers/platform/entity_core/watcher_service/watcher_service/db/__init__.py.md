# Diagram: entity_core/watcher_service/watcher_service/db/__init__.py


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
graph LR
  secrets[Secrets: fv.secrets.Secrets()]
  logger[logger (logging.Logger)]
  DB[(Database)]
  decor_log[/@log_elapsed_time/]
  decor_cursor[/@provide_entity_db_read_cursor/]

  remove[remove_expired_watched_entities()]
  getOrgMap[get_organization_mapping()]
  getAllOrg[get_all_organizations()]
  getShipper[get_shipper_location_name(location_code, organization_id)]
  getMulti[get_entities_multiple_holds(is_clear=False)]
  getDwell[get_entities_excessive_dwell()]
  lifeRead[__life_cycle_watcher_read(sql, cursor=None, many=True)]
  getPolicies[get_entity_policies()]
  setLast8[set_last8_entities(solution_id, limit, range)]

  secrets -->|instantiated| secrets
  remove -->|executes SQL| DB
  remove -->|logs| logger

  getOrgMap -->|calls| getAllOrg
  getAllOrg -->|executes SQL| DB

  getShipper -->|executes SQL with params| DB

  decor_log -->|wraps| getMulti
  getMulti -->|executes SQL| DB

  getDwell -->|executes SQL| DB

  decor_cursor -->|wraps| lifeRead
  lifeRead -->|mogrify & execute| DB
  getPolicies -->|calls| lifeRead

  setLast8 -->|executes parametrized update| DB
  setLast8 -->|logs via logging| logger
```

> SVG rendering failed for this diagram.
