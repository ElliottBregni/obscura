# Diagram: entity_core/watcher_service/watcher_service/db/shippability.py


> Auto-generated by Obscura crawlers

## Diagram 1

```mermaid
flowchart TD
  subgraph update_entity_shippability_on_hold
    A1([Start]) --> A2{Inputs}
    A2 -->|cursor, solution_id, external_id| A3[Create data dict: {"solution_id": solution_id, "external_id": external_id}]
    A3 --> A4[Define SQL: INSERT INTO entity_shippability ... SELECT EXISTS(... JOIN hold h JOIN entity e ... h."status" = 'ACTIVE')]
    A4 --> A5[query = cursor.mogrify(sql, data)]
    A5 --> A6[logging.info(query.decode())]
    A6 --> A7[cursor.execute(query)]
    A7 --> A8[result = cursor.fetchone()[0]]
    A8 --> A9([Return on_hold])
  end
```

> SVG rendering failed for this diagram.

## Diagram 2

```mermaid
flowchart TD
  subgraph update_entity_shippability_non_shippable_exception
    B1([Start]) --> B2{Inputs}
    B2 -->|cursor, solution_id, external_id, non_shippable_exception_list| B3[Create data dict with non_shippable_codes = tuple(non_shippable_exception_list)]
    B3 --> B4[Define SQL: INSERT INTO entity_shippability ... SELECT EXISTS(... JOIN exception ex JOIN entity e ... ex.status = 'ACTIVE' AND ex.cleared_event_date IS NULL AND ex.exception_type_id IN (SELECT id FROM exception_type WHERE reason_code IN %(non_shippable_codes)s))]
    B4 --> B5[query = cursor.mogrify(sql, data)]
    B5 --> B6[logging.info(query.decode())]
    B6 --> B7[cursor.execute(query)]
    B7 --> B8[result = cursor.fetchone()[0]]
    B8 --> B9([Return non_shippable_exception])
  end
```

> SVG rendering failed for this diagram.
