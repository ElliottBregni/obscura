# Diagram: entity_core/watcher_service/watcher_service/db/dwell.py


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
flowchart TD
    Start([Start]) --> Decorator[/provide_entity_db_write_cursor/]
    Decorator --> Function[/detect_entity_dwell_exceptions(cursor=None)/]
    Function --> BuildSQL["Construct SQL: WITH updated_dwell_cte → UPDATE entity_dwell_watch → RETURNING"]
    BuildSQL --> Execute["cursor.execute(sql)"]
    Execute --> DB[(Postgres DB)]
    DB --> Fetch["rows = cursor.fetchall()"]
    Fetch --> Return["return rows"]
    Return --> End([End])
```

> SVG rendering failed for this diagram.
