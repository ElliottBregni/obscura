# Diagram: entity_core/watcher_service/watcher_service/queue_consumer/update_actualtripleg_eta.py


> Auto-generated by Obscura crawlers

## Diagram 1

```mermaid
flowchart TD
    Start([Start]) --> Parse[Parse Records -> parse_message -> build shipments dict]
    Parse --> DBConn[DB_CONN.establish_connection()]
    DBConn --> Cursor[Get shipment_cursor]
    Cursor --> Map[map_trilegs_from_shipment_ids(cursor, shipments)]
    Map --> Triplegs{Triplegs to update}
    Triplegs --> ForEach[For each tripleg]
    ForEach --> Extract[extract ext_trip_leg_id, org_id, eta]
    Extract --> GetSol[invokinator_iam.invoke_get_solution_by_organization_id_and_feature(org_id,"Finished Vehicle")]
    GetSol --> Solutions{solutions or {}}
    Solutions --> Prepare[prepare_invoke_event(org_id, solutions)]
    Prepare --> UpdateCall[update_tripleg_eta(event_copy, ext_trip_leg_id, solutions.solution_id, eta)]
    UpdateCall --> CheckStatus{status_code}
    CheckStatus -->|200 OK| Updated[triplegs_updated.append(ext_trip_leg_id)]
    CheckStatus -->|404 Not Found| NotFound[triplegs_not_found.append(ext_trip_leg_id)]
    CheckStatus -->|other| Failed[triplegs_failed.append(ext_trip_leg_id) & logging.error]
    Updated --> Aggregate[Collect results]
    NotFound --> Aggregate
    Failed --> Aggregate
    Aggregate --> Response[fv.aws.lambdas.make_response({updated, notfound, failed})]
    Response --> End([End])
```

> SVG rendering failed for this diagram.

## Diagram 2

```mermaid
sequenceDiagram
    participant Event as Event
    participant Lambda as lambda_handler
    participant DB as DB_CONN
    participant Cursor as shipment_cursor
    participant Inv as InvokinatorIAM
    participant AWS as fv.aws.lambdas
    participant Update as update_actual_trip_leg
    Event->>Lambda: event with Records
    Lambda->>Lambda: parse_message(record) for each record -> build shipments dict
    Lambda->>DB: DB_CONN.establish_connection()
    DB->>Cursor: provide cursor
    Lambda->>Cursor: map_trilegs_from_shipment_ids(cursor, shipments)
    Cursor-->>Lambda: triplegs_to_update
    loop for each tripleg
        Lambda->>Inv: invoke_get_solution_by_organization_id_and_feature(org_id,"Finished Vehicle")
        Inv-->>Lambda: solutions (or {})
        Lambda->>AWS: get_sample_admin_event -> prepare_invoke_event
        Lambda->>Update: invoke_lambda("update_actual_trip_leg", full_payload=event_copy)
        Update-->>Lambda: response (statusCode)
        alt statusCode == 200
            Lambda->>Lambda: add ext_trip_leg_id to triplegs_updated
        else statusCode == 404
            Lambda->>Lambda: add ext_trip_leg_id to triplegs_not_found
        else
            Lambda->>Lambda: add ext_trip_leg_id to triplegs_failed; logging.error
        end
    end
    Lambda->>AWS: make_response({updated, notfound, failed})
    AWS-->>Event: final response
```

> SVG rendering failed for this diagram.
