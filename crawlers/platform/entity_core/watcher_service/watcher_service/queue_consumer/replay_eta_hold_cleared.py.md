# Diagram: entity_core/watcher_service/watcher_service/queue_consumer/replay_eta_hold_cleared.py


> Auto-generated by Obscura crawlers

## Diagram 1

```mermaid
flowchart TD
Start([Start]) --> CheckExtra{snsMessageExtraData present?}
CheckExtra -- No --> LogError1[Log error: "Event missing the 'snsMessageExtraData' field" & raise BadRequestError]
CheckExtra -- Yes --> Extract[Extract externalId, startingDate, solution_id]
Extract --> CheckFields{externalId AND startingDate present?}
CheckFields -- No --> LogError2[Log error: "snsMessageExtraData missing 'externalId' or 'startingDate'." & raise BadRequestError]
CheckFields -- Yes --> TryGetLastStatus[Call get_last_status_update(DB_CONN.cursor, solution_id, entity_external_id, starting_date)]
TryGetLastStatus -- exception --> LogError3[Log error and re-raise exception]
TryGetLastStatus -- no_result --> WarnNoLast[Log warning: "No last status update found" & return make_response({})]
TryGetLastStatus -- has_result --> InvokeUpdate[Call invoke_eta_milestone_update(event, solution_id, entity_external_id, last_status_update)]
InvokeUpdate -- exception --> LogError4[Log error and re-raise exception]
InvokeUpdate --> ReturnResp[Return make_response({})]
```

> SVG rendering failed for this diagram.

## Diagram 2

```mermaid
sequenceDiagram
participant SQS as SQS_Event
participant LH as lambda_handler
participant P as process
participant RU as replay_eta_utils
participant DB as DB_CONN.cursor
participant AWS as fv.aws.lambdas
participant L as logger

SQS->>LH: for each record parse body (json.loads if string)
LH->>P: process(parsed_event, context)
alt missing snsMessageExtraData
P->>L: logger.error("Event missing the 'snsMessageExtraData' field.")
P-->>LH: raise fv.error.BadRequestError
else has snsMessageExtraData
P->>P: extract externalId, startingDate, solution_id
alt missing fields
P->>L: logger.error("snsMessageExtraData missing 'externalId' or 'startingDate'.")
P-->>LH: raise fv.error.BadRequestError
else fields present
P->>RU: get_last_status_update(DB, solution_id, entity_external_id, starting_date)
RU-->>P: last_status_update or exception
alt exception retrieving last status
P->>L: logger.error("Error retrieving the last status update.")
P-->>LH: raise
else no last_status_update
P->>L: logger.warning("No last status update found.")
P->>AWS: make_response({})
else has last_status_update
P->>RU: invoke_eta_milestone_update(event, solution_id, entity_external_id, last_status_update)
RU-->>P: success or exception
alt exception invoking update
P->>L: logger.error("Error invoking eta_milestone_update.")
P-->>LH: raise
else success
P->>AWS: make_response({})
end
end
end
```

> SVG rendering failed for this diagram.
