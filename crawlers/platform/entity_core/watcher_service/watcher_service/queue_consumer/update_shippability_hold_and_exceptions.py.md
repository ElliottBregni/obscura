# Diagram: entity_core/watcher_service/watcher_service/queue_consumer/update_shippability_hold_and_exceptions.py


> Auto-generated by Obscura crawlers

## Diagram 1

```mermaid
sequenceDiagram
participant Event as EventRecords
participant Lambda as lambda_handler
participant Process as process
participant DB as FvDatabaseConnector
participant Config as get_system_configuration
participant Shipper as watcher_service_shippability
participant AWS as fv.aws.lambdas
Event->>Lambda: invoke with event (Records)
Lambda->>Process: for each record -> process(message, context)
Process->>Process: parse body and Message, extract snsMessageExtraData
alt missing snsMessageExtraData
Process-->>AWS: make_response("Missing snsMessageExtraData", 400)
else snsMessageExtraData present
Process->>Process: extract entity_external_id, solution_id, eventCode
alt solution_id not in SHIPPABILITY_ENABLED_SOLUTIONS
Process-->>AWS: make_response("Shippability not enabled for shipper {solution_id}")
else solution enabled
Process->>DB: DB_CONN.establish_connection()
DB-->>Process: cursor available
alt holdId present
Process->>Shipper: update_entity_shippability_on_hold(cursor, solution_id, entity_external_id)
Shipper-->>Process: on_hold_result
end
alt exceptionType present
Process->>Config: invoke_lambda("get_system_configuration", pathParameters, queryStringParameters)
Config-->>Process: response
alt response contains non_shippable_rules and exceptionType in rules
Process->>Shipper: update_entity_shippability_non_shippable_exception(cursor, solution_id, entity_external_id, non_shippable_exceptions)
Shipper-->>Process: non_shippable_exception_result
else not a non-shippable exception
Process->>Process: no action
end
end
Process-->>AWS: make_response(msg)
end
```

> SVG rendering failed for this diagram.

## Diagram 2

```mermaid
classDiagram
class Module {
  +DB_CONN: FvDatabaseConnector
  +SHIPPABILITY_ENABLED_SOLUTIONS: tuple
  +lambda_handler(event, context)
  +process(event, context, audit_refs={})
  +get_nonshippable_exceptions_config(cursor, solution_id)
}
class FvDatabaseConnector {
  +establish_connection()
  +cursor
}
class FvAwsLambdas {
  +make_response(body, statusCode?)
  +mandatory_lambda_handling(fail_gracefully=False)
  +invoke_lambda(name, pathParameters, queryStringParameters)
}
class WatcherServiceShippability {
  +update_entity_shippability_on_hold(cursor, solution_id, entity_external_id)
  +update_entity_shippability_non_shippable_exception(cursor, solution_id, entity_external_id, non_shippable_exceptions)
}
class SystemConfigQualifier {
  <<enumeration>>
  +NON_SHIPPABLE_RULES
}
Module --> FvDatabaseConnector : uses
Module --> FvAwsLambdas : uses
Module --> WatcherServiceShippability : calls
Module --> SystemConfigQualifier : references
```

> SVG rendering failed for this diagram.
