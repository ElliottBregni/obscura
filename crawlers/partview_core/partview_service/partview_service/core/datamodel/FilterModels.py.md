# Diagram: partview_service/partview_service/core/datamodel/FilterModels.py


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
classDiagram
    class AutocompleteKind {
        <<enumeration>>
        <<external enum>>
    }
    class AutocompleteValueSource {
        <<enumeration>>
        ORIGINAL_TERMS
    }
    class AutocompleteSpec {
        <<dataclass>>
        +kind : AutocompleteKind
        +value_source : AutocompleteValueSource = ORIGINAL_TERMS
        +lc_field : str = ""
        +original_field : str = ""
        +nested_path : str | None = None
        +suggest_field : str = ""
    }
    class DropdownSpec {
        <<dataclass>>
        +agg_name : str
        +key_fields_lc : list<tuple[str,str]>
        +display_fields : list<tuple[str,str]>
        +required_exists : list[str]
        +value_key : str
        +label_template : str
        +search_fields_contains : list[str]
        +nested_path : str | None = None
    }
    class FilterItem {
        <<dataclass>>
        +id : str
        +label : str
        +value : str
    }
    class FilterMeta {
        <<dataclass>>
        +totalCount : int = 0
        +currentPage : int = 0
        +totalPages : int = 0
    }
    class DropdownMeta {
        <<dataclass>>
        +searchAfter : dict[str,Any] | None = None
    }
    class FilterResponse {
        <<dataclass>>
        +data : list[FilterItem]
        +meta : FilterMeta | DropdownMeta | None = None
    }

    AutocompleteSpec --> AutocompleteKind : kind
    AutocompleteSpec --> AutocompleteValueSource : value_source
    FilterResponse o-- "0..*" FilterItem : data
    FilterResponse o-- "0..1" FilterMeta : meta
    FilterResponse o-- "0..1" DropdownMeta : meta
    DropdownSpec ..> "tuple" : key_fields_lc / display_fields
    DropdownSpec ..> "list" : required_exists / search_fields_contains
    DropdownMeta ..> "dict" : searchAfter
```

> SVG rendering failed for this diagram.
