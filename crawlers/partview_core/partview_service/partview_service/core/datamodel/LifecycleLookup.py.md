# Diagram: partview_service/partview_service/core/datamodel/LifecycleLookup.py


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
classDiagram
    class Persistable {
        +id: str
        +ts: datetime
        +modified: datetime
        +add_dirty_field(field, value)
    }
    class StringEnum {
    }
    enum LifeCycleLookupPropertiesS {
        SOLUTION_ID: "solution_id"
        NAME: "name"
        TYPE: "type"
    }
    class LifecycleLookup {
        -__solution_id: str
        -__name: str
        -__type: str
        +table_name: str = "lifecycle_lookup"
        +solution_id(): str
        +name(): str
        +type(): str
        +solution_id=(solution_id): LifecycleLookup
        +name=(name): LifecycleLookup
        +type=(type): LifecycleLookup
        +is_valid(): bool
    }
    Persistable <|-- LifecycleLookup
    StringEnum <|-- LifeCycleLookupPropertiesS
    LifecycleLookup ..> Persistable : calls add_dirty_field
```

> SVG rendering failed for this diagram.
