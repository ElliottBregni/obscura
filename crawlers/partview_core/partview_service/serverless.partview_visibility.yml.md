# Diagram: partview_service/serverless.partview_visibility.yml


> Auto-generated by Obscura crawlers

## Mermaid

```mermaid
flowchart LR
  subgraph AWS [AWS Provider]
    AWS[AWS (python3.13, arm64)]
    VPC((VPC))
    Layers((Layers: fv_core_layers))
    IAM((IAM Role & Policies))
    Env{{Environment Variables}}
    Resources((CloudFormation Resources))
  end

  APIGW[(API Gateway / HTTP Events)]
  SQSQueue[(SQS Queue: ${stage}-PV-dealer-onboard.fifo)]
  S3[(S3: ${opt:stage}-fv-csv-export/*)]
  Secrets[(SecretsManager: DD_API_KEY_SECRET_ARN)]
  SNS[(SNS)]
  CloudWatchDeny[(Deny: logs:CreateLogGroup/CreateLogStream/PutLogEvents)]

  AWS --> Layers
  AWS --> VPC
  AWS --> IAM
  AWS --> Env
  AWS --> Resources

  APIGW --> F1[/"trigger-post-visibility-grant-handler\nPOST /api/visibility-grants/data"/]
  APIGW --> F2[/"visibility-grant-handler\nGET/POST/PUT/PATCH/DELETE /api/visibility-grant"/]
  APIGW --> F3[/"partview-dealer-onboarding-handler\nPOST /api/partview-dealer-onboarding-upload"/]
  SQSQueue --> F4[/"dealer_onboard_batch_upload_receiver\nSQS consumer (batchSize:10,maxConcurrency:10)"/]

  Layers --> F1
  Layers --> F2
  Layers --> F3
  Layers --> F4

  Env --> F1
  Env --> F2
  Env --> F3
  Env --> F4

  F1 -->|Invoke & Secrets| IAM
  F2 -->|Invoke & Query/Path params| IAM
  F3 -->|Invoke & CORS| IAM
  F4 -->|Invoke (reservedConcurrency=10, timeout=900)| IAM

  IAM --> S3
  IAM --> Secrets
  IAM --> SNS
  IAM --> SQSQueue
  IAM -.-> CloudWatchDeny

  Resources --> APIGW
  Resources --> SQSQueue
  Resources --> S3
  Resources --> Secrets
  Resources --> SNS
```

> SVG rendering failed for this diagram.
